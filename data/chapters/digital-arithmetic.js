// Textbook chapter: Computer Arithmetic (Booth's algorithm, division, IEEE 754 precision).
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['digital-arithmetic'] = {
  figs: [
    {
      id: 'booth-shift-add',
      caption: 'Booth\'s algorithm scans the multiplier two bits at a time (current bit, previous bit): a 1→0 transition (10) subtracts the multiplicand, a 0→1 transition (01) adds it, and a run of equal bits (00 or 11) does nothing — followed by an arithmetic right shift after every step.',
      svg: '<svg viewBox="0 0 320 130" width="100%" style="max-width:320px;height:auto" xmlns="http://www.w3.org/2000/svg"><g font-size="11" fill="currentColor" font-family="monospace"><text x="10" y="25">bit pair 10  →  subtract M, then shift</text><text x="10" y="55">bit pair 01  →  add M, then shift</text><text x="10" y="85">bit pair 00  →  no op, then shift</text><text x="10" y="115">bit pair 11  →  no op, then shift</text></g></svg>'
    },
    {
      id: 'ieee-precision-vs-range',
      caption: 'Machine epsilon (precision near 1.0) and the smallest/largest representable magnitude (range) answer two different questions — epsilon depends only on the mantissa width, range depends on the exponent width.',
      svg: '<svg viewBox="0 0 320 110" width="100%" style="max-width:320px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><line x1="20" y1="60" x2="300" y2="60"/><line x1="150" y1="45" x2="150" y2="75"/><line x1="20" y1="50" x2="20" y2="70"/><line x1="300" y1="50" x2="300" y2="70"/></g><g font-size="10" fill="currentColor" text-anchor="middle"><text x="20" y="90">smallest (exponent field)</text><text x="150" y="35">1.0 ± epsilon (mantissa field)</text><text x="300" y="90">largest (exponent field)</text></g></svg>'
    }
  ],
  text: `
WHY THIS CHAPTER IS SEPARATE FROM NUMBER REPRESENTATION

The previous chapter covered how numbers are REPRESENTED — sign-magnitude, 1's/2's complement, BCD, IEEE 754's bit layout. This chapter covers how ARITHMETIC is actually PERFORMED on those representations at the algorithm level: how signed multiplication is done efficiently (Booth's algorithm), how division is done digit by digit in hardware (restoring and non-restoring division), and how IEEE 754's finite bit budget translates into concrete, calculable limits on PRECISION and RANGE — two genuinely different properties that are frequently, and wrongly, treated as interchangeable. Every algorithm here is best understood as a sequence of SHIFT and ADD/SUBTRACT operations, since that reduction to a tiny instruction set is exactly what makes each one implementable directly in hardware with a small, fixed set of building blocks.

2'S COMPLEMENT ADDITION AND SUBTRACTION: ONE ADDER, NO SPECIAL CASES

2's complement subtraction A−B is computed as A + (2's complement of B) = A + B′ + 1, where B′ is the bitwise complement of B — this reduces subtraction to ADDITION plus a simple bit-inversion, with NO special-casing needed for the signs of A or B at all. This is precisely the hardware payoff already flagged in the previous chapter: the identical adder circuit used for ordinary unsigned addition performs signed subtraction correctly, merely by feeding it B′ instead of B and setting the initial carry-in to 1 (supplying the "+1" of the invert-then-add-1 negation directly as that carry-in, rather than as a separate addition step).

OVERFLOW RULE, RESTATED PRECISELY: overflow in addition occurs if and only if the CARRY INTO the sign bit position DIFFERS from the CARRY OUT of the sign bit position — equivalently, adding two same-signed operands and obtaining a DIFFERENTLY-signed result signals overflow, and adding opposite-signed operands can never overflow (already derived in the previous chapter, restated here because every algorithm in this chapter depends on correctly recognising when an intermediate addition or subtraction step has overflowed).

BOOTH'S ALGORITHM: SIGNED MULTIPLICATION VIA BIT-PAIR EXAMINATION

Ordinary "add a shifted copy of the multiplicand for every 1-bit of the multiplier" multiplication (the shift-and-add method from the previous chapter) works correctly for UNSIGNED numbers, but handling SIGNED (2's complement) operands directly this way is awkward, and the method also performs no better when the multiplier has long runs of 1s than when it has none. BOOTH'S ALGORITHM solves both problems: it correctly handles signed 2's-complement multiplication directly, and it can significantly REDUCE the number of add/subtract operations needed for a multiplier containing long runs of consecutive 1s or 0s.

[[FIG:booth-shift-add]]

THE ALGORITHM, examining the multiplier TWO BITS AT A TIME (the current bit, and the bit immediately to its right — conceptually, an extra 0 is appended to the right of the multiplier's own LSB before starting, to give the very first pair a "previous bit" to compare against):

1. If the current pair reads 10 (a TRANSITION from 1 down to 0, moving right to left through the multiplier) — SUBTRACT the multiplicand from the running partial-product accumulator.
2. If the current pair reads 01 (a TRANSITION from 0 up to 1) — ADD the multiplicand to the accumulator.
3. If the current pair reads 00 or 11 (NO transition, i.e. inside a run of equal bits) — do NOTHING to the accumulator this step.
4. After EVERY step (regardless of whether an add, subtract, or no-op just occurred), perform an ARITHMETIC RIGHT SHIFT of the entire accumulator-plus-multiplier register (an arithmetic shift preserves the sign bit, copying it into the newly vacated most-significant position, exactly as needed to keep partial results correctly signed throughout the process).
5. Repeat for as many steps as there are multiplier bits, then read the final accumulator (plus the shifted-in multiplier bits) as the full-width product.

1. WHY the 10/01 transition rule correctly handles signed multiplication: a RUN of consecutive 1s in the multiplier, say from bit position j down to bit position i (inclusive), represents the value 2^(j+1) − 2^i (a standard geometric-series identity: a block of k consecutive 1-bits, as an unsigned value, equals 2^(top+1) − 2^bottom). Booth's algorithm recognises the START of this run (the 01 transition, moving from a 0 just below position i up into the run) as needing a SUBTRACTION of 2^i × multiplicand, and the END of the run (the 10 transition, moving from position j down into a 0) as needing an ADDITION of 2^(j+1) × multiplicand — together contributing exactly (2^(j+1) − 2^i) × multiplicand, matching the run's true value precisely, using only TWO operations (one add, one subtract) regardless of how LONG the run of 1s actually is, rather than one addition per individual 1-bit as the naive method would require.

Worked trace: multiply 3 × (−4) using 4-bit 2's-complement Booth's algorithm. Multiplicand M = 0011 (3). Multiplier Q = 1100 (−4 in 4-bit 2's complement), with an appended bit Q₋₁=0 to its right to seed the first comparison. Accumulator A starts at 0000.

1. Step 1: current pair (Q₀,Q₋₁) = (0,0) → no operation. Arithmetic-shift-right the combined A,Q,Q₋₁ register (24 bits total, shifting right by 1 and copying the sign bit into the new MSB): A=0000, Q=0110, Q₋₁=0.
2. Step 2: current pair (Q₀,Q₋₁) = (0,0) → no operation. Shift right: A=0000, Q=0011, Q₋₁=0.
3. Step 3: current pair (Q₀,Q₋₁) = (1,0) → this is a 0-to-1 transition (reading Q₋₁ then Q₀, i.e. moving from 0 up to 1) → SUBTRACT M: A = 0000−0011 = 1101 (in 4-bit 2's complement). Shift right (arithmetic, preserving sign): combined register 1101 1001 1 shifts to 1110 1100 1, giving A=1110, Q=1001, Q₋₁=1.
4. Step 4 (final step): current pair (Q₀,Q₋₁) = (1,1) → no operation (no transition, inside the run of 1s). Shift right: combined register 1110 1001 1 shifts to 1111 0100 1, giving A=1111, Q=0100.
5. Final product, reading the combined A,Q register (8 bits total): 1111 0100. Interpreting this 8-bit result as signed 2's complement: the sign bit is 1, so invert and add 1 to find its magnitude — 00001011+1=00001100=12, giving the value −12. This matches 3×(−4)=−12 exactly, confirming the algorithm's result.

GATE TRAP: it is tempting to assume Booth's algorithm ALWAYS reduces the number of add/subtract operations compared to the naive method — but for a multiplier with ALTERNATING bits (0101...0101), EVERY single bit-pair is a transition (either 01 or 10), meaning Booth's algorithm performs an add or subtract at literally EVERY step, no better than (and no worse than) the naive one-operation-per-bit method; Booth's algorithm's efficiency gain is specifically tied to the PRESENCE of long runs of consecutive equal bits, and it offers NO advantage for a multiplier that lacks such runs.

RADIX-4 (MODIFIED) BOOTH RECODING

Ordinary (radix-2) Booth's algorithm, as described above, still requires one pass per bit of the multiplier, even though several of those passes may be no-ops. RADIX-4 (or MODIFIED) BOOTH RECODING processes the multiplier THREE BITS AT A TIME (with a 1-bit overlap between successive groups) instead of two, HALVING the total number of steps needed — a genuine, guaranteed speed-up (unlike ordinary Booth's algorithm, whose gain depended on the specific bit pattern encountered).

1. Each group of 3 bits (the current 2-bit chunk plus 1 bit of overlap carried from the previous group) is decoded into ONE of five possible actions on the accumulator: add 0 (no-op), add or subtract the multiplicand once (×1), or add or subtract TWICE the multiplicand (×2, implemented as a simple 1-bit left shift of the multiplicand before adding — cheap in hardware, since shifting is far less costly than a true multiplication).
2. Because each group covers 2 NEW bits of the multiplier per step (the third bit is always the overlap carried in from the previous group, re-examined rather than newly consumed), an n-bit multiplier needs only n/2 total steps, compared to ordinary Booth's algorithm's n steps — this factor-of-2 reduction in the number of steps is GUARANTEED regardless of the specific bit pattern, unlike ordinary Booth's algorithm's pattern-dependent gain.
3. The five possible per-group actions (0, ±1×M, ±2×M) are exactly why this technique is sometimes described as re-expressing the multiplier in a signed-digit representation using digits from {−2,−1,0,1,2} instead of the ordinary binary digits {0,1} — a genuinely different digit SET (allowing both positive and negative digit values, and a magnitude-2 digit) achieving the same overall numeric value in HALF as many digit positions.

KEY: ordinary radix-2 Booth's algorithm and radix-4 modified Booth recoding are not two unrelated named techniques — radix-4 recoding is the SAME underlying idea (examine a window of multiplier bits and decide an add/subtract/no-op action based on the transitions found) generalised to a wider examination window, trading a slightly more complex per-step decision table (5 possible actions instead of 3) for a GUARANTEED halving of the total number of steps, rather than the pattern-dependent gain ordinary Booth's algorithm offers; recognising this relationship means the "why does examining more bits at once help" question always has the same one-line answer — a wider window captures more of a long run of equal bits within a single decision, at the cost of needing to handle more possible window patterns.

RESTORING AND NON-RESTORING DIVISION

Long division, performed digit by digit in binary hardware, needs a rule for handling the case where a trial subtraction of the divisor produces a negative partial remainder (meaning the divisor did not actually "fit" at that step) — RESTORING and NON-RESTORING division are the two standard hardware strategies for handling this.

RESTORING DIVISION: at each step, SUBTRACT the (appropriately shifted) divisor from the current partial remainder. If the result is negative, "RESTORE" the previous partial remainder by ADDING the divisor BACK before continuing to the next step (recording a quotient bit of 0 for this step); if the result is non-negative, keep it as the new partial remainder (recording a quotient bit of 1).

1. The "restore" step is a genuinely WASTED operation whenever it is needed — an extra addition performed purely to undo the just-completed subtraction, contributing nothing toward the final answer beyond correctly resetting the remainder for the next step.

NON-RESTORING DIVISION avoids ever performing this wasted restore step: instead of restoring immediately after a negative result, it lets the negative partial remainder STAND, and uses its SIGN to decide the next step's operation directly — if the current partial remainder is negative, the next step ADDS the (shifted) divisor instead of subtracting it; if the current partial remainder is NON-negative, the next step subtracts as usual. A quotient bit of 1 is recorded whenever the step's remainder comes out non-negative, and 0 whenever it comes out negative.

1. WHY this avoids restoring while still reaching the correct final result: adding the divisor back on the FOLLOWING step (rather than immediately) accomplishes the SAME net correction as an immediate restore-then-subtract-again sequence would have — algebraically, "subtract, get negative, restore by adding back, then subtract a HALF-shifted divisor again next step" and "leave it negative, then ADD a half-shifted divisor directly next step" arrive at the identical numeric partial remainder, since the restore-and-resubtract sequence's net effect (add D, then subtract D/2 for the next digit's shifted value) is exactly the same total quantity as directly adding D/2 without ever having restored the full D in between.
2. Non-restoring division therefore performs EXACTLY ONE arithmetic operation (either an add or a subtract, never both) per quotient bit produced, compared to restoring division's occasional TWO operations (a subtract, followed by a wasted restoring add) whenever a step's trial subtraction goes negative — a genuine, consistent efficiency gain, in contrast to Booth's algorithm's gain (which depended on the specific bit pattern of the multiplier and was not guaranteed in every case).

Worked trace (restoring division): divide 1011 (11) by 0011 (3) using 4-bit restoring division, tracking a single combined R:Q register (R the remainder half, Q the dividend/quotient half, R starting at 0000 and Q starting as the dividend 1011). Each step SHIFTS the entire combined register left by one bit (moving Q's most-significant bit into R's least-significant position, and opening up a blank LSB slot in Q for this step's quotient bit), then subtracts the divisor from R.

1. Step 1: shift gives R=0001, Q=0110 (blank LSB slot not yet filled). Subtract divisor: 0001−0011=1110, NEGATIVE (sign bit 1) — RESTORE (R reverts to 0001) and record quotient bit 0: Q=0110.
2. Step 2: shift gives R=0010, Q=1100. Subtract divisor: 0010−0011=1111, NEGATIVE — restore (R reverts to 0010), record quotient bit 0: Q=1100.
3. Step 3: shift gives R=0101, Q=1000. Subtract divisor: 0101−0011=0010, NON-NEGATIVE — keep R=0010, record quotient bit 1: Q=1001.
4. Step 4 (final): shift gives R=0101, Q=0010. Subtract divisor: 0101−0011=0010, NON-NEGATIVE — keep R=0010, record quotient bit 1: Q=0011.
5. Final result: quotient Q=0011 (3), remainder R=0010 (2). Cross-check directly: 3×3+2=11, matching the original dividend exactly.

GATE TRAP: restoring and non-restoring division are taught using a SINGLE combined shift register holding both the remainder and the not-yet-consumed dividend bits, with quotient bits inserted into the vacated LSB positions as the dividend bits shift out — tracking this combined register correctly (rather than accidentally treating the remainder and dividend/quotient halves as if they shifted independently of each other) is the single most common bookkeeping slip when working an example by hand, and cross-checking the FINAL quotient and remainder against direct multiplication (quotient × divisor + remainder = dividend) is a fast, reliable way to catch such an error before committing to a final answer.

Worked trace (non-restoring division): divide the same 1011 (11) by 0011 (3), now using non-restoring division, to see directly how it avoids the two restoring steps above. R=0000, Q=1011.

1. Step 1 (the FIRST step always subtracts, since there is no previous sign to consult yet): shift gives R=0001, Q=0110. Subtract divisor: 0001−0011=1110 — NEGATIVE (sign bit 1). Record quotient bit 0 (Q=0110), and do NOT restore; leave R at this negative value and carry the sign into the next step's decision.
2. Step 2 (previous result was negative, so this step ADDS instead of subtracting): shift gives R=1100, Q=1100. Add divisor: 1100+0011=1111 — still NEGATIVE. Record quotient bit 0: Q=1100.
3. Step 3 (previous was negative, so ADD again): shift gives R=1111, Q=1000. Add divisor: 1111+0011=0010 (the carry out beyond the 4-bit width is discarded, exactly as ordinary 2's-complement addition already permits) — NON-NEGATIVE. Record quotient bit 1: Q=1001.
4. Step 4 (previous was non-negative, so SUBTRACT): shift gives R=0101, Q=0010. Subtract divisor: 0101−0011=0010 — NON-NEGATIVE. Record quotient bit 1: Q=0011.
5. Final result: quotient 0011 (3), remainder 0010 (2) — exactly matching the restoring-division result above, confirming both methods agree, as they always must for the same inputs. Counting operations across the two methods on this same example: restoring division needed 6 total operations (4 subtractions plus 2 wasted restoring additions), while non-restoring division needed exactly 4 (one operation per step, no exceptions) — a direct, concrete confirmation of the operation-count saving described above.

KEY: non-restoring division's defining advantage is structural, not merely arithmetic — it NEVER performs the "subtract, discover negative, then undo with an addition" round trip that restoring division's restore step represents; instead, it lets a negative remainder stand and immediately decides the type of the NEXT operation from its sign, meaning the total operation count for an n-bit non-restoring division is ALWAYS exactly n (one operation per bit, no exceptions), while restoring division's total operation count VARIES between n (if no step ever goes negative) and 2n (if every single step goes negative and needs restoring) depending entirely on the specific dividend and divisor involved.

IEEE 754 PRECISION: MACHINE EPSILON

MACHINE EPSILON is the GAP between 1.0 and the next representable floating-point value greater than 1.0 — it measures PRECISION (how finely spaced representable values are NEAR a given magnitude), which is an entirely different question from RANGE (how large or small a representable magnitude can be at all).

1. For single precision (23 mantissa bits), machine epsilon = 2⁻²³ ≈ 1.19×10⁻⁷, corresponding to roughly 6-7 significant DECIMAL digits of precision.
2. For double precision (52 mantissa bits), machine epsilon = 2⁻⁵² ≈ 2.2×10⁻¹⁶, corresponding to roughly 15-16 significant decimal digits.
3. WHY epsilon depends ONLY on the MANTISSA width, and not on the exponent width at all: near the value 1.0 = 1.000...0 × 2⁰, the very next representable value increments only the LEAST SIGNIFICANT mantissa bit, adding exactly 2⁻(mantissa bits) to the significand — the exponent field plays no role whatsoever in determining this SPACING between adjacent representable values at a fixed magnitude.

[[FIG:ieee-precision-vs-range]]

IEEE 754 RANGE: SMALLEST AND LARGEST REPRESENTABLE VALUES

1. SMALLEST POSITIVE NORMALISED value: 1.0 × 2^(minimum exponent) — for single precision, 1.0×2⁻¹²⁶ ≈ 1.18×10⁻³⁸ (using the minimum NORMAL stored-exponent value of 1, giving true exponent 1−127=−126, exactly the fixed exponent the denormal range also uses, as covered in the previous chapter). For double precision, 1.0×2⁻¹⁰²² ≈ 2.2×10⁻³⁰⁸.
2. SMALLEST POSITIVE DENORMALISED value: pushes the mantissa's smallest possible nonzero bit all the way to the LEAST significant position at the FIXED minimum exponent — for single precision, 2⁻²³×2⁻¹²⁶ = 2⁻¹⁴⁹ ≈ 1.4×10⁻⁴⁵.
3. LARGEST FINITE value: uses the MAXIMUM normal stored exponent together with an all-1s mantissa (the largest significand just short of 2.0) — for single precision, (2−2⁻²³)×2¹²⁷ ≈ 3.4×10³⁸. For double precision, (2−2⁻⁵²)×2¹⁰²³ ≈ 1.8×10³⁰⁸.
4. WHY range depends ONLY on the EXPONENT width, and not the mantissa width: the RATIO between the largest and smallest representable magnitudes is dominated entirely by how large a POWER OF 2 the exponent field can express (2^(exponent bits worth of range)) — the mantissa only ever refines a value WITHIN one power-of-2 "octave," contributing a comparatively tiny multiplicative factor (at most just under 2×) next to the exponent's astronomically larger multiplicative range.

GATE TRAP: MACHINE EPSILON (a precision/rounding-granularity question, answered entirely by the MANTISSA width) and the SMALLEST REPRESENTABLE MAGNITUDE (a range question, answered entirely by the EXPONENT width) are frequently confused with one another under time pressure, despite both being expressed as "2 to a negative power" quantities that look superficially similar — 2⁻²³ (single-precision epsilon) and 2⁻¹²⁶ (single-precision smallest normal) answer completely different questions and must never be substituted for one another.

KEY: single vs double precision differ in BOTH dimensions simultaneously but for INDEPENDENT reasons — double precision's larger mantissa (52 vs 23 bits) buys roughly 9 extra significant decimal digits of PRECISION, while its larger exponent field (11 vs 8 bits) buys a vastly larger RANGE (exponents from −1022 to +1023, versus −126 to +127) — recognising these as two separate design knobs, each solving a separate problem (how finely can values be distinguished, versus how large or small a magnitude can be represented at all), rather than a single unified "double precision is just more accurate" idea, is essential for correctly answering a question that asks specifically about ONE of the two properties.

ROUNDING MODES

Since a computed real-arithmetic result frequently falls BETWEEN two adjacent representable floating-point values, IEEE 754 defines several standard ROUNDING MODES governing which representable value is actually stored:

1. ROUND TO NEAREST, TIES TO EVEN (the IEEE 754 DEFAULT): rounds to whichever representable value is CLOSEST to the true result; when the true result falls EXACTLY halfway between two representable values (a genuine tie), rounds to whichever of the two has an EVEN (0) least-significant mantissa bit. This tie-breaking rule exists specifically to eliminate the SYSTEMATIC statistical bias that an "always round the halfway case up" rule would otherwise introduce across a very large number of accumulated operations (rounding every tie up would, over many operations, drift the accumulated result consistently upward; alternating between rounding up and down based on which neighbour is even cancels this drift out on average).
2. ROUND TOWARD ZERO (TRUNCATION): simply discards the extra bits beyond the representable precision, always moving the result's MAGNITUDE toward zero (for a positive number this rounds down; for a negative number this rounds up toward zero) — the simplest rule computationally, but it introduces a small but CONSISTENT bias toward zero across repeated operations.
3. ROUND TOWARD POSITIVE INFINITY (CEILING): always rounds to the smallest representable value that is ≥ the true exact result.
4. ROUND TOWARD NEGATIVE INFINITY (FLOOR): always rounds to the largest representable value that is ≤ the true exact result.
5. The two DIRECTED rounding modes (toward +∞ and toward −∞) are specifically useful in INTERVAL ARITHMETIC, where a computation needs to guarantee a result is CONSERVATIVELY above (or below) the true mathematical value — computing an upper bound with round-toward-+∞ and a lower bound with round-toward-−∞ guarantees the true, exact result is always trapped somewhere between the two computed bounds, a property no other rounding mode alone can guarantee.

GATE TRAP: round-to-nearest-ties-to-even ONLY matters, and only makes a choice at all, in the EXACT tie case — for every OTHER value (the overwhelming majority of all possible results), the plain "round to the nearest representable value" rule alone already determines the outcome uniquely, with no even/odd tie-breaking question ever arising; treating the ties-to-even rule as though it applies to every rounding decision, rather than only the rare exact-halfway case, is a common misunderstanding of what this specific rounding mode actually does differently from an ordinary "round to nearest" rule.

CATASTROPHIC CANCELLATION

CATASTROPHIC CANCELLATION is a specific, easily overlooked way floating-point precision loss manifests: when SUBTRACTING two floating-point values that are CLOSE to each other in magnitude, the RESULT'S significant digits are dominated by whatever rounding error already existed in the two original operands, since the leading digits that were common to both operands cancel out entirely, leaving only the (already imprecise) trailing digits to determine the answer.

1. Concretely: if two floating-point numbers A and B each carry a small representation error of roughly machine-epsilon-times-their-magnitude, and A and B themselves are nearly equal, then A−B is a SMALL number, but the ABSOLUTE size of the error in A and B (which does NOT shrink just because A and B happen to be close to each other) is now a much LARGER fraction of that small result than it was of the original, larger operands — the RELATIVE error has been amplified enormously by the act of subtracting two nearly-equal quantities.
2. Worked illustration (using a deliberately short, simplified 4-significant-digit decimal floating-point format to make the effect visible without needing full IEEE 754 bit-level detail): let x = 1.235 and y = 1.234, each already rounded to 4 significant digits from slightly different true values. Their rounded difference x−y = 1.235−1.234 = 0.001 has only ONE significant digit remaining, despite each of x and y individually carrying a full 4 digits of apparent precision — three-quarters of the original precision has been destroyed by the cancellation, not by any NEW error introduced during the subtraction itself, but by the PRE-EXISTING rounding error in x and y being fully exposed once the common leading digits cancelled away.

GATE TRAP: catastrophic cancellation is NOT caused by the subtraction operation itself being inaccurate — floating-point subtraction, performed correctly, gives the CORRECTLY ROUNDED result of subtracting the two STORED (already-rounded) operand values exactly; the problem is entirely that the STORED operands themselves already carried rounding error from EARLIER computation steps, and subtracting two nearly-equal such values simply reveals how much of the operands' apparent precision was actually already-accumulated error rather than genuine significant digits — a subtlety worth stating precisely, since blaming "the subtraction" for introducing the error, rather than recognising it as EXPOSING pre-existing error, is a common conceptual mix-up.

KEY: the standard practical fix for catastrophic cancellation, whenever an algorithm's mathematical structure allows it, is ALGEBRAIC REFORMULATION to avoid the damaging subtraction entirely — the classic example is the quadratic formula, where computing the smaller-magnitude root via the naive formula (−b+√(b²−4ac))/(2a) can subtract two nearly-equal quantities when b²≫4ac and the sign works out unfavourably, while an algebraically EQUIVALENT reformulation (multiplying numerator and denominator by the conjugate to instead compute that same root as 2c/(−b−√(b²−4ac)), which involves an ADDITION of same-signed terms rather than a near-cancelling subtraction) avoids the problem entirely while computing the mathematically identical exact value — recognising cancellation as a reformulation problem, not merely an "use more precision" problem, is the key practical takeaway.

FIXED-POINT VS FLOATING-POINT ARITHMETIC: A DIRECT TRADE-OFF

FIXED-POINT representation (an implicit, unmoving binary point at some fixed bit position, exactly the representation style covered in the previous chapter before floating-point was introduced) and FLOATING-POINT representation each solve a genuinely different problem, and the choice between them is a real engineering trade-off rather than floating-point being a strictly "better, more modern" upgrade in every respect.

1. FIXED-POINT offers CONSTANT ABSOLUTE precision across its entire representable range — every representable value is spaced exactly the same fixed distance apart from its neighbours, regardless of magnitude — at the cost of a comparatively NARROW total range (bounded directly by the fixed number of integer and fractional bits allocated).
2. FLOATING-POINT offers a vastly WIDER range (as the range analysis above demonstrated) by allowing the spacing between representable values to GROW proportionally with magnitude — but this means its ABSOLUTE precision WORSENS at larger magnitudes (though its RELATIVE precision, the ratio of the spacing to the magnitude itself, stays roughly constant across the whole range, which is exactly what machine epsilon measures).
3. Fixed-point arithmetic is often preferred in resource-constrained embedded systems (simpler hardware, deterministic and often faster arithmetic, no need for exponent alignment before addition) specifically when the required range of values is KNOWN in advance and comfortably fits within a fixed-point format's narrower span — floating-point's extra hardware complexity (exponent alignment logic, normalisation after every operation, rounding-mode handling) is a genuine cost that fixed-point sidesteps entirely whenever its narrower range suffices for the task at hand.

GUARD, ROUND, AND STICKY BITS: HOW ROUNDING IS ACTUALLY COMPUTED

Correctly implementing round-to-nearest (the IEEE 754 default) requires knowing not just the bits that WILL be kept, but enough information about the bits being DISCARDED to decide which way to round — hardware floating-point units track exactly three extra bits beyond the stored mantissa width to make this decision correctly and efficiently, without needing to retain every discarded bit explicitly.

1. THE GUARD BIT is the first bit immediately beyond the retained mantissa precision — it directly indicates whether the discarded portion is at least "half" of one unit in the last retained position.
2. THE ROUND BIT is the SECOND bit beyond the retained precision, refining the guard bit's information one level further.
3. THE STICKY BIT is NOT a single specific bit position — it is the LOGICAL OR of ALL remaining bits beyond the guard and round bits, collapsed into one flag. It answers only the yes/no question "is there ANY nonzero bit anywhere further out," without needing to preserve exactly how many such bits there were or their exact pattern.

1. WHY three bits (and specifically a collapsed OR for everything beyond the second) suffice to round CORRECTLY: to decide between "round up," "round down," or "round to even" (in the exact-tie case), it is enough to know whether the discarded portion is LESS THAN half a unit (round down), EXACTLY half a unit (a genuine tie — guard bit 1, round and sticky bits combination indicating precisely half), or MORE than half a unit (round up) — the guard bit settles whether the discarded portion could be at least half, and the round-plus-sticky combination settles whether it is EXACTLY half or STRICTLY more than half; no information from FURTHER out than the sticky bit's OR-summary is ever actually needed to make this three-way decision correctly.
2. This is a genuine hardware efficiency trick worth recognising as such: computing the sticky bit as a single OR across a potentially very wide range of discarded bits is far cheaper in circuitry than propagating and comparing the FULL discarded bit pattern, and the guard/round/sticky scheme is provably SUFFICIENT (no rounding decision is ever made incorrectly due to insufficient information) despite discarding almost all detail about exactly how far past "half" the true discarded value actually was.

GATE TRAP: assuming the guard and round bits ALONE (without the sticky bit) are sufficient to detect an exact tie is a subtle but real error — a discarded pattern of 100000...0 (guard=1, round=0, all further bits 0, sticky=0) is a genuine exact tie, but a discarded pattern of 10000...01 (guard=1, round=0, but some later bit is 1, so sticky=1) is NOT a tie at all — it represents a value STRICTLY greater than half a unit, and must round up unconditionally rather than invoking the ties-to-even rule; the sticky bit's specific job is distinguishing exactly these two cases, which the guard and round bits alone cannot tell apart.

CARRY-SAVE ADDITION FOR MULTI-OPERAND SUMS

A hardware multiplier built the "naive" way (generate every partial product, then add them all together two at a time using ordinary ripple-carry or carry-lookahead addition) pays a full addition-chain DELAY for every single partial product added — for an n-bit multiplication producing n partial products, this naive approach needs roughly n sequential additions, each with its own carry-propagation delay, stacking up badly for wide multiplicands. CARRY-SAVE ADDITION avoids this by deferring carry propagation until the VERY LAST step of a long chain of additions.

1. A single CARRY-SAVE ADDER (structurally, just an ordinary array of full adders, one per bit position, exactly as in a ripple-carry adder) takes THREE operands of equal width and produces TWO outputs — a SUM vector and a CARRY vector — WITHOUT letting any carry propagate between bit positions at all; each full adder independently produces its own local sum and carry-out bit, and the carry-out bits are collected into a separate "carry vector" output rather than being fed into the next bit position's adder the way an ordinary ripple-carry adder does.
2. This means a carry-save adder's DELAY is just ONE full-adder delay, regardless of the operand width — there is no carry chain to ripple through at all, since carries are simply collected as an output rather than propagated internally.
3. To add MANY operands together (as a multiplier's several partial products require), a chain of carry-save adders can REDUCE them three-at-a-time, at each stage collapsing three operands into two (a sum vector and a carry vector) with only ONE full-adder delay per stage — repeating this reduction across all the partial products, and only at the VERY END feeding the last remaining sum and carry vectors into a single ordinary fast adder (ripple-carry or carry-lookahead) to produce the final, fully carry-propagated result.
4. This dramatically reduces the TOTAL delay for summing many operands: instead of paying a full carry-propagation delay for EVERY pairwise addition in the chain (as naive sequential addition would), only the SINGLE final addition (converting the last sum-and-carry pair into a true binary result) pays a full carry-propagation delay — every earlier stage costs only one full-adder delay each, regardless of operand width.

Worked trace: add three 4-bit operands 0101, 0011, 0110 (5+3+6=14) using one carry-save adder stage followed by one final ordinary addition. Bit-by-bit, each position's full adder takes the corresponding bit of all three operands as its three inputs and produces a local sum bit and carry-out bit, with NO propagation between positions. Position 0 (bits 1,1,0): sum=0, carry=1. Position 1 (bits 0,1,1): sum=0, carry=1. Position 2 (bits 1,0,1): sum=0, carry=1. Position 3 (bits 0,0,0): sum=0, carry=0. Sum vector: 0000. Carry vector (each carry shifted one position left, as is standard for combining with the sum vector): 1110. Final ordinary addition: 0000+1110=1110=14 — matching 5+3+6=14 exactly, confirming the carry-save reduction reproduces the correct total despite never propagating a carry internally during its own single-stage computation.

KEY: the carry-save adder's core insight — DEFER carry propagation until the last possible moment, rather than paying its cost at every intermediate addition step — is a direct efficiency parallel to non-restoring division's core insight (defer the "restore" correction rather than paying its cost immediately at every step where it might be needed): both techniques identify a repeated, expensive correction/propagation operation embedded inside a longer chain of similar steps, and both restructure the computation so that expensive step is paid for ONCE at the very end (or avoided converting to a cheaper form entirely) rather than being repeated needlessly at every intermediate stage.

WORKED PROBLEMS

1. BOOTH'S ALGORITHM TRANSITION IDENTIFICATION. For the multiplier 011100 (appending a seed bit Q₋₁=0 to its right), identify, scanning from the LSB, which bit-pairs trigger an add, a subtract, or no operation. With Q=011100 (bits Q₅Q₄Q₃Q₂Q₁Q₀ = 0,1,1,1,0,0) and Q₋₁=0: pair1=(Q₀,Q₋₁)=(0,0)→no-op. pair2=(Q₁,Q₀)=(0,0)→no-op. pair3=(Q₂,Q₁)=(1,0)→0-to-1 transition, the START of the run→SUBTRACT. pair4=(Q₃,Q₂)=(1,1)→no-op. pair5=(Q₄,Q₃)=(1,1)→no-op. pair6=(Q₅,Q₄)=(0,1)→1-to-0 transition, the END of the run→ADD. Net: one SUBTRACT (at the start of the 111-run, weight 2²=4) and one ADD (at its end, weight 2⁵=32), for a net contribution of 32−4=28 — exactly the run's true value (bits 2-4 set is 4+8+16=28) — using 2 operations instead of 3 separate additions the naive method would need.

2. NO-EFFICIENCY-GAIN CASE. For the alternating multiplier 0101, show that Booth's algorithm gains no efficiency advantage. With Q=0101 (bits Q₃Q₂Q₁Q₀ = 0,1,0,1) and Q₋₁=0, all four bits alternate with the seed (0,1,0,1,0 reading Q₋₁ up through Q₃), so every single pair is a transition: pair1=(Q₀,Q₋₁)=(1,0)→SUBTRACT (weight 2⁰=1). pair2=(Q₁,Q₀)=(0,1)→ADD (weight 2¹=2). pair3=(Q₂,Q₁)=(1,0)→SUBTRACT (weight 2²=4). pair4=(Q₃,Q₂)=(0,1)→ADD (weight 2³=8). Net: −1+2−4+8=5, matching Q's true value of 5 exactly. All 4 of the multiplier's steps trigger an operation — not one fewer, as a run-based saving would require — confirming no reduction at all below one operation per step for this alternating pattern, exactly as the GATE TRAP above describes.

3. MACHINE EPSILON VS SMALLEST NORMAL, DOUBLE PRECISION. State both the machine epsilon and the smallest positive normalised value for double precision, and explain why they are different quantities entirely. Machine epsilon = 2⁻⁵² ≈ 2.2×10⁻¹⁶ (mantissa-width-determined, a precision/spacing question near 1.0). Smallest positive normal = 2⁻¹⁰²² ≈ 2.2×10⁻³⁰⁸ (exponent-width-determined, a range question near the bottom of the representable magnitude scale) — the two happen to share a numeric coefficient (2.2) purely coincidentally in their approximate decimal forms, but they answer entirely different questions and are computed from entirely different fields of the 64-bit layout.

4. ROUNDING MODE COMPARISON. A true computed result is exactly 2.5, and the two nearest representable values in a simplified system are 2 (with an even final stored bit) and 3 (with an odd final stored bit). Determine the result under round-to-nearest-ties-to-even, round toward zero, and round toward positive infinity. Round-to-nearest-ties-to-even: since 2.5 is an exact tie between 2 and 3, and 2 has the even final bit, the result rounds to 2. Round toward zero (truncation): the result moves toward 0, giving 2 (truncating the .5 downward, since 2.5 is positive). Round toward positive infinity: rounds UP to the next value ≥ the true result, giving 3.

5. NON-RESTORING VS RESTORING OPERATION COUNT. For a 4-bit division where exactly 2 of the 4 steps produce a negative trial remainder, compare the total number of arithmetic operations performed by restoring division versus non-restoring division. Restoring division: 4 steps each perform 1 subtraction, PLUS an extra restoring addition for each of the 2 negative-result steps, totalling 4+2=6 operations. Non-restoring division: exactly 1 operation (add or subtract, decided by the previous step's sign) per step, totalling exactly 4 operations regardless of how many steps went negative — a genuine, guaranteed saving of 2 operations in this specific case, illustrating the general "at most 1 operation per quotient bit" property.

6. RADIX-4 BOOTH STEP COUNT. An 8-bit multiplier is processed using radix-4 modified Booth recoding. How many total steps are needed, compared to ordinary radix-2 Booth's algorithm on the same multiplier? Radix-4: 8/2=4 steps (guaranteed, regardless of the specific bit pattern). Ordinary radix-2 Booth's: 8 steps (one per bit, though some may be no-ops depending on the pattern) — radix-4 recoding needs exactly half as many steps in this case, a guaranteed reduction rather than a pattern-dependent one.

7. GUARD/ROUND/STICKY TIE DETECTION. A rounding unit discards the bit pattern 1000101 (7 bits) beyond the retained precision. Determine whether this represents an exact tie, and state the correct rounding direction. Guard bit (first discarded bit) = 1. Round bit (second discarded bit) = 0. Sticky bit = OR of all remaining discarded bits (0,1,0,1) = 1 (since at least one of them, specifically the two 1s further out, is nonzero). Since sticky=1, this is NOT an exact tie — it represents a value strictly greater than half a unit, so the result rounds UP unconditionally, with no ties-to-even question arising at all.

8. CATASTROPHIC CANCELLATION IDENTIFICATION. Which of the following computations risks catastrophic cancellation: (a) computing 1000000.1 − 1000000.0 in a format with 7 significant decimal digits, or (b) computing 1000000.1 + 1000000.0 in the same format? Explain. Computation (a) subtracts two nearly-equal large values (differing only in a small fractional part), risking catastrophic cancellation — the leading 7 digits "1000000" cancel almost entirely, leaving only the small trailing difference to carry the entire result, and any rounding error already present in either operand's stored representation is fully exposed. Computation (b), an ADDITION of two same-signed values, carries no such risk — addition of like-signed operands does not amplify relative error the way a near-cancelling subtraction does.

9. FIXED-POINT VS FLOATING-POINT SUITABILITY. A digital audio signal-processing system needs to represent values in a narrow, well-known range (say, −1.0 to +1.0) with the FINEST possible constant precision across that entire range, using minimal hardware complexity. Which representation is more suitable, and why? Fixed-point is the better choice here: since the required range is narrow and known in advance, fixed-point's constant absolute precision across its whole range (with no precision loss at the edges of the range the way floating-point's relative-precision-only guarantee would allow) is achieved with simpler, cheaper hardware — floating-point's wide dynamic range is simply not needed for a signal already known to stay within [−1.0, +1.0].

10. COMBINED MULTIPLICATION AND OVERFLOW CHECK. Using ordinary radix-2 Booth's algorithm, multiply 2 × 3 in 4-bit 2's complement, and verify the result. M=0010 (2), Q=0011 (3), Q₋₁=0, A=0000. Step 1: pair(Q₀,Q₋₁)=(1,0)→SUBTRACT M: A=0000−0010=1110. Shift right (arithmetic): A=1111, Q=0001, Q₋₁=1. Step 2: pair(Q₀,Q₋₁)=(1,1)→no-op. Shift: A=1111, Q=1000, Q₋₁=1. Step 3: pair(Q₀,Q₋₁)=(0,1)→ADD M: A=1111+0010=0001. Shift: A=0000, Q=1100, Q₋₁=0. Step 4: pair(Q₀,Q₋₁)=(0,0)→no-op. Shift: A=0000, Q=0110. Final combined register: A=0000, Q=0110, giving the 8-bit product 00000110 = 6 in decimal, matching 2×3=6 exactly. Since the true product 6 fits comfortably within the 8-bit product register's signed range, no overflow occurs — confirmed directly by the sign bit of the 8-bit result being 0 (positive), consistent with multiplying two positive operands.

11. CARRY-SAVE REDUCTION. Add three 4-bit operands 0111, 0101, 0010 (7+5+2=14) using a single carry-save adder stage followed by one final ordinary addition. Bit by bit: position 0 (bits 1,1,0): sum=0, carry=1. Position 1 (bits 1,0,1): sum=0, carry=1. Position 2 (bits 1,1,0): sum=0, carry=1. Position 3 (bits 0,0,0): sum=0, carry=0. Sum vector: 0000. Carry vector, shifted left one position: 1110. Final ordinary addition: 0000+1110=1110=14, matching 7+5+2=14 exactly.

CARRYING THIS FORWARD

Every algorithm in this chapter reduces, at its core, to the same tiny instruction set already established as the foundation of computer arithmetic: shift, add, and subtract, using the same 2's-complement addition hardware covered in the previous chapter with no special sign-handling logic required. Booth's algorithm and non-restoring division are both, in their own way, EFFICIENCY refinements over a more naive baseline (one-add-per-1-bit multiplication; restore-and-retry division) — refinements that pay off specifically under certain input patterns (long runs of equal multiplier bits; frequent negative trial remainders) rather than universally, which is exactly why each algorithm's efficiency claim in this chapter was stated with its specific triggering CONDITION attached, never as an unconditional guarantee. The chapter's other recurring theme — precision (mantissa-determined) versus range (exponent-determined) as two genuinely independent properties of a floating-point format — is worth carrying forward as a template for evaluating ANY numeric representation encountered later: always ask separately "how finely can nearby values be distinguished" and "how large or small a magnitude can be represented at all," since a representation's designers frequently trade off these two properties independently, and conflating them is one of the most persistent, easily avoided sources of error in this entire area of the syllabus.

KEY: across every algorithm in this chapter, the pattern-dependent efficiency techniques (ordinary radix-2 Booth's algorithm, non-restoring division) and the pattern-INDEPENDENT ones (radix-4 modified Booth recoding, guaranteed to halve the step count regardless of the multiplier's specific bits) represent two genuinely different KINDS of algorithmic improvement — one exploits a favourable but not-always-present structure in the specific input (worst case no better than the naive baseline, best case substantially faster), while the other achieves a fixed, guaranteed improvement by examining a strictly larger window of information at each step, at the cost of a slightly more complex decision table; recognising which category a proposed optimisation falls into is essential before claiming it will "definitely" speed up a computation, since a pattern-dependent technique's benefit must always be qualified by the specific input it is being applied to.

Two further connections are worth drawing explicitly between this chapter and the previous one on number representation. First, the earlier discussion of overflow detection (carry into the sign bit differing from carry out of it) is not merely a fact about ADDITION in isolation — it is the exact same underlying condition Booth's algorithm's partial-product accumulator must avoid triggering at each intermediate add or subtract step, and a full hardware Booth multiplier implementation must include the identical overflow-checking logic already derived for plain 2's-complement addition, simply applied repeatedly across the algorithm's several internal steps rather than once. Second, the previous chapter's observation that many ordinary decimal fractions never terminate in binary, and IEEE 754's consequent inability to store them exactly, is the same underlying phenomenon that eventually surfaces as catastrophic cancellation in this chapter — a value that was never exactly representable in the first place carries a small baked-in error from the moment it is first stored, and that error becomes disproportionately visible the instant it participates in a near-cancelling subtraction with another similarly imprecise value. Seeing catastrophic cancellation not as a new, separate phenomenon but as the DOWNSTREAM consequence of a representation limitation already established in the previous chapter is exactly the kind of connective, cross-chapter understanding this book has aimed to build throughout — a numeric quirk first encountered as an abstract curiosity about binary fractions resurfaces, later, as a concrete practical hazard in real floating-point computation, and recognising the connection between the two is worth more than memorising either fact about it in isolation.

A final practical note tying the whole chapter together: every worked trace above was checked by an independent method after the main derivation — Booth's algorithm's result was decoded back to its signed decimal value and compared against direct multiplication; both division methods' final quotient and remainder were checked against quotient×divisor+remainder=dividend; the IEEE 754 range and precision figures were derived from first principles (bit-count reasoning) rather than simply quoted. This discipline of an independent cross-check after every derivation — rather than trusting the forward derivation alone — is precisely the same practice used throughout every chapter of this study material, and it is worth continuing deliberately here: computer arithmetic algorithms are exactly the kind of multi-step, bit-level procedure where a single sign error or a single mistracked carry, easy to make and easy to overlook on a first pass, is nearly always caught immediately by a quick independent verification of the final answer against a simpler, more direct computation, rather than left to propagate silently into a confidently stated but ultimately incorrect result.

The carry-save adder section, introduced late in this chapter, is itself a fitting closing example of the whole chapter's central lesson: a genuinely different-looking hardware technique (deferring carry propagation entirely until a single final addition, rather than paying its cost at every intermediate step) turns out, on inspection, to share its underlying structural idea with a technique introduced much earlier (non-restoring division's decision to defer the restoring correction rather than paying for it immediately at every step where it might be needed). Recognising this shared "defer the expensive step to the very end" pattern across what might otherwise look like two unrelated named algorithms is exactly the kind of connective understanding worth carrying forward past this chapter, into any later material covering pipelined or otherwise deeply optimised arithmetic hardware built from these same small number of underlying design ideas. That pattern gets applied again and again in progressively more elaborate combinations as real processor arithmetic units grow more sophisticated in pursuit of ever lower per-operation latency at ever larger operand widths — all while continuing to rely, underneath every layer of added sophistication, on the exact same small set of shift, add, and subtract primitives this chapter opened with, arranged and combined ever more cleverly at each successive generation of hardware, but never fundamentally replaced by anything genuinely new at the level of the basic operations actually being performed. This holds across every layer of clever hardware restructuring described in this entire chapter, from Booth's algorithm and non-restoring division through to the carry-save adder and the guard/round/sticky rounding logic covered earlier — the same detailed discussion of correct, hardware-efficient floating-point rounding decisions made correctly, and cheaply, at every arithmetic step actually performed inside real, modern floating-point hardware units in widespread everyday practical use today.
`
};
