// Textbook chapter: Lexical Analysis.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['compiler-lexical'] = {
  figs: [
    {
      id: 'compiler-phase-pipeline',
      caption: 'The classical compiler front-end-to-back-end phase pipeline. Lexical analysis is the very first phase, turning raw source characters into a token stream every later phase consumes.',
      svg: '<svg viewBox="0 0 320 60" width="100%" style="max-width:320px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.1" fill="none"><rect x="5" y="15" width="55" height="30"/><rect x="70" y="15" width="55" height="30"/><rect x="135" y="15" width="55" height="30"/><rect x="200" y="15" width="55" height="30"/><rect x="260" y="15" width="55" height="30"/></g><g font-size="7.5" fill="currentColor" text-anchor="middle"><text x="32" y="33">Lexical</text><text x="97" y="33">Syntax</text><text x="162" y="33">Semantic</text><text x="227" y="33">ICG</text><text x="287" y="33">Codegen</text></g></svg>'
    },
    {
      id: 'two-buffer-scheme',
      caption: 'The two-buffer lexical scanning scheme: two equal-size buffers refilled alternately, with lexeme-begin and forward pointers scanning ahead, each buffer terminated by an eof sentinel to halve the per-character test overhead.',
      svg: '<svg viewBox="0 0 300 90" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.1" fill="none"><rect x="10" y="20" width="130" height="30"/><rect x="150" y="20" width="130" height="30"/><line x1="140" y1="20" x2="140" y2="50"/></g><g font-size="9" fill="currentColor" text-anchor="middle"><text x="75" y="39">Buffer 1</text><text x="215" y="39">Buffer 2</text><text x="140" y="65" font-size="8">eof sentinel at each boundary</text></g></svg>'
    }
  ],
  text: `
WHAT LEXICAL ANALYSIS IS FOR, PRECISELY

A compiler's very first job is to turn a raw, undifferentiated stream of individual characters — the exact bytes making up a source file — into something the REST of the compiler can actually reason about: a stream of TOKENS, each a meaningful, named unit (a keyword, an identifier, a number, an operator) the parser can build a syntax tree from. Lexical analysis is entirely about this one transformation, and understanding its scope precisely — what it can and cannot, and should and should not, be responsible for — is the single most important organizing idea for this entire chapter.

[[FIG:compiler-phase-pipeline]]

1. LEXICAL ANALYSIS CONSUMES: the raw source CHARACTER stream (after any preprocessing or macro expansion, in a design that includes such a stage as a separate earlier pass).
2. LEXICAL ANALYSIS PRODUCES: a stream of TOKENS (each a token-TYPE, attribute-VALUE pair) handed to the parser ON DEMAND, typically via a single getNextToken() call the parser invokes whenever it needs the next token; it also produces new SYMBOL-TABLE entries for identifiers first encountered, and reports LEXICAL errors specifically (illegal characters, malformed literals) — a category of error genuinely distinct from syntax or semantic errors, as this chapter's own error-handling section below makes precise.
3. THE FULL CLASSICAL PHASE PIPELINE, for reference and context: lexical analysis → syntax analysis → semantic analysis → intermediate code generation → code optimization → target code generation. The FRONT END phases (lexical, syntax, semantic, intermediate code generation) are SOURCE-language dependent but MACHINE-independent; the BACK END (target code generation) is machine-dependent; optimization can occur EITHER on the intermediate representation (machine-independent) or again on the generated target code (machine-dependent, typically called PEEPHOLE optimization) — this exact pipeline structure, and where lexical analysis sits within it, is developed across this entire Compiler Design chapter sequence, with each later chapter covering the phase immediately following the one before it.

REGULAR EXPRESSIONS: THE SPECIFICATION LANGUAGE FOR TOKENS

Every token type a lexer recognizes is specified using a REGULAR EXPRESSION — and this is not a coincidence or a convenient shortcut, but a DIRECT, deliberate application of this book's own first Theory of Computation chapter's central subject: token patterns are, almost without exception, genuinely REGULAR languages, since a token (a keyword, an identifier's character shape, a numeric literal's digit-and-decimal-point structure) never requires unbounded counting or nested matching to recognize — precisely the kind of language a finite automaton handles natively and efficiently.

1. THE STANDARD REGULAR-EXPRESSION ALGEBRA, exactly as covered in this book's own first Theory of Computation chapter: UNION (a|b, matching either a or b), CONCATENATION (ab, matching a immediately followed by b), KLEENE CLOSURE (a*, matching zero or more repetitions of a), and the derived shorthand forms POSITIVE CLOSURE (a+ = aa*, matching one or more repetitions) and OPTIONAL (a? = a|ε, matching zero or one occurrence).
2. THE CONSTRUCTION PIPELINE a lexical-analyzer GENERATOR (such as the classic Unix tool lex, or its modern descendant flex) performs INTERNALLY, entirely automatically, given a set of regular-expression token specifications: RE → (via THOMPSON'S CONSTRUCTION, covered in full in this book's own second Theory of Computation chapter) → NFA → (via SUBSET CONSTRUCTION, also covered in that same chapter) → DFA → (via the PARTITION-REFINEMENT minimization algorithm, likewise covered in that same chapter) → MINIMIZED DFA — this ENTIRE pipeline, developed piece by piece across an ENTIRELY DIFFERENT part of this book's own syllabus, turns out to be EXACTLY the machinery a real, practical lexical analyzer relies on internally, a genuinely direct and satisfying payoff of that earlier theoretical material.

KEY: recognising that Thompson's construction, subset construction, and DFA minimization — each individually developed as pure THEORY in this book's own Theory of Computation sequence — are not merely academic exercises but the LITERAL algorithms a real lexer-generator tool runs internally is exactly the kind of "abstract theory has a direct, concrete engineering payoff" connection this book has aimed to make explicit wherever it genuinely occurs; a student who has internalized WHY subset construction can, in the worst case, produce up to 2^n DFA states from an n-state NFA (as proven rigorously in the earlier chapter) already understands, without needing any NEW theory at all, exactly why a lexer specification with many overlapping, similar token patterns can occasionally produce a surprisingly large generated DFA.

1. Worked example (regular expression to minimal DFA, directly constructed): the classic pattern (a|b)*abb — "any string over {a,b} ending in abb" — has a minimal DFA with EXACTLY 4 states: A (start state, also the state reached after any 'b' that does not extend a partial match), B (last character seen was 'a'), C (last two characters seen were "ab"), D (accepting: last three characters seen were "abb"). Transition table: δ(A,a)=B, δ(A,b)=A, δ(B,a)=B, δ(B,b)=C, δ(C,a)=B, δ(C,b)=D, δ(D,a)=B, δ(D,b)=A. Trace "aabb": A→(a)→B→(a)→B→(b)→C→(b)→D, ACCEPT — correct, "aabb" ends in "abb". Trace "abab": A→(a)→B→(b)→C→(a)→B→(b)→C, REJECT (C is not accepting) — correct, "abab" does NOT end in "abb". This 4-state DFA is ALREADY minimal (each of A, B, C, D tracks a genuinely different length of matched SUFFIX of the target pattern "abb," so no two states are Myhill-Nerode equivalent, and a minimization pass changes nothing).

MAXIMAL MUNCH: THE CENTRAL TOKENIZATION RULE

1. MAXIMAL MUNCH (also called the LONGEST MATCH rule): at EVERY scan position, the lexer takes the LONGEST possible prefix of the remaining input that matches ANY of its specified token patterns — this single rule is precisely what resolves potentially ambiguous scans correctly: reading "<=" as ONE compound-operator token rather than "<" followed separately by "=", and reading "intx" as a SINGLE identifier token rather than the keyword "int" immediately followed by a separate identifier "x".
2. THE TIE-BREAKING RULE, needed specifically when TWO OR MORE patterns match the identical longest lexeme simultaneously (the most common case: a reserved word like "if" matches BOTH the specific keyword pattern for "if" AND the generic identifier pattern [a-zA-Z][a-zA-Z0-9]*): the pattern listed EARLIER — equivalently, given HIGHER PRIORITY — in the lexer specification wins. This is EXACTLY the mechanism by which a keyword correctly "beats" the generic identifier rule, PROVIDED the keyword's own pattern is listed with higher priority in the specification (the conventional, near-universal ordering in essentially every real lexer specification).
3. A LEXICAL ERROR is reported ONLY when NO pattern matches ANY prefix of the remaining input WHATSOEVER (typically, an illegal, unrecognized character) — every OTHER kind of problem a source file might contain (an undeclared variable, unbalanced parentheses, a type mismatch) is caught by a LATER phase (syntax or semantic analysis), NEVER by the lexer itself, precisely because REGULAR languages — and hence anything a lexer, built entirely from finite automata, can recognize — CANNOT count unboundedly or match nested structures, exactly the fundamental limitation this book's own first and second Theory of Computation chapters proved rigorously via the pumping lemma.

GATE TRAP: treating balanced-parentheses checking, begin-end block matching, or "used before declared" checking as something the LEXER itself could conceivably do is a fundamental scope confusion — regular expressions and finite automata CANNOT count unboundedly (proven rigorously via the pumping lemma in this book's own first Theory of Computation chapter) or verify a genuinely CONTEXT-SENSITIVE condition like prior declaration, so these checks are STRICTLY parser or semantic-analyzer responsibilities, never lexer responsibilities, no matter how "simple" or "obviously checkable" such a requirement might superficially seem.

GATE TRAP: splitting a COMPOUND operator (<=, ==, &&, ||, ->, ++, --, >>=) into SEPARATE single-character tokens, rather than recognizing it as ONE token via maximal munch, is one of the single most common tokenization mistakes — maximal munch is a GLOBAL rule applied at EVERY scan position without exception, and a multi-character operator's own individual characters, even though each ALSO happens to form a valid single-character token on its own (< is itself a valid comparison operator; + is itself a valid addition operator), must always be consumed TOGETHER as the single LONGER match whenever a longer matching pattern is available.

BUFFERING: HOW A LEXER ACTUALLY READS ITS INPUT EFFICIENTLY

[[FIG:two-buffer-scheme]]

1. READING a source file one character at a time via an individual system call for EACH character is FAR too slow in practice — the fixed per-call overhead of an actual operating-system read operation dwarfs the trivial cost of examining a single character, so real lexers instead read LARGE BLOCKS of the source file at once (commonly matching the underlying disk's own natural block size, such as 4096 bytes), amortizing that fixed system-call overhead across many characters at once.
2. THE TWO-BUFFER SCHEME: TWO buffers, each of the SAME fixed size N, are refilled ALTERNATELY as scanning proceeds — TWO pointers, conventionally named LEXEME-BEGIN (marking where the CURRENT token started) and FORWARD (scanning AHEAD, character by character, to find where the current token actually ENDS), sweep across these buffers; when FORWARD reaches the very END of one buffer, the OTHER buffer is (re)loaded with the NEXT block of source characters, and scanning continues seamlessly across the buffer boundary.
3. THE SENTINEL TRICK: each buffer is terminated with a special EOF SENTINEL character (guaranteed not to appear naturally anywhere in genuine source code) — this lets the FORWARD pointer's own inner scanning loop perform JUST ONE test per character ("is this character the eof sentinel?") rather than TWO separate tests that would otherwise be needed on every single character examined ("have I reached the end of the CURRENT buffer?" AND, separately, "have I reached the end of the ENTIRE source file?") — roughly HALVING the per-character test overhead in the lexer's own innermost, most frequently executed scanning loop, precisely the textbook motivation this specific buffering refinement is designed to address.

GATE TRAP: confusing the sentinel-based two-buffer scheme's OWN specific benefit (reducing the NUMBER OF TESTS performed per character, from two down to one, inside the lexer's innermost scanning loop) with a claim about reducing the NUMBER OF DISK READS performed overall — the REDUCTION in disk-read frequency is achieved ENTIRELY by the underlying BLOCK-BUFFERING strategy itself (reading N characters at once, rather than one at a time), which is a SEPARATE benefit from the sentinel trick; the sentinel specifically optimizes the SCANNING LOOP's own per-character test count, not the I/O layer's own read frequency, and conflating these two genuinely distinct optimizations is a common, subtle error.

THE SYMBOL TABLE: A CROSS-CUTTING COMPONENT

1. THE SYMBOL TABLE, and the ERROR HANDLER alongside it, are CROSS-CUTTING components that EVERY phase of the compiler (lexical, syntax, semantic, intermediate-code-generation, optimization, code generation) touches and interacts with, in contrast to the strictly SEQUENTIAL, phase-to-phase data flow (tokens, then a parse tree, then an annotated tree, then intermediate code, and so on) the main pipeline otherwise follows.
2. THE LEXER'S OWN SPECIFIC ROLE regarding the symbol table: upon encountering a new IDENTIFIER lexeme for the very FIRST time, the lexer typically INSTALLS a new entry for it into the symbol table (or, if that exact identifier has already been seen before, simply LOOKS UP its existing entry) — and returns, as the token's own ATTRIBUTE VALUE, a POINTER or INDEX into that single, canonical symbol-table entry, rather than the raw identifier string itself, so that every LATER phase of the compiler can share and reference the SAME single canonical record for that name, rather than each phase needing to re-parse or re-compare the raw character string over and over again.
3. NUMERIC and STRING CONSTANTS similarly carry their OWN attribute value (the actual parsed numeric value, or a pointer to the literal's own content) alongside their token type, while KEYWORD, OPERATOR, and PUNCTUATION tokens typically need NO additional attribute value at all, since the token TYPE alone already fully determines everything the parser could possibly need to know about that specific token.

1. Worked example (token counting with maximal munch and keyword-priority, independently re-verified line by line): tokenize the source line "if(x>=10&&flag){y=x++;}" applying maximal munch and keyword-before-identifier priority throughout. Scanning left to right: "if" (KEYWORD — beats the generic identifier pattern by priority, token 1), "(" (token 2), "x" (IDENTIFIER, token 3), ">=" (COMPOUND operator, ONE token via maximal munch — never split into "<" then "="", token 4), "10" (NUMBER, token 5), "&&" (COMPOUND operator, ONE token, token 6), "flag" (IDENTIFIER, token 7), ")" (token 8), "{" (token 9), "y" (IDENTIFIER, token 10), "=" (token 11), "x" (IDENTIFIER, token 12), "++" (COMPOUND operator, ONE token via maximal munch — never split into "+" then "+", token 13), ";" (token 14), "}" (token 15). TOTAL: 15 tokens. The most common WRONG answers arise from incorrectly splitting ">=" into two separate tokens (violating maximal munch), incorrectly splitting "++" into two separate "+" tokens (likewise violating maximal munch), or forgetting that "x++" still requires its OWN terminating ";" token entirely separate from the closing "}" that immediately follows it.

LEXICAL ERROR RECOVERY: WHAT HAPPENS AFTER AN ILLEGAL CHARACTER

A real, production-quality lexer cannot simply STOP the moment it encounters an illegal character — a compiler that halted entirely at the FIRST lexical error would be far less useful than one that can continue scanning, report every lexical error found across an entire source file in one pass, and let the programmer fix them all at once.

1. THE STANDARD RECOVERY STRATEGY, PANIC-MODE RECOVERY: upon detecting an illegal character (one matching no pattern at all), the lexer DELETES (skips over) successive characters from the remaining input until a character IS found that CAN begin some valid, recognizable token, then RESUMES normal scanning from that point — this is the SAME "panic mode" strategy this book's own later Syntax Analysis chapter will develop in far greater detail for SYNTAX-level error recovery, applied here at the simpler, character-level granularity a lexer operates at.
2. OTHER LEXICAL-LEVEL RECOVERY ACTIONS, used in specific situations beyond simple character deletion: INSERTING a missing character (for instance, silently treating an unterminated string literal at end-of-line as though a closing quote had been present, then reporting the omission as a warning rather than a hard error), or REPLACING an illegal character with a plausible, similar-looking legal one (a common heuristic for OCR-scanned or otherwise corrupted source text, substituting a commonly-confused character like 'l' for '1' or 'O' for '0').
3. WHATEVER SPECIFIC recovery strategy is used, the KEY design GOAL remains the SAME throughout: continue scanning far enough to find and report AS MANY genuine lexical errors as possible in ONE single compilation pass, rather than stopping dead at the very first one — directly maximizing the USEFUL information a single compile attempt returns to the programmer.

GATE TRAP: assuming a lexer, upon finding an illegal character, must necessarily ABORT the ENTIRE compilation process immediately is incorrect — real lexers are specifically designed to RECOVER from an illegal-character error (typically via panic-mode character skipping) and CONTINUE scanning the rest of the file, precisely so that a single compilation attempt can surface MULTIPLE independent lexical errors at once, rather than requiring one separate compile-fix-recompile cycle per individual error found.

LOOKAHEAD BEYOND MAXIMAL MUNCH: WHEN ONE CHARACTER OF LOOKAHEAD ISN'T ENOUGH

1. MAXIMAL MUNCH, as developed above, inherently requires the lexer to look AHEAD past the point where a SHORTER match would already be complete, specifically to check whether a LONGER match is ALSO available — this means a practical lexer's own FORWARD pointer routinely scans PAST the eventual end of the CURRENT token being recognized, before finally settling on exactly where that token actually ends, then RETRACTING (backing up) the forward pointer to the correct final boundary once the true longest match has been determined.
2. A CONCRETE case requiring GENUINE lookahead, beyond the SIMPLE "is the very next character part of a longer match" check: distinguishing a language's own RELATIONAL "less than" operator "<" from a compound "<=" operator requires examining exactly ONE character of lookahead (is the character immediately following "<" an "="?) — but SOME languages require MORE than a SINGLE character of lookahead to correctly identify a token boundary, the classic textbook example being FORTRAN's own historically famous whitespace-insensitivity, where "DO 5 I = 1,25" (a loop statement) and "DO5I = 1.25" (an assignment to a variable literally named "DO5I") are lexically ambiguous until enough of the REST of the statement (specifically, whether a comma or a period eventually appears) has been examined.
3. LOOKAHEAD OPERATORS in a lexical-specification language (conventionally written as a trailing slash, r1/r2, meaning "match r1, but ONLY when it is immediately followed by something matching r2, WITHOUT actually consuming r2 itself as part of the current token") are the standard mechanism real lexer-generator tools provide specifically for handling exactly this class of lookahead-dependent tokenization decision, when maximal munch's own default longest-match behavior alone is insufficient to resolve the ambiguity correctly.

1. Worked example (lookahead operator resolving a genuine ambiguity): a language's lexer specification needs to distinguish a FORTRAN-style "IF" keyword used as a LOGICAL-IF statement (IF (condition) statement) from the SAME "IF" keyword's use as the START of a BLOCK-IF statement (IF (condition) THEN ... ENDIF) — resolving this specific ambiguity correctly requires looking AHEAD, past the closing parenthesis of the condition, to check whether the KEYWORD "THEN" appears immediately afterward; a lookahead specification of the rough form IF(condition)/THEN (matching the "IF(condition)" portion only when followed by "THEN", without consuming "THEN" itself as part of the current token) is exactly the tool designed to resolve precisely this kind of forward-dependent tokenization decision.

REGULAR DEFINITIONS: BUILDING COMPLEX PATTERNS FROM NAMED PIECES

1. A REGULAR DEFINITION is a NAMED regular expression, allowed to reference OTHER, previously defined regular definitions by name, letting complex token patterns be built up systematically from smaller, individually simpler, reusable named pieces — for instance, defining digit as [0-9], then digits as digit+ (one or more repetitions of the ALREADY-named digit definition), then number as digits(.digits)?(E[+-]?digits)? (an integer, optionally followed by a decimal fraction, optionally followed by a scientific-notation exponent) — each definition BUILDING directly on the ones defined immediately before it.
2. THIS NAMED, COMPOSITIONAL STYLE is, at its structural core, EXACTLY the same "build complex expressions from smaller, individually well-understood named pieces, combined via a small fixed set of operators" discipline this book's own Digital Logic chapters applied to Boolean expressions, and its own Theory of Computation chapters applied to building complicated automata out of small Thompson's-construction fragments — recognising regular DEFINITIONS as simply this SAME compositional discipline, now applied specifically to the practical, everyday task of writing an actual, maintainable lexer specification, is a genuinely useful, transferable habit.
3. A CAREFULLY CHOSEN regular-definition decomposition, splitting a genuinely complex pattern (like the full "number" pattern above, correctly handling optional fractional parts and optional scientific notation together) into SEVERAL smaller, individually simple, individually-testable named pieces, is BOTH easier for a human reader to verify correct AND, in most real lexer-generator implementations, compiles down to essentially the SAME underlying NFA/DFA machinery as an equivalent, but far less readable, single giant unnamed regular expression written out in full.

1. Worked example (regular definitions for a realistic numeric-literal pattern): define digit = [0-9], digits = digit+, fraction = (.digits)?, exponent = (E[+-]?digits)?, number = digits fraction exponent. Trace which pieces of this composed definition match against the literal "3.14E-10": digits matches "3" (the integer part before any decimal point); fraction matches ".14" (the digits sub-piece contributing "14" after the required literal period); exponent matches "E-10" (the literal "E", the optional sign "-", and the digits sub-piece contributing "10"); the FULL number definition, concatenating all three pieces in sequence, matches the complete literal "3.14E-10" as intended, with each named sub-piece independently verifiable against its own, individually much simpler, defining pattern.

FINITE AUTOMATA IMPLEMENTATION: TABLE-DRIVEN VS DIRECT-CODED

Once a minimized DFA has been constructed for a lexer's own combined set of token patterns, that DFA must actually be IMPLEMENTED as running code — and there are two genuinely distinct standard implementation STYLES worth knowing explicitly.

1. TABLE-DRIVEN IMPLEMENTATION: the DFA's own transition function is stored DIRECTLY as a two-dimensional array (indexed by CURRENT STATE and INPUT SYMBOL, giving the NEXT state), and the scanning loop itself is a single small, generic piece of code that simply looks up this table repeatedly — GENERIC, compact, and easy to generate MECHANICALLY from an arbitrary DFA (exactly the approach a lexer-GENERATOR tool like lex/flex takes internally), but with a small runtime overhead from the table LOOKUP itself on every single character examined.
2. DIRECT-CODED (HAND-CODED) IMPLEMENTATION: each DFA state becomes its OWN separate block of code (commonly a labeled section reached via explicit jumps, or a case within a large switch statement), with the SPECIFIC transitions out of that state written out explicitly as ordinary conditional logic — typically FASTER at runtime (no table-lookup indirection at all, and a compiler's own optimizer can often optimize the resulting straight-line code more effectively than it could optimize an equivalent generic table-lookup loop), but considerably MORE code, and correspondingly harder to generate and verify correctly BY HAND for anything beyond a genuinely small number of states.
3. HAND-WRITTEN, PRODUCTION-QUALITY lexers for widely-used, performance-sensitive compilers OFTEN use the direct-coded style specifically for this performance reason, while lexer-GENERATOR tools (lex, flex, and similar) typically default to the table-driven style, since it is far easier to generate MECHANICALLY, CORRECTLY, and CONSISTENTLY from an arbitrary, generated DFA, regardless of that DFA's own specific size or structure.

GATE TRAP: assuming a "generated" (lexer-generator-tool-produced) lexer is ALWAYS slower than a hand-written one purely because of this table-driven-versus-direct-coded distinction oversimplifies a genuinely more nuanced real-world trade-off — while table-driven code DOES carry SOME per-character lookup overhead relative to direct-coded logic, a MECHANICALLY generated table-driven lexer is far LESS likely to contain subtle correctness bugs than a large, manually hand-coded direct implementation, and for MOST practical compilers, the lexer's own runtime cost is a genuinely small fraction of the ENTIRE compilation time regardless of which specific implementation style is chosen — a GATE question testing this specific trade-off is testing awareness of BOTH styles' existence and their respective trade-offs, not a blanket "one style is unconditionally faster/better" claim.

WORKED PROBLEMS

1. MAXIMAL MUNCH ON A NEW SOURCE LINE. Tokenize "while(a<=b||c!=0){a--;}" applying maximal munch and keyword priority throughout, and give the total token count. "while" (keyword,1), "(" (2), "a" (id,3), "<=" (compound op, 4), "b" (id,5), "||" (compound op, 6), "c" (id,7), "!=" (compound op, 8), "0" (number,9), ")" (10), "{" (11), "a" (id,12), "--" (compound op via maximal munch, 13), ";" (14), "}" (15). Total: 15 tokens.

2. MINIMAL DFA STATE COUNT FOR A DIFFERENT SUFFIX PATTERN. Using the SAME "remembers matched suffix length" construction as this chapter's own (a|b)*abb example, state the minimal DFA size for (a|b)*aab (strings over {a,b} ending in "aab"), and briefly justify. Exactly |aab|+1 = 4 states, by the identical "contains/ends-with substring" recipe from this book's own first Theory of Computation chapter — one state per length of the longest currently-matched prefix of the target suffix "aab" (lengths 0 through 3), with the length-3 state being the unique accepting state, and (since this is an "ends with" rather than "contains" pattern) that accepting state is NOT a sink, requiring its own overlap-aware fallback transitions exactly as that earlier chapter's own "ends with" construction required.

3. SYMBOL TABLE ATTRIBUTE VALUE IDENTIFICATION. For the token stream generated from "x = 42;", state which tokens carry a meaningful attribute value beyond their bare token type, and what that attribute value is for each. "x" (IDENTIFIER) carries a pointer/index into its own symbol-table entry. "42" (NUMBER) carries the actual parsed numeric value 42. "=" and ";" (operator/punctuation tokens) carry NO additional attribute value at all, since their token type alone is fully self-describing.

4. LEXICAL ERROR VS LATER-PHASE ERROR CLASSIFICATION. Classify each of the following as a lexical error, or an error caught by a LATER phase instead: (a) the character sequence "@#$" appearing where no pattern matches at all; (b) the source "if(x" with no closing parenthesis; (c) using a variable "y" that was never declared anywhere in the program. (a) IS a genuine lexical error (no token pattern matches this character sequence at all). (b) is a SYNTAX error (unbalanced parentheses require unbounded counting, provably beyond a lexer's own regular-language recognition power). (c) is a SEMANTIC error (checking prior declaration requires tracking an unbounded, arbitrary set of previously-declared names via the symbol table, a job for semantic analysis, not the lexer).

5. NFA-TO-DFA STATE COUNT FOR A CONCATENATED PATTERN. A lexer specification concatenates three independent basic regular expressions, each with a Thompson's-construction NFA of 4 states, into one combined pattern. Using Thompson's construction's own concatenation rule (chaining via a single shared ε-edge, with the second NFA's start effectively merged into the first NFA's own final state region), estimate the resulting combined NFA's total state count. Concatenating k NFAs of s states each via Thompson's construction typically yields close to k×s states (since concatenation adds only a single ε-edge connecting each pair, with no new states introduced by the concatenation operation itself, only by each individual basic RE's own construction) — for three 4-state NFAs, approximately 3×4=12 states total, though the EXACT count depends on the specific internal structure Thompson's construction assigns to each basic RE (start and accept states may or may not be literally shared/merged depending on the exact textbook variant of the construction used).

6. SENTINEL-TRICK TEST-COUNT SAVINGS. A source file requires scanning 100,000 characters in total. Without the sentinel trick, the lexer performs 2 tests per character in its innermost scanning loop; with the sentinel trick, it performs only 1. Compute the total number of tests saved. Without sentinel: 100,000×2 = 200,000 tests. With sentinel: 100,000×1 = 100,000 tests. Savings: 200,000−100,000 = 100,000 tests saved — exactly half the original test count, confirming the "roughly halving" benefit stated qualitatively above with a concrete worked number.

7. TIE-BREAKING RULE APPLIED TO A NEW KEYWORD. A lexer specification lists the identifier pattern [a-zA-Z][a-zA-Z0-9]* BEFORE the specific keyword pattern for "return" (an unusual, non-standard ordering). What token would the lexeme "return" actually receive under maximal munch's tie-breaking rule, and why? It would be tokenized as a plain IDENTIFIER, not the RETURN keyword — since the tie-breaking rule specifically favors whichever matching pattern is listed EARLIER (higher priority) in the specification, and in this specific (non-standard) ordering, the generic identifier pattern was listed first; this directly illustrates why real-world lexer specifications ALWAYS list keyword patterns BEFORE the generic identifier pattern, precisely to avoid this exact misclassification.

8. TWO-BUFFER BOUNDARY CROSSING. A lexeme begins near the very END of Buffer 1 and continues into the beginning of Buffer 2 (a token straddling the buffer boundary). Does the two-buffer scheme handle this correctly, and if so, how? Yes — the FORWARD pointer, upon reaching the end of Buffer 1, simply continues scanning into Buffer 2 once it is (re)loaded, treating the two buffers as one LOGICALLY CONTINUOUS stream of characters from the scanner's own point of view; the LEXEME-BEGIN pointer may itself point into Buffer 1 while FORWARD has already advanced into Buffer 2, and the eventual lexeme is correctly reconstructed by concatenating the relevant tail of Buffer 1 with the relevant head of Buffer 2 once the token's own end is finally located.

9. PANIC-MODE RECOVERY TRACE. A lexer scanning "x = 5 @ 3;" encounters the illegal character "@" at position 7. Describe, step by step, what panic-mode recovery does from this point onward. The lexer reports a lexical error at position 7 (illegal character "@"), then DELETES (skips) characters starting from "@" until a character is found that CAN begin some valid token — here, skipping past "@" and the following space, the lexer resumes scanning at "3", successfully recognizing "3" as a NUMBER token, then ";" as a punctuation token, allowing scanning of the REST of the line to complete normally despite the one illegal character encountered partway through.

10. REGULAR DEFINITION TRACE FOR AN IDENTIFIER PATTERN. Using regular definitions letter = [a-zA-Z_], digit = [0-9], id = letter(letter|digit)*, trace which of "_temp3", "3temp", and "temp_3" match the id pattern, and explain any mismatch. "_temp3" MATCHES (starts with "_", included in the letter definition's character class, followed by a mix of letters and digits, all covered by (letter|digit)*). "3temp" does NOT match (the id pattern's own leading letter portion requires the FIRST character specifically to come from the letter definition, and "3" is a digit, not a letter, so no valid starting point for this pattern exists at this position). "temp_3" MATCHES (starts with the letter "t", followed by a mix of letters, an underscore — itself included in the letter character class — and a digit, all covered correctly by the closure portion).

11. LOOKAHEAD OPERATOR NECESSITY CHECK. For the token patterns INT (matching the keyword "int") and ID (matching a generic identifier), does distinguishing them from each other require a LOOKAHEAD operator, or does ordinary maximal munch plus keyword-priority tie-breaking already suffice? Ordinary maximal munch plus the standard keyword-priority tie-breaking rule ALREADY suffices — since both patterns match the EXACT SAME lexeme "int" with EQUAL length in this specific case (no LONGER match is available from either pattern), the tie-break rule (earlier-listed pattern wins) resolves the ambiguity directly, with no need for any FORWARD lookahead into the SUBSEQUENT characters at all; lookahead operators are needed specifically when the correct classification of the CURRENT token genuinely depends on characters that come AFTER it has already been fully matched, which is not the case here.

12. TABLE-DRIVEN IMPLEMENTATION SIZE ESTIMATE. A lexer's combined DFA (built from every token pattern in the specification, merged together) has 40 states and an input alphabet of 128 ASCII characters. Estimate the size, in table ENTRIES, of the resulting table-driven transition-table implementation. Table size = (number of states) × (alphabet size) = 40 × 128 = 5120 entries — each entry holding the resulting next-state number (or a designated "error" marker) for that specific state-symbol combination, illustrating directly why table-driven implementations, while simple and mechanically generatable, can require a genuinely sizable amount of static memory for a lexer specification covering a large token set over a full ASCII (or wider, for Unicode-aware lexers) input alphabet.

COMMENT AND WHITESPACE HANDLING

1. COMMENTS and WHITESPACE (spaces, tabs, newlines used purely for source-code formatting and readability) are RECOGNIZED by the lexer using their own dedicated regular-expression patterns, exactly like any other token type — but, CRITICALLY, they produce ZERO tokens passed on to the parser; the lexer simply MATCHES and DISCARDS them, continuing on to find the NEXT genuinely meaningful token.
2. DESPITE producing no tokens, comments and whitespace still often need to be ACTIVELY SCANNED (not simply ignored via some entirely separate mechanism) specifically to correctly TRACK line numbers — since accurate LINE-NUMBER information is essential for producing genuinely useful error messages in EVERY later compiler phase (a syntax error reported as "somewhere in this 10,000-line file" is far less useful than one reported as "line 342"), and NEWLINE characters occurring INSIDE a multi-line comment must still be individually counted even though the comment itself contributes no token.
3. NESTED COMMENTS (a comment containing another, complete comment entirely within it) present a GENUINE, structurally important complication directly connected to this book's own Theory of Computation material: correctly matching ARBITRARILY DEEPLY nested comment delimiters is, in general, NOT a regular-language-recognizable pattern at all (structurally identical to the balanced-parentheses matching problem this chapter has already flagged as beyond a lexer's own regular-language power) — which is EXACTLY why many real programming languages (C and C++ among them) DELIBERATELY DISALLOW nested comments in their own language design specifically to keep comment recognition safely within a lexer's own regular-language capability, while OTHER languages that DO permit genuinely nested comments (certain functional languages, for instance) must handle that specific requirement using a SEPARATE, small ad-hoc COUNTER mechanism layered on top of the otherwise-regular lexer, rather than attempting to express nested-comment matching as a single, pure regular expression.

GATE TRAP: assuming ANY comment syntax whatsoever is automatically, unconditionally within a lexer's regular-language recognition power is incorrect — SIMPLE, NON-nested comment delimiters (a fixed start marker and a fixed end marker, matched via ordinary regular-expression concatenation) are indeed perfectly regular and lexer-friendly, but GENUINELY NESTED comment matching (requiring the lexer to correctly track and match an UNBOUNDED comment-nesting DEPTH) is structurally identical to the balanced-parentheses problem, and is provably beyond a pure finite-automaton's own regular-language recognition power — exactly why real language designs deliberately choose one approach or the other, rather than nested comments being universally supported or universally regular by some unstated default assumption.

FROM A GATE PERSPECTIVE: TYPICAL QUESTION FORMATS ON THIS TOPIC

1. THE MOST COMMON GATE-STYLE format directly tests the Thompson's-construction-to-subset-construction-to-minimization PIPELINE, typically starting from a GIVEN small regular expression and asking for either the number of NFA states (via Thompson's construction's own mechanical, rule-by-rule state count), the number of DFA states after subset construction (requiring the student to actually TRACE which subsets are REACHABLE, rather than assuming the full worst-case 2^n), or the number of states in the FINAL, minimized DFA (requiring the partition-refinement algorithm, exactly as covered in this book's own first Theory of Computation chapter, to be applied correctly).
2. A SECOND common format directly tests MAXIMAL MUNCH and tokenization, typically presenting a short, deliberately tricky source-code snippet (often including compound operators, keyword-versus-identifier ambiguity, or comment/whitespace edge cases) and asking for the EXACT total token count — exactly the style of this chapter's own worked examples and worked problems above.
3. A THIRD common format tests the BUFFERING and sentinel-scheme material CONCEPTUALLY rather than numerically — asking WHY the sentinel trick specifically halves the per-character test count, or asking students to correctly distinguish the sentinel trick's OWN specific benefit from the SEPARATE benefit block-buffering itself already provides, exactly the distinction this chapter's own GATE TRAP on that exact topic addresses directly above.

KEY: recognising these three RECURRING GATE question FORMATS (automaton-construction-pipeline numericals; maximal-munch tokenization traces; buffering/sentinel conceptual distinctions) as the three PRIMARY lenses through which this chapter's material is typically examined is a genuinely useful, practical exam-preparation habit — rather than attempting to memorise every possible specific fact this chapter covers in complete isolation, recognising WHICH of these three recurring formats a given unfamiliar question is actually testing immediately narrows down WHICH of this chapter's own specific tools (the automaton-construction pipeline; the maximal-munch-plus-tie-break rule; the buffering/sentinel distinction) is the relevant one to apply.

WORKED PROBLEMS

This chapter is the FIRST of this book's own Compiler Design sequence, and its own placement as the very FIRST phase in the classical compiler pipeline is not an arbitrary ordering choice — every later phase in this sequence (syntax analysis, semantic analysis, intermediate code generation, and beyond) depends directly on receiving a clean, well-formed TOKEN stream as its own starting input, exactly as a parser (the very next chapter's own subject) is defined to consume a sequence of tokens, never raw, undifferentiated source characters directly. Recognising lexical analysis's own PRECISE, deliberately LIMITED scope — regular-language-recognizable token patterns only, with anything requiring genuine COUNTING or NESTING explicitly deferred to a later, more powerful phase — is exactly the same "match the tool's power to the problem's actual requirements, no more and no less" engineering discipline this book's own Theory of Computation sequence developed in full generality, now applied here to its FIRST genuinely practical, systems-engineering payoff.

KEY: this entire chapter is, in a very real and direct sense, simply this book's own first Theory of Computation chapter (regular languages and finite automata) APPLIED to a concrete engineering problem — Thompson's construction, subset construction, DFA minimization, and even the pumping-lemma-based argument for WHY certain checks (balanced parentheses, prior declaration) are IMPOSSIBLE for a lexer to perform, are ALL direct, unmodified imports from that earlier theoretical chapter, repurposed here with no new underlying mathematics required at all — recognising this direct continuity, rather than treating Compiler Design as an entirely separate, unrelated subject requiring its own fresh theoretical foundation, is the single most valuable framing this chapter can offer as this book's own Compiler Design sequence begins.

The next chapter in this sequence, on SYNTAX ANALYSIS (parsing), picks up exactly where this chapter leaves off — taking the TOKEN stream this chapter's own lexical analysis produces as its own starting input, and applying the STRICTLY MORE POWERFUL context-free grammar and pushdown-automaton machinery (developed in full in this book's own second Theory of Computation chapter) to build a complete SYNTAX TREE, handling precisely the kind of unbounded counting and nested matching (balanced parentheses, correctly nested expressions) this chapter has repeatedly emphasised lies entirely BEYOND a lexer's own more limited, regular-language-restricted recognition power.

KEY: the nested-comment example above is worth holding onto as a genuinely memorable, concrete illustration of a much broader, recurring design principle this entire Compiler Design sequence will keep returning to — every single PHASE boundary in the classical compiler pipeline (lexical-to-syntax, syntax-to-semantic, and so on, each covered in its own dedicated chapter across this sequence) exists PRECISELY at a point where a STRICTLY MORE POWERFUL computational tool becomes genuinely necessary to handle some SPECIFIC requirement the PREVIOUS, weaker phase provably cannot satisfy on its own; a real PROGRAMMING LANGUAGE's own design choices (whether to permit nested comments; whether declaration-before-use is enforced at the syntax level or deferred to semantic analysis; how deeply expressions may nest) are themselves, quite often, DIRECT, deliberate engineering responses to exactly this same underlying computational-power boundary, made visible and concrete by a real language designer's own practical choices rather than remaining a purely abstract theoretical distinction.

A FINAL PRACTICAL SUMMARY: THE FIVE TOOLS THIS CHAPTER HAS ASSEMBLED

Before moving to the next chapter's own material, it is worth explicitly listing the five distinct, individually reusable tools this chapter has developed, since a well-prepared exam answer very often requires correctly identifying WHICH of these five tools a specific question is actually asking about, rather than attempting to recall the chapter as one large, undifferentiated block of facts.

1. THE AUTOMATON-CONSTRUCTION PIPELINE (regular expression → Thompson's-construction NFA → subset-construction DFA → minimized DFA) — for questions about STATE COUNTS at any stage of this pipeline.
2. MAXIMAL MUNCH plus the KEYWORD-PRIORITY TIE-BREAK RULE — for questions about correctly TOKENIZING a specific piece of source code, including compound operators and keyword-versus-identifier ambiguity.
3. THE TWO-BUFFER SCHEME and its SENTINEL-CHARACTER refinement — for questions about the practical, systems-level MECHANICS of how a lexer actually reads its own input efficiently.
4. THE SYMBOL TABLE's specific role in the lexical-analysis phase (installing or looking up identifier entries, and what attribute VALUE each different kind of token carries) — for questions about what information specifically flows FROM the lexer INTO later compiler phases.
5. THE SCOPE BOUNDARY between lexical, syntax, and semantic analysis (regular-language-recognizable patterns only; NO unbounded counting or nested matching; NO checking of context-sensitive conditions like prior declaration) — for questions asking WHERE, specifically, a given kind of source-code error would actually be detected within the overall compiler pipeline.

Internalising this five-tool breakdown, rather than the chapter's own many individual worked examples and worked problems considered purely in isolation from one another, is what allows a genuinely UNFAMILIAR lexical-analysis question — one phrased using different specific source code, a different specific regular expression, or a different specific buffering scenario than anything shown explicitly above — to still be answered with full confidence, simply by first identifying which of these five already-thoroughly-understood tools actually applies to the question at hand.

KEY: it is worth stating explicitly, as a final closing observation tying this entire chapter's own material back to the very beginning of this book, that lexical analysis is a genuinely representative FIRST example of a pattern this book has now demonstrated repeatedly across an enormous range of subjects — a real-world engineering problem (efficiently and correctly turning raw source text into a structured token stream) turns out, upon careful examination, to be COMPLETELY solvable using tools ALREADY developed, in full theoretical generality, in an entirely different and seemingly unrelated part of this same book's own syllabus, requiring essentially no new mathematics of its own whatsoever. This same pattern — a seemingly practical, applied engineering topic resolving cleanly into an application of already-established theoretical machinery — recurs throughout the REMAINDER of this book's own Compiler Design sequence: syntax analysis will directly reuse this book's own context-free-grammar and pushdown-automaton machinery; and even later, entirely different subjects covered elsewhere in this book (network protocol design, database query optimization, and others) will each, in their own way, turn out to be applications of foundational tools developed earlier, in a different guise, for a seemingly unrelated purpose. Recognising and actively anticipating this recurring pattern — rather than being surprised anew each time a "new" applied subject turns out to lean heavily on already-familiar foundational material — is, in the end, one of the most genuinely valuable habits a careful, connected reading of this entire book, chapter after chapter and subject after subject, is specifically designed to build.

A closing practical note specific to exam preparation: because this chapter's own material draws so directly and so heavily on tools already covered in full in this book's own Theory of Computation sequence, a student who has THOROUGHLY mastered that earlier sequence — Thompson's construction, subset construction, DFA minimization, and the precise reasoning behind the pumping lemma's own limitations on what a finite automaton can and cannot recognize — will find this chapter's own material requires comparatively LITTLE additional new memorization, beyond the specific engineering vocabulary (maximal munch, the two-buffer scheme, the sentinel trick, regular definitions) this chapter has introduced to describe how that same underlying theory gets APPLIED in a genuinely practical, engineering context. This is, once again, simply this book's own consistently applied lesson in miniature: genuine understanding of a foundational subject, once thoroughly built, continues paying dividends far beyond that subject's own original chapter, often in places a first reading might not have anticipated at all.

One further worked illustration, tying together several of this chapter's own tools at once in a single combined scenario, is worth walking through before closing out this chapter entirely. Consider a lexer specification for a small language that needs to recognize exactly three token categories over the alphabet {a,b,0,1}: KEYWORDS (the fixed set "if" and "for"), IDENTIFIERS (any string of letters a-z not matching a keyword), and NUMBERS (any nonempty string of digits 0 and 1). Constructing this lexer from scratch requires, in careful sequence, every tool this chapter has developed: first, three separate regular expressions are written, one per category (a KEYWORD alternation "if"|"for"; an IDENTIFIER pattern [a-z]+; a NUMBER pattern [01]+); second, Thompson's construction converts each of these three regular expressions into its own small NFA fragment, then a single combined NFA is formed by adding one new overall start state with an epsilon-transition into each of the three individual fragment starts (exactly the UNION construction rule from Thompson's own method, covered in this book's own second Theory of Computation chapter); third, subset construction converts this combined NFA into a single DFA, in which any state reachable via a path through the KEYWORD fragment's own accepting states is marked accepting for the KEYWORD token type, and similarly for the other two categories, with the specific combination of NFA states composing each DFA state directly determining which token type (or types, in the case of overlapping matches, such as "if" itself also matching the identifier pattern) that DFA state should ultimately report; fourth, the KEYWORD-versus-IDENTIFIER overlap for lexemes like "if" is resolved at RUNTIME using the tie-breaking priority rule (KEYWORD listed with higher priority in the specification), exactly as this chapter's own maximal-munch section developed in full; and finally, this entire combined DFA is implemented, in an actual running lexer, using either the table-driven or the direct-coded implementation style covered above, with the specific choice between the two depending on the target compiler's own performance requirements and development constraints. Tracing through this single combined scenario end to end, applying every one of this chapter's own five core tools in the correct order, is exactly the kind of fully worked, multi-tool synthesis this book has now modeled repeatedly across nearly every chapter it contains, and it is precisely this kind of combined, sequential application, never any single tool considered entirely in isolation, that a genuinely difficult, well-constructed exam question on this topic is actually designed to test.

Working carefully through this final combined example, step by step, from the initial regular-expression specification all the way through to a concrete implementation choice, is exactly the right note on which to close this particular chapter — every individual tool covered above has now been shown not merely in isolated demonstration, but chained together within one single, coherent, end-to-end engineering scenario, precisely mirroring how these same tools would need to be combined in genuine practice when building an actual, working lexical analyzer for a real programming language from scratch, exactly the kind of complete, connected understanding this entire chapter has been carefully built to deliver from its very first section onward, all the way through to this final, fully and carefully worked closing scenario presented just a moment ago above.
`
};
