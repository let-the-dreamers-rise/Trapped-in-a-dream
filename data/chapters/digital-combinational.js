// Textbook chapter: Combinational Circuits.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['digital-combinational'] = {
  figs: [
    {
      id: 'mux-shannon-tree',
      caption: 'The Shannon-expansion trick: an n-variable function fits on a 2^(n-1)-to-1 multiplexer by using n-1 variables as selects and feeding each data input with 0, 1, the remaining variable, or its complement.',
      svg: '<svg viewBox="0 0 300 150" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><path d="M100,20 L100,130 L160,100 L160,50 Z"/></g><g font-size="10" fill="currentColor" text-anchor="middle"><text x="70" y="30">D0=0</text><text x="70" y="55">D1=C</text><text x="70" y="80">D2=C′</text><text x="70" y="105">D3=1</text><text x="130" y="45">A,B</text><text x="130" y="130">select</text><text x="200" y="78">Y</text></g><line x1="160" y1="78" x2="190" y2="78" stroke="currentColor" stroke-width="1.2"/></svg>'
    },
    {
      id: 'hazard-boundary-gap',
      caption: 'A static-1 hazard: two adjacent K-map groups meet at a shared edge, but no single product term is active across that boundary — during the transition, both terms can momentarily go false at once, glitching the output to 0.',
      svg: '<svg viewBox="0 0 280 120" width="100%" style="max-width:280px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><rect x="20" y="20" width="120" height="80"/><rect x="140" y="20" width="120" height="80"/></g><g font-size="11" fill="currentColor" text-anchor="middle"><text x="80" y="65">term 1 (1s)</text><text x="200" y="65">term 2 (1s)</text><text x="140" y="15">shared edge — no term covers both sides</text></g></svg>'
    }
  ],
  text: `
WHY COMBINATIONAL CIRCUITS, AND THE BUILDING-BLOCK APPROACH

A combinational circuit's output depends only on its current inputs, with no memory of past inputs at all — this chapter is about a small library of STANDARD, NAMED building blocks (multiplexers, decoders, encoders, adders, comparators, parity circuits) that appear so often in real hardware designs and in worked questions that recognising a circuit as "just a mux" or "just a decoder plus an OR combiner" is far faster than re-deriving its behaviour from a truth table every time. The unifying theme across this entire chapter is that every one of these blocks is, underneath its specific name, a direct application of the canonical SOP/POS forms and Boolean laws already built out in the earlier Boolean algebra chapter — a decoder IS a physical minterm generator, a multiplexer IS Shannon's expansion wired directly into hardware, and so on. Learning to see through each named block to the Boolean-algebra idea underneath it is what makes this chapter's material feel connected rather than an arbitrary list of components to memorise.

MULTIPLEXERS: PHYSICAL SHANNON EXPANSION

A 2ⁿ-TO-1 MULTIPLEXER (MUX) has n SELECT lines and one output, which equals whichever of its 2ⁿ DATA inputs the select lines currently point to: Y = Σᵢ (mᵢ·Dᵢ), where mᵢ is the MINTERM of the select lines matching value i — this is exactly a canonical SOP expression, with the select-line minterms playing the role of "which term is active" and the data inputs playing the role of each term's own contribution.

1. Any n-variable Boolean function can be implemented DIRECTLY on a 2ⁿ-to-1 MUX by wiring each data input Dᵢ to the EXACT value of f at that minterm (a hardwired 0 or 1) — this is the most direct, if not most economical, MUX-based implementation, using every one of the n variables purely as select lines.
2. THE SHANNON-EXPANSION MUX TRICK reduces the MUX size needed by HALF: an n-variable function can instead be realised on a SMALLER 2^(n−1)-TO-1 MUX by using only n−1 of the variables as select lines, and setting each data input to 0, 1, the REMAINING variable xₖ, or its complement xₖ′ — exactly the four possible "half-row" outcomes Shannon's expansion (from the Boolean algebra chapter) can produce when one variable is factored out and the function is evaluated over the remaining n−1 variables' two possible values.

[[FIG:mux-shannon-tree]]

Worked trace: implement f(A,B,C) = Σm(1,2,6,7) on a 4-to-1 MUX, using A,B as select lines and C as the "residue" variable fed into the data inputs (the Shannon-expansion trick, halving the naive 8-to-1 MUX that would otherwise be needed for 3 variables).

1. Row AB=00: minterms 0 (C=0) and 1 (C=1). f(0,0,0)=0 (not in the ON-set list), f(0,0,1)=1 (minterm 1 is listed) → the data input for this row must be 0 when C=0 and 1 when C=1 — exactly the behaviour of C itself. D₀=C.
2. Row AB=01: minterms 2 (C=0) and 3 (C=1). f(0,1,0)=1 (minterm 2 listed), f(0,1,1)=0 (minterm 3 not listed) → the data input must be 1 when C=0 and 0 when C=1 — exactly C′. D₁=C′.
3. Row AB=10: minterms 4 (C=0) and 5 (C=1). Neither is in the ON-set list, so f is 0 regardless of C for this row. D₂=0.
4. Row AB=11: minterms 6 (C=0) and 7 (C=1). BOTH are listed, so f is 1 regardless of C for this row. D₃=1.
5. Final wiring: D₀=C, D₁=C′, D₂=0, D₃=1, onto a 4-to-1 MUX selected by A (as the more significant select bit) and B — this realises f EXACTLY, using only ONE mux and a single inverter (for C′), with no other gates needed at all.

GATE TRAP: sizing the MUX incorrectly is a frequent, purely mechanical error — an n-variable function needs a FULL 2ⁿ-to-1 MUX if NO variable is pushed onto the data inputs (every variable used as a select line, data inputs hardwired to 0/1 constants only), or a SMALLER 2^(n−1)-to-1 MUX if the Shannon trick is used (one variable's worth of information folded into the data inputs as 0, 1, xₖ, or xₖ′) — conflating these two design choices, or assuming the smaller MUX size applies without actually having pushed a variable onto the data inputs, miscounts the required MUX size.

DEMULTIPLEXERS AND DECODERS

A 1-TO-2ⁿ DEMULTIPLEXER (DEMUX) routes a single input D to exactly one of 2ⁿ outputs, chosen by n select lines: output i = D·mᵢ (the input D is passed through to output i only when the select lines match pattern i, and every other output stays 0).

A DECODER is exactly a DEMUX with its data input D permanently tied to 1 — an n-to-2ⁿ decoder asserts EXACTLY ONE output line per input combination, and that one asserted output is precisely the minterm of the input matching the decoder's current input code.

KEY: because a decoder's outputs ARE the minterms of its inputs, decoders are the NATURAL hardware realisation of a Σm expression — any function given in canonical Σm form can be built by feeding the relevant inputs into a decoder and ORing together EXACTLY the output lines corresponding to the function's listed minterms; this is precisely why a decoder's outputs are so often described as "one wire per row of the truth table."

GATE TRAP: a BARE decoder, by itself, CANNOT realise an arbitrary Boolean function — it only DECODES (asserts the one matching minterm line); an EXTERNAL OR gate (explicitly added, or achieved via the decoder's own enable-line structure in some decoder families) is always needed to COMBINE the selected minterm lines into the function's actual output; assuming a decoder alone is a complete function-realisation block, with no additional combining logic, is a common and easily corrected misconception.

Decoders CASCADE naturally: a larger n-to-2ⁿ decoder can be built from smaller decoders plus an ENABLE structure — the general pattern uses the TOP (most significant) input bits to select WHICH smaller sub-decoder is enabled, while the remaining LOWER bits feed identically into every sub-decoder in parallel (only the enabled one actually produces active outputs).

PRIORITY ENCODERS

An ENCODER is the structural INVERSE of a decoder: it takes 2ⁿ inputs and produces the n-bit BINARY CODE of whichever single input is asserted. An ordinary encoder FAILS the moment more than one input is asserted simultaneously (its output becomes ambiguous, since it has no way to indicate which of several simultaneously-active inputs should "win").

A PRIORITY ENCODER resolves this ambiguity directly: it outputs the binary code of the HIGHEST-PRIORITY (conventionally, the highest-INDEXED) asserted input among potentially several simultaneously active ones, together with an additional VALID (or "GS," group-signal) output bit that is asserted only when AT LEAST ONE input is active at all.

GATE TRAP: forgetting that a priority encoder needs this DISTINCT valid-output bit is a common oversight — without it, the ALL-INPUTS-ZERO case (no input asserted at all) is INDISTINGUISHABLE, purely from the numeric output code, from the case where input 0 SPECIFICALLY is the one asserted input (since both cases would otherwise produce the identical all-zero output code) — the valid bit is the ONLY way to tell these two genuinely different situations apart.

ADDERS: THE LOGIC-LEVEL VIEW

The FULL numeric derivation of adder arithmetic already lives in the previous computer-arithmetic chapter; this chapter's concern is the LOGIC-LEVEL structure and DELAY behaviour of the actual circuits.

A HALF ADDER (two inputs, no carry-in support) computes Sum = A⊕B, Carry = A·B — directly the truth table for adding two single bits with no incoming carry to account for.

A FULL ADDER (three inputs — A, B, and a carry-in Cin) computes Sum = A⊕B⊕Cin, and Cout = AB+BCin+ACin — this Cout expression is exactly the MAJORITY FUNCTION of the three inputs (true whenever AT least two of the three are true), which makes direct intuitive sense: a carry is generated out of this bit position exactly when at least two of the three quantities being combined (the two data bits and the incoming carry) are 1.

A full adder can be built from TWO HALF ADDERS plus one OR GATE: the first half adder computes A⊕B (call it S₁) and AB (call it C₁); the second half adder computes S₁⊕Cin (giving the final Sum, since (A⊕B)⊕Cin = A⊕B⊕Cin exactly) and S₁·Cin (call it C₂); the final OR gate combines C₁+C₂ to produce Cout — verified algebraically: C₁+C₂ = AB + (A⊕B)Cin = AB + (AB′+A′B)Cin = AB+AB′Cin+A′BCin+... simplifying via the distributive and absorption laws recovers exactly AB+BCin+ACin, confirming the two-half-adder-plus-OR construction produces the correct majority function.

RIPPLE-CARRY VS CARRY-LOOKAHEAD DELAY

A RIPPLE-CARRY ADDER chains n full adders together via their carry lines (stage i's carry-out feeds directly into stage i+1's carry-in) — the WORST-CASE delay to the FINAL carry-out grows LINEARLY with n, since stage i+1 genuinely cannot begin computing its own carry-out until stage i's carry-out has settled; this sequential dependency, ripping through every stage one after another, is exactly what gives the ripple-carry adder its name and its defining weakness.

A CARRY-LOOKAHEAD ADDER (CLA) avoids this sequential dependency by computing every carry directly from GENERATE and PROPAGATE signals, rather than waiting for the previous stage: define Gᵢ=AᵢBᵢ (bit position i GENERATES a carry on its own, regardless of any incoming carry, exactly when both its own inputs are 1) and Pᵢ=Aᵢ⊕Bᵢ (bit position i PROPAGATES an incoming carry through to its own carry-out exactly when its two inputs differ). Then Cᵢ₊₁ = Gᵢ+PᵢCᵢ, and EXPANDING this recursively removes the ripple entirely: C₂ = G₁+P₁G₀+P₁P₀C₀, and so on — every carry is now computable directly from the P and G signals (themselves computed directly and in parallel from the original inputs) via a fixed two-LEVEL AND-OR logic structure, giving a delay that is a SMALL CONSTANT independent of the bit width (until practical fan-in limits eventually force a hierarchical, block-level cascade for very wide adders).

Worked trace: for a 4-bit ripple-carry adder where each full adder's carry-out takes 2 gate delays after its carry-in is stable, and sum takes 3 gate delays after both its inputs are stable: total worst-case delay to the FINAL carry-out C4 = 4 stages × 2 = 8 gate delays. Total delay to the most-SIGNIFICANT sum bit = delay to C3 (3 stages × 2 = 6, since S3 needs C3, the carry INTO bit 3, not C4) plus 3 more for S3's own sum logic = 9 gate delays total.

GATE TRAP: computing the delay to the final SUM bit by using the FINAL carry-out C4's delay (rather than C3, the carry actually feeding INTO the last bit's own sum logic) is a common off-by-one error — the n-th (final) sum bit needs the carry INTO that bit (Cₙ₋₁ in a 0-indexed n-bit adder), which has rippled through only n−1 stages, not all n, and then needs its OWN additional sum-logic delay on top of that.

For a carry-lookahead adder computing all carries in a fixed 2 gate-delay levels (one level for generate/propagate, one more for the combined carry equation) regardless of bit width: C4 is ready in just 2 gate delays, and every sum bit needs one more XOR-level delay on top of its own carry, giving a worst-case total delay of about 2+3=5 gate delays for ANY width — a dramatic, width-independent advantage over the ripple-carry adder's linearly GROWING delay, and exactly the comparison GATE numeric questions test by asking for total delay at n=8, 16, or 32 bits and comparing the growth rate (linear for ripple-carry; essentially flat, ignoring real hierarchical CLA's logarithmic block-cascade overhead at very large widths, for lookahead).

COMPARATORS AND PARITY CIRCUITS

A single-bit EQUALITY COMPARATOR is simply XNOR: (A⊕B)′=1 exactly when A=B. An n-bit equality comparator ANDs together n such XNOR outputs (all n bit-positions must match for the two n-bit numbers to be equal).

A MAGNITUDE COMPARATOR (producing separate A>B, A=B, A<B outputs) is built bit by bit, starting from the most significant bit and working down — since the comparison between two multi-bit numbers is entirely decided by the HIGHEST bit position at which they actually differ (every lower bit position's comparison outcome is irrelevant once a higher position has already broken the tie), the logic checks the MSB first, and only "falls through" to examine the next lower bit if the current bit position is still tied.

A PARITY GENERATOR computes the XOR of all data bits: for even parity, the generator's output is chosen so the total number of 1-bits (data bits plus the parity bit itself) is even; for ODD parity, the complement of that. A PARITY CHECKER, at the receiving end, RECOMPUTES the same XOR over the received data-plus-parity bits and compares against the expected fixed value (0 for even parity, 1 for odd) to detect whether a single bit was flipped in transmission.

PROGRAMMABLE LOGIC: PROM, PAL, AND PLA

Three programmable logic device families differ in exactly WHICH of their two internal logic arrays (an AND array feeding an OR array, mirroring the canonical two-level SOP structure directly) is FIXED versus PROGRAMMABLE:

A PROM (Programmable Read-Only Memory) has a FIXED AND array (which, being fixed, always decodes all 2ⁿ possible minterms of the n inputs — structurally, exactly an n-to-2ⁿ decoder) and a PROGRAMMABLE OR array (choosing which minterms feed into each output). This is EFFICIENT when many outputs each genuinely need many minterms (since the AND array's cost is fixed regardless), but WASTEFUL when only a few minterms are actually needed per output (the exhaustive AND array decodes every minterm regardless of whether most go unused).

A PAL (Programmable Array Logic) has a PROGRAMMABLE AND array (choosing which specific product terms to generate) but a FIXED OR array (each output sums a FIXED, small SET of specific product-term lines, decided at manufacture, not by later programming). This is generally CHEAPER and faster than a PROM for functions needing only a few product terms per output, but each output is LIMITED to that fixed number of product-term inputs, however many the actual function might otherwise benefit from.

A PLA (Programmable Logic Array) has both arrays PROGRAMMABLE — the most FLEXIBLE of the three, since product terms can be SHARED freely across multiple outputs wherever a common minimised term set allows it, giving the best AREA efficiency when multiple functions genuinely share structure, though historically slower and more costly to manufacture than a PAL.

GATE TRAP: confusing WHICH specific array is fixed between PAL and PLA is one of the most frequently tested facts in this section — PAL has a fixed OR array (and programmable AND), while PLA has BOTH arrays programmable; a question phrased as "which array is fixed in a PAL" is testing exactly this distinction, and mixing up the two device names' fixed/programmable assignment is a purely definitional error worth memorising precisely rather than guessing under pressure.

KEY: counting the number of AND gates (product terms) or fuses needed to realise a GIVEN set of functions differs fundamentally between a PROM and a PLA — a PROM ALWAYS needs the full 2ⁿ minterms decoded per output GROUP regardless of how few minterms any individual output actually uses (no sharing benefit is possible, since the AND plane is already exhaustive by design), while a PLA's actual product-term count depends entirely on whether a COMMON, shared, minimised set of product terms can be found across the multiple target functions — the same set of functions can therefore need dramatically fewer total AND gates on a PLA than the PROM's fixed 2ⁿ-per-output count, precisely because sharing is only possible on the PLA.

STATIC AND DYNAMIC HAZARDS

[[FIG:hazard-boundary-gap]]

A STATIC-1 HAZARD occurs when an output that SHOULD remain steadily at 1 throughout a single input transition instead GLITCHES momentarily down to 0 during that transition — this happens specifically when the circuit's SOP realisation has NO single product term that stays active across the entire transition, even though the output's true logical value never actually changes: two ADJACENT K-map groups meet at a shared boundary edge, but neither group's own product term individually covers both sides of that boundary, so as the input crosses the boundary, one term switches off a moment before (or after) the other term switches on, leaving a brief window where neither term is active and the output incorrectly reads 0.

THE FIX: add a REDUNDANT CONSENSUS TERM covering exactly that shared boundary — this is precisely why the consensus theorem (xy+x′z+yz=xy+x′z), though ALGEBRAICALLY redundant and droppable for pure minimisation purposes, is DELIBERATELY kept in (or specifically added back into) a REAL hardware realisation: the "unnecessary" consensus term yz is exactly the term that stays active across the AB-boundary where the other two terms individually fail to overlap, eliminating the glitch entirely.

A STATIC-0 HAZARD is the exact POS DUAL of a static-1 hazard: an output that should remain steadily at 0 instead glitches momentarily UP to 1 during a transition, fixed by adding a redundant SUM term to the POS realisation (the exact dual repair to the static-1 fix).

GATE TRAP: static-1 and static-0 hazards require DIFFERENT fixes applied to DIFFERENT realisation forms — a static-1 hazard is fixed by adding a redundant PRODUCT term to an SOP realisation, while a static-0 hazard is fixed by adding a redundant SUM term to a POS realisation; attempting to apply the SOP-style fix (a redundant product term) to a POS circuit exhibiting a static-0 hazard, or vice versa, does not correctly address the actual hazard present.

DYNAMIC HAZARDS (where an output transitions more than once during what should be a single, clean transition — for instance 0→1→0→1 instead of a clean single 0→1) arise specifically in circuits with RECONVERGING signal paths of UNEQUAL delay across MULTIPLE LEVELS of logic — a signal that splits, travels through different numbers of logic levels along two different paths, and then reconverges can arrive at slightly different times along each path, causing the output to "bounce" more than once before settling.

KEY: dynamic hazards CANNOT occur in a genuinely TWO-LEVEL (plain SOP or POS) circuit — they specifically require MULTIPLE levels of logic with reconverging paths of unequal length, a structural precondition a flat two-level circuit simply does not have (every path from any input to the single output passes through exactly the same small, fixed number of logic levels); recognising this distinguishing fact is a fast way to rule dynamic hazards IN or OUT of consideration purely from a circuit's described STRUCTURE, without needing to trace through its specific timing in detail.

XOR/XNOR GATE-COUNT CLASSICS AND PARITY TREES

Since {NAND} and {NOR} are each individually functionally complete (established in the Boolean algebra chapter), it is worth knowing exactly how many NAND-only or NOR-only gates are needed to build an XOR (or XNOR) function directly, since this specific gate-count is a recurring, easily-checked numeric fact.

1. XOR FROM NAND GATES needs a MINIMUM of 4 NAND gates: XOR(A,B) = AB′+A′B, and building this from NAND alone requires first computing NAND(A,B)=(AB)′ (one gate), then using it together with A and B in two more NAND gates to build the two needed AND-like terms, and a final NAND to combine them — the standard 4-NAND XOR construction is a fixed, well-known circuit worth recognising by GATE COUNT alone.
2. XOR FROM NOR GATES needs a MINIMUM of 5 NOR gates — one MORE than the NAND construction, since NOR's dual relationship to AND/OR requires an extra inversion step somewhere in the standard construction that the NAND version avoids.
3. XNOR is the exact logical COMPLEMENT of XOR, so building XNOR from NAND needs 5 (the 4-NAND XOR construction plus one more NAND used purely as an inverter on the final output), and XNOR from NOR needs 4 (the reverse relationship — NOR's natural output-inverting structure makes XNOR its "native" 4-gate construction, with XOR needing the extra 5th gate instead).

PARITY CIRCUITS ARE XOR TREES: the parity (XOR) of n bits is 1 exactly when an ODD number of those bits are individually 1 — this follows directly from XOR's own defining truth table extended associatively (XOR is associative and commutative, so "the XOR of n bits" is well-defined regardless of grouping order), and a HARDWARE parity circuit for n bits is typically built as a TREE of 2-input XOR gates, needing exactly n−1 XOR gates arranged in ⌈log₂n⌉ levels for a balanced tree (versus a linear chain of n−1 XORs needing n−1 sequential levels) — the same tree-versus-chain delay trade-off already familiar from the carry-lookahead-versus-ripple-carry comparison above, now applied to a different combinational structure entirely.

KEY: recognising that a parity circuit is STRUCTURALLY identical to a balanced binary tree of XOR gates (rather than a special, separately-derived circuit) means the SAME tree-depth-versus-chain-depth delay analysis used for adders applies here directly — a BALANCED XOR tree computes n-bit parity in O(log n) gate delays, while a naive LINEAR chain of XORs takes O(n) gate delays, exactly mirroring the carry-lookahead-versus-ripple-carry trade-off, confirming that "arrange operations in a balanced tree rather than a sequential chain to trade more hardware for less delay" is a general combinational-design principle, not a fact specific to adders alone.

THE BCD ADDER: A DIRECT APPLICATION OF THE +6 CORRECTION

A BCD ADDER adds two BCD-encoded decimal digits using an ORDINARY 4-bit binary adder as its core, with an ADDITIONAL correction stage applied afterward — this is the HARDWARE realisation of the +6 correction rule already derived in the number-systems chapter, now built as an actual combinational circuit rather than performed by hand.

1. Add the two 4-bit BCD digits using a standard 4-bit binary adder, producing a raw 4-bit sum plus a carry-out.
2. CORRECTION LOGIC checks whether the raw sum EXCEEDS 9 (falls into one of the six invalid BCD patterns) OR a carry-out was already generated from the binary addition — if EITHER condition holds, ADD the correction 0110 (6) to the raw sum using a SECOND 4-bit binary adder, and assert the DECIMAL carry-out; otherwise, the raw sum is already valid, and no correction is applied.
3. The "does the raw sum exceed 9" check is itself a small combinational circuit — recognising that sums of 10 through 15 all have EITHER bit 3 set together with (bit 2 OR bit 1) set, OR the binary carry-out already set, gives a compact Boolean expression for exactly when correction is needed, built directly from the raw sum's own bit pattern with no additional external comparator circuitry required.

CODE CONVERTERS

A CODE CONVERTER is a combinational block whose job is translating one binary encoding into another — binary-to-Gray, Gray-to-binary, binary-to-excess-3, and BCD-to-seven-segment are all standard examples, and every one of them is, structurally, an ordinary Boolean function of its inputs, designed by writing out the target code's truth table against the source code and then minimising exactly as with any other combinational function.

1. Since binary-to-Gray and Gray-to-binary conversions were already derived algebraically in the number-systems chapter (each output bit expressed as an XOR chain of specific input bits), building the actual combinational circuit is immediate: each output bit's formula IS directly the Boolean expression for one output line, requiring only as many two-input XOR combiners as there are output bits beyond the copied most-significant one.
2. A BCD-TO-SEVEN-SEGMENT DECODER is a slightly larger example of the same idea: it has 4 inputs (one BCD digit) and 7 outputs (one per segment of a standard seven-segment display), with each output's truth table derived from which segments must be illuminated to display each of the 10 valid BCD digit patterns — the six INVALID BCD input combinations (1010 through 1111) are typically treated as DON'T CARES in the design's own truth table, since a well-formed BCD source is assumed never to present them, letting the resulting per-segment expressions be minimised more aggressively than if those six patterns had to be assigned a specific, meaningful output.
3. Recognising a code converter design task as "just another Boolean minimisation problem, once the target truth table is written out" — rather than as a fundamentally different design activity — connects this section directly back to the K-map and Quine-McCluskey minimisation techniques from the Boolean algebra chapter, applied here to per-output-bit truth tables instead of a single function.

COMPARING IMPLEMENTATION CHOICES FOR THE SAME FUNCTION

A single Boolean function can be realised through any of several of this chapter's named blocks — a decoder plus an external combiner, a multiplexer, a programmable array — and comparing their relative costs directly is a recurring, genuinely useful exercise, since no one implementation choice is universally cheapest across every possible function.

1. A DECODER-BASED realisation always needs the FULL 2ⁿ-line decoder (regardless of how few minterms the target function actually uses) plus one combiner input per ON-set minterm — efficient when the function's ON-set is large relative to 2ⁿ (using most of the decoder's already-committed output lines), wasteful when the ON-set is small (most of the decoder's outputs going unused).
2. A MULTIPLEXER-based realisation (using the full 2ⁿ-to-1 approach, no Shannon trick) needs exactly the same 2ⁿ-line "selection" structure internally, but requires NO external combiner at all — the mux's own internal selection logic already performs the equivalent of the decoder-plus-combiner's job, trading the combiner's cost for the mux's own built-in structure.
3. The SHANNON-EXPANSION mux trick specifically HALVES the needed select-line count (and correspondingly the internal selection structure's size) at the cost of needing a small amount of EXTRA logic (an inverter, at most) on the residue variable fed into the data inputs — a genuine net saving whenever that extra residue logic is cheaper than the halved selection structure it replaces, which is very often the case.
4. A function needed across MULTIPLE related outputs (sharing some minterms in common) benefits most from a PLA-style realisation specifically BECAUSE of this sharing — a single decoder-based or mux-based realisation, by contrast, is inherently a SINGLE-OUTPUT-at-a-time design style, and achieving the same sharing benefit across several outputs using decoders or muxes requires deliberately routing a single decoder's outputs to feed multiple external combiners, which is exactly what a PLA's programmable AND-array structure already does natively.

This comparison is worth approaching as a genuine cost-benefit exercise on a case-by-case basis, rather than memorising a single "best" implementation style — the actual best choice depends on the specific function's ON-set size relative to 2ⁿ, on whether multiple related outputs need to be realised together (favouring shared-structure approaches like a PLA), and on which specific resource (component count, selection-structure width, external combiner count) is genuinely the scarce or expensive one in the design context at hand.

ITERATIVE (BIT-SLICED) COMBINATIONAL DESIGN

Several blocks already covered in this chapter — the ripple-carry adder, the n-bit magnitude comparator, an n-bit equality checker — share a common structural pattern worth naming explicitly: each is built from identical single-bit "slices" chained together, with each slice taking a small amount of information from its neighbour (a carry, a "still tied so far" flag) and passing an updated version of that same kind of information forward to the next slice.

1. This ITERATIVE (or "bit-sliced," or "cascaded") design style trades a potentially large, hand-designed n-input truth table for a SMALL, fixed single-bit design (the "slice") replicated n times — a massive reduction in DESIGN effort (only one slice's truth table needs to be worked out, however large n eventually needs to be), at the cost of the exact same kind of SEQUENTIAL-PROPAGATION delay penalty already seen in the ripple-carry adder's linearly growing worst-case delay.
2. The magnitude comparator's bit-by-bit, MSB-down structure covered above is a second concrete instance of this exact pattern: each bit-slice compares its own pair of input bits and, if they are EQUAL, passes the decision along unchanged to the next (less significant) slice; if they DIFFER, the slice settles the entire comparison immediately and no further slices' outputs matter at all — structurally identical to a carry PROPAGATING or being GENERATED at each adder stage, just carrying a different kind of one-bit decision forward instead of a numeric carry.
3. Recognising the SAME iterative-slice structural pattern across superficially different-looking blocks (an adder's carry, a comparator's "still tied" flag, an n-bit equality checker's running AND of per-bit XNOR results) is worth doing explicitly, since it means a single mental template — "one small slice design, replicated n times, with ONE piece of state passed forward between slices" — covers several apparently distinct named circuits at once, rather than needing n separate, unrelated designs memorised independently.

WORKED PROBLEMS

1. MUX SIZING. How large a MUX (in terms of select lines and data inputs) is needed to implement a 5-variable function (a) with no variables pushed onto the data inputs, and (b) using the Shannon-expansion trick with one variable pushed onto the data inputs? (a) A full 2⁵=32-to-1 MUX, with 5 select lines and 32 hardwired data inputs. (b) A 2⁴=16-to-1 MUX, with 4 select lines and 16 data inputs, each set to 0, 1, the fifth variable, or its complement.

2. MUX IMPLEMENTATION. Implement f(A,B,C)=Σm(0,3,5,6) on a 4-to-1 MUX with A,B as select and C as the residue variable. Row AB=00: minterms 0(C=0)=1,1(C=1)=0 → D₀=C′. Row AB=01: minterms 2(C=0)=0,3(C=1)=1 → D₁=C. Row AB=10: minterms 4(C=0)=0,5(C=1)=1 → D₂=C. Row AB=11: minterms 6(C=0)=1,7(C=1)=0 → D₃=C′.

3. DECODER-BASED FUNCTION REALISATION. Using a 3-to-8 decoder plus an external OR combiner, realise f(A,B,C)=Σm(1,4,6). Feed A,B,C into the decoder's inputs; OR together the decoder's output lines 1, 4, and 6 (and only those three) to produce f directly.

4. PRIORITY ENCODER. A 4-input priority encoder (inputs I0-I3, I3 highest priority) has I1 and I3 both asserted simultaneously, all others 0. State the encoder's output code and valid bit. Since I3 has the highest priority among the asserted inputs, the output code is 11 (binary for 3), and the valid bit is asserted (1), correctly indicating at least one input is active despite I1 also being asserted but overridden by priority.

5. RIPPLE-CARRY DELAY, MSB SUM. An 8-bit ripple-carry adder has each full adder's carry-out ready 2 ns after its carry-in is stable, and each sum output ready 3 ns after both its inputs (including carry-in) are stable. Find the worst-case delay to the most significant sum bit S7. The carry must ripple through bits 0 to 6 (7 stages) before reaching bit 7's carry-in C7: 7×2=14 ns. S7 then needs 3 more ns: 14+3=17 ns total — NOT the 8×2=16 ns needed for the final carry-out C8, since S7 depends only on C7 (7 stages), not C8 (8 stages).

6. PLA VS PROM PRODUCT-TERM COUNT. Two functions f1=Σm(0,1,2) and f2=Σm(0,1,3) (each over 2 variables, so 4 total possible minterms) are to be realised together. Compare the number of AND gates (product terms) needed on a PROM versus a PLA, given that f1 and f2 share the minterms 0 and 1 in common. A PROM decodes all 2²=4 minterms unconditionally (4 AND gates total, regardless of sharing), then ORs the relevant ones into each output. A PLA can instead generate only the DISTINCT minterms actually needed across BOTH functions combined — here, minterms {0,1,2,3} are collectively needed (f1 needs 0,1,2; f2 needs 0,1,3; union is all 4) so in this specific case the PLA also needs all 4 product terms, illustrating that PLA sharing helps only when the functions' minterm sets are NOT collectively exhaustive — had f1 and f2 instead needed only 3 total distinct minterms between them, the PLA would need just 3 AND gates versus the PROM's fixed 4.

7. STATIC-1 HAZARD IDENTIFICATION AND FIX. A circuit realises f=AC+A′B (two SOP terms) and exhibits a static-1 hazard when B=C=1 and A transitions. Identify the redundant consensus term needed to fix it, and confirm it matches the consensus theorem's pattern. Matching AC+A′B against the consensus-theorem pattern xy+x′z (with x=A, y=C, z=B): the consensus term is yz=CB=BC. Adding it gives f=AC+A′B+BC, eliminating the static-1 hazard at the A-transition boundary where B=C=1, exactly the consensus theorem's redundant term used deliberately for hazard elimination rather than dropped for pure algebraic minimality.

8. DYNAMIC HAZARD STRUCTURAL CHECK. Can a plain two-level SOP circuit (a single layer of AND gates feeding one OR gate) ever exhibit a dynamic hazard? Explain using the structural requirement for dynamic hazards. No — dynamic hazards require MULTIPLE levels of logic with RECONVERGING paths of unequal delay; a two-level SOP circuit has every signal path passing through exactly two logic levels (one AND, one OR) with no reconverging paths of differing length at all, so the structural precondition for a dynamic hazard is simply absent regardless of the specific function being realised.

9. XOR/XNOR GATE COUNT. How many NAND gates are needed to build an XNOR function directly? 5 — the 4-gate XOR-from-NAND construction plus one more NAND used as a final inverter, since XNOR is exactly the complement of XOR.

10. PARITY TREE DEPTH. An 8-bit parity circuit is built as a balanced binary tree of 2-input XOR gates. How many gates total, and how many levels of delay? Total gates: 8−1=7 (one fewer XOR than the number of input bits, exactly as any binary combining tree needs n−1 combining operations for n leaves). Levels: ⌈log₂8⌉=3, versus a naive linear chain's 7 sequential levels — a direct 7-to-3 delay improvement from balancing the tree.

11. BCD ADDER CORRECTION CHECK. Add BCD digits 6 (0110) and 7 (0111) using a 4-bit binary adder, and determine whether the +6 correction is needed. Raw binary sum: 0110+0111=1101 (13 in binary, no carry-out from the 4-bit addition itself). Since 13>9, correction is needed: 1101+0110=10011, giving a decimal carry-out of 1 and a corrected digit of 0011 (3) — matching 6+7=13 (carry 1, digit 3) exactly.

12. CODE CONVERTER SIZING. A binary-to-Gray code converter takes a 5-bit binary input. How many XOR combiners are needed to produce the 5-bit Gray output? The MSB is copied unchanged (no combiner needed for it), and each of the remaining 4 output bits is the XOR of two adjacent binary input bits — so exactly 4 two-input XOR combiners are needed in total.

13. ITERATIVE COMPARATOR TRACE. Using the bit-sliced, MSB-down comparator design, compare 4-bit values A=1011 and B=1010. At the MSB (bit 3): both are 1, tied, pass along. Bit 2: both are 0, tied, pass along. Bit 1: both are 1, tied, pass along. Bit 0: A has 1, B has 0 — they differ here, and since A's bit is the larger one at the FIRST differing position (scanning from the MSB down), the comparator settles A>B immediately, with no further (there are no further, since bit 0 is the last) slices needed.

14. TREE-VS-CHAIN GENERAL APPLICATION. A running 8-input logical AND is needed. Compare the worst-case delay of a chain realisation (7 two-input AND combiners in sequence) against a balanced tree realisation (7 two-input AND combiners arranged in a balanced binary tree). Chain: 7 sequential levels, each contributing its own combiner delay — total delay proportional to 7. Tree: ⌈log₂8⌉=3 levels, since a balanced tree of 8 leaves needs only 3 levels of pairwise combining — a direct 7-to-3 reduction in worst-case delay, using the identical total component count (7 two-input combiners) in both realisations, illustrating that the SAME hardware cost can buy very different delay depending purely on how the components are structurally arranged.

15. BCD-TO-SEVEN-SEGMENT DON'T-CARE USE. A BCD-to-seven-segment decoder's segment-g output (the middle horizontal segment) happens to be 0 for BCD digits 0, 1, and 7, and 1 for digits 2 through 6, 8, and 9. Treating BCD inputs 10-15 as don't-cares, explain in general terms why this typically allows a SIMPLER minimised expression for segment-g than would be obtained if those six patterns instead had to be assigned a specific fixed value. Don't-cares give the minimisation process FREEDOM to assign whichever of 0 or 1 EACH invalid pattern such that it best enlarges an adjacent group of valid-input 1s (or 0s) — since a real BCD source is assumed to never actually present the six invalid codes 1010-1111, there is no correctness cost to exploiting this freedom, and the resulting minimised expression for segment-g can end up with strictly fewer literals than if those six unused input combinations had been forced to a specific committed value, exactly the same don't-care-driven simplification already demonstrated on a K-map in the Boolean algebra chapter.

16. IMPLEMENTATION COST COMPARISON. A 3-variable function has an ON-set of just 1 minterm out of 8 possible. Compare the relative efficiency of a decoder-based realisation versus a mux-based realisation (full 8-to-1, no Shannon trick) for this specific function. Decoder-based: needs the full 8-line decoder plus a combiner with just 1 active input (the single ON-set minterm) — 7 of the 8 decoded lines go entirely unused for this particular function. Mux-based: needs the same 8-way internal selection structure, but with only ONE data input hardwired to 1 and the other seven hardwired to 0, requiring no external combiner at all — for a function with such a SMALL ON-set, the mux-based approach avoids the decoder's separate combiner stage entirely, making it the more economical choice in component count for this specific sparse function.

17. MULTIPLE-OUTPUT SHARING. Two 3-variable functions f1=Σm(0,1,2,3) and f2=Σm(0,1,4,5) share minterms 0 and 1 in common. Explain briefly why a shared-decoder-with-two-combiners realisation (one 8-line decoder feeding two separate external combiners, one per function) is more economical than building two ENTIRELY SEPARATE decoders, one per function. A single 8-line decoder already produces every minterm line either function could possibly need, and both f1's combiner and f2's combiner can tap directly into the SAME shared set of 8 decoded lines, each selecting only the specific lines its own function requires (0,1,2,3 for f1; 0,1,4,5 for f2) — building two entirely separate 8-line decoders would duplicate the ENTIRE decoding structure needlessly, when a single shared decoder already provides every minterm line both outputs could ever need, with only the two small external combiners actually differing between the two functions — precisely the same sharing benefit a PLA's programmable AND-array structure is specifically designed to exploit automatically across many outputs at once, here achieved manually with a single fixed decoder feeding two independently-wired combiners instead.

18. CODE CONVERTER DON'T-CARE COUNTING. A BCD-to-excess-3 code converter has 4 inputs and 4 outputs. How many of the 16 possible input combinations are genuine, required mappings, and how many can be treated as don't-cares in the design? Since BCD only uses input patterns 0000 through 1001 (10 valid digit codes), exactly 10 of the 16 possible 4-bit input combinations are genuine, required mappings; the remaining 6 (patterns 1010 through 1111) can be treated as don't-cares in each output bit's own minimisation, since a well-formed BCD source is assumed never to present them — exactly the same 10-valid-plus-6-don't-care split that made the BCD adder's own correction logic, and the BCD-to-seven-segment decoder's own segment expressions, simplify more aggressively than a converter built without exploiting this freedom at all, confirming that this specific 10-and-6 split is a recurring structural feature of BCD-based combinational design generally, not a coincidence limited to any one of these three specific circuits, and worth recognising immediately on sight whenever a new BCD-related design task is encountered, rather than being re-derived laboriously from scratch each and every time such a task happens to come up in a slightly different guise.

This closing observation about the 10-and-6 split is itself a small instance of the chapter's larger recurring theme: a structural feature discovered once, in one specific circuit, very often turns out to recur across an entire FAMILY of related circuits sharing the same underlying encoding. Actively looking for such recurring structure — rather than treating each new named block as an entirely fresh design problem to be solved from first principles every single time — is consistently the fastest, most reliable route to a correct, well-justified answer on any combinational-design question this chapter's material could plausibly be applied to. This holds whether the question names one of the specific standard blocks covered explicitly above by its usual textbook name, or instead presents itself, at first glance, as an apparently unfamiliar variation dressed up in slightly different terminology, or wrapped inside a longer, multi-part design scenario combining several of this chapter's individual named blocks into one larger overall system spanning multiple stages of logic connected end to end. Each individual stage remains recognisable on its own the moment it is mentally isolated, one stage at a time, from all of the surrounding stages built up around it in the larger overall system design being examined.

A practical way to build this pattern-recognition skill deliberately is to keep a short running list, updated after every practice problem, of which named building block each new question actually turned out to reduce to underneath its surface description. Over enough practice problems, that list itself becomes the most valuable single revision resource for this chapter, since it reveals directly which specific disguises — an unusual variable naming convention, an unfamiliar physical application described in the question's preamble, or a block described only by its truth table rather than by its standard name — a given examiner has tended to favour, letting later practice focus precisely on recognising those particular disguises quickly under real time pressure rather than re-deriving each underlying block completely from scratch every single time.

CARRYING THIS FORWARD

Every named block in this chapter is best remembered not as an independent fact to memorise, but as a specific HARDWARE INSTANTIATION of an idea already built out fully in the Boolean algebra chapter: a decoder IS a physical minterm generator (the canonical Σm form made concrete in silicon); a multiplexer IS Shannon's expansion, with its select lines choosing which half of the expansion to output; a PROM's fixed AND array IS an exhaustive minterm decoder exactly like a plain decoder; and a static hazard's fix IS the consensus theorem's "unnecessary" term, kept deliberately rather than dropped, because REAL hardware transitions happen gradually through time in a way pure Boolean algebra's instantaneous truth-table view does not capture. Recognising this throughout — that this chapter's material is Boolean algebra given a physical, timing-aware embodiment, not a separate new body of facts — is what makes the individually-named blocks (mux, decoder, encoder, adder, comparator, PROM/PAL/PLA, hazard) feel like a connected, derivable system rather than a long list to be recalled independently of one another under time pressure.

KEY: three genuinely distinct DELAY-SAVING strategies recur throughout this chapter, and distinguishing which one a specific design technique is using is worth doing explicitly rather than lumping them all together as "faster circuits": carry-lookahead addition and the balanced XOR parity tree both trade MORE hardware for a LOGARITHMIC (or constant, for a fixed-width lookahead block) rather than linear delay, by computing several results in PARALLEL from shared intermediate signals instead of waiting for a sequential chain to complete; the two-half-adders-plus-OR full-adder construction and the iterative bit-sliced comparator design both trade DESIGN EFFORT (working out one small, reusable slice once) for a larger overall circuit, without necessarily changing the fundamental delay characteristics at all; and the hazard-elimination consensus term trades a small amount of EXTRA hardware (one redundant product term) for eliminating a transient GLITCH that pure static Boolean-algebra minimisation would never even reveal as a problem in the first place, since it is purely a TIMING phenomenon invisible to a truth table. Confusing these three motivations — is this technique buying speed, buying design simplicity, or buying timing correctness — is a common source of imprecise reasoning about why a specific combinational technique is actually used.

A second cross-cutting theme worth naming explicitly is the recurring TREE-VERSUS-CHAIN structural choice: a ripple-carry adder is a CHAIN (each stage waits for its immediate predecessor), while a carry-lookahead adder computes every carry from a shared set of generate/propagate signals in effectively PARALLEL fashion; a naive linear XOR chain for parity is likewise a CHAIN, while a balanced binary XOR tree computes the same result with logarithmically fewer sequential levels. This same chain-versus-tree choice recurs constantly in real digital design far beyond the two specific examples worked out in full here — any combinational function built by combining n inputs pairwise (a multi-input AND, a multi-input OR, a running maximum or minimum, a wide equality check) admits both a chain realisation (simple to design, O(n) delay) and a tree realisation (more hardware, O(log n) delay), and recognising this as a single general structural choice — rather than as a separate fact specific to adders, or specific to parity circuits — is one of the most transferable ideas in this entire chapter, directly applicable to combinational structures never explicitly named here at all.

A final connecting observation ties this chapter back to the very first one on Boolean algebra: the key skill trained across both chapters is recognising when a seemingly bespoke circuit or expression is actually a NAMED, standard pattern in disguise — a static hazard is a disguised instance of the consensus theorem; a decoder's outputs are disguised minterms; a multiplexer's data-input assignment is a disguised Shannon expansion; an iterative comparator slice is a disguised carry-propagation structure wearing a different label. Each of this chapter's "GATE TRAP" warnings is, on inspection, a warning about mistaking one of these disguised patterns for something else entirely (a bare decoder mistaken for a complete function realiser; a static-0 hazard's fix mistaken for a static-1 hazard's fix; a PAL's fixed array mistaken for a PLA's programmable one) — and the single most durable habit this chapter can leave behind is the instinct to ask, on encountering an unfamiliar-looking combinational circuit, "which one of the small number of standard patterns from this chapter is this circuit secretly an instance of," before attempting to analyse it entirely from first principles every single time.
`
};
