// Textbook chapter: Sequential Circuits.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['digital-sequential'] = {
  figs: [
    {
      id: 'master-slave-ff',
      caption: 'A master-slave flip-flop: the master latch is transparent while the clock is high, the slave transparent while it is low. Data can only propagate all the way through during the brief instant the clock switches, giving edge-triggered behaviour from two level-sensitive latches.',
      svg: '<svg viewBox="0 0 300 110" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><rect x="20" y="20" width="100" height="60"/><rect x="160" y="20" width="100" height="60"/></g><g font-size="11" fill="currentColor" text-anchor="middle"><text x="70" y="45">MASTER</text><text x="70" y="62">(enabled when CLK=1)</text><text x="210" y="45">SLAVE</text><text x="210" y="62">(enabled when CLK=0)</text></g><line x1="120" y1="50" x2="160" y2="50" stroke="currentColor" stroke-width="1.2"/><text x="10" y="45" font-size="10" fill="currentColor">D</text><text x="270" y="45" font-size="10" fill="currentColor">Q</text></svg>'
    },
    {
      id: 'setup-hold-window',
      caption: "The setup/hold window around a clock edge: data must be stable for at least Tsetup before the edge and Thold after it. A synchronous path's maximum clock delay and minimum path delay are each checked against one end of this window.",
      svg: '<svg viewBox="0 0 300 100" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><line x1="150" y1="10" x2="150" y2="90" stroke="currentColor" stroke-width="1.5"/><g stroke="currentColor" stroke-width="1" stroke-dasharray="3,2"><line x1="110" y1="10" x2="110" y2="90"/><line x1="180" y1="10" x2="180" y2="90"/></g><g font-size="10" fill="currentColor" text-anchor="middle"><text x="150" y="5">clock edge</text><text x="130" y="98">Tsetup</text><text x="165" y="98">Thold</text></g></svg>'
    }
  ],
  text: `
WHY SEQUENTIAL CIRCUITS, AND THE ONE NEW IDEA THEY ADD

Every combinational circuit in the previous chapter had an output determined entirely by its current inputs. A sequential circuit adds exactly one new idea on top of that: MEMORY — an output that also depends on the circuit's PAST behaviour, stored as an internal STATE. This chapter builds that idea up in three layers: first, the basic memory CELL (the latch, then the flip-flop, each storing a single bit); second, a small library of NAMED flip-flop types (SR, JK, D, T) distinguished by how their inputs control the next stored bit; and third, a systematic DESIGN PROCEDURE for building an entire sequential circuit (a counter, or any other state machine) out of several flip-flops plus combinational logic. Every one of these three layers builds directly on the previous chapter's combinational-circuit material — a flip-flop is combinational feedback wired carefully to avoid instability, and a sequential circuit's DESIGN procedure is, at its final step, nothing more than ordinary combinational (K-map) minimisation applied to a specially-constructed table.

LATCHES: LEVEL-SENSITIVE MEMORY

A LATCH is LEVEL-SENSITIVE: it is TRANSPARENT (its output follows its input immediately) for as long as its enable (or clock) signal is asserted, and it "FREEZES" its current output — genuinely remembering it — only once that enable signal is de-asserted.

The SR LATCH is the base memory cell everything else in this chapter builds from. Built from two cross-coupled NOR gates (an active-HIGH SR latch): S=1,R=0 SETS the output to Q=1; S=0,R=1 RESETS it to Q=0; S=0,R=0 HOLDS whatever the output already was (the memory behaviour); S=1,R=1 is the FORBIDDEN state, forcing Q=Q′=0 simultaneously — a genuinely INVALID output pair (Q and Q′ are supposed to always be complements of each other, and this state breaks that guarantee), and the output becomes UNPREDICTABLE the moment S and R are both released back to 0 together (the circuit "races" between two equally valid resting states with no way to determine which one it settles into).

A NAND-based SR latch (built from cross-coupled NAND gates, using ACTIVE-LOW inputs S′,R′) has the exact DUAL forbidden state: S′=R′=0 is now the forbidden combination (rather than S=R=1), since NAND gates invert the same underlying logic the NOR-based latch uses directly.

FLIP-FLOPS: EDGE-TRIGGERED MEMORY

A FLIP-FLOP is EDGE-TRIGGERED: rather than being transparent throughout an entire clock phase, it samples its inputs and updates its output only at a single CLOCK EDGE (rising or falling), remaining completely stable at every other instant, including throughout the rest of the clock period during which the level-sensitive latch above would still be transparent and vulnerable to any input glitch.

[[FIG:master-slave-ff]]

Flip-flops are built from a MASTER-SLAVE PAIR of latches specifically to achieve this edge-triggered behaviour cleanly: the MASTER latch is transparent while the clock is in one phase (say, clock HIGH), and the SLAVE latch is transparent while the clock is in the other phase (clock LOW) — since the two latches are never transparent at the same time, data can only propagate all the way from the flip-flop's input to its output during the brief TRANSITION between the two phases, giving the appearance of sampling at a single clean edge rather than being transparent throughout an entire level.

1. WHY a single level-sensitive latch, directly clocked, would NOT give correct edge-triggered behaviour on its own: if a latch were simply enabled for an entire clock HIGH phase (rather than paired with a slave), its output could keep CHANGING throughout that whole phase in response to any change at its input — a genuine RACE-THROUGH problem, where the latch's own OUTPUT could feed back (directly or through combinational logic) into its OWN input before the clock phase ends, causing multiple, uncontrolled updates within what should have been a single, clean state transition. The master-slave structure eliminates this specific race by ensuring the two halves are never simultaneously transparent, so feedback from the slave's output can never reach back through to affect the master while the master is still open to new input.

CHARACTERISTIC TABLES AND EQUATIONS

Each named flip-flop type is distinguished entirely by its CHARACTERISTIC TABLE (giving the next state Q⁺ as a function of the current state Q and the flip-flop's own inputs) and the corresponding CHARACTERISTIC EQUATION (the same information compressed into one Boolean formula).

The SR FLIP-FLOP: S=0,R=0 holds (Q⁺=Q, no change). S=0,R=1 resets (Q⁺=0). S=1,R=0 sets (Q⁺=1). S=1,R=1 is FORBIDDEN, exactly inheriting the underlying SR latch's own forbidden state. Characteristic equation: Q⁺ = S+R′Q, with the added RESTRICTION that SR=0 must always hold (S and R may never both be 1 simultaneously) — this restriction is not optional decoration on the equation; it is the formal statement of "the forbidden state must never actually be applied."

The JK FLIP-FLOP: J=0,K=0 holds (Q⁺=Q). J=0,K=1 resets (Q⁺=0). J=1,K=0 sets (Q⁺=1). J=1,K=1 TOGGLES (Q⁺=Q′) — this is the crucial difference from SR: the JK flip-flop RESOLVES the SR forbidden case directly, by DEFINING J=K=1 to mean "toggle" rather than leaving it undefined. Characteristic equation: Q⁺ = JQ′+K′Q.

GATE TRAP: it is tempting to assume the SR restriction (S and R must never both be 1) carries over onto JK by analogy, since JK is built directly from an SR-like structure internally — but J=K=1 is a COMPLETELY LEGAL, well-defined input combination for a JK flip-flop (producing a clean toggle), in sharp contrast to the genuinely forbidden S=R=1 case for SR; transferring the SR restriction onto JK by mistaken analogy is one of the most common conceptual errors in this entire chapter.

The D FLIP-FLOP: Q⁺=D always, regardless of the current state Q — the simplest possible characteristic equation, since the output simply COPIES the D input at the next active clock edge, one full clock cycle later, with no dependence on the PREVIOUS state at all.

The T FLIP-FLOP: T=0 holds (Q⁺=Q). T=1 toggles (Q⁺=Q′). Characteristic equation: Q⁺ = T⊕Q — a T flip-flop is exactly a JK flip-flop with its J and K inputs permanently tied together (J=K=T), since tying J=K=0 reduces to JK's own hold case and J=K=1 reduces to JK's own toggle case, covering both of T's two possible behaviours exactly.

GATE TRAP: confusing the JK characteristic equation (Q⁺=JQ′+K′Q) with the T characteristic equation (Q⁺=T⊕Q) is a common memory slip under pressure, especially since T is DERIVED from JK by tying its two inputs together — writing out the small characteristic table directly from first principles (rather than attempting to recall either formula from memory alone) is a safer, more reliable habit whenever there is any doubt about which equation applies to which flip-flop type.

EXCITATION TABLES: DESIGNING BACKWARD FROM A DESIRED TRANSITION

A CHARACTERISTIC table answers "given these inputs and the current state, what is the next state" — the FORWARD direction. An EXCITATION table answers the exact opposite, REVERSE question: "given that the state needs to go from Q to a specific desired Q⁺, what INPUT values would make that happen" — this reverse-lookup table is exactly what is needed when DESIGNING a sequential circuit (where the desired state transitions are already known from a state diagram, and the flip-flop INPUTS that will produce them are what still needs to be determined).

SR EXCITATION: Q=0→Q⁺=0 needs S=0,R=X (X meaning don't-care: setting R=0 or R=1 both leave Q at 0 once S=0, since R=1's reset action produces the same Q⁺=0 result R=0's hold action would also give here). Q=0→Q⁺=1 needs S=1,R=0 (only SET can raise Q from 0 to 1). Q=1→Q⁺=0 needs S=0,R=1 (only RESET can lower it). Q=1→Q⁺=1 needs S=X,R=0.

JK EXCITATION: Q=0→Q⁺=0 needs J=0,K=X. Q=0→Q⁺=1 needs J=1,K=X. Q=1→Q⁺=0 needs J=X,K=1. Q=1→Q⁺=1 needs J=X,K=0.

D EXCITATION: trivial and DON'T-CARE-FREE — D must simply equal the DESIRED Q⁺ directly in every single case (Q=0→Q⁺=0 needs D=0; Q=0→Q⁺=1 needs D=1; Q=1→Q⁺=0 needs D=0; Q=1→Q⁺=1 needs D=1), since D's characteristic equation Q⁺=D already IS the excitation requirement with no inversion or combination needed at all.

T EXCITATION: Q=0→Q⁺=0 needs T=0 (no change required, don't toggle). Q=0→Q⁺=1 needs T=1 (must toggle). Q=1→Q⁺=0 needs T=1 (must toggle). Q=1→Q⁺=1 needs T=0 (no change required).

KEY: the DON'T-CARE entries in the SR and JK excitation tables are not incidental — they are EXACTLY the extra design freedom that lets the eventual flip-flop-input K-maps be minimised more aggressively than they otherwise could be, precisely mirroring the don't-care-driven minimisation already used for combinational circuits with genuinely unused input combinations; a sequential-circuit design task ("design a JK-flip-flop-based counter") hinges ENTIRELY on filling in this excitation table correctly for every required transition, then K-mapping each flip-flop's J and K inputs SEPARATELY, exploiting every available don't-care exactly as in ordinary combinational minimisation.

THE FIVE-STEP SEQUENTIAL DESIGN PROCEDURE

1. DRAW the state diagram (or an equivalent state table) for the required behaviour — list, for every present state and every relevant input combination, what the next state must be.
2. STATE REDUCTION (if applicable — merge any states that are truly EQUIVALENT, meaning they produce identical future behaviour for every possible future input sequence, typically found via an implication or partitioning table) and STATE ASSIGNMENT (choose specific binary codes for each surviving state — a Gray-code assignment, where consecutive states differ in only one bit, often reduces both the resulting logic's complexity and the risk of transient hazards, exactly the K-map-adjacency-style benefit already familiar from the Boolean algebra chapter).
3. For EACH flip-flop, use its EXCITATION table to determine the required flip-flop INPUT values for every present-state-to-next-state transition in the design — this produces one excitation COLUMN per flip-flop input (e.g. a J-column and a K-column, for a JK-based design), each expressed purely as a function of the present-state bits (and any external input the design depends on).
4. K-MAP and minimise EACH flip-flop's input equation SEPARATELY, exploiting every available don't-care from both step 1's unused states and step 3's excitation-table don't-cares — this step is, in every respect, an ORDINARY combinational-minimisation exercise, applied here to the excitation columns rather than to a single target output function.
5. DRAW the final circuit (the flip-flops, connected via their now-minimised input equations) and VERIFY that any UNUSED states either safely fall into don't-care territory or, if the design must be SELF-STARTING, correctly flow BACK into the intended valid cycle rather than becoming trapped in an isolated "LOCK-OUT" loop of their own.

MOD-N COUNTERS: RIPPLE VS SYNCHRONOUS

A MOD-N COUNTER needs AT least ⌈log₂N⌉ flip-flops (the minimum number of bits able to represent N distinct states). A counter that genuinely uses all 2ⁿ possible states of its n flip-flops is called a FULL counter; otherwise, the UNUSED states (as flagged in step 5 above) must be explicitly checked for safe, self-starting convergence back into the valid cycle.

A RIPPLE (ASYNCHRONOUS) COUNTER has each flip-flop toggle directly off the OUTPUT transition of the PREVIOUS stage (a natural fit for T or JK flip-flops wired permanently into toggle mode) — simple to build (just a chain of toggling flip-flops, no combinational input logic needed at all in the basic case), but its cumulative PROPAGATION delay grows LINEARLY, as n×(single flip-flop delay), since each stage must wait for the PREVIOUS stage's output to actually settle before it can begin toggling itself — and during this ripple settling window, the counter's DECODED outputs can transiently show an entirely wrong, intermediate value (a genuine glitch, even though the FINAL settled count is eventually correct).

A SYNCHRONOUS COUNTER has every flip-flop sharing the same clock signal, with each flip-flop's input logic computed directly from the present state (via the five-step design procedure above) rather than chained off a neighbour's output — every flip-flop therefore CHANGES TOGETHER at the same single clock edge, giving a total delay of just one flip-flop delay plus one input-logic delay, ENTIRELY INDEPENDENT of how many bits (how long) the counter actually is — the reason synchronous designs are consistently preferred, and consistently the design style directly compared against ripple counters, whenever speed genuinely matters.

TIMING PARAMETERS: SETUP, HOLD, AND CLOCK-TO-Q

[[FIG:setup-hold-window]]

SETUP TIME (Tsetup): the MINIMUM time before the active clock edge that data must already be STABLE at a flip-flop's input, for that data to be reliably captured on the upcoming edge.

HOLD TIME (Thold): the MINIMUM time after the active clock edge that data must REMAIN stable, again for reliable capture — data changing too soon after the edge, even though it was stable before the edge, can still cause an incorrect capture.

CLOCK-TO-Q DELAY (Tcq): the propagation delay from the active clock edge to the flip-flop's own output actually finishing its change — this is the delay a DOWNSTREAM stage must wait through before its own input can even begin settling.

THE MAXIMUM CLOCK FREQUENCY constraint for a synchronous path between two flip-flops: Tclk ≥ Tcq + Tcombinational(max) + Tsetup — the clock period must be long enough to cover the SLOWEST possible journey a signal could take: leaving the first flip-flop (Tcq), passing through the WORST-CASE combinational logic delay between the two flip-flops, and then satisfying the second flip-flop's own setup-time requirement before the next clock edge arrives.

The HOLD-TIME constraint is an ENTIRELY SEPARATE check, on the FASTEST possible path rather than the slowest: Tcombinational(min) + Tcq ≥ Thold — the combinational logic's minimum possible delay, added to the first flip-flop's clock-to-Q delay, must still be AT least the second flip-flop's hold-time requirement, or new data could arrive and corrupt the second flip-flop's capture of the OLD data before its hold window has even closed.

GATE TRAP: a SETUP violation CAN be fixed by simply SLOWING DOWN the clock (giving the slowest path more time to settle before the next edge arrives) — but a HOLD violation CANNOT be fixed this way at all, since the hold constraint depends ENTIRELY on the FASTEST path's delay and the flip-flops' own fixed hold-time requirement, neither of which the clock PERIOD has any influence over whatsoever; a genuine hold violation requires an actual circuit-level fix (deliberately added delay on the offending fast path, or faster/different flip-flops with a smaller inherent hold-time requirement), never merely a slower clock — treating a hold violation as if it were just another setup-style problem solvable by the same slow-the-clock remedy is a serious and common misconception.

METASTABILITY: if a flip-flop's setup or hold requirement is VIOLATED, its output can enter a genuinely UNSTABLE, UNPREDICTABLE intermediate state for an UNBOUNDED (though probabilistically rapidly DECAYING) amount of time before it eventually resolves to a valid logic level — SYNCHRONIZERS (typically a chain of two or more flip-flops in series, sampling an asynchronous signal) exist specifically to reduce the PROBABILITY that such a metastable event propagates further into the rest of a synchronous system, by giving any metastability an extra clock cycle (or more) to resolve before its value is used elsewhere.

FULL WORKED DESIGN: A MOD-6 SYNCHRONOUS COUNTER USING JK FLIP-FLOPS

Design a Mod-6 synchronous counter (repeating states 0 through 5, then back to 0) using JK flip-flops, following the five-step procedure exactly.

1. STATE TABLE (step 1): 3 flip-flops Q₂Q₁Q₀ are needed (⌈log₂6⌉=3), giving states 000 through 101 as the valid cycle, with 110 and 111 UNUSED. Transitions: 000→001, 001→010, 010→011, 011→100, 100→101, 101→000 (back to the start).
2. STATE ASSIGNMENT (step 2): plain binary counting order is used directly here (0 through 5), since the transitions themselves are already the natural binary increment — no separate re-assignment is needed for this particular design.
3. EXCITATION COLUMNS (step 3): applying the JK excitation table to EACH bit's Q→Q⁺ transition at every one of the 6 valid states produces the following six rows (state: J₂K₂, J₁K₁, J₀K₀): state 000→001: (0,X),(0,X),(1,X). State 001→010: (0,X),(1,X),(X,1). State 010→011: (0,X),(X,0),(1,X). State 011→100: (1,X),(X,1),(X,1). State 100→101: (X,0),(0,X),(1,X). State 101→000: (X,1),(0,X),(X,1).
4. K-MAP MINIMISATION (step 4), treating states 110 and 111 as don't-cares in every column: J₀ is required to be 1 at every EVEN-Q₀ state (0,2,4) and is a don't-care at every odd-Q₀ state — the simplest satisfying choice is the CONSTANT J₀=1 (using the don't-cares as 1 too). K₀ is required to be 1 at every ODD-Q₀ state (1,3,5) and don't-care at even states — again the constant K₀=1 satisfies every requirement. (J₀=K₀=1 makes Q₀ behave as a plain TOGGLE flip-flop, toggling every single cycle — exactly matching the fact that the LSB of any binary count must flip on every step.) J₁ is required to be 1 ONLY at state 001, and 0 at states 000, 100, 101 (with 010,011 don't-care) — the expression J₁=Q₂′Q₀ satisfies every required row exactly (1 at 001 since Q₂′=1,Q₀=1; 0 at 000 since Q₀=0; 0 at 100 and 101 since Q₂′=0). K₁ is required to be 0 at state 010 and 1 at state 011 (others don't-care) — K₁=Q₀ satisfies both exactly. J₂ is required to be 1 only at state 011 (0 elsewhere among the required rows) — J₂=Q₁Q₀ satisfies this exactly (1 only when both Q₁ and Q₀ are 1). K₂ is required to be 0 at state 100 and 1 at state 101 — K₂=Q₀ satisfies both.
5. FINAL EQUATIONS AND SELF-STARTING CHECK (step 5): J₀=1, K₀=1, J₁=Q₂′Q₀, K₁=Q₀, J₂=Q₁Q₀, K₂=Q₀. Checking the two unused states explicitly: state 110 (Q₂Q₁Q₀=1,1,0) computes J₀=1,K₀=1 (Q₀ toggles 0→1); J₁=Q₂′Q₀=0·0=0,K₁=Q₀=0 (Q₁ holds at 1); J₂=Q₁Q₀=1·0=0,K₂=Q₀=0 (Q₂ holds at 1) — giving next state 111. State 111 computes similarly: Q₀ toggles 1→0; J₁=0,K₁=1 (Q₁ resets to 0); J₂=0,K₂=1 (Q₂ resets to 0) — giving next state 000, flowing DIRECTLY back into the valid cycle. Both unused states funnel safely into the intended 6-state loop within at most two extra clock edges, confirming the design is SELF-STARTING with no lock-out risk.

STATE REDUCTION AND EQUIVALENT STATES

Before assigning binary codes to states (step 2 of the design procedure), it is sometimes possible to REDUCE the total number of states needed by merging any two states that are truly EQUIVALENT — meaning the two states produce identical future output behaviour for every possible future input sequence, making them indistinguishable from outside the circuit no matter how they are tested.

1. Two states are equivalent if, for EVERY possible input, they produce the SAME output AND transition to states that are THEMSELVES equivalent (a definition that is naturally RECURSIVE, since "equivalent" for the next states is exactly the same condition being checked one step later).
2. An IMPLICATION (or PARTITIONING) TABLE formalises this check systematically: start by grouping states according to their IMMEDIATE output behaviour alone (states producing different outputs for the same input can never be equivalent, so this first pass already rules out many pairs); then repeatedly REFINE the grouping by checking whether each remaining candidate pair's NEXT states (for every input) still fall within the SAME group from the previous pass — if a pair's next states end up split across DIFFERENT groups, that pair is NOT actually equivalent after all, and must be separated into different groups itself; repeat this refinement until no further splitting occurs (the partition has STABILISED), at which point every group remaining together represents a genuine equivalence class.
3. Merging equivalent states BEFORE state assignment reduces the total number of flip-flops needed (fewer distinct states means a smaller ⌈log₂(number of states)⌉), and correspondingly simplifies the resulting excitation-table K-maps in step 4 — a real, practical payoff for doing this reduction step whenever a design's initially-drawn state diagram happens to contain redundant, equivalent states that were not obvious from the original problem description alone.

KEY: state reduction and state assignment are DIFFERENT steps solving DIFFERENT problems, and conflating them is a common error — reduction asks "can any of these states be MERGED because they behave identically," reducing the total COUNT of distinct states needed; assignment asks "which specific BINARY CODE should each surviving, already-distinct state receive," a purely representational choice (such as Gray-code ordering) that does not change how many states exist at all, only how cheaply their transitions can be expressed as Boolean functions of the chosen bits.

MEALY VS MOORE MACHINES

Every sequential state machine falls into one of two named categories, distinguished by WHERE the output actually comes from: a MOORE MACHINE's output depends only on the CURRENT STATE (never directly on the current input), while a MEALY MACHINE's output depends on both the current state AND the current input simultaneously.

1. Because a Moore machine's output is a function of state alone, its output can only CHANGE at a clock edge (when the state itself changes) — it is INHERENTLY synchronised to the clock and cannot glitch in response to a mid-cycle input change.
2. A Mealy machine's output, by contrast, can change THE MOMENT the input changes, even BETWEEN clock edges — this often lets a Mealy machine react one clock cycle FASTER than an equivalent Moore machine (the output reflects the input immediately, rather than waiting for the state to update first), but it also means a Mealy output can genuinely GLITCH if the input itself is noisy or glitchy, since the output is directly, combinationally, exposed to the raw input signal.
3. Both machine types are EQUALLY POWERFUL in terms of what sequences of behaviour they can describe — any Mealy machine can be converted into an EQUIVALENT Moore machine (typically needing ONE OR MORE additional states to "absorb" the input-dependence that the Moore output cannot express directly) and vice versa; the choice between them is a design trade-off (faster response vs glitch-immunity and synchronisation), not a difference in fundamental descriptive power.

GATE TRAP: assuming a Moore machine and a Mealy machine describing "the same" behaviour must have the SAME number of states is incorrect — converting a Mealy machine into an equivalent Moore machine typically REQUIRES additional states (since the Moore machine must represent, as separate STATES, distinctions that the Mealy machine could express directly through its combinational output-versus-input dependence instead), so a state-COUNT comparison between a Mealy design and its Moore equivalent is not a fair like-for-like comparison unless this extra-state cost is accounted for explicitly.

REGISTERS AND SHIFT REGISTERS

A REGISTER is simply a group of flip-flops (typically D flip-flops) sharing a common clock, used to store a multi-bit value — the most basic possible "memory" building block above the single-bit flip-flop level, with no additional logic beyond the flip-flops themselves.

A SHIFT REGISTER chains flip-flops together so that, on each clock edge, every bit moves one position to the LEFT or RIGHT, with a new bit entering at one end and the bit at the other end falling off (or wrapping around, in a specific circular variant).

1. SISO (Serial-In, Serial-Out): one bit enters per clock cycle, and one bit exits per clock cycle — the simplest shift register, needing only a single input line and a single output line regardless of the register's total width.
2. SIPO (Serial-In, Parallel-Out): bits enter one at a time (serially) but the ENTIRE register's current contents are available simultaneously (in parallel) at any instant — used for SERIAL-TO-PARALLEL conversion, letting a slow, single-wire serial data stream be "unpacked" into a fast, wide parallel word once fully shifted in.
3. PISO (Parallel-In, Serial-Out): the entire register is LOADED simultaneously (in parallel, typically via a separate load-enable control distinct from the ordinary shift operation), then SHIFTED OUT one bit per clock cycle — used for PARALLEL-TO-SERIAL conversion, the exact reverse operation of a SIPO register.
4. A RING COUNTER is a specific circular shift register variant: exactly ONE bit is initialised to 1 (all others to 0), and that single 1 simply CIRCULATES around the register indefinitely as clocking continues, with the bit that falls off one end fed directly back into the other end rather than being discarded — after n clock cycles (for an n-bit ring counter), the pattern returns exactly to its starting configuration, giving a clean, easily-decoded n-state sequence with NO combinational decoding logic needed at all (each flip-flop's own output directly indicates "we are currently in state i").
5. A JOHNSON (TWISTED-RING) COUNTER is a close variant: the COMPLEMENT of the bit falling off one end is fed back into the other end (rather than the bit itself, unchanged, as in a plain ring counter) — this doubles the number of DISTINCT states reachable for the same number of flip-flops (an n-bit Johnson counter cycles through 2n distinct states, compared to a plain n-bit ring counter's only n states), at the cost of needing a SMALL amount of decoding logic (rather than none at all) to distinguish each of the 2n states cleanly from the flip-flops' raw outputs.

GATE TRAP: assuming a ring counter and a Johnson counter of the SAME bit-width cycle through the SAME number of states is a common error — an n-bit ring counter has exactly n valid states (one bit set at a time, circulating), while an n-bit Johnson counter has 2n states (since the complemented feedback produces a longer, non-repeating pattern before returning to its start) — this factor-of-2 difference is a frequently tested numeric fact worth having memorised precisely rather than assumed by loose analogy between the two "circular shift register" designs.

STATE-ENCODING STRATEGIES: BINARY VS ONE-HOT

Beyond Gray-code assignment (already covered above), two other common state-encoding strategies are worth distinguishing directly by their respective trade-offs: BINARY encoding (using the minimum ⌈log₂N⌉ flip-flops for N states, as used throughout this chapter's examples so far) and one-HOT encoding (using exactly N flip-flops — ONE PER STATE — with exactly one flip-flop set to 1 at any given time, identifying the current state directly by WHICH flip-flop is set).

1. Binary encoding MINIMISES the flip-flop COUNT (using only ⌈log₂N⌉ bits) but generally requires MORE COMPLEX combinational logic to decode the current state and compute each bit's next value, since a given state's meaning is spread across the COMBINATION of several bits together.
2. One-hot encoding uses MORE flip-flops (N instead of ⌈log₂N⌉ — a potentially large increase for a state machine with many states) but often produces SIMPLER, faster excitation logic for EACH individual flip-flop, since each flip-flop's own next-state condition can frequently be expressed as a simple function of just the ONE (or few) states that transition directly into it, without needing to decode a multi-bit combination first.
3. One-hot encoding also makes DECODING the current state for external use TRIVIAL (the current state IS directly and immediately readable from which single flip-flop is set, with no decoding logic needed at all), in contrast to binary encoding, where identifying "are we in state 5" specifically requires actual combinational decoding logic examining several state bits together.

KEY: the binary-vs-one-hot choice is a genuine flip-flop-count-vs-logic-complexity trade-off, directly analogous to several other trade-offs already met in this book (fixed-point vs floating-point's range-vs-precision trade-off; literal-count vs input-count in multi-level combinational design) — no single encoding strategy is universally "better," and the right choice depends on which resource (flip-flop count, or per-flip-flop logic complexity and speed) is actually the scarce or expensive one in a given design's specific target hardware.

RACE CONDITIONS AND CRITICAL RACES IN ASYNCHRONOUS DESIGN

An ASYNCHRONOUS sequential circuit (one without a shared clock disciplining every state change, in contrast to every SYNCHRONOUS design covered so far in this chapter) can suffer from a RACE CONDITION whenever two OR more state variables are required to change simultaneously in response to a single input change — since real hardware signals never change in perfectly exact simultaneity, the state variables involved actually change in some unpredictable ORDER, and if that order can affect which final state the circuit settles into, the race is called a CRITICAL RACE (a genuine design flaw); if every possible ordering still arrives at the same correct final state regardless, the race is NON-CRITICAL (harmless, despite technically being a "race").

1. The standard fix for a critical race is a careful STATE ASSIGNMENT that ensures any transition requiring MULTIPLE state variables to change is instead reachable through a sequence of transitions each changing only ONE variable at a time (sometimes requiring the deliberate insertion of an extra, intermediate "pass-through" state purely to break up a problematic multi-bit transition into several single-bit steps).
2. This entire class of problem is SPECIFIC to asynchronous design — the master-slave flip-flop structure (and synchronous design generally) SIDESTEPS the race-condition problem entirely, since every state variable in a synchronous design changes at the SAME shared clock edge, with the flip-flops' own internal structure (not a carefully engineered state assignment) responsible for ensuring a clean, well-defined transition regardless of any internal combinational-logic timing variation.

KEY: critical races are a genuinely SEPARATE hazard category from the static/dynamic hazards covered in the combinational-circuits chapter — static and dynamic hazards are TRANSIENT GLITCHES in an otherwise eventually-correct output, while a critical race can cause the circuit to settle into a GENUINELY WRONG final state permanently, not merely glitch momentarily before self-correcting; conflating "hazard" (a combinational, self-correcting glitch) with "critical race" (a sequential, potentially permanent wrong-state outcome) blurs two importantly different failure modes that call for different fixes (redundant consensus terms for hazards; careful state assignment or extra pass-through states for critical races).

WORKED PROBLEMS

1. LATCH FORBIDDEN STATE. A NOR-based SR latch has S=1, R=1 momentarily, then both drop to 0 simultaneously. Describe the resulting behaviour. While S=R=1, both outputs Q and Q′ are forced to 0 (the forbidden state, violating the usual complementary-output guarantee). When S and R drop to 0 together, the latch's next state is UNPREDICTABLE — it "races" between the two valid resting states (Q=0 or Q=1) with no way to determine, from the circuit alone, which one it actually settles into.

2. JK VS SR LEGALITY. Is J=K=1 a legal input for a JK flip-flop? Is S=R=1 legal for an SR flip-flop? J=K=1 IS legal for JK — it produces a well-defined TOGGLE (Q⁺=Q′), exactly the case JK was specifically designed to resolve. S=R=1 is NOT legal for SR — it is the forbidden state, producing an invalid, unpredictable output.

3. T FLIP-FLOP FROM JK. Show how tying a JK flip-flop's J and K inputs together produces T flip-flop behaviour. With J=K=T: when T=0, J=K=0, giving JK's HOLD case (Q⁺=Q) — matching T's own hold behaviour. When T=1, J=K=1, giving JK's TOGGLE case (Q⁺=Q′) — matching T's own toggle behaviour exactly. Both of T's two possible behaviours are recovered exactly by this tie, confirming T is a genuine special case of JK.

4. EXCITATION TABLE LOOKUP. A JK flip-flop needs to transition from Q=1 to Q⁺=0. What are the required J,K values? From the JK excitation table, Q=1→Q⁺=0 needs J=X (don't care), K=1 (K must be 1 to either reset directly from J=0,K=1, or toggle down from J=1,K=1 — either way Q ends at 0, so J's value genuinely does not matter for this specific transition).

5. RIPPLE VS SYNCHRONOUS DELAY. A 4-bit ripple counter has each flip-flop's toggle delay of 5 ns. A 4-bit synchronous counter has flip-flop delay 5 ns and input-logic delay 3 ns. Compare their worst-case delays to a fully settled count. Ripple: 4×5=20 ns (each stage waits for the previous one). Synchronous: 5+3=8 ns (independent of the 4-bit width) — a substantial speed advantage for the synchronous design, and this gap would widen even further for a wider counter, since the ripple delay grows with width while the synchronous delay does not.

6. MAXIMUM CLOCK FREQUENCY. Given Tcq=2ns, Tcombinational(max)=6ns, Tsetup=2ns, find the maximum clock frequency. Tclk(min) = 2+6+2 = 10 ns, so fmax = 1/10ns = 100 MHz.

7. HOLD VIOLATION CHECK. Given Tcombinational(min)=1ns, Tcq=2ns, Thold=4ns, determine whether a hold violation exists, and whether slowing the clock would fix it if so. Check: Tcombinational(min)+Tcq = 1+2 = 3ns, which is LESS than Thold=4ns — a genuine HOLD VIOLATION exists. Since hold violations are independent of clock period entirely, slowing the clock would NOT fix this — a circuit-level fix (added delay on the fast path, or a flip-flop with a smaller hold-time requirement) is required instead.

8. STATE-ASSIGNMENT BENEFIT. Explain briefly why assigning Gray-code binary values to a sequential circuit's states (rather than plain binary counting order) often produces simpler flip-flop input equations. Gray-code assignment ensures CONSECUTIVE states differ in only ONE bit, exactly the K-map-adjacency property already exploited in combinational minimisation — since state TRANSITIONS in many designs occur between states that are logically "close" to each other, a Gray-code assignment tends to keep the corresponding excitation-table entries adjacent on a K-map, enabling larger, cheaper groupings than an arbitrary or plain-binary assignment would allow.

9. MEALY VS MOORE OUTPUT TIMING. A Mealy machine and an equivalent Moore machine both detect a specific 2-bit input pattern. Which one can signal detection FASTER after the pattern actually appears, and why? The Mealy machine signals faster — since its output depends directly on the CURRENT input as well as the current state, it can assert its detection output THE MOMENT the qualifying input arrives, within the same clock cycle, while the Moore machine must wait for the NEXT state transition (the following clock edge) before its state-only output can reflect the detection.

10. RING COUNTER STATE COUNT. How many distinct states does a 5-bit ring counter cycle through, and how many does a 5-bit Johnson counter cycle through? Ring counter: exactly 5 states (one bit set at a time, circulating). Johnson counter: 2×5=10 states (double the ring counter's count, due to the complemented feedback).

11. SHIFT REGISTER CONFIGURATION IDENTIFICATION. A shift register is loaded with an 8-bit value all at once via a parallel load line, then clocked 8 times to output the value one bit at a time on a single output wire. Which configuration is this? PISO (Parallel-In, Serial-Out) — parallel loading followed by serial shifting-out is exactly the PISO configuration, used for parallel-to-serial conversion.

12. FLIP-FLOP COUNT COMPARISON. A state machine has 10 states. Compare the flip-flop count needed for a binary encoding versus a one-hot encoding. Binary: ⌈log₂10⌉=4 flip-flops. One-hot: exactly 10 flip-flops (one per state) — a substantial increase in flip-flop count, trading for potentially simpler per-flip-flop excitation logic and trivial state decoding.

13. CRITICAL RACE IDENTIFICATION. An asynchronous circuit's state must transition from 00 to 11 in a single step in response to one input change, with both bits required to flip simultaneously. Explain why this is a race condition, and how it could be fixed. Since BOTH state bits must change together, and real bits never change in perfectly exact simultaneity, the circuit could transiently pass through EITHER 01 or 10 depending on which bit happens to change first — if either of these intermediate states leads to a DIFFERENT eventual outcome than the intended direct 00→11 transition, this is a CRITICAL race. The standard fix is inserting an extra intermediate state (for instance forcing the transition through 01 deliberately, changing only one bit at a time across two steps: 00→01→11) so that only ONE bit changes per actual transition, eliminating the ambiguous ordering entirely.

14. NON-CRITICAL RACE RECOGNITION. A different asynchronous transition also requires two state bits to change simultaneously, but this design happens to reach the SAME correct final state regardless of which of the two possible intermediate orderings actually occurs. Is this still a race condition needing a fix? This is technically still a RACE (multiple state variables genuinely do change without guaranteed simultaneity), but it is a NON-CRITICAL race — since both possible orderings converge on the identical correct final state, the ambiguity in ordering causes no actual incorrect behaviour, and no state-assignment fix or extra intermediate state is required for this specific transition, unlike the genuinely critical race in the previous problem.

15. FULL D-FLIP-FLOP MOD-4 COUNTER DESIGN. Design a Mod-4 synchronous counter (states 00,01,10,11, repeating) using D flip-flops, following the five-step procedure. Since D's excitation table requires D to simply EQUAL the desired next state directly (D-excitation is don't-care-free, as noted above), the design reduces almost immediately to writing out D₁ and D₀ as functions of the present state Q₁Q₀. State table: 00→01, 01→10, 10→11, 11→00. Applying D-excitation directly (D=Q⁺ in every case): at 00, D₁D₀ must equal 01, so D₁=0,D₀=1. At 01, next is 10, so D₁=1,D₀=0. At 10, next is 11, so D₁=1,D₀=1. At 11, next is 00, so D₁=0,D₀=0. Tabulating D₁ against Q₁Q₀ (00→0, 01→1, 10→1, 11→0): this is exactly Q₁⊕Q₀ (XOR), confirmed by checking all four rows against the XOR truth table directly. Tabulating D₀ against Q₁Q₀ (00→1, 01→0, 10→1, 11→0): this is exactly Q₀′ (the complement of Q₀ alone), confirmed since D₀=1 exactly when Q₀=0 in every one of the four rows. Final equations: D₁=Q₁⊕Q₀, D₀=Q₀′. Verifying self-consistency by simulating one full cycle from 00: state 00→(D₁=0⊕0=0,D₀=0′=1)→01. State 01→(D₁=0⊕1=1,D₀=1′=0)→10. State 10→(D₁=1⊕0=1,D₀=0′=1)→11. State 11→(D₁=1⊕1=0,D₀=1′=0)→00 — the full 4-state cycle is reproduced exactly, confirming both equations are correct.

This D-flip-flop design is worth contrasting directly against the earlier JK-based Mod-6 counter design, since the two illustrate opposite ends of a genuine design trade-off: D-flip-flop excitation is always trivial (D simply equals the desired next state, with no don't-cares and no separate excitation-table lookup needed at all), making the DESIGN PROCESS itself faster and less error-prone, but the RESULTING per-bit equations (here, a plain XOR and a plain complement) do not automatically benefit from any don't-care-driven simplification the way the JK design's excitation columns did. JK excitation, by contrast, is more involved to work out at each step (requiring the explicit excitation-table lookup demonstrated in the Mod-6 example), but its abundant don't-cares frequently lead to SIMPLER final equations than a D-based design of the identical target sequence would produce, precisely because the JK design has more logical freedom available to exploit during the K-mapping step. Neither flip-flop type is unconditionally the better choice for every design — the D flip-flop trades a faster, more mechanical design PROCESS for potentially costlier final logic, while JK trades a more involved design process for potentially cheaper final logic, and recognising this as a genuine trade-off (rather than assuming one flip-flop type is simply "better" in general) is worth carrying forward into any future sequential-design task where the choice of flip-flop type is left open to the designer rather than dictated by the problem statement itself.

A closely related practical habit, worth stating explicitly: whenever a design task genuinely leaves the flip-flop type unspecified, working the same target sequence through with both a D-based and a JK-based (or T-based) derivation, and comparing the two resulting sets of equations directly, is a fast and reliable way to identify whichever realisation happens to be more economical for that specific sequence — since, as the Mod-4 and Mod-6 examples above illustrate, there is no way to predict in advance, purely from the shape of the target sequence, which flip-flop type will end up producing the simpler final logic without actually carrying out (or at least sketching) both derivations far enough to compare their resulting equations directly against each other, term by term, before committing to whichever choice actually turns out cheaper.

Stepping back across the whole chapter one final time: every one of its topics, from the most basic SR latch through to critical races in asynchronous design, is answerable using nothing more than the five-step design procedure, the four flip-flop characteristic and excitation tables, and ordinary combinational K-map minimisation already covered fully in the earlier Boolean algebra chapter. Nothing genuinely new needs to be learned beyond these building blocks; every additional named concept in this chapter — Mealy versus Moore, shift registers, ring and Johnson counters, state encoding, races — is a specific, named APPLICATION or CONSEQUENCE of that same small, already-familiar foundation, applied to a slightly different question each time. Internalising this — that a sequential-design question, however unfamiliar its specific wording, is always answerable by returning to this same small foundation and working carefully through it from first principles — is the single most valuable, transferable outcome this entire chapter has to offer, more valuable in the long run than memorising any individual named result covered along the way.

This closing habit is precisely the same one this book has emphasised, chapter after chapter, across every subject covered so far: a small number of genuinely foundational ideas, understood deeply enough to be re-derived confidently from first principles under time pressure, consistently outperforms a much longer list of memorised named facts whose underlying derivation has been forgotten, half-remembered, or never fully understood properly in the first place. Depth, applied consistently and confidently across every topic in this book, beats sheer breadth memorised without it every single time such genuine understanding is actually put to the test under real, unforgiving conditions, where recall alone is not enough and the underlying reasoning must be reconstructed on the spot, quickly, confidently, and correctly. It must come from first principles alone, rather than from a half-remembered fact dredged up under time pressure and trusted blindly without any real re-derivation standing behind it, at that one critical moment when it genuinely mattered most, with nothing else left to fall back on in that single decisive instant. A candidate who can still redraw a JK flip-flop's own excitation table from first principles, ten minutes before the exam clock runs out, has genuinely internalised this chapter — not merely browsed through it once.

CARRYING THIS FORWARD

The entire chapter reduces to two ideas layered on top of everything already built in the two previous Digital Logic chapters: FIRST, a bit of memory is created by carefully arranging combinational feedback (a latch) and then disciplining WHEN that feedback is allowed to act (a flip-flop, via the master-slave structure, restricting updates to a single clean edge) — memory is not a fundamentally new primitive, but combinational logic used in a specific, carefully controlled way. Second, DESIGNING a sequential circuit is, at its very last and most consequential step, nothing more than ORDINARY combinational K-map minimisation — applied not to a single target function, but to several EXCITATION columns constructed by looking BACKWARD from a desired state transition rather than forward from a given input. Every named flip-flop type (SR, JK, D, T) is simply a different characteristic equation, and every named counter style (ripple, synchronous) is simply a different choice about whether each flip-flop's input comes directly from a NEIGHBOUR's output or from FRESH combinational logic computed off the shared present state — recognising both of these as variations on the same small set of underlying ideas, rather than as an ever-growing list of unrelated named circuits, is exactly the same connective habit the previous two Digital Logic chapters were built to instil, now extended one further layer to circuits that remember.

KEY: this chapter's later material — Mealy/Moore machines, shift registers and ring/Johnson counters, state-encoding trade-offs, and race conditions — each add exactly ONE additional design DIMENSION on top of the five-step procedure and the flip-flop taxonomy already built out in full: Mealy/Moore is a choice about WHERE an output is read from (state alone, or state-plus-input); shift registers and ring/Johnson counters are specific, highly REGULAR state-transition patterns that happen to need no (or minimal) combinational excitation logic at all, since each flip-flop simply passes its neighbour's value along; state encoding is a choice about HOW MANY flip-flops to spend versus how simple the resulting excitation logic becomes; and race conditions are a hazard specific to REMOVING the clock discipline that every other topic in this chapter otherwise relies on. None of these five additional topics requires a genuinely new theoretical foundation beyond the flip-flop characteristic/excitation tables and the five-step design procedure already covered — each is best understood as that SAME foundation, viewed through a different specific lens or applied to a specific, highly regular use case.

A further connection worth drawing explicitly, tying this chapter back to the combinational-circuits chapter directly preceding it: the RING COUNTER's defining property — needing NO combinational decoding logic at all, since each state is directly readable off a single flip-flop's own output — is the sequential-circuit analogue of a DECODER's own defining property (each of its output lines directly signals one specific input combination, again with no further combining logic needed to identify which line is "active"). Both structures trade a larger number of output/storage elements (many decoder output lines; many ring-counter flip-flops) for the elimination of a combining or decoding step that a more compact representation (a Σm expression combined via an external OR; a binary-encoded counter's own state-decoding logic) would otherwise require — the same general "spend more elements, buy back a combining or decoding step" trade-off recurring here in a sequential context, having already appeared in a purely combinational one.

A final practical note, closing out this chapter and the three-chapter Digital Logic sequence on Boolean algebra, combinational circuits, and sequential circuits together: every worked design in this chapter — the Mod-6 counter's full five-step derivation, the state-reduction discussion, the ring-versus-Johnson-counter state-count comparison — was checked by an independent method after the main derivation, exactly the same verification discipline maintained throughout every chapter of this material. The Mod-6 counter's final equations were checked not only against the SIX intended states, but explicitly against the two unused states as well, confirming self-starting behaviour rather than merely assuming it — a step that is easy to skip under time pressure, but is precisely the step that catches a design that looks correct on its six main states while silently containing a lock-out trap on states that were never checked at all.
`
};
