// Textbook chapter: The Transport Layer — UDP, TCP, Connection Management,
// Flow Control, and Congestion Control. Written directly (no subagent) to
// match the depth and voice of the other chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['cn-transport'] = {
  figs: [
    {
      id: 'tcp-three-way-handshake',
      caption: 'The TCP three-way handshake: SYN, then SYN-ACK, then a final ACK — each side confirms it can both send and receive before either transmits any actual application data.',
      svg: '<svg viewBox="0 0 240 100" width="100%" style="max-width:240px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1" fill="none"><line x1="30" y1="10" x2="30" y2="90"/><line x1="210" y1="10" x2="210" y2="90"/><line x1="30" y1="25" x2="210" y2="40"/><line x1="210" y1="50" x2="30" y2="65"/><line x1="30" y1="75" x2="210" y2="90"/></g><g font-size="8" fill="currentColor" text-anchor="middle"><text x="30" y="8">Client</text><text x="210" y="8">Server</text><text x="120" y="30">SYN</text><text x="120" y="55">SYN-ACK</text><text x="120" y="85">ACK</text></g></svg>'
    },
    {
      id: 'tcp-congestion-window',
      caption: 'TCP\'s congestion window over time: exponential growth during slow start, then linear (additive) growth after reaching the slow-start threshold, with a sharp drop back down upon detecting loss.',
      svg: '<svg viewBox="0 0 260 100" width="100%" style="max-width:260px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><polyline points="10,85 40,70 60,50 75,35 85,28 85,28 150,15 210,10"/><line x1="210" y1="10" x2="212" y2="70"/><polyline points="212,70 260,55"/></g><g font-size="7" fill="currentColor" text-anchor="middle"><text x="50" y="95">slow start</text><text x="150" y="8">congestion avoidance</text><text x="235" y="90">loss → drop</text></g></svg>'
    }
  ],
  text: `
TRANSPORT-LAYER RESPONSIBILITIES AND MULTIPLEXING

The previous chapter's own NETWORK layer delivers packets between HOSTS (machines identified by an IP address); the transport layer's own job is delivering data between PROCESSES (specific applications running on those hosts) — this PROCESS-to-process delivery, called MULTIPLEXING and DEMULTIPLEXING, is the single foundational service every transport-layer protocol must provide, on top of whatever additional reliability guarantees it may or may not add.

1. MULTIPLEXING carefully gathers data from MULTIPLE application processes on a SENDING host and combines it into segments for transmission over the network; DEMULTIPLEXING, the exact reverse process performed at the RECEIVING host, delivers each arriving segment's payload precisely to the CORRECT application process, and to no other one.
2. A PORT NUMBER (a 16-bit field, giving a range of 0-65535, or 65,536 distinct values total) identifies a SPECIFIC process (or, more precisely, a specific communication endpoint within a process) on a host — a full transport-layer connection endpoint is identified precisely by the COMBINATION of an IP address and a port number, called a SOCKET.
3. PORTS below 1024 are RESERVED (WELL-KNOWN) ports, conventionally assigned to standard, widely deployed services (port 80 for HTTP, port 443 for HTTPS, port 25 for SMTP, port 53 for DNS) — a client initiating a connection typically uses a randomly assigned EPHEMERAL port (from a much larger available range above 1024) for its own end of that specific connection, since it is genuinely the SERVER's own well-known port that must remain fixed and predictable for clients to actually find and reach it.
4. A TCP connection is therefore uniquely identified, in full, by the 4-TUPLE (source IP, source port, destination IP, destination port) — this is PRECISELY why a single server can simultaneously handle MANY independent connections all arriving at the SAME well-known destination port, since each individual connection's OWN source IP and source port still distinguish it uniquely from every other concurrent connection to that identical server port, letting the operating system demultiplex each arriving segment to the correct connection-specific socket purely by inspecting this four-tuple.

UDP: THE CONNECTIONLESS TRANSPORT PROTOCOL

1. UDP (User Datagram Protocol) provides ONLY the bare minimum transport-layer service: multiplexing/demultiplexing via port numbers, plus a simple CHECKSUM for basic error detection (directly reusing the Internet checksum mechanism from this book's own previous Network-layer chapter) — it adds NO reliability, NO ordering guarantee, and NO flow or congestion control whatsoever beyond what IP itself already provides.
2. UDP is genuinely CONNECTIONLESS: no handshake or setup phase occurs before data can be sent, and each UDP datagram is handled COMPLETELY independently of every other one, with NO memory of any prior datagrams kept by either endpoint at all — this gives UDP its characteristic LOW overhead and LOW latency, since there is no connection-state to establish or maintain.
3. UDP's header is minimal, just 8 bytes: SOURCE PORT, DESTINATION PORT, LENGTH, and CHECKSUM (each 16 bits) — dramatically simpler than TCP's own considerably larger header, examined next. UDP's checksum computation additionally covers a PSEUDO-HEADER (containing the source and destination IP addresses, the protocol number, and the UDP length, all borrowed conceptually from the IP header one layer below), specifically so that a segment accidentally delivered to the WRONG destination address, or one whose length field has been corrupted, is caught even though these particular fields technically belong to a different, lower layer's own header.

1. Worked example (UDP checksum computation, independently verified using one's-complement addition): a UDP payload consists of two 16-bit words, 0110011001100000 and 0101010101010101, with the pseudo-header contributing a third 16-bit word, 0000111100001111. Compute the checksum. Adding the first two words in ordinary binary: 0110011001100000 + 0101010101010101 = 1011101110110101 (no carry beyond 16 bits in this step). Adding the third word: 1011101110110101 + 0000111100001111 = 1100101011000100 (again no overflow beyond 16 bits here). The CHECKSUM field transmitted is the ONE'S COMPLEMENT (bitwise NOT) of this final sum: NOT(1100101011000100) = 0011010100111011. At the receiver, summing all three original words TOGETHER WITH this transmitted checksum value should yield a result of all 1-bits (1111111111111111) if no error occurred during transmission, directly confirming the Internet checksum's own general correctness property already introduced in the previous Data Link Layer chapter, now applied specifically at the UDP transport layer with its own pseudo-header extension.
4. UDP is the appropriate choice for applications that VALUE LOW latency over guaranteed reliability (live audio/video streaming, where a dropped or late packet is better simply skipped than retransmitted after the moment it was needed has already passed), for SIMPLE query-response protocols where the OVERHEAD of a full connection setup would dominate a tiny exchange (DNS lookups, for instance), and for applications that prefer to implement THEIR OWN, custom reliability or ordering logic directly at the application layer rather than accepting TCP's specific, one-size-fits-all guarantees.

TCP HEADER FORMAT AND CONNECTION ESTABLISHMENT

[[FIG:tcp-three-way-handshake]]

1. TCP (Transmission Control Protocol) is CONNECTION-ORIENTED, RELIABLE, and provides IN-ORDER, byte-stream delivery — its header includes SOURCE and DESTINATION PORT (16 bits each), SEQUENCE NUMBER (32 bits, identifying the position of the FIRST byte of this segment's own payload within the overall byte stream), ACKNOWLEDGMENT NUMBER (32 bits, the NEXT sequence number the sender of this segment EXPECTS to receive), a set of CONTROL FLAGS (including SYN, ACK, FIN, and RST), a WINDOW SIZE field (advertising the sender's OWN currently available receive-buffer space, directly supporting flow control), and a CHECKSUM.
2. THE THREE-WAY HANDSHAKE establishes a TCP connection: the CLIENT sends a SEGMENT with the SYN flag set, carrying an INITIAL sequence number it has chosen (typically randomized, for security reasons, rather than starting at a predictable fixed value); the SERVER responds with a segment having BOTH the SYN and ACK flags set, acknowledging the client's sequence number (ACK = client's sequence number + 1) and carrying its OWN randomly chosen initial sequence number; the CLIENT then sends a final segment with the ACK flag set, acknowledging the server's sequence number (ACK = server's sequence number + 1) — after this THIRD segment, BOTH sides have confirmed they can both SEND and RECEIVE, and application data may now flow in EITHER direction.
3. THE handshake requires EXACTLY three segments — never two, and never four — because EACH side must independently confirm BOTH that its own SYN was received AND that it has received the OTHER side's SYN — a two-way handshake would leave the SERVER uncertain whether its own SYN-ACK ever actually reached the client, since the client's FINAL acknowledgment is what confirms this; a fourth segment would be REDUNDANT, since the client's third segment already carries all the acknowledgment information the server needs.
4. CONNECTION TERMINATION uses a FOUR-way exchange (rather than the establishment's three-way handshake) because TCP connections are FULL-DUPLEX, and EACH direction of the connection must be closed INDEPENDENTLY — the side initiating closure sends a FIN; the other side ACKs it (closing that ONE direction), but may CONTINUE sending its OWN data for a time before it, too, sends its OWN FIN, which the original initiator then ACKs, closing the SECOND direction and completing the full connection teardown. The side that sends the FINAL ACK of this exchange typically enters a TIME-WAIT state for a fixed interval (conventionally twice the network's own maximum expected segment lifetime) before fully discarding the connection's state, specifically to ensure any stray, delayed duplicate segment still traveling through the network from this now-closed connection cannot be mistakenly delivered to and confused with an entirely new, unrelated connection that later happens to reuse the same port pair.

GATE TRAP: assuming the SYN flood attack (a well-known denial-of-service technique) exploits some flaw specific to TCP's ENCRYPTION or authentication is a common misunderstanding — the actual vulnerability lies in the SERVER's own need to allocate and hold STATE (a half-open connection entry) immediately upon receiving a SYN, BEFORE the handshake's third segment ever confirms the client is genuine and reachable; an attacker flooding a server with SYN segments carrying spoofed source addresses can exhaust this half-open-connection table entirely, denying service to legitimate clients, without needing to break any cryptographic mechanism at all.

RELIABLE DATA TRANSFER: SEQUENCE NUMBERS AND ACKNOWLEDGMENTS

1. TCP treats the data it transmits as a single, CONTINUOUS BYTE STREAM (NOT as discrete, independently numbered messages) — its sequence numbers therefore count INDIVIDUAL BYTES, not segments, meaning a segment carrying 1000 bytes starting at sequence number 5000 covers bytes 5000 through 5999, and the NEXT segment (assuming no gaps) would begin at sequence number 6000.
2. TCP uses CUMULATIVE ACKNOWLEDGMENTS: an ACK carrying acknowledgment number N confirms that ALL bytes UP TO (but not including) byte N have been received CORRECTLY and IN ORDER — a segment arriving OUT of order does NOT advance the acknowledgment number at all, since TCP (in its BASIC form) cannot describe a "hole" in the byte stream using only a single cumulative number. This single-number acknowledgment scheme is considerably more compact than tracking a separate acknowledgment bit per individual byte or segment would be, at the direct cost of the coarser, all-or-nothing granularity this section goes on to describe.
3. TCP's SELECTIVE ACKNOWLEDGMENT (SACK) option EXTENDS basic cumulative acknowledgment specifically to address this weakness, allowing a receiver to ADDITIONALLY report which OUT-of-order byte ranges it HAS successfully received, letting the sender retransmit ONLY the genuinely missing gaps rather than potentially retransmitting data the receiver already has — directly analogous to the previous Data Link chapter's own Selective Repeat protocol, applied here at the TCP transport layer instead of at the data link layer. SACK is negotiated during the initial three-way handshake itself (both endpoints must advertise support for it before either will actually use it), and is carried as an OPTIONAL field appended after TCP's own fixed-size header, listing up to several disjoint received byte ranges within a single acknowledgment segment.
4. FAST RETRANSMIT is an important complement to timeout-based retransmission specifically because it triggers a retransmission WITHOUT waiting for a full retransmission-timeout period to expire: if the sender receives THREE DUPLICATE acknowledgments (the SAME acknowledgment number repeated three additional times, since the receiver keeps re-acknowledging the LAST correctly received in-order byte while a gap persists), it INFERS that a segment was likely lost (rather than merely reordered, since a genuinely reordered-but-not-lost segment typically produces at most one or two duplicate ACKs before the gap is filled) and retransmits IMMEDIATELY, considerably faster than waiting for a timeout.

FLOW CONTROL VIA THE RECEIVE WINDOW

1. TCP's OWN flow control (distinct from the CONGESTION control examined later in this chapter) uses the WINDOW SIZE field in every segment's header to let the RECEIVER advertise exactly how much FREE buffer space it CURRENTLY has available — the sender is obligated to keep its own AMOUNT of unacknowledged, outstanding data at or BELOW this advertised window, preventing it from overwhelming the receiver's own buffer, directly generalizing the sliding-window flow control this book's own previous Data Link chapter examined at that lower layer.
2. THE advertised window field itself is 16 bits wide in the base TCP header, limiting it to a maximum of 65,535 bytes unless the WINDOW SCALE option (negotiated during connection setup, again alongside SACK) is used to represent a considerably larger effective window via a shift factor — this scaling option becomes practically necessary on precisely the kind of high-bandwidth, high-RTT links whose large bandwidth-delay product, examined in this book's own earlier Computer Networks Basics chapter, would otherwise exceed what a plain 16-bit field could ever express, since a window smaller than that link's own bandwidth-delay product would leave the connection unable to keep the link's own pipe fully occupied, echoing that earlier chapter's own stop-and-wait worked example precisely. IF a receiver's advertised window drops to ZERO (its buffer is completely full), the sender must STOP sending new data entirely — to avoid a DEADLOCK where the receiver's LATER window-update segment (announcing newly freed buffer space) is itself LOST, TCP has the sender periodically send small PERSISTENCE TIMER probe segments, prompting the receiver to re-confirm its current window size even if its own update was lost.
3. TCP's flow-control window and the congestion-control mechanisms examined next OPERATE SIMULTANEOUSLY and INDEPENDENTLY — the SENDER's actual, EFFECTIVE window at any moment is the MINIMUM of the receiver's own advertised flow-control window and the sender's own internally computed congestion window, ensuring NEITHER the receiver's buffer NOR the network's own capacity is ever overwhelmed. Concretely, if a receiver advertises a flow-control window of 20 KB while the sender's own congestion window has grown to 50 KB, the sender is still bound by the SMALLER of the two figures (20 KB), and will only be permitted to send the larger amount once the receiver itself advertises a correspondingly larger available window.

CONGESTION CONTROL: SLOW START AND CONGESTION AVOIDANCE

[[FIG:tcp-congestion-window]]

1. CONGESTION CONTROL addresses a fundamentally DIFFERENT concern from flow control: flow control protects a SINGLE receiver's own buffer, while congestion control protects the SHARED NETWORK itself from being overwhelmed by the AGGREGATE traffic of MANY senders simultaneously — TCP infers network congestion INDIRECTLY, primarily from observing PACKET LOSS (a timeout, or triple-duplicate-ACKs), since routers along the path do not (in the classic scheme) explicitly signal their own congestion level back to the sender.
2. SLOW START begins a NEW (or previously idle) TCP connection with a SMALL congestion window (CWND), typically starting at 1 MSS (Maximum Segment Size), and DOUBLES cwnd every ROUND-TRIP TIME (adding 1 MSS to cwnd for EVERY individual ACK received, which — since a full window's worth of ACKs return within roughly one RTT — produces EXPONENTIAL growth) — this continues until CWND reaches a THRESHOLD value (ssthresh) or a loss is detected, whichever occurs first.
3. AFTER reaching ssthresh, TCP switches to CONGESTION AVOIDANCE, growing cwnd LINEARLY rather than exponentially (roughly 1 MSS added per FULL round-trip time, regardless of how many individual ACKs arrive within it) — this ADDITIVE-INCREASE behavior probes for additional available capacity far more CAUTIOUSLY than slow start's own exponential growth, once the connection is believed to be operating reasonably close to the network's own actual capacity.
4. UPON detecting LOSS via a TIMEOUT, TCP treats this as a SEVERE congestion signal: ssthresh is set to HALF the current cwnd, and cwnd itself is RESET all the way back down to 1 MSS, restarting slow start from scratch — a considerably more DRASTIC response than the milder reaction to loss detected via triple-duplicate-ACKs, examined next.
5. UPON detecting loss via TRIPLE DUPLICATE ACKNOWLEDGMENTS (paired with fast retransmit, from the earlier RELIABLE DATA TRANSFER section), TCP instead performs FAST RECOVERY: ssthresh is set to HALF the current cwnd (the SAME halving rule as the timeout case), but cwnd is set to ssthresh PLUS 3 MSS (rather than collapsing all the way to 1), and growth resumes in CONGESTION AVOIDANCE mode directly from there — reflecting the MILDER interpretation that triple-duplicate-ACKs indicate the network is STILL delivering SOME segments successfully (only one appears genuinely lost), unlike a full timeout, which suggests the connection may be COMPLETELY stalled.

GATE TRAP: confusing the RESPONSE to a TIMEOUT (cwnd collapses to 1 MSS, full slow start restarts) with the response to TRIPLE DUPLICATE ACKS (cwnd drops only to ssthresh+3, resuming congestion avoidance directly, WITHOUT restarting slow start) is one of the single most common errors on this entire topic — the underlying design principle to remember is that a TIMEOUT is treated as evidence of SEVERE, possibly total, network failure (warranting the MOST conservative possible restart), while triple-duplicate-ACKs are treated as evidence of MILDER, partial congestion (warranting a considerably gentler pull-back), and this difference in SEVERITY of the inferred problem directly explains the difference in severity of TCP's own corrective response.

1. Worked example (slow-start growth trace, independently verified): a TCP connection begins with cwnd=1 MSS and ssthresh=16 MSS, with NO losses occurring. Trace cwnd after each of the first 5 round-trip times. RTT 1: cwnd doubles from 1 to 2. RTT 2: cwnd doubles from 2 to 4. RTT 3: cwnd doubles from 4 to 8. RTT 4: cwnd doubles from 8 to 16 — at this point cwnd has reached ssthresh (16), so the connection SWITCHES to congestion avoidance for all subsequent growth. RTT 5 (now in congestion avoidance, growing by 1 MSS per RTT rather than doubling): cwnd increases from 16 to 17 — directly confirming the transition from EXPONENTIAL growth (1→2→4→8→16, doubling each round) to LINEAR growth (16→17, a single additive increment) occurs EXACTLY at the round-trip time where cwnd first reaches or exceeds the ssthresh value.

TCP VARIANTS: TAHOE, RENO, AND MODERN REFINEMENTS

1. TCP TAHOE, an EARLY TCP implementation, treated EVERY detected loss (whether via timeout OR via triple-duplicate-ACKs) with the SAME drastic response: collapse cwnd to 1 MSS and restart slow start from scratch — TAHOE did NOT distinguish between the two loss-detection mechanisms the way the modern behavior described earlier in this chapter does, treating every single loss signal as equally severe regardless of how it was actually detected, a design choice later found to leave considerable achievable throughput on the table under otherwise ordinary, moderate loss conditions.
2. TCP RENO introduced FAST RECOVERY specifically to handle triple-duplicate-ACKs more gently (the cwnd=ssthresh+3 behavior already described in this chapter's own CONGESTION CONTROL section), while STILL treating a genuine timeout with Tahoe's own original, drastic full-restart response — Reno's refinement is PRECISELY the differentiated behavior this chapter's own earlier GATE TRAP box already emphasized, and Reno (or one of its many later refinements) is the behavior assumed by essentially every modern GATE question on TCP congestion control unless a question explicitly specifies "Tahoe" by name.
3. NEWRENO further refined Reno's own fast-recovery behavior to handle MULTIPLE losses within a SINGLE window more gracefully (Reno's original fast recovery could, under certain multi-loss patterns, exit fast recovery PREMATURELY, before every lost segment in that window had actually been successfully retransmitted) — GATE questions rarely probe NewReno's own specific refinement in deep detail, but its EXISTENCE illustrates that TCP congestion control has continued to evolve incrementally well beyond the foundational Tahoe/Reno distinction this chapter emphasizes as the core, examinable material. Specifically, plain Reno's fast recovery relies on a SINGLE partial acknowledgment to signal that recovery is complete, which works correctly when only ONE segment within a window was lost, but can be misled when SEVERAL segments within the same window were lost simultaneously; NewReno instead continues in fast recovery, retransmitting one additional lost segment per partial acknowledgment received, until a FULL acknowledgment covering the entire original window finally arrives, confirming that every lost segment has genuinely been recovered.
4. MODERN congestion-control variants (CUBIC, the current Linux default, and BBR, a considerably more recent, model-based approach explicitly measuring bottleneck bandwidth and round-trip propagation delay rather than relying purely on loss as a congestion signal) depart much further from the classic AIMD/slow-start framework this chapter has developed — GATE's own syllabus focuses on the classical Tahoe/Reno-era mechanisms examined in depth throughout this chapter, but recognizing that these represent one specific, historically foundational approach rather than the industry's own current, final state is worth keeping in mind. CUBIC in particular grows its own congestion window as a CUBIC function of the elapsed time since the LAST loss event, rather than a purely linear function of elapsed round trips, specifically so its own recovery speed does not depend so heavily on RTT the way plain AIMD's own additive increase does — directly addressing the very RTT-based unfairness concern this chapter's own THROUGHPUT AND FAIRNESS section already identified as a genuine practical limitation of the classical scheme.

GATE TRAP: assuming "TCP" without further qualification always means the specific Reno-style differentiated timeout-versus-triple-duplicate-ACK behavior is a subtle trap some questions deliberately probe — a question that EXPLICITLY names "TCP Tahoe" is asking about the older, UNDIFFERENTIATED behavior (EVERY loss, regardless of detection mechanism, collapses cwnd to 1 MSS), and applying Reno's own gentler fast-recovery rule to a question that has specifically named Tahoe produces an INCORRECT answer — always check whether a question names a SPECIFIC TCP variant before assuming the modern, differentiated Reno-style behavior applies by default.

CARRYING THIS FORWARD

This chapter has examined the COMPLETE transport-layer machinery sitting directly on top of the previous chapter's own best-effort, host-to-host IP delivery service: MULTIPLEXING/DEMULTIPLEXING via port numbers (letting many processes share a single host's network connection), UDP's minimal, connectionless service for latency-sensitive or custom-reliability applications, and TCP's considerably richer connection-oriented service, providing RELIABLE delivery (sequence numbers, cumulative and selective acknowledgment, fast retransmit), FLOW control (the advertised receive window, protecting a single receiver's buffer), and CONGESTION control (slow start, congestion avoidance, and the differentiated responses to timeout versus triple-duplicate-ACK loss signals, protecting the shared network itself from overload). The single most valuable connecting insight to carry forward is that TCP's own two DISTINCT window mechanisms — flow control's receiver-advertised window and congestion control's sender-computed window — solve two GENUINELY different problems (protecting one specific receiver's buffer, versus protecting the shared network from many senders' combined traffic) using a structurally SIMILAR windowing mechanism, and a GATE question's use of the word "window" should always be checked carefully against WHICH of the two concerns it is actually describing.

KEY: TCP's own choice to infer congestion INDIRECTLY from loss, rather than requiring routers to signal congestion EXPLICITLY, is the SAME "keep the shared, common layer minimal and push additional intelligence to the endpoints" end-to-end design philosophy the previous Network Layer chapter's own closing section already identified as underlying the entire IP/TCP division of labor — congestion control lives almost entirely within TCP's own sender logic specifically because the network layer beneath it was deliberately kept simple and does not, in its classic form, provide any richer explicit-congestion-notification signal for TCP to rely on instead.

KEY: the differentiated timeout-versus-triple-duplicate-ACK response is structurally the SAME "match the severity of the corrective action to the severity of the inferred problem" principle already seen in the earlier Databases Transactions chapter's own distinction between wait-die and wound-wait deadlock-prevention schemes reacting differently depending on relative transaction age, and in the Data Link chapter's own binary exponential backoff scaling its retry range to the OBSERVED number of consecutive collisions — recognizing this recurring "graduated response proportional to inferred severity" pattern across entirely different subjects is far more valuable than memorizing TCP's own specific numeric constants (halving, +3 MSS, resetting to 1) as isolated facts.

KEY: TCP's cumulative-versus-selective acknowledgment distinction is, once again, precisely the SAME Go-Back-N-versus-Selective-Repeat trade-off examined in depth in the previous Data Link chapter, now reappearing at the transport layer — cumulative ACKs (TCP's basic behavior) can force retransmission of data the receiver may already have safely received, exactly as Go-Back-N does, while SACK (TCP's optional extension) achieves Selective Repeat's own more precise, minimal-retransmission behavior — confirming that the SAME reliability-mechanism trade-off, once understood at ONE layer, transfers directly and completely to an entirely different layer facing a structurally identical underlying problem.

RETRANSMISSION TIMEOUT ESTIMATION

1. TCP must choose a RETRANSMISSION TIMEOUT (RTO) — too SHORT a timeout causes UNNECESSARY retransmissions of segments that were merely delayed, not lost, wasting bandwidth; too LONG a timeout causes the sender to wait needlessly before recovering from a GENUINE loss, hurting throughput.
2. TCP estimates the round-trip time DYNAMICALLY using an EXPONENTIALLY WEIGHTED MOVING AVERAGE: EstimatedRTT = (1−α)×EstimatedRTT + α×SampleRTT, where SampleRTT is the MOST RECENTLY measured round-trip time for one specific segment/ACK pair, and α is typically 0.125 — this SMOOTHS out short-term fluctuations while still ADAPTING to genuine, sustained changes in the actual network delay.
3. TCP ADDITIONALLY tracks the VARIATION in RTT (DevRTT), since a highly VARIABLE RTT should warrant a more CONSERVATIVE (larger) timeout than a highly STABLE one at the SAME average value: DevRTT = (1−β)×DevRTT + β×|SampleRTT − EstimatedRTT|, typically with β=0.25.
4. WHENEVER a retransmission timeout actually occurs and a retransmission is sent, TCP typically DOUBLES the RTO for the NEXT attempt (a form of exponential backoff directly analogous to the binary exponential backoff already examined in the previous Data Link Layer chapter's own CSMA/CD material), rather than immediately reverting to the ordinary, dynamically estimated value — this avoids repeatedly retrying too aggressively against a link that may currently be experiencing a sustained period of genuine congestion or outright failure. THE final RTO is then computed as RTO = EstimatedRTT + 4×DevRTT — this specific formula (Jacobson's algorithm) directly incorporates VARIABILITY, not merely the average, precisely because a network exhibiting large RTT swings needs substantially more margin before a genuinely delayed (but not lost) segment would incorrectly trigger a premature retransmission.

1. Worked example (RTO computation, independently verified): given EstimatedRTT=100ms and DevRTT=20ms after several rounds of measurement, compute the resulting RTO. RTO = 100 + 4×20 = 100+80 = 180 ms — directly confirming that DevRTT's own contribution (80ms) can be a SUBSTANTIAL fraction of the total RTO even when EstimatedRTT itself is comparatively small, precisely reflecting Jacobson's algorithm's own deliberate emphasis on variability rather than merely the raw average delay.

NAGLE'S ALGORITHM AND DELAYED ACKNOWLEDGMENT

1. NAGLE'S ALGORITHM specifically addresses the "SILLY WINDOW SYNDROME" problem where an application generates data in very SMALL chunks (a single keystroke, for instance), each of which, if sent IMMEDIATELY as its own tiny TCP segment, would waste a disproportionate fraction of the network's own capacity on HEADER overhead relative to the tiny genuine payload — Nagle's algorithm instead BUFFERS small amounts of outgoing data until EITHER a full-sized segment's worth has accumulated, OR all currently outstanding data has been ACKNOWLEDGED, whichever occurs FIRST, batching many small writes into fewer, more efficient segments.
2. DELAYED ACKNOWLEDGMENT, examined next, is the RECEIVER-side counterpart to Nagle's own sender-side mechanism: rather than sending a SEPARATE acknowledgment segment for every single received segment, a receiver may briefly DELAY its acknowledgment (typically up to 200ms, or until it in any case has its OWN outgoing data ready to piggyback the ACK onto), hoping to COMBINE the acknowledgment with either a later ACK for a subsequent segment, or with actual outgoing application data traveling in the OPPOSITE direction, reducing the total number of purely-acknowledgment-carrying segments sent.
3. NAGLE'S ALGORITHM and DELAYED ACKNOWLEDGMENT can, notoriously, INTERACT badly together: if a sender is waiting (per Nagle) for an acknowledgment before sending its next small chunk, while the receiver is simultaneously DELAYING that very acknowledgment (per delayed ACK) hoping for outgoing data to piggyback onto that never actually arrives, BOTH sides can end up stalled, each waiting on the OTHER, for the FULL delayed-ACK timeout period — this specific interaction is a well-documented, genuinely real-world performance pathology, and is frequently disabled (Nagle's algorithm specifically) for latency-sensitive, interactive applications that send small, time-critical messages frequently.

GATE TRAP: assuming Nagle's algorithm and delayed acknowledgment serve the SAME purpose is a common confusion — Nagle's algorithm is a SENDER-side mechanism reducing the NUMBER of small outgoing DATA segments; delayed acknowledgment is a RECEIVER-side mechanism reducing the number of separate ACKNOWLEDGMENT-only segments — they address the SAME general "too many small segments" inefficiency, but from OPPOSITE ends of the connection, and their well-known negative interaction arises PRECISELY because each one, independently reasonable, can end up waiting on a condition the OTHER mechanism is itself delaying.

TCP THROUGHPUT AND FAIRNESS UNDER AIMD

1. TCP's own congestion-avoidance behavior, examined already earlier in this chapter — ADDITIVE INCREASE (cwnd grows by roughly 1 MSS per RTT when no loss occurs) combined with MULTIPLICATIVE DECREASE (cwnd is HALVED upon detecting loss) — is commonly abbreviated AIMD, and this SPECIFIC combination (rather than, say, multiplicative increase with additive decrease) is not arbitrary: AIMD can be MATHEMATICALLY shown to converge toward an EQUAL, FAIR division of a shared bottleneck link's own capacity among multiple competing TCP connections, REGARDLESS of their respective starting points.
2. A widely cited, simplified THROUGHPUT FORMULA for TCP under STEADY, low-to-moderate random loss (ignoring slow start and assuming the connection spends the overwhelming majority of its time in ordinary congestion avoidance) approximates average throughput as proportional to MSS / (RTT × √(loss probability)) — this formula, though a considerable simplification of TCP's full behavior, directly illustrates TWO separate practical consequences: a connection with a LARGER round-trip time achieves systematically LOWER throughput than one with a smaller RTT experiencing the IDENTICAL loss rate (a documented real-world FAIRNESS concern for long-distance connections competing against short, local ones), and throughput is far more SENSITIVE to loss probability than to a modest change in RTT, since loss probability appears under a SQUARE ROOT with an inverse relationship.
3. THIS RTT-based unfairness is a genuinely important, well-documented practical limitation of ordinary TCP, worth keeping distinct from the loss-rate-based fairness AIMD otherwise achieves so cleanly: two connections sharing the identical bottleneck link and experiencing the SAME loss rate will NOT actually achieve equal throughput if their OWN round-trip times differ substantially, since the shorter-RTT connection can grow its OWN congestion window (and therefore recover from any given loss event) considerably FASTER in absolute wall-clock time than the longer-RTT connection can.

1. Worked example (relative TCP throughput comparison, independently verified): two TCP connections share a bottleneck link and experience an IDENTICAL loss probability. Connection A has RTT=20ms; connection B has RTT=80ms (four times longer). Using the simplified throughput formula (proportional to 1/RTT for a fixed loss probability and MSS), compute the RATIO of connection A's throughput to connection B's throughput. Since throughput is proportional to 1/RTT here, the ratio is RTT_B/RTT_A = 80/20 = 4 — connection A (the shorter-RTT connection) achieves roughly FOUR TIMES the throughput of connection B under this simplified model, despite BOTH connections experiencing the exact same loss rate on the exact same shared bottleneck link, directly illustrating the RTT-based unfairness this section describes.

THE SOCKET API: HOW APPLICATIONS USE TRANSPORT SERVICES

1. A SOCKET is the operating-system-level ABSTRACTION an application program uses to access transport-layer services — from the application's own point of view, a socket behaves much like a FILE descriptor (data can be written to it and read from it using largely the same familiar read/write system calls a program already uses for ordinary files), hiding essentially all of the underlying segmentation, sequencing, and retransmission detail this chapter has examined in depth.
2. A TYPICAL TCP SERVER'S socket lifecycle, followed in this exact order, involves: CREATING a socket, BINDING it to a specific local port, LISTENING for incoming connection requests, ACCEPTING an incoming connection (which, notably, returns a BRAND-NEW socket dedicated to that SPECIFIC client, leaving the original listening socket free to accept FURTHER, additional incoming connections), and then READING and WRITING data over that new, per-connection socket.
3. A TYPICAL TCP CLIENT'S socket lifecycle is considerably simpler than the server's own multi-step sequence above: CREATING a socket and CONNECTING it directly to the server's known IP address and port (this CONNECT call is what actually triggers the three-way handshake examined earlier in this chapter), then reading and writing data over the resulting, already-established connection.
4. A UDP socket's usage differs in ONE structurally important way: since UDP is connectionless, there is no persistent "connected" state analogous to TCP's — each individual SEND operation must specify the destination address explicitly (rather than relying on an established connection to remember it), and each RECEIVE operation typically returns the SENDER's address ALONGSIDE the received data, since a UDP socket may legitimately receive datagrams from MANY different, unrelated senders in sequence.
5. A concurrent TCP server, handling MANY simultaneous clients, typically dedicates a SEPARATE thread or process to each ACCEPTED connection's own new socket, allowing that connection's own read/write operations to BLOCK independently without stalling the server's ability to accept and service OTHER, unrelated connections concurrently — an ALTERNATIVE, single-threaded design instead uses NON-BLOCKING sockets together with an event-driven mechanism (such as select or epoll on Unix-like systems) to monitor many sockets simultaneously within a single thread, trading the simplicity of one-thread-per-connection for considerably better scalability when handling a very large number of concurrent, mostly-idle connections at once — directly echoing this book's own earlier Operating Systems material comparing multi-threaded and event-driven concurrency models for a broader class of problems well beyond networking alone.

KEY: the socket API's own "returns a brand-new, per-connection socket from accept()" behavior is structurally the SAME idea as an operating system's own process FORK — a listening resource spawns off a fresh, independent instance dedicated to ONE specific client, while the ORIGINAL resource remains available to service further requests, directly echoing this book's own earlier Operating Systems Process-Management chapter's own discussion of process creation as a mechanism for handling multiple independent units of work concurrently from a single originating point.

KEY: TCP's own MAXIMUM SEGMENT SIZE (MSS) is directly related to, but distinct from, the Maximum Transmission Unit (MTU) examined in the previous Network Layer chapter's own fragmentation discussion — MSS is chosen to be small enough that a TCP segment, once wrapped in its own TCP and IP headers, still fits within the path's own smallest MTU WITHOUT requiring IP-level fragmentation at all (typically MSS = MTU − 40, accounting for a 20-byte IP header plus a 20-byte TCP header) — TCP's own preference for avoiding fragmentation entirely, rather than relying on IP to fragment an oversized segment, reflects the SAME "push complexity to the endpoints, keep intermediate handling simple" philosophy already identified as a recurring theme running through this entire Computer Networks sequence.

KEY: this chapter's own RTO-estimation formula (EstimatedRTT smoothed via an exponentially weighted moving average, with RTO further padded by a multiple of the observed variability) is a specific instance of a GENERAL "adapt a threshold to OBSERVED, dynamically changing conditions rather than fixing it at a single static value" pattern this book has now encountered repeatedly — directly paralleling the previous Data Link chapter's own binary exponential backoff (adapting retry timing to OBSERVED contention) and the Databases Indexing chapter's own dynamic hashing schemes (adapting bucket structure to OBSERVED data volume), reinforcing that dynamically self-adjusting parameters, rather than fixed constants, are a recurring hallmark of robust systems design across this book's entire syllabus.

WORKED PROBLEMS, PART TWO: RTO AND THROUGHPUT

6. EWMA RTT UPDATE TRACE. EstimatedRTT is currently 100ms. A new SampleRTT measurement of 140ms arrives. Using α=0.125, compute the updated EstimatedRTT. New EstimatedRTT = (1−0.125)×100 + 0.125×140 = 0.875×100 + 0.125×140 = 87.5+17.5 = 105 ms — directly confirming that a SINGLE sample, even one 40ms above the previous estimate, moves the smoothed estimate by only 5ms, exactly reflecting the exponentially weighted moving average's own deliberate resistance to being swayed too heavily by any ONE individual measurement.

7. DEVRTT UPDATE AND FULL RTO RECOMPUTATION. Continuing WP6, DevRTT was previously 10ms. Using β=0.25 and the new SampleRTT of 140ms with the PRIOR EstimatedRTT of 100ms (the value used to compute the deviation, per the standard formula), compute the updated DevRTT and the resulting new RTO. |SampleRTT − EstimatedRTT| = |140−100| = 40. New DevRTT = (1−0.25)×10 + 0.25×40 = 7.5+10 = 17.5 ms. New RTO = new EstimatedRTT + 4×new DevRTT = 105 + 4×17.5 = 105+70 = 175 ms — illustrating that a single, unusually large SampleRTT measurement can noticeably inflate DevRTT (nearly doubling it here, from 10 to 17.5), which in turn substantially raises the resulting RTO, exactly the intended, deliberately conservative response to newly observed variability in the connection's own delay.

GATE TRAP: using the NEWLY updated EstimatedRTT (rather than the PRIOR value) when computing the deviation |SampleRTT − EstimatedRTT| for the DevRTT update is a common computational error — the standard formula specifically uses the EstimatedRTT value AS IT STOOD BEFORE this round's own update, not the value AFTER incorporating the current SampleRTT, since the deviation is meant to measure how far the LATEST sample strayed from the PREVIOUSLY settled estimate, not from an estimate that has already been adjusted to partially account for that very same sample.

WORKED PROBLEMS

1. TCP VERSUS UDP PROTOCOL SELECTION. For each of the following applications, identify whether TCP or UDP is the more appropriate transport protocol, and justify the choice using this chapter's own criteria: (a) a file-transfer application where every byte must arrive correctly and in order; (b) a live video conferencing application where an occasional dropped frame is preferable to the added latency of waiting for retransmission; (c) a DNS resolver performing a single, small query-response exchange. For (a): TCP, since guaranteed, in-order, reliable delivery of every byte is explicitly required, exactly the guarantee TCP's own sequence-number and acknowledgment machinery provides. For (b): UDP, since the application explicitly prioritizes low latency over perfect reliability, and TCP's own retransmission-based reliability would introduce exactly the kind of delay this application wants to avoid. For (c): UDP, since the exchange is small enough that TCP's own three-way-handshake connection-setup overhead would likely exceed the cost of the actual query-response data itself, directly matching the SIMPLE query-response use case this chapter's own UDP section identifies.

2. THREE-WAY HANDSHAKE SEQUENCE-NUMBER TRACE. A client initiates a TCP connection with initial sequence number 5000. The server responds with initial sequence number 9000. Trace the exact sequence and acknowledgment numbers carried in all three handshake segments. SEGMENT 1 (client to server, SYN): sequence number = 5000 (the client's chosen initial value), no acknowledgment number applicable (ACK flag not set). SEGMENT 2 (server to client, SYN-ACK): sequence number = 9000 (the server's own chosen initial value), acknowledgment number = 5001 (client's initial sequence number + 1, since a SYN itself consumes one sequence-number slot). SEGMENT 3 (client to server, ACK): sequence number = 5001 (continuing from the client's own initial value, now incremented for its SYN), acknowledgment number = 9001 (server's initial sequence number + 1) — directly confirming that BOTH sides increment their peer's advertised initial sequence number by exactly 1 specifically to acknowledge the SYN flag's own one-unit sequence-space consumption.

3. CUMULATIVE ACKNOWLEDGMENT UNDER PACKET LOSS. A TCP sender transmits three segments carrying bytes 1000-1999, 2000-2999, and 3000-3999 respectively. The SECOND segment (2000-2999) is lost, but the first and third arrive successfully. State the acknowledgment number(s) the receiver sends, and explain why it cannot acknowledge byte 4000. Upon receiving the first segment (1000-1999), the receiver acknowledges with ACK=2000 (the next expected byte). Upon receiving the THIRD segment (3000-3999) — which arrives OUT of order, since the second segment is still missing — the receiver can only RE-ACKNOWLEDGE ACK=2000 again (a DUPLICATE acknowledgment), since basic cumulative acknowledgment can only confirm a single, unbroken prefix of the byte stream, and byte 2000 itself is still missing; the receiver CANNOT acknowledge byte 4000 despite already holding bytes 3000-3999, precisely because doing so would falsely claim bytes 2000-2999 were also received, which is exactly the limitation SACK (described earlier in this chapter) exists to address.

4. FAST RETRANSMIT TRIGGER IDENTIFICATION. Continuing WP3's scenario, the sender subsequently transmits TWO more segments (bytes 4000-4999 and 5000-5999), both of which arrive successfully at the receiver. Determine how many duplicate ACKs the sender has now received for ACK=2000, and whether fast retransmit should trigger. Each of the two additional out-of-order segments (4000-4999 and 5000-5999) prompts ANOTHER duplicate ACK=2000 from the receiver, exactly as segment three's own arrival already did — bringing the total to THREE duplicate ACKs (beyond the original, non-duplicate ACK=2000). Per the earlier RELIABLE DATA TRANSFER section's own rule, three duplicate ACKs trigger FAST RETRANSMIT: the sender should immediately retransmit the missing segment (bytes 2000-2999) WITHOUT waiting for its retransmission timer to expire, considerably speeding up recovery from this single lost segment.

5. CONGESTION-CONTROL RESPONSE COMPARISON. A TCP connection has cwnd=32 MSS when a loss is detected. Compute the resulting cwnd and ssthresh under (a) a timeout, and (b) triple duplicate ACKs, and compare the two outcomes. Under (a) TIMEOUT: ssthresh = 32/2 = 16 MSS; cwnd resets to 1 MSS, and slow start restarts entirely from scratch. Under (b) TRIPLE DUPLICATE ACKS (fast recovery): ssthresh = 32/2 = 16 MSS (the SAME halving rule); cwnd is set to ssthresh + 3 = 16+3 = 19 MSS, and congestion avoidance resumes DIRECTLY from there, without ever collapsing all the way back to 1. Comparing the two: BOTH halve ssthresh identically, but the resulting cwnd differs dramatically (1 MSS versus 19 MSS) — directly quantifying, with concrete numbers, the GATE TRAP box's own qualitative claim that a timeout provokes a far more drastic corrective response than triple-duplicate-ACKs do for the identical starting cwnd.

6. FULL SYNTHESIS: DIAGNOSING A SLOW FILE TRANSFER. A large file transfer over TCP is progressing far more slowly than the link's own advertised bandwidth would suggest, and network monitoring shows the connection is currently in congestion avoidance with a stable, small cwnd, experiencing occasional isolated packet loss events roughly once every few hundred round trips, on a connection with an unusually large RTT (a satellite link, echoing the earlier Computer Networks Basics chapter's own bandwidth-delay-product discussion). Explain, drawing on this chapter's own THROUGHPUT AND FAIRNESS section, why this specific combination of symptoms points to RTT itself, rather than the loss rate, as the primary bottleneck, and state what would happen to throughput if the SAME connection instead ran over a short, low-RTT local link experiencing the identical loss rate. Since the simplified throughput formula is proportional to 1/(RTT×√(loss probability)), and RTT here is unusually LARGE (a satellite link's own propagation delay, as quantified in this book's own earlier delay-component discussion, can be hundreds of milliseconds), throughput suffers DIRECTLY and proportionally from this large RTT, independent of whatever the loss rate happens to be. Moving the SAME connection to a short, low-RTT local link while holding the loss rate fixed would, per the same formula, INCREASE throughput by roughly the SAME ratio as the RTT reduction (exactly the fourfold relationship WP5 in this chapter's own worked-problems section already demonstrated numerically) — directly confirming that for a connection already experiencing only occasional, isolated loss (rather than frequent, heavy loss), RTT itself, not the loss rate, is very often the DOMINANT factor limiting achievable throughput, a genuinely important practical diagnostic insight for exactly the kind of long-distance, high-RTT connections increasingly common on today's globally distributed Internet.

7. IDENTIFYING A SYN FLOOD IN PROGRESS. A server's connection-state table shows an unusually large number of half-open connections (received a SYN, sent a SYN-ACK, but never received the final ACK), originating from many different, seemingly random source IP addresses, none of which ever proceed to send any actual application data. Using this chapter's own earlier GATE TRAP discussion of the SYN flood vulnerability, explain what is happening and why a simple firewall rule blocking "excessive SYN segments from a single source IP" would be an ineffective defense here specifically. This is a classic SYN FLOOD: an attacker is deliberately sending SYN segments with SPOOFED (fake, essentially random) source addresses, exploiting the server's own obligation to allocate half-open connection state immediately upon receiving each SYN, well BEFORE the three-way handshake's final ACK ever confirms the client is genuine and reachable. A firewall rule targeting "excessive SYNs from a SINGLE source" is ineffective here PRECISELY because the attack's own spoofed source addresses are spread across MANY different (fake) values rather than concentrated on one real, identifiable source, defeating any defense that relies on identifying and blocking a single, persistently offending IP address; genuinely effective defenses instead include SYN cookies (a technique that avoids allocating real state until the final ACK actually arrives, encoding the necessary state cryptographically within the SYN-ACK's own sequence number rather than storing it server-side) rather than any simple, per-source-IP rate-limiting rule.

Before moving to the next chapter, it is worth retracing why this chapter's own sections again build in a specific, deliberate order, exactly as the previous two chapters' own closing sections did. Multiplexing/demultiplexing via port numbers had to come first, since every subsequent mechanism in this chapter operates WITHIN a single, already-identified connection or datagram flow between two specific processes. UDP's minimal service came next, establishing the "floor" of what a transport protocol must provide at an absolute minimum, before TCP's considerably richer machinery — connection establishment, reliable delivery, flow control, and congestion control — was examined as a series of ADDITIONAL guarantees layered progressively on top of that same floor. The RTO-estimation, Nagle/delayed-ACK, and TCP-variant material that followed then showed how TCP's own abstract mechanisms are TUNED and REFINED in real, practical implementations, before the socket-API material closed the loop by showing how an ordinary application program actually INVOKES all of this machinery through a comparatively simple, file-like programming interface. A candidate who can trace this progression — from bare process identification, through a minimal service, through a maximal service's individual guarantees, through practical tuning, to the application-facing interface itself — will find that even an unfamiliar Transport Layer question almost always reduces to identifying which specific stage of this progression it is actually probing.

The NEXT chapter in this sequence, on the APPLICATION LAYER, builds DIRECTLY on this chapter's own TCP and UDP transport services to examine the actual, user-facing protocols — DNS, HTTP, SMTP/POP/FTP — that applications use to communicate meaningfully over the network, along with the basics of WiFi and network security, completing this book's own five-chapter Computer Networks sequence exactly as the earlier Databases sequence was completed by its own final Transactions chapter.
`
};
