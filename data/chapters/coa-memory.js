// Textbook chapter: Memory Hierarchy and Cache Organization.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['coa-memory'] = {
  figs: [
    {
      id: 'memory-hierarchy-pyramid',
      caption: 'The memory hierarchy: speed and cost per bit fall while capacity rises as you move down from registers to secondary storage. Each level exists purely to make the level below it look faster than it really is.',
      svg: '<svg viewBox="0 0 300 170" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><polygon points="130,10 170,10 190,40 110,40"/><polygon points="110,40 190,40 210,75 90,75"/><polygon points="90,75 210,75 235,115 65,115"/><polygon points="65,115 235,115 265,160 35,160"/></g><g font-size="9" fill="currentColor" text-anchor="middle"><text x="150" y="28">Registers</text><text x="150" y="61">Cache (L1/L2/L3)</text><text x="150" y="99">Main memory (DRAM)</text><text x="150" y="141">Secondary storage</text></g></svg>'
    },
    {
      id: 'address-partition-diagram',
      caption: 'How a single memory address splits into tag, index, and offset fields for direct-mapped and set-associative caches. A fully associative cache drops the index field entirely — the whole non-offset address becomes tag.',
      svg: '<svg viewBox="0 0 300 90" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><rect x="10" y="15" width="120" height="30"/><rect x="130" y="15" width="90" height="30"/><rect x="220" y="15" width="60" height="30"/></g><g font-size="10" fill="currentColor" text-anchor="middle"><text x="70" y="34">Tag</text><text x="175" y="34">Set Index</text><text x="250" y="34">Block Offset</text><text x="150" y="65" font-size="9">MSB ← address bits → LSB</text></g></svg>'
    }
  ],
  text: `
WHY A HIERARCHY AT ALL

A single memory technology cannot simultaneously be fast, cheap per bit, and large — the three properties trade off directly against each other in the underlying physics and economics of memory devices, so any real system that wants the ILLUSION of a single large, fast, cheap memory must instead build a HIERARCHY of several progressively larger, slower, cheaper technologies, and rely on two empirical properties of real programs to make that illusion work almost perfectly in practice.

1. TEMPORAL LOCALITY: a memory location accessed once is DISPROPORTIONATELY likely to be accessed again SOON — a loop counter, a frequently called function's own instructions, a hot data structure inside a tight loop, are all accessed repeatedly within a short span of time.
2. SPATIAL LOCALITY: a memory location accessed once makes NEARBY locations disproportionately likely to be accessed SOON afterward — sequential instruction fetch, array traversal, and record-field access all touch adjacent addresses in close succession.

KEY: the entire cache-design discipline in this chapter is best understood as a single, repeated engineering answer to one question — "given that whatever was JUST accessed (or something physically close to it) will PROBABLY be accessed again very soon, how do we keep exactly that material in the FASTEST available memory, while keeping everything else in slower, cheaper memory, without the programmer ever having to manage this split by hand?" Every mapping scheme, replacement policy, and write policy in this chapter is one particular answer to some sub-question nested inside that same overall goal.

[[FIG:memory-hierarchy-pyramid]]

HIT RATIO, MISS RATIO, AND WHY THEY DRIVE EVERYTHING

HIT RATIO (h) at a given memory level = (accesses satisfied AT that level) / (total accesses reaching that level). MISS RATIO = 1 − h, the fraction that must be serviced by searching FURTHER DOWN the hierarchy, at correspondingly greater latency cost.

1. A cache with a hit ratio close to 1 behaves, from the processor's point of view, almost as if the ENTIRE memory ran at cache speed — this is the entire economic justification for building a small, expensive, fast cache alongside a large, cheap, slow main memory rather than simply building all of memory out of the fast technology (which would be prohibitively expensive) or all of it out of the cheap technology (which would be far too slow).
2. Because real programs exhibit strong locality, a cache holding only a SMALL fraction of main memory's total capacity can nonetheless achieve a VERY high hit ratio — often above 90% for a reasonably sized cache on typical workloads — which is precisely why the hierarchy approach works so well in practice rather than being merely a theoretical compromise.

AVERAGE MEMORY ACCESS TIME: THE SINGLE-LEVEL FORMULA

AMAT (single cache level) = Hit time + Miss rate × Miss penalty.

1. HIT TIME is the time to determine a hit occurred AND deliver the data, assuming the access does in fact hit — this is the cost paid on EVERY access, hit or miss alike, since the cache must first be checked before any determination can be made either way.
2. MISS PENALTY is the ADDITIONAL time required, beyond the hit time already paid, to fetch the requested data from the NEXT level down the hierarchy once a miss has been detected — this additional cost is paid ONLY on the fraction of accesses that actually miss, which is exactly why it is multiplied by the miss rate rather than added in full to every single access.

1. Worked check: a cache has hit time 1 ns, miss rate 5%, and miss penalty 80 ns. AMAT = 1 + 0.05×80 = 1+4 = 5 ns — five times slower than a guaranteed hit would be, illustrating directly how even a fairly small 5% miss rate, multiplied by a LARGE miss penalty, can dominate the average access time entirely.

GATE TRAP: writing AMAT as simply "Hit time × Hit rate + Miss time × Miss rate" and then treating "Miss time" as the FULL time to reach the next level (rather than the ADDITIONAL time beyond the hit time already spent checking the cache) is a subtle but common double-counting or under-counting error — the standard, GATE-expected formula is Hit time + Miss rate × Miss penalty, where miss penalty is defined as the EXTRA time beyond the hit-time check, not the next level's access time in isolation.

MULTILEVEL CACHES: LOCAL VS GLOBAL MISS RATES

Once a second cache level (L2) sits between L1 and main memory, TWO different notions of "miss rate" become necessary and must never be confused with each other.

1. LOCAL MISS RATE of L2 = (misses AT L2) / (accesses REACHING L2) — critically, "accesses reaching L2" already EXCLUDES every access that hit in L1, since those never reach L2 at all. This is the rate an L2 cache designer would naturally quote when describing L2's own effectiveness in isolation.
2. GLOBAL MISS RATE of L2 = (misses at L2) / (TOTAL original CPU references, including the ones that hit in L1) = L1 miss rate × L2 local miss rate — this is the fraction of ALL the processor's memory references, from the very start, that end up needing to reach all the way down to main memory.
3. L1's own local and global miss rates are ALWAYS IDENTICAL, since by definition every single CPU reference reaches L1 first — the local/global distinction only becomes meaningful starting from the SECOND level onward, where the population of "accesses reaching this level" has already been filtered by every level above it.

GATE TRAP: GATE numericals frequently give ONE of {local miss rate, global miss rate} explicitly and expect the OTHER to be derived via Global = L1 miss rate × L2 local miss rate — silently assuming a given miss-rate figure is "the" miss rate without checking WHICH of the two kinds it actually is (local to that level, or global across all references) is a reliable source of an answer that is wrong by a multiplicative factor equal to the higher level's own miss rate.

AMAT FOR TWO CACHE LEVELS

AMAT (two levels, L1 and L2, before main memory) = L1 Hit time + L1 Miss rate × (L2 Hit time + L2 LOCAL Miss rate × Main memory access time).

1. This formula is built by RECURSIVELY applying the single-level AMAT idea one level at a time: from the processor's perspective, "missing L1" itself has its OWN average cost, and that cost is EXACTLY the AMAT of the L2-plus-main-memory subsystem sitting beneath L1 — the same "hit time plus miss rate times miss penalty" pattern reapplied one layer down.
2. An EQUIVALENT form using the GLOBAL L2 miss rate instead: AMAT = L1 Hit time + L1 Miss rate × L2 Hit time + Global L2 Miss rate × Main memory access time — algebraically identical to the recursive form above, since Global L2 miss rate is defined as L1 miss rate × L2 local miss rate, and distributing the L1-miss-rate factor across the recursive form's inner parenthesis produces exactly this flattened version.

1. Worked example (two-level AMAT, local and global miss rates cross-checked): L1 hit time = 2 ns, L1 local miss rate = 8%. L2 hit time = 15 ns, L2 local miss rate = 25% (of requests reaching L2, i.e., of L1 misses). Main memory access time = 100 ns.
2. Global L2 miss rate = L1 miss rate × L2 local miss rate = 0.08 × 0.25 = 0.02 — exactly 2% of ALL CPU references miss both cache levels and must reach main memory.
3. Recursive-form AMAT = L1 hit time + L1 miss rate × (L2 hit time + L2 local miss rate × main memory time) = 2 + 0.08×(15 + 0.25×100) = 2 + 0.08×(15+25) = 2 + 0.08×40 = 2 + 3.2 = 5.2 ns.
4. Flattened-form cross-check using the global miss rate directly: AMAT = L1 hit time + L1 miss rate × L2 hit time + Global L2 miss rate × main memory time = 2 + 0.08×15 + 0.02×100 = 2 + 1.2 + 2.0 = 5.2 ns — matching the recursive form exactly, confirming the local/global relationship was applied consistently in both computations.

CACHE MAPPING: THE BIT-PARTITION RECIPE

Every mapping scheme answers the SAME underlying question — "given a requested main-memory address, which cache LINE(S) could possibly hold it, and how do we verify a genuine hit once we look there?" — and every scheme's answer takes the form of splitting the address into a small number of contiguous bit fields.

[[FIG:address-partition-diagram]]

1. WORD/BYTE OFFSET BITS (o) = log₂(block size B) — identical across ALL THREE mapping schemes below, since the offset's only job is to locate one specific byte WITHIN whichever block has already been found; how that block itself gets located is what actually differs between the schemes.
2. DIRECT MAPPED: number of cache lines = C/B (cache size divided by block size). INDEX BITS (i) = log₂(C/B) — selects the SINGLE specific line a given memory block is forced to occupy. TAG BITS (t) = log₂(M) − i − o, where M is main memory size. Address layout: [ Tag | Line Index | Block Offset ]. Every memory block maps to EXACTLY one line, so two blocks sharing the same index but different tags will repeatedly evict each other even while the REST of the cache sits idle — a phenomenon called a CONFLICT MISS.
3. FULLY ASSOCIATIVE: there is NO index field at all — a block may be placed in ANY line whatsoever, so the ENTIRE remaining address beyond the offset becomes the tag. TAG BITS (t) = log₂(M) − o. Address layout: [ Tag | Block Offset ] only. Locating a block requires comparing the incoming tag against EVERY line's stored tag SIMULTANEOUSLY (a genuinely associative, parallel hardware search) — maximally flexible placement, but the comparator hardware cost scales with the number of lines, which is why fully associative caches are used only where the number of lines is small (a TLB being the classic example).
4. SET-ASSOCIATIVE (n-way): number of SETS (S) = (C/B)/n. SET-INDEX BITS (i) = log₂(S) = log₂(C/(B×n)). TAG BITS (t) = log₂(M) − i − o. Address layout: [ Tag | Set Index | Block Offset ]. A block may go into any of the n lines WITHIN its own designated set only — direct-mapped is the special case n=1 (one line per set), and fully associative is the special case n = C/B (one giant set containing every line).

GATE TRAP: forgetting that a fully associative cache has NO index field whatsoever, and instead trying to compute an "index bit count" for it as though it were merely a set-associative cache with some particular associativity, is a frequent source of an incorrect tag-width answer — the correct fully-associative tag width consumes ALL of the non-offset address bits, none of which are ever spent on an index.

1. Worked example (bit-partition, set-associative): main memory = 4 GB = 2^32 bytes, so the total address width = 32 bits. Cache size = 256 KB = 2^18 bytes. Block size = 64 bytes = 2^6 bytes. Associativity = 4-way.
2. Offset bits: o = log₂(64) = 6.
3. Number of sets: S = (C/B)/n = (2^18/2^6)/4 = 2^12/4 = 2^10 = 1024, so index bits i = log₂(1024) = 10.
4. Tag bits: t = 32 − 10 − 6 = 16.
5. Address layout check: 16 (tag) + 10 (index) + 6 (offset) = 32 bits total — exactly matching the given 32-bit address width, confirming the partition is complete and consistent.

GATE TRAP: an off-by-one power error is the single most common mistake in this entire bit-partition topic — always convert every size given in KB, MB, or GB into raw bytes FIRST (256 KB = 2^18 bytes, 4 GB = 2^32 bytes, and so on), and confirm each conversion explicitly (2^10=1024=1K, 2^20=1M, 2^30=1G) before taking any logarithm, rather than trusting a rushed mental exponent under time pressure.

REPLACEMENT POLICIES: WHICH LINE GETS EVICTED

A replacement policy is needed only for associative or set-associative caches — a direct-mapped cache has exactly one possible destination line per block, so there is never any actual CHOICE to make, and hence no replacement policy question arises for it at all.

1. LRU (LEAST RECENTLY USED): evict whichever line has gone UNUSED for the LONGEST time — directly exploits temporal locality (the working assumption that a line unused for a long stretch is unlikely to be needed again SOON), but requires extra recency-tracking hardware (counters or a small ordered structure) per set, and that hardware's cost GROWS with the associativity, since more lines per set means more relative orderings to track.
2. FIFO (FIRST IN, FIRST OUT): evict whichever line was LOADED longest ago, REGARDLESS of how recently it was actually USED — cheaper to implement than LRU (a single counter or pointer per set suffices), but can perform noticeably worse whenever an old-but-still-frequently-used line gets evicted purely because of its age rather than its actual usage pattern.
3. RANDOM: evict a RANDOMLY chosen line from the set — the cheapest possible policy to implement in hardware, and, surprisingly, becomes fairly competitive with LRU once associativity is HIGH, since with many candidate lines the chance of randomly picking one that genuinely will not be needed again soon becomes reasonably good on its own.
4. OPTIMAL (BELADY'S / MIN) REPLACEMENT: evict whichever line will NOT be needed again for the LONGEST time INTO THE FUTURE — this achieves the theoretical LOWEST POSSIBLE miss rate for any given cache configuration and reference sequence, but requires KNOWING the future reference sequence in advance, so it is never implementable as an online, real-time hardware policy; it exists purely as a BENCHMARK against which real, implementable policies like LRU are measured.

GATE TRAP: assuming LRU always strictly outperforms FIFO and Random in every single reference sequence is not correct — LRU is the best PRACTICAL heuristic on AVERAGE across typical workloads exhibiting genuine locality, but for a SPECIFIC adversarially chosen reference sequence, it is entirely possible to construct a case where FIFO or even Random happens to perform better; GATE questions computing an actual hit/miss sequence for a SPECIFIC given reference string must be worked through the stated policy's own exact rule mechanically, never assumed by policy reputation alone.

WRITE POLICIES: WRITE-THROUGH VS WRITE-BACK

A cache's WRITE policy governs when a write made to cache actually propagates down to main memory, and this choice has a direct, quantifiable impact on memory-bus traffic.

1. WRITE-THROUGH: every single write updates BOTH the cache line AND main memory IMMEDIATELY, in the same operation — main memory is ALWAYS kept fully consistent with the cache, which greatly simplifies any multi-processor cache-coherence protocol, but it generates memory-bus traffic on EVERY write, not merely on cache misses, since the write must reach main memory regardless of whether it was a cache hit or miss.
2. WRITE-BACK (COPY-BACK): a write updates ONLY the cache line itself, which is then marked DIRTY via a dedicated dirty bit; the modified line is written back to main memory ONLY when it is eventually EVICTED, and only if it is still marked dirty at that moment — this produces FAR less memory-bus traffic overall (a line rewritten many times while resident in cache generates only ONE eventual memory write, on eviction, rather than one write per modification), at the cost of a temporarily inconsistent main memory and a slightly more involved coherence protocol in a multi-processor setting.
3. WRITE-ALLOCATE (fetch-on-write-miss): on a WRITE MISS specifically, the block is first BROUGHT INTO the cache (exactly as a read miss would fetch it), and the write is then applied to that now-resident cached copy — typically paired with write-back, since write-back's whole benefit (absorbing many writes into one eventual memory write) only materializes if the block stays resident afterward.
4. NO-WRITE-ALLOCATE (write-around): on a write miss, the write goes DIRECTLY to main memory WITHOUT loading the block into the cache at all — typically paired with write-through, since write-through writes to memory unconditionally anyway, so there is little benefit to also caching a block that may never be read again.

1. Memory traffic under write-through (per write) ≈ (number of writes) × (word size, if only the modified word is sent to memory on a hit) — though a write MISS under a write-allocate-on-miss policy still separately fetches an entire BLOCK first, so the two traffic contributions (per-write word traffic, and per-miss block-fetch traffic) must be counted separately when both occur in the same workload.
2. Memory traffic under write-back = (number of block EVICTIONS that are DIRTY at eviction time) × (block size) [for writing the dirty block back] + (number of MISSES) × (block size) [for fetching each newly needed block] — the key structural difference from write-through is that traffic here scales with EVICTIONS-of-dirty-blocks, not with every individual write, so a block modified many times while resident still contributes only ONE eventual write-back.

GATE TRAP: assuming write-back ALWAYS produces strictly less total memory traffic than write-through in every conceivable scenario is an overgeneralization — it holds true on AVERAGE for workloads with reasonable write reuse and locality (many writes to the same resident block before it is evicted), but a write-heavy workload where every written block gets evicted again almost immediately affords write-back little opportunity to absorb repeated writes, and its traffic can then approach write-through's own; the formula-level distinction (traffic tied to dirty-block EVICTIONS, not to every write individually) is the actual mechanism GATE numericals test, not a blanket "write-back always wins" assumption.

1. Worked example (write traffic comparison): a workload performs 1000 writes to blocks of size 8 words (32 bytes each), and due to strong write locality, these 1000 writes touch only 40 DISTINCT blocks, each written multiple times before eventually being evicted exactly once, dirty. Write-through (word-granularity): 1000 × (word size, say 4 bytes) = 4000 bytes of write traffic. Write-back: 40 evictions × 32 bytes (one full dirty block write-back per distinct block) = 1280 bytes of write traffic — substantially LESS than write-through's 4000 bytes here, precisely because write-back absorbed an average of 1000/40 = 25 writes per block into a single eventual write-back each, exactly the mechanism the write-back policy is designed to exploit.

VIRTUAL MEMORY AS A FURTHER HIERARCHY LEVEL

Virtual memory extends the SAME hierarchy idea one level further, treating main memory itself as a "cache" for a much larger secondary-storage-backed address space, using PAGES (fixed-size blocks, typically several KB) rather than cache lines as the unit of transfer.

1. A PAGE TABLE performs the address-translation role a cache's tag comparison performs for cache blocks — mapping a VIRTUAL page number to a PHYSICAL frame number, with a PRESENT/VALID bit recording whether that page currently resides in main memory at all.
2. A PAGE FAULT (the virtual-memory analogue of a cache miss) triggers an OPERATING-SYSTEM-level fault handler, since the required page must be fetched from secondary storage (disk) — several orders of magnitude slower than even a main-memory access, which is exactly why virtual memory's replacement policy (deciding which resident page to evict) receives so much more design care than a cache's replacement policy: the PENALTY for a wrong choice is dramatically larger at this level of the hierarchy.
3. A TRANSLATION LOOKASIDE BUFFER (TLB) is itself a small, FULLY ASSOCIATIVE cache — specifically caching the MOST RECENTLY used virtual-to-physical page-table translations, so that the vast majority of memory accesses can skip a full, potentially multi-level page-table walk entirely; this is precisely why TLBs are built fully associative despite the general hardware cost of associative search, since a TLB's entry COUNT is deliberately kept very small.

1. Worked example (effective access time with a TLB): TLB hit time = 1 ns, TLB hit rate = 95%. On a TLB miss, a page-table walk costs an additional 20 ns to obtain the physical address, after which the actual memory access proceeds identically either way at 5 ns. Effective address-translation-plus-access time = TLB hit rate × (TLB hit time + memory access) + TLB miss rate × (TLB hit time + page-table walk + memory access) = 0.95×(1+5) + 0.05×(1+20+5) = 0.95×6 + 0.05×26 = 5.7 + 1.3 = 7.0 ns — the SAME recursive AMAT-style structure used throughout this chapter, now applied one level further out, to address translation itself rather than to data access.

THE THREE Cs: CLASSIFYING WHY A MISS HAPPENED AT ALL

Every cache miss, regardless of mapping scheme, can be classified into exactly one of three root causes, and knowing which kind of miss dominates a given workload points directly at which design change would actually help.

1. COMPULSORY (COLD-START) MISSES: the very FIRST access to a block that has NEVER been in the cache before, at any point in the program's execution — these misses are UNAVOIDABLE no matter how large or how associative the cache is made, since the data simply has not been fetched yet the first time it is ever touched. The only genuine mitigation is PREFETCHING (speculatively fetching a block before it is actually requested, based on a predicted access pattern) or increasing the BLOCK size (so that one compulsory miss brings in more of what will likely be needed soon via spatial locality, at the cost of wasting bandwidth on any of that extra data that turns out unused).
2. CAPACITY MISSES: a miss that occurs because the cache is simply TOO SMALL to hold the ENTIRE working set of blocks a program is actively cycling through — even a FULLY ASSOCIATIVE cache of the same total size would still suffer this exact same miss, since no placement flexibility can compensate for the cache genuinely not having enough total space. The only real mitigation is a LARGER cache.
3. CONFLICT MISSES: a miss that occurs ONLY because of the specific mapping restriction in place (direct-mapped or set-associative), where a block is evicted to make room for another block mapping to the SAME line or set, even though OTHER lines elsewhere in the cache sit completely unused at that moment — a fully associative cache of the identical total size would NOT suffer this particular miss, since it could have placed the new block into one of those otherwise-idle lines instead. The mitigation is INCREASING ASSOCIATIVITY (more ways per set gives a conflicting block more alternative homes before eviction becomes forced).

1. Worked example (distinguishing conflict misses from capacity misses): a direct-mapped cache holds 4 lines, and the reference sequence to 5 distinct blocks that all map to the SAME single line, cycling A,B,A,B,A, produces a miss on every single access (5 misses for 5 accesses) purely due to conflict — despite the cache having 3 OTHER lines sitting completely idle and unused throughout. Making the SAME cache 2-way set-associative (so A and B can coexist in the same set, occupying its two ways) eliminates every one of those conflict misses entirely, leaving only the two genuinely unavoidable compulsory misses (the first touch of A, and the first touch of B) — a direct, worked illustration of why associativity is the specific mitigation that targets conflict misses and no other kind.

KEY: the Three Cs classification is a DIAGNOSTIC tool, not merely a taxonomy to memorise — a GATE question describing a specific miss-rate change under a specific design modification (a larger cache, more associativity, a bigger block size) is implicitly asking which of the three miss categories that modification actually addresses, and a change that targets the WRONG category (say, increasing block size to fix what are actually capacity misses) will do little or nothing to help, occasionally even making matters worse by evicting more useful data per miss to make room for a now-larger incoming block.

GATE TRAP: increasing block size reduces compulsory misses (via spatial locality prefetching nearby data for free) but, PAST some workload-dependent optimal point, actually INCREASES both capacity misses (fewer total blocks now fit in the same total cache size) and conflict misses (fewer total lines exist to map into) — assuming "bigger block size is always strictly better" ignores this well-known non-monotonic trade-off, which is precisely why real cache designs settle on a moderate block size (typically 32 to 128 bytes) rather than the largest block size technically possible.

MEMORY INTERLEAVING AND BANDWIDTH

Beyond caching, a second, complementary technique widens the EFFECTIVE bandwidth between the cache (or CPU) and main memory itself, by splitting main memory into several independently-addressable MODULES (banks) that can each be accessed IN PARALLEL, rather than relying on a single memory module serviced strictly one request at a time.

1. LOW-ORDER INTERLEAVING: consecutive memory ADDRESSES are distributed across DIFFERENT modules in round-robin fashion (address 0 in module 0, address 1 in module 1, ..., address k−1 in module k−1, address k back in module 0, and so on, for k modules) — this means a BLOCK of consecutive addresses (exactly the access pattern a cache-line fill or a sequential array traversal produces) can be fetched from SEVERAL modules SIMULTANEOUSLY, directly multiplying the effective bandwidth for exactly this common access pattern.
2. HIGH-ORDER INTERLEAVING: consecutive addresses instead stay WITHIN the same module for a large contiguous chunk before moving to the next module — this simplifies EXPANDING memory capacity (each module is simply a self-contained, independently addressable chunk) but provides NO parallelism benefit for a sequential block access, since consecutive addresses in such an access pattern all land in the SAME single module.
3. A k-WAY LOW-ORDER INTERLEAVED memory system can, in the best case (a purely sequential access pattern with no bank conflicts), deliver UP TO k times the bandwidth of a single non-interleaved module of the same individual access time — though this best case assumes the access PATTERN cooperates (sequential, or otherwise spread evenly across modules); an access pattern that repeatedly hits the SAME module (a BANK CONFLICT) receives no such speedup for those particular accesses, since that one module still must service them strictly one at a time.

1. Worked example (interleaved memory fetch time): a cache line consists of 4 words, and main memory is 4-way LOW-ORDER interleaved, with each module's own individual access time being 20 ns and a fixed 5 ns bus-transfer time per word once a module's data becomes ready. Fetching the full 4-word line: all 4 modules are accessed IN PARALLEL (since the 4 consecutive words fall one into each of the 4 modules under low-order interleaving), so the module-access latency of 20 ns is paid ONCE, not four times; the four resulting words must still be transferred to the CPU one at a time over the single shared bus, adding 4×5=20 ns of serialized transfer time. Total fetch time = 20 (parallel module access, paid once) + 20 (four sequential 5 ns bus transfers) = 40 ns — dramatically faster than a NON-interleaved single module servicing all 4 word-fetches strictly sequentially, which would cost 4×20 + 4×5 = 80+20 = 100 ns, a direct 2.5× speedup purely from the parallelism low-order interleaving makes available for exactly this sequential block-fetch access pattern.

GATE TRAP: assuming interleaving automatically multiplies bandwidth by exactly the number of modules k in EVERY scenario ignores that the BUS itself, if shared and only capable of transferring one word at a time, remains a serializing bottleneck regardless of how many modules can be accessed in parallel underneath it — the worked example above shows the REALISTIC total time still includes k sequential bus-transfer terms even though the k module-access latencies themselves fully overlap; treating the parallel module-access saving as the ENTIRE picture, while ignoring the still-sequential bus-transfer component, is a common source of an overly optimistic timing answer.

KEY: interleaving and caching are complementary rather than competing techniques, and a well-designed system genuinely uses BOTH together — a cache reduces the NUMBER of times main memory needs to be touched at all (by absorbing repeat accesses through locality), while interleaving reduces the TIME COST of each individual trip to main memory that does still occur (specifically for the block-sized, largely sequential fetch pattern a cache MISS produces when it needs to pull in an entire line at once); recognising that a cache-line fill is EXACTLY the sequential access pattern interleaving is best suited to accelerate is what connects these two, otherwise separately-taught, techniques into a single coherent memory-system design story rather than two unrelated topics that happen to appear in the same chapter.

CACHE COHERENCE: A BRIEF FORWARD LOOK

In any system with MULTIPLE caches observing the SAME underlying main memory — most commonly, one cache per processor core in a multiprocessor system — a further concern arises beyond everything covered so far for a single cache in isolation: keeping all of those caches' views of a given shared memory location CONSISTENT with one another.

1. THE COHERENCE PROBLEM, STATED PRECISELY: if processor P1 writes a new value into its own cache for address X (under write-back, this update may not yet even be visible in main memory at all), and processor P2 subsequently reads address X from ITS OWN cache, P2 must somehow see P1's newly written value rather than a now-STALE copy left over from before P1's write — otherwise the two processors would observe genuinely different, contradictory values for the exact same memory location at the exact same real time.
2. A WRITE-INVALIDATE protocol handles this by having P1's write broadcast an INVALIDATION signal to every other cache holding a copy of that same line, forcing those other caches to discard (invalidate) their now-stale copies, so that P2's next access is guaranteed to miss and fetch the genuinely up-to-date value fresh.
3. A WRITE-UPDATE protocol instead broadcasts the NEW VALUE itself to every other cache holding a copy, updating them all directly rather than merely invalidating them — trading a higher immediate broadcast-bandwidth cost for avoiding the extra re-fetch miss a write-invalidate protocol's other caches would otherwise need to pay on their very next access to that same line.

KEY: cache coherence is, at its structural core, the write-policy question (write-through versus write-back) from earlier in this chapter revisited at a MULTI-CACHE scale — exactly as write-through keeps a single cache and main memory consistent by propagating every write immediately, and write-back instead defers that propagation, a write-invalidate or write-update protocol is choosing HOW and WHEN to propagate a write's effect not merely to one memory level beneath it, but to every SIBLING cache sharing the same underlying memory, making coherence protocols a natural, direct extension of ideas this chapter has already built up from a simpler, single-cache starting point.

WORKED PROBLEMS

1. SINGLE-LEVEL AMAT. A cache has hit time 2 ns, miss rate 4%, and miss penalty 60 ns. Compute AMAT. AMAT = 2 + 0.04×60 = 2+2.4 = 4.4 ns.

2. DIRECT-MAPPED BIT PARTITION. Main memory = 1 GB (2^30 bytes). Cache size = 64 KB (2^16 bytes). Block size = 32 bytes (2^5 bytes). Find the tag, index, and offset widths for a direct-mapped design. Offset o = log₂(32) = 5. Number of lines = 2^16/2^5 = 2^11, so index i = 11. Tag t = 30 − 11 − 5 = 14. Check: 14+11+5 = 30 ✓.

3. FULLY ASSOCIATIVE TAG WIDTH. Using the SAME memory and cache sizes as WP2 (1 GB memory, 64 KB cache, 32-byte blocks), but now fully associative, find the tag width. Offset stays o=5 (block size unchanged). No index field at all: tag t = 30 − 5 = 25 — an 11-bit LARGER tag than the direct-mapped case in WP2, since all 11 of those former index bits now fold into the tag instead.

4. SET-ASSOCIATIVE, GIVEN ASSOCIATIVITY. Using the same 1 GB memory, 64 KB cache, 32-byte blocks as above, now made 8-way set-associative, find the set-index and tag widths. Number of sets S = (2^16/2^5)/8 = 2^11/2^3 = 2^8 = 256, so index i = 8. Tag t = 30 − 8 − 5 = 17 — between the direct-mapped (11-bit index, 14-bit tag) and fully-associative (0-bit index, 25-bit tag) extremes, exactly as expected since 8-way sits strictly between those two special cases of associativity.

5. LOCAL VS GLOBAL MISS RATE. L1's global (and hence also local) miss rate is 10%. L2's GLOBAL miss rate (fraction of all CPU references reaching main memory) is measured at 3%. Find L2's LOCAL miss rate. Global L2 = L1 miss rate × L2 local, so L2 local = Global L2 / L1 miss rate = 0.03/0.10 = 0.30 = 30% of the requests that actually reach L2 (i.e., of L1's own misses) go on to miss in L2 as well.

6. TWO-LEVEL AMAT FROM SCRATCH. L1 hit time 1 ns, L1 miss rate 6%. L2 hit time 10 ns, L2 local miss rate 20%. Main memory 120 ns. Compute AMAT. AMAT = 1 + 0.06×(10 + 0.20×120) = 1 + 0.06×(10+24) = 1 + 0.06×34 = 1 + 2.04 = 3.04 ns.

7. REPLACEMENT POLICY MISS-COUNT. A fully associative cache holds 3 lines and starts empty. The reference sequence is A,B,C,A,D,B under LRU. Trace the hits/misses. A: miss (cache: A). B: miss (cache: A,B). C: miss (cache: A,B,C, now full). A: HIT (A is reused, becomes most-recently-used; LRU order now B,C,A oldest-to-newest). D: miss — evict LRU line B (cache becomes C,A,D). B: miss (B was just evicted, cache: A,D,B after evicting LRU line C). Total: 5 misses, 1 hit.

8. WRITE-THROUGH VS WRITE-BACK TRAFFIC. A block (16 bytes) is written to 5 times while resident in cache, then evicted while dirty. Compare total memory-write traffic under write-through (word size 4 bytes, one word written per write) versus write-back. Write-through: 5 × 4 = 20 bytes. Write-back: 1 eviction × 16 bytes (whole dirty block written back once) = 16 bytes — write-back is lower here, though by a smaller margin than in the WORKED EXAMPLE above, since fewer total writes (5, versus 25 per block there) were absorbed into the one eventual write-back.

9. TLB EFFECTIVE ACCESS TIME. TLB hit time 0.5 ns, hit rate 98%. TLB miss adds a 15 ns page-table walk. Memory access after translation costs 4 ns either way. Compute the effective translation-plus-access time. = 0.98×(0.5+4) + 0.02×(0.5+15+4) = 0.98×4.5 + 0.02×19.5 = 4.41 + 0.39 = 4.80 ns.

10. IDENTIFYING THE MAPPING SCHEME FROM AN ADDRESS PARTITION. A 32-bit address partitions as 20-bit tag, 8-bit index, 4-bit offset for a cache holding 2^8 = 256 lines. Is this cache direct-mapped, set-associative, or fully associative, and what is the block size? Block size = 2^4 = 16 bytes. Since there are exactly as many index values (2^8=256) as there are lines (256) — i.e., the index field alone can uniquely select any one of the lines with no further associative search needed within the group it selects — this is a DIRECT-MAPPED cache (n=1 way), not a set-associative one, since a genuinely set-associative design here would need the index field to select among FEWER groups (sets) than there are total lines, with several lines living inside each set.

11. CLASSIFYING A MISS. A fully-associative cache of size 8 lines, running a workload whose working set is exactly 8 distinct blocks accessed in a repeating round-robin pattern, shows a nonzero steady-state miss rate. Is this compulsory, capacity, or conflict? Since a fully associative cache is used, conflict misses are impossible by construction (any block can go in any line). Since the working set size (8 blocks) exactly EQUALS the cache size (8 lines), there should be no ongoing capacity pressure once every block has been loaded once — so the ONLY misses possible here are COMPULSORY, one for each of the 8 distinct blocks' first-ever access, with zero further misses once all 8 are resident (assuming the round-robin pattern never evicts a still-needed block early).

12. INTERLEAVED MEMORY BANDWIDTH. Main memory is 8-way low-order interleaved, individual module access time 24 ns, bus transfer time 3 ns per word. Compute the time to fetch an 8-word cache line, and compare to a non-interleaved single module. Interleaved: 24 (one parallel module-access latency, all 8 modules accessed simultaneously) + 8×3 (eight sequential bus transfers) = 24+24 = 48 ns. Non-interleaved: 8×24 + 8×3 = 192+24 = 216 ns. Speedup = 216/48 = 4.5× — illustrating that interleaving's benefit, while substantial, still falls short of the full 8× a naive "each module runs in parallel" intuition alone might suggest, precisely because the shared bus's sequential transfer cost is paid in full either way.

CARRYING THIS FORWARD

Every formula in this chapter is a variation on the SAME two ideas already established at the very start: a HIERARCHY of memory technologies is worth building only because programs exhibit strong temporal and spatial locality, and every "average access time" figure anywhere in this chapter — single-level AMAT, two-level AMAT, TLB-augmented effective access time — is built by the SAME recursive pattern of "pay the local hit cost always, then pay an additional penalty only on the fraction of accesses that miss, and that penalty is itself the AVERAGE cost of everything beneath this level." Recognising this one recursive template is what turns an apparently long list of formulas (single-level AMAT, two-level AMAT via local rates, two-level AMAT via global rates, TLB effective access time) into a single, reusable idea applied repeatedly at different points in the hierarchy, rather than a set of separately memorised expressions that happen to look superficially similar.

KEY: bit-partition problems (direct-mapped, fully associative, and set-associative address layouts) are, underneath their specific cache-design language, the SAME counting exercise this book has applied since its very first Digital Logic chapters — given a fixed total number of address bits, how are those bits divided among fields that each need to distinguish some specific number of distinct possibilities (which line, which set, which byte within a block)? The formulas here (o=log₂B, i=log₂(number of lines or sets), t=log₂M−i−o) are not a new kind of reasoning at all, merely this SAME log₂-counting idea applied to a new, memory-specific context.

The overall lesson this chapter reinforces, tying directly back to the pipelining chapter immediately preceding it in this same Computer Organization and Architecture sequence, is that BOTH pipelining and caching are, at bottom, techniques for hiding an unavoidable latency behind OTHER useful work or behind exploited regularity in access patterns — a pipeline hides instruction latency behind the OVERLAPPED execution of several instructions at once, while a cache hides main-memory latency behind the EXPLOITED regularity (locality) of typical memory-reference patterns; recognising both as instances of this same general "hide unavoidable latency using some exploitable structure in the workload" engineering strategy is a genuinely transferable habit that extends naturally into the very next chapter's own treatment of input/output, where yet another latency-hiding strategy (interrupts and DMA, hiding I/O latency behind continued CPU computation) will appear as one more variation on this exact same underlying theme.

KEY: the Three Cs classification (compulsory, capacity, conflict) and the two write policies (write-through, write-back) are best kept mentally organised as answers to two SEPARATE questions that a cache-design numerical can mix together within a single problem statement — "WHY did this particular miss happen" (the Three Cs, a question about the READ path and the mapping/placement decisions that caused a specific eviction or absence) versus "WHEN does a write actually reach main memory" (write-through immediately, write-back only on a dirty eviction, a question entirely about the WRITE path) — a well-posed exam question rarely confuses these two axes directly, but a hastily read one can lead a student to answer the wrong axis's question entirely, for instance explaining a miss-rate change using write-policy language when the actual cause was a change in associativity, or vice versa.

A FINAL WORKED SYNTHESIS PROBLEM, COMBINING SEVERAL TOOLS AT ONCE

To see how the tools developed across this entire chapter combine within one single, fully worked, exam-style numerical exactly as a real GATE question would present them together, consider the following combined scenario, worked through step by step using nothing beyond what has already been derived above.

1. SCENARIO: A system has a 2-way set-associative L1 cache of size 32 KB with 32-byte blocks, hit time 1 ns. An L2 cache is 512 KB, 8-way set-associative, 64-byte blocks, hit time 8 ns, with a measured LOCAL miss rate of 15%. Main memory access time is 100 ns. Main memory is 4-way low-order interleaved with each module's access latency being 25 ns and a 2 ns-per-word bus-transfer time, servicing an L2-miss block fetch of 64 bytes (16 words) from main memory. L1's own measured miss rate is 6%. Find: (a) the L1 bit-partition fields, (b) the two-level AMAT treating the L2-to-memory transfer using the interleaved timing rather than the flat 100 ns figure, and (c) the global L2 miss rate.
2. STEP 1 — L1 BIT PARTITION: offset o = log₂(32) = 5. Number of sets = (32 KB/32 B)/2 = 1024/2 = 512, so index i = log₂(512) = 9. Assuming a 32-bit address, tag t = 32 − 9 − 5 = 18.
3. STEP 2 — INTERLEAVED MEMORY FETCH TIME FOR ONE L2-MISS BLOCK: a 64-byte block split across 4-way interleaving with 16 total words to transfer means all 4 modules are accessed in parallel (25 ns, paid once), followed by 16 sequential word transfers over the shared bus at 2 ns each = 32 ns. Total memory-side time for one full block fetch = 25 + 32 = 57 ns — this REPLACES the flat "100 ns main memory access time" that a simpler problem statement might have given directly, and is exactly the kind of realistic combination a fully worked exam question is likely to actually present, requiring the interleaving-timing tool from earlier in this chapter to be folded into the AMAT calculation rather than treating memory access time as a single flat given number.
4. STEP 3 — TWO-LEVEL AMAT USING THE COMPUTED MEMORY TIME: AMAT = L1 hit time + L1 miss rate × (L2 hit time + L2 local miss rate × interleaved memory time) = 1 + 0.06×(8 + 0.15×57) = 1 + 0.06×(8+8.55) = 1 + 0.06×16.55 = 1 + 0.993 = 1.993 ns ≈ 1.99 ns.
5. STEP 4 — GLOBAL L2 MISS RATE: Global = L1 miss rate × L2 local miss rate = 0.06×0.15 = 0.009 = 0.9% of all original CPU references end up needing the full interleaved main-memory fetch.
6. CROSS-CHECK using the flattened AMAT form: AMAT = L1 hit time + L1 miss rate × L2 hit time + Global L2 miss rate × interleaved memory time = 1 + 0.06×8 + 0.009×57 = 1 + 0.48 + 0.513 = 1.993 ns — matching Step 3 exactly, confirming every tool (bit-partition, local/global miss-rate relation, two-level AMAT, and interleaved memory timing) was combined correctly and consistently across the full synthesis problem.

This single combined problem demonstrates precisely why this chapter's material rewards being understood as a small connected TOOLKIT rather than a checklist of separate topics to be revised in isolation — a genuinely difficult GATE-level question on memory hierarchy rarely tests any ONE formula from this chapter in complete isolation, but instead nests several of these tools inside one another exactly as shown here, and the discipline of working through each nested layer in careful sequence (bit-partition first, then the innermost timing computation, then folding that result outward into the AMAT formula, then cross-checking with the alternate flattened form) is the single most valuable habit this chapter's worked problems, taken together, have aimed to build.

A closing note on how this chapter's tools transfer beyond the specific numbers used throughout: none of the formulas here depend on any PARTICULAR choice of cache size, block size, associativity, or timing figure — they are general RELATIONSHIPS that hold for any values a specific question happens to supply, which is exactly why memorising the DERIVATION (why offset bits equal log₂ of block size, why AMAT recurses one level at a time, why global miss rate equals the product of local rates layer by layer, why interleaved fetch time separates a parallel module-access term from a sequential bus-transfer term) is far more valuable, and far more robust against an unfamiliar problem phrasing, than memorising any specific worked numeric answer from this chapter's own worked problems. A question that swaps GATE's characteristically clean, round numbers for awkward ones, or that combines three of this chapter's tools in an order not identical to any single worked example shown above, is still answerable by the exact same handful of underlying relationships — it merely requires recognising which relationship applies to which part of the question, in whichever order that particular question happens to present its givens, rather than pattern-matching against a memorised worked-example template. Internalising the underlying relationships, not the surface numbers, is what makes this entire chapter's toolkit genuinely reusable under exam pressure on a completely unfamiliar numeric variant.
`
};
