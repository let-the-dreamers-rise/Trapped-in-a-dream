// Textbook chapter: Input/Output Organization — Interrupts, DMA, and Disks.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['coa-io'] = {
  figs: [
    {
      id: 'io-technique-ladder',
      caption: 'The three I/O transfer techniques, ordered by how much CPU attention each demands per transfer: programmed I/O busy-waits for every item, interrupt-driven I/O is notified only when ready, and DMA offloads the entire block transfer to dedicated hardware.',
      svg: '<svg viewBox="0 0 320 130" width="100%" style="max-width:320px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><rect x="10" y="10" width="300" height="30"/><rect x="10" y="50" width="300" height="30"/><rect x="10" y="90" width="300" height="30"/></g><g font-size="10" fill="currentColor" text-anchor="middle"><text x="160" y="29">Programmed I/O — CPU busy-waits, polls every item</text><text x="160" y="69">Interrupt-driven — device signals CPU only when ready</text><text x="160" y="109">DMA — controller moves whole block, one interrupt at end</text></g></svg>'
    },
    {
      id: 'disk-service-time-breakdown',
      caption: 'Disk service time for a single random-sector access: seek time to reach the correct track, rotational latency waiting for the sector to arrive, then transfer time to read it.',
      svg: '<svg viewBox="0 0 300 100" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><rect x="10" y="20" width="100" height="30"/><rect x="110" y="20" width="100" height="30"/><rect x="210" y="20" width="80" height="30"/></g><g font-size="9" fill="currentColor" text-anchor="middle"><text x="60" y="39">Seek time</text><text x="160" y="39">Rotational latency</text><text x="250" y="39">Transfer</text><text x="150" y="70" font-size="9">Total = sum of all three, left to right</text></g></svg>'
    }
  ],
  text: `
WHY I/O NEEDS ITS OWN DESIGN DISCIPLINE

Every I/O device operates at a SPEED wildly mismatched to the CPU's own — a keyboard produces a new character perhaps once every few HUNDRED milliseconds, while a modern CPU executes billions of instructions per second, and even the FASTEST I/O devices (disks, network interfaces) remain orders of magnitude slower than main-memory access, let alone CPU register or cache access. This entire chapter is the answer to a single practical question that mismatch forces: given that the CPU is always vastly faster than whatever device it needs to talk to, how should the CPU's attention be ALLOCATED — should it wait idly, be interrupted only when truly needed, or be bypassed from the data-movement task entirely — and each of the three following techniques answers this same question with a progressively cleverer division of labour between CPU and hardware.

PROGRAMMED I/O: THE SIMPLEST, MOST WASTEFUL APPROACH

Under PROGRAMMED I/O, the CPU itself repeatedly POLLS a device's STATUS (or READY/BUSY) register in a tight software loop, checking again and again whether the device has become ready, and only once it reports ready does the CPU itself move the actual data item (one word, or one byte, at a time) between the device and memory.

1. This BUSY-WAITING loop consumes CPU cycles for the ENTIRE duration the CPU spends waiting for the device to become ready — cycles that could otherwise have gone toward genuinely useful computation, making programmed I/O the most CPU-wasteful of the three techniques covered in this chapter.
2. Despite that waste, programmed I/O remains the SIMPLEST to implement (no interrupt hardware, no separate DMA controller needed at all) and is entirely adequate for slow, INFREQUENT devices (a keyboard, for instance) where the fraction of total CPU time lost to polling, even though nonzero, remains genuinely small relative to everything else the system is doing.

[[FIG:io-technique-ladder]]

INTERRUPT-DRIVEN I/O: LET THE DEVICE ASK FOR ATTENTION

Under INTERRUPT-DRIVEN I/O, the CPU proceeds with OTHER, genuinely useful work after INITIATING a device operation, rather than busy-waiting — the device itself raises an INTERRUPT REQUEST signal only once it has actually become ready, at which point the CPU temporarily suspends its current work to service that specific request.

1. On receiving an interrupt request, the CPU (once it decides to ACCEPT the interrupt, typically only at an instruction BOUNDARY, never in the middle of executing one) first completes its currently executing instruction, then SAVES the minimal state needed to resume correctly afterward — almost always the program counter, and very often the flags/status register (PSW) as well — onto a stack or into dedicated save registers, and finally VECTORS to the appropriate Interrupt Service Routine (ISR).
2. VECTORED INTERRUPTS: the interrupting device itself supplies (directly, or via a lookup indexed by a device-supplied identifying number) the ADDRESS of its own specific ISR, letting the CPU jump straight there without first needing to determine which device actually caused the interrupt — a fast dispatch mechanism.
3. NON-VECTORED INTERRUPTS: every interrupt instead jumps to ONE single, fixed common address, and a software polling routine located there must then check EVERY device in some fixed priority order to determine which one actually raised the request — functionally closer to programmed I/O's own polling idea, but now happening only ONCE per actual interrupt, rather than continuously while waiting.

GATE TRAP: assuming vectored interrupts still require the CPU to poll every device to find the interrupting source is backwards — vectoring is PRECISELY the technique that AVOIDS this polling step, by having the device (or a vector table indexed by a device-supplied ID) supply the ISR address directly; it is NON-vectored interrupts that still require a software polling loop at the common dispatch address.

1. INTERRUPT LATENCY = the time elapsed from the interrupt signal first being asserted to the FIRST instruction of the ISR actually beginning execution — this includes finishing whatever instruction was already in progress, saving the required state, and any PRIORITY ARBITRATION needed among multiple simultaneous interrupt requests competing for the CPU's attention at the same moment.
2. Multiple simultaneous (or nested) interrupt requests are ordered by PRIORITY, using either a software POLLING scheme (the CPU checks devices in a fixed sequence — cheap in hardware, but slower for a low-priority device forced to wait behind every higher-priority check) or a hardware VECTORED-PRIORITY / DAISY-CHAINING scheme (dedicated hardware directly grants the bus, or acknowledges, the SINGLE highest-priority pending request immediately — faster response, at the cost of more dedicated hardware).
3. NESTED INTERRUPTS occur when a HIGHER-priority interrupt is allowed to interrupt an ISR ALREADY running for a LOWER-priority request — this requires interrupts to remain (or be re-)enabled during ISR execution, and requires the interrupted ISR's OWN state to be saved in turn before the higher-priority ISR begins, exactly the same save-state mechanism used for the original interruption, applied recursively, nested as many levels deep as the hardware's own priority scheme and the available stack space together actually permit.

GATE TRAP: treating interrupt-driven I/O as though it carries ZERO CPU overhead, the same way DMA does, is a common conflation — interrupt-driven I/O STILL requires the CPU to execute the entire ISR and STILL requires the CPU itself to move each unit of data between device and memory; the ONLY thing interrupt-driven I/O saves relative to programmed I/O is the CPU-cycle-wasting BUSY-WAIT loop while the device is not yet ready, NOT the actual data-movement work itself, which only DMA (below) genuinely offloads.

DMA: OFFLOADING THE DATA MOVEMENT ITSELF

DIRECT MEMORY ACCESS (DMA) goes a full step further than either prior technique — a dedicated DMA CONTROLLER, once programmed by the CPU with a source address, a destination address, and a word/byte COUNT, independently drives the memory and data buses to move an ENTIRE BLOCK of data directly between the device and memory, WITHOUT the CPU's continued involvement in moving any individual data item at all.

1. The CPU's role under DMA shrinks to just TWO brief moments: an initial SETUP (programming the DMA controller's registers, a handful of cycles), and a single COMPLETION interrupt once the ENTIRE block transfer has finished — a dramatic reduction from interrupt-driven I/O's own per-item ISR execution, let alone programmed I/O's continuous busy-waiting.
2. CYCLE STEALING: the DMA controller requests the shared memory bus from the CPU only for the brief instants it actually needs it, INTERLEAVED with the CPU's own memory accesses, effectively "stealing" individual memory cycles away from the CPU's own instruction stream one at a time as the transfer proceeds.
3. BURST (BLOCK) MODE: alternatively, the DMA controller instead holds the bus for the ENTIRE duration of the transfer in one uninterrupted stretch, fully blocking ordinary CPU memory access for that whole period — faster for the transfer itself, but more disruptive to the CPU's own concurrent progress than the more gradual cycle-stealing approach.

GATE TRAP: confusing DMA's CYCLE-STEALING mode (brief, interleaved per-cycle bus grants, the more common assumption in a GATE numerical unless stated otherwise) with its BURST/BLOCK mode (the DMA controller holds the bus continuously for the whole transfer, fully blocking the CPU meanwhile) leads to a fundamentally different CPU-time-penalty calculation — always check which mode a given question specifies or clearly implies before applying either formula below.

CYCLE-STEALING QUANTITATIVE MODEL

1. FRACTION OF MEMORY CYCLES STOLEN BY DMA = (number of DMA memory cycles used per second) / (total memory cycles available per second) = (DMA transfer rate, in words per second) × (memory cycle time, in seconds), assuming the DMA controller needs exactly one memory cycle per word transferred — this is simply the DMA controller's own OWN memory-bandwidth DEMAND expressed as a fraction of the memory's TOTAL available bandwidth.
2. EFFECTIVE CPU EXECUTION TIME WITH DMA ACTIVE = (Original CPU time, with no DMA contention) / (1 − fraction of cycles stolen by DMA) — since every memory cycle stolen by the DMA controller is one cycle the CPU's OWN instruction stream could not use during that same interval, proportionally stretching out however long the CPU's own work now takes to actually complete.

1. Worked example (DMA cycle stealing, CPU time penalty): a DMA controller transfers data at 2×10^6 words/second (one word per memory cycle). Main memory cycle time is 200 ns, so memory can perform 1/(200×10^-9) = 5×10^6 cycles/second. Fraction of memory cycles stolen by DMA = (2×10^6)/(5×10^6) = 0.4 = 40%. A CPU task that would take 10 ms with no DMA contention instead takes, with DMA active: 10/(1−0.4) = 10/0.6 ≈ 16.67 ms — the task is slowed to roughly 1.67× its original duration purely from memory-cycle contention, even though the CPU itself never executes a single additional instruction because of the DMA transfer.

GATE TRAP: in cycle-stealing percentage problems, dividing the DMA transfer rate by the CPU's own CLOCK rate instead of by the MEMORY cycle rate produces a wrong fraction entirely — the contention here is specifically for shared MEMORY bus cycles, not CPU instruction-execution cycles, and using the wrong denominator (a very tempting mistake, since both are "rates" measured in per-second units) silently produces a plausible-looking but incorrect answer.

KEY: programmed I/O, interrupt-driven I/O, and DMA form a genuine PROGRESSION, each stage removing one further layer of CPU involvement from the SAME underlying task (getting data between a device and memory) — programmed I/O has the CPU both WAIT for readiness and MOVE the data itself; interrupt-driven I/O removes the WAITING (the CPU does other work until notified) but still requires the CPU to MOVE each item itself inside the ISR; DMA removes the data-MOVEMENT step too, leaving the CPU responsible only for initial setup and a single final acknowledgment — recognising this ladder of "what does the CPU still have to do" is far more useful for answering a comparison-style question correctly than memorising three unconnected technique names.

INTERRUPT VS DMA: A DIRECT COMPARISON ON PRIORITY AND USE CASE

1. Interrupt-driven I/O suits MEDIUM-speed devices, or scenarios where OVERLAPPING the wait with other useful CPU work genuinely matters — a device slow enough that busy-waiting would waste significant CPU time, but not so fast (or so bulk-oriented) that even the ISR's own per-item overhead becomes a bottleneck in its own right.
2. DMA specifically suits HIGH-speed, BULK transfers (disk reads, network packet reception) where the sheer VOLUME of data would make even interrupt-driven I/O's own per-item ISR execution prohibitively expensive if it had to run once per individual data unit rather than once per entire block.
3. A well-designed I/O SUBSYSTEM typically uses DMA to move the BULK data efficiently, while STILL relying on a single completion INTERRUPT (rather than programmed-I/O-style polling) to notify the CPU once that DMA-driven transfer has fully finished — the two techniques are complementary rather than mutually exclusive, and this exact combination (DMA for movement, interrupt for completion notification) is the standard real-world arrangement for high-speed device I/O.

DISK STORAGE: THE SERVICE-TIME MODEL

A magnetic disk's SERVICE TIME (the average time to satisfy one random single-sector access request) decomposes into exactly three additive components, each with its own distinct physical origin.

[[FIG:disk-service-time-breakdown]]

1. SEEK TIME: the time for the read/write head to physically move to the correct CYLINDER (track) — depends on how far the head must travel from its current position; an AVERAGE seek time is either given directly, or computed as an average over some assumed distribution of track-to-track distances.
2. ROTATIONAL LATENCY: the time spent WAITING for the desired sector to rotate around underneath the now-correctly-positioned head. AVERAGE rotational latency = (1/2) × (time for one FULL disk revolution) = (1/2) × (60/RPM) seconds, since on average the desired sector is HALF a revolution away from the head at the moment the head arrives at the correct track.
3. TRANSFER TIME = (amount of data to transfer) / (data transfer rate), where the transfer RATE itself = (bytes per track) × (RPM/60) bytes per second — equivalently, the transfer time for exactly ONE sector = (time for one revolution) / (number of sectors per track), since a full revolution passes every single sector on that track exactly once.

GATE TRAP: forgetting that average rotational latency is HALF a revolution, not a FULL revolution, is an extremely common factor-of-two slip — the desired sector's position relative to the head, averaged over many random requests, is uniformly distributed across the full revolution, and the AVERAGE of a uniform distribution over [0, one full revolution] is exactly half that full-revolution time, not the full-revolution time itself.

GATE TRAP: RPM (revolutions PER MINUTE) must always be converted to a PER-SECOND or absolute-time basis before use — computing time-per-revolution directly as 60/RPM (giving seconds per revolution) is the correct conversion; using 1/RPM directly, without the factor of 60 converting minutes to seconds, silently produces an answer wrong by a factor of 60.

1. Worked example (single-sector disk service time): a disk rotates at 6000 RPM, has 500 sectors per track, and an average seek time of 8 ms. Time for one revolution = 60/6000 = 0.01 s = 10 ms. Average rotational latency = 10/2 = 5 ms. Transfer time per sector = 10 ms / 500 = 0.02 ms. Total service time for one random sector = seek + rotational latency + transfer = 8 + 5 + 0.02 = 13.02 ms.

MULTI-TRACK SEQUENTIAL TRANSFERS AND TRACK SWITCHES

1. For a LARGE sequential transfer spanning MULTIPLE tracks, the initial seek and rotational-latency cost is paid only ONCE (to reach the very first sector of the sequential run), after which the transfer proceeds sector by sector, crossing into a NEW track whenever the current track's sectors are exhausted.
2. TOTAL TIME (multi-track sequential transfer) = initial (seek + rotational latency) + (total sectors × transfer time per sector) + (number of track/head switches × head-switch time), where the NUMBER OF SWITCHES needed = ⌈total sectors / sectors-per-track⌉ − 1, since crossing from the FIRST track into the SECOND is the FIRST switch, and so on, with no switch needed after the very LAST track since the transfer simply ends there.

GATE TRAP: in multi-track sequential-read problems, forgetting to count head/track switches SEPARATELY from the raw per-sector transfer time (or making an off-by-one error in "number of switches = number of tracks touched − 1", for instance using the raw track count instead of one less than it) is a common source of an answer that is either missing a genuine cost component entirely, or that overcounts it by exactly one switch too many.

1. Worked example (multi-track sequential transfer, using the same disk parameters as the single-sector example above): reading 1200 sectors sequentially, starting from a random position. Number of tracks spanned = ⌈1200/500⌉ = 3, requiring 3−1 = 2 track switches. Assuming no head-switch-time parameter is given (so it contributes zero): Total = 8 (seek) + 5 (rotational latency) + (1200×0.02) (transfer) = 13 + 24 = 37 ms. If instead a head-switch time of 1 ms per switch were given explicitly, the total would become 37 + (2×1) = 39 ms — always check whether the question supplies a switch-time parameter before silently assuming it to be zero.

DISK CAPACITY

DISK CAPACITY = (number of recording SURFACES) × (tracks per surface) × (sectors per track) × (bytes per sector) — the same "multiply every independent dimension together" counting principle that has appeared throughout this book whenever a total capacity or a total count needed to be derived from several independent contributing factors (much as a memory's total byte capacity is the product of its independently-addressable dimensions).

1. Worked example (disk capacity): a disk has 4 surfaces, 2000 tracks per surface, 400 sectors per track, and 512 bytes per sector. Total capacity = 4 × 2000 × 400 × 512 = 4 × 2000 = 8000; 8000 × 400 = 3,200,000; 3,200,000 × 512 = 1,638,400,000 bytes ≈ 1.526 GiB (dividing by 2^30 ≈ 1,073,741,824).

I/O BUS ARBITRATION: WHO GETS THE BUS

When multiple devices (or a DMA controller and the CPU itself) may simultaneously wish to use a SHARED bus, some ARBITRATION scheme must decide which one actually gets it at any given moment — the same underlying resource-contention problem this book has already encountered in the datapath chapter's own single-bus-organization discussion, now applied specifically to I/O devices contending for bus access rather than to register-transfer microoperations contending for a shared internal bus.

1. DAISY-CHAIN (CENTRALIZED, SERIAL) PRIORITY: devices are physically connected in a fixed serial chain, and a bus-grant signal propagates DOWN the chain from the highest-priority device to the lowest — a device wanting the bus intercepts the grant signal and stops it from propagating further, automatically implementing a fixed, HARDWARE-ENFORCED priority order with very little additional logic, though a device physically positioned earlier in the chain can indefinitely STARVE a later, LOWER-priority device under sustained contention.
2. POLLING (CENTRALIZED, SOFTWARE OR HARDWARE-COUNTER DRIVEN): the bus controller sequentially ASKS each device, in some fixed or rotating order, whether it wants the bus — more FLEXIBLE (the poll order or which devices are even included can be changed easily) but SLOWER than a daisy-chain's near-instantaneous grant propagation, since each polled device must be individually addressed and must individually respond before the next one is checked.
3. INDEPENDENT REQUESTING (a request/grant line PER DEVICE): each device has its OWN dedicated request and grant lines directly to the arbiter, allowing the arbiter to apply ANY priority scheme it likes (fixed, rotating/round-robin, or otherwise) in hardware, WITHOUT the daisy-chain's inherent starvation risk or the polling scheme's sequential-checking delay — at the cost of needing considerably MORE physical wiring (two dedicated lines per device, rather than one shared chain or one shared poll bus).

I/O ADDRESSING: ISOLATED VS MEMORY-MAPPED I/O

Before a CPU can read from or write to a device register at all, that register needs its OWN address the CPU's instructions can refer to — and there are exactly two fundamentally different schemes for assigning such addresses.

1. ISOLATED (PORT-MAPPED) I/O: device registers occupy a SEPARATE address space, entirely distinct from main memory's own address space, accessed only via DEDICATED I/O instructions (commonly named IN and OUT) that the instruction set provides specifically for this purpose — ordinary memory-reference instructions (LOAD, STORE, and so on) simply cannot address a device register at all under this scheme, since the hardware routes I/O-space addresses and memory-space addresses onto entirely separate address decoding logic.
2. MEMORY-MAPPED I/O: device registers instead occupy addresses WITHIN the SAME single address space main memory itself uses, so that the SAME ordinary memory-reference instructions (LOAD, STORE) that access memory can ALSO access a device register, simply by using an address that happens to fall within the range reserved for I/O rather than for actual memory.

1. ISOLATED I/O's key advantage: the FULL memory address space remains available exclusively for actual memory, since I/O addresses live entirely separately — no memory capacity is ever "lost" to I/O address reservations. Its key disadvantage: it requires a SEPARATE, dedicated instruction set (IN/OUT) purely for I/O, and these dedicated instructions typically cannot use the FULL range of addressing modes that ordinary memory instructions support.
2. MEMORY-MAPPED I/O's key advantage: device registers can be manipulated using the ENTIRE existing repertoire of memory-reference instructions and addressing modes (including indexed and indirect addressing, useful for iterating through a block of related device registers), with NO dedicated I/O instruction set needed at all — genuinely simplifying the instruction set. Its key disadvantage: the addresses reserved for I/O devices are addresses that can no longer be used for actual memory, effectively REDUCING the maximum usable memory capacity by however much address space the reserved I/O region consumes.

GATE TRAP: assuming memory-mapped I/O is strictly "better" in every respect because it needs no dedicated instructions is an oversimplification — the reduced effective memory capacity (address space consumed by I/O rather than available for RAM) is a genuine, quantifiable cost that isolated I/O entirely avoids, and the actual trade-off a real system designer must weigh (instruction-set simplicity and addressing-mode flexibility versus preserving the largest possible memory address range) is precisely the kind of two-sided comparison GATE numericals and conceptual questions on this specific topic are built to test.

KEY: the isolated-versus-memory-mapped I/O choice is structurally the SAME kind of trade-off this book has already encountered, in a different guise, in the machine-instructions chapter's own comparison of CISC's dedicated, specialised instructions against RISC's smaller, more uniform instruction set that reuses the SAME small set of addressing and access mechanisms for every purpose — memory-mapped I/O is, in this specific sense, the "RISC-flavoured" choice (reuse the existing LOAD/STORE mechanism for a new purpose) while isolated I/O is the "CISC-flavoured" choice (introduce dedicated new instructions purpose-built for the new need), and recognising this parallel is a genuinely transferable habit for any future architectural trade-off phrased in similarly "reuse an existing general mechanism" versus "introduce a dedicated specialised one" terms.

I/O PROCESSORS AND CHANNELS: PUSHING DELEGATION FURTHER

Some systems push the CPU-offloading idea this chapter has developed even further than a simple DMA controller, using a dedicated I/O PROCESSOR (sometimes called a CHANNEL) — essentially a small, specialised processor in its own right, capable of executing its OWN short I/O-specific program (a "channel program") that can coordinate an entire SEQUENCE of transfers, involving multiple devices, with only a SINGLE initiating instruction and a single completion interrupt needed from the main CPU for the entire sequence.

1. This represents the LOGICAL extreme of the same CPU-offloading progression this chapter has developed step by step: programmed I/O offloads nothing; interrupt-driven I/O offloads only the waiting; DMA offloads the movement of a SINGLE block; an I/O processor offloads the coordination of an ENTIRE SEQUENCE of blocks and devices, freeing the main CPU almost completely from I/O-related work beyond the very initial request.
2. The trade-off, exactly as with every other delegation technique covered throughout this chapter, is HARDWARE COST — an I/O processor is a genuinely separate, capable processor in its own right, considerably more expensive to include than a comparatively simple DMA controller, which is itself more expensive than the essentially free (in hardware terms) programmed-I/O approach.

RAID: RELIABILITY AND PERFORMANCE ACROSS MULTIPLE DISKS

Beyond a single disk's own service-time model, modern storage systems frequently combine SEVERAL physical disks into a single logical unit using a RAID (Redundant Array of Independent Disks) scheme, trading some raw capacity for improved performance, improved reliability, or both simultaneously.

1. RAID 0 (STRIPING, NO REDUNDANCY): data is split into stripes distributed ACROSS all the disks in the array, allowing several disks to service PARTS of a single large request in PARALLEL — directly analogous to this book's own memory-interleaving discussion, now applied to disks rather than memory modules. RAID 0 improves throughput proportionally to the number of disks, but provides NO redundancy whatsoever — a single disk failure loses ALL data in the entire array, since every file's data is spread across every disk.
2. RAID 1 (MIRRORING): every single block of data is written IDENTICALLY to two (or more) separate disks — read requests can be serviced by EITHER mirrored copy (potentially improving read throughput via load-balancing between the two), and the array survives the complete failure of any ONE disk in a mirrored pair, at the direct cost of HALVING the usable capacity relative to the raw total disk capacity purchased (since every byte is stored twice).
3. RAID 5 (STRIPING WITH DISTRIBUTED PARITY): data is striped across disks exactly as in RAID 0, but one additional PARITY block per stripe (computed as the XOR of all the corresponding data blocks in that stripe) is also stored, with the parity block's own location ROTATING across different disks for different stripes rather than being concentrated on one dedicated parity disk — this allows the array to RECONSTRUCT the contents of any single FAILED disk (by XOR-ing together the surviving data blocks and the parity block for each affected stripe), tolerating exactly ONE disk failure, while consuming only 1/N of total capacity for redundancy (for an N-disk array), a far smaller capacity cost than RAID 1's full 50% overhead.

1. Worked example (RAID capacity comparison): an array of 5 disks, each 2 TB, is configured three different ways. RAID 0: usable capacity = 5×2 = 10 TB (no redundancy overhead at all, but zero fault tolerance). RAID 1 (mirrored pairs, here treated as one large mirrored set for simplicity): usable capacity = (5×2)/2 = 5 TB. RAID 5: usable capacity = (5−1)×2 = 8 TB (capacity equivalent to one disk out of the five is consumed by distributed parity, tolerating exactly one disk's failure).

GATE TRAP: assuming RAID 5's fault tolerance matches RAID 1's is incorrect — RAID 5 tolerates exactly ONE simultaneous disk failure (a second concurrent failure, before the first is fully rebuilt, loses data irrecoverably, since a single XOR parity block cannot reconstruct TWO simultaneously missing pieces of information), whereas certain RAID 1 configurations with more than two mirrored copies can tolerate more than one simultaneous failure, at a correspondingly higher capacity cost; capacity efficiency and fault-tolerance level are two genuinely separate quantities that do not automatically track one another across different RAID levels.

KEY: RAID striping (RAID 0 and RAID 5 alike) is, at its structural core, the exact same "spread the work across several independent parallel units" idea already covered in this chapter's discussion of memory interleaving, and before that, in the pipelining chapter's own throughput-improving overlap of multiple instructions — recognising RAID striping as simply this same recurring parallelism idea applied to disks, rather than as an entirely unrelated new storage-specific concept, is exactly the kind of connective habit this book has aimed to build consistently across every subject it covers.

BUFFERING AND ITS ROLE IN SMOOTHING SPEED MISMATCHES

A BUFFER (a small, dedicated block of temporary storage, either in the device controller itself or in main memory) is a further technique for handling the same speed-mismatch problem this entire chapter addresses, specifically useful when a device's INSTANTANEOUS data rate fluctuates or when the CPU and device operate on different, not-perfectly-synchronized schedules.

1. SINGLE BUFFERING: one buffer holds data while it is being transferred in ONE direction; while the buffer is being filled by the device, the CPU cannot yet safely consume it, and while the CPU is consuming a full buffer, the device cannot yet safely refill it — a genuine serialization between device activity and CPU activity remains.
2. DOUBLE BUFFERING: TWO buffers alternate roles — while the device fills ONE buffer, the CPU simultaneously consumes data already sitting in the OTHER, already-filled buffer, and the two roles swap once both operations complete, allowing the device's own filling activity and the CPU's own consuming activity to proceed GENUINELY concurrently, rather than being forced to alternate strictly one at a time.

GATE TRAP: assuming double buffering ELIMINATES all speed-mismatch waiting entirely is incorrect — it allows device-fill and CPU-consume activity to OVERLAP rather than strictly alternate, but if the device is dramatically SLOWER than the CPU's own consumption rate, the CPU will still eventually finish consuming the second buffer and be forced to wait for the device to finish filling the next one; double buffering IMPROVES overlap and throughput, but does not, by itself, change the FUNDAMENTAL underlying rate mismatch between a genuinely slow device and a genuinely fast CPU.

KEY: buffering, DMA, and I/O processors are all, at their core, further instances of this book's single most recurring architectural strategy — introduce SOME dedicated hardware (a buffer's storage, a DMA controller's registers, an I/O processor's own execution unit) specifically to absorb a mismatch between two components running at genuinely different speeds, so that the FASTER of the two components spends as little of its own time as possible waiting on, or being disrupted by, the SLOWER one; every single technique in this chapter, examined closely, turns out to be one particular embodiment of that one general strategy, applied at a different point along the path between a device and the CPU.

INTERRUPT VS TRAP VS EXCEPTION: A TERMINOLOGY CLARIFICATION

The general mechanism by which the CPU suspends normal instruction execution to service some OTHER, unplanned event is shared across three related but genuinely distinct terms that GATE questions frequently expect to be told apart precisely.

1. An INTERRUPT (in the strict, narrow sense used throughout the rest of this chapter) is EXTERNALLY generated — it originates from a device or from hardware entirely OUTSIDE the currently executing instruction stream (a keyboard press, a DMA completion signal, a timer expiring), and can occur at essentially any point in a program's execution, independent of what that program itself happens to be doing at that moment.
2. A TRAP (sometimes called a SOFTWARE INTERRUPT) is INTERNALLY generated, DELIBERATELY, by the currently executing program itself — typically via a dedicated instruction (commonly named INT, SYSCALL, or TRAP depending on the architecture) used specifically to request an operating-system service, such as reading a file or allocating memory, that user-level code cannot perform directly on its own.
3. An EXCEPTION is also INTERNALLY generated, but UNINTENTIONALLY, as a direct, unavoidable CONSEQUENCE of the currently executing instruction itself encountering some erroneous or otherwise exceptional condition it cannot proceed past — a division by zero, an attempt to access an invalid memory address, an illegal or undefined opcode, or a page fault (itself, notably, the very same page-fault mechanism this book's own memory-hierarchy chapter already introduced as virtual memory's analogue of a cache miss).

GATE TRAP: using the words "interrupt," "trap," and "exception" as fully interchangeable synonyms, though colloquially common, obscures a distinction GATE questions frequently test directly — the KEY discriminating question to ask is WHERE the event originates (external hardware: interrupt; the program's OWN deliberate request: trap; an UNINTENDED consequence of the program's own current instruction: exception) and whether it was INTENTIONAL (a trap, deliberately invoked) or UNINTENTIONAL (an exception, an unplanned error condition) when internally generated.

1. Despite these differing origins, ALL THREE share the exact SAME underlying dispatch mechanism this chapter has already developed in full for ordinary hardware interrupts — the current instruction is completed (or, for certain fatal exceptions, abandoned mid-execution), the minimal required state is saved, and control vectors to an appropriate handler routine (an ISR for a hardware interrupt, a system-call handler for a trap, an exception handler for an exception) — which is precisely why all three are frequently grouped together under the single broader umbrella term "interrupt" in a looser, more general sense, even though the strict terminology distinguishes them carefully by origin and intent.

A CLOSING WORKED SYNTHESIS PROBLEM COMBINING MULTIPLE TOOLS

To see this chapter's tools combined within a single fully worked exam-style scenario, exactly as the memory-hierarchy chapter's own closing synthesis problem combined several of ITS tools together, consider the following.

1. SCENARIO: A system reads a 10 MB sequential file from a disk rotating at 7200 RPM, with 400 sectors per track (each sector 512 bytes), average seek time 5 ms, using DMA at a rate matched exactly to the disk's own transfer rate. The main memory cycle time is 100 ns. Find (a) the raw disk-side time to read the file sequentially, (b) the DMA controller's effective transfer rate in words (4 bytes each) per second implied by the disk's own transfer rate, (c) the fraction of memory cycles this DMA activity steals, and (d) the effective time for a separate CPU computation that would otherwise take 50 ms, running concurrently with this file read.
2. STEP 1 — SECTORS NEEDED: 10 MB = 10×1024×1024 = 10,485,760 bytes; sectors needed = 10,485,760/512 = 20,480 sectors.
3. STEP 2 — DISK TIMING PARAMETERS: one revolution = 60/7200 s = 8.333 ms. Average rotational latency = 8.333/2 = 4.167 ms. Transfer time per sector = 8.333/400 ≈ 0.02083 ms.
4. STEP 3 — TRACK SWITCHES: tracks spanned = ⌈20480/400⌉ = 52, so switches = 52−1 = 51; assume zero head-switch-time overhead (none given).
5. STEP 4 — TOTAL DISK-SIDE TIME: seek + rotational latency + (sectors × transfer time per sector) = 5 + 4.167 + (20480×0.02083) ≈ 5 + 4.167 + 426.67 ≈ 435.84 ms.
6. STEP 5 — DMA TRANSFER RATE IN WORDS/SEC: the disk delivers 10,485,760 bytes over the transfer-time PORTION of that total (426.67 ms of actual data movement, excluding the one-time seek and rotational-latency setup) = 10,485,760 bytes / 0.42667 s ≈ 24,576,000 bytes/sec ≈ 6,144,000 words/sec (dividing by 4 bytes per word).
7. STEP 6 — FRACTION OF MEMORY CYCLES STOLEN: memory cycles/sec = 1/(100×10^-9) = 10,000,000. Fraction stolen = 6,144,000/10,000,000 = 0.6144 = 61.44%.
8. STEP 7 — EFFECTIVE CONCURRENT CPU TIME: 50/(1−0.6144) = 50/0.3856 ≈ 129.7 ms — the separate CPU computation is slowed to roughly 2.59× its own original duration purely from memory-cycle contention with this one large DMA-driven file transfer, a direct, concrete illustration of why DMA, despite dramatically reducing the CPU's OWN instruction-level involvement in a transfer, still is not entirely "free" from the rest of the system's point of view whenever that transfer's own data rate becomes a large fraction of the memory's total available bandwidth.

This combined problem demonstrates the same lesson the memory-hierarchy chapter's own closing synthesis problem demonstrated for its subject: the disk service-time model and the DMA cycle-stealing model, while each simple and mechanical in isolation, chain together naturally once one model's own output (here, the disk's own effective transfer rate, derived from the service-time breakdown) becomes the OTHER model's own required input (the DMA rate needed to compute the fraction of memory cycles stolen) — and recognising exactly this kind of "one tool's output feeds the next tool's input" chaining is, once again, the single most valuable habit a fully worked, combined exam question of this kind is designed to test and reward.

A brief final note on how such a combined question can be varied without introducing any genuinely new formula: the same scenario could equally well have specified the DMA transfer rate DIRECTLY, as a given, rather than requiring it to be derived from the disk's own service-time breakdown first — in that variant, Steps 1 through 5 above would simply be skipped entirely, and the problem would begin directly at Step 6, treating the given DMA rate as an already-known input to the cycle-stealing fraction calculation. Recognising which of a combined problem's several stages are ACTUALLY necessary for a given question's specific phrasing, versus which stages exist merely to derive an intermediate quantity that a DIFFERENT phrasing of the very same underlying scenario might instead have simply handed over as a given number, is itself a valuable exam-reading skill distinct from the underlying formulas themselves — a student under time pressure who mechanically works through every step of a memorised worked-example template, without first checking which quantities the ACTUAL question in front of them has already supplied directly, wastes valuable time re-deriving a number that did not need deriving at all in that particular version of the question.

WORKED PROBLEMS

1. TOTAL DMA CPU-TIME PENALTY. A DMA controller transfers at 4×10^6 words/second, one word per memory cycle. Memory cycle time is 250 ns. Find the fraction of memory cycles stolen, and the effective time for a CPU task that would otherwise take 20 ms. Memory cycles/sec = 1/(250×10^-9) = 4×10^6. Fraction stolen = (4×10^6)/(4×10^6) = 1.0 — the DMA controller alone would consume the ENTIRE memory bandwidth, leaving the CPU with essentially none; the effective CPU time formula breaks down (division by 1−1=0, i.e., infinite effective time), correctly signalling that this specific DMA rate is not sustainable alongside any concurrent CPU memory access at all under simple cycle-stealing.

2. A MORE REALISTIC DMA SCENARIO. Repeat WP1's setup, but with a DMA transfer rate of 1.5×10^6 words/second instead. Fraction stolen = (1.5×10^6)/(4×10^6) = 0.375 = 37.5%. Effective time for the same 20 ms CPU task = 20/(1−0.375) = 20/0.625 = 32 ms.

3. SINGLE-SECTOR DISK SERVICE TIME. A disk rotates at 7200 RPM, has 300 sectors per track, average seek time 6 ms. Compute the average service time for one random sector. One revolution = 60/7200 s = 8.33 ms. Average rotational latency = 8.33/2 ≈ 4.17 ms. Transfer time per sector = 8.33/300 ≈ 0.0278 ms. Service time = 6 + 4.17 + 0.0278 ≈ 10.20 ms.

4. MULTI-TRACK SEQUENTIAL READ WITH SWITCH TIME. Using the disk from WP3, read 900 sectors sequentially, with a head-switch time of 0.5 ms. Tracks spanned = ⌈900/300⌉ = 3, switches = 3−1 = 2. Total = 6 (seek) + 4.17 (rotational latency) + (900×0.0278) (transfer) + (2×0.5) (switches) = 6+4.17+25.02+1 ≈ 36.19 ms.

5. DISK CAPACITY. A disk has 6 surfaces, 1500 tracks per surface, 250 sectors per track, 512 bytes per sector. Find total capacity in bytes and approximate GiB. Capacity = 6×1500×250×512 = 6×1500=9000; 9000×250=2,250,000; 2,250,000×512=1,152,000,000 bytes ≈ 1.073 GiB.

6. VECTORED VS NON-VECTORED DISPATCH TIME. A system has 8 devices. Non-vectored dispatch polls devices in fixed priority order, taking 2 μs to check each device before finding the interrupting one. Vectored dispatch takes a fixed 3 μs regardless of which device interrupted. If the interrupting device is, on average, the 4th one checked under non-vectored dispatch, compare the average dispatch times. Non-vectored: 4×2 = 8 μs average. Vectored: 3 μs, fixed — vectored dispatch is faster here specifically because it avoids the sequential per-device checking non-vectored dispatch requires, regardless of which device actually interrupted.

7. CYCLE-STEALING FRACTION FROM A GIVEN CPU SLOWDOWN. A CPU task's effective time is measured at 15 ms when its DMA-free time would have been 12 ms. Find the fraction of memory cycles being stolen by the concurrently active DMA controller. Effective = Original/(1−fraction), so fraction = 1 − Original/Effective = 1 − 12/15 = 1 − 0.8 = 0.2 = 20%.

8. DAISY-CHAIN STARVATION SCENARIO. In a daisy-chain bus arbitration scheme with devices D1 (highest priority) through D5 (lowest), D1 issues bus requests continuously and D5 never receives a grant as a result. Which arbitration technique would eliminate this specific starvation risk, and why? INDEPENDENT REQUESTING (a dedicated request/grant line per device) would eliminate this, since the arbiter could then apply a ROUND-ROBIN or otherwise fairness-aware priority scheme directly in hardware, rather than being constrained by the daisy chain's strictly fixed, positional priority order that inherently favours devices physically closer to the start of the chain.

9. TRANSFER-TECHNIQUE SELECTION. A system needs to move 50 MB from a fast SSD to memory as quickly as possible, with minimal CPU disruption, and separately needs to read occasional single keystrokes from a keyboard. Identify the appropriate technique for each. The 50 MB bulk SSD transfer should use DMA (high-volume, minimal CPU involvement needed for the movement itself, with the CPU only handling setup and one completion interrupt). The occasional keyboard keystrokes are well suited to either interrupt-driven I/O (avoiding a wasteful busy-wait for a device that becomes ready only rarely) or, if system complexity must be kept minimal and CPU idle time is not a concern, even simple programmed I/O — DMA would be clear overkill for a device producing data this infrequently and in such small individual units.

10. AVERAGE SEEK TIME FROM TRACK-DISTANCE DATA. A disk has an equal chance of the head needing to move 0, 10, 20, or 30 tracks for any given request, with a fixed cost of 1 ms of head-movement time per track moved (plus a fixed 1 ms settling time regardless of distance). Find the average seek time. Average track distance = (0+10+20+30)/4 = 60/4 = 15 tracks. Average seek time = 15×1 + 1 = 16 ms.

11. RAID CAPACITY AND FAULT TOLERANCE. An array of 8 disks, each 4 TB, is configured as RAID 5. Find the usable capacity, and state how many simultaneous disk failures it tolerates. Usable capacity = (8−1)×4 = 28 TB. It tolerates exactly 1 simultaneous disk failure (one disk's worth of capacity is consumed by distributed parity, sufficient to reconstruct any single missing disk via XOR, but not two simultaneously missing disks).

12. MEMORY-MAPPED I/O ADDRESS-SPACE COST. A system has a 16-bit address space (2^16 = 65536 addressable locations total) and reserves 1024 of those addresses for memory-mapped device registers. Find the maximum usable memory capacity remaining, and the fraction of the total address space consumed by I/O. Usable memory = 65536 − 1024 = 64512 locations. Fraction consumed by I/O = 1024/65536 = 1/64 = 1.5625% — illustrating concretely the exact capacity cost memory-mapped I/O's own GATE TRAP above warns about, here reducing usable memory by a small but entirely nonzero, quantifiable fraction of the total address space.

CARRYING THIS FORWARD

This chapter completes the Computer Organization and Architecture sequence by extending the SAME core theme that has run through every chapter in it: a computer system is, from top to bottom, a collection of components running at WILDLY different speeds, and nearly every architectural technique this sequence has covered is some specific answer to the question of how to keep a much FASTER component from being needlessly slowed down by a much SLOWER one it must nonetheless cooperate with. Pipelining hid instruction latency behind overlapped execution of several instructions; caching hid main-memory latency behind exploited locality; and this chapter's three I/O techniques — programmed, interrupt-driven, and DMA — hide device latency behind progressively less CPU involvement, culminating in DMA's near-complete removal of the CPU from the data-movement path entirely, mirroring the same "get the fast component out of the way of the slow one wherever possible" strategy this entire sequence has repeatedly, deliberately reused.

KEY: the disk service-time model (seek + rotational latency + transfer) and the DMA cycle-stealing model are, despite their very different subject matter, both instances of the SAME general "additive cost breakdown" reasoning this book has applied since the pipelining chapter's own CPI-based speedup formula — a total observed cost (service time; effective CPU time) is decomposed into a BASE component (transfer time; original CPU time) plus one or more ADDITIONAL, situation-specific penalty components (seek and rotational latency; the DMA-contention slowdown factor), and correctly identifying every contributing component, without double-counting or omitting one, is the single skill both models genuinely test.

A final, practical synthesis worth carrying forward from this entire chapter: a real exam question rarely tests DMA's cycle-stealing formula, or the disk service-time model, or interrupt-priority arbitration in complete isolation. A well-constructed combined question might, for instance, ask for the total time to load a large sequential disk file into memory VIA DMA, requiring the disk service-time model to determine how long the raw device-side transfer itself takes, and THEN the DMA cycle-stealing model to determine how much that concurrent memory contention slows down whatever OTHER CPU computation happens to be running at the same time. This is exactly the kind of nested, multi-tool combination this entire Computer Organization and Architecture sequence has repeatedly demonstrated, from the datapath chapter's own combined control-store-sizing-and-timing problem, through the pipelining chapter's combined CPI-and-speedup problem, through the memory chapter's own combined bit-partition-and-AMAT-and-interleaving synthesis problem, and now here, completing the sequence with I/O's own version of the exact same recurring lesson: master each individual tool thoroughly in isolation first, but always expect — and be ready — to apply several of them together, in careful sequence, within a single fully worked question.

With this chapter's completion, all five Computer Organization and Architecture chapters in this sequence — machine instructions and addressing modes, datapath and control-unit design, instruction pipelining, memory hierarchy and cache organization, and now input/output organization — form a single, deliberately connected arc, running from the smallest unit this subject examines (a single bit field within one instruction word) all the way up to an entire multi-disk storage subsystem coordinating with a CPU through interrupts and DMA. Every later chapter in this arc built directly and explicitly on specific tools introduced by an earlier one: the datapath chapter's RTL notation gave pipelining's stall and flush concepts their precise underlying meaning; pipelining's own throughput-hiding strategy reappeared, essentially unchanged in spirit, as caching's locality-hiding strategy and again as this chapter's device-latency-hiding strategy; and the counting principles established all the way back in the very first chapter's opcode-expanding recipe reappeared, entirely unchanged in their underlying logic, in this chapter's own memory-mapped-I/O address-space-cost calculations. Carrying this same habit of actively looking for such connections forward into every remaining subject this book still has left to cover is, by this point, less an occasional trick and more simply how this entire book has consistently asked to be read from its very first chapter onward. A reader who has worked carefully through all five of these chapters in order, deriving rather than merely memorising each formula along the way, now holds a genuinely complete and internally consistent picture of how a modern computer actually moves information between its own smallest components and the outside world it ultimately serves — a picture built entirely from a small handful of reusable ideas applied repeatedly, rather than from any long, disconnected list of separately memorised facts.
`
};
