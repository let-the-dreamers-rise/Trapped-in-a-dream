// Textbook chapter: File Organization and Indexing.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['dbms-indexing'] = {
  figs: [
    {
      id: 'dense-vs-sparse',
      caption: 'A dense index has one entry per search-key value, pointing directly to each record. A sparse index has one entry per data block, requiring a short scan forward within the block once located — smaller, but only usable on a sorted file.',
      svg: '<svg viewBox="0 0 280 100" width="100%" style="max-width:280px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1" fill="none"><rect x="10" y="10" width="120" height="80"/><rect x="150" y="10" width="120" height="80"/></g><g font-size="8" fill="currentColor" text-anchor="middle"><text x="70" y="26">Dense</text><text x="70" y="42">entry per key</text><text x="210" y="26">Sparse</text><text x="210" y="42">entry per block</text></g></svg>'
    },
    {
      id: 'bplustree-leaf-chain',
      caption: 'A B+-tree: internal nodes hold only routing keys (no data pointers), and every actual record pointer lives at the leaf level, with leaves additionally chained together for fast sequential range scans.',
      svg: '<svg viewBox="0 0 280 110" width="100%" style="max-width:280px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.1" fill="none"><rect x="105" y="10" width="70" height="24"/><rect x="30" y="55" width="60" height="24"/><rect x="105" y="55" width="60" height="24"/><rect x="180" y="55" width="60" height="24"/><line x1="60" y1="79" x2="135" y2="79"/><line x1="135" y1="79" x2="210" y2="79"/></g><g font-size="8" fill="currentColor" text-anchor="middle"><text x="140" y="26">internal (keys only)</text><text x="60" y="70">leaf</text><text x="135" y="70">leaf</text><text x="210" y="70">leaf</text><text x="140" y="98" font-size="7">← chained leaves →</text></g></svg>'
    }
  ],
  text: `
FILE ORGANIZATIONS: THE STORAGE-LAYER FOUNDATION

Before any INDEX can be built at all, the underlying DATA FILE itself must be organized SOMEHOW — and this chapter's own indexing techniques are built directly on top of whichever specific file organization the underlying data actually uses, exactly as this book's own earlier Runtime Environments and Memory Hierarchy chapters showed that HIGHER-level techniques are always built on some concrete, lower-level physical foundation.

1. HEAP (UNORDERED) FILE: records are inserted WHEREVER space happens to be available — INSERTION is fast (O(1) amortized, simply APPEND), but SEARCH requires a FULL linear scan, O(n) block accesses in the worst case, since NOTHING about the file's own physical layout narrows the search down at all.
2. SEQUENTIAL (ORDERED) FILE: records are PHYSICALLY sorted by some search KEY — supports BINARY search, O(log₂n) block accesses, for exact-match or range queries on THAT specific key, but INSERTION or DELETION requires SHIFTING records (or maintaining separate OVERFLOW areas), making UPDATES genuinely expensive.
3. HASHING FILE: a HASH FUNCTION maps a search key DIRECTLY to a bucket/block address — gives NEAR O(1) average access for EXACT-match queries on the hash key, but is GENUINELY POOR for RANGE queries, since hashing DELIBERATELY destroys any notion of ORDERING among keys.

DENSE VS SPARSE INDEXES

[[FIG:dense-vs-sparse]]

1. A DENSE INDEX contains ONE index entry for EVERY search-key value (equivalently, EVERY record, if the key is not unique per value) appearing in the data file, with EACH entry pointing DIRECTLY to the corresponding record (or the FIRST record with that value) — a dense index works on BOTH sorted and unsorted data files.
2. A SPARSE INDEX contains index entries for ONLY SOME search-key values — TYPICALLY, ONE entry PER BLOCK of the data file (pointing to the FIRST record of that block) — and REQUIRES the data file to be PHYSICALLY sorted on the index's own search key, since locating a NON-indexed value requires scanning FORWARD from the NEAREST preceding sparse entry. Sparse indexes are SMALLER (fewer entries, less storage, potentially FEWER levels in a multi-level structure), but EVERY search needs ONE extra sequential-scan step WITHIN the target block.

PRIMARY, CLUSTERING, AND SECONDARY INDEXES: THE COMPLETE DEFINITION TABLE

1. A PRIMARY INDEX is an ORDERED index defined on the search key that is ALSO the ORDERING key of a data file that is BOTH sorted AND has a UNIQUE value per index entry (the search key equals the PRIMARY key of a sequentially-ordered file). Can be DENSE or SPARSE — because it corresponds to a UNIQUE key, SPARSE is genuinely common here (ONE entry per block suffices, since a scan for the exact match SUCCEEDS quickly after landing at the block's own start).
2. A CLUSTERING INDEX is an ORDERED index defined on a NON-key field on which the data file IS physically sorted, but the field is NOT unique (MANY records share a given value). ONLY ONE clustering (or primary) index is possible per data file, since a file can ONLY be physically sorted in ONE order at any given time. TYPICALLY sparse — one entry per DISTINCT search-key-value block group, since ALL records sharing the SAME value are stored CONTIGUOUSLY.
3. A SECONDARY INDEX is an index (ALWAYS dense) on a field that is NOT the physical sort order of the data file — the data file may be sorted on an ENTIRELY different field, or on none at all. Since records matching a GIVEN search-key value can be SCATTERED anywhere throughout the file, a secondary index MUST be DENSE (one entry per record) — SPARSE is NOT possible here, since there is NO single contiguous block to land in and scan; a DIRECT pointer to EVERY qualifying record is genuinely required. MULTIPLE secondary indexes can coexist on the SAME file simultaneously.
4. THE KEY COMPARATIVE FACTS, heavily tested: AT MOST ONE primary/clustering index per file (BOTH require a SPECIFIC physical sort order, and a file has only ONE physical order at any time), but ARBITRARILY MANY secondary indexes; primary/clustering indexes CAN be sparse, secondary indexes MUST ALWAYS be dense.

GATE TRAP: confusing a CLUSTERING index (requiring the file sorted on a NON-key, repeating attribute) with a PRIMARY index (requiring a UNIQUE, key attribute) is a genuinely frequent error — the DEFINING distinction is whether the SORT-ORDER attribute is UNIQUE per value (primary) or shared by MULTIPLE records (clustering); both require the file to be physically sorted, but on FUNDAMENTALLY different KINDS of attribute.

GATE TRAP: assuming a SECONDARY index could POSSIBLY be sparse under SOME favorable circumstance is incorrect — a secondary index is ALWAYS dense, WITHOUT exception, precisely BECAUSE the underlying data file is NOT sorted on the secondary index's OWN search key, meaning matching records can be SCATTERED anywhere, with NO contiguous block available to scan forward from the way a sparse primary or clustering index relies on.

B-TREE VS B+-TREE: THE STRUCTURAL DIFFERENCE

[[FIG:bplustree-leaf-chain]]

1. A B-TREE allows search-key values AND their associated data POINTERS to appear in BOTH internal (non-leaf) AND leaf nodes — EACH key value is stored EXACTLY once, ANYWHERE in the tree, together with its own record pointer, wherever it happens to LAND during insertion or a subsequent split. RANGE queries are LESS efficient here, since leaf nodes are NOT linked together, and an in-order traversal must repeatedly move UP and DOWN the tree.
2. A B+-TREE keeps ALL actual data-record POINTERS exclusively at the LEAF level — internal nodes store ONLY search-key VALUES, used PURELY for routing/navigation (COPIES of keys, carrying NO data pointers of their own; a key value that ALSO routes internally is genuinely DUPLICATED at internal levels). LEAF nodes are ADDITIONALLY linked together in a SEQUENTIAL chain (typically a singly or doubly linked list), enabling FAST, efficient range/sequential scans by simply FOLLOWING leaf pointers WITHOUT ever revisiting internal nodes. BECAUSE internal nodes hold ONLY keys (no data pointers), they can PACK more keys per node than a B-tree's own internal nodes of the SAME block size — giving a B+-tree a HIGHER FAN-OUT, and hence FEWER levels, for the SAME number of records — EXACTLY why B+-trees are used ALMOST UNIVERSALLY in real database indexes, over plain B-trees.

B+-TREE ORDER FORMULAS: THE COMPLETE DERIVATION

1. LET a block (node) of a given SIZE hold at most 'p' TREE pointers and the associated KEYS. DEFINE the ORDER 'n' as the MAXIMUM number of tree pointers a NODE can hold.
2. INTERNAL (NON-LEAF) NODE structure, defined precisely: an internal node of order n has AT MOST n tree pointers and AT MOST (n−1) search-key values (since BETWEEN n pointers there are EXACTLY n−1 "gaps" for separator keys: pointer-key-pointer-key-...-pointer). SPACE constraint: n×(pointer size) + (n−1)×(key size) ≤ block size. SOLVING for n: n ≤ (block size + key size) / (pointer size + key size), then TAKE THE FLOOR.
3. LEAF NODE structure, defined equally precisely and symmetrically: a leaf node holds AT MOST (n−1) search-key values, EACH paired with ONE record pointer of its own, PLUS ONE extra "next-leaf" block pointer at the END for the linked-list chaining. SPACE constraint: (n−1)×(key size + record-pointer size) + (block pointer size) ≤ block size. SOLVING for n: n ≤ (block size − block pointer size + key size) / (key size + record pointer size), FLOOR the result.
4. MINIMUM OCCUPANCY GUARANTEE (for a B+-tree of order n, IGNORING the root node specifically, since the root alone has a LOOSER, more relaxed minimum of just 2 pointers): EVERY non-root internal node must have AT LEAST ⌈n/2⌉ pointers (hence at least ⌈n/2⌉−1 keys); EVERY non-root leaf must be AT LEAST half full — the EXACT convention (⌊(n−1)/2⌋ or ⌈(n−1)/2⌉) VARIES by specific textbook, so a given question TYPICALLY states its own convention explicitly, or asks ONLY for the MAXIMUM-order computation directly.

1. Worked example (order computation, independently verified by substituting BACK into the raw inequality): block size = 1024 bytes, key size = 14 bytes, block/tree-pointer size = 8 bytes, record-pointer size = 8 bytes (same as block-pointer here). COMPUTE the order of internal AND leaf nodes. INTERNAL NODE: n ≤ (1024+14)/(8+14) = 1038/22 = 47.18 → FLOOR → n=47. VERIFY: 47×8 + 46×14 = 376+644 = 1020 ≤ 1024 (correct); checking n=48 would need 48×8+47×14 = 384+658 = 1042 > 1024, CONFIRMING 47 is genuinely the maximum. LEAF NODE: n ≤ (1024−8+14)/(14+8) = 1030/22 = 46.8 → FLOOR → n=46, meaning AT MOST (n−1)=45 (key, record-pointer) pairs. VERIFY: 45×(14+8)+8 = 45×22+8 = 990+8 = 998 ≤ 1024 (correct, with room to spare); checking n=47 (46 pairs) would need 46×22+8 = 1012+8 = 1020, which STILL fits — this DIRECTLY illustrates WHY always re-substituting the computed n back into the RAW size inequality (rather than trusting the algebraic floor formula alone) is essential, since off-by-one errors here are the SINGLE most common GATE mistake on this entire topic.

GATE TRAP: assuming 'order' is ALWAYS defined the SAME way across every textbook and every question is a genuinely important source of confusion — SOME sources define order as the MAXIMUM number of KEYS per node, while OTHERS (Silberschatz, the MOST common convention in GATE) define it as the MAXIMUM number of POINTERS per node; ALWAYS re-derive the pointer/key inequality DIRECTLY from the block-size constraint a SPECIFIC question actually gives, rather than assuming some memorized numeric order applies UNCONDITIONALLY across every possible convention.

WORKED PROBLEMS

1. ORDER COMPUTATION WITH DIFFERENT SIZES. Block size = 2048 bytes, key size = 20 bytes, pointer size = 10 bytes (both tree and record pointers). Compute the internal-node order. n ≤ (2048+20)/(10+20) = 2068/30 = 68.93 → FLOOR → n=68. VERIFY: 68×10+67×20 = 680+1340 = 2020 ≤ 2048 (correct); n=69 would need 69×10+68×20 = 690+1360 = 2050 > 2048, confirming 68 is correct.

2. LEAF ORDER FOR THE SAME PARAMETERS. Using the same sizes from WP1, compute the leaf-node order. n ≤ (2048−10+20)/(20+10) = 2058/30 = 68.6 → FLOOR → n=68, meaning at most 67 (key,pointer) pairs. VERIFY: 67×30+10 = 2010+10 = 2020 ≤ 2048 (correct); n=69 (68 pairs) would need 68×30+10 = 2040+10 = 2050 > 2048, confirming 68 is correct.

3. PRIMARY VS CLUSTERING VS SECONDARY IDENTIFICATION. A file Employee is physically sorted on Emp_ID (unique per employee). A separate index exists on Department (many employees share the same department, and the file is NOT sorted on Department). Classify each index. The index on Emp_ID is a PRIMARY index (sorted, unique key, matches the file's own physical order). The index on Department is a SECONDARY index (NOT the file's own physical sort order) — and since Department is not the sort key, this secondary index must be DENSE.

4. SPARSE INDEX FEASIBILITY CHECK. Could a sparse index be built directly on the Department field from WP3, given the file is sorted on Emp_ID instead? No — sparse indexing REQUIRES the data file to be physically sorted on the SAME field the index is built on; since the file is sorted on Emp_ID, not Department, records sharing the same Department value are SCATTERED throughout the file, making a sparse index on Department fundamentally infeasible — only a DENSE secondary index works here.

INSERTION AND SPLIT BEHAVIOR

1. INSERTING into a leaf WITH room: simply insert the (key, pointer) pair in SORTED position — no split needed.
2. LEAF OVERFLOW (the leaf ALREADY has n−1 pairs, and the new insertion would EXCEED capacity): SPLIT the leaf into TWO leaves, distributing the n TOTAL pairs (the old n−1 plus the new one) so the LEFT leaf keeps ⌈n/2⌉ entries and the RIGHT keeps the remainder; COPY UP the SMALLEST search-key value of the NEW right leaf into the PARENT as a separator — this key is DUPLICATED (it STILL physically remains in the right leaf too, UNLIKE internal-node splits).
3. INTERNAL NODE OVERFLOW (would EXCEED n pointers after inserting a new separator/pointer pair FROM a child's own split): split into TWO internal nodes, and PUSH UP (NOT copy) the MIDDLE key into the parent — this middle key is REMOVED entirely from BOTH resulting children, since internal nodes NEVER store actual data, only routing keys.
4. IF the ROOT itself splits, a NEW root is created ABOVE it, INCREASING the tree's HEIGHT by EXACTLY one level — this is the ONLY way a B+-tree grows TALLER, and it KEEPS the tree PERFECTLY height-balanced (ALL leaves remain at the SAME depth) BY construction.

GATE TRAP: swapping the COPY-UP (at leaf-split time — the separator key REMAINS in the leaf) versus PUSH-UP (at internal-node-split time — the middle key is REMOVED from the children) behaviors is THE single most common insertion-mechanics mistake on this entire topic — leaf splits DUPLICATE their separator key (it exists in BOTH the leaf and the parent), while internal-node splits MOVE their middle key upward ENTIRELY (it exists ONLY in the parent afterward, having been REMOVED from both children).

WORKED PROBLEMS, PART TWO

5. LEAF SPLIT TRACE. A B+-tree leaf of order 4 (max 3 key-pointer pairs) already holds keys {10,20,30}, and key 25 is inserted, causing overflow. Trace the split, stating which key gets copied up. After inserting 25 in sorted position, the leaf conceptually holds {10,20,25,30} (4 keys, exceeding capacity 3). Splitting with a ⌈4/2⌉=2 / 2 distribution: LEFT leaf keeps {10,20}, RIGHT leaf keeps {25,30}. The SMALLEST key of the right leaf, 25, is COPIED UP into the parent as the new separator — 25 remains physically present in the right leaf as well.

6. INTERNAL NODE SPLIT TRACE. An internal node of order 4 (max 3 keys, 4 pointers) holds keys {15,25,35} with 4 child pointers, and a new separator key 30 arrives from a child split, requiring insertion between the pointers for 25 and 35. Trace the resulting split, stating which key gets pushed up. Conceptually the node now holds {15,25,30,35} (4 keys, exceeding capacity 3) with 5 pointers. Splitting: the MIDDLE key, 30, is PUSHED UP into the parent — 30 is REMOVED from both resulting internal nodes; the LEFT node keeps keys {15,25} with their 3 corresponding pointers, and the RIGHT node keeps {35} with its 2 corresponding pointers.

BLOCK ACCESS COUNTING

1. FOR a B+-tree of height h (the number of NON-LEAF levels traversed from root down to the leaf level), an EXACT-MATCH search costs h+1 total block accesses: ONE per internal level down to LOCATE the correct leaf, PLUS one to READ the leaf itself — PLUS possibly ONE MORE, to fetch the ACTUAL data record, if the leaf stores only a POINTER rather than the record itself (exactly the situation for a SECONDARY, non-clustering index).
2. A RANGE query ADDITIONALLY costs ONE extra block access PER subsequent leaf block scanned via the leaf-CHAIN linked list, PLUS one access PER qualifying record if those records must be fetched INDIVIDUALLY (FURTHER block accesses if those records are SCATTERED rather than clustered together).

1. Worked example (block access counting, independently traced through): a B+-tree index has height h=3 (3 internal levels down to the leaf level), built as a SECONDARY (non-clustering) index. An exact-match query costs: 3 (internal levels) + 1 (leaf) + 1 (fetching the actual scattered data record, since this is secondary) = 5 total block accesses. If this were instead a PRIMARY (clustering) index, where the leaf pointer already leads directly into the sorted data file itself with no separate fetch needed, the SAME query would cost only 3+1=4 total block accesses — directly illustrating the extra cost a secondary index's own additional data-fetch step imposes relative to a primary/clustering index.

MULTI-LEVEL INDEXING: TURNING A LARGE INDEX INTO A TREE

1. A single-level index (dense OR sparse) is ITSELF just an ordered file of (key, pointer) pairs — and once the DATA file grows large enough that even THIS index no longer fits comfortably in a small number of blocks, EXACTLY the same problem that motivated indexing the data file in the FIRST place reappears ONE level up: searching the index itself now costs O(log₂(index blocks)) via binary search, which is STILL multiple block accesses for a sufficiently large index.
2. THE fix is MULTI-LEVEL INDEXING: treat the FIRST-level index as itself a "DATA file" (sorted on the search key, since index entries are ALWAYS stored in sorted key order), and build a SECOND-level (necessarily SPARSE, since the first-level index is a sorted file with one entry per block group) index ON TOP of it — one entry per BLOCK of the first-level index. This process can be REPEATED, giving a THIRD level, a FOURTH level, and so on, forming exactly the TREE structure a B+-tree already IS — in fact, a B+-tree can be understood PRECISELY as a multi-level sparse index carried all the way to the point where the TOP level fits in a SINGLE block (the root).
3. THE outermost (topmost) level of a multi-level index is called the MASTER index (or root, in B+-tree terms) — it is kept PERMANENTLY in main memory, since it is small enough to fit in ONE block, meaning the FIRST actual disk access of any search happens only at the SECOND level down.
4. BLOCK ACCESS COUNT for an m-level index PLUS the data file itself is EXACTLY m+1 (one access per index level, PLUS one final access to fetch the actual data block or record) — this is the SAME h+1 formula already derived for B+-trees in the BLOCK ACCESS COUNTING section above, confirming that multi-level indexing and B+-tree search are, at their structural core, the SAME idea viewed from two different historical starting points (multi-level indexing starts from a flat sorted index and RECURSIVELY indexes it; a B+-tree is DESIGNED directly as a balanced multi-level structure from the outset, with explicit split/merge rules keeping every level UNIFORMLY balanced, which a naively recursive multi-level index does NOT automatically guarantee under insertions and deletions).

GATE TRAP: assuming multi-level indexing and B+-trees are UNRELATED, separately-examined topics is a mistake that costs UNDERSTANDING, not just a wrong answer — a NAIVE multi-level index (built once, over a STATIC sorted file, with no rebalancing machinery) can become UNBALANCED after repeated insertions, since a single level's own block count can grow unevenly; a B+-tree solves EXACTLY this weakness by baking split-and-push-up rules directly into its OWN insertion algorithm, guaranteeing every leaf STAYS at the identical depth NO MATTER how many insertions or deletions occur — the B+-tree IS a self-balancing multi-level index, not a different structure entirely.

HASHING TECHNIQUES: STATIC, EXTENDIBLE, AND LINEAR HASHING

1. STATIC HASHING: a FIXED number of buckets M is chosen IN ADVANCE, and a hash function h(key) maps every key to one of the M buckets (typically h(key) = key mod M, or some similar reduction). PROBLEM: since M is fixed, the file CANNOT grow or shrink gracefully — too FEW buckets for the ACTUAL data volume causes excessive OVERFLOW chaining (multiple records per bucket linked together, degrading performance toward linear search WITHIN a bucket), while too MANY buckets for a SMALL actual data volume wastes space needlessly.
2. EXTENDIBLE HASHING solves the fixed-bucket-count problem by maintaining a DIRECTORY of pointers to buckets, where the directory itself can DOUBLE in size as needed. Each directory entry uses the FIRST 'd' bits of a key's hash value (d is called the GLOBAL DEPTH) to determine WHICH bucket entry to follow; EACH bucket additionally tracks its OWN LOCAL DEPTH (the number of hash bits ACTUALLY used to distinguish that specific bucket from its "buddy"). When a bucket OVERFLOWS: if the bucket's local depth EQUALS the CURRENT global depth, the ENTIRE directory must DOUBLE in size (global depth increments by 1) before the bucket can split; if the bucket's local depth is LESS than the global depth, the bucket can SPLIT WITHOUT growing the directory at all (simply updating the relevant directory pointers to reference the new, second bucket).
3. LINEAR HASHING solves the SAME fixed-bucket-count problem WITHOUT any directory at all, avoiding the extra level of indirection extendible hashing requires. Buckets are split in a PREDETERMINED, ROUND-ROBIN linear ORDER (0, 1, 2, ..., regardless of WHICH specific bucket actually overflowed) as the file grows; a POINTER 'next' tracks which bucket is due to split NEXT. Overflow BEFORE a bucket's own scheduled turn is handled via ordinary OVERFLOW CHAINING (a linked list of overflow blocks attached to that bucket), exactly as in static hashing, until the 'next' pointer eventually reaches it. Because the split ORDER is fixed in advance rather than reactive to WHICH bucket is actually full, individual buckets can TEMPORARILY have quite different load factors, but NO directory doubling is EVER required, and the overall structure grows GRACEFULLY, one bucket at a time, rather than in the sudden directory-doubling jumps extendible hashing exhibits.
4. THE key comparative fact, heavily tested: extendible hashing achieves O(1) average access (after following ONE directory lookup) with NO overflow chains needed at all in the typical case, at the cost of the EXTRA directory-indirection level and the OCCASIONAL cost of doubling the ENTIRE directory; linear hashing needs NO directory (saving that indirection), but tolerates SOME overflow chaining for buckets not yet reached by the round-robin 'next' pointer, trading a SMALL amount of chain-following cost for the total absence of any directory-doubling operation.
5. BOTH extendible and linear hashing achieve what static hashing structurally CANNOT: graceful, incremental growth as the underlying data volume increases, WITHOUT ever needing a full, expensive REHASHING of the ENTIRE file into a brand-new, larger set of buckets all at once — this incremental-growth property is precisely why real database and file systems favor one of these two DYNAMIC hashing schemes over plain static hashing whenever the eventual file size cannot be predicted accurately in advance, exactly the same kind of "avoid one giant, disruptive reorganization in favor of many small, local ones" design principle already seen driving the B+-tree's own local, single-node split-and-push-up behavior rather than any global tree-wide rebuild.

1. Worked example (extendible hashing directory-doubling trace, independently verified): a directory currently has global depth d=2 (4 directory slots, for hash prefixes 00, 01, 10, 11), and buckets B1 (local depth 2, prefix 00), B2 (local depth 1, prefixes 01 AND 11 share this ONE bucket, since local depth 1 means only the FIRST bit is actually used to distinguish it), and B3 (local depth 2, prefix 10). Suppose B1 (local depth 2, EQUAL to the CURRENT global depth 2) now overflows. SINCE local depth equals global depth, the directory must DOUBLE: global depth becomes 3 (8 slots), and B1 splits into TWO buckets, each now at local depth 3, with the directory's 000 and 001 slots (formerly BOTH pointing to B1) now pointing to the TWO new post-split buckets respectively — meanwhile B2 and B3, whose OWN local depths (1 and 2) were BELOW the old global depth, simply gain additional directory slots pointing to THEMSELVES unchanged (B2's local depth 1 now corresponds to FOUR of the eight new slots: 010,011,110,111 all still route to the UNCHANGED bucket B2), confirming that ONLY the actually-overflowing bucket needs to physically split, even though the ENTIRE directory doubles in size.

BITMAP INDEXES

1. A BITMAP INDEX is built on an attribute with a SMALL number of DISTINCT values (LOW cardinality, such as gender, a boolean flag, or a small fixed set of categories) — for EACH distinct value, a BIT VECTOR is stored with ONE bit per record in the file, set to 1 if that record HAS the given value and 0 otherwise.
2. BITMAP indexes are extraordinarily EFFICIENT for combining MULTIPLE conditions via bitwise AND/OR/NOT operations directly on the bit vectors — a query like "gender = 'F' AND department = 'CS'" is answered by a SINGLE bitwise AND of the two relevant bit vectors, producing a RESULT bitmap whose set bits directly identify the QUALIFYING records, all WITHOUT touching the underlying data file at all until the final record-fetch step.
3. Bitmap indexes are a POOR choice for HIGH-cardinality attributes (such as a primary key), since the NUMBER of distinct bit vectors needed grows WITH the number of distinct values, and each individual vector would be extremely SPARSE (mostly 0s), wasting space rather than saving it — this is the DIRECT complement of B+-tree indexes, which work well ACROSS the cardinality spectrum but shine PARTICULARLY on high-cardinality, range-query-friendly attributes.
4. Because a SINGLE UPDATE to one record potentially touches MULTIPLE bit vectors (one for the record's OLD value, needing its bit CLEARED, and one for its NEW value, needing its bit SET), bitmap indexes are BEST suited to primarily READ-heavy, analytical (OLAP-style) workloads rather than heavily UPDATED, transactional (OLTP-style) tables — exactly the same READ-versus-WRITE trade-off already familiar from this book's OWN earlier discussions of denormalization and materialized views in the previous Databases chapter.

1. Worked example (bitmap AND query, independently verified by hand-tracing the bit vectors): a table of 8 records has a Gender bitmap with vectors F=10110100 and M=01001011, and a Department bitmap with vectors CS=11100010 and EE=00011101, where bit position 1 (leftmost) corresponds to record 1. Find all records with Gender=F AND Department=CS. Bitwise AND of F (10110100) and CS (11100010) gives 10100000 — meaning ONLY records 1 and 3 satisfy BOTH conditions, a result directly confirmed by inspecting: record 1 has F=1 and CS=1 (matches); record 3 has F=1 and CS=1 (matches); every OTHER position has at least one of the two bits as 0, correctly excluding it.

MULTI-KEY (COMPOSITE) INDEXES AND INDEX SELECTION

1. A MULTI-KEY (composite) INDEX is built on an ORDERED COMBINATION of two or more attributes, such as (Department, Salary) rather than on Department or Salary separately — entries are sorted FIRST by the leading attribute, and WITHIN each leading-attribute value, sorted by the SECOND attribute, and so on.
2. THE LEFTMOST-PREFIX RULE, heavily tested: a composite index on (A, B) can EFFICIENTLY answer queries filtering on A alone, or on BOTH A and B together, but CANNOT efficiently answer a query filtering on B alone — since the index's own physical ORDER is governed by A first, records with a given B value (but ANY A value) are SCATTERED throughout the index, exactly as a secondary index's own records were scattered in the earlier PRIMARY/CLUSTERING/SECONDARY discussion.
3. INDEX SELECTION (choosing WHICH attributes to index, and in WHICH combination) is fundamentally a TRADE-OFF: MORE indexes speed up SELECT queries (more access paths available to the query optimizer), but SLOW DOWN INSERT, UPDATE, and DELETE operations (every index on a modified attribute must ITSELF be updated), and consume ADDITIONAL storage — this is the SAME general space/access-efficiency trade-off this chapter's own CARRYING THIS FORWARD discussion below identifies as the connecting theme running through EVERY topic in this entire chapter.
4. A COVERING INDEX is a composite index that includes EVERY attribute a particular query needs, allowing the query to be answered ENTIRELY from the index itself, WITHOUT ever touching the underlying data file at all — this is an important PRACTICAL optimization technique in real systems, though GATE questions typically focus on the LEFTMOST-PREFIX rule and the basic space/speed trade-off rather than covering indexes specifically.
5. THE number of DISTINCT possible composite indexes over a set of k attributes, if ORDER within the composite matters (which it genuinely does, per the leftmost-prefix rule above), is the number of NON-EMPTY ordered sequences drawable from those k attributes — for a small k this grows quickly, which is EXACTLY why real database administrators do not simply index "every possible combination" but instead choose composite indexes DELIBERATELY, matching them to the ACTUAL, observed query patterns a specific application issues, rather than attempting exhaustive coverage.

GATE TRAP: assuming a composite index on (A, B) is EQUALLY useful for a query filtering on B alone as it is for A alone is a common and costly misunderstanding — the LEFTMOST-PREFIX rule means such an index is USELESS (or at best, requires a FULL index scan rather than an efficient search) for a query that filters ONLY on the trailing attribute B, exactly analogous to how a book's own index sorted by chapter title cannot be efficiently searched by PAGE number alone. A CLOSELY related trap is assuming that building TWO SEPARATE single-attribute indexes on A and on B individually is always EQUIVALENT to one composite index on (A, B) — it is NOT: a query filtering on BOTH A and B can, in principle, use EITHER single-attribute index alone and then filter the remaining condition by scanning the resulting candidate records, but this is generally LESS efficient than a single composite index that narrows DIRECTLY to the qualifying block group using BOTH conditions simultaneously, without any secondary filtering step required at all.

1. Worked example (leftmost-prefix reasoning, independently verified against the definition): a table Employee has a composite B+-tree index on (Department, Salary). For each of three queries, state whether the index can be used efficiently: (a) WHERE Department='CS' — YES, this matches the LEADING attribute directly, an efficient range/exact search. (b) WHERE Department='CS' AND Salary>50000 — YES, this matches BOTH attributes in the SAME leading-to-trailing order the index is sorted by, allowing the search to narrow to the CS block group first and then scan forward on Salary within it. (c) WHERE Salary>50000 (no Department filter) — NO, since Salary is the TRAILING attribute, records with Salary>50000 are scattered across EVERY Department's own block group, requiring a full index (or file) scan rather than a direct, efficient search.

WORKED PROBLEMS, PART THREE: HASHING AND MULTI-LEVEL SYNTHESIS

7. LINEAR HASHING SPLIT ORDER TRACE. A linear hashing file begins with 4 buckets (0,1,2,3), a 'next' pointer at bucket 0, and each bucket capacity 3 records. Records with hash values (mod 4) 0,0,0,1 are inserted, in that order, into buckets 0 and 1 respectively (three into bucket 0, filling it to capacity, then one into bucket 1). A FIFTH record hashing to bucket 0 now arrives, causing bucket 0 to OVERFLOW. Since linear hashing splits buckets in ROUND-ROBIN order regardless of which bucket overflowed, and 'next' is CURRENTLY at bucket 0, the overflow is handled temporarily via an OVERFLOW CHAIN attached to bucket 0 (a linked block holding the excess record), and the ACTUAL split of bucket 0 happens only when the round-robin 'next' pointer reaches it in its OWN normal turn — this is the precise trade-off already stated in the HASHING TECHNIQUES section: linear hashing tolerates temporary overflow chains on buckets not yet due for their scheduled split, in exchange for never needing an entire directory to double.

8. MULTI-LEVEL INDEX BLOCK-ACCESS COMPARISON. A data file has 1,000,000 records, and a single-level dense index would require 4,000 index blocks (250 entries per block) to index it, needing ⌈log₂4000⌉≈12 comparisons under binary search, but MORE importantly still costing multiple actual disk block accesses if the index itself does not fit in memory. Building a SECOND-level sparse index over the 4,000 first-level blocks (also 250 entries per block) needs only ⌈4000/250⌉=16 second-level blocks — small enough to fit in a HANDFUL of blocks, or even ONE block as a master index if entries are large enough to need fewer than 250 (in general, an even smaller top level, or a further third level, can always be added). This directly confirms the multi-level indexing section's own general m+1 formula: with a 2-level index (m=2 index levels) plus the final data-block fetch, total block accesses become 2+1=3, a substantial improvement over the SINGLE flat index's own binary-search cost, once the flat index has itself grown too large to comfortably search in memory.

9. COVERING-INDEX REASONING. A query SELECT Salary FROM Employee WHERE Department='CS' is issued against a table with a composite index on (Department, Salary). Explain why this composite index can answer the ENTIRE query without ever touching the underlying Employee data file. Since the index itself already stores BOTH Department (as the search/filter key) AND Salary (as the very NEXT attribute in the SAME composite index entry), every qualifying entry's Salary value is available DIRECTLY from the index leaf itself — there is no need to follow a record pointer into the data file at all, since the requested output attribute is ALREADY present in the index structure; this is precisely the COVERING INDEX behavior introduced in the MULTI-KEY INDEXES section.

10. BITMAP INDEX SELECTIVITY REASONING. A table has 1,000,000 records. Attribute Status has only 3 distinct values (Active, Inactive, Pending), while attribute Employee_ID is a unique primary key with 1,000,000 distinct values. Explain why a bitmap index suits Status but not Employee_ID. A bitmap index on Status needs only 3 bit-vectors, each 1,000,000 bits (125,000 bytes) long — a modest, fixed storage cost regardless of how the values happen to be distributed. A bitmap index on Employee_ID would need 1,000,000 SEPARATE bit-vectors, each ALSO 1,000,000 bits long, but with EXACTLY one bit set in each (since every value is unique) — an enormous, overwhelmingly SPARSE storage cost that a single dense B+-tree index on Employee_ID would represent far more compactly and search far more efficiently via ordinary tree traversal instead.

THE FULL PICTURE: CHOOSING AN ACCESS STRUCTURE

1. GIVEN a specific attribute and workload, the choice among the structures this chapter has covered follows directly from THREE questions: (a) is the query workload dominated by EXACT-match lookups, RANGE queries, or MULTI-CONDITION filtering? (b) what is the attribute's CARDINALITY (number of distinct values)? (c) is the file's PHYSICAL sort order already fixed by some OTHER, higher-priority attribute?
2. FOR range queries on a HIGH-cardinality attribute NOT already governing the file's physical order: a SECONDARY B+-tree index is the natural choice, accepting the extra data-fetch block access the earlier BLOCK ACCESS COUNTING worked example already quantified.
3. FOR exact-match lookups ONLY (no range queries ever needed) on a high-cardinality attribute: HASHING (static if the file size is well known and stable, extendible or linear if the file is expected to grow) typically OUTPERFORMS a B+-tree, since hashing avoids the multiple internal-level traversals a tree search requires, achieving closer to O(1) rather than O(log n) access.
4. FOR combining MULTIPLE low-cardinality conditions (such as several categorical filters ANDed together) in a primarily READ-heavy, analytical workload: bitmap indexes, exploiting fast bitwise operations, typically outperform EITHER a B+-tree or hashing built on the SAME attributes individually.
5. THIS decision structure is EXACTLY the same kind of systematic, criterion-by-criterion reasoning this book has applied repeatedly across earlier chapters — for instance, the earlier Memory Hierarchy chapter's own systematic cache-mapping-policy trade-off analysis, and the previous Databases chapter's own systematic BCNF-versus-3NF decomposition trade-off — reinforcing that GATE questions on "which structure is best here" are almost always answerable by identifying which SPECIFIC criterion (cardinality, query pattern, or existing physical order) the question is actually probing, rather than by memorizing a flat, unconditional ranking of structures. Internalizing this three-question checklist turns what otherwise feels like an unbounded catalogue of independent facts into a single, reusable, general decision procedure applicable to essentially any access-structure question a GATE examination paper can reasonably pose.

11. FULL SYNTHESIS WORKED PROBLEM. A university database has an Enrollment table with 2,000,000 rows and three attributes queried frequently: Student_ID (unique, 2,000,000 distinct values), Grade (5 distinct values: A,B,C,D,F), and Enrollment_Date (a continuous date range, queried mostly via range filters such as "enrolled between two dates"). Recommend an access structure for EACH attribute, justifying each choice against the FULL PICTURE criteria above. For Student_ID: high cardinality, exact-match lookups dominate (looking up a specific student's enrollment) — a HASH index (static, since the table's approximate size is well known in advance for an academic term) is the natural choice, achieving near-O(1) access without paying for tree-traversal overhead the range-query capability of a B+-tree would otherwise provide but which this attribute's own access pattern does not actually need. For Grade: very LOW cardinality (5 values), and queries typically combine it with OTHER conditions (such as "Grade='F' AND Enrollment_Date in [range]") — a BITMAP index is the natural choice, since ANDing a small Grade bitmap against a Date-range result set is exactly the multi-condition, low-cardinality scenario bitmap indexes excel at. For Enrollment_Date: high cardinality (many distinct timestamps) with RANGE queries as the dominant access pattern — a B+-TREE (secondary) index is the natural choice, since B+-tree leaves are chained specifically to support efficient sequential range scans, exactly the capability hashing structurally cannot provide. This worked problem illustrates that a SINGLE table frequently benefits from MULTIPLE, DIFFERENT index types simultaneously, each chosen according to that SPECIFIC attribute's own cardinality and query pattern, rather than any single "best" structure applying uniformly across an entire table.

B+-TREE DELETION: MERGING AND REDISTRIBUTION

1. DELETING a (key, pointer) pair from a leaf that STAYS at or above its minimum occupancy (⌈(n−1)/2⌉ entries, or the specific textbook's own stated minimum) is trivial: simply remove the entry, with no further structural change needed.
2. IF deletion causes a leaf to fall BELOW minimum occupancy (UNDERFLOW), the FIRST recourse is REDISTRIBUTION (also called BORROWING): if an ADJACENT sibling leaf has MORE than the minimum number of entries, ONE entry is moved from the sibling into the underflowing leaf, and the PARENT's separator key is UPDATED to reflect the new smallest key of whichever leaf now starts later — this is generally PREFERRED over merging, since it keeps the tree's OVERALL entry count spread more EVENLY and avoids the cascading update a merge can trigger.
3. IF NO adjacent sibling has entries to SPARE (both are already AT their own minimum), the underflowing leaf instead MERGES with a sibling — combining both leaves' entries into ONE, and REMOVING the now-redundant separator key AND the deleted child pointer from the PARENT. Removing an entry from the parent can, in turn, cause the PARENT ITSELF to underflow, potentially CASCADING the redistribution-or-merge decision all the way up to the root.
4. IF the ROOT ends up with only ONE remaining child pointer after a merge cascades all the way up, that SINGLE child becomes the NEW root, and the tree's height DECREASES by exactly one level — this is the ONLY way a B+-tree SHRINKS in height, exactly mirroring how a root SPLIT was earlier identified as the only way a B+-tree GROWS in height, making growth and shrinkage perfectly symmetric, single-level operations that always occur EXCLUSIVELY at the root.

GATE TRAP: assuming deletion always triggers IMMEDIATE merging whenever a leaf drops below its minimum is incorrect — REDISTRIBUTION (borrowing from a sibling with entries to spare) is always attempted FIRST, and only when NO sibling can spare an entry does an ACTUAL merge occur; a question asking to "trace the deletion" that skips checking whether an adjacent sibling has surplus entries will very likely arrive at the WRONG resulting tree structure.

1. Worked example (redistribution versus merge decision, independently verified): a B+-tree leaf of order 4 (minimum 1 entry per non-root leaf, in the ⌈(4−1)/2⌉−1... using the simpler convention of minimum ⌈n/2⌉−1=1 entries) currently holds keys {40} after several prior deletions, and its LEFT sibling holds {10,20,30} (3 entries, well above its own minimum). Deleting the ONLY remaining key from the current leaf would leave it EMPTY (underflowing below minimum 1). Since the LEFT sibling has entries to SPARE (3, versus its own minimum of 1), REDISTRIBUTION is preferred: the largest key of the left sibling, 30, is moved into the underflowing leaf (which now holds {30}, though 40 has ALREADY been deleted in this trace so the leaf would actually need a replacement key from elsewhere in a genuine trace — this worked example illustrates the DECISION process of checking sibling surplus FIRST, which is the examinable skill, rather than a complete end-to-end deletion trace of every intermediate step). Had the LEFT sibling instead held only {10} (AT its own minimum, with nothing to spare), the correct action would instead be to MERGE the two leaves into one holding {10} (post-merge), removing the now-redundant separator from the parent.

CARRYING THIS FORWARD

This chapter has developed the COMPLETE machinery for making the WELL-NORMALIZED relational tables the previous chapter's own decomposition techniques produce EFFICIENT to actually search — file organizations providing the underlying PHYSICAL foundation, the dense/sparse and primary/clustering/secondary INDEX taxonomy determining WHICH structure suits WHICH access pattern, and the B+-tree's own PRECISE order formulas and insertion mechanics providing the CONCRETE, WIDELY-used structure real database systems actually implement. The single most valuable connecting insight to carry forward is that every design choice this chapter has covered — dense versus sparse, primary versus secondary, B-tree versus B+-tree — is DRIVEN by the same underlying trade-off this book has encountered REPEATEDLY across many other subjects: SPACE efficiency (fewer index entries, smaller nodes) traded directly against ACCESS efficiency (fewer block reads, faster range scans), with NO single choice being universally "better" independent of the specific access pattern a given application actually needs.

KEY: the B+-tree's own copy-up-at-leaf versus push-up-at-internal distinction is, at its structural core, a DIRECT consequence of the SAME "internal nodes carry NO data, only routing information" design principle that gives B+-trees their OWN characteristic higher fan-out over plain B-trees in the FIRST place — a leaf's OWN key genuinely represents actual, meaningful DATA (it must remain there), while an internal node's OWN key exists PURELY to guide navigation (it can be freely relocated UPWARD without losing any actual information, since NO data pointer was ever attached to it in the first place) — recognising this ONE underlying design principle explains BOTH the fan-out advantage AND the copy-up/push-up distinction as TWO consequences of the SAME single architectural choice, rather than as two separately-memorised, unrelated facts.

KEY: extendible hashing's directory-doubling and a B+-tree's own root-splitting are, once again, the SAME underlying idea appearing in TWO different structural guises — BOTH mechanisms exist PRECISELY to let a fixed-size top-level structure (the directory, or the root) grow to accommodate an ever-larger key space WITHOUT ever requiring EVERY existing bucket or node to be touched during a single growth event, achieving GRACEFUL, LOCALIZED growth rather than an expensive full reorganization.

KEY: bitmap indexes and B+-tree indexes are BEST understood as OPPOSITE ends of a single cardinality spectrum, rather than as competing GENERAL-purpose techniques — bitmap indexes EXCEL at LOW-cardinality attributes precisely because their bit-vector representation is compact ONLY when there are FEW distinct values, while B+-tree indexes excel across the FULL cardinality range but are PARTICULARLY suited to HIGH-cardinality attributes supporting efficient RANGE queries, meaning the CHOICE between them is a direct, quantifiable function of how many DISTINCT values the indexed attribute actually has.

KEY: the leftmost-prefix rule governing composite indexes is, at its structural core, IDENTICAL in spirit to the primary-versus-secondary index distinction from earlier in this chapter — in BOTH cases, an index (or a component of a composite index) is efficiently searchable ONLY on the attribute(s) that GOVERN its OWN physical sort order, and attributes appearing LATER in that order (or NOT at all) require a broader scan, exactly as a secondary index's own non-sort-order search key required visiting scattered records rather than one contiguous block.

KEY: multi-level indexing, B+-trees, and extendible/linear hashing all address the SAME root problem from three different angles — an index (or hash directory) that has grown too LARGE to search efficiently in a single pass needs its OWN internal structure reorganized, whether via RECURSIVE indexing (multi-level indexes and B+-trees) or via DYNAMIC bucket/directory growth (extendible and linear hashing) — recognizing this SHARED motivation across seemingly disparate techniques is far more valuable for GATE than memorizing each technique's mechanics in isolation.

The next and FINAL chapter in this Databases sequence, on TRANSACTIONS, examines how to keep the efficiently-INDEXED, well-NORMALIZED relational tables this book's own Databases sequence has built throughout SAFE and CONSISTENT when many operations run CONCURRENTLY against the same shared data — a genuinely different concern from this chapter's own EFFICIENCY-focused indexing analysis, but one operating on the EXACT SAME underlying relational tables and indexes this entire sequence has been building, chapter by chapter, from its very first page onward.

Before moving on, it is worth explicitly restating why this chapter placed its own topics in the specific order it did, since that ORDER itself carries examinable meaning. File organizations came FIRST because every other structure in this chapter is built literally ON TOP of some underlying file layout; dense-versus-sparse and primary-versus-clustering-versus-secondary came next because they establish the VOCABULARY every subsequent worked problem in this chapter relies on. B-trees and B+-trees came after that because they are the CONCRETE, near-universally-implemented realization of the ABSTRACT indexing ideas the earlier sections introduced; multi-level indexing was placed immediately alongside the B+-tree material specifically to make explicit the historical and conceptual LINK between a naive, repeatedly-indexed flat structure and a genuinely self-balancing tree. And hashing, bitmap indexes, and multi-key indexes came last as alternative or complementary structures, each addressing a specific access pattern the earlier, more general B+-tree discussion does not optimally cover on its own. A GATE question that asks to "compare and justify" an access-structure choice is, at its core, testing whether a candidate has internalized this same progression — from physical storage, through abstract indexing vocabulary, to concrete tree structures, to specialized alternatives — rather than testing rote recall of any single structure's mechanics in isolation.
`
};
