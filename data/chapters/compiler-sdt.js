// Textbook chapter: Syntax-Directed Translation and Semantic Analysis.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['compiler-sdt'] = {
  figs: [
    {
      id: 'synthesized-vs-inherited',
      caption: 'Synthesized attributes flow upward from children to a parent (computed at reduction time, fitting bottom-up parsers naturally). Inherited attributes flow downward and sideways from parent and left siblings to a child.',
      svg: '<svg viewBox="0 0 300 110" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.1" fill="none"><circle cx="75" cy="20" r="12"/><line x1="65" y1="30" x2="45" y2="60"/><line x1="85" y1="30" x2="105" y2="60"/><circle cx="225" cy="90" r="12"/><line x1="215" y1="80" x2="195" y2="50"/><line x1="235" y1="80" x2="255" y2="50"/></g><g font-size="9" fill="currentColor" text-anchor="middle"><text x="75" y="100">Synthesized: children→parent</text><text x="225" y="15">Inherited: parent→children</text></g></svg>'
    },
    {
      id: 'backpatch-lists',
      caption: 'Backpatching threads a jump instruction with a blank target into a truelist or falselist attribute, resolving the actual target address only once it becomes known later in the same left-to-right pass.',
      svg: '<svg viewBox="0 0 300 90" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.1" fill="none"><rect x="10" y="15" width="120" height="25"/><rect x="150" y="15" width="140" height="25"/><line x1="70" y1="40" x2="70" y2="60"/></g><g font-size="9" fill="currentColor" text-anchor="middle"><text x="70" y="31">goto ___ (blank)</text><text x="220" y="31">backpatch(list, target)</text><text x="70" y="75" font-size="8">filled in once target known</text></g></svg>'
    }
  ],
  text: `
WHAT THIS PHASE CONSUMES AND PRODUCES

Syntax-directed translation is the mechanism by which a compiler ATTACHES meaning — types, memory addresses, generated code fragments, error diagnostics — to the SYNTACTIC structure the previous chapter's parser already built. It CONSUMES the parse tree (or, more commonly in an actual implementation, the parser's own REDUCTION or DERIVATION sequence directly, with NO explicit tree data structure ever built at all) together with a set of SEMANTIC RULES the grammar's own author has associated with each production. It PRODUCES ATTRIBUTE VALUES attached to individual grammar-symbol INSTANCES within that parse (types, addresses, code fragments, error flags), and, as those attributes are computed, TRIGGERS the actual semantic-analysis WORK a compiler needs done — type checking, symbol-table updates, and, in the very next chapter's own subject, intermediate-code emission.

1. A SYNTAX-DIRECTED DEFINITION (SDD), the very first formal concept this chapter introduces and builds every later section upon, associates ONE OR MORE attributes and semantic RULES with EACH grammar production, WITHOUT prescribing any particular EVALUATION order — an SDD is a purely DECLARATIVE specification of WHAT each attribute's value should be, leaving HOW and WHEN to actually compute it as a separate implementation concern.
2. A SYNTAX-DIRECTED TRANSLATION SCHEME (SDT), the closely related second concept introduced here, instead EMBEDS semantic ACTIONS (conventionally written inside curly braces, {...}) at SPECIFIC positions within a production's own right-hand side — this FIXES a definite evaluation order, tied DIRECTLY to the exact moment, during an actual parse, when that embedded action's position is reached.
3. TWO KINDS of ATTRIBUTE altogether exist and matter greatly here throughout this whole material: SYNTHESIZED attributes are computed from a symbol's OWN children (or, for a terminal, supplied directly by the lexer — id.lexval being the standard example), flowing strictly BOTTOM-UP through the parse tree; INHERITED attributes are computed from a symbol's PARENT and its LEFT SIBLINGS, flowing TOP-DOWN and LEFT-TO-RIGHT instead. A terminal's own only meaningful attribute is normally synthesized, supplied directly by the lexical-analysis phase this book's own earlier Compiler Design chapter developed in full.

[[FIG:synthesized-vs-inherited]]

S-ATTRIBUTED SDDs: THE BOTTOM-UP-FRIENDLY CASE

1. AN S-ATTRIBUTED SDD, the simplest and most restricted of the two attribute classes this chapter develops carefully in turn, uses EXCLUSIVELY synthesized attributes — EVERY attribute in the ENTIRE definition is synthesized, with NO inherited attributes anywhere at all.
2. EVALUATION ORDER: an S-attributed SDD can be evaluated in ANY order CONSISTENT with the parse tree's own bottom-up structure — a straightforward POSTORDER traversal (evaluate every child completely before evaluating the parent) always suffices, and no more elaborate ordering discipline is ever needed.
3. WHY S-attributed SDDs fit BOTTOM-UP (LR-family) PARSERS so naturally: a REDUCTION, by its very definition (as developed in this book's own previous Syntax Analysis chapter), happens ONLY after ALL of the symbols making up the reduced production's right-hand side have ALREADY been fully recognized and are already sitting on the parser's own stack — meaning every CHILD's own attribute values are ALREADY available, sitting directly on a parallel SEMANTIC-VALUE stack running alongside the ordinary parsing stack, EXACTLY at the moment the semantic action for that reduction needs to run, with NO further waiting or reordering required at all.

L-ATTRIBUTED SDDs: THE BROADER, TOP-DOWN-COMPATIBLE CASE

1. AN L-ATTRIBUTED SDD, the second and broader of the two attribute classes central to this chapter's own development, permits BOTH synthesized AND inherited attributes, with EXACTLY one restriction on inherited attributes specifically: EACH inherited attribute of a symbol appearing on a production's right-hand side may depend ONLY on: attributes of symbols STRICTLY TO ITS LEFT within that SAME production, and/or INHERITED attributes of the production's OWN head (left-hand-side) nonterminal — NEVER on anything to its right, and NEVER on the head's OWN synthesized attribute (the head's synthesized attribute is what OTHER rules FEED INTO, not something this rule may draw FROM).
2. THIS SPECIFIC RESTRICTION is EXACTLY what makes an L-attributed SDD evaluable via a SINGLE depth-first, LEFT-TO-RIGHT traversal of the parse tree — since every attribute an L-attributed rule needs is GUARANTEED to already be available by the time a left-to-right, depth-first visit reaches that specific point, with NO need to look ahead to information not yet computed.
3. THIS SINGLE-PASS, LEFT-TO-RIGHT evaluability is precisely why L-attributed SDDs fit NATURALLY with TOP-DOWN (predictive/recursive-descent) parsing, with actions embedded DIRECTLY in-line at the appropriate points within each parsing procedure — and, with an ADDITIONAL grammar TRANSFORMATION (introducing a fresh MARKER nonterminal, with an ε-production, at each point an embedded action needs to occur), L-attributed SDDs CAN ALSO be implemented on BOTTOM-UP parsers, a genuinely important point this chapter's own GATE TRAP below addresses directly.
4. S-ATTRIBUTED SDDs are a STRICT SUBSET of L-attributed SDDs — every S-attributed SDD is TRIVIALLY also L-attributed, since having NO inherited attributes at all automatically, vacuously satisfies the "depends only on left/parent" restriction (there is simply nothing to check).

GATE TRAP: mislabeling an SDD as S-attributed simply because it "LOOKS bottom-up-oriented" at a casual glance, WITHOUT actually checking EVERY SINGLE attribute individually, is a common and consequential error — ONE single inherited attribute ANYWHERE in the entire definition, even if every OTHER attribute is synthesized, DISQUALIFIES the whole SDD from being S-attributed (though it may still be, and often is, L-attributed instead); the S-attributed classification requires a COMPLETE, exhaustive check across EVERY attribute the SDD defines, not a general impression of the SDD's overall "flavor."

GATE TRAP: assuming L-attributed SDDs can ONLY be implemented using top-down parsers is incorrect — L-attributedness is a restriction on DEPENDENCY DIRECTION (an inherited attribute may only depend on the left and the parent), COMPLETELY INDEPENDENT of which specific PARSING algorithm is actually used; the standard marker-nonterminal transformation (inserting a fresh, ε-producing nonterminal at exactly the position each embedded action needs to occur) lets an L-attributed SDD run perfectly well on a BOTTOM-UP (LR-family) parser too, simply by converting each embedded mid-production action into an ordinary end-of-production semantic action attached to the freshly-introduced marker nonterminal's own (trivial) reduction.

1. Worked example (L-attributed SDD for variable declarations, independently traced through in full): grammar D → T L ; ; T → int | float ; L → L1, id | id. Rules: T.type = "int" for T→int (synthesized on T), T.type = "float" for T→float (synthesized on T). D → T L { L.in = T.type } (L.in is INHERITED, set from T's own already-computed synthesized attribute). L → L1, id { L1.in = L.in; addtype(id.entry, L.in) } (L1's inherited attribute depends on L's OWN inherited attribute — allowed, since it is the head's own inherited attribute, not anything to L1's right). L → id { addtype(id.entry, L.in) }. CHECKING L-attributedness explicitly: L1.in depends only on L.in (the head's own inherited attribute — permitted); the final id's use of L.in likewise draws only from the head's inherited attribute (permitted, since nothing here depends on anything to id's own RIGHT, and there is nothing to id's right in this specific production anyway). No attribute anywhere depends on anything to its own right, so this SDD IS L-attributed — but it is NOT S-attributed, since L.in is genuinely inherited, not synthesized. TRACING "int a, b, c": T.type="int" is computed FIRST (a synthesized attribute, trivially available immediately); this value is then THREADED DOWN as L.in through each successive L→L1,id application, tagging a, b, and c ALL as type "int" — exactly the left-to-right, depth-first evaluation order this SDD's own L-attributed status guarantees is sufficient.

GENERAL ATTRIBUTE GRAMMARS: THE DEPENDENCY-GRAPH APPROACH

1. FOR A FULLY GENERAL SDD (one that is NEITHER S-attributed NOR L-attributed — permitting an inherited attribute to depend on symbols to its RIGHT, for instance), a SINGLE, simple traversal order (postorder for S-attributed; left-to-right depth-first for L-attributed) is NO LONGER guaranteed sufficient.
2. INSTEAD, a DEPENDENCY GRAPH is built, SEPARATELY, for EACH specific parse tree — one NODE per individual ATTRIBUTE INSTANCE (a specific attribute of a specific symbol occurrence within THIS particular parse tree), with a DIRECTED EDGE from node a to node b whenever b's own semantic rule USES a's value in computing itself.
3. A TOPOLOGICAL SORT of this dependency graph gives a VALID evaluation order — compute every attribute instance only AFTER every attribute instance it depends on has already been computed, exactly the standard topological-ordering guarantee.
4. IF the dependency graph contains a CYCLE, for SOME specific parse tree, the SDD is considered ILL-DEFINED (unevaluable) for that specific tree — a cyclic dependency graph has NO valid topological order at all, meaning the attribute values genuinely CANNOT be consistently computed for that tree.

GATE TRAP: confusing the FULLY GENERAL dependency-graph-plus-topological-sort machinery (needed ONLY for genuinely general SDDs, neither S- nor L-attributed) with the CONSIDERABLY simpler single-pass evaluation order that ALREADY suffices specifically for S-attributed and L-attributed SDDs is a common and genuinely time-costly error under exam conditions — a topological sort is ALWAYS a technically SAFE, universally correct approach for ANY SDD whatsoever, but it is needless, overkill MACHINERY for the S-attributed or L-attributed cases specifically, where a much simpler, directly-stated evaluation order (postorder, or left-to-right depth-first) is already known, in advance, to work correctly with no further graph construction required at all.

WORKED PROBLEMS

1. S-ATTRIBUTED VS L-ATTRIBUTED CLASSIFICATION. An SDD has the single rule E → E1 + E2 { E.val = E1.val + E2.val }, with no other rules anywhere in the grammar. Classify this SDD, and justify precisely. This SDD IS S-attributed (E.val is synthesized, computed purely from the ALREADY-available values of E1 and E2, both children of E — no inherited attributes appear anywhere at all), and since S-attributed is a strict subset of L-attributed, it is ALSO, trivially, L-attributed.

2. IDENTIFYING AN L-ATTRIBUTED-BUT-NOT-S-ATTRIBUTED VIOLATION. An SDD proposes the rule A → BC { B.in = C.val }, where B.in is meant to be an INHERITED attribute of B, computed from C's SYNTHESIZED attribute val. Is this SDD L-attributed? No — B appears to the LEFT of C in the production A→BC, so B's inherited attribute B.in is being made to depend on C's value, which sits to B's own RIGHT — this DIRECTLY VIOLATES the L-attributed restriction (an inherited attribute may depend only on symbols to its LEFT, or on the head's own inherited attributes), so this specific rule, as proposed, is NOT L-attributed at all (nor S-attributed, since B.in is inherited).

3. MARKER-NONTERMINAL TRANSFORMATION FOR BOTTOM-UP IMPLEMENTATION. An L-attributed SDT has an embedded mid-production action A → X {action} Y. Sketch the marker-nonterminal transformation needed to implement this same SDT on a bottom-up (LR) parser. Introduce a fresh nonterminal M with the single ε-production M → ε {action}, and rewrite the original production as A → X M Y — now the embedded action runs at the moment M's own (trivial, ε) production is REDUCED, which occurs at EXACTLY the point in the bottom-up parse corresponding to where the original mid-production action was positioned, letting an otherwise top-down-flavored embedded action run correctly within a bottom-up parser's own strictly end-of-production reduction-triggered semantic-action discipline.

4. DEPENDENCY-GRAPH CYCLE DETECTION. An SDD has rules A.s = B.i + 1 and B.i = A.s − 1, both attributes of sibling symbols A and B within the same production. Does this SDD's dependency graph contain a cycle for this production, and what does that imply? Yes — A.s depends on B.i (via the first rule), and B.i depends on A.s (via the second rule), giving a direct two-node CYCLE in the dependency graph (A.s → B.i → A.s). This means the SDD is ILL-DEFINED for this production — there is NO valid topological order, and hence no way to consistently compute either attribute's value at all, since each one's own definition circularly depends on the other's not-yet-computed value.

CARRYING THIS FORWARD, PART ONE

The material developed so far in this chapter — SDDs versus SDTs, synthesized versus inherited attributes, S-attributed versus L-attributed classification, and the general dependency-graph fallback — provides the CONCEPTUAL FRAMEWORK for attaching semantic meaning to a parse. The remainder of this chapter now applies that framework to two of a compiler's most important CONCRETE semantic-analysis tasks: TYPE CHECKING (verifying a program's own type usage is internally consistent) and CODE GENERATION FOR CONTROL FLOW via BACKPATCHING (generating correct, target-resolved jump instructions for boolean expressions and control structures in a single left-to-right pass) — both genuinely central, heavily GATE-tested applications of the attribute machinery this chapter has now developed in full.

TYPE CHECKING: VERIFYING CONSISTENT TYPE USAGE

1. TYPE CHECKING, one of the two central semantic-analysis tasks this entire chapter concerns itself with, verifies that EVERY operator/operand type COMBINATION appearing in a program is LEGAL according to the source language's own type rules, using a formal TYPE-EXPRESSION system — BASIC types (int, float, char, boolean, and similar), plus TYPE CONSTRUCTORS building more complex types from simpler ones (arrays, pointers, product/record types combining several fields, and function types conventionally written using an arrow, domain→range).
2. TYPE SYNTHESIS is typically performed BOTTOM-UP within an SDD — for a production like E → E1 + E2, E.type is DERIVED from a rule (or lookup table) COMBINING E1.type and E2.type (a typical rule set: int+int=int; int+float=float, via an IMPLICIT WIDENING coercion; and so on, for whatever specific combinations the language's own design permits) — a combination NOT covered by ANY defined rule or coercion is flagged as a genuine SEMANTIC (type) error.
3. TYPE CHECKING of STATEMENTS (assignments, function calls, array indexing) uses this SAME synthesized/inherited attribute machinery to COMPARE a declared type against the ACTUAL type of some expression (an assignment's own right-hand side against its left-hand side's declared type, for instance), and to CHECK the ARITY (correct number of arguments) and TYPE compatibility of ACTUAL versus FORMAL parameters at every function-CALL site.

GATE TRAP: treating "TYPE CHECKING" and "TYPE INFERENCE" as fully interchangeable, identical operations is a subtle but genuinely important distinction — type CHECKING verifies that a type is CONSISTENT with an ALREADY-declared or otherwise ALREADY-expected type (comparing two known things against each other); type INFERENCE instead DERIVES a type for an expression or variable where NONE was EXPLICITLY declared at all (producing a NEW piece of information, rather than merely VERIFYING an existing one); a GATE question's own correct answer can hinge DIRECTLY on which of these two genuinely different operations a specific given semantic rule is actually performing.

SYMBOL TABLE OPERATIONS DURING SYNTAX-DIRECTED TRANSLATION

1. enter(name, type): CREATES a NEW symbol-table entry, invoked when a DECLARATION is being processed. lookup(name): RETRIEVES the NEAREST enclosing declaration for a given name, correctly following the source language's own SCOPING rules (an inner, more tightly nested scope's own declaration of a name SHADOWS an outer scope's declaration of the SAME name).
2. THE SYMBOL TABLE, as implemented in essentially every real compiler front end built to handle nested scoping correctly, is typically organized as EITHER one SEPARATE table PER scope, individually CHAINED to its own immediately enclosing scope's table (so a lookup failing in the innermost table automatically falls back to searching the next-enclosing table, and so on outward), OR as a SINGLE combined table with explicit SCOPE-MARKER entries PUSHED and POPPED as scope boundaries are entered and exited during the parse.
3. SEMANTIC ACTIONS for DECLARATIONS, in essentially every real programming language design encountered in practice, typically run S-ATTRIBUTED style, specifically at the very END of a declaration's own production — so that the FULL, completely-assembled type (which may itself be built up incrementally, LEFT TO RIGHT, as in "int" followed by a "*" pointer-modifier followed by array-dimension brackets) is FULLY known before the actual symbol-table ENTRY is finally created. This is EXACTLY why declaration handling commonly uses an INHERITED "type-so-far" attribute, flowing LEFT TO RIGHT through an entire declarator LIST, exactly mirroring this chapter's own earlier worked L-attributed variable-declaration example.

BACKPATCHING: GENERATING CONTROL-FLOW CODE IN ONE PASS

[[FIG:backpatch-lists]]

1. BACKPATCHING is the STANDARD technique for generating CODE for BOOLEAN expressions and CONTROL-FLOW constructs (if statements, while loops) in ONE SINGLE left-to-right pass, WITHOUT needing to know a jump instruction's own eventual TARGET address IN ADVANCE, at the moment that jump instruction is first generated.
2. THE MECHANISM: an INCOMPLETE jump instruction is generated with its OWN TARGET field deliberately left BLANK, and that specific instruction's own NUMBER is recorded into a SYNTHESIZED list attribute — conventionally named TRUELIST (jumps that should be TAKEN when the associated condition evaluates to TRUE), FALSELIST (jumps taken when FALSE), or NEXTLIST (jumps that should target whatever code comes immediately AFTER the current construct, regardless of any true/false distinction). ONCE the actual target address eventually becomes KNOWN (typically, once the code for whatever comes NEXT has itself already been generated), the function BACKPATCH(list, target) is called, filling in the BLANK target field of EVERY instruction recorded on that list with the now-resolved address, all at once.
3. FOR a production of the classic form E → E1 or M E2 (where M is a MARKER nonterminal, with an ε-production recording the CURRENT instruction count at exactly that point in the parse — M.instr = nextinstr), the standard "OR" pattern is: E.truelist = merge(E1.truelist, E2.truelist) (either sub-expression being true makes the WHOLE "or" true); E.falselist = E2.falselist (ONLY if BOTH sub-expressions are false does the whole "or" become false); and, CRITICALLY, E1's OWN falselist is BACKPATCHED to M.instr — since if E1 evaluates false, execution must FALL THROUGH into evaluating E2 next, exactly the instruction address M has already recorded. The SYMMETRIC "AND" pattern (E → E1 and M E2) swaps the roles of truelist and falselist throughout.

1. Worked example (backpatching for "if (B) S1", traced through with concrete instruction numbers): grammar S → if ( B ) M S1, with M → ε { M.instr = nextinstr } marking the current instruction count at that exact point. Semantic rule: B.truelist is BACKPATCHED to M.instr (the address where S1's own code begins); S.nextlist = merge(B.falselist, S1.nextlist) (BOTH the case where B evaluates false, AND the case where S1's own code simply falls through at its own end, must eventually jump to whatever code follows this ENTIRE if statement). TRACING "if (a < b) x = 1;" concretely: parsing B = "a<b" emits TWO incomplete three-address instructions — instruction 100: "if a<b goto _" (target blank, added to B.truelist), and instruction 101: "goto _" (target blank, added to B.falselist). M records instr=102 (the very next instruction about to be generated, for S1). S1 = "x = 1;" emits instruction 102: "x = 1". BACKPATCH B.truelist={100} to target 102 — instruction 100 is now fully resolved: "if a<b goto 102". S.nextlist = merge({101}, S1.nextlist) = {101} (assuming S1 itself generates no further unresolved jumps of its own) — instruction 101 remains DELIBERATELY unresolved, carried FORWARD as part of S.nextlist, until whatever code follows this entire if statement is eventually generated, at which point IT will finally be backpatched to that following address. This trace shows PRECISELY why backpatching needs only a SINGLE pass: instruction 100's own target (102) becomes known ALMOST IMMEDIATELY, right after parsing "(B)", while instruction 101's target is DEFERRED, correctly, by carrying it forward as an ordinary synthesized LIST attribute rather than requiring any second, separate pass over the code.

GATE TRAP: in backpatching questions, MIXING UP truelist (jumps taken specifically when the associated condition evaluates to TRUE) with falselist (jumps taken specifically when FALSE) is one of the single most common and consequential errors — and, separately, FORGETTING that a marker nonterminal M's own ε-production is EXACTLY what captures "the instruction number AT THIS SPECIFIC POINT in the parse," WITHOUT emitting any actual executable code of its OWN at all, is an equally common oversight; M exists PURELY to record a position, never to generate an instruction.

WORKED PROBLEMS, PART TWO

5. TYPE-CHECKING RULE APPLICATION. Given the coercion rules int+int=int, int+float=float, and NO rule at all for float+string, determine the result of type-checking the expression "3 + 2.5" and separately "x + y" where x:float and y:string. "3 + 2.5" (int + float): matches the int+float=float rule directly, giving RESULT TYPE float, via implicit widening — NO type error. "x + y" (float + string): matches NO defined coercion rule at all — this is FLAGGED as a genuine semantic (type) ERROR, since the language's own type system provides no legal way to combine a float and a string via the + operator as defined.

6. CHECKING VS INFERENCE IDENTIFICATION. A rule states: "for a variable declared as 'auto x = 5;' (with no explicit type given), the compiler determines x's type to be int, based on the type of the initializer expression 5." Is this type CHECKING or type INFERENCE? This is type INFERENCE — no type was EXPLICITLY declared for x at all; the compiler is DERIVING (inferring) a NEW piece of type information from the initializer's own type, rather than verifying an already-declared type against some expected value.

7. SYMBOL-TABLE SCOPING TRACE. A program has an OUTER scope declaring "int x;" and an INNER, nested scope declaring "float x;", with a use of "x" occurring WITHIN that inner scope. Which declaration does lookup(x) resolve to at that specific point, and why? lookup(x) resolves to the INNER declaration ("float x"), since the standard scope-chaining rule specifies that lookup always finds the NEAREST enclosing declaration first — the inner scope's own table is searched BEFORE falling back to the outer, enclosing scope's table, and since the inner scope's own table ALREADY contains an entry for x, that entry SHADOWS the outer scope's own separate declaration entirely for uses occurring within the inner scope.

8. BACKPATCHING FOR A WHILE LOOP. Sketch the backpatching pattern for S → while M1 ( B ) M2 S1 (a classic while-loop grammar, with M1 marking the position to jump BACK to for re-testing the condition, and M2 marking the position where the loop body S1's own code begins). B.truelist is backpatched to M2.instr (entering the loop body once the condition is true). S1.nextlist is backpatched to M1.instr (after the loop body finishes, jump BACK to re-test the condition, forming the loop). S.nextlist = B.falselist (once the condition finally evaluates false, execution falls through to whatever code follows the entire while loop, carried forward exactly as B's own falselist, needing no further backpatching within THIS specific production itself).

THE "AND" PATTERN: THE SYMMETRIC COUNTERPART TO "OR"

1. FOR a production of the form E → E1 and M E2, the roles of truelist and falselist SWAP relative to the "or" pattern developed above: E.falselist = merge(E1.falselist, E2.falselist) (EITHER sub-expression being false makes the WHOLE "and" false); E.truelist = E2.truelist (ONLY if BOTH sub-expressions are true does the whole "and" become true); and E1's OWN truelist is BACKPATCHED to M.instr — since if E1 evaluates TRUE, execution must CONTINUE on to evaluate E2 as well (a true first operand does NOT yet determine the "and" expression's overall truth value, so evaluation must proceed), exactly the instruction address M has already recorded.
2. THIS SYMMETRY between the "or" and "and" patterns — truelist and falselist simply TRADING ROLES, with the SAME underlying marker-nonterminal-and-backpatch mechanism otherwise UNCHANGED — is worth recognising explicitly as a SINGLE unified pattern rather than two entirely separate rules to memorise independently: in BOTH cases, the SECOND operand is evaluated ONLY when the FIRST operand's result does NOT yet determine the overall expression's truth value (a false first operand for "or"; a true first operand for "and"), and in BOTH cases, it is PRECISELY that "does not yet determine the result" branch of the FIRST operand that gets backpatched to jump into evaluating the SECOND operand's own code.

1. Worked example (backpatching for "if (a<b and c<d) x=1;", the "and" pattern traced through with concrete instruction numbers, independently verified via a standalone script): parsing the first condition "a<b" emits instruction 100: "if a<b goto _" (truelist={100}) and instruction 101: "goto _" (falselist={101}). M records instr=102. Parsing the second condition "c<d" emits instruction 102: "if c<d goto _" (this becomes the OVERALL truelist, ={102}) and instruction 103: "goto _" (this becomes part of the OVERALL falselist). BACKPATCH the FIRST condition's own truelist ({100}) to M.instr=102 — instruction 100 becomes "if a<b goto 102" (if a<b is true, proceed to test c<d next, exactly as the "and" pattern requires). The OVERALL falselist = merge({101},{103}) = {101,103} (EITHER condition being false should skip the "x=1" body). M2 (a SECOND marker, recording the instruction count immediately before generating "x=1") records instr=104. S1="x=1" emits instruction 104. Backpatch the overall truelist {102} to 104 — instruction 102 becomes "if c<d goto 104". The overall S.nextlist = merge({101,103}, S1.nextlist) = {101,103} — BOTH of these remain deliberately unresolved, to be backpatched later once whatever code follows the ENTIRE if statement is finally generated.

WHY BACKPATCHING NEEDS ONLY A SYNTHESIZED ATTRIBUTE, NEVER AN INHERITED ONE

1. IT IS worth stating explicitly, carefully, and at some length, precisely WHY the ENTIRE backpatching scheme — despite dealing with FORWARD references (a jump instruction generated NOW, whose target is only known LATER) — can be implemented using PURELY SYNTHESIZED attributes (truelist, falselist, nextlist), with NO inherited attributes needed for the lists THEMSELVES at all: a synthesized LIST attribute doesn't need to KNOW its own eventual target AT THE TIME it is created — it merely needs to CARRY FORWARD, bottom-up, the INSTRUCTION NUMBERS that will EVENTUALLY need patching, MERGING these lists together as larger syntactic constructs are built from smaller ones, until FINALLY, at whatever point the actual target address becomes available, a SINGLE backpatch(list, target) call resolves EVERY instruction on that accumulated list AT ONCE.
2. THE ONLY genuinely INHERITED information backpatching's OWN machinery needs at all, across every single one of the patterns developed throughout this entire chapter, is the INSTRUCTION-COUNT SNAPSHOT a marker nonterminal captures — and even THAT is handled through the ALREADY-familiar marker-nonterminal technique (an ε-production's own SYNTHESIZED attribute, M.instr, capturing nextinstr AT exactly the point that nonterminal is reduced), rather than through any genuinely NEW kind of inherited-attribute machinery beyond what this chapter has already developed in full.

KEY: recognising that backpatching achieves what LOOKS, at first glance, like it OUGHT to require solving a genuine "forward reference" problem (referring to something not yet known) using ONLY synthesized attributes and a clever bottom-up MERGING discipline, rather than requiring any fundamentally new or more powerful attribute-evaluation machinery beyond what this chapter has ALREADY developed for ordinary S-attributed SDDs, is precisely the kind of elegant, engineering-level insight this specific technique is famous for among compiler-construction practitioners — and it is exactly WHY backpatching remains the STANDARD, near-universally-used technique for control-flow code generation in real, practical compilers, rather than some more elaborate two-pass or explicit-relocation-table alternative.

ERROR ATTRIBUTES AND CASCADING ERROR SUPPRESSION

1. A SEMANTIC error (a type mismatch, an undeclared-variable use, an arity mismatch at a function call) is typically recorded by SETTING a designated ERROR attribute on the relevant grammar-symbol instance — commonly, by assigning that symbol's own TYPE attribute a special value conventionally written type_error, rather than any genuine, ordinary type.
2. CASCADING ERROR SUPPRESSION: once an EXPRESSION's own type has already been marked type_error, EVERY semantic rule that subsequently COMBINES that expression with ANYTHING else (via a further arithmetic operation, an assignment, a function-call argument position, and so on) should PROPAGATE type_error FORWARD as well, WITHOUT generating an ADDITIONAL, entirely separate error message for what is really just a DOWNSTREAM consequence of the SAME original error.
3. THIS DESIGN CHOICE — treating type_error as a genuinely CONTAGIOUS value that SILENTLY propagates through subsequent type computations, rather than triggering a FRESH error report at every single point it is subsequently touched — is EXACTLY why a real compiler typically reports ONE clear, targeted error message for a genuine type mistake, rather than an OVERWHELMING CASCADE of dozens of secondary, entirely DERIVATIVE error messages, all ultimately stemming from that SAME single, original mistake.

GATE TRAP: assuming a compiler should report a SEPARATE error message at EVERY point where an already-erroneous expression's own faulty type gets used in some FURTHER computation is a common misconception about how real semantic-error handling actually works — well-designed error attributes are specifically built to PROPAGATE silently (via the type_error contagion discussed above) precisely to AVOID this kind of unhelpful error CASCADE, reporting the underlying, ROOT problem exactly ONCE, rather than burying the genuinely useful diagnostic information under a flood of purely downstream, entirely derivative secondary errors.

WORKED PROBLEMS, PART THREE

9. "AND" VS "OR" BACKPATCH DIRECTION CHECK. For the production E → E1 and M E2, which of E1's own list attributes gets backpatched to M.instr, and why (contrasted directly with the "or" pattern)? E1's TRUELIST gets backpatched to M.instr (NOT falselist, which is the "or" pattern's own choice instead) — because for an "and" expression, if E1 evaluates TRUE, evaluation must CONTINUE on to test E2 as well (a true E1 alone does not yet determine the whole "and" expression's truth value), so the instruction recording "if E1 is true, jump here" needs to target exactly where E2's own code begins, which is precisely what M.instr records.

10. ERROR-ATTRIBUTE CASCADE TRACE. Given the expression "(x + "hello") * 3" where x is declared int and "hello" is a string literal, trace how the type_error attribute propagates, and state how many DISTINCT error messages a well-designed compiler should report. "x + "hello"" (int + string) matches NO defined coercion rule, so this sub-expression's own type is set to type_error, and EXACTLY ONE error message is reported HERE, at this specific point. The OUTER multiplication, "(...) * 3", then combines this ALREADY-type_error sub-expression with the integer 3 — since the left operand's type is ALREADY type_error, this outer combination's own type is ALSO set to type_error, but WITHOUT generating any FURTHER, separate error message (the type_error value propagates silently upward). A well-designed compiler reports exactly ONE error message total for this entire expression, not two.

11. MARKER NONTERMINAL POSITION IDENTIFICATION. In the production S → while M1 ( B ) M2 S1, identify precisely what INFORMATION each of M1 and M2 individually captures, and why TWO separate marker nonterminals are needed rather than just one. M1 (positioned immediately BEFORE the condition B is parsed) captures the instruction number to JUMP BACK to for RE-TESTING the condition on each loop iteration. M2 (positioned immediately AFTER the condition, before the loop body S1) captures the instruction number where the LOOP BODY's own code begins, needed for backpatching B's own truelist. TWO separate markers are needed because these are TWO GENUINELY DIFFERENT instruction-count snapshots, captured at two DIFFERENT points in the parse, each independently required by a DIFFERENT backpatch call later in the same production's own semantic rules.

12. GENERAL SDD VS L-ATTRIBUTED RECOGNITION. An SDD has the rule A → B C { B.i = C.s }, where B.i is inherited and C.s is C's own synthesized attribute. Is this SDD L-attributed, and if not, what evaluation approach is required? This is NOT L-attributed — B appears to the LEFT of C in the production body, so B's inherited attribute depending on C's value (which sits to B's own RIGHT) directly violates the L-attributed restriction. This requires the FULLY GENERAL dependency-graph-plus-topological-sort approach instead — building a dependency graph for each specific parse tree and topologically sorting it, since neither the S-attributed nor the L-attributed single-pass shortcut applies here.

CARRYING THIS FORWARD

This chapter has developed the CONCEPTUAL machinery (S-attributed versus L-attributed SDDs, the general dependency-graph fallback) AND two of its most important CONCRETE applications (type checking, backpatching-based control-flow code generation). The single most valuable connecting insight to carry forward is that both of these concrete applications are simply SPECIFIC INSTANCES of the same general attribute-evaluation framework this chapter opened with. Type checking is an almost entirely S-ATTRIBUTED application (types synthesize bottom-up from sub-expressions to their enclosing expression, fitting naturally with bottom-up LR parsing exactly as this chapter's own S-attributed section predicted), while backpatching's own truelist/falselist/nextlist machinery is a genuinely elegant example of SYNTHESIZED LIST attributes (rather than single values) flowing bottom-up, combined with STRATEGICALLY PLACED marker nonterminals supplying exactly the INHERITED positional information (an instruction number) that would otherwise require an inherited attribute to obtain.

KEY: the marker-nonterminal technique — introducing a fresh, ε-producing nonterminal at exactly the point some piece of "what has happened so far" information (an instruction count, a type accumulated left-to-right, or any other positional or contextual fact) needs to be captured mid-production — is, upon reflection, a SINGLE, GENERAL-PURPOSE trick this chapter has now applied to TWO seemingly different problems (converting an L-attributed, top-down-flavored embedded action into something a bottom-up parser can execute at reduction time; and capturing an instruction-count "snapshot" for backpatching's own control-flow code generation) — recognising this ONE trick's own repeated, general applicability, rather than treating each specific USE of a marker nonterminal as an unrelated, individually-memorised pattern, is exactly the kind of connective habit this book has aimed to build consistently across every subject it covers.

The very next chapter in this Compiler Design sequence, on INTERMEDIATE CODE GENERATION, builds directly on this chapter's own backpatching machinery — extending the same synthesized-list-attribute, marker-nonterminal-based technique developed here specifically for BOOLEAN expressions and simple if/while control structures into a COMPLETE, general-purpose scheme for generating three-ADDRESS CODE (the standard intermediate representation covered in full in that next chapter) for an entire program, including expressions, assignments, and every remaining control-flow construct a real programming language actually needs.

KEY: the S-attributed-versus-L-attributed classification developed at the very start of this chapter and the SLR(1)-versus-LALR(1)-versus-LR(1) power hierarchy developed in the immediately PRECEDING chapter are, upon close inspection, answering STRUCTURALLY analogous questions about two DIFFERENT aspects of the SAME overall compilation pipeline — the parsing chapter's own hierarchy asked "how much LOOKAHEAD CONTEXT does a parser need to correctly decide which production applies," while THIS chapter's own S/L-attributed hierarchy asks "how much DEPENDENCY DIRECTION freedom does an attribute-evaluation scheme need to correctly compute every attribute's value" — both are, at their core, examples of the SAME general engineering question this book has now posed repeatedly across many different specific contexts: given some fixed computational task, what is the MINIMAL, most RESTRICTED mechanism that still suffices to perform it correctly, and what specific ADDITIONAL power must be added once that minimal mechanism provably falls short?

A FULLY WORKED SYNTHESIS: FROM GRAMMAR TO TYPED, BACKPATCHED CODE

To bring every tool this chapter has developed into one connected, end-to-end scenario, consider translating the single statement "if (x > 0 and y > 0) z = x + y;" all the way from its own parse down to fully-resolved three-address code with correctly backpatched jump targets, exactly the kind of complete pipeline a real compiler's own semantic-analysis phase must execute.

1. STEP 1 — TYPE CHECKING THE CONDITION: assuming x, y, z are all declared int, the sub-expressions "x > 0" and "y > 0" both type-check successfully (int compared against int, producing a boolean result via the language's own comparison-operator type rule) — no type_error attribute is triggered anywhere in this specific condition.
2. STEP 2 — APPLYING THE "AND" BACKPATCH PATTERN: using the "and" pattern developed above, "x > 0" emits its own truelist/falselist pair, a marker M records the instruction count immediately before "y > 0" is generated, "x > 0"'s own truelist is backpatched to M.instr, and the combined condition's overall truelist/falselist are assembled exactly as this chapter's own worked "and" example demonstrated in full, concrete instruction-number detail above.
3. STEP 3 — APPLYING THE "IF" BACKPATCH PATTERN: a second marker M2 records the instruction count immediately before the assignment "z = x + y" is generated; the combined condition's own overall truelist is backpatched to M2.instr (entering the assignment only once BOTH comparisons succeed); the statement's own overall S.nextlist is set to the combined condition's own falselist (skipping the assignment entirely, and falling through to whatever follows, the moment EITHER comparison fails).
4. STEP 4 — TYPE-CHECKING AND GENERATING THE ASSIGNMENT: "x + y" type-checks as int+int=int (matching one of this chapter's own earlier-introduced coercion rules directly), and the assignment "z = x + y" itself type-checks successfully as well, since z's own declared type (int) matches the right-hand side's synthesized type exactly, with no coercion or error needed at all; a genuine three-address instruction "z = x + y" is emitted at exactly the instruction position M2 had already recorded.
5. STEP 5 — FINAL RESULT: a fully type-checked, fully backpatched sequence of three-address instructions, with EVERY jump target correctly resolved through the SAME single left-to-right pass this chapter's own backpatching machinery has been built, from the very start of this chapter, to guarantee — no separate, second pass over the generated code is EVER required, precisely because every forward reference was tracked, throughout, as an ordinary SYNTHESIZED list attribute, exactly as this chapter's own dedicated section on "why backpatching needs only a synthesized attribute" explained in full above.

This synthesis demonstrates directly how TYPE CHECKING and BACKPATCHING-BASED CODE GENERATION, though developed as two seemingly separate applications earlier in this chapter, operate simultaneously and COOPERATIVELY within a single real compilation pass — type checking verifies every individual operation is semantically LEGAL, while backpatching concurrently assembles the actual EXECUTABLE code implementing those same operations, with both processes drawing on the EXACT SAME underlying attribute-evaluation framework (synthesized attributes flowing bottom-up, marker nonterminals supplying positional snapshots where needed) this chapter developed, piece by piece, from its very first section onward.

KEY: it is worth stating, as a final unifying observation before this chapter's own close, that EVERY worked example and EVERY worked problem in this entire chapter — the variable-declaration L-attributed example, the "or" and "and" backpatching patterns, the type-checking coercion rules, the error-cascade-suppression discussion, and this final combined synthesis — are all, without exception, applications of the SAME three foundational ideas introduced in this chapter's own opening sections: the synthesized-versus-inherited attribute distinction, the S-attributed-versus-L-attributed classification determining what evaluation order suffices, and the marker-nonterminal technique for capturing positional "snapshot" information mid-production. A student who has genuinely internalized these THREE foundational ideas, rather than memorising this chapter's many individual worked examples as separate, disconnected facts, is fully equipped to construct a correct SDD or SDT for an ENTIRELY unfamiliar language construct never explicitly covered anywhere in this chapter's own text, simply by applying these same three ideas directly and methodically to that new, unfamiliar situation.

A closing note on how this chapter's material typically appears on GATE, worth stating explicitly for anyone using this book specifically for exam preparation. GATE questions on syntax-directed translation tend to fall into three recurring formats, each drawing on a different one of this chapter's own major sections. The FIRST, and most common, format presents a short SDD with a handful of rules and asks whether it is S-attributed, L-attributed, or neither — this requires nothing beyond the careful, symbol-by-symbol dependency check this chapter's own worked examples (and the deliberately tricky WP2 and WP12 above) have demonstrated repeatedly. The second format presents a small backpatching scenario — usually an if, while, or compound "and"/"or" boolean expression — and asks either for the resulting instruction sequence, or for which specific list attribute (truelist, falselist, or nextlist) a particular jump instruction ends up on; this requires exactly the disciplined instruction-by-instruction tracing this chapter's own worked backpatching examples modeled in full concrete detail, complete with actual instruction numbers rather than only symbolic reasoning. The third, somewhat less common format tests type-checking rules directly, typically asking whether a specific expression type-checks successfully under a given, explicitly stated set of coercion rules, or asking how many distinct error messages a well-designed compiler should report for a given expression containing one or more genuine type errors — exactly the kind of question this chapter's own error-cascade-suppression discussion, and its accompanying worked problem, were built to prepare a reader for directly.

Recognising which of these three recurring formats a specific unfamiliar question belongs to is, in practice, at least as valuable as knowing this chapter's own individual rules and definitions by heart. A student confronted with an SDD they have never seen before should immediately reach for the S-attributed/L-attributed dependency check; a student confronted with an unfamiliar control-flow construct's own backpatching scheme should immediately reach for the marker-nonterminal-plus-list-attribute tracing discipline; and a student confronted with an unfamiliar coercion-rule table should immediately reach for the type-synthesis-plus-cascade-suppression framework. In every one of these three cases, the specific unfamiliar details of a new question are handled by the same general-purpose tool this chapter has already built and thoroughly exercised, rather than requiring any genuinely new piece of theory to be learned on the spot, under exam-time pressure, from scratch. Practising this three-way sorting habit deliberately, on a handful of deliberately unfamiliar examples, is what makes it fast enough to apply confidently within a real exam's time budget.

KEY: it is worth stating one final, genuinely important practical distinction before closing this chapter entirely — an SDD's own S-attributed or L-attributed CLASSIFICATION is a property of the DEFINITION itself, checkable directly from the grammar and its rules alone, with no need to actually run a parse at all; but ACTUALLY EVALUATING that SDD (whether by hand, when working through a GATE numerical, or by an actual compiler implementation) still requires carrying out the specific TRAVERSAL order that classification guarantees is sufficient — a postorder traversal for S-attributed, a single left-to-right depth-first traversal for L-attributed, or the fully general dependency-graph topological sort otherwise. A common exam-time shortcut mistake is correctly identifying an SDD's classification but then failing to actually CARRY OUT the corresponding evaluation carefully, step by step, in the correct order — exactly the kind of careful, disciplined tracing this chapter's own worked examples (the variable-declaration L-attributed trace, and both backpatching traces) have modeled in full, deliberately verbose detail throughout, precisely so that this exact evaluation discipline becomes second nature rather than a step that gets rushed or skipped under time pressure.

This chapter, taken as a whole, sits at a genuinely pivotal position within this book's own Compiler Design sequence — it is the FIRST chapter in this sequence whose subject matter is not itself directly borrowed, in essentially unmodified form, from this book's own earlier Theory of Computation sequence (unlike lexical analysis's direct reuse of finite-automaton theory, and syntax analysis's direct reuse of context-free-grammar and pushdown-automaton theory) but is instead a GENUINELY NEW body of technique, specific to the practical engineering problem of attaching semantic meaning to an already-recognized syntactic structure. This does not mean the connective, "reduce a new problem to already-familiar tools" habit this book has modeled consistently throughout suddenly stops applying here — quite the opposite: this chapter's own S-attributed and L-attributed evaluation-order guarantees are themselves directly analogous to the postorder and preorder tree-traversal disciplines any reader will already be familiar with from ordinary data-structures coursework, and the marker-nonterminal technique is, at its structural core, simply an application of the same "introduce an auxiliary symbol to capture information that would otherwise require a more complicated mechanism" engineering trick this book has now applied, in one form or another, across an enormous range of otherwise quite different subjects. Even where this chapter's specific techniques are genuinely new material, the underlying HABITS OF MIND this book has built up across every chapter preceding it continue to apply directly, and continue to pay off, exactly as they have throughout this entire book.

Looking ahead just briefly to what remains of this Compiler Design sequence: the intermediate code generation chapter immediately following this one will show how three-address code, already glimpsed in this chapter's own backpatching examples, serves as the standard machine-independent target every front-end phase eventually produces; the runtime environments chapter after that will explain how the activation records and stack-frame layouts a running program actually needs are themselves derived directly from exactly the same symbol-table and scoping discipline this chapter has already developed; and the final code optimization chapter will show how three-address code, once generated, can be systematically improved before ever reaching an actual target machine. Each of these remaining chapters, in its own way, continues building on the same attribute-driven, syntax-directed foundation this chapter has now laid in full.

One final practical observation, worth carrying forward specifically as this book moves from theory-heavy chapters toward the more implementation-flavored remainder of this Compiler Design sequence: the tools this chapter has developed are not merely abstract exam material but are, quite literally, what a real compiler's own front end actually does, line by line, as it processes a source file — every widely-used production compiler, from small hobby-language interpreters through to industrial-strength systems like GCC or LLVM's own Clang front end, implements some concrete variant of exactly the synthesized-and-inherited attribute machinery, the symbol-table scoping discipline, and the backpatching-based control-flow code generation this chapter has developed here from first principles. Recognising this direct, unbroken line from a GATE-style textbook exercise all the way through to genuinely industrial-strength compiler engineering is worth holding onto, not merely as a motivating aside, but as a concrete reminder that the abstractions this book builds are never purely academic constructs invented solely for examination purposes — they are, in every single case covered so far, the actual working machinery underlying real, widely-deployed software that runs on essentially every computer in daily use around the world today.
`
};
