// Textbook chapter: Number Systems and Codes.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['digital-number-systems'] = {
  figs: [
    {
      id: 'twos-complement-wheel',
      caption: 'The 3-bit 2’s-complement number wheel: values wrap from 011 (+3) to 100 (−4) at the top, giving one extra negative value and exactly one representation of zero.',
      svg: '<svg viewBox="0 0 260 260" width="100%" style="max-width:260px;height:auto" xmlns="http://www.w3.org/2000/svg"><circle cx="130" cy="130" r="95" stroke="currentColor" stroke-width="1.2" fill="none"/><g font-size="11" fill="currentColor" text-anchor="middle" font-family="monospace"><text x="130" y="42">000 (0)</text><text x="196" y="60">001 (1)</text><text x="222" y="130">010 (2)</text><text x="196" y="200">011 (3)</text><text x="130" y="222">100 (-4)</text><text x="64" y="200">101 (-3)</text><text x="38" y="130">110 (-2)</text><text x="64" y="60">111 (-1)</text></g></svg>'
    },
    {
      id: 'gray-binary-chain',
      caption: 'Gray-to-binary conversion is a chain: each binary bit depends on the previous COMPUTED binary bit XORed with the current Gray bit, scanning from the MSB (copied directly) down to the LSB.',
      svg: '<svg viewBox="0 0 300 110" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none" marker-end="url(#gbarrow)"><line x1="50" y1="55" x2="110" y2="55"/><line x1="150" y1="55" x2="210" y2="55"/></g><defs><marker id="gbarrow" markerWidth="7" markerHeight="7" refX="5" refY="3.5" orient="auto"><path d="M0,0 L7,3.5 L0,7 Z" fill="currentColor"/></marker></defs><g font-size="11" fill="currentColor" text-anchor="middle" font-family="monospace"><text x="30" y="30">g3</text><text x="30" y="80">b3=g3</text><text x="130" y="30">g2</text><text x="130" y="80">b2=b3^g2</text><text x="250" y="30">g1</text><text x="250" y="80">b1=b2^g1</text></g></svg>'
    }
  ],
  text: `
WHY NUMBER SYSTEMS, AND WHAT ACTUALLY VARIES BETWEEN THEM

Every number system covered in this chapter represents the SAME abstract quantities — the integers, and fractional values built from them — using a different BASE (radix) or a different ENCODING SCHEME for handling the sign. The mechanical conversion procedures (dividing by a radix, multiplying a fraction repeatedly) are worth memorising precisely, but the deeper, more frequently tested content is WHY each scheme behaves the way it does: why 2's complement has one extra negative value, why BCD addition needs a +6 correction, why Gray code is a chained rather than independent bit-by-bit conversion. Every one of these "why" questions has a short, derivable answer, and understanding the derivation is what prevents the mechanical steps from being misremembered under pressure.

POSITIONAL NUMBER SYSTEMS AND THE RADIX CONCEPT

A RADIX-r (or BASE-r) positional system represents a number as Σᵢ dᵢ·rⁱ, where each digit dᵢ lies in the range [0, r−1], and the digit's POSITION i determines the POWER of r it is multiplied by (positions counting outward from the "units" position, i=0, with positive i to the left for the integer part and negative i to the right for the fractional part). The radices most relevant here are binary (r=2, digits 0-1), octal (r=8, digits 0-7), decimal (r=10, digits 0-9), and hexadecimal (r=16, digits 0-9 then A-F representing 10-15).

KEY: any two radices that are BOTH POWERS OF A COMMON BASE (2, 8=2³, 16=2⁴ are all powers of 2) convert to and from each other by SIMPLE DIGIT GROUPING — grouping binary digits in sets of 3 converts to/from octal, and grouping in sets of 4 converts to/from hexadecimal, with NO arithmetic required at all beyond reading off the fixed-width binary pattern for each group. This is precisely WHY octal and hexadecimal conversions are always performed VIA binary as an intermediate step, never digit-by-digit directly against each other or against decimal — the power-of-2 relationship that makes grouping work simply does not exist between, say, base-7 and base-9.

INTEGER CONVERSIONS: THE DIVISION AND GROUPING METHODS

DECIMAL TO BINARY (or any target radix r): repeatedly DIVIDE by r, recording the REMAINDER at each step, and read the final digit sequence by taking the remainders from the LAST one computed back to the FIRST (bottom-to-top).

1. Worked trace: convert decimal 25 to binary. 25÷2=12 remainder 1. 12÷2=6 remainder 0. 6÷2=3 remainder 0. 3÷2=1 remainder 1. 1÷2=0 remainder 1 (division stops once the quotient reaches 0). Reading the remainders from LAST to FIRST: 1,1,0,0,1 → 11001.
2. WHY the remainders are read bottom-to-top: the FIRST remainder computed (dividing the original number by r) is the number's value MOD r — exactly its LEAST significant digit (the r⁰ position). Each SUBSEQUENT division effectively shifts attention to the next-higher digit position, so the LAST remainder computed corresponds to the MOST significant digit — reading bottom-to-top simply lists the digits from most-significant to least-significant, the natural left-to-right reading order for a number.

BINARY TO DECIMAL: sum bᵢ·2ⁱ over every SET bit (bit equal to 1). Worked check: 11001 = 1·2⁴+1·2³+0·2²+0·2¹+1·2⁰ = 16+8+0+0+1 = 25 — matching the conversion above exactly, confirming the round trip.

BINARY TO OCTAL: group bits in sets of 3, counting outward from the binary point (padding with leading zeros on the integer side, or trailing zeros on the fractional side, as needed to complete a full group), then convert each 3-bit group directly (000-111 map to 0-7).

BINARY TO HEXADECIMAL: group bits in sets of 4, counting outward from the binary point the same way, converting each 4-bit group directly (0000-1111 map to 0-F).

OCTAL/HEX TO BINARY: the reverse operation — expand EACH digit independently into its fixed-width binary group (3 bits per octal digit, 4 bits per hex digit) and concatenate the groups together in order, with no carrying or borrowing between digits at all (each digit converts entirely independently of its neighbours, which is exactly the shortcut that direct digit-by-digit decimal conversion could never offer).

FRACTIONAL CONVERSIONS: THE MULTIPLICATION METHOD

DECIMAL FRACTION TO BINARY: repeatedly MULTIPLY the fractional part by 2, recording the INTEGER part (0 or 1) produced at each step, and read the digits TOP-to-bottom (the OPPOSITE reading order from the integer-conversion procedure above).

GATE TRAP: the single most common conversion error in this entire chapter is READING THE DIGITS IN THE WRONG ORDER — the integer procedure reads its remainders bottom-to-top, while the fractional procedure reads its integer parts top-to-bottom; swapping these two reading conventions (perhaps out of habit from just having done the opposite procedure moments before) silently produces a completely wrong, though plausible-looking, final digit string.

1. WHY the fractional procedure reads top-to-bottom, in direct contrast to the integer procedure: multiplying the fractional part by 2 and taking the integer part produced is exactly extracting the value at the NEXT position OUT from the binary point (position 2⁻¹ on the first multiplication, then 2⁻² on the second, and so on) — the FIRST multiplication directly produces the MOST significant fractional digit (closest to the binary point), so reading top-to-bottom is already the correct most-to-least-significant order, unlike the integer procedure where the first remainder is the LEAST significant digit.

Worked trace: convert 0.625 to binary. 0.625×2=1.25 → take 1, keep the fractional remainder 0.25. 0.25×2=0.5 → take 0, keep 0.5. 0.5×2=1.0 → take 1, remaining fractional part is now exactly 0, so the process terminates. Reading top-to-bottom: 1,0,1 → 0.101.

KEY: this multiplication process does NOT always terminate — many ordinary decimal fractions produce an INFINITELY REPEATING binary expansion (0.1 in decimal, for instance, becomes the infinitely repeating binary 0.0(0011), the pattern 0011 repeating forever) even though 0.1 looks perfectly simple and exact in decimal. This is EXACTLY why IEEE 754 floating-point representations cannot store many "simple-looking" decimal fractions exactly — a finite number of stored bits can only ever capture a TRUNCATED version of an infinitely repeating binary expansion, introducing a small but genuine representation error baked in from the very first conversion step, entirely independent of any later arithmetic performed on the value.

BINARY FRACTION TO DECIMAL: sum bᵢ·2⁻ⁱ for each fractional bit position i=1,2,3,... (counting outward from the binary point). This is the direct mirror of the integer-side binary-to-decimal sum, just using negative exponents for positions past the point.

CONVERTING VIA AN INTERMEDIATE RADIX

To convert directly between two radices that are NOT both powers of a common base (for instance, base-7 to base-9), there is NO shortcut digit-grouping method available — the fixed-width-group trick relies entirely on the power-of-2 relationship among binary, octal, and hexadecimal, and it simply does not exist for an arbitrary pair of radices.

GATE TRAP: attempting to convert digit-by-digit directly between two non-power-related radices (treating each source digit independently, the way octal-to-hex grouping works) produces a WRONG answer, since there is no consistent fixed-width correspondence between digits of unrelated bases — the CORRECT and only general procedure is to convert through an intermediate radix (typically decimal, or binary if one of the two radices happens to relate to it), fully evaluating the number's actual VALUE first, and then converting that value into the target radix from scratch using the standard division (or multiplication, for fractions) procedure.

Worked trace — a full conversion chain exercising every rule above: convert decimal 173.625 to binary, then to octal, then to hexadecimal.

1. Integer part 173: 173÷2=86 r1, 86÷2=43 r0, 43÷2=21 r1, 21÷2=10 r1, 10÷2=5 r0, 5÷2=2 r1, 2÷2=1 r0, 1÷2=0 r1. Reading bottom-to-top: 10101101.
2. Fractional part 0.625: 0.625×2=1.25 (take 1), 0.25×2=0.5 (take 0), 0.5×2=1.0 (take 1, remainder 0, stop). Reading top-to-bottom: .101.
3. Combined binary: 10101101.101.
4. Converting to octal by grouping in 3s from the binary point outward: integer side 10 101 101, padded on the left to a multiple of 3 bits → 010 101 101 → digit groups 2, 5, 5. Fractional side 101 is already exactly 3 bits → digit 5. Result: 255.5 in octal.
5. Converting to hexadecimal by grouping in 4s: integer side 1010 1101 → digit groups A, D. Fractional side .101 padded with a trailing zero to make a full 4-bit group → 1010 → digit A. Result: AD.A in hexadecimal.
6. Sanity check, converting BOTH results back to decimal independently: octal 255.5 = 2(64)+5(8)+5(1)+5(1/8) = 128+40+5+0.625 = 173.625 — matches. Hexadecimal AD.A = 10(16)+13(1)+10(1/16) = 160+13+0.625 = 173.625 — also matches, confirming every step of the conversion chain.

SIGNED NUMBER REPRESENTATIONS FOR AN n-BIT WORD

SIGN-MAGNITUDE: the MOST SIGNIFICANT BIT is a dedicated SIGN bit (0=positive, 1=negative), and the remaining (n−1) bits store the MAGNITUDE directly, exactly as an unsigned number would. Range: −(2ⁿ⁻¹−1) to +(2ⁿ⁻¹−1). Has TWO distinct representations of zero (+0 = 00...0, and −0 = 10...0, differing only in the sign bit despite representing the identical numeric value).

1'S COMPLEMENT: a negative number is formed by INVERTING every bit of the corresponding positive value. Range: identical to sign-magnitude, −(2ⁿ⁻¹−1) to +(2ⁿ⁻¹−1). ALSO has two representations of zero (00...0 and 11...1). Addition in 1's complement requires a special correction called an END-AROUND CARRY: any carry generated OUT of the most significant bit position during addition must be added BACK IN to the least significant bit position for the result to be correct.

2'S COMPLEMENT: a negative number is formed by inverting every bit of the positive value and then ADDING 1 (equivalently, computing 2ⁿ minus the magnitude directly). Range: −(2ⁿ⁻¹) to +(2ⁿ⁻¹−1) — ONE EXTRA negative value compared to both other schemes, and exactly ONE representation of zero.

[[FIG:twos-complement-wheel]]

1. WHY 2's complement has exactly one more negative value than positive: the n-bit pattern space has exactly 2ⁿ distinct bit patterns total. With exactly ONE pattern (all-zero) reserved for zero, the remaining 2ⁿ−1 patterns must split between positive and negative values — but this split is NOT symmetric, because the single MOST NEGATIVE pattern (10...0, the sign bit alone set) has NO positive counterpart under the invert-then-add-1 negation rule: negating 10...0 (invert to give 01...1, then add 1) produces 10...0 again — this specific pattern is its OWN 2's-complement negation, meaning it cannot pair up with any distinct positive value the way every other magnitude does, and it is conventionally assigned to represent the single most negative value −2ⁿ⁻¹, with no positive value +2ⁿ⁻¹ available to balance it (since +2ⁿ⁻¹ itself would require an (n+1)-bit pattern to represent, exceeding the available n bits).
2. WHY 2's complement is universally used in real hardware, in preference to the other two schemes: 2's-complement ADDITION AND SUBTRACTION of signed numbers use the EXACT SAME binary adder circuitry as ordinary UNSIGNED addition — no end-around-carry correction (needed for 1's complement) and no separate sign/magnitude comparison logic (needed for sign-magnitude) is required at all; the hardware simply adds the bit patterns as if they were unsigned, and the 2's-complement encoding is specifically constructed so that this naive addition produces the CORRECT signed result automatically.

COMPARISON TABLE — n-BIT SIGNED RANGES (n=8 shown as a concrete worked example): sign-magnitude gives −127 to +127 with two zeros (+0 and −0), requiring extra sign/magnitude-aware logic for arithmetic. 1's complement gives the SAME −127 to +127 range with the SAME two zeros, but requires an end-around carry correction on addition. 2's complement gives −128 to +127 with exactly ONE zero, and needs NO extra correction step at all — plain binary addition already works correctly. In general n-bit terms: sign-magnitude and 1's complement both give the range −(2ⁿ⁻¹−1) to +(2ⁿ⁻¹−1); 2's complement gives −(2ⁿ⁻¹) to +(2ⁿ⁻¹−1).

GATE TRAP: assuming 1's complement and 2's complement share the exact same RANGE (since their positive-side maximum, +2ⁿ⁻¹−1, does coincide) is a common error — 2's complement has exactly ONE MORE representable negative value (−2ⁿ⁻¹, with no counterpart in the other two schemes), a direct and frequently tested consequence of the single-zero asymmetry derived above.

OVERFLOW DETECTION IN 2'S COMPLEMENT ADDITION

OVERFLOW occurs in 2's-complement addition if and only if the CARRY INTO the sign bit position DIFFERS from the CARRY OUT of the sign bit position — equivalently, and often faster to check by inspection: overflow occurs exactly when adding two POSITIVE operands yields a result that LOOKS negative, or when adding two NEGATIVE operands yields a result that looks positive.

GATE TRAP: overflow can NEVER occur when adding operands of DIFFERENT signs in 2's complement — adding a positive number to a negative number always produces a result whose magnitude is strictly SMALLER than at least one of the two original operands' magnitudes, which can never exceed the representable range in either direction; misapplying the overflow rule to a mixed-sign addition (incorrectly flagging overflow, or checking for it at all, when the two operands' signs already differ) is a wasted step that the "different signs" observation should immediately rule out.

Worked trace: represent −45 in 8-bit sign-magnitude, 1's complement, and 2's complement, then check whether adding +100 and +45 (in 8-bit 2's complement) overflows.

1. +45 in binary: 00101101. Sign-magnitude −45: flip ONLY the sign bit → 10101101. 1's complement −45: invert EVERY bit of 00101101 → 11010010. 2's complement −45: invert then add 1 → 11010010+1 = 11010011.
2. Overflow check: +100 = 01100100 (fits within 8-bit signed range, since 100≤127). +45 = 00101101. Adding: 01100100+00101101 = 10010001 in binary.
3. Interpreting the 8-bit result 10010001 as signed 2's complement: the sign bit is 1, meaning hardware reports a NEGATIVE result — decoding it (invert then add 1: 01101110+1=01101111=111 decimal) gives −111. But the TRUE mathematical sum of +100 and +45 is +145, a POSITIVE number nowhere near −111.
4. Since two POSITIVE operands produced a result that LOOKS negative, this is a genuine OVERFLOW — 145 exceeds the maximum representable value +127 in signed 8-bit 2's complement, confirmed directly by the "positive+positive=negative-looking result implies overflow" rule.

BCD, EXCESS-3, AND GRAY CODE

BCD (BINARY CODED DECIMAL): each DECIMAL digit (0-9) is encoded independently in its OWN 4-bit binary group (0000 through 1001); the six bit patterns 1010 through 1111 are INVALID and never used within a single BCD digit, even though a 4-bit group could otherwise represent up to 15.

1. BCD ADDITION requires a CORRECTION STEP: after adding two 4-bit BCD groups using ordinary binary addition, if the result EXCEEDS 9 (falls into the six invalid patterns) OR produces a carry out of that group, ADD 6 (binary 0110) to that group's result — this correction "skips over" the six invalid codes, forcing the result back into valid BCD range and correctly propagating any needed carry into the next decimal digit's group.
2. WHY adding exactly 6 fixes this: a 4-bit group has 16 total possible patterns, but BCD uses only 10 of them (0-9), leaving 6 unused. When a binary addition naturally "overflows" past 9 within the 4-bit group, it has effectively counted through those 6 unused patterns before reaching the next valid decimal boundary — adding 6 compensates for exactly this gap, realigning the result with the correct decimal digit (and correctly generating a carry into the next group, exactly as ordinary decimal carrying would).

EXCESS-3 CODE: encodes each decimal digit as (digit value + 3), represented in 4 bits (digit 0 → 0011, digit 9 → 1100). Excess-3 is SELF-COMPLEMENTING: the excess-3 code of (9 − digit) is EXACTLY the bitwise complement of the excess-3 code of digit — a property directly useful for performing BCD subtraction via complement arithmetic (the same "complement-and-add" trick used for 2's-complement subtraction above, adapted to work directly on decimal digits encoded in excess-3).

1. Worked verification of self-complementation: digit 3 in excess-3 is 3+3=6 → 0110. Digit (9−3)=6 in excess-3 is 6+3=9 → 1001. Checking: the bitwise complement of 0110 is 1001 — exactly matching the excess-3 code of 6, confirming the self-complementing property directly.

GRAY CODE: a UNIT-DISTANCE code in which any two SUCCESSIVE values (including the wrap-around from the highest value back to the lowest) differ in EXACTLY ONE bit — the exact property the K-map adjacency structure in the Boolean algebra chapter relies on, and also used in mechanical/optical position encoders to avoid transient multi-bit "glitch" readings during a transition (since only one bit ever changes at a time, there is no window in which the reading could be misread as an entirely different, far-away value due to bits changing at slightly different physical moments).

[[FIG:gray-binary-chain]]

BINARY-TO-GRAY CONVERSION: g(i) = b(i) XOR b(i+1) for each bit position i, with the MOST significant bit copied UNCHANGED (g(MSB)=b(MSB)) — scanning from the MSB toward the LSB, each Gray bit is the XOR of the CURRENT binary bit with the NEXT-MORE-SIGNIFICANT binary bit.

GRAY-TO-BINARY CONVERSION: b(MSB)=g(MSB) (copied unchanged, exactly mirroring the forward direction), then working DOWN from the MSB toward the LSB, b(i) = b(i+1) XOR g(i) — each BINARY bit depends on the PREVIOUSLY COMPUTED binary bit (not simply the previous Gray bit alone), making this a genuinely CHAINED computation rather than an independent bit-by-bit lookup.

GATE TRAP: converting Gray-to-binary using ONLY the current Gray bit (treating each position as an independent lookup, the way binary-to-octal grouping works) is a common and serious error — the Gray-to-binary direction genuinely REQUIRES the previously computed BINARY bit as an input to each subsequent step, since each binary bit is defined recursively in terms of the one before it, and skipping this chaining (perhaps by analogy with the simpler-looking forward direction) silently produces a wrong result from the second bit onward.

Worked trace: convert binary 1011 to Gray code, then back to binary to confirm the round trip. Binary bits from MSB to LSB: b3=1, b2=0, b1=1, b0=1. Forward: g3(MSB)=b3=1. g2=b3 XOR b2=1 XOR 0=1. g1=b2 XOR b1=0 XOR 1=1. g0(LSB)=b1 XOR b0=1 XOR 1=0. Gray code (MSB to LSB): 1110. Reverse: recovered-b3=g3=1. b2=b3 XOR g2=1 XOR 1=0. b1=b2 XOR g1=0 XOR 1=1. b0=b1 XOR g0=1 XOR 0=1. Recovered binary: 1011 — matching the original exactly, confirming the round trip.

GENERALISED COMPLEMENT ARITHMETIC: r's COMPLEMENT AND (r−1)'s COMPLEMENT

The 1's-complement and 2's-complement schemes above are actually SPECIAL CASES (for radix r=2) of a MORE GENERAL pair of complement definitions that apply to ANY radix r, and recognising this generalisation explains directly why the same "invert, or invert-then-add-1" pattern recurs so cleanly.

1. THE (r−1)'s COMPLEMENT of an n-digit number N in radix r is defined as (rⁿ−1)−N — subtracting N from the LARGEST possible n-digit value in that radix. For r=2, rⁿ−1 is the all-1s bit pattern, and (rⁿ−1)−N is EXACTLY equivalent to inverting every bit of N (since subtracting each bit from 1 flips it) — this is precisely 1's complement, now understood as the r=2 instance of a general rule.
2. THE r's COMPLEMENT of N is defined as rⁿ−N (equivalently, the (r−1)'s complement PLUS 1). For r=2, this is exactly 2's complement — invert-then-add-1, now understood as the r=2 instance of the SAME general rule applied one step further.
3. This generalisation is directly useful for radices OTHER than binary: the 9's complement (r−1=9, for r=10) of a decimal number, and the 10's complement (r=10), work exactly analogously to 1's and 2's complement, and are the basis of decimal subtraction-by-addition on some historical calculating machines — recognising the pattern makes it straightforward to derive the correct complement for ANY radix on demand, rather than needing radix-specific rules memorised independently for binary, decimal, octal, or hexadecimal.

Worked trace: find the 9's complement and 10's complement of the 3-digit decimal number 274. 9's complement: (10³−1)−274 = 999−274 = 725 (equivalently, subtract each digit from 9: 9−2=7, 9−7=2, 9−4=5, giving 725 directly, digit by digit, mirroring how 1's complement inverts each bit independently). 10's complement: 725+1 = 726 (equivalently, 1000−274=726 directly).

SUBTRACTION VIA COMPLEMENT ADDITION

The entire POINT of defining complements is that SUBTRACTION can be performed using ONLY an ADDER circuit (no separate subtractor hardware needed) — computing A−B is done by adding A to the r's complement of B (discarding any final carry-out beyond the word's own width), exactly the mechanism that makes 2's-complement addition/subtraction share identical hardware, generalised here to any radix.

1. Worked trace using 10's complement, decimal subtraction 725−274 performed entirely via addition: 10's complement of 274 (using n=3 digits) is 1000−274=726. Adding: 725+726 = 1451. Since this is a 3-digit-word computation, DISCARD the leading carry-out digit (the leftmost "1" beyond the 3-digit width), leaving 451 — matching the direct subtraction 725−274=451 exactly.
2. This "discard the carry-out beyond the word width" step is exactly what the general r's-complement subtraction procedure requires, and it is the DIRECT decimal analogue of a 2's-complement binary subtraction simply letting any carry out of the most significant bit fall away unused — no end-around correction is needed for r's complement (in contrast to the (r−1)'s complement scheme, which DOES require an end-around carry, exactly mirroring 1's complement's own need for one).

IEEE 754 FLOATING-POINT REPRESENTATION

Representing a very wide range of magnitudes (extremely large AND extremely small numbers) with a FIXED number of bits requires giving up the fixed-position "binary point" of the schemes above in favour of a FLOATING one — the IEEE 754 standard represents a number as (−1)^S × 1.M × 2^(E−bias), splitting the available bits into a SIGN bit S, an EXPONENT field E (stored with a fixed BIAS added, so it can represent both positive and negative exponents using only unsigned bit patterns), and a MANTISSA (or SIGNIFICAND) field M (storing only the FRACTIONAL part of the significand, since the leading "1." before the binary point is IMPLICIT and never stored explicitly for a normalised number).

1. SINGLE PRECISION (32-bit): 1 sign bit, 8 exponent bits (bias 127), 23 mantissa bits. DOUBLE PRECISION (64-bit): 1 sign bit, 11 exponent bits (bias 1023), 52 mantissa bits.
2. THE IMPLICIT LEADING 1: since a NORMALISED binary number always has the form 1.xxxxx × 2^k (the leading digit before the point is, for any nonzero number, ALWAYS exactly 1 once properly normalised — there is no other nonzero binary digit it could be), storing this fixed leading 1 explicitly would waste one bit of precision on every single value; IEEE 754 exploits this by leaving it IMPLICIT, storing only the mantissa bits AFTER the point, and effectively gaining one extra bit of real precision for free.
3. THE BIAS lets the exponent field, which is stored as an ordinary UNSIGNED binary number, represent NEGATIVE true exponents as well: the TRUE exponent is (stored E value) − bias, so a stored E of 0 represents the most negative possible true exponent, and a stored E at its maximum represents the most positive — this avoids needing a separate sign bit just for the exponent itself.

Worked trace: represent decimal 173.625 (already converted to binary as 10101101.101 in the earlier worked trace) in IEEE 754 single precision. Normalise: 10101101.101 = 1.0101101101 × 2⁷ (shifting the binary point left by 7 places to leave exactly one leading 1 before it). Sign bit: 0 (positive). True exponent: 7, so stored E = 7+127 = 134 = 10000110 in 8-bit binary. Mantissa: the bits after the leading "1." are 0101101101, padded with trailing zeros to fill all 23 mantissa bits. Final 32-bit pattern: 0 (sign) 10000110 (exponent) 01011011010000000000000 (mantissa, padded).

GATE TRAP: forgetting the IMPLICIT leading 1 — either by storing it explicitly (wasting a mantissa bit and shifting every subsequent bit one position out of place) or by forgetting to ADD it back in when reconstructing a value FROM its IEEE 754 bit pattern — is one of the most common errors when working with this representation; the leading 1 is never present in the stored bits, but it is ALWAYS present in the actual represented value for any normalised (nonzero, non-special) number.

KEY: IEEE 754's design choices are not arbitrary conventions — each one solves a specific representational problem directly: the implicit leading 1 recovers one "free" bit of precision by exploiting the fact that a normalised binary significand's leading digit is always exactly 1; the bias lets an unsigned exponent field represent negative true exponents without a dedicated exponent sign bit; and the earlier observation that many decimal fractions (like 0.1) never terminate in binary is exactly why floating-point arithmetic on "ordinary-looking" decimal values so often produces small, visible representation errors — the finite mantissa field can only ever store a TRUNCATED prefix of what may be an infinitely repeating true binary expansion.

SPECIAL IEEE 754 VALUES: ZERO, INFINITY, AND NAN

The exponent field's two EXTREME values (all-zero, and all-one) are RESERVED for special cases outside the ordinary normalised-number encoding described above, rather than representing ordinary numbers via the usual implicit-leading-1 formula.

1. ALL-ZERO EXPONENT with an all-zero mantissa represents ZERO itself (with the sign bit still distinguishing +0 from −0, an asymmetry IEEE 754 deliberately permits, unlike 2's complement's insistence on a single unique zero). An all-zero exponent with a NONZERO mantissa represents a DENORMALISED (subnormal) number — a special encoding used to represent values SMALLER in magnitude than the smallest ordinary normalised number can reach, by DROPPING the usual implicit leading 1 (using an implicit leading 0 instead) and gracefully "running out" of precision near zero rather than abruptly jumping to zero once the ordinary normalised range is exceeded.
2. ALL-ONE EXPONENT with an all-zero mantissa represents INFINITY (positive or negative, per the sign bit) — the result of an operation like dividing a nonzero number by zero. ALL-ONE EXPONENT with a NONZERO mantissa represents NaN ("Not a Number") — the result of an undefined operation such as 0/0 or ∞−∞, deliberately distinguished from ordinary infinity so that further arithmetic involving a NaN can be flagged as itself undefined rather than silently producing a plausible-looking but meaningless numeric result.
3. Reserving these two extreme exponent values for special cases means the ORDINARY, usable exponent range is slightly NARROWER than the full range the exponent field's bit width alone would suggest — for single precision, the 8-bit exponent field's 256 possible stored values include 254 used for ordinary normalised numbers (stored E from 1 to 254, giving true exponents from −126 to +127) plus the two reserved extremes (0 and 255) for zero/denormals and infinity/NaN respectively.

GATE TRAP: assuming EVERY possible exponent bit pattern represents an ordinary number via the standard implicit-leading-1 formula — forgetting that the two extreme patterns (all-zero and all-one) are reserved for zero/denormals and infinity/NaN respectively — leads directly to a wrong interpretation of a bit pattern whose exponent field happens to be exactly the minimum or maximum stored value, and to an incorrect count of how many DISTINCT ordinary normalised values a given exponent width can actually represent.

KEY: IEEE 754 does not treat its representable value space as a single uniform continuum of "ordinary numbers" — it deliberately carves out the two extreme exponent patterns for zero/denormals (handling the boundary near zero gracefully via denormalised numbers, using an implicit 0 rather than an implicit 1) and infinity/NaN (handling overflow and undefined results explicitly, rather than allowing them to silently wrap into some other, misleadingly ordinary-looking bit pattern) — recognising this three-way split (ordinary normalised range, zero/denormal range, infinity/NaN range) is essential before reading ANY IEEE 754 bit pattern whose exponent field is not a "typical," strictly-interior value.

BINARY ARITHMETIC: ADDITION, SUBTRACTION, AND MULTIPLICATION DIRECTLY

Ordinary binary arithmetic follows the SAME positional carrying/borrowing logic as decimal arithmetic, with only the DIGIT ALPHABET (0,1 instead of 0-9) and the CARRY/BORROW THRESHOLD (2 instead of 10) actually different.

1. BINARY ADDITION rules, bit by bit from the least significant position: 0+0=0 (no carry). 0+1=1+0=1 (no carry). 1+1=0 with a CARRY of 1 into the next position. 1+1+1 (an incoming carry plus two 1-bits) = 1 with a carry of 1.
2. BINARY SUBTRACTION rules (direct borrowing, mirroring decimal borrowing exactly): 0−0=0. 1−0=1. 1−1=0. 0−1=1 with a BORROW of 1 taken from the next position (exactly as decimal borrowing works, just with a threshold of 2 instead of 10 — borrowing "2" from the next position rather than "10").
3. BINARY MULTIPLICATION reduces to REPEATED SHIFTED ADDITION: multiply the multiplicand by each bit of the multiplier in turn (a bit of 1 copies the multiplicand unchanged; a bit of 0 contributes a row of all zeros), shifting each partial product one position further left for each successively higher-order multiplier bit, then SUM all the partial products using ordinary binary addition — this is the exact binary analogue of the long-multiplication algorithm taught for decimal numbers, and it is precisely why binary multiplication in actual hardware is often implemented as a sequence of SHIFT and ADD operations.

Worked trace, binary addition: compute 1011+1101. Aligning by position (from the right): position 0: 1+1=0, carry 1. Position 1: 1+0+carry1=0, carry 1. Position 2: 0+1+carry1=0, carry 1. Position 3: 1+1+carry1=1, carry 1. Final carry-out: 1. Result: 11000. Cross-check in decimal: 1011=11, 1101=13, 11+13=24, and 11000 in binary = 16+8=24 — matches.

Worked trace, binary subtraction with borrowing: compute 1100−0111. Position 0: 0−1 needs a borrow → 1 (borrow 1 from position 1). Position 1: (0 after lending)−1−(the borrow already taken means position 1 effectively has 0)... working through carefully: 1100−0111, aligning bit by bit from LSB: bit0: 0−1, borrow, result 1, borrow-out 1. bit1: 0−1−(borrow-in 1)=0−1−1, needs a borrow again, result 0 (since 2−1−1+... tracking via 2's-complement-style borrowing: 0−1−1 = −2, add 2 (borrow base) to get 0, borrow-out 1). bit2: 1−1−(borrow-in1)=1−1−1=−1, add 2 → 1, borrow-out 1. bit3: 1−0−(borrow-in 1)=0, borrow-out 0. Result: 0101. Cross-check in decimal: 1100=12, 0111=7, 12−7=5, and 0101=5 — matches.

Worked trace, binary multiplication via shifted addition: compute 101×110 (5×6 in decimal). Multiplier bits from LSB: bit0=0 → partial product row of 000 (shift 0). bit1=1 → partial product 101 shifted left by 1: 1010. bit2=1 → partial product 101 shifted left by 2: 10100. Summing the nonzero rows: 01010+10100 = 11110. Cross-check in decimal: 5×6=30, and 11110=16+8+4+2=30 — matches.

KEY: binary arithmetic requires NO new rules beyond ordinary decimal-style carrying, borrowing, and shifted-addition multiplication — the only genuine difference is the smaller digit alphabet {0,1} and the correspondingly smaller carry/borrow threshold (2, rather than 10); a binary arithmetic error under time pressure is almost always a simple bookkeeping slip in tracking a carry or borrow across several positions, not a conceptual misunderstanding of a genuinely different arithmetic rule, and re-deriving each digit position independently and carefully (as in the worked traces above) is more reliable than attempting several positions at once from memory.

CHARACTER ENCODING: ASCII

ASCII (American Standard Code for Information Interchange) is a fixed-width character encoding assigning each character (letters, digits, punctuation, and control characters) a unique numeric CODE POINT, originally defined using 7 bits (later commonly extended to a full 8-bit byte, with the extra bit historically used for parity checking or extended character sets).

1. With 7 bits, ASCII can represent exactly 2⁷=128 distinct characters — this direct bit-count-to-character-count relationship is the same kind of "how many distinct patterns fit in n bits" counting question that appears throughout this chapter (n-bit signed ranges, BCD's 16 total patterns with only 10 valid, and so on), and recognising it as the SAME underlying counting principle avoids needing a separately memorised "ASCII has 128 characters" fact disconnected from why that number is what it is.
2. A useful structural fact about the ASCII layout worth recognising: the digit characters '0' through '9' are assigned CONSECUTIVE code points (48 through 57 in decimal), meaning the NUMERIC VALUE of a decimal digit character can be recovered directly by SUBTRACTING the code point of '0' from the character's own code point — a trick used constantly in low-level string-to-number parsing code, and directly explainable by this consecutive-assignment structural fact rather than needing to be treated as an unexplained coincidence.

WORKED PROBLEMS

1. FULL RADIX CONVERSION. Convert decimal 94 to binary, octal, and hexadecimal. Binary: 94÷2=47r0, 47÷2=23r1, 23÷2=11r1, 11÷2=5r1, 5÷2=2r1, 2÷2=1r0, 1÷2=0r1 → reading bottom-to-top: 1011110. Octal (group in 3s from the right, padding left): 001 011 110 → 1,3,6 → 136. Hex (group in 4s): 0101 1110 → 5,E → 5E.

2. FRACTIONAL CONVERSION. Convert decimal 0.3125 to binary. 0.3125×2=0.625 (take 0). 0.625×2=1.25 (take 1). 0.25×2=0.5 (take 0). 0.5×2=1.0 (take 1, remainder 0, stop). Reading top-to-bottom: 0101 → 0.0101.

3. SIGN-MAGNITUDE, 1'S, AND 2'S COMPLEMENT. Represent −27 in 8-bit sign-magnitude, 1's complement, and 2's complement. +27 = 00011011. Sign-magnitude: 10011011. 1's complement: 11100100 (invert all bits). 2's complement: 11100100+1 = 11100101.

4. OVERFLOW CHECK, NEGATIVE OPERANDS. In 8-bit 2's complement, add −70 and −80, and check for overflow. −70: +70=01000110, invert=10111001, +1=10111010. −80: +80=01010000, invert=10101111,+1=10110000. Sum: 10111010+10110000 = 101101010, an 8-bit result (dropping the 9th bit carry-out) of 01101010. Sign bit is 0 (looks positive), but two NEGATIVE operands were added — this is overflow (the true sum −150 falls outside the representable range −128 to +127).

5. NO-OVERFLOW MIXED-SIGN CHECK. In 8-bit 2's complement, add +90 and −30. Explain directly why overflow cannot occur here, without computing the full sum. Since the two operands have DIFFERENT signs, overflow is impossible by the mixed-sign rule — no computation is needed to confirm this.

6. BCD ADDITION WITH CORRECTION. Add BCD-encoded 7 (0111) and 8 (1000). Binary sum: 0111+1000=1111 (15 in binary, but 1111 is one of the six invalid BCD patterns). Since the result exceeds 9, add 6 (0110): 1111+0110=10101, i.e. a carry-out of 1 into the next decimal digit's group, plus a remaining 4-bit group of 0101 (5) — giving the correct BCD result "15" (carry 1, digit 5), matching 7+8=15 exactly.

7. EXCESS-3 SELF-COMPLEMENTING CHECK. Verify the self-complementing property for digit 2. Excess-3 of 2: 2+3=5 → 0101. Excess-3 of (9−2)=7: 7+3=10 → 1010. Bitwise complement of 0101 is 1010 — matching the excess-3 code of 7 exactly, confirming self-complementation.

8. BINARY-TO-GRAY AND BACK. Convert binary 1001 to Gray, then back to binary. Forward: g(MSB)=1. Next: 0 XOR 1=1... wait, working correctly bit by bit from MSB: b=1,0,0,1. g1(MSB)=b1=1. g2=b1 XOR b2 = 1 XOR 0=1. g3=b2 XOR b3=0 XOR 0=0. g4=b3 XOR b4=0 XOR 1=1. Gray: 1101. Reverse: recovered-b1=g1=1. b2=b1 XOR g2=1 XOR 1=0. b3=b2 XOR g3=0 XOR 0=0. b4=b3 XOR g4=0 XOR 1=1. Recovered: 1001 — matches the original.

9. 9'S AND 10'S COMPLEMENT. Find the 9's and 10's complement of the 4-digit decimal number 3082. 9's complement: subtract each digit from 9: 9−3=6,9−0=9,9−8=1,9−2=7 → 6917. 10's complement: 6917+1 = 6918.

10. DECIMAL SUBTRACTION VIA 10'S COMPLEMENT. Compute 3082−1975 using 10's complement addition. 10's complement of 1975 (4 digits): 10000−1975=8025. Adding: 3082+8025=11107. Discarding the leading carry-out digit (the leftmost 1 beyond the 4-digit width): 1107 — matching the direct subtraction 3082−1975=1107 exactly.

11. IEEE 754 SINGLE PRECISION. Represent decimal 10.0 in IEEE 754 single precision. Binary: 10.0 = 1010 = 1.010×2³. Sign: 0. True exponent 3, stored E=3+127=130=10000010. Mantissa: 010 padded with trailing zeros to 23 bits: 01000000000000000000000. Full pattern: 0 10000010 01000000000000000000000.

12. BINARY ADDITION. Compute 1110+1011 directly, bit by bit. Position 0: 0+1=1. Position 1: 1+1=0, carry 1. Position 2: 1+0+carry1=0, carry 1. Position 3: 1+1+carry1=1, carry 1. Final carry-out 1. Result: 11001. Cross-check: 1110=14, 1011=11, 14+11=25, 11001=16+8+1=25 — matches.

13. BINARY MULTIPLICATION. Compute 11×101 (3×5) via shifted addition. Multiplier bits from LSB: bit0=1 → row 011 (shift 0). bit1=0 → row 000 shifted 1. bit2=1 → row 011 shifted 2 = 01100. Sum: 00011+01100=01111. Cross-check: 3×5=15, 01111=8+4+2+1=15 — matches.

14. IEEE 754 SPECIAL VALUES. In single precision, what does the bit pattern with sign=0, exponent=11111111 (all ones), mantissa=00000000000000000000000 (all zeros) represent? An all-one exponent with an all-zero mantissa represents INFINITY; combined with sign=0, this represents POSITIVE infinity (+∞).

15. ASCII DIGIT-VALUE EXTRACTION. If the ASCII code point of '0' is 48, and a character has code point 53, find the numeric digit value it represents. 53−48=5 — the character represents the digit 5, using the consecutive-code-point structural fact directly.

16. FULL COMBINED EXERCISE. A 12-bit unsigned binary counter reads 101100110101 at some instant. Convert this value to decimal, octal, and hexadecimal, and state whether interpreting the SAME bit pattern as a signed 12-bit 2's-complement number would change the answer. Decimal: summing set bits at positions 11,9,8,5,4,2,0 (reading the pattern left to right as positions 11 down to 0): 2048+512+256+32+16+4+1 = 2869. Octal: grouping in 3s from the right: 101 100 110 101 → 5,4,6,5 → 5465. Hexadecimal: grouping in 4s: 1011 0011 0101 → B,3,5 → B35. Signed interpretation: the MSB (position 11) is 1, so as a signed 12-bit 2's-complement number this pattern represents a NEGATIVE value — invert all bits (010011001010) and add 1 (010011001011 = 1227 in decimal), giving −1227, a completely different numeric meaning from the unsigned reading of 2869 despite being the IDENTICAL underlying bit pattern; the same 12 bits carry two entirely different intended values depending purely on which interpretation convention (signed vs unsigned) the surrounding context specifies.

CLOSING WORKED EXAMPLE: WHY THE SAME BITS NEED A STATED CONVENTION

The final worked problem above is worth dwelling on explicitly, since it captures the chapter's single most important practical lesson: a raw bit pattern, on its own, carries NO inherent numeric meaning at all — every interpretation covered in this chapter (unsigned binary, sign-magnitude, 1's complement, 2's complement, BCD, excess-3, Gray code, or an IEEE 754 floating-point field) is a CONVENTION imposed on top of the bits by whichever system is reading them, and the identical 12, 32, or 64 bits can validly mean entirely different quantities depending purely on which convention the surrounding context has specified. A question that asks "what value does this bit pattern represent" without stating which of these conventions applies is, strictly speaking, incompletely posed — and recognising this explicitly is what prevents the far more common error of silently ASSUMING the most familiar convention (usually plain unsigned binary) applies by default, when the actual context (a stated word width, a stated signedness, a stated encoding scheme) may require a very different reading of the exact same underlying bits.

This convention-dependence is worth connecting back explicitly to the chapter's very first observation about radix systems: a radix-r positional representation is itself already one specific convention among many possible ones for turning a sequence of digit symbols into a numeric value, and every scheme covered afterward — sign-magnitude, 1's and 2's complement, BCD, excess-3, Gray code, IEEE 754 — is best understood as a FURTHER layer of convention stacked on top of that basic positional idea, each one solving a specific representational problem (representing negative values efficiently in hardware, representing decimal digits directly without a lossy radix conversion, avoiding multi-bit transition glitches, representing a wide dynamic range of magnitudes within a fixed bit budget) that the plain unsigned positional scheme alone cannot solve on its own. Approaching every new encoding met in this chapter by asking "what specific problem does this convention solve, that the simpler schemes already covered could not" is a far more durable way to retain the material than memorising each scheme's mechanical conversion rule in isolation, since the mechanical rule can usually be RE-DERIVED quickly once the underlying problem the scheme was designed to solve is clearly understood, exactly as this chapter has attempted to do at every stage, from the very first division-and-remainder procedure through to the final special-value encodings of IEEE 754 floating-point.

Taking stock of the full progression one more time: positional notation itself solves the problem of representing arbitrarily large magnitudes with a small, fixed digit alphabet; grouping-based conversion between power-related radices solves the problem of moving between representations without repeated full arithmetic; sign-magnitude, 1's complement, and 2's complement each solve the problem of representing negative values, with 2's complement specifically solving it in a way that reuses unsigned addition hardware directly; BCD and excess-3 solve the problem of representing decimal digits without a lossy radix conversion, at the cost of leaving some bit patterns unused; Gray code solves the problem of avoiding multi-bit transition glitches in physical encoders and enables the K-map adjacency property; and IEEE 754 solves the problem of representing an enormous dynamic range of magnitudes within a small, fixed number of bits, at the cost of the representation errors that follow directly from a necessarily finite mantissa. Every one of these is a genuine engineering trade-off, not an arbitrary historical convention, and understanding each scheme as the answer to a specific, nameable problem is what makes the whole chapter's material feel like a connected body of reasoning rather than a long list of independently memorised procedures to be recalled by rote whenever a superficially similar-looking question happens to appear.

Carrying this problem-first framing forward into later chapters pays off directly: computer arithmetic hardware (adders, multipliers, and the overflow-detection logic built directly on top of the 2's-complement scheme covered here) and floating-point unit design both build immediately on the representational choices made in this chapter, and neither can be understood at any real depth without first having internalised exactly why each encoding scheme here takes the specific shape that it does. The arithmetic circuits built later are, in a very real sense, simply hardware implementations of the bit-level rules this chapter has already derived by hand — a full adder is nothing more than the binary-addition truth table above, wired directly into silicon, and an overflow-detection circuit is nothing more than the sign-bit-carry-comparison rule above, likewise built as a small piece of combinational logic rather than checked manually on paper each time a result needs validating. This is exactly what this chapter's own overflow-checking worked traces already did by hand, one bit position at a time, tracing every carry explicitly rather than trusting a remembered shortcut applied without re-derivation — the shortcut and the from-scratch derivation must always, by construction, agree with each other on any correctly worked example, and any disagreement between the two is itself a reliable signal that an arithmetic slip has crept in somewhere along the way. That disagreement is worth hunting down carefully before ever fully trusting a final stated numeric answer in a longer, multi-step chain of hand-worked computation done under real time pressure.

CARRYING THIS FORWARD

Every named error flagged in this chapter's traps is a specific instance of the same general failure: applying a procedure MECHANICALLY, from memory, without re-deriving (even briefly) WHY that specific procedure's direction, correction step, or applicability condition holds — reading remainders in the wrong direction, assuming 1's and 2's complement share a range, skipping the BCD +6 correction, treating Gray-to-binary as an independent lookup rather than a chain, or checking for overflow on a mixed-sign addition that could never overflow in the first place. Every one of these procedures was DERIVED explicitly in this chapter from a small number of underlying facts (what a remainder or a multiplication's integer part actually represents positionally; how many total bit patterns an n-bit word has and how the zero/sign asymmetry falls out of that count; which of a 4-bit group's patterns are actually valid in BCD; how Gray code's one-bit-at-a-time adjacency requirement forces a chained rather than independent recovery formula) — and re-deriving the relevant one-line justification on the spot, rather than trying to recall the correct mechanical direction from memory alone under pressure, is the single most reliable way to avoid every trap catalogued above.

A second theme running through the entire chapter is that several apparently separate schemes are, on inspection, the SAME underlying construction applied at different scales or in different radices. 1's complement and 2's complement were shown to be the r=2 special cases of the general (r−1)'s-complement and r's-complement definitions, which apply just as directly to decimal (9's and 10's complement) or any other radix — a single generalised idea rather than three unrelated named schemes to memorise independently. Subtraction-via-complement-addition works identically whether the underlying radix is 2 or 10, always by adding the complement and discarding the carry-out beyond the word's own width, which is exactly the mechanism that lets 2's-complement hardware reuse ordinary unsigned-addition circuitry for signed subtraction with no extra logic. Binary arithmetic itself was shown to need no separate rule set at all beyond ordinary decimal-style carrying and borrowing, merely with a smaller digit alphabet and a correspondingly smaller carry threshold — recognising this collapses what might otherwise look like an entirely new arithmetic system into a direct, mechanical restatement of arithmetic already familiar from decimal, differing only in which threshold triggers a carry or borrow at each position.

A third connecting thread links this chapter directly to floating-point representation: the observation that many ordinary decimal fractions never terminate in binary (0.1 becoming an infinitely repeating pattern) is not an isolated curiosity about the multiplication-based fractional conversion procedure — it is the SAME underlying fact that explains why IEEE 754's finite mantissa field can only ever store a truncated prefix of such a fraction's true, infinitely long binary expansion, producing the small but genuine representation errors that arise whenever ordinary decimal values are stored and manipulated in floating-point hardware. Seeing the fractional-conversion procedure and the floating-point precision limitation as two faces of the identical underlying phenomenon — rather than as an unrelated conversion quirk on one hand and an unrelated hardware limitation on the other — is exactly the kind of cross-topic connection this chapter, and this entire book-depth study approach, is built to surface.
`
};
