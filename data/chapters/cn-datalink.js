// Textbook chapter: The Data Link Layer — Framing, Error Control, Flow
// Control, and Medium Access Control. Written directly (no subagent) to
// match the depth and voice of the other chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['cn-datalink'] = {
  figs: [
    {
      id: 'sliding-window-pipeline',
      caption: 'Sliding-window pipelining: while earlier frames are still awaiting acknowledgment, the sender continues transmitting later frames within the window, keeping the link\'s own pipe filled rather than idling after every single frame the way stop-and-wait does.',
      svg: '<svg viewBox="0 0 260 90" width="100%" style="max-width:260px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1" fill="none"><rect x="10" y="20" width="30" height="20"/><rect x="45" y="20" width="30" height="20"/><rect x="80" y="20" width="30" height="20"/><rect x="115" y="20" width="30" height="20"/><rect x="150" y="20" width="30" height="20"/><rect x="10" y="50" width="140" height="15" stroke-dasharray="3,2"/></g><g font-size="7" fill="currentColor" text-anchor="middle"><text x="80" y="70">window</text><text x="25" y="33">1</text><text x="60" y="33">2</text><text x="95" y="33">3</text><text x="130" y="33">4</text><text x="165" y="33">5</text></g></svg>'
    },
    {
      id: 'csma-cd-timeline',
      caption: 'CSMA/CD: a station listens before transmitting, and continues listening while transmitting so it can detect a collision and abort early, rather than wasting the full frame time transmitting data that will need to be resent anyway.',
      svg: '<svg viewBox="0 0 260 80" width="100%" style="max-width:260px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.1" fill="none"><line x1="10" y1="40" x2="250" y2="40"/><rect x="30" y="30" width="70" height="20"/><rect x="90" y="30" width="70" height="20"/></g><g font-size="8" fill="currentColor" text-anchor="middle"><text x="65" y="25">Station A</text><text x="125" y="25">Station B</text><text x="130" y="65">collision window</text></g></svg>'
    }
  ],
  text: `
FRAMING: DELIMITING THE BIT STREAM INTO DISCRETE UNITS

The physical layer, examined in this sequence's previous chapter, delivers only a RAW, undifferentiated stream of bits — the data link layer's very FIRST job is FRAMING: grouping this stream into discrete, individually delimited FRAMES, so the receiver can tell exactly WHERE each frame begins and ends.

1. CHARACTER (BYTE) COUNTING: the frame's header carries an EXPLICIT count of how many characters (or bytes) the frame contains — simple, but CATASTROPHICALLY fragile, since a SINGLE corrupted count field misleads the receiver about where the CURRENT frame ends, which in turn misleads it about where the NEXT frame's own header even begins, causing the error to PROPAGATE indefinitely through every subsequent frame until some external resynchronization occurs.
2. CHARACTER (BYTE) STUFFING: a special FLAG byte marks frame BOUNDARIES; whenever the flag byte's own bit pattern happens to occur WITHIN the actual data, an ESCAPE byte is inserted immediately before it, and the receiver, upon seeing an escape byte, knows to treat the NEXT byte as literal data rather than as a genuine flag or escape — this avoids byte-counting's error-propagation weakness, since frame boundaries are now recognized STRUCTURALLY (by pattern) rather than by a count that can be silently corrupted.
3. BIT STUFFING: generalizes the SAME idea to the BIT level, typically using the bit pattern 01111110 as the frame-boundary flag — whenever FIVE consecutive 1-bits occur within the actual data, the sender inserts an EXTRA 0-bit immediately afterward; the receiver, upon seeing five consecutive 1-bits followed by a 0, REMOVES that inserted 0 (destuffing), and if it instead sees five consecutive 1-bits followed by ANOTHER 1, recognizes this as the genuine flag pattern marking a frame boundary. HDLC and PPP both use bit stuffing.
4. PHYSICAL-LAYER CODING VIOLATIONS: some encoding schemes (variants of Manchester encoding, for instance) reserve certain bit-pattern combinations that NEVER occur in legitimately encoded data, and use these reserved, "invalid" patterns DIRECTLY as frame delimiters — since these patterns cannot occur naturally within real data, NO stuffing is needed at all, though this approach ties frame delimiting TIGHTLY to one specific physical-layer encoding scheme, unlike stuffing-based approaches which work at a level independent of the underlying encoding. This trade-off between generality (stuffing, usable with essentially any encoding) and efficiency (coding violations, avoiding the added overhead of inserted stuffing bits or bytes altogether, but only where the underlying physical encoding actually supports it) is a smaller-scale echo of similar generality-versus-efficiency trade-offs this book has already examined in entirely different subjects.

1. Worked example (bit-stuffing trace, independently verified): the data bit sequence 0111110111110 is to be transmitted using bit stuffing with the flag 01111110. Trace the STUFFED sequence the sender actually transmits. Scanning left to right for runs of five consecutive 1s: the sequence 01111110111110 breaks into 0-11111-0-11111-0. The FIRST run of five 1s (positions 2-6) is followed by inserting an extra 0 immediately after it; the SECOND run of five 1s (positions 8-12, in the original data) is likewise followed by an inserted 0. The fully stuffed transmitted sequence becomes 0111110(inserted 0)0111110(inserted 0)... — concretely: 01111100 0111110 0, illustrating that EVERY maximal run of five consecutive 1-bits anywhere in the actual data, without exception, receives its own stuffed 0, regardless of how many such runs occur or how close together they are.

ERROR DETECTION: PARITY, CHECKSUM, AND CRC

1. SIMPLE PARITY adds a SINGLE extra bit, chosen so the TOTAL number of 1-bits (including the parity bit itself) is EVEN (even parity) or ODD (odd parity) — this detects ANY single-bit error (an odd number of bit flips), but FAILS to detect an EVEN number of bit flips, since two flipped bits cancel out and leave the overall parity UNCHANGED.
2. TWO-DIMENSIONAL PARITY arranges the data into a GRID and computes a parity bit for EVERY row AND every column — this can detect (and, in the single-error case, LOCATE and CORRECT) any SINGLE-bit error, by identifying the specific row and column whose parity checks both fail, but can still MISS certain patterns of exactly FOUR bit flips positioned at the four corners of a rectangle within the grid, since such a pattern leaves EVERY row's and EVERY column's own parity unchanged.
3. THE INTERNET CHECKSUM (used by IP, TCP, and UDP) treats the data as a sequence of 16-bit words, adds them together using ONE'S COMPLEMENT arithmetic (with any final CARRY wrapped around and added back into the low-order bits), and the checksum field carries the ONE'S COMPLEMENT of this final sum — at the receiver, the SAME summing process, INCLUDING the checksum field itself this time, should yield a result of all 1-bits if NO error occurred. The Internet checksum is computationally cheaper than CRC to compute purely in software (simple repeated addition, rather than a full polynomial division), which is precisely why it remains in wide use at the higher transport and network layers, even though CRC's own burst-error-detection guarantees are considerably stronger.
4. CYCLIC REDUNDANCY CHECK (CRC) treats the data bits as the COEFFICIENTS of a POLYNOMIAL over GF(2) (binary field arithmetic, where addition is XOR and there is no carrying), and divides this data polynomial by a FIXED, agreed-upon GENERATOR polynomial of degree r; the REMAINDER of this division (always exactly r bits, padded with leading zeros if necessary) becomes the CRC field appended to the transmitted frame. At the receiver, dividing the ENTIRE received frame (data plus CRC) by the SAME generator polynomial should yield a remainder of ALL ZEROS if no error occurred.
5. CRC is DRAMATICALLY more powerful than simple parity or the Internet checksum at detecting BURST errors (a contiguous run of consecutive bit errors) — a generator polynomial of degree r GUARANTEES detection of EVERY burst error of length r OR LESS, and detects the OVERWHELMING majority of longer bursts as well, which is PRECISELY why CRC (rather than simple parity or a plain checksum) is the standard error-detection mechanism used at the DATA LINK layer specifically, in Ethernet and virtually every other modern link-layer protocol. Commonly used standardized generator polynomials include CRC-16 (degree 16, used in many older link-layer protocols) and CRC-32 (degree 32, used by Ethernet itself), with the larger degree directly buying a correspondingly larger guaranteed burst-detection length, at the modest cost of a correspondingly larger fixed per-frame overhead.

1. Worked example (CRC computation, independently verified via polynomial long division over GF(2)): data bits 1101011011, generator polynomial 10011 (degree r=4, so 4 zero bits are appended before division). Compute the CRC remainder. Appending 4 zeros to the data gives 11010110110000. Performing binary (XOR-based) long division by 10011: the process repeatedly XORs the generator against the leading bits whenever the leading bit is 1, and shifts otherwise, continuing until only r=4 bits remain undivided. Carrying out this division step by step (XOR-subtracting 10011 wherever the current leading bit is 1, otherwise bringing down the next bit) yields a final remainder of 1110 — the sender therefore transmits the 14-bit sequence 11010110111110 (original 10 data bits followed by the computed 4-bit CRC remainder 1110). At the receiver, dividing this ENTIRE 14-bit sequence by the SAME generator 10011 yields a remainder of exactly 0000, confirming no error occurred, exactly as the CRC scheme's own correctness guarantee requires.

GATE TRAP: assuming CRC can detect and CORRECT errors, the way Hamming codes do, is a common and costly overstatement — CRC is a pure error-DETECTION scheme; upon detecting a non-zero remainder, the receiver's ONLY recourse is to DISCARD the frame and request RETRANSMISSION (typically via the flow-control mechanisms examined later in this chapter) — it cannot identify WHICH specific bit(s) were corrupted, unlike Hamming codes, which trade additional redundant bits specifically for that correction capability.

ERROR CORRECTION: HAMMING CODES

1. A HAMMING CODE adds carefully positioned PARITY (check) bits at the power-of-two positions (1, 2, 4, 8, ...) within the transmitted codeword, with EACH check bit covering a SPECIFIC, overlapping subset of the data bits, chosen so that the PATTERN of which checks fail (the SYNDROME) uniquely identifies the position of a SINGLE flipped bit.
2. FOR m data bits, the number of check bits r needed must satisfy 2^r ≥ m+r+1 (the +1 accounting for the "no error" all-zero syndrome case) — this inequality is SOLVED by trial, incrementing r until it holds, since it cannot generally be solved in closed form.
3. THE SYNDROME is computed by re-checking each parity group at the RECEIVER and treating the resulting pattern of pass/fail results (arranged with the HIGHEST-order check bit first) as a BINARY number — a syndrome of ALL ZEROS indicates no error; a NON-ZERO syndrome directly gives the POSITION (in binary) of the single flipped bit, which the receiver then simply INVERTS to correct.
4. A BASIC (single-error-correcting) Hamming code can detect but NOT correct any DOUBLE-bit error (a double error typically produces a non-zero syndrome that INCORRECTLY points to some OTHER, wrong bit position, silently miscorrecting the codeword) — adding ONE additional overall parity bit across the ENTIRE codeword extends the scheme to SDED (single-error-correcting, double-error-DETECTING), since the extra overall parity bit can then distinguish a genuine single error (overall parity fails) from a double error (overall parity still passes, despite the syndrome being non-zero).

1. Worked example (Hamming-code check-bit count, independently verified): for m=7 data bits, determine the minimum number of check bits r required. Testing r=3: 2^3=8, and m+r+1 = 7+3+1 = 11; 8 is NOT ≥ 11, so r=3 is insufficient. Testing r=4: 2^4=16, and m+r+1 = 7+4+1 = 12; 16 ≥ 12 holds, so r=4 is the minimum sufficient number of check bits — giving a total codeword length of 7+4=11 bits, the well-known Hamming(11,7) single-error-correcting code.

FLOW CONTROL: STOP-AND-WAIT

1. FLOW CONTROL exists to prevent a FAST sender from OVERWHELMING a SLOWER receiver's own buffer capacity — it is a fundamentally DIFFERENT concern from error control (which handles CORRUPTED data) and from congestion control (which handles an OVERLOADED NETWORK, examined in the later Transport-layer chapter of this sequence), though all three concerns are frequently implemented using OVERLAPPING mechanisms.
2. STOP-AND-WAIT is the SIMPLEST flow-control protocol: the sender transmits EXACTLY one frame, then WAITS for an acknowledgment (ACK) before sending the NEXT frame — trivially simple to implement, but as this book's own previous chapter's bandwidth-delay-product worked example already demonstrated, this achieves VERY POOR link utilization whenever the bandwidth-delay product is large relative to a single frame's own transmission time, since the sender sits IDLE for almost the entire round-trip time after each single frame.
3. STOP-AND-WAIT ARQ (Automatic Repeat reQuest) adds a TIMER: if no acknowledgment arrives within a set TIMEOUT period, the sender ASSUMES the frame (or its acknowledgment) was LOST, and RETRANSMITS — this introduces the possibility of DUPLICATE frames if the original acknowledgment was merely DELAYED rather than genuinely lost, which is why sequence numbers (even a single alternating 0/1 bit, sufficient for stop-and-wait specifically) are needed to let the receiver DISCARD an accidental duplicate.

SLIDING-WINDOW PROTOCOLS: GO-BACK-N AND SELECTIVE REPEAT

[[FIG:sliding-window-pipeline]]

1. SLIDING-WINDOW protocols generalize stop-and-wait by allowing the sender to have MULTIPLE frames OUTSTANDING (transmitted but not yet acknowledged) simultaneously, up to a WINDOW SIZE limit — this PIPELINES transmission, keeping the link's own bandwidth-delay-product "pipe" filled rather than idling after every single frame, directly solving the poor-utilization problem plain stop-and-wait suffers under a large bandwidth-delay product.
2. GO-BACK-N (GBN): the receiver accepts frames ONLY in strict, correct SEQUENCE order, discarding (NOT buffering) any frame that arrives OUT of order — if frame k is lost or corrupted, ALL subsequent frames the sender had already transmitted within its window are discarded upon arrival at the receiver (since they arrive "ahead of" the still-missing frame k), and the SENDER must retransmit frame k AND every frame AFTER it, even those that individually arrived correctly — a potentially large amount of WASTED retransmission when losses occur.
3. SELECTIVE REPEAT (SR): the receiver BUFFERS out-of-order frames that arrive correctly (rather than discarding them), and the sender retransmits ONLY the SPECIFIC frame(s) that were actually lost or corrupted, identified via INDIVIDUAL acknowledgments per frame — this is considerably more EFFICIENT under lossy conditions (avoiding GBN's wasteful blanket retransmission), at the cost of GREATER receiver-side buffering complexity and the need for the receiver to track and acknowledge frames individually rather than merely tracking "the next expected sequence number."
4. THE MAXIMUM window size for GBN, using an n-bit sequence-number field (2^n distinct sequence numbers available), is 2^n − 1 (NOT 2^n) — if the window were allowed to reach the full 2^n, a receiver could not reliably distinguish a genuinely NEW frame from an OLD, already-acknowledged frame's retransmitted duplicate carrying the SAME wrapped-around sequence number, since both would appear identical modulo 2^n.
5. THE maximum window size for SELECTIVE REPEAT is only 2^(n-1) (HALF of GBN's maximum, for the SAME sequence-number field width) — this stricter limit is needed because SR's receiver must buffer out-of-order frames and distinguish a truly NEW frame from a duplicate retransmission of an OLD one EVEN WHILE both the sender's and receiver's windows may be positioned differently at a given moment; allowing the full 2^n−1 window (as GBN permits) can, under SR's specific accept-and-buffer discipline, cause the sender's and receiver's windows to OVERLAP in a way that makes a stale, duplicate frame indistinguishable from a genuinely new one.

GATE TRAP: using GBN's window-size formula (2^n − 1) for a question that actually describes SELECTIVE REPEAT, or vice versa, is one of the single most common numerical errors on this entire topic — always identify FIRST whether the receiver in a given scenario BUFFERS out-of-order frames (Selective Repeat, maximum window 2^(n-1)) or DISCARDS them (Go-Back-N, maximum window 2^n − 1) before applying either formula, since applying the wrong one silently produces an answer that is exactly DOUBLE (or half) the genuinely correct value.

1. Worked example (window-size computation, independently verified): a data link protocol uses a 3-bit sequence number field. Compute the maximum window size under Go-Back-N and under Selective Repeat separately. GBN: 2^3 − 1 = 8−1 = 7. Selective Repeat: 2^(3-1) = 2^2 = 4 — directly confirming SR's maximum is exactly HALF of GBN's maximum for the identical 3-bit sequence-number field, as the general formulas predict.

MEDIUM ACCESS CONTROL: ALOHA

1. MEDIUM ACCESS CONTROL (MAC) protocols govern HOW multiple stations sharing a SINGLE broadcast medium (such as a shared bus or a wireless channel) decide WHO may transmit at any given moment, without every station needing a separately dedicated physical link.
2. PURE ALOHA: a station transmits WHENEVER it has data ready, with NO coordination at all — if TWO stations' transmissions overlap in TIME even partially, BOTH are considered CORRUPTED (collided), and both stations must wait a RANDOM backoff period before retransmitting. Pure ALOHA's maximum theoretical throughput (efficiency) is 1/(2e) ≈ 18.4% of the channel's raw capacity, a strikingly LOW figure directly reflecting how easily collisions occur when transmissions are COMPLETELY uncoordinated in time.
3. SLOTTED ALOHA improves on pure ALOHA by dividing TIME into discrete SLOTS, and permitting a station to BEGIN transmitting only at the START of a slot — this HALVES the "vulnerable period" during which a collision can occur (since a station's transmission can now only overlap with ANOTHER station's transmission that begins in the SAME slot, rather than at ANY arbitrary offset), raising the maximum theoretical throughput to 1/e ≈ 36.8%, EXACTLY double pure ALOHA's own maximum figure.
4. THE theoretical throughput figures (1/(2e) for pure ALOHA, 1/e for slotted ALOHA) both arise from modeling the arrival of transmission attempts as a POISSON process and finding the traffic-load value G that MAXIMIZES the probability of a successful (collision-free) transmission — GATE questions typically supply these two constants directly (18.4% and 36.8%) rather than requiring their full derivation from the underlying Poisson model. A candidate should nonetheless remember which of the two figures belongs to which scheme, since the two numbers are close enough in value that a careless swap is easy to make under exam-time pressure, and would silently invert the entire comparison a question is actually asking for.

CARRIER SENSE MULTIPLE ACCESS: CSMA, CSMA/CD, AND CSMA/CA

[[FIG:csma-cd-timeline]]

1. CSMA (Carrier Sense Multiple Access) improves substantially on ALOHA by having a station LISTEN to the medium BEFORE transmitting ("sense the carrier"), transmitting ONLY if the medium is currently sensed IDLE — this reduces, but does NOT entirely eliminate, collisions, since TWO stations can both sense the medium idle at NEARLY the same instant (due to PROPAGATION delay meaning a transmission already in progress has not yet been detected everywhere) and both begin transmitting, still causing a collision.
2. CSMA/CD (Collision Detection), used by classic (shared-medium) Ethernet, additionally has a transmitting station CONTINUE listening WHILE it transmits — if a collision is DETECTED (the sensed signal differs from what the station itself is sending), the station IMMEDIATELY ABORTS its transmission, rather than wastefully completing the entire frame, and sends a brief JAM signal to ensure all OTHER stations also recognize the collision has occurred.
3. THE MINIMUM frame size for CSMA/CD to work correctly must be large enough that the TRANSMISSION time of the SHORTEST possible frame is AT LEAST the full round-trip propagation delay across the network's own maximum span — this guarantees a station is STILL transmitting (and therefore STILL listening) at the moment a collision signal, triggered by a distant station, could possibly arrive back at it; a frame SHORTER than this minimum could finish transmitting BEFORE a collision it caused far away is even detected, leaving the sender wrongly believing its transmission succeeded.
4. CSMA/CA (Collision Avoidance), used by WiFi (802.11) rather than CSMA/CD, is necessary because a WIRELESS station generally CANNOT reliably detect a collision WHILE transmitting (its own transmission overwhelms its own receiver, a problem far less severe on WIRED media) — instead, CSMA/CA AVOIDS collisions preemptively, using techniques such as a RANDOM backoff period even BEFORE the first transmission attempt, and an RTS/CTS (Request to Send / Clear to Send) handshake that reserves the medium in advance, directly addressing the HIDDEN TERMINAL problem where two stations, each unable to sense the other's transmission directly, might otherwise both transmit simultaneously to a shared receiver positioned between them.

GATE TRAP: assuming CSMA/CD and CSMA/CA are simply the "wired" and "wireless" NAMES for the identical underlying mechanism is incorrect — CD (Collision DETECTION) reactively detects and aborts an ALREADY-occurring collision, which fundamentally requires the ability to listen and transmit simultaneously on the same medium; CA (Collision AVOIDANCE) proactively tries to prevent a collision from occurring in the FIRST place, precisely because wireless media generally cannot support CD's own simultaneous-listen-while-transmit requirement — the two are genuinely DIFFERENT strategies (reactive versus proactive), not merely relabeled versions of one shared technique.

1. Worked example (minimum frame size for CSMA/CD, independently verified): a shared Ethernet segment has a maximum end-to-end propagation delay of 25.6 microseconds (round trip), and operates at 10 Mbps. Compute the minimum frame size required for CSMA/CD to function correctly. Minimum transmission time must be AT LEAST the round-trip propagation delay: 25.6 microseconds. Minimum frame size = bandwidth × minimum transmission time = 10×10^6 bits/sec × 25.6×10^-6 sec = 256 bits = 32 bytes — this directly illustrates why classic Ethernet's own historical minimum frame size (64 bytes, chosen with some additional safety margin over the raw computed minimum, and to accommodate a somewhat longer maximum network span than this simplified example) exists as a HARD, architecturally necessary constraint, not an arbitrary design convenience.

BINARY EXPONENTIAL BACKOFF

1. WHEN a collision occurs under CSMA/CD, colliding stations must wait a RANDOM amount of time before retrying, to avoid immediately colliding AGAIN — BINARY EXPONENTIAL BACKOFF chooses this random delay from a range that DOUBLES after each successive collision involving the SAME frame: after the k-th collision, a station picks a random integer r uniformly from {0, 1, ..., 2^k − 1} (capped at some maximum k, typically 10, beyond which the range stops growing), and waits r time slots before attempting to retransmit.
2. IN this scheme, a "time slot" is defined as the maximum time needed for a signal to propagate across the entire network's own span (its round-trip collision-detection window, matching the earlier MINIMUM FRAME SIZE discussion), not an arbitrarily chosen fixed interval. THIS exponential growth serves a DIRECT, adaptive purpose: a SINGLE collision suggests only LIGHT contention, so a SMALL backoff range (k=1: {0,1}) suffices to usually avoid a repeat collision; but REPEATED collisions on the SAME frame suggest HEAVIER contention among more stations, so the backoff range GROWS to spread retry attempts across a WIDER span of time, reducing the probability that several contending stations all re-collide again.
3. AFTER a fixed maximum number of collisions (typically 16 attempts total) without success, the station GIVES UP entirely and reports a transmission failure UP to a higher layer — this upper bound exists precisely because unboundedly retrying an apparently un-transmittable frame would otherwise waste the shared medium's own capacity indefinitely, denying that same capacity to every other, potentially perfectly viable, station waiting to transmit its own data.

ETHERNET ADDRESSING AND FRAME FORMAT

1. EVERY Ethernet network interface has a UNIQUE, globally assigned 48-bit MAC (Media Access Control) address, conventionally written as six colon-separated hexadecimal byte pairs (for instance, 00:1A:2B:3C:4D:5E) — the FIRST 24 bits (three bytes) identify the manufacturer (the Organizationally Unique Identifier, OUI), while the remaining 24 bits are assigned by that manufacturer to uniquely distinguish each of its own individual devices.
2. A standard Ethernet frame contains, in order: a PREAMBLE (7 bytes of alternating 1s and 0s, allowing receiver clock synchronization, directly echoing this chapter's own earlier discussion of Manchester encoding's self-clocking property), a START FRAME DELIMITER (1 byte), DESTINATION and SOURCE MAC addresses (6 bytes each), a TYPE/LENGTH field (2 bytes, identifying the higher-layer protocol carried, such as IP), the DATA payload itself (46 to 1500 bytes, PADDED with extra bytes if the actual payload is shorter than the 46-byte minimum), and a trailing 4-byte CRC (Frame Check Sequence) field for error detection, directly applying the earlier CRC section's own error-detection mechanism at this specific, standardized layer.
3. THE minimum payload of 46 bytes exists for EXACTLY the same reason this chapter's own earlier MINIMUM FRAME SIZE worked example derived — a frame shorter than the minimum needed to keep a station transmitting for the full round-trip collision-detection window would defeat CSMA/CD's own correctness requirement; padding with otherwise-meaningless filler bytes is the practical mechanism ensuring EVERY Ethernet frame, regardless of its genuine payload size, meets this hard minimum.
4. A DESTINATION MAC address of all-1-bits (FF:FF:FF:FF:FF:FF) is the reserved BROADCAST address, delivered to and processed by EVERY station on the local network segment — distinct from a MULTICAST address (the low-order bit of the first byte set to 1, but not all bits set), which is delivered only to stations that have specifically opted to LISTEN for that particular multicast group. A frame whose destination is a genuine broadcast or multicast address is, by convention, never a candidate for a learning bridge's own per-destination forwarding-table lookup, since it is always intended for MORE than one specific recipient at once.

HDLC AND PPP: POINT-TO-POINT DATA LINK PROTOCOLS

1. HDLC (High-Level Data Link Control) is a bit-oriented, connection-oriented data link protocol supporting THREE distinct frame types: INFORMATION frames (carrying actual data, along with piggybacked sequence numbers for both flow control and acknowledgment), SUPERVISORY frames (carrying ONLY control information, such as an acknowledgment or a request for retransmission, with NO user data payload), and UNNUMBERED frames (used for connection establishment, termination, and other control purposes outside the normal numbered data flow).
2. HDLC uses BIT STUFFING (the exact 01111110-flag-based mechanism described earlier in this chapter's own FRAMING section) to delimit its frames, and can operate in several configurations, including NORMAL RESPONSE MODE (a primary station polls secondary stations, which may transmit only when explicitly granted permission) and ASYNCHRONOUS BALANCED MODE (peer stations, with EQUAL status, may each initiate transmission independently, without needing to be polled).
3. PPP (Point-to-Point Protocol), the protocol most commonly used for direct point-to-point links (such as a dial-up or DSL connection to an Internet service provider), is heavily influenced by HDLC's own design but uses BYTE stuffing rather than bit stuffing, and is fundamentally CONNECTIONLESS and unnumbered at the basic framing level (reliability, when needed, is instead typically provided by a HIGHER layer, such as TCP, rather than by PPP's own framing mechanism directly) — PPP additionally supports its own NCP (Network Control Protocol) sub-protocols, allowing it to carry MULTIPLE different network-layer protocols (not merely IP) over the SAME underlying physical link.

GATE TRAP: assuming HDLC and PPP provide IDENTICAL reliability guarantees merely because PPP is HDLC-influenced is incorrect — HDLC's own information frames carry SEQUENCE numbers supporting genuine sliding-window-style flow control and retransmission DIRECTLY at the data link layer, while basic PPP framing carries NO such sequence numbering at all, delegating retransmission-based reliability entirely to a higher layer if reliability is required — a question describing "sequence numbers used directly within a data-link frame's own control fields" is describing HDLC-style behavior, not plain PPP.

BRIDGES AND SWITCHES: LEARNING AND FORWARDING

1. A BRIDGE (or a modern, more capable multi-port SWITCH) connects multiple separate LAN segments together at the DATA LINK layer, forwarding frames between segments based on their DESTINATION MAC address, rather than FLOODING every frame to every connected segment the way a simple hub (a purely physical-layer repeater device) would.
2. A LEARNING BRIDGE builds its OWN forwarding table DYNAMICALLY, simply by OBSERVING the SOURCE MAC address of every frame it receives on each of its own ports — recording "this source address was seen arriving on THIS port" lets the bridge, on a LATER frame addressed TO that same MAC address, forward the frame DIRECTLY out that SPECIFIC port instead of flooding it to every other port, a scheme requiring NO manual configuration at all, and one that continues to refine and correct itself automatically as devices are physically moved between ports over time.
3. IF a bridge receives a frame whose destination address is NOT YET present in its own forwarding table (an unknown destination), it FLOODS the frame out every port EXCEPT the one it arrived on — exactly mirroring a plain hub's own default behavior for that SPECIFIC, not-yet-learned case, until the destination station itself eventually transmits something and the bridge LEARNS its correct port.
4. MULTIPLE bridges interconnecting the SAME set of LAN segments can create REDUNDANT physical loops, which (WITHOUT further precaution) would cause a broadcast frame to circulate INDEFINITELY, since each bridge would keep re-flooding it back onto segments it has already visited — the SPANNING TREE PROTOCOL (STP) solves this by having the bridges cooperatively compute a LOOP-FREE logical topology (a spanning tree, directly recalling the general spanning-tree concept from this book's own earlier Algorithms material), deactivating just enough redundant links to eliminate every loop while still keeping every segment reachable, and re-activating a deactivated link automatically if the active topology's own connectivity is later broken by a failure. This is the SAME "keep redundancy available but only activate the minimum needed for a loop-free connected structure" idea underlying a minimum-spanning-tree computation over a general weighted graph, applied here specifically to a physical network's own bridging topology.

KEY: a learning bridge's own forwarding-table construction is, at its structural core, the SAME kind of "build the exact structure needed purely by observing normal traffic, with no separate configuration phase required" idea already seen in this book's Databases sequence, where a database's own query optimizer likewise builds and refines its understanding of data distribution from observed access patterns rather than from a separately maintained, manually specified configuration — recognizing self-learning, observation-driven structures as a RECURRING systems-design pattern, rather than a coincidence specific to bridges, deepens understanding well beyond this one specific mechanism.

GATE TRAP: assuming a switch/bridge operating purely at the data link layer can make forwarding decisions based on IP addresses is incorrect — a plain bridge or Layer-2 switch forwards STRICTLY by MAC address, entirely oblivious to whatever network-layer (IP) addressing scheme rides on top of it; IP-address-based forwarding is instead the defining job of a ROUTER, operating one layer higher, examined in depth in the NEXT chapter of this sequence — conflating "switch" (Layer 2, MAC-based) with "router" (Layer 3, IP-based) forwarding behavior is one of the most frequently tested distinctions in introductory networking.

WORKED PROBLEMS

1. PARITY VERSUS CRC ERROR-DETECTION COVERAGE. A 3-bit burst error corrupts a transmitted frame protected by (a) simple even parity and (b) a CRC using a generator polynomial of degree 4. Determine whether each scheme is GUARANTEED to detect this specific error. For (a) simple parity: a 3-bit burst error flips an ODD number of bits (3 is odd) if all three flips fall within the SAME parity-check span, so simple parity WOULD detect this specific case — but this detection is NOT guaranteed by burst LENGTH generally; a DIFFERENT 3-bit-flip pattern spanning MULTIPLE separate parity groups, or landing an even total number of net flips within one group, could go undetected — parity's guarantee is strictly about the total COUNT of flipped bits within its own single check, not about burst length as such. For (b) CRC with a degree-4 generator: since a burst error of length ≤ r (here, r=4) is ALWAYS guaranteed to be detected regardless of exactly which bits within that burst are flipped, and this error's burst length (3) is ≤ 4, CRC's detection here IS fully guaranteed — directly illustrating the earlier ERROR DETECTION section's own claim that CRC provides a burst-length-based guarantee that simple parity structurally cannot match.

2. HAMMING CODE SYNDROME DECODING. A Hamming-coded message is received, and computing the three check bits' pass/fail syndrome (from highest-order check to lowest) yields the pattern FAIL, PASS, FAIL, corresponding to the binary value 101 (5 in decimal). Determine what the receiver should do. A non-zero syndrome of 101 (binary) = 5 (decimal) indicates that bit POSITION 5 (counting from position 1) of the received codeword is the single flipped bit — the receiver should INVERT bit position 5 to correct the error, exactly as the earlier HAMMING CODES section's syndrome-decoding rule states; no retransmission is needed at all, since Hamming codes, unlike CRC, support direct correction rather than mere detection.

3. GO-BACK-N RETRANSMISSION TRACE. Using Go-Back-N with a window size of 4, the sender transmits frames 0,1,2,3 in sequence. Frame 1 is lost in transit, but frames 0, 2, and 3 arrive successfully at the receiver. Trace what the receiver does with frames 2 and 3, and what the sender must eventually retransmit. Since Go-Back-N's receiver accepts frames ONLY in strict sequence order, frame 2 arrives while the receiver is still expecting frame 1 — frame 2 is therefore DISCARDED (not buffered), and likewise frame 3 is discarded upon arrival for the identical reason. Once the sender's timeout for frame 1 expires without an acknowledgment, it must retransmit frame 1 AND every frame after it that was within its window at the time — meaning frames 1, 2, and 3 ALL get retransmitted, even though 2 and 3 had already been transmitted correctly once; this directly illustrates Go-Back-N's own characteristic wasted-retransmission behavior described in the SLIDING-WINDOW PROTOCOLS section above.

4. SELECTIVE REPEAT VERSUS GO-BACK-N COMPARISON ON THE SAME SCENARIO. Repeat WP3's identical scenario (frame 1 lost, frames 0/2/3 arrive successfully), but now assume Selective Repeat is used instead. Determine what the receiver does with frames 2 and 3, and what the sender must retransmit. Under Selective Repeat, the receiver BUFFERS the out-of-order frames 2 and 3 upon their correct arrival (rather than discarding them), and sends individual acknowledgments for each. Once the sender's timeout for frame 1 expires, it retransmits ONLY frame 1 — frames 2 and 3 need NOT be retransmitted at all, since the receiver already has them safely buffered and will deliver all four frames, 0 through 3, to the layer above in the correct order once frame 1's retransmission successfully arrives and fills the gap. Comparing directly against WP3: Go-Back-N retransmitted 3 frames (1, 2, and 3) for this single lost frame, while Selective Repeat retransmitted only 1 frame (just frame 1) — a concrete, numeric illustration of the efficiency gap between the two protocols under exactly identical loss conditions.

5. ALOHA THROUGHPUT COMPARISON. A shared channel has raw capacity 1 Mbps. Using the standard theoretical maximum throughput figures, compute the maximum ACHIEVABLE throughput under pure ALOHA and under slotted ALOHA. Pure ALOHA: 1/(2e) × 1 Mbps ≈ 0.184 × 1 Mbps ≈ 184 kbps. Slotted ALOHA: 1/e × 1 Mbps ≈ 0.368 × 1 Mbps ≈ 368 kbps — confirming slotted ALOHA achieves EXACTLY double pure ALOHA's maximum achievable throughput on the identical shared channel, directly reflecting the halved vulnerable period slotting provides.

WORKED PROBLEMS, PART TWO

6. BINARY EXPONENTIAL BACKOFF RANGE COMPUTATION. A station experiences its THIRD consecutive collision transmitting the same frame under CSMA/CD. Determine the range of possible backoff values (in time slots) it will choose from. After the k-th collision, the backoff value is drawn from {0, 1, ..., 2^k − 1}. For k=3: 2^3 − 1 = 7, so the station selects uniformly at random from {0,1,2,3,4,5,6,7}, an EIGHT-value range — directly confirming that the range DOUBLES with each successive collision (a first collision would offer only {0,1}, a second {0,1,2,3}, and this third collision {0,...,7}), exactly the adaptive, contention-responsive widening the BINARY EXPONENTIAL BACKOFF section describes.

7. ETHERNET FRAME OVERHEAD COMPUTATION. An application sends 40 bytes of actual payload data over Ethernet. Compute the total size of the resulting Ethernet frame ON THE WIRE, accounting for padding and all header/trailer overhead (ignore the preamble and start-frame delimiter, which are not counted as part of the frame itself in most textbook conventions). Since 40 bytes is BELOW the 46-byte minimum payload, PADDING brings it up to exactly 46 bytes. Adding destination MAC (6) + source MAC (6) + type/length field (2) + padded data (46) + CRC (4) = 6+6+2+46+4 = 64 bytes total — this is, not coincidentally, EXACTLY Ethernet's own well-known minimum frame size, directly confirming that any payload smaller than 46 bytes is padded specifically to reach this same 64-byte minimum total frame size on the wire.

8. LEARNING BRIDGE FORWARDING-TABLE TRACE. A learning bridge has two ports, P1 and P2, with an initially EMPTY forwarding table. It receives, in order: a frame from source A arriving on P1 destined for B; then a frame from source B arriving on P2 destined for A; then a SECOND frame from source A arriving on P1, again destined for B. Trace the bridge's forwarding-table state and its forwarding decision at each step. STEP 1: source A is learned on P1 (table: {A→P1}); destination B is unknown, so the frame is FLOODED out every port except P1 (here, out P2). STEP 2: source B is learned on P2 (table: {A→P1, B→P2}); destination A IS now known (on P1), so the frame is forwarded DIRECTLY out P1 only, with no flooding. STEP 3: source A's entry is simply RE-CONFIRMED on P1 (table unchanged: {A→P1, B→P2}); destination B is now known (on P2), so this frame too is forwarded DIRECTLY out P2 only — illustrating that only the VERY FIRST frame toward a previously unseen destination needs to be flooded, with every subsequent frame benefiting from the table entries learned from earlier traffic.

9. HDLC VERSUS PPP RELIABILITY SCENARIO. A network engineer needs a point-to-point link where the DATA LINK layer itself must directly guarantee reliable, in-order delivery via its own built-in sequence numbering, without depending on any higher layer for this guarantee. Which of HDLC or PPP is directly suited to this requirement, and why? HDLC is directly suited, since its INFORMATION frames carry built-in sequence numbers supporting genuine sliding-window flow control and retransmission AT the data link layer itself, exactly as the earlier HDLC AND PPP section describes; basic PPP framing, lacking this sequence-numbering machinery, would instead need to rely on a higher layer (typically TCP) to provide the SAME reliability guarantee, making it unsuited to a requirement that specifically demands the guarantee live AT the data link layer itself.

10. FULL SYNTHESIS: DESIGNING A RELIABLE LAN SEGMENT. A small office network needs a shared Ethernet segment connecting 6 workstations, where occasional frame corruption due to electrical interference is a known concern, and the office also wants to segment its network into two physically separate wiring closets connected by a single interconnecting device. Identify, for each requirement, which mechanism from this chapter directly addresses it. For detecting corrupted frames caused by interference: the standard Ethernet frame's own trailing CRC (Frame Check Sequence) field, directly applying this chapter's own ERROR DETECTION section's CRC mechanism at the specific, standardized Ethernet frame level, catching essentially all burst errors up to the generator polynomial's own degree and the vast majority of longer ones. For connecting the two physically separate wiring closets into one logical network: a LEARNING BRIDGE (or an equivalent modern switch), which will automatically build its own forwarding table purely from observed traffic, requiring no manual configuration, and will avoid needlessly flooding traffic that belongs entirely within one closet across to the other, exactly as the BRIDGES AND SWITCHES section describes. For managing collisions among the six workstations sharing the same segment: CSMA/CD, with binary exponential backoff automatically adapting the retry timing to the actual, currently observed level of contention on the shared medium. This worked problem, much like the analogous full-synthesis problems appearing in the DBMS Indexing and DBMS Transactions chapters earlier in this book, illustrates that a genuinely realistic small-network design question typically requires correctly identifying and combining SEVERAL of this chapter's mechanisms at once, rather than applying any single mechanism in isolation.

11. HIDDEN-TERMINAL PROBLEM IDENTIFICATION. Three wireless stations, X, Y, and Z, are positioned so that X and Z can each communicate directly with the central access point Y, but X and Z are too far apart to sense each other's transmissions directly. Explain why plain CSMA (carrier sensing alone, without RTS/CTS) can fail here, and how CSMA/CA's RTS/CTS handshake specifically addresses this failure. Under plain CSMA, X might sense the medium as IDLE (since it cannot hear Z's ongoing transmission to Y) and begin transmitting to Y at the same time Z is already doing so, causing a COLLISION at Y that neither X nor Z can directly detect, since neither can sense the other's signal — this is precisely the HIDDEN TERMINAL problem named in the earlier CSMA/CA section. The RTS/CTS handshake solves this by having X first send a short RTS (Request to Send) frame to Y; Y then broadcasts a CTS (Clear to Send) response that BOTH X and Z can hear (since both are within direct range of Y, even though not of each other), and Z, upon hearing this CTS addressed to X, knows to defer its OWN transmission for the duration Y's response specifies — reserving the shared medium around the intended recipient Y, rather than relying on each transmitting station's own, potentially incomplete carrier-sensing range. This RTS/CTS reservation mechanism trades a small additional handshake overhead, incurred on every transmission attempt, for the elimination of an entire class of collisions that plain carrier sensing structurally cannot prevent on a wireless medium.

CARRYING THIS FORWARD

This chapter has examined the COMPLETE set of concerns the data link layer must address to turn a physical layer's own raw, error-prone, shared bit stream into a reliable, efficiently-utilized, hop-by-hop communication channel: FRAMING (delimiting the stream into discrete units), ERROR DETECTION and CORRECTION (parity, checksums, CRC, and Hamming codes), FLOW CONTROL (stop-and-wait and the sliding-window generalizations Go-Back-N and Selective Repeat), and MEDIUM ACCESS CONTROL (ALOHA and the CSMA family) for links where the medium itself must additionally be SHARED among multiple contending stations. The single most valuable connecting insight to carry forward is that every mechanism in this chapter exists to solve exactly one of three distinct problems — data corruption (error control), a fast sender overwhelming a slow receiver (flow control), or multiple stations contending for one shared medium (medium access control) — and correctly identifying WHICH of these three problems a given GATE question is actually describing is the fastest route to recognizing which specific mechanism, and which specific formula, actually applies.

KEY: the sliding-window protocols examined here (Go-Back-N and Selective Repeat) are, at their structural core, TRADING receiver-side complexity (buffering out-of-order arrivals) for retransmission EFFICIENCY — exactly the SAME kind of "more sophisticated bookkeeping purchased in exchange for less wasted work" trade-off this book has already encountered in the Databases Transactions chapter's own comparison of pessimistic (2PL) versus optimistic (validation-based) concurrency control, reinforcing that this specific engineering trade-off pattern recurs across entirely unrelated layers of a computing system, not merely within a single subject.

KEY: CSMA/CD's collision-detection strategy and CSMA/CA's collision-avoidance strategy mirror the SAME reactive-versus-proactive design-philosophy split already seen in the Databases Transactions chapter's own concurrency-control discussion (locking makes a transaction WAIT proactively to prevent a conflict, while timestamp ordering reactively REJECTS an operation only once an actual ordering violation is detected) — recognizing that "detect and recover after the fact" versus "prevent the problem from ever arising" is a recurring, cross-cutting engineering choice, appearing in contexts as different as database concurrency control and wireless medium access, is far more valuable than treating each domain's own specific instance of this choice as an unrelated, isolated fact.

KEY: the Hamming-code minimum-check-bits inequality (2^r ≥ m+r+1) and the earlier Databases Indexing chapter's own B+-tree order inequality (n ≤ (block+key)/(pointer+key)) are both examples of the SAME general problem-solving pattern — deriving a STRUCTURAL constraint directly from a SPACE or CAPACITY limitation, then solving that constraint (by algebraic floor computation in one case, by incremental trial in the other) for the specific parameter a question actually asks for — reinforcing, once again, that this book's recurring emphasis on re-deriving formulas from their underlying constraints, rather than memorizing final numeric answers, pays off identically across entirely different GATE subjects.

KEY: the Ethernet frame's own minimum-size padding requirement, the Hamming code's minimum-check-bits inequality from earlier in this chapter, and the earlier Databases Indexing chapter's own B+-tree order-computation inequality are all, at their structural core, instances of the SAME general pattern — a HARD physical or structural constraint (a collision-detection timing window, a syndrome's information-carrying capacity, a fixed block size) forces a MINIMUM or MAXIMUM value on some designed parameter, and correctly answering a GATE question about any of them requires re-deriving that constraint directly from first principles, rather than recalling a single memorized numeric answer that may not transfer to a question using different underlying parameters.

Before moving on, it is worth explicitly tracing why this chapter's own sections appear in the specific order they do, since that progression mirrors a genuine, chronological dependency among the concerns themselves. Framing had to come FIRST because every other mechanism in this chapter operates on already-delimited frames — error detection cannot check a frame's integrity until the frame's own boundaries are known, and flow control cannot count outstanding frames until frames themselves are well-defined units. Error detection and correction came next because a receiver must be able to trust the CONTENT of a frame before any higher-level decision (accepting it, buffering it, discarding it) can be made about that content. Flow control followed naturally from there, since it governs how many trusted, correctly-received frames may be outstanding at once between a single sender and receiver pair. Medium access control came last among the general mechanisms because it addresses a fundamentally different question — not how two already-connected endpoints manage their own data flow, but how many potentially unconnected stations share access to a single physical resource in the first place — before any of the earlier point-to-point mechanisms can even begin to operate on a shared medium. The concluding practical material — binary exponential backoff, Ethernet's concrete frame format, HDLC and PPP, and bridging — then showed how these four abstract concerns are actually realized in real, deployed link-layer technologies, tying the chapter's theoretical machinery to the concrete protocols a GATE question is most likely to describe by name.

A candidate who can trace this dependency chain — framing enables trust in frame boundaries, error control enables trust in frame content, flow control governs pacing between two trusted endpoints, and medium access control governs contention among many potential endpoints before any of the rest can apply — will find that even an unfamiliar-looking Data Link Layer question almost always reduces to identifying which single link in this chain it is actually probing, rather than requiring recall of some entirely separate, unconnected fact.

The next chapter in this sequence, on the NETWORK LAYER, builds on this chapter's own delivery guarantees (a data link only guarantees RELIABLE transfer across a single hop) to examine how packets are addressed, fragmented, and ROUTED across an entire, multi-hop, potentially heterogeneous internetwork — a genuinely different scope of problem from this chapter's own single-hop focus, but one that still relies, at every intermediate hop, on this chapter's own framing, error-detection, and medium-access mechanisms to actually move each packet safely across each individual link along its complete end-to-end path.
`
};
