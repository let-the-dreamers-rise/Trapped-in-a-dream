// Textbook chapter: Finite Automata and Regular Languages.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['toc-regular'] = {
  figs: [
    {
      id: 'nfa-dfa-blowup',
      caption: 'The subset-construction relationship: an n-state NFA can require as many as 2^n states in its minimal equivalent DFA. Most NFAs convert to far fewer, but the exponential bound is genuinely tight for some languages.',
      svg: '<svg viewBox="0 0 300 110" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><circle cx="70" cy="55" r="45"/><circle cx="220" cy="55" r="45"/></g><g font-size="10" fill="currentColor" text-anchor="middle"><text x="70" y="30">NFA</text><text x="70" y="60">n states</text><text x="220" y="30">DFA</text><text x="220" y="60">up to 2^n</text><text x="150" y="100" font-size="9">subset construction →</text></g></svg>'
    },
    {
      id: 'substring-dfa-shape',
      caption: 'Minimal DFA shape for "contains substring ab": one state per length of the longest matched prefix of ab, with the fully-matched state becoming an accepting sink.',
      svg: '<svg viewBox="0 0 280 90" width="100%" style="max-width:280px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><circle cx="40" cy="45" r="20"/><circle cx="140" cy="45" r="20"/><circle cx="240" cy="45" r="20"/><circle cx="240" cy="45" r="15"/></g><g font-size="9" fill="currentColor" text-anchor="middle"><text x="40" y="49">q0</text><text x="140" y="49">q1</text><text x="240" y="49">q2</text></g></svg>'
    }
  ],
  text: `
WHAT A "MACHINE" MEANS IN THIS SUBJECT

Every automaton this chapter studies is, underneath its formal notation, an answer to one single question: given a string presented one symbol at a time, using only a bounded, FIXED amount of memory (a finite set of internal states, nothing more), can the machine correctly decide whether that string belongs to some target language? The entire finite-automata theory that follows is the precise mathematical account of exactly what such a bounded-memory recognizer CAN and cannot do, and regular languages are, by definition, exactly the class of languages some such finite-memory machine can recognize.

FORMAL DEFINITIONS: DFA, NFA, AND EPSILON-NFA

1. A DETERMINISTIC FINITE AUTOMATON (DFA) is a 5-tuple M = (Q, Σ, δ, q0, F), where Q is a finite set of states, Σ is a finite input alphabet, δ: Q × Σ → Q is the TRANSITION FUNCTION (crucially, a TOTAL function — EXACTLY one transition exists for every state-symbol pair, never zero and never more than one), q0 ∈ Q is the start state, and F ⊆ Q is the set of accepting (final) states. The language ACCEPTED by M, written L(M), is the set of all strings w such that the EXTENDED transition function δ*(q0, w) lands in F.
2. A NONDETERMINISTIC FINITE AUTOMATON (NFA) relaxes the transition function to δ: Q × Σ → 2^Q — for a given state and input symbol, the machine may have ZERO, ONE, or SEVERAL possible next states available simultaneously. A string w is accepted if THERE EXISTS at least one computation PATH (one particular sequence of nondeterministic choices) that ends in an accepting state — even if many OTHER possible paths for that same string fail to reach one.
3. An ε-NFA further extends this by also allowing δ: Q × {ε} → 2^Q — transitions the machine may take WITHOUT consuming any input symbol at all. ECLOSE(q), the EPSILON-CLOSURE of state q, is the set of every state reachable from q using zero or more consecutive ε-transitions alone.
4. THE EQUIVALENCE THEOREM: DFAs, NFAs, and ε-NFAs are all EXACTLY equally expressive — every language accepted by any one of the three is accepted by some machine of each of the OTHER two kinds as well, all three defining PRECISELY the class of regular languages. This equivalence is proved constructively: SUBSET CONSTRUCTION converts any NFA into an equivalent DFA, and ε-ELIMINATION (folding each ε-closure directly into the ordinary transitions) converts any ε-NFA into an equivalent ordinary NFA.

GATE TRAP: forgetting that a DFA's transition function must be TOTAL — every single state needs a transition defined for EVERY symbol of the alphabet, with no missing entries — while an NFA is explicitly permitted to have states with NO transition at all on some symbol (an implicit "dead" outcome for that path) is a frequently tested distinction; a DFA description that appears to omit a transition for some state-symbol pair is either an error in the problem, or implicitly assumes a single, unstated TRAP (dead, non-accepting sink) state absorbing every such missing transition.

SUBSET CONSTRUCTION: BUILDING THE EQUIVALENT DFA

The SUBSET CONSTRUCTION algorithm converts an n-state NFA into an equivalent DFA whose states are SUBSETS of the NFA's own original state set Q — intuitively, the DFA's current "state" at any point is simply the ENTIRE SET of NFA states that COULD currently be occupied, given the input read so far, tracking every nondeterministic possibility simultaneously rather than committing to just one.

[[FIG:nfa-dfa-blowup]]

1. Since Q has n elements, its power set 2^Q has 2^n elements, so the resulting DFA has AT MOST 2^n states — but critically, this is only an UPPER bound; only the subsets actually REACHABLE starting from {q0} (or from ECLOSE(q0), for an ε-NFA) ever need to be constructed at all, and for many NFAs, only a small fraction of the full 2^n subsets turn out to be reachable.
2. THE BOUND IS TIGHT: the 2^n figure is not merely a worst-case artifact of a loose proof — there GENUINELY EXIST n-state NFAs whose minimal equivalent DFA requires EXACTLY 2^n states, with no smaller DFA able to accept the identical language. The canonical witness: the language of strings over {0,1} whose SYMBOL n POSITIONS FROM THE END is a 1 has a straightforward (n+1)-state NFA (nondeterministically GUESS which position is n-from-the-end, then count forward to confirm reaching the actual end), yet its minimal DFA needs 2^n states, since the DFA must deterministically remember the ENTIRE content of the last n symbols seen (a sliding window of n bits, hence 2^n distinct possible memory states) to know, once the string ends, whether that guessed position was correctly identified.
3. Practical exam-relevant bounds: DFA states ≤ 2^(NFA states) always; and NFA states ≤ DFA states ALWAYS as well, trivially, since any DFA is itself already a valid (deterministic) NFA, so no NFA recognizing the same language can ever need MORE states than the smallest DFA already provides.

GATE TRAP: assuming EVERY subset-construction conversion actually blows up toward the full 2^n bound is a serious overgeneralization — an NFA that happens to already BE essentially deterministic (each state having at most one transition per symbol) converts to a DFA of essentially the SAME size, one-to-one; the 2^n bound is the worst case that some carefully constructed languages genuinely achieve, not a typical or expected outcome for an arbitrary NFA presented in a problem.

GATE TRAP: the "k-th symbol from the end is 1" language and the seemingly similar "FIRST symbol is 1" language behave utterly differently despite superficial resemblance — "first symbol is 1" needs only 2 states in even its minimal DFA (check the first symbol, then ignore everything else forever), with NO exponential blowup at all, since a DFA never needs to REMEMBER anything about symbols once the first check is resolved; it is specifically the "K-TH FROM THE END" phrasing, requiring a SLIDING WINDOW of memory that must be continuously updated as new symbols arrive (since the machine cannot know in advance how many more symbols remain), that forces the exponential state count — recognising WHICH kind of positional condition a given language describes is the actual skill being tested, not a blanket "counting positions is always exponential" rule.

MINIMAL DFA STATE-COUNTING RECIPES

Certain families of languages recur often enough in GATE-style questions that memorising their MINIMAL DFA state-count RECIPE directly, rather than re-deriving from scratch each time, is a substantial time-saver.

1. MOD-k COUNTER LANGUAGES (e.g., "accept iff the number of 1's in the string is congruent to 0 modulo k"): need EXACTLY k states, one per residue class 0 through k−1, transitioning to the NEXT residue class on the counted symbol and looping in place on every OTHER symbol — this is the most direct possible finite-state encoding of a modular count, since "current residue mod k" is itself already a bounded quantity taking only k distinct values.
2. "CONTAINS SUBSTRING w" (with |w| = m): the minimal DFA needs EXACTLY m+1 states — one state for every possible LENGTH of the longest prefix of w matched by the input read so far (lengths 0 through m), with the m-th state (the fully-matched state) becoming an ACCEPTING SINK, since once w has appeared anywhere, the string is accepted regardless of whatever follows. The FALLBACK transitions (what happens on a mismatch partway through matching w) are built using exactly the same failure-function idea underlying the KMP string-matching algorithm.
3. "ENDS WITH w" (also |w| = m): ALSO needs exactly m+1 states by the identical prefix-matching construction, but with one crucial structural difference — the FINAL (fully-matched) state is NOT a sink here, since a later mismatch can genuinely break an already-matched suffix if the string continues past that point; the machine must keep tracking ongoing partial matches even after first reaching the accepting state, using w's own self-overlap structure to determine the correct fallback transition out of that state.

[[FIG:substring-dfa-shape]]

GATE TRAP: confusing "contains substring w" (accepting sink once matched, m+1 states) with "ends with substring w" (non-sink final state that must continue tracking overlaps, also m+1 states but with genuinely different outgoing transitions from the final state) is one of the single most common numerical mix-ups in this entire topic — both share the SAME state count by coincidence of the underlying construction, but the actual transition STRUCTURE from the final state differs in a way that changes which strings the two languages actually accept.

1. Worked example (state-count recipe applied): the minimal DFA for "contains the substring 101" over {0,1} needs exactly |101|+1 = 4 states (tracking matched-prefix lengths 0, 1, 2, 3), with the length-3 state being an accepting sink. The minimal DFA for "ends with 101" also needs 4 states, but its length-3 (accepting) state has an outgoing transition on '1' that must fall back to the length-2 state rather than looping on itself, since "...1011" still ends in a partial re-match toward another eventual "101" occurring later.

MYHILL-NERODE THEOREM: AN EXACT STATE-COUNTING TOOL

1. DEFINE the relation x ≡_L y (for strings x, y, with respect to a language L) to hold if and only if, for EVERY possible suffix z, xz ∈ L exactly when yz ∈ L — intuitively, x and y are "indistinguishable" with respect to L, since no future continuation can ever tell them apart in terms of whether the resulting full string belongs to L.
2. THE MYHILL-NERODE THEOREM: L is regular if and only if ≡_L partitions all strings into FINITELY many equivalence classes, and moreover, the NUMBER of such classes EQUALS EXACTLY the number of states in L's OWN minimal DFA — not merely an upper or lower bound, but an EXACT count.
3. This gives a genuinely alternative, and often FASTER, method for proving a language is NOT regular: exhibit an INFINITE set of pairwise-distinguishable strings (strings that can always be told apart by SOME suffix), which directly forces infinitely many equivalence classes, hence non-regularity — frequently a cleaner argument than working through the pumping lemma's own more elaborate case-splitting structure.

1. Worked example (non-regularity via Myhill-Nerode): let L = {0^n 1^n : n ≥ 0}. Consider the infinite family of strings 0^i for i = 0, 1, 2, 3, .... For any two distinct indices i ≠ j, choose the distinguishing suffix z = 1^i: then 0^i 1^i ∈ L (by definition of L, with equal counts), but 0^j 1^i ∉ L (since j ≠ i, the counts do not match). Since z distinguishes EVERY pair 0^i, 0^j with i ≠ j, all infinitely many strings 0^i lie in DIFFERENT equivalence classes, forcing infinitely many classes overall — by the Myhill-Nerode theorem, L is therefore NOT regular.
2. Worked example (exact state count via Myhill-Nerode): let L = strings over {0,1} containing an EVEN number of 1's. There are exactly TWO equivalence classes: [ε] (strings seen so far containing an even count of 1's) and [1] (an odd count so far) — any two strings with the same current parity of 1's are indistinguishable by any future suffix (since only the RUNNING parity, not the specific string, determines the eventual outcome), giving exactly 2 classes, matching the direct, obvious 2-state DFA construction exactly.

THE PUMPING LEMMA FOR REGULAR LANGUAGES

1. STATEMENT: if L is regular, there exists a constant p (the PUMPING LENGTH, which can always be taken to be AT LEAST the number of states in some DFA for L) such that EVERY string w ∈ L with |w| ≥ p can be decomposed as w = xyz satisfying THREE conditions simultaneously: |xy| ≤ p (the split point falls within the first p symbols), |y| ≥ 1 (the "pumped" middle piece is genuinely nonempty), and for EVERY i ≥ 0, the string xy^i z (y repeated i times, including i=0, meaning y removed entirely) is ALSO a member of L.
2. THE CORRECT PROOF-BY-CONTRADICTION TEMPLATE for showing a language L is NOT regular: (a) assume, for contradiction, that L IS regular, and let p be its guaranteed pumping length; (b) CHOOSE one specific string w ∈ L, with |w| ≥ p, chosen CLEVERLY (often expressed IN TERMS of p itself, such as w = 0^p 1^p) so that every possible split will be forced into an exploitable position; (c) the ADVERSARY, not you, then gets to choose ANY decomposition w = xyz satisfying |xy| ≤ p and |y| ≥ 1 — your argument must handle EVERY SUCH possible choice, not merely one convenient example; (d) show that for SOME value of i (commonly i=0, sometimes i=2), the resulting string xy^i z FAILS to belong to L, for EVERY possible adversarial split from step (c); (e) this contradicts the pumping lemma's own guarantee, so the original assumption (L is regular) must be false.

GATE TRAP: the pumping lemma provides only a NECESSARY condition for regularity, never a SUFFICIENT one — a language that HAPPENS to satisfy the pumping property is not thereby proven regular, since certain carefully constructed NON-regular languages can still be pumped successfully for reasons unrelated to actual regularity; the pumping lemma can ONLY be used to demonstrate NON-regularity (by finding a violation), and attempting to use it to demonstrate that some language IS regular is a fundamental logical error, regardless of how the pumping seems to "work out."

1. Worked example (pumping-lemma proof that L={ww : w ∈ {0,1}*} is not regular): assume L is regular with pumping length p. Choose w = 0^p 1 0^p 1 (length 2p+2), which lies in L since it has the form uu with u = 0^p 1. Any adversarial split with |xy| ≤ p is forced entirely within the FIRST block of p zeros, so y = 0^k for some k with 1 ≤ k ≤ p. Pumping down with i=0 removes exactly those k zeros from the FIRST block ONLY (leaving the second 0^p block untouched), producing 0^(p−k) 1 0^p 1 — the two halves of this string no longer have matching zero-counts (p−k versus p), so this pumped-down string is no longer of the required uu form, hence not in L — a contradiction for every possible adversarial split, proving L is not regular.

CLOSURE PROPERTIES OF REGULAR LANGUAGES

Regular languages enjoy the RICHEST set of closure properties among every language class this book's Theory of Computation sequence will cover — they are closed under union, intersection, complement, set difference, concatenation, Kleene star, Kleene plus, string reversal, homomorphism, inverse homomorphism, and quotient with any other language whatsoever.

1. CLOSURE UNDER COMPLEMENT specifically follows almost immediately from the DFA definition's own TOTALITY requirement — given a DFA for L, simply SWAP which states count as accepting and which do not (F becomes Q∖F), and the resulting machine accepts precisely the complement of L; this construction relies critically on the DFA being total (every string has SOME well-defined path to SOME state), which is exactly why this simple swap trick does NOT work directly on an NFA without first converting it to an equivalent DFA.
2. CLOSURE UNDER INTERSECTION follows from a PRODUCT CONSTRUCTION: given DFAs M1 = (Q1,Σ,δ1,q1,F1) and M2 = (Q2,Σ,δ2,q2,F2) for languages L1 and L2, build a new DFA whose states are ORDERED PAIRS (p,q) ∈ Q1×Q2, transitioning both components in lockstep on each input symbol, and accepting a pair (p,q) exactly when BOTH p ∈ F1 AND q ∈ F2 — this SAME product-construction machine, with its acceptance condition changed to "EITHER p ∈ F1 OR q ∈ F2," equally proves closure under UNION.

KEY: recognising that intersection, union, AND complement can all be handled through variations of essentially the SAME product-automaton idea (run two machines in lockstep, then combine their two accept/reject verdicts using the appropriate Boolean operation) is far more useful than memorising three separate, seemingly unrelated closure proofs — this single construction pattern, adjusted only in its final acceptance-condition Boolean logic, is the common thread underlying nearly this entire closure-properties list, and this richness (regular languages closed under literally every common operation) stands in DIRECT, deliberate contrast to context-free languages, covered in the very next chapter, which will be shown to FAIL closure under intersection and complement specifically.

DFA MINIMIZATION: THE PARTITION-REFINEMENT ALGORITHM

Given ANY DFA, there is always a UNIQUE (up to state renaming) minimal equivalent DFA with the FEWEST possible states — and a standard algorithm, based on iteratively REFINING a partition of the state set, computes it directly.

1. INITIAL PARTITION: split all states into exactly two groups — the accepting states F, and the non-accepting states Q∖F — since these two groups are trivially DISTINGUISHABLE from one another (one group accepts ε as a suffix continuation from that state, the other does not, in the degenerate zero-length-suffix sense).
2. REFINEMENT STEP: repeatedly examine each current group, and SPLIT it further whenever two states within that SAME group behave differently — that is, whenever some single input symbol sends them to states that currently lie in DIFFERENT groups. Two states remain grouped together only if, for EVERY symbol, both transition into the SAME current group as each other.
3. TERMINATION: repeat the refinement step until no group splits any further in an entire pass — at that point, the resulting groups are EXACTLY the Myhill-Nerode equivalence classes for the language, and merging each group into a single state produces the minimal DFA directly.
4. UNREACHABLE STATE ELIMINATION: before (or as part of) minimization, any state NOT reachable from q0 by any input string whatsoever must first be deleted entirely — an unreachable state can never affect L(M) in any way, so retaining it would only inflate the DFA's state count without changing the language recognized, directly contradicting the goal of finding the truly MINIMAL machine.

1. Worked example (minimization by partition refinement): a 4-state DFA over {0,1} has states {A,B,C,D}, A the start state, D the only accepting state, with δ(A,0)=B, δ(A,1)=C, δ(B,0)=A, δ(B,1)=D, δ(C,0)=D, δ(C,1)=A, δ(D,0)=D, δ(D,1)=D. Initial partition: {A,B,C} (non-accepting) and {D} (accepting). Check {A,B,C} for splits: on symbol '1', A→C (stays in {A,B,C}), B→D (leaves to {D}), C→A (stays in {A,B,C}) — B behaves differently from A and C on symbol '1', so {A,B,C} splits into {A,C} and {B}. New partition: {A,C}, {B}, {D}. Check {A,C} again: on symbol '0', A→B (in group {B}), C→D (in group {D}) — A and C now land in DIFFERENT groups on symbol '0', so {A,C} splits further into {A} and {C}. Final partition: {A}, {B}, {C}, {D} — all four original states turn out to be pairwise distinguishable, so this DFA was ALREADY minimal, with no states left to merge.

GATE TRAP: applying the partition-refinement algorithm but stopping the refinement process TOO EARLY (after only one pass, without checking whether an already-split group might split AGAIN based on the newly refined partition from that same pass) produces an INCORRECT, over-merged result — the algorithm must be repeated until a FULL pass produces NO further splits at all, exactly as the worked example above required a SECOND refinement pass (splitting {A,C} further into {A} and {C}) after the first pass had already produced one split.

REGULAR EXPRESSIONS AND THOMPSON'S CONSTRUCTION

Every regular expression can be mechanically converted into an equivalent ε-NFA via THOMPSON'S CONSTRUCTION, giving a direct, constructive proof that every language describable by a regular expression is indeed regular (the "regex implies regular language" half of Kleene's theorem, whose other half — that every regular language has SOME describing regex — is proved separately via the state-elimination method below).

1. BASE CASES: the regex ∅ becomes a 2-state ε-NFA with no path from start to accept at all (accepting nothing); the regex ε becomes a 2-state machine connected by a single ε-transition (accepting only the empty string); the regex for a single symbol a becomes a 2-state machine with one transition labeled a from start to accept.
2. UNION (r+s): build Thompson machines for r and s separately, add a NEW start state with ε-transitions into BOTH of their individual start states, and a NEW single accept state reached via ε-transitions from BOTH of their individual accept states.
3. CONCATENATION (rs): build Thompson machines for r and s separately, and connect r's accept state directly to s's start state via a single ε-transition, with the COMBINED machine's start being r's own start and its accept being s's own accept.
4. KLEENE STAR (r*): build a Thompson machine for r, wrap it with a NEW start state and a NEW accept state connected directly to each other by ε (handling the zero-repetitions, empty-string case), and add an ε-transition from the new start into r's own start, PLUS an ε-transition from r's own accept BACK to r's own start (handling repeated iterations), and finally an ε-transition from r's own accept forward to the new accept state (handling the "stop after some finite number of repetitions" case).

KEY: Thompson's construction, mechanical as it appears, embodies the SAME general principle already used throughout this book's own digital-logic chapters for building complex combinational circuits out of small, individually simple, well-understood building blocks wired together according to a fixed compositional recipe — here, the "building blocks" are tiny ε-NFA fragments for each regex operator, and the "wiring recipe" is a small, fixed set of ε-transition patterns (for union, concatenation, and star) that combine any two ALREADY-CORRECT sub-machines into one larger, ALSO-CORRECT machine, letting an arbitrarily complicated regex be handled purely by recursively decomposing it into these three base cases.

STATE ELIMINATION: FROM A DFA/NFA BACK TO A REGULAR EXPRESSION

The REVERSE direction — starting from a finite automaton and producing an EQUIVALENT regular expression — uses the STATE ELIMINATION method, which generalizes ordinary transition labels to allow entire regular expressions as edge labels, then progressively removes intermediate states one at a time.

1. SETUP: introduce a single new start state with an ε-edge into the machine's original start state, and a single new accept state with ε-edges IN from every one of the machine's original accepting states — ensuring there is now exactly ONE start and exactly ONE accept state overall, a necessary precondition for the elimination process to terminate cleanly.
2. ELIMINATION STEP: to remove some intermediate state q (any state other than the designated overall start or accept state), for EVERY pair of edges IN to q (labeled r1) and OUT of q (labeled r2), plus q's own self-loop (labeled r3, or ∅ if q has none), add a DIRECT edge from that in-neighbor to that out-neighbor labeled r1(r3)*r2 — capturing every path that would have passed THROUGH q, expressed now as a single combined regex — then delete q and all its original edges entirely.
3. TERMINATION: repeat the elimination step until only the designated start and accept states remain, connected by a SINGLE edge — the regex labeling that final edge is the regular expression describing L(M) in its entirety.

1. Worked example (state elimination, small case): a 2-state DFA has states {q0 (start), q1 (accept)}, with δ(q0,a)=q1, δ(q0,b)=q0, δ(q1,a)=q1, δ(q1,b)=q0. Add a new overall start S with ε→q0, and a new overall accept T with q1→ε→T. Eliminate q0 first: in-edges to q0 are S→q0 (label ε) and q1→q0 (label b); out-edges from q0 are q0→q1 (label a) and q0's own self-loop on b; q0 has no self-loop of its own here actually — recomputing carefully, q0's only self-loop is on 'b' (δ(q0,b)=q0), so r3=b. Combining S's path through q0: ε(b)*a = b*a, giving a new direct edge S→q1 labeled b*a. Combining q1's own path back through q0 (the b-loop from q1 to q0 then back out): b(b)*a = bb*a, added as a new self-loop-like edge on q1's incoming side, meaning q1 gains a new "return path" labeled bb*a feeding back into itself when combined with its EXISTING direct self-loop on 'a'. The final combined self-loop on q1 becomes a + bb*a = a+bb*a, and the edge into T remains ε, giving the overall regex b*a(a+bb*a)* for this small machine — while this final expression looks more complex than the original transition table, it is a fully mechanical, guaranteed-correct translation, illustrating why state elimination, though completely reliable, typically produces regexes considerably LESS compact than what a human might construct by inspection for the same simple language.

GATE TRAP: forgetting to first reduce a machine to EXACTLY one start state and EXACTLY one accept state (by adding the two new ε-connected states as described above) before beginning state elimination is a common procedural error — attempting to eliminate states directly against an ORIGINAL machine with multiple accepting states produces an ill-defined or incomplete final expression, since the elimination process is only guaranteed to terminate cleanly toward a SINGLE final combined edge.

REGULAR GRAMMARS: THE GRAMMAR-BASED CHARACTERIZATION

A third equivalent characterization of the regular languages, alongside finite automata and regular expressions, is given by RIGHT-LINEAR (or equivalently LEFT-LINEAR) grammars — a language is regular if and only if it is generated by some grammar of one of these two restricted forms.

1. A RIGHT-LINEAR GRAMMAR restricts every production to one of exactly two forms: A → aB (a single terminal followed by AT MOST one nonterminal, and that nonterminal must appear at the very END of the right-hand side) or A → a (a single terminal alone) — the nonterminal, whenever present at all, is always the RIGHTMOST symbol.
2. A LEFT-LINEAR GRAMMAR is the exact mirror image: every production has the form A → Ba or A → a, with the nonterminal, whenever present, always the LEFTMOST symbol.
3. THE EQUIVALENCE: right-linear grammars, left-linear grammars, and finite automata all generate/accept EXACTLY the same class of languages — the regular languages — and the direct correspondence between a right-linear grammar and an NFA is immediate: each nonterminal corresponds to a state, and each production A → aB corresponds directly to a transition δ(A,a) ∋ B, with a production A → a corresponding to a transition into a (possibly new) accepting state on symbol a.

GATE TRAP: MIXING left-linear and right-linear productions within the SAME single grammar (e.g., some productions of form A→aB and others of form A→Ba within one grammar) does NOT, in general, still describe a regular language — the equivalence with regular languages holds specifically for a grammar that is ENTIRELY right-linear or ENTIRELY left-linear throughout, not for an arbitrary mixture of the two forms, which can in fact describe non-regular languages depending on the specific mixture.

1. Worked example (right-linear grammar to NFA): the right-linear grammar S → aA | bS, A → aA | b directly corresponds to an NFA with states {S,A} (S the start state), transitions δ(S,a)∋A, δ(S,b)∋S, δ(A,a)∋A, and A's production A→b (a terminal alone) meaning δ(A,b) leads to a NEW accepting state (or, if b were instead written as a self-loop production ending the derivation, A itself could be marked accepting directly for the b-only production case) — the grammar generates exactly the language b*a(a*b), matching the language a straightforward reading of the NFA's own transition structure would also produce directly.

ARDEN'S THEOREM: SOLVING REGULAR-EXPRESSION EQUATIONS DIRECTLY

A further, purely algebraic technique for converting a finite automaton into a regular expression uses ARDEN'S THEOREM, which solves systems of regular-expression EQUATIONS (one equation per state, expressing that state's own language in terms of its neighbors') directly, without state elimination's own graph-surgery approach.

1. ARDEN'S THEOREM STATEMENT: given an equation of the form R = Q + RP (where R is the unknown language being solved for, and P, Q are already-known regular expressions, with P NOT containing the empty string ε), the UNIQUE solution is R = QP*.
2. APPLICATION METHOD: for each state qi in a machine, write an equation expressing qi's own "language of strings reaching an accepting state starting from qi" in terms of the SAME quantity for every state qi transitions to, plus an ε term if qi itself is accepting — then solve the resulting system of simultaneous equations using ordinary substitution, applying Arden's theorem wherever a state's equation refers back to itself (a genuine self-loop dependency, of exactly the R = Q + RP form Arden's theorem is built to resolve).

1. Worked example (Arden's theorem, tiny system): a 2-state machine has states {A (start), B (accepting)}, with A's own equation A = εq0-marker-omitted... more concretely: suppose the transition equations are A = aA + bB + ε (A is itself also accepting, contributing the ε term, plus self-loop on 'a' and incoming dependency on B via 'b') and B = aA (B's only incoming path is from A via symbol 'a', with B itself not looping). Substituting B's equation into A's: A = aA + b(aA) + ε = aA + baA + ε = (a+ba)A + ε. This is now exactly Arden's form R = Q + RP with R=A, Q=ε, P=(a+ba), so A = ε(a+ba)* = (a+ba)*, and then B = aA = a(a+ba)*, giving the accepting language directly as B's own solved expression.

KEY: minimization (via partition refinement), regex-to-automaton (via Thompson's construction), automaton-to-regex (via BOTH state elimination and Arden's theorem), and grammar-to-automaton (via the right-linear correspondence) are FOUR entirely separate-looking algorithms that together prove ONE single, unifying fact — finite automata, regular expressions, and right-linear (or left-linear) grammars are three completely INTERCHANGEABLE ways of describing the EXACT SAME class of languages, with a fully mechanical, algorithmic translation available in every direction between any two of the three representations; recognising this three-way equivalence as the actual PAYOFF of this chapter's algorithmic machinery, rather than viewing each conversion procedure as an unrelated standalone technique to separately memorise, is what turns this material into a coherent whole.

KEY: Arden's theorem and state elimination solve the EXACT SAME underlying problem (automaton to regular expression) via two structurally different but equally valid routes — state elimination works DIAGRAMMATICALLY, by directly manipulating the automaton's own graph structure one state at a time, while Arden's theorem works ALGEBRAICALLY, by setting up and solving a system of regular-expression equations using ordinary substitution; a GATE question may present either approach, or ask which is more suitable for a specific small machine, and recognising both as valid, equally correct alternative paths to the identical final answer (rather than assuming one is somehow the single "correct" method) avoids needless confusion when a solution presented using one method looks superficially different from a solution obtained via the other.

TWO FURTHER GATE TRAPS ON THE MINIMIZATION AND EQUIVALENCE MACHINERY

1. GATE TRAP: a common assumption that the FIRST partition-refinement pass always fully determines the final minimal DFA, without checking for a SECOND (or third, or further) pass, is a serious procedural shortcut that produces an under-refined, INCORRECT result whenever the language actually requires more distinguishing power than a single pass reveals — as the minimization worked example above demonstrated directly, where the SECOND pass revealed that {A,C}, which had SURVIVED the first pass intact, still needed splitting further into {A} and {C} individually; the algorithm's correctness depends entirely on iterating to a FULL fixed point, not merely running for some fixed, small number of passes chosen in advance.
2. GATE TRAP: assuming that TWO automata with the SAME number of states must therefore accept different languages (or, conversely, that automata with DIFFERENT state counts must accept different languages) is entirely unfounded — state count alone says nothing about which specific language is accepted; two entirely different-looking DFAs, quite possibly built by two different people working independently from the same informal language description, can both be genuinely minimal and yet still differ superficially in their state LABELING while remaining, after minimization, ISOMORPHIC (identical up to consistent state renaming) if and only if they truly accept the exact same language — the only fully rigorous way to confirm two automata are equivalent is to minimize both and check for this exact isomorphism, or equivalently, to construct their symmetric difference (via the product construction covered earlier) and confirm it accepts the empty language.

WORKED PROBLEMS

1. MINIMAL DFA FOR "CONTAINS ab". Construct the minimal DFA over {a,b} for strings containing "ab" as a substring, and state its size. States: q0 (start, no progress toward "ab"), q1 (just saw 'a'), q2 (matched "ab", accepting sink). Transitions: δ(q0,a)=q1, δ(q0,b)=q0, δ(q1,a)=q1, δ(q1,b)=q2, δ(q2,a)=q2, δ(q2,b)=q2. Total states = |ab|+1 = 3, matching the "contains substring" recipe exactly.

2. SUBSET CONSTRUCTION SIZE. An NFA has 4 states, and by direct inspection, only 6 of its 2^4=16 possible subsets are ever actually reachable from the start subset. State the resulting DFA's exact size, and explain why this does not contradict the 2^n upper bound. The resulting DFA has exactly 6 states — the 2^4=16 figure is only an UPPER bound (the full power set), and only REACHABLE subsets need ever be constructed; this NFA happens to reach far fewer than the worst case, which is entirely consistent with the bound being an upper bound rather than a guaranteed exact value.

3. MOD-3 COUNTER STATE COUNT. Build (conceptually) the minimal DFA accepting binary strings where the number of 1's is congruent to 2 modulo 3, and state its size. Exactly 3 states (residues 0, 1, 2 mod 3), with state "2" being the unique accepting state, transitioning residue→(residue+1) mod 3 on a '1' symbol and looping on a '0' symbol.

4. MYHILL-NERODE NON-REGULARITY PROOF. Prove L = {a^n b^n c^n : n ≥ 0} is not regular using Myhill-Nerode. Consider the infinite family a^i for i=0,1,2,.... For i≠j, choose the distinguishing suffix z = b^i c^i: then a^i b^i c^i ∈ L, but a^j b^i c^i ∉ L (the a-count j does not match the required b/c-count i). This gives infinitely many pairwise-distinguishable strings, hence infinitely many equivalence classes, so L is not regular.

5. PUMPING LEMMA APPLICATION. Prove L = {0^i 1^j : i > j} is not regular. Assume regular with pumping length p. Choose w = 0^(p+1) 1^p, which lies in L since p+1 > p. Any split with |xy| ≤ p forces xy entirely within the 0-block, so y = 0^k for some k ≥ 1. Pumping UP with i=2 gives 0^(p+1+k) 1^p, which still satisfies (p+1+k) > p — this particular pumping does not immediately fail, so instead pump DOWN with i=0: 0^(p+1−k) 1^p. Since 1 ≤ k ≤ p, we have p+1−k ≥ 1, and crucially p+1−k could equal or fall below p (whenever k ≥ 1), violating i > j whenever p+1−k ≤ p, i.e., whenever k ≥ 1 — which is ALWAYS true since y is nonempty by the pumping lemma's own guarantee. So pumping down always produces a string violating i>j, contradicting L's regularity.

6. CLOSURE PROPERTY IDENTIFICATION. Given regular languages L1 and L2, is L1∩L2 guaranteed regular? Is L1's complement (relative to Σ*) guaranteed regular? Both are guaranteed regular — regular languages are closed under BOTH intersection (via the product-DFA construction) and complement (via the accept/non-accept state swap on a DFA), unlike context-free languages, which fail both these closures in general.

7. DFA VS NFA STATE-COUNT RELATIONSHIP. An NFA accepting some language L has 5 states. What is the SMALLEST possible number of states its minimal equivalent DFA could have, and the LARGEST? Smallest possible: as few as 1 state, in principle (if the NFA happens to accept either Σ* or ∅, both of which have trivial 1-state minimal DFAs, regardless of how many states the original NFA happened to use). Largest possible: up to 2^5=32 states, by the tight subset-construction bound — both extremes are achievable depending on the specific language involved, illustrating that the NFA's own state count alone does not pin down the DFA's exact size, only an upper bound on it.

8. "ENDS WITH" VS "CONTAINS" DFA COMPARISON. Both "contains 11" and "ends with 11" over {0,1} need |11|+1=3 states. Describe the key transition-structure difference between their respective final (matched) states. For "contains 11", the matched state is a SINK (both '0' and '1' loop back to itself, since once "11" has appeared anywhere, the string is permanently accepted regardless of what follows). For "ends with 11", the matched state's transition on '0' must fall BACK to the initial (no-match) state (since a trailing '0' immediately breaks any "ends with 11" claim), while its transition on '1' loops on itself (since "...111" still genuinely ends in "11").

9. REGEX SIMPLIFICATION. Simplify the regular expression (a+b)*a(a+b)* + (a+b)*b(a+b)* to something clearly recognizable, using regex identities. The first term matches any string CONTAINING at least one 'a' somewhere; the second matches any string containing at least one 'b' somewhere. Their union is every string that contains an 'a' OR a 'b' anywhere — which is every NONEMPTY string over {a,b} (the only string excluded is ε, the empty string, which contains neither symbol) — simplifying to (a+b)(a+b)*, i.e., (a+b)+ in Kleene-plus notation.

10. EXPONENTIAL BLOWUP WITNESS LANGUAGE. For the language "the 3rd symbol from the end is 1" over {0,1}, state the NFA size (via the guess-and-verify construction) and the minimal DFA size, and briefly justify each. NFA: 4 states (guess the position, then count forward exactly 3 more symbols including the guessed one, to confirm reaching the string's actual end) — one state per "how many more symbols confirmed since the guess" stage, 0 through 3. Minimal DFA: 2^3=8 states, since the DFA must deterministically remember the exact content of the last 3 symbols seen at every point (a sliding window over 3 bits, hence 8 possible remembered windows), confirming the tight exponential bound for this specific k=3 instance.

11. ε-NFA TO NFA VIA EPSILON-CLOSURE. An ε-NFA state q has ECLOSE(q) = {q, r, s}, where only state s is accepting. After eliminating ε-transitions, should the resulting plain-NFA state q itself become an accepting state? Yes — since ECLOSE(q) contains the accepting state s, reaching q in the ε-NFA (with zero further input needed) already constitutes a valid acceptance path via the ε-moves alone, so q must be marked accepting in the ε-eliminated NFA to preserve exactly the same accepted language.

12. UNION VIA PRODUCT CONSTRUCTION. Two 2-state DFAs M1 (states p0,p1, p1 accepting) and M2 (states q0,q1, q1 accepting) recognize L1 and L2 respectively. State the number of states in the product-construction DFA for L1∪L2, and its accepting condition. The product DFA has 2×2=4 states: (p0,q0), (p0,q1), (p1,q0), (p1,q1). For UNION, a pair state is accepting if EITHER component is accepting: so (p0,q1), (p1,q0), and (p1,q1) are all accepting (any pair including at least one of p1 or q1), while only (p0,q0) is rejecting.

13. DFA MINIMIZATION APPLIED. A 3-state DFA over {0,1} has states {P,Q,R}, P the start state and only accepting state, with δ(P,0)=Q, δ(P,1)=R, δ(Q,0)=P, δ(Q,1)=R, δ(R,0)=R, δ(R,1)=R. Apply partition refinement and state whether this DFA is already minimal. Initial partition: {P} (accepting), {Q,R} (non-accepting). Check {Q,R}: on '0', Q→P (group {P}), R→R (group {Q,R}) — Q and R land in different groups, so {Q,R} splits into {Q} and {R}. Final partition: {P},{Q},{R} — all three states pairwise distinguishable, so this DFA is already minimal, with no states to merge.

14. ARDEN'S THEOREM ON A SELF-LOOPING STATE. A single-state machine has state A, which is both the start state and the only accepting state, with a self-loop on symbol 'a' and no other transitions. Write A's own equation and solve it via Arden's theorem. A = aA + ε (the self-loop contributes the aA term, and A being accepting itself contributes the ε term for the zero-symbols-consumed case). This is Arden's form R=Q+RP with R=A, Q=ε, P=a, so A = εa* = a* — correctly matching the obvious observation that this single-state, self-looping machine accepts exactly the language of all strings consisting of any number (including zero) of the symbol 'a'.

CARRYING THIS FORWARD

This chapter opens the Theory of Computation sequence with the WEAKEST (least computationally powerful) class of languages this sequence will study, and every subsequent chapter's own language class will be defined, in large part, by exactly WHAT ADDITIONAL kind of memory or computational power is added beyond a finite automaton's own strictly bounded state set — a pushdown automaton (the very next chapter) adds an unbounded STACK, letting it recognize context-free languages that a finite automaton provably cannot (the {0^n 1^n : n≥0} example proved non-regular above being the canonical such language); a Turing machine (two chapters ahead) adds an unbounded, freely READ-AND-WRITE tape, recognizing an even larger class still. Understanding finite automata's own PRECISE limitations — captured exactly by the pumping lemma and by the Myhill-Nerode theorem's exact equivalence-class count — is therefore not merely this chapter's own topic in isolation, but the essential BASELINE against which every subsequent chapter's own added computational power will be measured and justified.

KEY: the subset construction (NFA to DFA), the product construction (intersection and union of DFAs), and the ε-elimination construction (ε-NFA to NFA) are three SEPARATE algorithms, but all three share the SAME underlying engineering principle already familiar from this book's own digital-logic chapters — track a LARGER, more complex piece of STATE (a subset of states; a pair of states; a set of states reachable via ε-moves) using an equivalent, purely deterministic finite machine, precisely because any computation involving only finitely many possible configurations can ALWAYS, in principle, be re-expressed as a plain, ordinary DFA over those configurations, exactly as this book's own sequential-circuits chapter demonstrated that any finite-state behavior whatsoever can be realized directly as a state-transition table.

The Myhill-Nerode theorem deserves one final emphasis before moving on: unlike the pumping lemma, which only ever proves NON-regularity, Myhill-Nerode is a genuine IF-AND-only-IF characterization — finitely many equivalence classes is not merely necessary but sufficient for regularity as well, and the exact class count directly gives the exact minimal DFA size, making it, in a real sense, the single most POWERFUL individual tool introduced in this entire chapter — a fact worth carrying forward explicitly into any future encounter with a language whose regularity status is not immediately obvious from inspection alone, since reaching first for Myhill-Nerode's exact equivalence-class-counting approach, rather than immediately reaching for the pumping lemma's more elaborate adversarial-split machinery, is very often the faster and cleaner path to a correct, complete answer.

A final practical habit worth carrying forward from this chapter into every remaining one in this Theory of Computation sequence: whenever a new machine model or a new grammar restriction is introduced, the single most useful question to ask immediately is "what class of languages does this model characterise exactly, and by what equivalence proof?" — exactly the question this chapter has answered three separate but interlocking ways for finite automata (via the automaton-to-regex-to-grammar triangle of equivalences developed across this entire chapter). A machine model's raw definition, taken alone, rarely reveals its true expressive power at a glance; it is only once that model has been shown EQUIVALENT to some other, perhaps more intuitively graspable representation (a regex, a grammar, or a closed-form combinatorial description) that its actual capabilities and, just as importantly, its actual LIMITATIONS become genuinely clear and usable for solving concrete problems. Every remaining chapter in this sequence will introduce its own new machine model specifically to overcome some named, precisely identified limitation of the model covered immediately before it, and carrying forward this exact habit of first asking about equivalent characterizations, rather than treating each new model as an isolated definition to be accepted purely on its own terms, is the single most transferable lesson this particular chapter leaves behind for everything that follows.

It is also worth stating plainly, as a bridge into what comes next, exactly WHERE this chapter's machinery genuinely stops working. Every algorithm developed here — subset construction, minimization, Thompson's construction, state elimination, Arden's theorem, the right-linear grammar correspondence — depends critically on the underlying machine having only FINITELY many possible configurations to reason about at any point. The moment a language requires even a single unboundedly growing piece of memory to recognize correctly, such as counting an unbounded, unpredictable number of occurrences of one symbol before matching it against occurrences of another, every one of this chapter's tools becomes simply inapplicable, not merely harder to apply. This is exactly the boundary the next chapter's pushdown automata are built to cross, and recognizing precisely where and why that boundary falls is the most direct, concrete payoff this entire chapter offers going forward. Keeping that boundary in mind while working through the next chapter's own material — asking, for any given new language, whether a bounded amount of memory could ever suffice to track what needs tracking, or whether some genuinely unbounded count is unavoidable — turns the pushdown-automata chapter's own added stack mechanism from an arbitrary new definition into a directly motivated, clearly necessary extension of exactly the limitation this chapter has now carefully established and proved. This forward-looking habit of connecting a new model's own capabilities directly back to the specific gap it was built to fill is, once again, simply this book's own consistent approach to every subject it covers, applied here at the very boundary between two major chapters, and it is exactly the one single habit most worth keeping close at hand from this point onward.
`
};
