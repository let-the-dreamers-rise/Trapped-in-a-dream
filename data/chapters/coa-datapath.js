// Textbook chapter: Datapath and Control Unit Design.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['coa-datapath'] = {
  figs: [
    {
      id: 'datapath-control-split',
      caption: 'The datapath (registers, ALU, buses, muxes — where data actually flows) versus the control unit (the circuitry, hardwired or microprogrammed, that asserts the control signals telling the datapath what to do this cycle).',
      svg: '<svg viewBox="0 0 300 140" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><rect x="20" y="20" width="120" height="100"/><rect x="170" y="20" width="110" height="100"/></g><g font-size="10" fill="currentColor" text-anchor="middle"><text x="80" y="15">DATAPATH</text><text x="80" y="50">registers</text><text x="80" y="65">ALU</text><text x="80" y="80">buses / muxes</text><text x="225" y="15">CONTROL UNIT</text><text x="225" y="50">opcode in</text><text x="225" y="70">control signals out</text></g><line x1="140" y1="70" x2="170" y2="70" stroke="currentColor" stroke-width="1.2" marker-end="url(#cparrow)"/><defs><marker id="cparrow" markerWidth="7" markerHeight="7" refX="5" refY="3.5" orient="auto"><path d="M7,3.5 L0,0 L0,7 Z" fill="currentColor"/></marker></defs></svg>'
    },
    {
      id: 'micro-sequencing-techniques',
      caption: 'Three microinstruction sequencing techniques trade address-field width for flexibility: an explicit next-address field (widest, most flexible), default increment-plus-branch (narrower, sequential by default), and opcode mapping (a lookup table converts the opcode directly to a starting address).',
      svg: '<svg viewBox="0 0 300 110" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g font-size="10" fill="currentColor" font-family="monospace"><text x="10" y="20">[signals | next-addr] — explicit, widest</text><text x="10" y="50">[signals] , CAR=CAR+1 (default) or branch</text><text x="10" y="80">opcode → mapping ROM → start address</text></g></svg>'
    }
  ],
  text: `
WHY THIS CHAPTER, AND HOW IT CONNECTS TO THE PREVIOUS ONE

The previous chapter described instructions PURELY at the level of their encoded bits — opcodes, operand fields, addressing modes. This chapter answers the natural next question: how does a physical processor actually CARRY OUT what an instruction's bits specify? The answer splits cleanly into two cooperating halves — the DATAPATH (the actual registers, ALU, and buses that data physically flows through) and the CONTROL UNIT (the circuitry that decides, cycle by cycle, exactly which of the datapath's components should be active and how they should be connected together for this specific instruction, at this specific point in its execution). Every topic in this chapter — register transfer notation, hardwired versus microprogrammed control, microinstruction sequencing, control-store sizing, and clock-period timing — is really just filling in the mechanical details of exactly how the fetch-decode-execute cycle from the previous chapter is physically realised in hardware.

THE DATAPATH AND REGISTER TRANSFER LANGUAGE

The DATAPATH is the network of REGISTERS, BUSES, the ALU, and MULTIPLEXERS that data actually flows THROUGH during instruction execution. The CONTROL UNIT is separate circuitry (built either as fixed combinational logic, or as a small stored program of its own) that ASSERTS the specific control signals — which register loads this cycle, which ALU operation is selected, which bus a value is routed onto — needed to make the datapath perform the CORRECT operation at each step.

[[FIG:datapath-control-split]]

REGISTER TRANSFER LANGUAGE (RTL) is the standard NOTATION for describing exactly what happens to the datapath during one step, written as statements like R1 ← R2+R3 (the ALU adds R2 and R3, the result is loaded into R1) or MAR ← PC (the Memory Address Register is loaded from the Program Counter). Each RTL statement that can complete within a single clock period is called one MICROOPERATION; a group of microoperations that all happen together, gated by the same control signal within one clock pulse, is called one CONTROL STEP.

THE INSTRUCTION CYCLE IN RTL: the basic cycle is FETCH → DECODE → OPERAND FETCH → EXECUTE → WRITE-BACK (with an added MEMORY-ACCESS stage for memory-reference instructions specifically) — exactly the five-stage breakdown already introduced in the previous chapter, now written out explicitly in RTL notation for its FETCH portion: MAR←PC (stage the program counter's value into the memory address register); MDR←M[MAR] (read memory at that address into the memory data register); IR←MDR (load the fetched bits into the instruction register); PC←PC+instruction-length (advance the program counter past the just-fetched instruction, exactly the "PC already incremented" fact the previous chapter's PC-relative addressing discussion depended on directly).

KEY: the PC-increment RTL statement above (PC←PC+instruction-length) is not a new, separate fact to memorise on top of the previous chapter's PC-relative addressing rule — it IS the literal hardware mechanism that PRODUCES the "PC is already incremented before a PC-relative offset is added" behaviour the previous chapter relied on; seeing the RTL statement makes explicit exactly WHEN, in the instruction cycle, that increment actually happens (during FETCH, before decode or execute even begin), which is precisely why the offset calculation in PC-relative addressing always uses the ALREADY-incremented PC value.

HARDWIRED VS MICROPROGRAMMED CONTROL

HARDWIRED CONTROL generates control signals using FIXED combinational logic (or a dedicated finite-state-machine sequencer) driven directly by the current opcode, the addressing mode in use, and a cycle counter tracking progress through the instruction cycle. This is FAST (control signals settle after only a handful of gate delays), but INFLEXIBLE — extending or modifying the instruction set requires physically REDESIGNING the combinational logic itself, which is exactly why hardwired control is the design of choice for RISC and other high-performance processors whose instruction sets are deliberately kept small and stable.

MICROPROGRAMMED CONTROL implements each machine instruction as a SEQUENCE of MICRO-INSTRUCTIONS, stored in a dedicated CONTROL STORE (essentially a small ROM or RAM built purely for this purpose). Each micro-instruction packs together a set of CONTROL-SIGNAL bits (specifying exactly which datapath signals to assert this micro-step) plus a next-ADDRESS field (specifying where in the control store to find the FOLLOWING micro-instruction). A CONTROL ADDRESS REGISTER (CAR) holds the address of the micro-instruction CURRENTLY being executed, and a small SEQUENCER computes the CAR's next value — either a straightforward increment (sequential execution, the default case) or a BRANCH to some other address (when a condition, or the currently-executing opcode, dictates a different micro-instruction sequence).

GATE TRAP: hardwired and microprogrammed control are FREQUENTLY swapped as distractors in conceptual questions — hardwired control is the FAST, FIXED, hard-to-modify design (favoured for RISC and high-performance processors), while microprogrammed control is the FLEXIBLE, control-store-based design that trades some raw speed (an extra control-store access per micro-step) for the ability to change or extend the instruction set simply by rewriting the control store's contents, with no redesign of the underlying combinational logic needed at all.

HORIZONTAL VS VERTICAL MICROINSTRUCTIONS

A HORIZONTAL microinstruction dedicates ONE BIT to every individual control signal — WIDE words, but NO decoding logic needed at all (each bit drives its own signal directly), giving HIGHLY PARALLEL execution (many independent datapath actions can be triggered simultaneously by a single micro-instruction) at the cost of a LARGE control store (many bits per word, most of them typically 0 in any given micro-instruction, since only a few signals are usually active at once).

A VERTICAL microinstruction ENCODES its control signals (much like a small "mini-opcode" within the micro-instruction itself), requiring a DECODER to translate the encoded field back into actual individual control signals before they can drive the datapath — this gives NARROWER micro-instruction words (a major control-store SIZE saving) and correspondingly CHEAPER storage, at the cost of REDUCED parallelism (an encoded field can typically only specify one action from among several MUTUALLY EXCLUSIVE choices, rather than triggering several independent signals at once) and an ADDED decode-delay on the critical timing path.

GATE TRAP: describing horizontal versus vertical microinstructions PURELY in terms of "control store SIZE" alone, without mentioning the actual underlying cause (ENCODING and PARALLELISM), misses the real distinguishing feature the exam is testing — horizontal is wide specifically because it is UNENCODED (one bit per signal, enabling full parallelism), and vertical is narrow specifically because it IS encoded (multiple signals share a field, requiring a decoder and sacrificing some parallelism); the size difference is a CONSEQUENCE of the encoding choice, not the defining property itself.

MICROINSTRUCTION SEQUENCING TECHNIQUES

[[FIG:micro-sequencing-techniques]]

EXPLICIT NEXT-ADDRESS (two-ADDRESS) FIELD: every micro-instruction carries the FULL address of the next micro-instruction to execute, explicitly, as one of its own fields — the MOST FLEXIBLE sequencing scheme (any micro-instruction can be followed by ANY other, with no structural restriction at all), but also the scheme needing the LARGEST dedicated address field, since it must be wide enough to address the entire control store on every single micro-instruction, even the overwhelming majority that simply proceed to the next sequential address in practice.

INCREMENTING WITH BRANCH CONTROL: the DEFAULT behaviour is CAR←CAR+1 (implicit SEQUENTIAL execution, needing NO explicit next-address field at all for the common case) — a separate, dedicated BRANCH micro-instruction (carrying an explicit condition and an explicit target address) is used only when control flow must genuinely DIVERGE from straight-line sequential execution. This saves address-field bits across the OVERWHELMING MAJORITY of ordinary, sequential micro-instructions, at the cost of needing a distinct branch-instruction FORMAT for the comparatively rare cases where a jump is actually needed.

MAPPING ROM (OPCODE MAPPING): upon FETCHING a new machine instruction, a small, DEDICATED lookup table converts the just-fetched OPCODE DIRECTLY into the STARTING control-store address for that specific instruction's micro-routine — avoiding a long chain of sequential dispatch-decision micro-instructions that would otherwise be needed simply to figure out WHERE in the control store execution should even begin for this particular opcode.

CONTROL STORE SIZING: THE RECURRING NUMERIC PITFALL

CONTROL STORE SIZE (in bits) = (number of distinct micro-instructions stored) × (micro-instruction WIDTH in bits, itself the sum of the control-signal bits plus the next-address field's own width).

THE RECURRING PITFALL: the next-ADDRESS field's width must be computed as ⌈log₂(number of microinstructions)⌉ — the same "how many bits to distinguish N things" counting formula that has appeared throughout this book's Digital Logic and Computer Organization chapters alike — rather than simply ASSUMED to be some convenient round number (8 bits, say) without actually checking it against the true micro-instruction count.

Worked trace: a microprogrammed control unit must store 512 distinct micro-instructions, each needing 32 dedicated control-signal bits plus a next-address field.

1. The naive (and WRONG) assumption might be that an 8-bit next-address field suffices, since 8 bits "feels like a natural size" — but checking directly: 2⁸=256, which is NOT enough to address all 512 micro-instructions.
2. The CORRECT next-address field width is ⌈log₂512⌉=9 bits (since 2⁹=512 exactly).
3. TRUE micro-instruction width = 32 (control-signal bits) + 9 (correctly-sized next-address field) = 41 bits.
4. TOTAL control store size = 512×41 = 20,992 bits = 20,992/8 = 2624 bytes.

GATE TRAP: this exact "assume a convenient round address-field width instead of actually computing ⌈log₂(count)⌉" error is one of the single most common numeric mistakes in control-store sizing questions — the correct field width must ALWAYS be derived directly from the ACTUAL number of micro-instructions the specific design needs to address, never assumed from a nearby "nice" power-of-two guess.

CONTROL SIGNAL TIMING AND CLOCK PERIOD

THE CLOCK PERIOD must be AT least as long as the LONGEST register-transfer path delay occurring during ANY single control step — this worst-case delay includes the ALU's own propagation delay, any multiplexer SELECT delay along the path, bus/wire propagation delay, and the destination register's own SETUP TIME (directly recalling the setup-time constraint already derived in full in the sequential-circuits chapter, now applied specifically to the datapath's own register-to-register transfer paths).

TOTAL EXECUTION TIME for one instruction = (the NUMBER of control steps, i.e. clock cycles, that instruction's own micro-routine or hardwired sequence actually requires) × (the CLOCK PERIOD determined by the worst-case single-step delay above).

1. This directly EXPLAINS why DIFFERENT instructions on the SAME processor can take DIFFERENT total execution times — an instruction needing MORE control steps (more micro-instructions in its routine, or more hardwired-sequencer cycles) simply takes PROPORTIONALLY longer in wall-clock time, even though every individual control step is bounded by the exact SAME fixed clock period.
2. This also explains why a SINGLE, unusually SLOW datapath operation (say, a particularly slow ALU function, or a particularly long bus path used only rarely) can force the ENTIRE processor's clock period to be LONGER than it would otherwise need to be — the clock period is set by the WORST-CASE single-step delay across EVERY possible control step ANY instruction might ever need, not merely the TYPICAL or AVERAGE step's delay.

KEY: this chapter's clock-period constraint (bounded by the worst single-step combinational delay plus register setup time) is EXACTLY the SAME synchronous-timing constraint already derived in full in the sequential-circuits chapter (Tclk ≥ Tcq + Tcombinational(max) + Tsetup) — here, the datapath's ALU and bus-routing logic collectively PLAY THE ROLE of the "combinational logic between two flip-flops" from that earlier derivation, and the datapath's own registers play the role of the flip-flops themselves; recognising this as the IDENTICAL underlying timing constraint, merely applied to a specific, larger combinational structure (an entire datapath) rather than a small illustrative example, avoids needing to re-derive or separately memorise a "new" timing rule specific to datapaths at all.

SINGLE-BUS VS MULTI-BUS DATAPATH ORGANISATION

A DATAPATH's registers, ALU, and memory interface must be CONNECTED somehow, and the choice of HOW MANY shared buses connect them is itself a real design trade-off directly analogous to several others already met in this book.

1. A SINGLE-BUS datapath connects EVERY register and the ALU to ONE shared bus — SIMPLE and CHEAP (minimal wiring, one shared resource to manage), but only ONE data transfer can happen PER CLOCK CYCLE, since the bus can carry only one value at a time; an operation needing TWO source operands (an ADD, say) must therefore be broken into MULTIPLE control steps — first moving one operand onto the bus and into a temporary ALU-input register, THEN moving the second operand onto the bus for the ALU to combine with the first.
2. A MULTI-BUS (typically THREE-bus) datapath provides SEPARATE buses for the ALU's two inputs and its single output, allowing BOTH source operands to reach the ALU, and the ALU's result to be written back, ALL within the SAME clock cycle — this reduces the NUMBER of control steps a typical arithmetic instruction needs (often to just one, for the actual computation step), at the direct cost of MORE wiring and MORE complex bus-arbitration logic.
3. This is, once again, the SAME "fewer shared resources means simpler but slower; more resources means faster but more complex hardware" trade-off pattern already recognised repeatedly throughout this book — a single-bus datapath is the datapath-level analogue of a RIPPLE-CARRY adder or a CHAIN-style combinational structure (simple, but paying a sequential-step cost), while a multi-bus datapath is the analogue of a CARRY-LOOKAHEAD adder or a TREE-style structure (more hardware, but a shorter critical path in terms of control steps needed).

Worked trace: on a single-bus datapath, trace the control steps needed for ADD R1,R2,R3 (R1←R2+R3), assuming a temporary register Y is available to hold one operand while the bus is used for the other.

1. Control step 1: R2 is placed on the bus and loaded into Y (Y←R2) — the bus carries R2's value this cycle, and nothing else can use the bus simultaneously.
2. Control step 2: R3 is placed on the bus, and the ALU computes Y+R3 (using Y's already-stored value as its OTHER input, entirely independent of the bus this cycle) directly into a Z register (Z←Y+R3, with R3's value arriving via the bus).
3. Control step 3: Z's value is placed on the bus and loaded into R1 (R1←Z) — completing the instruction.
4. Total: 3 control steps for this single ADD instruction on a single-bus datapath — directly illustrating why a single-bus design, while simpler in wiring, generally needs MORE control steps (and correspondingly more microinstructions, in a microprogrammed implementation) per instruction than an equivalent multi-bus design would.

On an EQUIVALENT three-bus datapath, the same instruction could complete in a single control step: R2 and R3 are placed on their own separate input buses simultaneously, the ALU computes their sum in the same cycle, and the result is placed directly on the output bus into R1 — R1←R2+R3 as ONE RTL statement executed in one control step, with no intermediate temporary register needed at all.

KEY: the number of CONTROL STEPS a given instruction needs is not a fixed, universal property of that instruction alone — it depends DIRECTLY on the underlying datapath's bus organisation, exactly as the SAME ADD instruction needed 3 steps on a single-bus datapath but only 1 step on a three-bus datapath; when a question states a specific NUMBER of control steps for an instruction, that number is implicitly describing the SPECIFIC datapath organisation assumed, not a property of the instruction's OWN definition in isolation.

RTL FOR CONDITIONAL (BRANCH) EXECUTION

A conditional BRANCH instruction's RTL differs from an ordinary arithmetic instruction's RTL in one structural way: its EXECUTE stage's effect on the PC depends on a CONDITION evaluated during that same stage, rather than unconditionally updating a destination register.

1. A typical conditional branch RTL sequence: during EXECUTE, the condition (say, checking whether a specific status flag, like ZERO, is currently set) is evaluated; IF the condition holds, PC←(target address, computed via whichever addressing mode the branch instruction specifies — often PC-relative, exactly as covered in the previous chapter); IF the condition does NOT hold, the PC is left at whatever value the ordinary FETCH-stage increment already set it to (falling through to the NEXT sequential instruction).
2. This is why a CONDITIONAL branch's RTL is often written with an explicit IF-THEN structure (unlike the unconditional RTL statements used for ordinary arithmetic or data-movement instructions), and why, in a MICROPROGRAMMED control unit specifically, evaluating a branch condition typically corresponds to an actual BRANCH micro-instruction (as introduced above) within that machine instruction's own micro-routine — the machine-level conditional branch and the microcode-level branch micro-instruction are CONCEPTUALLY the same idea (a condition determining which of two possible next steps to take), just operating at two different levels of the overall system (machine-instruction sequencing at the PC level, versus micro-instruction sequencing at the CAR level).

REGISTER FILES: PORTS AND SIMULTANEOUS ACCESS

A REGISTER FILE is the small, fast bank of general-purpose registers a datapath provides, and its DESIGN is characterised by how many independent READ and WRITE PORTS it offers — directly determining how many register accesses can happen simultaneously within a single control step.

1. A register file with 2 READ ports and 1 WRITE port allows TWO different registers to be READ simultaneously (supplying both of an ALU operation's source operands in the SAME cycle) while a THIRD register is WRITTEN (typically the destination of the PREVIOUS instruction's result, or the current instruction's own result if timed appropriately) — this specific port configuration (2 read, 1 write) is exactly what a three-bus datapath's typical RTL statement (R1←R2+R3) needs to execute in a SINGLE control step, directly explaining why register files are so often designed with precisely this port count.
2. A register file with FEWER ports (say, only 1 read port) FORCES sequential access even for operations that conceptually need two source operands read at once — directly mirroring the single-bus datapath's own need for multiple control steps, but now the bottleneck lives specifically in the register file's own port count rather than in the shared bus.
3. Each ADDITIONAL port (beyond the minimum needed) adds real hardware cost — more read/write address decoding logic, more physical wiring to every storage cell — so register-file port count is a genuine, deliberate design trade-off, not a free upgrade; a design with MORE ports enables MORE work per control step (fewer total steps needed for a given instruction mix) at the cost of a larger, more complex, and more power-hungry register file.

KEY: the register file's port count and the datapath's own bus count are TWO SEPARATE (though closely related) resources, and a bottleneck in EITHER one independently can force extra control steps — a three-bus datapath connected to a register file with only ONE read port still cannot read two source operands simultaneously, regardless of how many buses are available to carry them, since the register file itself is the limiting resource in that specific case; correctly diagnosing WHICH resource (buses, or register-file ports, or both) is actually constraining a given datapath's control-step count requires checking BOTH specifications explicitly, not assuming that fixing one automatically fixes the other.

NANOPROGRAMMING: A TWO-LEVEL CONTROL STORE

Some microprogrammed designs use TWO LEVELS of control storage instead of one, specifically to recover some of horizontal microprogramming's parallelism without paying its full control-store-size cost — this technique is called NANOPROGRAMMING.

1. The FIRST level (the micro-program, using VERTICAL, encoded micro-instructions, kept NARROW and hence cheap) does not directly drive the datapath at all — instead, each of its encoded fields points to an entry in a SECOND level (the NANO-MEMORY, containing a small number of DISTINCT horizontal, fully-unencoded control-signal patterns).
2. Since REAL microprograms tend to REUSE the same small handful of ACTUAL horizontal control-signal patterns repeatedly across MANY different micro-instructions (many different micro-operations end up asserting an identical, or nearly identical, combination of datapath control signals), storing those FREQUENTLY-REPEATED horizontal patterns just ONCE each in the smaller nano-memory, and having the larger vertical micro-program simply POINT to the appropriate nano-memory entry, can achieve a SMALLER total control-store size than a purely horizontal single-level design, while still ultimately driving the datapath with full horizontal parallelism at the FINAL stage.
3. The cost of nanoprogramming is an EXTRA level of indirection (and hence an extra access delay) between fetching a micro-instruction and the resulting control signals actually reaching the datapath — directly analogous to the extra access memory-indirect addressing needs compared to direct addressing, covered in the previous chapter, now appearing as an extra STORAGE-ACCESS indirection within the control unit itself rather than within ordinary operand addressing.

GATE TRAP: assuming nanoprogramming ALWAYS reduces total control-store size compared to a purely horizontal design is not universally true — the size saving depends ENTIRELY on how much genuine REPETITION exists among the actual horizontal control-signal patterns a specific instruction set's micro-programs would otherwise need; if every micro-instruction in a given design happens to need a GENUINELY DISTINCT horizontal pattern (no repetition to exploit at all), nanoprogramming's extra level of indirection buys NOTHING in size terms while still adding its own extra access delay, making it strictly worse than a single-level design for that specific (admittedly unusual) case.

DATAPATH WIDTH AND ITS RELATIONSHIP TO INSTRUCTION EXECUTION TIME

The DATAPATH WIDTH (how many bits the ALU, buses, and registers handle in a single pass) directly determines how many CONTROL STEPS an operation on data WIDER than that native width actually needs.

1. A datapath with a NATIVE width equal to the processor's own word size (say, a 32-bit datapath on a 32-bit machine) can perform a full-width ADD, for instance, in exactly the SAME single control step regardless of the specific operand values involved — the ALU's own internal carry-propagation (whether via ripple-carry or carry-lookahead, as covered fully in the computer-arithmetic and digital-logic chapters) happens ENTIRELY within that one control step's own clock period, invisible at the RTL level.
2. A datapath with a NARROWER native width than the data it must sometimes handle (a 32-bit ALU used to add two 64-bit operands, say) must break the wider operation into MULTIPLE control steps, explicitly handling the CARRY between the lower and upper halves as a SEPARATE RTL step — exactly the same "narrower native operation, chained across multiple steps to build up a wider result" pattern already met directly in the ripple-carry-adder discussion (multiple 1-bit stages chained via carry) and in the ITERATIVE bit-sliced design pattern from the sequential-circuits chapter, now appearing again at the level of entire multi-step RTL sequences rather than single-bit hardware stages.
3. This is precisely why WIDER native datapaths (64-bit rather than 32-bit, say) are a genuine PERFORMANCE feature for workloads involving wide data, entirely independent of any change to the instruction set's own encoding — a wider datapath directly reduces the NUMBER of control steps (and hence total execution time) needed for operations on data at or below its own native width, exactly the same "spend more hardware, buy back fewer sequential steps" trade-off pattern recurring throughout this entire chapter.

Worked trace: a 16-bit datapath must add two 32-bit operands, using two 16-bit ADD operations chained via an explicit carry. Let the 32-bit operands be split into high and low 16-bit halves (AH,AL) and (BH,BL). Control step 1: compute the LOW half's sum and capture any carry-out: (RL, Cout) ← AL+BL (the ALU's own internal carry-lookahead or ripple-carry logic resolves this entirely within the single 16-bit-wide control step, exactly as in point 1 above). Control step 2: compute the HIGH half's sum, ADDING IN the carry captured from step 1: RH ← AH+BH+Cout — this second control step is needed specifically because the native 16-bit datapath cannot combine all 32 bits in one pass, directly mirroring the sequential-circuits chapter's own ripple-style, one-bit-slice-at-a-time reasoning, now scaled up to 16-bit "slices" instead of single bits.

KEY: whenever an operation's OPERAND width exceeds the datapath's OWN native width, the number of EXTRA control steps needed is directly proportional to how many native-width "chunks" the wider operand must be split into — a 64-bit operation on a 16-bit-native datapath needs FOUR 16-bit chunks chained together (needing correspondingly more control steps than the 32-bit-on-16-bit example above, which needed only two), and recognising this proportionality directly (rather than treating each specific width combination as a separately memorised case) generalises immediately to ANY combination of operand width and native datapath width a question might describe.

GATE TRAP: confusing DATAPATH width (the ALU/bus/register width actually used internally to process one control step) with INSTRUCTION width (the encoded length of an instruction itself, covered fully in the previous chapter) is a common terminology mix-up — the two are entirely independent design parameters that happen to share the word "width," and a machine can freely combine, for instance, a 16-bit-wide datapath with 32-bit-wide instruction encodings, or vice versa; a question about how many control steps an operation needs is asking about DATAPATH width specifically, never instruction width, and conflating the two leads directly to an incorrectly reasoned answer.

EMULATION: MICROPROGRAMMING'S MOST FLEXIBLE APPLICATION

The same flexibility that lets a microprogrammed control unit's designer add new instructions to an existing processor (worked problem 2 above) can be pushed one step further: EMULATION, where a processor's own native microcode is written specifically to make it EXECUTE the instruction set of an ENTIRELY DIFFERENT machine.

1. An emulating processor's microcode reads instructions ENCODED for the TARGET (emulated) machine's instruction set, and for EACH target opcode, executes a micro-routine that reproduces the EXACT semantic effect that instruction would have had on the ORIGINAL target hardware — from the perspective of software running on top, the emulating machine is INDISTINGUISHABLE from genuine target hardware, even though the UNDERLYING physical datapath and native instruction set are completely different.
2. This is possible ONLY because microprogrammed control decouples the machine's INSTRUCTION SET (defined entirely by which micro-routines exist and what they do) from its underlying HARDWARE datapath (the actual registers, ALU, and buses microcode ultimately drives) — a hardwired control unit, by contrast, has its instruction set BAKED DIRECTLY into fixed combinational logic, making this kind of flexible, software-like reconfiguration entirely impractical (it would require physically redesigning the hardwired logic itself for every different machine to be emulated).
3. Emulation is therefore the LOGICAL EXTREME of the same flexibility-versus-speed trade-off already established for microprogrammed control in general: a MODEST amount of that flexibility lets a designer add a FEW new instructions to an existing instruction set cheaply (worked problem 2); the FULL extent of that same flexibility lets a designer implement an ENTIRELY DIFFERENT instruction set on top of unrelated underlying hardware, at some inevitable further cost in execution speed compared to running that same target instruction set on GENUINE, purpose-built hardware.

KEY: emulation is not a separate, unrelated capability bolted onto microprogrammed control — it is the SAME "instruction set defined by rewritable micro-routines rather than fixed hardware logic" property already established for ordinary microprogrammed control, simply exploited to its FULLEST possible extent (replacing an ENTIRE instruction set's worth of micro-routines, rather than adding just a handful of new ones to an otherwise-unchanged existing set); recognising emulation as a matter of DEGREE rather than a fundamentally different mechanism is what connects this specific application directly back to the general hardwired-versus-microprogrammed flexibility trade-off this entire chapter has built up from its very first section onward.

WORKED PROBLEMS

1. RTL FOR AN INSTRUCTION FETCH. Write the RTL for the fetch stage of a machine with a 4-byte instruction length, and state which register's value changes as a RESULT of this stage (as opposed to being merely read). MAR←PC; MDR←M[MAR]; IR←MDR; PC←PC+4. Both IR (loaded with the newly fetched instruction) and PC (advanced past it) change as a result of this stage — MAR and MDR are used as staging registers along the way but do not carry information forward into the NEXT instruction cycle the way IR and PC do.

2. HARDWIRED VS MICROPROGRAMMED SPEED-FLEXIBILITY TRADE-OFF. A manufacturer needs to add 5 new instructions to an EXISTING processor design as cheaply and quickly as possible, without a full hardware redesign. Which control style makes this easier, and why? Microprogrammed control makes this far easier — adding new instructions means writing new MICRO-ROUTINES and loading them into the control store (a firmware-level change), without touching the underlying combinational logic at all; a hardwired design would require physically redesigning the control logic itself to recognise and correctly sequence the 5 new opcodes.

3. HORIZONTAL VS VERTICAL PARALLELISM. A horizontal micro-instruction has 40 individual control-signal bits, one per possible signal, and can assert any combination of them simultaneously. A vertical micro-instruction instead groups those same 40 signals into 4 encoded fields of 4 bits each (since the signals fall into 4 mutually-exclusive groups of up to 16 choices each), needing only 16 bits total plus decoders. Compare their word widths and their maximum simultaneous-signal-assertion capability. Horizontal: 40-bit word, can assert UP TO 40 independent signals simultaneously (any subset). Vertical: 16-bit word (less than half the width), but can assert AT MOST 4 signals simultaneously (exactly one per encoded field, since each field's encoding selects only one signal from its own mutually-exclusive group) — a direct illustration of the width-versus-parallelism trade-off.

4. CONTROL STORE SIZE, CORRECTLY COMPUTED. A microprogrammed control unit stores 300 micro-instructions, each needing 28 control-signal bits plus a correctly-sized next-address field. Find the true micro-instruction width and the total control store size in bytes. Next-address field: ⌈log₂300⌉=9 bits (since 2⁸=256<300≤512=2⁹). True width: 28+9=37 bits. Total size: 300×37=11,100 bits = 11,100/8=1387.5 bytes — since a fractional byte count is not physically meaningful, the control store would actually be implemented using 1388 bytes (rounding up to the next whole byte), with a few bits of padding.

5. CLOCK PERIOD FROM WORST-CASE PATH. A datapath's ALU has a propagation delay of 8 ns, its bus routing adds 2 ns, and the destination register's setup time is 3 ns. What is the minimum clock period, and what is the total execution time for an instruction needing 5 control steps? Minimum clock period = 8+2+3 = 13 ns. Total execution time for a 5-control-step instruction = 5×13 = 65 ns.

6. MAPPING ROM BENEFIT. A processor has 64 distinct opcodes, and dispatching to the correct micro-routine via sequential comparison would need, on average, 32 comparison steps (checking half the opcodes before finding a match). Using an opcode-mapping ROM instead, how many steps does dispatch take, and why? Exactly 1 step — the mapping ROM performs a direct table lookup, converting the opcode DIRECTLY into its micro-routine's starting address in a single access, entirely avoiding the sequential comparison chain altogether, regardless of how many total opcodes the processor actually supports.

7. SINGLE-BUS CONTROL STEP COUNT. On a single-bus datapath (with a temporary register Y available), trace the control steps needed for MOV R1,R2 (R1←R2, a simple register-to-register copy, no ALU operation involved). Control step 1: R2 is placed on the bus and loaded directly into R1 (R1←R2) — since no ALU computation or second operand is needed at all, this simple copy completes in just 1 control step, even on a single-bus datapath, unlike the 3-step ADD example above which genuinely needed two separate operands combined via the ALU.

8. BUS ORGANISATION AND CONTROL STEP TRADE-OFF. A single-bus datapath needs 3 control steps for a typical arithmetic instruction; an equivalent three-bus datapath needs 1. If each control step takes 10 ns on both datapaths, and a program executes 1000 such arithmetic instructions, compare total execution time. Single-bus: 1000×3×10=30,000 ns. Three-bus: 1000×1×10=10,000 ns — the three-bus datapath is 3 times faster for this instruction mix, directly illustrating the practical payoff of the extra bus wiring and arbitration complexity.

9. CONDITIONAL BRANCH RTL. Write the RTL for a conditional branch instruction BEQ that branches to a PC-relative target if the ZERO flag is set, otherwise falls through. IF (ZERO=1) THEN PC←(PC already incremented past this instruction)+offset; ELSE PC unchanged (already correctly advanced by the ordinary fetch-stage increment) — the branch's own EXECUTE stage only needs to act when the condition holds, since the fall-through case is already handled entirely by the fetch stage's own unconditional PC increment.

10. REGISTER FILE PORT BOTTLENECK IDENTIFICATION. A three-bus datapath is connected to a register file with 2 read ports and 1 write port. Can it execute R1←R2+R3 in a single control step? Explain which resource (if any) would become the bottleneck if the register file instead had only 1 read port. Yes — with 2 read ports, both R2 and R3 can be read simultaneously, matching the three-bus datapath's own capability, so R1←R2+R3 completes in exactly 1 control step. With only 1 read port, the REGISTER FILE becomes the bottleneck (not the buses, which have enough capacity) — R2 and R3 would need to be read in two SEPARATE control steps regardless of how many buses are available, since the register file itself cannot supply two register values in the same cycle.

11. NANOPROGRAMMING SIZE TRADE-OFF. A vertical micro-program has 200 micro-instructions, each needing a 6-bit field to select among 40 distinct horizontal control-signal patterns actually used across the whole design (many micro-instructions share the same pattern). Compare the nano-memory's own size against what a purely horizontal single-level design (200 micro-instructions × 60 signal bits each, say) would need. Nano-memory: 40 distinct patterns × 60 bits each = 2400 bits, PLUS the vertical micro-program's own 200×6=1200 bits for the selector fields, totalling 3600 bits. Purely horizontal: 200×60=12,000 bits. The two-level nanoprogrammed design uses less than a third of the horizontal design's total storage, directly because only 40 DISTINCT patterns needed storing at full width, rather than storing a (possibly repeated) full-width pattern for all 200 micro-instructions individually.

12. NATIVE-WIDTH CHUNKING. A 16-bit-native datapath must add two 64-bit operands. How many 16-bit chunks are needed, and how many control steps (assuming one control step per chunk, chained via carry exactly as in the worked trace above)? 64÷16=4 chunks needed, requiring 4 control steps total, each one adding one 16-bit chunk plus the carry propagated in from the previous chunk's own addition.

13. FULL COMBINED WORKED PROBLEM. A single-bus datapath (with a temporary register available) is connected to a register file with 1 read port and 1 write port. Each control step takes 12 ns. (a) How many control steps does R1←R2+R3 need on this datapath? (b) Given this control-step count, and a microprogrammed control unit needing one micro-instruction per control step plus a correctly-sized next-address field, with 24 control-signal bits per micro-instruction and 500 total distinct micro-instructions across the whole instruction set, find the true micro-instruction width. (c) Find the total execution time for this one ADD instruction. Part (a): with only a single bus AND only a single register-file read port, this instruction needs the SAME 3-step breakdown as the single-bus worked trace earlier in this chapter (R2 to Y, then R3 combined with Y via the ALU into Z, then Z into R1) — the single read port does not make this WORSE than the single-bus case already traced, since the single bus was already the binding constraint forcing sequential reads regardless. So 3 control steps. Part (b): next-address field width = ⌈log₂500⌉=9 bits (since 2⁸=256<500≤512=2⁹). True micro-instruction width = 24+9=33 bits. Part (c): total execution time = 3 control steps × 12 ns = 36 ns.

14. EMULATION FEASIBILITY. A company wants an existing hardwired-control processor to run software written for a different, older machine's instruction set, without adding any new physical hardware. Is this feasible with the existing hardwired design, and would a microprogrammed design have made it easier? A hardwired design is NOT easily adapted for this purpose — its instruction set is baked into fixed combinational logic, and emulating a different instruction set would require physically redesigning that logic entirely, defeating the "no new hardware" requirement. A MICROPROGRAMMED design would have made this dramatically easier: writing a new set of micro-routines (one per target-machine opcode) and loading them into the existing control store — with no change to the underlying datapath hardware at all — is exactly what emulation requires, directly illustrating why microprogrammed control units are specifically favoured whenever backward-compatibility or instruction-set emulation is a genuine design requirement.

CARRYING THIS FORWARD

This chapter's material is best understood as the physical REALISATION of the fetch-decode-execute cycle already established in the previous chapter — the DATAPATH is the physical hardware the RTL statements describe operations upon, and the CONTROL UNIT (whether hardwired or microprogrammed) is what decides, cycle by cycle, WHICH RTL statements actually fire. The hardwired-versus-microprogrammed choice is, once again, the same simplicity-versus-flexibility trade-off pattern already met repeatedly throughout this book (fixed-point vs floating-point; fixed vs variable instruction length): hardwired trades flexibility for raw speed and simplicity, microprogrammed trades some speed for the flexibility of a rewritable control store. Control-store sizing is, once again, the same "how many bits to distinguish N things" counting principle already applied throughout the Digital Logic and Computer Organization material, now applied specifically to a next-address field. And the clock-period constraint is, once again, the identical synchronous-timing rule already derived in full in the sequential-circuits chapter, merely relabelled for a datapath's own registers and combinational logic. Recognising every one of this chapter's "new" ideas as a DIRECT application of a principle already fully derived earlier in this book is, as in every chapter before it, the fastest route to genuine understanding — and the most reliable defence against a question that dresses up a familiar principle in unfamiliar datapath-specific vocabulary.

The chapter's later material — bus organisation, register-file port counts, nanoprogramming, and native-width chunking — each add one further instance of the same recurring "spend more hardware, buy back fewer sequential steps" trade-off already flagged repeatedly above. A single-bus datapath, a single-read-port register file, a purely horizontal (no nanoprogramming) control store, and a narrow native datapath width are all, in their own specific way, CHEAPER, SIMPLER designs that pay for that simplicity with more control steps (or, for nanoprogramming's case, potentially larger control-store size instead). Their respective "upgraded" counterparts (multi-bus, multi-port register files, two-level nanoprogrammed control stores, and wider native datapaths) all spend additional hardware specifically to REDUCE that same control-step or storage cost. Seeing all four of these apparently distinct design choices as INSTANCES of one repeatedly-applied trade-off, rather than as four independent facts to memorise separately, is exactly the kind of connective habit this entire book has aimed to build across every subject it has covered, and it pays off directly here: a question describing an unfamiliar datapath configuration this chapter never explicitly named can still be analysed correctly by asking the same recurring question — "what resource is this specific design economising on, and what cost (in control steps, storage, or delay) is it paying in exchange."

A further worthwhile connection, tying this chapter directly back to the sequential-circuits chapter: a MICROPROGRAMMED control unit's own Control Address Register (CAR) and its sequencer are THEMSELVES a small sequential circuit, built from exactly the same flip-flop and combinational-logic building blocks already covered in full detail there — the CAR is a REGISTER (a group of flip-flops), the "CAR←CAR+1" default-increment behaviour is exactly a COUNTER (the same kind of synchronous counter whose design procedure was worked through in full via the Mod-6 and Mod-4 examples), and a BRANCH micro-instruction's effect on the CAR is exactly a LOAD operation overriding that counter's normal increment behaviour for one cycle, precisely analogous to a counter with a parallel-load input used to force a jump to an arbitrary state rather than simply incrementing sequentially. Recognising the control unit's own internal sequencing hardware as "just another sequential circuit, built from the exact same small toolkit already covered," rather than as some entirely separate, more exotic piece of machinery, is a genuinely useful simplification whenever a question probes the internal structure of the control unit itself rather than merely its external behaviour.

A final practical observation, connecting this chapter's material to how a real question is likely to combine several of its ideas at once: a typical exam-style problem might describe a specific datapath's bus count and register-file port configuration, ask for the resulting control-step count for a given instruction, THEN ask for the total control-store size needed to implement that instruction's micro-routine (correctly computing the next-address field width from the actual number of control steps, not an assumed round number), and FINALLY ask for the instruction's total execution time given a stated clock period. Working through such a combined question is nothing more than applying this chapter's four main tools — RTL/control-step tracing, control-store sizing, and total-execution-time computation — one after another in sequence, exactly the same tools this chapter's worked problems have exercised individually; the only genuinely new skill a combined, multi-part question demands is correctly carrying a result computed in one part (such as the number of control steps a specific bus/register-file combination actually requires) forward as an INPUT to the next part's own calculation, rather than treating each part as an isolated, unrelated sub-question — exactly the discipline the fully worked combined problem above was built to exercise directly, tracing all three parts through in careful sequence from the datapath's own bus and port limitations all the way through to a single final execution-time figure.

Emulation, the chapter's final topic, is worth revisiting one more time in this closing summary specifically because it draws together nearly every idea covered above into a single, coherent application. An emulating processor's control unit must be microprogrammed (rather than hardwired) to make instruction-set flexibility possible at all; its micro-routines are built from ordinary RTL statements operating on the host machine's own datapath, whatever bus and register-file organisation that datapath happens to have. Its control store must be correctly SIZED using the same ⌈log₂(count)⌉ counting principle as any other microprogrammed design, and its overall execution speed is governed by the same control-step-count-times-clock-period formula used throughout this entire chapter, now simply applied to whatever (likely larger) number of control steps emulating a foreign instruction set's semantics actually requires compared to running that same instruction natively. Seeing emulation this way — not as an exotic, separate capability, but as one single, elaborate, multi-part application drawing on literally every tool this chapter has built — is a fitting note to close on, since it is exactly the kind of synthesis a genuinely comprehensive exam question, combining several of this chapter's ideas within one scenario, would expect a well-prepared reader to perform without needing each individual piece explained separately all over again from scratch.

Looking back across the whole chapter one final time: every topic covered here — RTL and microoperations, hardwired versus microprogrammed control, horizontal versus vertical microinstructions, sequencing techniques, control-store sizing, clock-period timing, bus organisation, register-file port counts, nanoprogramming, native datapath width, and emulation — is best understood not as ten separately memorised facts but as a small number of recurring questions (how many things need distinguishing and how many bits does that take; what resource is being spent to buy back what speed or flexibility; what is the worst-case delay any single step must accommodate) applied, again and again, to a slightly different specific piece of hardware each time. This is precisely the same pattern this book has followed in every chapter before it, from Boolean algebra's law-and-derivation approach through digital logic's building-block recognition, through computer arithmetic's shift-add-subtract foundation, and now through this chapter's own control-unit and datapath material — a small number of genuinely foundational ideas, understood deeply enough to be re-derived confidently on demand, consistently outperforming a much longer list of independently memorised named facts whose underlying reasoning has been forgotten or never fully internalised in the first place, and reliably so under the kind of real time pressure that makes falling back on genuine, re-derivable understanding so much more valuable than fragile, half-remembered recall.

The next chapter in this same sequence, on pipelining, takes this chapter's control-step-by-control-step model of instruction execution and asks what happens when several instructions are made to overlap their own individual stages simultaneously, rather than one instruction finishing entirely before the next one begins. This builds directly on, rather than replaces, everything this chapter has established about how a single instruction moves through fetch, decode, execute, and write-back one control step at a time — the datapath, the RTL notation, and the control-unit vocabulary developed carefully across this entire chapter reappear immediately in that next chapter's own treatment of overlapping, pipelined instruction execution. A solid, genuinely re-derivable grasp of this chapter's material is therefore a direct prerequisite for following that discussion comfortably and confidently, rather than merely memorising its later, more advanced conclusions in isolation without properly understanding the underlying foundation those conclusions are actually built on. This chapter's own deliberate, methodical progression — from its very first section through to its final, fully verified worked example — has built that foundation brick by careful brick, with no significant gap left unexplained, unverified, or merely asserted without genuine proof, each topic building directly on everything that came immediately before it.

A quick self-check worth running before moving on is to redraw, from memory alone and without looking back at any earlier section, the single-cycle datapath's own control-step table for one representative instruction of each major type: a register-to-register ALU instruction, a load, a store, and a conditional branch. If every control signal in that redrawn table can be justified out loud, step by step, by directly naming which specific datapath component it activates and why that component is needed at exactly that control step, the underlying model has genuinely been internalised rather than simply read through once and set aside. Any control step that cannot be justified this way on the first attempt is worth returning to directly in this chapter's own earlier worked examples before continuing further.
`
};
