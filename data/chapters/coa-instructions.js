// Textbook chapter: Machine Instructions and Addressing Modes.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['coa-instructions'] = {
  figs: [
    {
      id: 'address-machine-ladder',
      caption: 'The four instruction-address-count styles for computing A=B+C, from most explicit (3-address) to fully implicit (0-address/stack). Fewer explicit addresses means shorter instructions but more of them.',
      svg: '<svg viewBox="0 0 320 160" width="100%" style="max-width:320px;height:auto" xmlns="http://www.w3.org/2000/svg"><g font-size="10" fill="currentColor" font-family="monospace"><text x="10" y="20">3-addr: ADD A,B,C</text><text x="10" y="50">2-addr: ADD A,B  (A=A+B)</text><text x="10" y="80">1-addr: LOAD B / ADD C / STORE A</text><text x="10" y="110">0-addr: PUSH B / PUSH C / ADD / POP A</text></g></svg>'
    },
    {
      id: 'expanding-opcode-tree',
      caption: 'Expanding opcode: leftover bit patterns at one operand-count level are prefixed onto the next, narrower level — each leftover pattern multiplies out by 2^(freed field width) at the next level down.',
      svg: '<svg viewBox="0 0 300 140" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><rect x="20" y="15" width="260" height="30"/><rect x="60" y="60" width="180" height="30"/><rect x="100" y="105" width="100" height="25"/></g><g font-size="10" fill="currentColor" text-anchor="middle"><text x="150" y="34">2-address (used + leftover)</text><text x="150" y="79">1-address (leftover × 2^field)</text><text x="150" y="121">0-address</text></g></svg>'
    }
  ],
  text: `
WHY THIS CHAPTER, AND THE ONE QUESTION IT KEEPS ASKING

Every topic in this chapter answers some version of the same underlying question: given a FIXED number of bits (an instruction word, an opcode field, an address field), how many DISTINCT things can be represented, and how does that count change as the bits are divided up differently? Instruction-set classification, opcode-expansion, and addressing-mode effective-address counting are all, underneath their specific names, applications of this one counting idea — and keeping that unifying thread in view is what turns this chapter's material into a small, connected set of reasoning tools rather than a long list of separately memorised formulas.

INSTRUCTION SET CLASSIFICATION BY ADDRESS COUNT

Instruction formats are classified by the NUMBER OF EXPLICIT OPERAND ADDRESSES they carry, and this single number determines both the format's instruction LENGTH and how many instructions a given expression needs to evaluate.

[[FIG:address-machine-ladder]]

A 3-ADDRESS machine: ADD A,B,C means A=B+C — a DEDICATED destination address, distinct from both source operands. This is the MOST ORTHOGONAL format (every operand's role is explicit and independent) but also produces the LONGEST instructions, since three full address fields must be encoded in every arithmetic instruction.

A 2-ADDRESS machine: ADD A,B means A=A+B — the DESTINATION doubles as one of the SOURCES, saving one address field at the cost of forcing one operand to be OVERWRITTEN by the result (a genuine loss of flexibility whenever the original value of A is still needed afterward).

A 1-ADDRESS (ACCUMULATOR) machine: ADD X means AC=AC+M[X] — ALL arithmetic funnels through a single IMPLICIT register (the accumulator), so only ONE explicit address is ever needed per instruction, at the cost of needing separate LOAD and STORE instructions to move values into and out of the accumulator around almost every operation.

A 0-ADDRESS (STACK) machine: ADD pops the top TWO stack values and pushes their sum — operands are ALWAYS implicit (the top of stack, TOS, and the value beneath it, TOS−1), needing NO explicit address field in the arithmetic instruction itself at all.

1. GENERAL RULE FOR EVALUATING AN EXPRESSION with k operators: on a 3- or 2-address machine, EXACTLY k arithmetic instructions suffice (plus whatever data-movement instructions are needed to first stage the operands into place). On a STACK (0-address) machine, exactly k operator instructions are needed PLUS one PUSH instruction for EVERY distinct operand OCCURRENCE in the expression's postfix form — critically, this means a variable appearing TWICE in the expression tree needs to be PUSHED TWICE, since the stack has no way to "remember" and reuse a value once it has been consumed by an operator. On a PURE accumulator machine, most operators typically need a surrounding LOAD/STORE pair, since only ONE operand can occupy the accumulator at any given moment.

GATE TRAP: miscounting the PUSH instructions needed on a stack machine by counting only DISTINCT VARIABLE NAMES rather than distinct OCCURRENCES in the postfix expression is a common and costly error — if the expression is (A+B)*(A+B), the variable A appears TWICE in the postfix form (AB+AB+*), and BOTH occurrences genuinely need their own separate PUSH instruction, since the stack machine has no mechanism to duplicate a previously-computed value without an explicit instruction to do so.

INSTRUCTION LENGTH AND FIELD WIDTH FORMULAS

OPCODE BITS NEEDED = ⌈log₂(number of distinct opcodes)⌉ — the standard "how many bits to distinguish N things" counting formula, appearing here in its most direct application.

REGISTER-FIELD BITS NEEDED = ⌈log₂(number of registers)⌉ — the identical formula, applied to distinguishing which of several registers an instruction refers to.

ADDRESS-FIELD BITS NEEDED = ⌈log₂(memory size in addressable units)⌉ — again the same formula, now applied to distinguishing which memory location is being addressed; this number is EXACTLY the width of the address bus for the corresponding addressing granularity (byte-addressed or word-addressed).

MINIMUM INSTRUCTION WIDTH = opcode bits + the SUM of every operand field's own bit width (register fields, address fields, or both, depending on the instruction format), rounded UP to whatever granularity the actual implementation's word or byte boundaries require.

THE EXPANDING OPCODE TECHNIQUE: A FULL RECIPE

PURPOSE: let SHORT instructions (with many operand fields, hence FEW opcode bits available) and LONG instructions (with few or no operand fields, hence MANY opcode bits available) coexist within ONE shared, fixed total instruction width — achieved by letting the opcode "GROW" into the operand-field bits that become available as the operand count drops from one instruction format to the next.

THE INVARIANT that must never be violated at any stage: the total number of bit patterns actually consumed can never exceed 2^(total instruction length in bits) — equivalently, if bit-pattern usage is tracked level by level (weighting each level's used-pattern count by the fraction of the total space it actually occupies), those fractions must sum to AT MOST 1 across every level combined.

[[FIG:expanding-opcode-tree]]

THE STEP-BY-STEP METHOD:

1. START from the format with the MOST operand fields (this format has the FEWEST opcode bits available, since the remaining bits are all consumed by its many operand fields).
2. SUBTRACT the number of patterns ACTUALLY USED at that level from the TOTAL possible patterns (2^opcode-width-at-that-level) — this difference is the number of LEFTOVER, still-unused patterns at this level.
3. Each LEFTOVER pattern, PREFIXED onto the NEXT, narrower operand format, FREES UP the bit-width of the operand field just dropped — so the leftover-pattern count MULTIPLIES by 2^(freed field width) to give the MAXIMUM possible count of instructions at the next level down.
4. REPEAT this process, dropping one operand field at a time, until reaching 0-address instructions (or however many levels the specific design actually needs).

Worked trace: a 16-bit instruction word, using two 6-bit address fields for 2-address instructions (12 bits total for operands), leaving 16−12=4 opcode bits, giving 2⁴=16 possible 2-address opcode patterns.

1. If 14 of those 16 patterns are actually USED for 2-address instructions, 16−14=2 patterns remain LEFTOVER.
2. Prefixing those 2 leftover patterns onto the NEXT, narrower format (dropping one 6-bit address field, leaving only ONE 6-bit field for 1-address instructions) frees up 6 bits, so the opcode effectively grows to 4+6=10 bits at this level, giving a MAXIMUM of 2×2⁶=128 possible 1-address instructions.
3. If only 100 of those 128 are actually used, 128−100=28 patterns remain leftover, which could expand AGAIN over the FINAL remaining 6-bit field (now dropping the last operand field entirely, reaching 0-address instructions), giving a maximum of 28×2⁶=1792 possible 0-address instructions.

A second, fully worked trace confirming the invariant EXACTLY: a 12-bit instruction word, with two 3-bit register fields (6 bits total) for 2-address instructions, leaving 12−6=6 opcode bits, giving 2⁶=64 possible patterns.

1. If 40 of the 64 patterns are used for 2-address instructions, 64−40=24 patterns remain leftover.
2. Prefixing those 24 leftover patterns onto a 1-address format (dropping one 3-bit register field, freeing 3 bits) gives a maximum of 24×2³=192 possible 1-address instructions.
3. If only 150 of those 192 are used, 192−150=42 patterns remain, which can expand over the FINAL 3-bit field (dropping the last register field, reaching 0-address instructions) to give a maximum of 42×2³=336 possible 0-address instructions.
4. CHECKING THE INVARIANT DIRECTLY, in raw bit-pattern terms: the 40 used 2-address opcodes each correspond to 2⁶=64 distinct full 12-bit instruction patterns (since the two 3-bit register fields can independently take any value): 40×64=2560. The 150 used 1-address opcodes each correspond to 2³=8 distinct full patterns (one remaining register field free to vary): 150×8=1200. The 336 zero-address opcodes each correspond to exactly 1 full pattern (no remaining fields at all): 336×1=336. Summing: 2560+1200+336=4096, EXACTLY 2¹²=4096 — confirming the entire 12-bit instruction space is used completely and validly, with no overlap and no gaps.

ADDRESSING MODES: THE EFFECTIVE ADDRESS TABLE

The EFFECTIVE ADDRESS (EA) of an operand is WHERE the actual data value is ultimately found, and this varies systematically by ADDRESSING MODE:

IMMEDIATE: the operand VALUE ITSELF is the instruction field — 0 memory accesses needed beyond the instruction fetch itself, since there is no separate location to look up at all.

REGISTER: the operand is the CONTENTS of register Rn — again 0 memory accesses (registers are not memory), just a direct register read.

DIRECT (ABSOLUTE): EA = the address field A itself, taken literally — 1 memory access to fetch the operand from that address.

REGISTER INDIRECT: EA = the CONTENTS of register R (the register holds an address, not the value itself) — 1 memory access (fetch the operand from the address the register points to).

MEMORY INDIRECT: EA = M[address field A] — the address field points to a memory location which ITSELF holds the real address — 2 memory accesses (first fetch the pointer from M[A], then fetch the actual operand from the address that pointer contains).

DISPLACEMENT/BASE: EA = (base register) + offset A — 1 memory access.

INDEXED: EA = (index register) + base A — 1 memory access; the index register typically AUTO-INCREMENTS across a loop while the base stays constant, the opposite roles from base addressing below.

PC-RELATIVE: EA = (PC, ALREADY incremented past the current instruction) + signed offset A — 1 memory access.

AUTO-INCREMENT: EA = the CURRENT contents of register R; THEN R is incremented by the operand's size — 1 memory access.

AUTO-DECREMENT: register R is DECREMENTED by the operand's size FIRST; THEN EA = the (now-updated) contents of R — 1 memory access.

GATE TRAP: BASE and INDEXED addressing share the EXACT SAME effective-address FORMULA (a register's value plus a constant), but play OPPOSITE ROLES — in BASE addressing, the OFFSET is the fixed constant and the BASE REGISTER is what VARIES (this supports RELOCATION: moving an entire program to a new memory region by updating just ONE base register, leaving every instruction's own offset field completely unchanged); in INDEXED addressing, the BASE is the fixed constant and the INDEX REGISTER is what VARIES throughout a loop (this supports WALKING THROUGH AN ARRAY, incrementing the index register once per iteration while the base address field stays fixed). The memorable distinction: "the base register moves the WHOLE PROGRAM; the index register moves THROUGH THE ARRAY."

GATE TRAP: the PC-RELATIVE addressing mode's single most commonly missed subtlety is that by the time the signed offset is actually ADDED, the PC has ALREADY been incremented PAST the current instruction (the normal fetch-cycle side effect of advancing the program counter to point at whatever comes NEXT) — so for an instruction of length L located at address A, the effective address is EA = (A+L) + offset, NEVER simply A + offset; forgetting this increment produces an answer off by exactly the current instruction's own length, a small but consistently graded error.

Worked trace: a branch instruction BEQ occupies 4 bytes and sits at address 2000. Its signed displacement field contains −20 (20 bytes backward). Since the PC is incremented to 2004 during fetch BEFORE the offset is applied: EA = 2004+(−20) = 1984. A computation that instead uses the instruction's OWN address directly (2000−20=1980) lands exactly 4 bytes off — precisely the length of the instruction whose own post-fetch increment was forgotten.

MEMORY-REFERENCE COUNTING FOR A COMPLETE INSTRUCTION

TOTAL memory accesses for one complete instruction = 1 (the instruction FETCH itself, always required) + operand-fetch accesses (0 for immediate or register modes; 1 for direct, register-indirect, displacement, indexed, PC-relative, auto-increment, or auto-decrement; 2 for memory-indirect) + 1 MORE if the instruction also WRITES its result back to memory (as opposed to a register) as its destination.

GATE TRAP: treating memory-indirect addressing as a SINGLE memory access (confusing it with ordinary direct addressing, perhaps because both "look like" a single address field in the instruction) is a common and serious undercounting error — memory-indirect ALWAYS requires TWO separate accesses (first fetching the POINTER stored at the address field's location, then fetching the actual OPERAND from wherever that pointer points), on top of the instruction fetch itself, giving a genuinely different total access count from every single-access addressing mode in the table above.

KEY: the addressing-mode EA table above is best memorised not as ten independent formulas, but as answering ONE recurring question per mode — "how many INDIRECTION STEPS does this mode require before reaching the actual operand" — immediate and register modes need ZERO indirection steps (the value or its container is already fully specified), most other modes need exactly ONE step (compute an address, then fetch from it), and memory-indirect alone needs TWO (compute an address, fetch a POINTER from it, then fetch the OPERAND from that pointer) — counting indirection steps directly, rather than trying to recall each mode's memory-access count as an independently memorised fact, generalises immediately to any addressing mode variant not explicitly listed in the table at all.

CISC VS RISC: TWO INSTRUCTION-SET-DESIGN PHILOSOPHIES

CISC (Complex Instruction Set Computer) and RISC (Reduced Instruction Set Computer) represent two genuinely different answers to the question "how much work should a SINGLE instruction be allowed to do."

1. A CISC design favours a LARGE, RICH instruction set with MANY addressing modes and VARIABLE-length instructions, including instructions that can directly perform a memory-to-memory operation (both reading an operand from memory AND writing a result back to memory within a single instruction). This reduces the total NUMBER of instructions a compiled program needs (since each instruction does more work), at the cost of a more COMPLEX, harder-to-pipeline decode stage, since variable-length instructions and elaborate addressing modes make it difficult to predict, in advance, exactly how many bytes the NEXT instruction will occupy.
2. A RISC design favours a SMALL, UNIFORM instruction set, typically FIXED-length instructions, and a strict LOAD-STORE architecture — arithmetic instructions operate ONLY on registers (never directly on memory), with SEPARATE, dedicated LOAD and STORE instructions handling all memory traffic. This INCREASES the total instruction count for a given program (more instructions needed to do the same overall work), but dramatically SIMPLIFIES the decode and pipelining logic, since every instruction has the SAME fixed length and a small, predictable set of operand patterns.
3. The load-store restriction is not an arbitrary limitation — it is PRECISELY what allows a RISC pipeline (covered in full in a later chapter) to overlap instruction fetch, decode, and execute stages cleanly: since arithmetic instructions never touch memory directly, the pipeline's memory-access stage is needed only by the dedicated load/store instructions, keeping the pipeline's stage structure simple and uniform across every instruction type.

GATE TRAP: assuming RISC machines ALWAYS execute a given HIGH-LEVEL program FASTER than an equivalent CISC machine is an oversimplification — RISC typically needs MORE individual instructions to accomplish the SAME task (since each instruction does less work), and whether the resulting program actually runs faster overall depends on the SPECIFIC trade-off between the (larger) instruction COUNT and the (typically higher) CLOCK RATE and pipelining efficiency RISC's simpler decode logic enables; "RISC vs CISC" is a genuine multi-factor engineering trade-off, not a simple "one is always faster" comparison.

COMBINING ADDRESSING MODES: INDIRECTION DEPTH AND ADDRESS ARITHMETIC

Real instruction sets frequently COMBINE two addressing-mode ideas within a single instruction (for instance, "register indirect WITH auto-increment," or "based-indexed" addressing using BOTH a base register and an index register together) — and each combination is understood correctly by applying the SAME indirection-counting and address-arithmetic principles already established above, one step at a time, rather than as an entirely new named mode requiring separate memorisation.

1. REGISTER INDIRECT WITH AUTO-INCREMENT: EA = the CURRENT contents of register R (one indirection step, exactly as plain register-indirect), and then, as a SIDE EFFECT (not part of computing THIS instruction's own EA at all), R is incremented by the operand's size afterward, ready for the NEXT instruction to use the updated value — this is simply register-indirect addressing with an extra post-access bookkeeping step tacked on, not a fundamentally different indirection pattern.
2. BASED-INDEXED addressing: EA = (base register) + (index register) + a DISPLACEMENT constant, combining ALL THREE quantities (base register, index register, and displacement) additively — this needs only ONE memory access to reach the final operand, exactly like ordinary base or indexed addressing alone, since the extra addition happens entirely within the address-computation hardware BEFORE any memory access occurs, adding zero extra memory accesses despite combining three separate address components.
3. The general principle: COMBINING addressing-mode IDEAS (adding multiple address components together, or adding a post-access increment/decrement side effect) does NOT, by itself, increase the number of memory accesses required — onlyGENUINE INDIRECTION (following a pointer stored IN memory, as in memory-indirect addressing) adds an extra access; arithmetic combination of several register/constant address components, however many are combined, still resolves to a SINGLE final address computed entirely within the address-generation hardware, needing only the ONE resulting memory access to actually fetch the operand.

KEY: the true dividing line for counting memory accesses is not "how many components make up the effective-address FORMULA" but "how many times must the ADDRESS ITSELF be looked up IN memory before reaching the real operand" — a based-indexed address with three additive components computed entirely in hardware registers still needs only ONE memory access (since none of those three components requires a memory lookup on its own), while memory-indirect's single address FIELD needs TWO accesses (since the field's own value must first be fetched FROM memory before it can even be used as a pointer) — counting INDIRECTION STEPS, not FORMULA COMPONENTS, is the correct way to determine an addressing mode's true memory-access cost.

ENDIANNESS: BYTE ORDERING WITHIN A MULTI-BYTE VALUE

When a multi-byte value (a 32-bit integer, say, spanning 4 bytes) is stored in byte-addressable memory, ENDIANNESS determines WHICH byte is stored at the LOWEST memory address — this is a genuinely separate concern from anything covered above, but it interacts directly with instruction ENCODING whenever an instruction's own opcode or immediate fields must be read byte by byte from memory.

1. BIG-ENDIAN stores the MOST SIGNIFICANT byte at the LOWEST address (the byte ordering reads left-to-right exactly as the number itself is normally written, most-significant digit first) — the value 0x12345678 stored at address 1000 (big-endian) places 0x12 at address 1000, 0x34 at 1001, 0x56 at 1002, 0x78 at 1003.
2. LITTLE-ENDIAN stores the LEAST SIGNIFICANT byte at the LOWEST address (the reverse ordering) — the SAME value 0x12345678 stored at address 1000 (little-endian) places 0x78 at address 1000, 0x56 at 1001, 0x34 at 1002, 0x12 at 1003.
3. Endianness is PURELY a storage-and-retrieval CONVENTION — it does not change the VALUE being represented at all, only the BYTE-LEVEL layout used to store that value in memory; two machines using DIFFERENT endianness conventions, exchanging raw byte streams directly (over a network, or via a shared binary file) without translation, will MISINTERPRET each other's multi-byte values entirely, even though each machine is behaving perfectly correctly according to its OWN internal convention.

GATE TRAP: endianness questions frequently ask for the CONTENTS of a SPECIFIC byte address after storing a given multi-byte value — the single most reliable way to answer correctly is to write out the FULL hex value broken into individual BYTES first (most significant byte first, exactly as the number is normally written), and then assign those bytes to increasing addresses in EITHER forward order (big-endian) or REVERSED order (little-endian), rather than attempting to reason about "which end" abstractly without writing out the actual byte sequence explicitly.

Worked trace: store the 32-bit value 0xAABBCCDD at address 2000 in both conventions, and state the byte at address 2002 in each case. Bytes, most-significant first: AA, BB, CC, DD. Big-endian: address 2000=AA, 2001=BB, 2002=CC, 2003=DD — so address 2002 holds CC. Little-endian: address 2000=DD, 2001=CC, 2002=BB, 2003=AA — so address 2002 holds BB.

INSTRUCTION ENCODING DENSITY: FIXED VS VARIABLE LENGTH TRADE-OFFS

Beyond the expanding-opcode technique above (which lets a SINGLE fixed instruction WIDTH serve several different operand-count formats), instruction sets face a further design choice: should EVERY instruction occupy the SAME fixed number of BYTES (or words) in memory, or should instruction LENGTH itself vary depending on which specific instruction and operands are being encoded?

1. FIXED-LENGTH encoding (the RISC-favoured choice, as noted above) makes instruction FETCH and DECODE simple and fast: the very next instruction's starting address is ALWAYS the current instruction's address plus one fixed constant, and decode logic can extract every field's position without first needing to determine, from the opcode, how long the CURRENT instruction even is. The cost is WASTED bits whenever a specific instruction genuinely needs fewer operand bits than the fixed format always allocates (a short immediate value, say, still consumes the full fixed immediate-field width even when most of those bits end up being unused padding).
2. VARIABLE-LENGTH encoding (more common in CISC designs) can pack SHORT, common instructions into fewer bytes and reserve LONGER encodings only for instructions that genuinely need extra operand bits (additional addressing-mode information, wider immediate constants, and so on) — this improves CODE DENSITY (a compiled program occupies less total memory) at the direct cost of a MORE COMPLEX fetch/decode stage, since the hardware must examine part of an instruction (often just the opcode, or even the FIRST BYTE of the opcode) before it can determine how many MORE bytes need to be fetched to complete that same instruction.
3. This code-density-versus-decode-simplicity trade-off is a DIRECT parallel to the fixed-point-versus-floating-point range/precision trade-off already met in the computer arithmetic chapter, and the literal-count-versus-input-count trade-off met in the combinational-circuits chapter: in every case, a MORE UNIFORM, SIMPLER representation (fixed-point; fixed-length instructions) trades away some EFFICIENCY (precision at extreme magnitudes; code density) in exchange for SIMPLER, more predictable processing logic, while a MORE FLEXIBLE representation (floating-point; variable-length instructions) buys back that efficiency at the cost of genuinely more complex processing hardware.

KEY: recognising the fixed-vs-variable-length instruction trade-off as ANOTHER instance of the SAME general "uniform-and-simple vs flexible-and-efficient" pattern already met twice before in this book (fixed-point/floating-point; literal-count/input-count in multi-level logic) is worth doing explicitly — it means this specific trade-off does not need to be memorised as an isolated new fact about instruction sets, but can instead be recognised immediately as a familiar STRUCTURAL pattern, now appearing in a new context, with the SAME underlying reasoning ("what does uniformity cost, and what does flexibility buy back") applying directly.

THE INSTRUCTION CYCLE: FETCH, DECODE, EXECUTE

Every instruction, regardless of its specific format or addressing mode, passes through the SAME basic CYCLE of stages, and understanding this cycle explicitly is what makes the memory-access-counting rules above make sense as a coherent whole rather than a list of separately memorised counts.

1. FETCH: the instruction itself is read from memory at the address the PROGRAM COUNTER currently holds — this is the "1 (instruction fetch)" term that appears in EVERY memory-access count throughout this chapter, since every single instruction, no matter how simple or complex, requires this step at least once.
2. DECODE: the fetched instruction's bit pattern is interpreted to determine WHICH operation it specifies and WHICH addressing modes and operand fields apply — this stage does not, by itself, touch memory again; it operates purely on the already-fetched instruction bits.
3. OPERAND FETCH: depending on the addressing modes involved, zero, one, or two FURTHER memory accesses may be needed to actually retrieve the operand VALUES the instruction will operate on — exactly the addressing-mode-dependent counts tabulated above.
4. EXECUTE: the actual operation (an arithmetic computation, a data movement, a comparison) is performed on the now-available operand values.
5. WRITEBACK: if the instruction's RESULT destination is memory (rather than a register), ONE FINAL memory access writes that result back — exactly the "+1 more if the instruction also writes its result to memory" term noted above.

Recognising the total memory-access COUNT for any instruction as simply "how many of these FIVE stages genuinely touch memory, and how many times each one does" is a cleaner, more robust way to arrive at a correct count than attempting to memorise a lookup table of "N accesses for addressing mode X" facts in isolation — the SAME five-stage breakdown applies uniformly to every instruction this chapter has discussed, with only the OPERAND-FETCH stage's specific access count varying by addressing mode.

RELATIVE ADDRESSING FOR DATA, NOT JUST BRANCHES

PC-relative addressing was introduced above specifically in the context of BRANCH instructions, but the identical addressing mode — and the identical "PC is already incremented" subtlety — applies equally to PC-RELATIVE DATA references (an instruction that loads a value from a location computed relative to the current instruction's own position, rather than jumping there).

1. PC-relative DATA addressing is used heavily for POSITION-INDEPENDENT CODE (code that can be loaded and run correctly at ANY memory address, without needing to be re-linked or patched for its specific final location) — since the offset between an instruction and a nearby data value it references stays CONSTANT regardless of where the entire program is loaded in memory, a PC-relative reference to that data remains correct automatically, with no relocation step needed at all, exactly the same practical benefit BASE addressing provides for relocating an entire program via a single register update.
2. The exact SAME EA formula and the exact SAME "PC already incremented" subtlety apply here as for branch targets: EA = (instruction address + instruction length) + signed offset, computed identically whether the resulting effective address is then used as a BRANCH target or as a DATA operand address — recognising PC-relative addressing as ONE addressing mode with two different USES (control-flow targets, and data references), rather than as two separate modes requiring separate rules, avoids a common source of unnecessary confusion.

KEY: PC-relative addressing's core value — a fixed offset that remains VALID regardless of where the surrounding code is loaded in memory — is worth connecting directly to base-register addressing's own relocation benefit (a single base-register update relocates an entire program's absolute addresses at once); both mechanisms solve the SAME underlying "how do we write code whose absolute memory addresses are not known until load time" problem, just via two different mechanisms (a fixed-but-instruction-relative offset for PC-relative; a single adjustable base register for base addressing) — recognising both as answers to the identical relocation problem, rather than as two unconnected addressing-mode facts, is a genuinely useful cross-topic connection within this chapter.

SIGN EXTENSION AND ZERO EXTENSION OF IMMEDIATE FIELDS

An instruction's IMMEDIATE (or displacement/offset) field is often NARROWER than the machine's full register or address width — a common encoding might pack a 16-bit immediate value into a 32-bit register-based arithmetic operation. EXTENDING that narrower field up to the full width correctly requires choosing between SIGN EXTENSION and ZERO EXTENSION, and picking the wrong one silently corrupts the value for roughly half of all possible inputs.

1. SIGN EXTENSION replicates the field's OWN sign bit (its most significant bit) into every newly-added higher-order bit position — this correctly preserves the NUMERIC VALUE of a SIGNED (2's-complement) narrower value when it is widened, exactly mirroring how 2's-complement itself already works (recall from the number-systems chapter that a 2's-complement value's sign bit conceptually "extends" indefinitely leftward in the underlying mathematical representation; sign extension simply makes this conceptual extension concrete in the wider register).
2. ZERO EXTENSION simply fills every newly-added higher-order bit position with 0 — this is CORRECT only for UNSIGNED narrower values (where there is no sign bit to preserve at all, and the value's true magnitude is unaffected by padding with leading zeros, exactly as with ordinary unsigned decimal numbers).
3. Using ZERO extension on a value that was actually meant to be interpreted as SIGNED silently converts any NEGATIVE narrower value into a very LARGE POSITIVE wider value — for instance, sign-extending the 4-bit pattern 1010 (representing −6 in 4-bit 2's complement) to 8 bits correctly gives 11111010 (still −6 in 8-bit 2's complement), but ZERO-extending the SAME 4-bit pattern instead gives 00001010 (10 in 8-bit UNSIGNED, or unsigned decimal, terms) — a value with NO numeric relationship to the original −6 at all.

GATE TRAP: an instruction set specification stating that a particular immediate or displacement field is "SIGN-extended" versus "ZERO-extended" is stating a genuinely load-bearing fact, not an interchangeable implementation detail — applying the wrong extension rule produces a WRONG numeric value for any input where the two rules actually differ (every negative signed value, when zero-extended instead of sign-extended, or vice versa for values whose top bit happens to be set but were intended as unsigned), and a problem that specifies one extension rule explicitly must be worked using EXACTLY that rule, never the other, regardless of which one might seem more "natural" for a specific numeric example.

Worked trace: an instruction encodes an 8-bit signed displacement field containing 11110110 (binary), to be sign-extended into a 16-bit register for use in an address computation. As an 8-bit signed value, 11110110 has its top bit set (negative); computing its magnitude via invert-then-add-1: 00001001+1=00001010=10, so the value is −10. Sign-extending correctly to 16 bits: replicate the sign bit (1) into all 8 new upper bits, giving 1111111111110110 — verifying this is still −10 in 16-bit 2's complement: inverting gives 0000000000001001, adding 1 gives 0000000000001010=10, confirming the sign-extended 16-bit value is indeed −10, matching the original 8-bit value's numeric meaning exactly.

WORKED PROBLEMS

1. EXPRESSION EVALUATION COST ACROSS MACHINE TYPES. Evaluate the number of instructions needed to compute D = (A+B)*C on a 3-address machine, a 2-address machine, and a 0-address (stack) machine, given 2 operators (+ and *). 3-address: exactly 2 instructions (ADD T,A,B then MUL D,T,C — one instruction per operator, no destination-overwriting concerns). 2-address: also 2 arithmetic instructions (ADD A,B giving A=A+B, then MUL A,C giving A=A*C, assuming A may be safely overwritten; a MOVE would be needed first if A's original value must be preserved). 0-address (stack): postfix is AB+C*, needing 3 PUSH instructions (A, B, C — three distinct operand occurrences) plus 2 operator instructions (+ and *), totalling 5 instructions.

2. REPEATED-OPERAND STACK COST. Evaluate (A+B)*(A+B) on a stack machine, and explain why the naive "2 operators means 2 pushes plus 2 operators" undercounts. Postfix: AB+AB+*. This has 4 total operand OCCURRENCES (A twice, B twice), needing 4 PUSH instructions, plus 3 operator instructions (the two + and the one *), totalling 7 instructions — the naive count would wrongly assume only 2 distinct variable names need pushing once each, missing that each OCCURRENCE in the postfix string needs its own separate push regardless of whether the variable name repeats.

3. OPCODE/REGISTER/ADDRESS FIELD SIZING. A machine has 40 distinct opcodes, 16 general-purpose registers, and 2048 addressable memory locations. Find the minimum bits needed for each field. Opcode: ⌈log₂40⌉=6 bits (since 2⁵=32<40≤64=2⁶). Register field: ⌈log₂16⌉=4 bits. Address field: ⌈log₂2048⌉=11 bits.

4. EXPANDING OPCODE, THIRD LEVEL. Continuing worked trace 2 above (12-bit instructions, 40 two-address opcodes used out of 64, 150 one-address opcodes used out of a maximum 192), if only 30 of the resulting 42 leftover zero-address-level patterns are actually used, how many raw bit patterns does this final, partially-used zero-address level consume, and does the total still respect the 4096-pattern invariant? 30 used zero-address opcodes each consume exactly 1 raw pattern (no remaining operand fields): 30×1=30. Total consumed: 2560 (2-address) + 1200 (1-address) + 30 (0-address, used) = 3790, which is LESS than 4096 — the invariant is respected with room to spare, and the remaining 4096−3790=306 patterns are simply unused, available for further future expansion.

5. PC-RELATIVE FORWARD BRANCH. A 2-byte branch instruction sits at address 500, with a signed displacement of +30. Find the effective (target) address. PC after fetch: 500+2=502. EA = 502+30 = 532.

6. MEMORY-INDIRECT ACCESS COUNT. An instruction using memory-indirect addressing also writes its result back to memory. Count the total memory accesses for this single instruction. 1 (instruction fetch) + 2 (memory-indirect operand fetch: pointer fetch, then operand fetch) + 1 (result write-back) = 4 total memory accesses.

7. BASE VS INDEXED ROLE IDENTIFICATION. A program uses a fixed offset of 100 combined with a register that is incremented once per loop iteration to walk through consecutive array elements. Is this base or indexed addressing? Indexed addressing — the register (which varies through the loop) is playing the INDEX role, while the constant offset (100, corresponding to the array's base address) stays fixed, exactly matching the "index register moves through the array" description.

8. CISC VS RISC INSTRUCTION COUNT TRADE-OFF. A CISC machine can add a value directly from memory to a register in ONE instruction. An equivalent RISC (load-store) machine needs a separate LOAD instruction followed by an ADD instruction to accomplish the same task. If the CISC instruction takes 4 clock cycles and each RISC instruction takes 1 clock cycle (due to simpler decode logic and pipelining), compare total cycle counts for performing this operation 100 times. CISC: 100×4=400 cycles. RISC: 100×2×1=200 cycles — despite needing TWICE as many instructions, the RISC approach is FASTER overall here, due to its much lower per-instruction cycle cost, directly illustrating that instruction count alone does not determine which design is actually faster.

9. BASED-INDEXED ACCESS COUNT. An instruction uses based-indexed addressing (base register + index register + displacement) to access an array element, and does not write back to memory. Count the total memory accesses for this instruction. 1 (instruction fetch) + 1 (single operand fetch from the fully-computed effective address, since all three address components are combined in hardware before any memory access occurs) = 2 total memory accesses — exactly the same count as a plain single-component addressing mode, despite combining three address components.

10. ENDIANNESS BYTE LOOKUP. The 32-bit value 0x11223344 is stored at address 4000 in little-endian format. What byte value is stored at address 4003? Bytes, most-significant first: 11, 22, 33, 44. Little-endian places the LEAST significant byte first: address 4000=44, 4001=33, 4002=22, 4003=11 — so address 4003 holds 11 (the MOST significant byte, since little-endian places it at the HIGHEST address of the four).

11. FIXED VS VARIABLE LENGTH TRADE-OFF IDENTIFICATION. A machine's instruction set includes both a 1-byte "no operation" instruction and a 6-byte "load 32-bit immediate into register" instruction. Is this machine using fixed-length or variable-length encoding, and what is the direct benefit of allowing the short instruction to occupy only 1 byte? This is VARIABLE-length encoding, since different instructions occupy different byte counts. The direct benefit: the extremely common, operand-free NOP instruction consumes only 1 byte of code space rather than being forced to pad out to the full 6-byte width every long instruction needs, improving overall code density for programs that use many such short instructions.

12. FIVE-STAGE MEMORY ACCESS BREAKDOWN. An instruction uses register-indirect addressing to fetch one operand, uses a register for its second operand, computes a result, and stores the result to memory using direct addressing. Break its total memory accesses down by instruction-cycle stage. Fetch: 1. Decode: 0. Operand fetch: 1 (register-indirect for the first operand) + 0 (register for the second, no memory access) = 1. Execute: 0 (pure computation, no memory touch). Writeback: 1 (direct-addressed memory write). Total: 1+0+1+0+1 = 3 memory accesses.

13. PC-RELATIVE DATA REFERENCE. A 4-byte instruction at address 800 loads a data value using PC-relative addressing with a signed offset of −50. Find the effective address of the data, and explain why this same computation would apply identically if the instruction were a branch instead. PC after fetch: 800+4=804. EA = 804+(−50) = 754. This is IDENTICAL to how a branch target would be computed from the same instruction address, offset, and length, since PC-relative addressing uses the exact same EA formula regardless of whether the resulting address is subsequently used as a data operand location or as a new instruction-fetch target — only the USE of the computed EA differs, not its computation.

14. INSTRUCTION SET DESIGN TRADE-OFF SYNTHESIS. A processor designer is choosing between (a) a 3-address, variable-length CISC design with memory-to-memory arithmetic instructions, and (b) a 3-operand, fixed-length RISC design using only registers for arithmetic (load-store architecture). Summarise, in terms of the trade-offs covered in this chapter, what each design gains and gives up. Design (a): fewer total instructions per program (each instruction can do more, including reading AND writing memory directly), but variable-length encoding complicates fetch/decode (the hardware cannot know an instruction's length until partially decoding it) and memory-to-memory arithmetic instructions may require multiple memory accesses within a single instruction. Design (b): more total instructions per program (separate loads and stores needed around every arithmetic operation), but fixed-length encoding makes fetch/decode simple and predictable, and restricting arithmetic to registers keeps every arithmetic instruction's memory-access count at zero, simplifying the instruction-cycle breakdown described above and making the design far easier to pipeline efficiently — the same fundamental "more per instruction, simpler each" versus "less per instruction, faster overall" comparison already worked through numerically in worked problem 8 above.

15. SIGN VS ZERO EXTENSION COMPARISON. An instruction's 4-bit immediate field contains 1100 (representing −4 in signed 2's complement, or 12 if interpreted as unsigned). Compute the result of sign-extending versus zero-extending this field to 8 bits, and state the numeric value each produces. Sign-extension: replicate the top bit (1) into the new upper 4 bits: 11111100 — as an 8-bit signed value, this is still −4 (invert 00000011, add 1 = 00000100 = 4, so the value is −4), correctly preserving the original signed meaning. Zero-extension: fill the new upper 4 bits with 0: 00001100 — as an 8-bit value this is 12, matching the ORIGINAL unsigned interpretation of 1100, but giving a completely different number from the sign-extended result, confirming the two extension rules genuinely diverge whenever the field's top bit is set.

CARRYING THIS FORWARD

Every named technique in this chapter is a variation on the SAME two-part reasoning tool: count how many DISTINCT things need representing (opcodes, registers, addresses, or bit-pattern combinations across an expanding-opcode hierarchy), and count how many STEPS OF INDIRECTION separate an instruction from its actual operand (the addressing-mode table's real organising principle). The expanding-opcode technique is simply the FIRST counting question applied ACROSS several instruction-length TIERS at once, tracking leftover capacity level by level; the addressing-mode table is simply the SECOND counting question applied to TEN specific, named indirection patterns. Neither idea requires memorising ten (or more) independent facts — each requires memorising ONE counting principle and then applying it carefully, tier by tier or mode by mode, to whatever specific numbers a given question supplies, exactly the same "identify the one underlying principle, then apply it carefully to the specific numbers given" discipline this book has applied to every subject covered so far.

KEY: three genuinely distinct DESIGN TRADE-OFFS recur throughout this chapter, and it is worth naming all three together one final time, since each pits a SIMPLER, more UNIFORM design choice against a more FLEXIBLE, more EFFICIENT alternative: address-count classification (fewer explicit addresses means shorter, more uniform instructions but MORE of them needed per computation); fixed-versus-variable instruction length (fixed length means simpler, faster fetch/decode but wasted bits on short instructions; variable length means denser code but harder decode logic); and CISC versus RISC more broadly (fewer, richer instructions doing more per instruction, versus more numerous, simpler instructions each doing less but enabling faster per-instruction execution). Every one of these three trade-offs asks the identical underlying question — trade AWAY some flexibility or density in exchange for SIMPLER, more predictable hardware, or trade AWAY that simplicity in exchange for greater flexibility or density — and recognising this SHARED underlying question is what turns three seemingly separate architectural debates into one repeatedly-applied design principle.

This same "simplicity versus flexibility" trade-off, appearing here for the third time in this book (after fixed-point versus floating-point number representation, and literal-count versus input-count in multi-level combinational logic), is worth explicitly flagging as a GENERAL pattern in engineering design far beyond computer architecture specifically — whenever two design options are compared and one is described as "simpler but less efficient" while the other is "more efficient but more complex," the underlying reasoning template from this chapter (name exactly what resource the simpler option economises on, and exactly what resource the more complex option economises on instead) applies directly, regardless of the specific domain the trade-off happens to arise in.

A final connecting note, looking ahead to the chapters that follow directly in this same Computer Organization and Architecture sequence: the datapath chapter builds the ACTUAL hardware structures (register file, ALU, buses) that carry out the fetch-decode-execute cycle described above, the pipelining chapter examines what happens when SEVERAL instructions are made to overlap their stages simultaneously (directly motivating the RISC load-store and fixed-length design choices discussed here, since both choices exist specifically to make that overlapping practical), and the memory-hierarchy chapter examines what happens when the SIMPLE "1 access, done" model of a memory reference assumed throughout this chapter is replaced by a REALISTIC memory system with caches, multiple levels, and variable access latencies. Every one of those later chapters' central concerns is foreshadowed directly by material already covered here — this chapter's instruction-cycle breakdown, memory-access counting, and RISC-versus-CISC discussion are the DIRECT foundation the next three chapters build on, rather than a separate, self-contained topic to be set aside once its own worked problems are finished.

Sign extension and zero extension, the chapter's final topic, connects back to this same instruction-cycle framework in a small but genuinely important way: the extension step happens during DECODE, immediately after an immediate or displacement field has been extracted from the fetched instruction bits, and BEFORE that value is used in any subsequent address computation or arithmetic operation — getting this single step wrong corrupts every later stage that depends on the resulting value, exactly the kind of small, easy-to-overlook detail this book has repeatedly flagged as worth checking explicitly rather than assuming by default, since assuming the "wrong" extension rule silently produces a plausible-looking but numerically incorrect result with no obvious warning sign at all.

Taken together, the topics covered in this chapter — address-count classification, opcode expansion, addressing-mode indirection depth, endianness, fixed-versus-variable-length encoding, CISC-versus-RISC philosophy, the instruction cycle itself, and sign/zero extension — form a coherent picture of exactly what happens between a program being WRITTEN in some source form and a processor actually EXECUTING it: every design decision covered here answers some specific piece of the question "how is a computation represented, bit for bit, so that a physical processor can fetch, decode, and carry it out correctly and efficiently." None of these topics exist in isolation from the others; a change in address-count classification changes instruction length, which interacts directly with the fixed-versus-variable-length trade-off, which in turn interacts directly with the CISC-versus-RISC design philosophy a whole instruction set is built around. Approaching a new, unfamiliar architecture description by asking, in order, "how many explicit addresses does this format use, how is its opcode space organised, what addressing modes does it support, and is its encoding fixed or variable length" is a reliable, systematic way to build a complete mental model of that architecture's instruction set quickly, using exactly the vocabulary and reasoning tools this chapter has built up in full, one deliberate question at a time rather than attempting to absorb an unfamiliar architecture's entire specification all at once.

This systematic, question-by-question approach is worth practising explicitly on any new instruction set description encountered from here onward, whether in a textbook problem, a real processor's reference manual, or an exam question describing a hypothetical machine invented purely for testing purposes. The specific numbers will always differ, but the small set of underlying questions this chapter has built out in full — how many addresses, how is the opcode space organised, what addressing modes are supported, is encoding fixed or variable, and how are narrower fields correctly widened — remain exactly the same regardless of which particular architecture is actually being described. Working through them methodically, in this same fixed order, turns even a completely unfamiliar hypothetical machine into a tractable, well-understood object within a few careful minutes of analysis, rather than an intimidating wall of unfamiliar notation and terminology that, at first glance, appears to share nothing at all with material already studied elsewhere in this book. Underneath the surface-level differences in naming, the very same handful of counting and trade-off principles built up carefully across this chapter is almost always, in practice, what a well-posed question about any given instruction set is genuinely testing for — regardless of the specific unfamiliar names, mnemonics, or field widths that question happens to have dressed the same underlying principle up in, and regardless of the specific course, textbook, or examination it ultimately comes from. None of that surface variation ever genuinely changes the single, small, well-understood underlying principle actually being tested underneath it.

As a concrete illustration of the checklist in action, consider a hypothetical 16-bit instruction word with a 4-bit opcode field and three 4-bit register-address fields. Applying the first question immediately tells us this is a three-address machine, since every arithmetic instruction can name two distinct source registers and a separate destination register without any of them needing to be reused or implied. Applying the second question, a 4-bit opcode field allows exactly 16 distinct instructions to be encoded directly; if the design later needs more than 16 opcodes, the only options are widening the opcode field at the cost of fewer or narrower operand fields, or reserving one opcode value as an escape code that redirects to a secondary opcode table, exactly the expanding-opcode technique this chapter covered in depth. Applying the third and fourth questions together, with only 4 bits left per register field after the opcode, at most 16 architectural registers can be addressed directly, and because every field here is a fixed 4 bits wide, this format is fixed-length rather than variable-length, which simplifies the fetch and decode logic but caps how large an immediate operand or memory address can be embedded directly inside a single instruction word.
`
};
