// Textbook chapter: Instruction Pipelining.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['coa-pipelining'] = {
  figs: [
    {
      id: 'pipeline-fill-drain',
      caption: 'A 5-stage pipeline processing 4 instructions: each row is an instruction, each column a clock cycle. The diagonal shape shows the pipeline filling (top-left) then draining (bottom-right) — exactly the source of the "+(k-1)" overhead term.',
      svg: '<svg viewBox="0 0 320 130" width="100%" style="max-width:320px;height:auto" xmlns="http://www.w3.org/2000/svg"><g font-size="9" fill="currentColor" font-family="monospace"><text x="10" y="20">I1: F D E M W</text><text x="10" y="40">I2:   F D E M W</text><text x="10" y="60">I3:     F D E M W</text><text x="10" y="80">I4:       F D E M W</text></g></svg>'
    },
    {
      id: 'forwarding-paths',
      caption: "Forwarding paths route a result directly from a later stage's output latch back to an earlier stage's ALU input, skipping the register file entirely — EX/MEM to EX handles a one-cycle-away dependency, MEM/WB to EX handles a two-cycle-away one.",
      svg: '<svg viewBox="0 0 320 110" width="100%" style="max-width:320px;height:auto" xmlns="http://www.w3.org/2000/svg"><g font-size="10" fill="currentColor" font-family="monospace"><text x="10" y="20">EX</text><text x="80" y="20">MEM</text><text x="160" y="20">WB</text></g><g stroke="currentColor" stroke-width="1.2" fill="none" marker-end="url(#fwarrow)"><path d="M80,30 Q40,55 20,70"/><path d="M160,30 Q90,70 20,90"/></g><defs><marker id="fwarrow" markerWidth="7" markerHeight="7" refX="5" refY="3.5" orient="auto"><path d="M7,3.5 L0,0 L0,7 Z" fill="currentColor"/></marker></defs></svg>'
    }
  ],
  text: `
WHY PIPELINING, AND THE ONE IDEA IT ADDS TO EVERYTHING SO FAR

The datapath chapter established how a SINGLE instruction moves through fetch, decode, execute, memory-access, and write-back, one control step at a time, with the NEXT instruction's own fetch beginning only once the CURRENT instruction has fully finished. PIPELINING adds exactly one new idea on top of that: let SEVERAL instructions occupy DIFFERENT stages of this SAME cycle SIMULTANEOUSLY — while instruction 1 is being decoded, instruction 2 can already be fetched; while instruction 1 executes, instruction 2 decodes and instruction 3 fetches, and so on. This chapter is entirely about the CONSEQUENCES of that one idea: the SPEEDUP it buys under ideal conditions, the specific ways instructions sharing a pipeline can genuinely CONFLICT with one another (hazards), and the standard techniques for detecting and resolving each kind of conflict.

THE IDEAL PIPELINE: FILL, DRAIN, AND THE SPEEDUP FORMULA

[[FIG:pipeline-fill-drain]]

A k-STAGE pipeline processing n instructions, under IDEAL conditions (no hazards, no stalls of any kind), reaches STEADY STATE where, once the pipeline is FULL, ONE instruction COMPLETES every single clock cycle.

1. NON-PIPELINED total time for n instructions, each needing all k stages executed SEQUENTIALLY (one instruction's entire k-stage journey finishing before the next instruction's own first stage begins): k×n cycles.
2. PIPELINED total time for the SAME n instructions: (k+n−1) cycles — the FIRST instruction still needs the full k cycles to traverse all k stages (the pipeline's FILL time), but every SUBSEQUENT instruction, once the pipeline is full, adds just ONE additional cycle each (since it follows immediately behind the previous instruction, one stage back, at every point) — giving k (for the first instruction) plus (n−1) more cycles (one per remaining instruction) = k+n−1 total.
3. IDEAL SPEEDUP: S = (k×n)/(k+n−1) — directly the ratio of the two total-time formulas above. As n→∞, the "+n−1" term in the denominator dominates over the fixed "+k" fill/drain overhead, and S approaches k exactly — the THEORETICAL MAXIMUM possible speedup, equal to the number of pipeline stages, achieved only in the limit of an arbitrarily long instruction stream.

Worked trace: a processor has a 5-stage pipeline (k=5) executing 100 instructions (n=100), with NO stalls. Non-pipelined time: 5×100=500 cycles. Pipelined time: 5+100−1=104 cycles. Speedup: S=500/104≈4.81 — notably CLOSE to, but still meaningfully below, the theoretical maximum of 5, confirming that even for a moderately large n=100, the fixed "+4" fill/drain overhead still costs a measurable fraction of the ideal speedup.

GATE TRAP: the ideal speedup formula S=(k×n)/(k+n−1) assumes ZERO STALLS of any kind — the MOMENT a question mentions hazards, stall cycles, or branch penalties, this formula no longer applies at all, and the CORRECT approach switches entirely to the CPI-based speedup formula covered next; applying the ideal, no-stall formula to a question that has ALREADY specified stall information is a serious and common error, discarding real information the question has explicitly provided.

CPI-BASED SPEEDUP: THE REALISTIC FORMULA WITH STALLS

Once STALLS (from any hazard type) are present, the PIPELINED CPI (cycles per instruction) is no longer simply 1 — it becomes CPI_pipelined = 1 + (average stall cycles incurred per instruction, averaged across every hazard type that contributes any stalls at all).

REALISTIC SPEEDUP: Speedup = CPI_non-pipelined / CPI_pipelined (assuming both machines share the SAME clock period — if the clock periods genuinely differ, the fully general formula is Speedup = (CPI_non-pipe × Tclock,non-pipe) / (CPI_pipelined × Tclock,pipe), directly analogous to the total-execution-time formula already used throughout the datapath chapter).

PIPELINED EXECUTION TIME for n instructions, INCLUDING all stalls = (k+n−1+total stall cycles)×Tclock, or, EQUIVALENTLY once steady-state stall behaviour is captured directly in CPI_pipelined, simply n×CPI_pipelined×Tclock.

EFFICIENCY = S/k (how close the ACTUAL achieved speedup comes to the theoretical maximum k). THROUGHPUT = n/(total execution time), often expressed directly as INSTRUCTIONS PER CYCLE, IPC=1/CPI (the reciprocal relationship between CPI and IPC is worth holding as an instantly-recalled fact, since the two measures are simply two different ways of expressing the identical underlying rate).

Worked trace: a 5-stage pipelined CPU has a BASE CPI of 1, but 20% of all instructions are LOADS immediately followed by a dependent instruction (each such case incurring exactly 1 stall cycle, the classic load-use hazard covered in full below), and 15% of instructions are TAKEN branches with a 2-cycle penalty each (a predict-not-taken scheme, also covered below).

1. CPI_pipelined = 1 + (0.20×1) + (0.15×2) = 1+0.20+0.30 = 1.50 — building up the realistic CPI from the BASE rate (1) plus each hazard TYPE's own contribution (its FREQUENCY multiplied by its PER-OCCURRENCE stall penalty).
2. If the equivalent NON-pipelined machine has CPI_non-pipe=5 (one instruction serially traverses all 5 stages) and both machines share the same clock period: Speedup = 5/1.50 ≈ 3.33.
3. This demonstrates directly how hazard-induced stalls ERODE the IDEAL 5× speedup down to a REALISTIC 3.33× — the standard two-step computation pattern this entire topic is built around: FIRST build CPI_pipelined from each hazard type's frequency and per-occurrence penalty, THEN divide the reference CPI by that realistic figure to get the true, achievable speedup.

STRUCTURAL HAZARDS

A STRUCTURAL HAZARD occurs when TWO instructions, simultaneously occupying DIFFERENT pipeline stages, both need the SAME physical hardware RESOURCE at the SAME cycle — the classic example is a single, UNIFIED memory port needed simultaneously by one instruction's INSTRUCTION FETCH and another instruction's DATA-MEMORY access.

1. THE FIX: either DUPLICATE the contested resource (separate INSTRUCTION and DATA caches or memory ports — the standard modern solution, entirely eliminating this specific structural hazard by construction) or STALL one of the two competing instructions until the resource becomes free (a cheaper hardware solution, at the direct cost of a lost cycle whenever the conflict actually arises).
2. Structural hazards are, in a real sense, the MOST STRAIGHTFORWARD hazard category — they are a pure HARDWARE-RESOURCE-COUNTING problem (exactly the same "how many of resource X are available versus how many are simultaneously demanded" question already met throughout this book, from register-file port counts in the previous chapter to bus-sharing constraints before that), and they can always be eliminated ENTIRELY given enough hardware duplication, unlike data and control hazards, which are FUNDAMENTAL to overlapping otherwise-dependent instructions and cannot be eliminated by hardware duplication alone.

DATA HAZARDS: RAW, WAR, AND WAW

A DATA HAZARD occurs when an instruction DEPENDS on the result of a PRIOR instruction that is STILL somewhere in the pipeline, not yet fully completed. Three sub-types, classified by the ORDER of reads and writes involved:

RAW (READ AFTER WRITE, or TRUE dependency): a LATER instruction reads a register that an EARLIER, not-yet-completed instruction will WRITE. This is the ONLY hazard type reflecting a GENUINE program dependency (the later instruction's correctness truly DEPENDS on the earlier instruction's result), and it is the ONLY hazard type that FORWARDING (covered next) can directly resolve.

WAR (WRITE AFTER READ, or ANTI-dependency): a LATER instruction WRITES a register BEFORE an EARLIER instruction has actually READ the OLD value — CANNOT occur at all in a SIMPLE, IN-ORDER pipeline with a SINGLE write-back stage (since instructions complete their write-backs strictly in PROGRAM order in such a design, an earlier instruction's read always happens before a later instruction's write physically could), but becomes a genuine concern in OUT-OF-ORDER or multi-issue superscalar designs, where instructions may complete in a DIFFERENT order than they were fetched.

WAW (WRITE AFTER WRITE, or OUTPUT dependency): TWO instructions write the SAME register OUT of program order, risking the FINAL stored value being WRONG (the earlier instruction's write incorrectly overwriting the later instruction's already-completed write) — again, mainly a concern in OUT-OF-ORDER or superscalar designs, not in a simple in-order single-issue pipeline.

GATE TRAP: a question asking "which hazard type CANNOT occur in a simple 5-stage IN-ORDER pipeline with one write-back stage" is testing EXACTLY this distinction directly — the answer is WAR and WAW TOGETHER (neither can occur), since a simple in-order design's SINGLE write-back stage, combined with instructions necessarily completing write-back in the SAME order they were fetched, structurally PREVENTS either anti- or output-dependency violations from ever arising; ONLY RAW hazards are genuinely possible in such a design, making RAW the ONLY data-hazard type a simple in-order pipeline's forwarding logic ever actually needs to address.

FORWARDING (BYPASSING): RESOLVING RAW HAZARDS WITHOUT STALLING

[[FIG:forwarding-paths]]

FORWARDING routes a RESULT directly from the OUTPUT LATCH of one pipeline stage (typically the EX/MEM latch, or the MEM/WB latch) BACK to the INPUT of an EARLIER stage (typically the EX stage's own ALU input multiplexer) — completely BYPASSING the normal, slower path of writing the result to the register file and then reading it BACK out again, which would otherwise force a stall while waiting for that full round trip.

1. EX/MEM-to-EX forwarding: handles the case where the IMMEDIATELY PRECEDING instruction (one position earlier in program order) produces the value the CURRENT instruction needs — a "one-cycle-away" RAW hazard, resolved by forwarding the result the moment it becomes available at the end of the producing instruction's own EX stage.
2. MEM/WB-to-EX forwarding: handles the case where the instruction TWO positions earlier produces the needed value — a "two-cycles-away" RAW hazard, resolved by forwarding from the LATER MEM/WB latch instead, since by the time the CONSUMING instruction reaches its own EX stage, the PRODUCING instruction's result has already progressed one stage further (into MEM/WB) than the immediately-preceding-instruction case above.
3. Both forwarding paths together handle EVERY ALU-to-ALU RAW dependency in a standard 5-stage pipeline WITHOUT needing any stall cycles at all — the forwarded value simply arrives at the ALU's input multiplexer in time for the cycle it is actually needed, entirely sidestepping the register file round trip.

THE LOAD-USE HAZARD: THE ONE RAW CASE FORWARDING CANNOT FULLY FIX

EVEN with FULL forwarding implemented, one specific RAW hazard pattern CANNOT be entirely eliminated by forwarding alone: a LOAD instruction's result is only available at the END of its own MEM stage (since the value must actually be FETCHED from memory during that stage before it exists at all), but an IMMEDIATELY FOLLOWING dependent instruction needs that value at the START of ITS OWN EX stage — precisely ONE CYCLE EARLIER than forwarding, however aggressively implemented, could possibly supply it.

1. This forces EXACTLY ONE UNAVOIDABLE stall cycle (often called a LOAD-DELAY SLOT or a PIPELINE INTERLOCK BUBBLE) whenever a load is IMMEDIATELY followed by an instruction that depends on its result — UNLESS the COMPILER can reorder the instruction stream to insert some genuinely INDEPENDENT instruction into that gap instead, filling the otherwise-wasted cycle with real, useful work rather than a pure stall.
2. GENERAL RULE for the number of stall cycles a RAW hazard needs WITHOUT forwarding: (the pipeline stage where the value is PRODUCED) minus (the pipeline stage where the value is actually NEEDED), whenever the needed stage would otherwise come TOO EARLY relative to when the value becomes available. WITH forwarding, this reduces to exactly 0 for essentially all ordinary ALU-to-ALU dependencies (since forwarding paths deliver the value exactly when needed), and to exactly 1 for the SPECIFIC load-use case (since forwarding still cannot reach one cycle EARLIER than the value's own true availability, no matter how the forwarding paths are wired).

GATE TRAP: assuming forwarding eliminates ALL possible data hazards is a serious and very commonly tested misconception — forwarding fully eliminates ordinary ALU-to-ALU RAW hazards, but the LOAD-USE hazard specifically remains, requiring EXACTLY one unavoidable stall cycle in a classic 5-stage pipeline, REGARDLESS of how complete the forwarding network otherwise is; recognising load-use as a genuinely SEPARATE, structurally unavoidable case (rather than just another instance forwarding should "obviously" also fix) is essential to correctly counting stall cycles in any pipeline-CPI computation.

CONTROL HAZARDS: THE BRANCH PROBLEM

A CONTROL HAZARD (or BRANCH HAZARD) arises because the pipeline does NOT know the NEXT instruction's correct address until a BRANCH instruction has been RESOLVED (its condition evaluated and its target computed) — and this resolution typically happens SEVERAL stages AFTER the branch itself was fetched, during which time the pipeline may have ALREADY fetched one or more WRONG-PATH instructions that must later be discarded if the branch's actual outcome differs from what was assumed.

FOUR standard techniques for handling this, each trading off DIFFERENT amounts of penalty against DIFFERENT amounts of hardware complexity:

1. STALL (FREEZE) until the branch resolves: the SIMPLEST approach — fetch NOTHING new until the branch's outcome and target are both known, paying a fixed penalty of (branch-resolution stage − fetch stage) cycles on EVERY SINGLE branch encountered, regardless of whether it is ultimately taken or not.
2. PREDICT NOT-TAKEN: speculatively fetch the FALL-THROUGH instruction (the very next sequential instruction) WHILE the branch is still resolving; if the branch turns out to actually BE taken, the speculatively-fetched fall-through instructions must be FLUSHED (discarded) and refetching begins from the correct target — the penalty is paid ONLY on TAKEN branches, not on every branch.
3. PREDICT TAKEN: the mirror-image strategy — speculatively fetch from the PREDICTED TARGET address instead of the fall-through — the penalty is paid ONLY when the prediction turns out WRONG (a MISPREDICTED branch), which for predict-taken specifically means paying the penalty only on branches that turn out NOT to be taken after all.
4. DELAYED BRANCH: the COMPILER deliberately places an instruction that must EXECUTE REGARDLESS of the branch's eventual outcome into one or more "DELAY SLOT(S)" immediately following the branch instruction itself — since this delay-slot instruction executes UNCONDITIONALLY either way, the pipeline always does GENUINELY USEFUL work during what would otherwise be a wasted stall cycle, PROVIDED the compiler can actually find enough independent instructions to fill the available delay slot(s) in the first place.

GATE TRAP: getting the branch-penalty DIRECTION backwards for predict-taken versus predict-not-taken is a very commonly tested confusion — PREDICT-NOT-TAKEN pays its penalty ONLY when the branch IS actually taken (the speculative fall-through fetch turned out wrong), while PREDICT-TAKEN pays its penalty ONLY when the branch is actually NOT taken (the speculative target fetch turned out wrong) — the two strategies' penalty conditions are EXACT OPPOSITES of each other, and confusing which strategy is penalised by which outcome is an easy, easily-avoided error worth double-checking explicitly on every such question.

BRANCH PENALTY AND ITS CONTRIBUTION TO CPI

BRANCH PENALTY = (the number of instructions FETCHED, then later SQUASHED, due to the delay before the branch is actually resolved) × 1 cycle each — directly the count of wrong-path (or simply not-yet-useful) instructions that entered the pipeline before the branch's true outcome became known.

THE OVERALL CPI CONTRIBUTION from branches = (branch FREQUENCY, the fraction of all instructions that are branches) × (branch PENALTY, in cycles, per relevant occurrence) × (MISPREDICTION RATE, if a prediction scheme rather than an unconditional stall is being used — for an unconditional stall strategy, this misprediction-rate factor is simply 1, since EVERY branch pays the full penalty regardless of its actual outcome).

1. For a PREDICT-NOT-TAKEN scheme specifically, the RELEVANT "misprediction rate" to multiply by is the fraction of branches that are ACTUALLY TAKEN (since ONLY taken branches trigger the flush-and-penalty under this specific scheme) — a subtlety worth stating explicitly, since the word "misprediction rate" can otherwise be misread as some SEPARATE quantity rather than being, in this specific case, EXACTLY the taken-branch fraction.
2. For a PREDICT-TAKEN scheme, the relevant rate is instead the fraction of branches that are ACTUALLY NOT TAKEN (the mirror-image condition, exactly matching the GATE TRAP stated above about the two schemes' opposite penalty conditions).

Worked trace: a pipeline resolves branches 3 stages after fetch (so an UNCONDITIONAL stall would cost 3 cycles per branch). Branches make up 20% of all instructions, and 60% of branches are actually taken. Using a PREDICT-NOT-TAKEN scheme (penalty paid only on taken branches, at the full 3-cycle cost each):

1. CPI contribution from branches = (branch frequency) × (penalty per taken branch) × (fraction actually taken) = 0.20 × 3 × 0.60 = 0.36.
2. Contrast this directly against an UNCONDITIONAL STALL strategy on the identical branch mix: CPI contribution = 0.20 × 3 × 1 (every branch pays the full penalty, regardless of outcome) = 0.60 — noticeably WORSE than the predict-not-taken scheme's 0.36, since predict-not-taken entirely avoids paying any penalty on the 40% of branches that are NOT taken (which, under an unconditional stall, would have paid the full 3-cycle cost for no reason at all).

A FULLY WORKED PIPELINE DIAGRAM WITH A HAZARD

Trace, cycle by cycle, the following short instruction sequence through a classic 5-stage (F,D,E,M,W) pipeline WITH full forwarding, to see a load-use stall arise directly and explicitly: LOAD R1,0(R2); ADD R3,R1,R4 (depends on R1, the just-loaded value); SUB R5,R6,R7 (independent of both prior instructions).

1. Cycle 1: LOAD is in F.
2. Cycle 2: LOAD in D; ADD in F.
3. Cycle 3: LOAD in E; ADD in D; SUB in F.
4. Cycle 4: LOAD in M (its value becomes available only at the END of this cycle); ADD would normally be in E here, but since ADD needs R1's value AT THE START of its own E stage — one cycle EARLIER than LOAD's M-stage result becomes available — the hazard-detection hardware INSERTS A STALL (a bubble): ADD is HELD in D for one extra cycle instead of advancing to E, and SUB is correspondingly held in F for one extra cycle as well (nothing behind ADD can advance past it while it is stalled).
5. Cycle 5: LOAD in W; ADD (now able to receive R1 via MEM/WB-to-EX forwarding, since LOAD has just completed its MEM stage) proceeds into E; SUB proceeds into D.
6. Cycle 6 onward: ADD proceeds to M, then W; SUB proceeds to E, M, W in its own turn, one stage behind ADD throughout, with NO further stalls needed since SUB was never actually dependent on either preceding instruction at all.

KEY: this fully traced example makes the load-use hazard's single unavoidable stall cycle CONCRETE rather than abstract — the stall is not an arbitrary "penalty" imposed by the hazard-detection hardware out of caution; it is the DIRECT, unavoidable consequence of LOAD's result genuinely not existing yet (not being computed, not merely "not yet forwarded") at the exact moment ADD's own EX stage would otherwise need it, and no forwarding path, however cleverly wired, can deliver a value that has not been produced yet at all.

SUPERSCALAR AND SUPERPIPELINED DESIGNS

Beyond a SINGLE k-stage pipeline processing ONE instruction per stage at a time, two further DISTINCT scaling strategies push performance further, and it is worth keeping them CAREFULLY distinguished since they solve DIFFERENT problems and are frequently confused with one another.

1. A SUPERPIPELINED design INCREASES the NUMBER of pipeline stages (a deeper pipeline, say 10 or 15 stages instead of 5), keeping the same ONE-instruction-per-cycle issue rate but making EACH individual stage's own work SIMPLER and hence FASTER, allowing a HIGHER clock frequency overall — this directly increases k in the ideal-speedup formula above, pushing the theoretical maximum speedup higher, but also generally INCREASES the number and SEVERITY of hazards (a deeper pipeline means MORE cycles between a value's production and its use before forwarding could possibly help, and a LARGER branch-resolution penalty measured in cycles, even if the underlying WALL-CLOCK time for that penalty stays similar).
2. A SUPERSCALAR design instead INCREASES the NUMBER of instructions that can be ISSUED (started) PER CYCLE — a 2-WAY superscalar processor can fetch, decode, and potentially execute TWO independent instructions simultaneously within the SAME pipeline stage, effectively multiplying the pipeline's steady-state THROUGHPUT beyond the single-instruction-per-cycle ceiling that an ordinary (non-superscalar) pipeline, however deep, can never exceed on its own.
3. These two strategies are ORTHOGONAL — a processor can be BOTH deeply superpipelined AND highly superscalar simultaneously, combining a longer, faster-clocked pipeline with the ability to issue multiple instructions per cycle through it, and real high-performance processors typically DO combine both strategies together rather than choosing only one.

GATE TRAP: confusing "MORE pipeline stages" (superpipelining, which INCREASES k, the depth) with "MORE instructions issued per cycle" (superscalar execution, which increases the WIDTH of issue) is a common terminology mix-up — a superpipelined design's theoretical maximum speedup is still bounded by ONE instruction completing per cycle in steady state (just at a HIGHER clock frequency, since each stage is simpler), while a superscalar design can EXCEED one-instruction-per-cycle throughput even at a FIXED clock frequency, by processing SEVERAL instructions through parallel copies of some pipeline stages simultaneously — the two techniques attack entirely DIFFERENT bottlenecks (clock period versus per-cycle issue width) and are not interchangeable descriptions of the same underlying idea.

KEY: superscalar execution introduces its OWN additional hazard-detection complexity beyond everything already covered for a simple single-issue pipeline — since MULTIPLE instructions are now examined for hazards SIMULTANEOUSLY within the same cycle (rather than one at a time, staggered across cycles as in a simple pipeline), a superscalar design's hazard-detection logic must check for RAW, WAR, and WAW dependencies not only BETWEEN instructions already in different pipeline stages, but also AMONG the several instructions being issued TOGETHER in the very same cycle — this is precisely why WAR and WAW hazards, entirely impossible in a simple in-order single-issue design (as established above), become GENUINE concerns the moment a design becomes superscalar (or otherwise allows out-of-order completion), since multiple instructions' writes can now genuinely race against each other within, or very close to, the same cycle.

PIPELINE THROUGHPUT VS LATENCY: A DIRECT DISTINCTION

Two DIFFERENT performance measures are worth keeping explicitly separate when discussing a pipeline's performance: LATENCY (how long a SINGLE instruction takes, from its own fetch to its own completion — always AT LEAST k cycles for a k-stage pipeline, and typically EXACTLY k cycles for an instruction encountering no hazards at all) and THROUGHPUT (how many instructions complete per unit TIME, in STEADY STATE, once the pipeline is full — approaching ONE instruction per cycle for an ideal, hazard-free single-issue pipeline, REGARDLESS of how many stages k that pipeline actually has).

1. Pipelining does NOT reduce any single instruction's own LATENCY at all — in fact, a DEEPER pipeline (more stages, as in superpipelining above) generally INCREASES the latency of each individual instruction, measured in cycles, even though it INCREASES overall throughput by allowing a higher clock frequency and hence more total instructions completed per unit of WALL-CLOCK time.
2. Pipelining's entire performance benefit comes ENTIRELY from improved THROUGHPUT (more instructions completing per unit time OVERALL, once the pipeline is full), NEVER from making any one instruction finish FASTER in isolation — a single, solitary instruction running entirely alone through an otherwise-empty pipeline takes EXACTLY as long (in fact, very slightly longer, once pipeline-register overhead is accounted for) as it would running through an equivalent NON-pipelined design with the same total work divided into the same k stages.

GATE TRAP: assuming pipelining makes EACH instruction execute faster (lower latency) is a fundamental and very common misconception — pipelining is PURELY a throughput-improving technique for a STREAM of MANY instructions overlapping their execution, and it provides absolutely NO latency benefit whatsoever to any single, isolated instruction considered entirely on its own; a question asking specifically about the completion time of ONE particular instruction (rather than the throughput of an entire instruction stream) must be answered using the SAME k-cycle latency a non-pipelined design would also need, not some reduced, "sped up" figure.

DYNAMIC BRANCH PREDICTION: LEARNING FROM PAST BEHAVIOUR

The predict-taken and predict-not-taken schemes above are both STATIC strategies — they apply the SAME fixed rule to every branch, regardless of that specific branch's own individual history. DYNAMIC BRANCH PREDICTION instead tracks EACH branch's own PAST behaviour and predicts its FUTURE outcome based on that specific history, typically achieving substantially better accuracy than any single fixed static rule ever could.

1. A 1-BIT PREDICTOR stores a single bit per branch, recording whether that branch was taken or not LAST time it executed, and predicts the SAME outcome will repeat next time — simple, but has a specific WEAKNESS: a branch that is USUALLY taken but occasionally not (the classic case being a LOOP's exit condition, taken on every iteration except the very LAST one) causes the 1-bit predictor to MISPREDICT TWICE in a row around that single exception — once when the loop actually exits (having predicted "taken" from all the prior iterations), and AGAIN on the NEXT time that same loop is entered afresh (having just updated its single bit to "not taken" from the exit, now wrongly predicting the loop's very FIRST iteration, which is normally taken).
2. A 2-BIT (SATURATING COUNTER) predictor fixes EXACTLY this specific weakness: it uses a small state machine with FOUR states (strongly-taken, weakly-taken, weakly-not-taken, strongly-not-taken), MOVING one step toward the extreme on a correct prediction and one step back only on a WRONG prediction — this means a SINGLE anomalous outcome (the loop-exit case above) shifts the counter only ONE step (from strongly-taken to weakly-taken, say), NOT all the way to fully predicting "not taken," so the VERY NEXT (normal, taken) iteration is STILL correctly predicted taken, avoiding the 1-bit predictor's characteristic double-misprediction around a single exception.
3. A BRANCH TARGET BUFFER (BTB) caches, for RECENTLY-seen branches, not just the PREDICTED outcome but the actual TARGET ADDRESS itself — allowing the pipeline to begin fetching from the predicted target IMMEDIATELY upon recognising the branch (by matching its own address against the BTB's cached entries), rather than needing to WAIT for the branch instruction to be fully decoded before even beginning to compute where its target might be.

KEY: the progression from static prediction (a single fixed rule for every branch) to a 1-bit dynamic predictor (per-branch history, but with a specific double-misprediction weakness around isolated exceptions) to a 2-bit saturating counter (fixing that exact weakness via hysteresis, requiring TWO consecutive wrong predictions before actually flipping the prediction) is a genuine, deliberate engineering REFINEMENT, each step targeting a SPECIFIC, IDENTIFIABLE weakness of the step before it — recognising this refinement-by-identified-weakness pattern (rather than treating "2-bit predictors are just better" as an unexplained given) is worth carrying forward as a template for understanding ANY multi-generation engineering improvement described in similarly incremental terms.

Worked trace: a loop executes 10 iterations, with its exit-condition branch TAKEN on iterations 1 through 9 and NOT TAKEN on iteration 10 (the exit), and this ENTIRE 10-iteration loop then repeats a second time from the start. Compare the number of mispredictions a 1-bit predictor versus a 2-bit saturating counter makes across BOTH runs of the loop combined (assuming the predictor starts in a "predict taken" or "strongly taken" state respectively, carried over correctly from a prior, unrelated execution of this same branch).

1. 1-BIT PREDICTOR: iterations 1–9 of run 1 are all correctly predicted taken (matches the stored bit). Iteration 10 (exit, not taken) is a MISPREDICTION (the bit still says "taken" from iteration 9) — the bit then flips to "not taken." Iteration 1 of run 2 (taken) is now ALSO a misprediction (the bit says "not taken" from the just-completed exit) — the bit flips back to "taken." Iterations 2–9 of run 2 are correctly predicted. Iteration 10 of run 2 (exit) is AGAIN a misprediction. Total: 3 mispredictions across both runs (one at each exit, plus one extra at the start of run 2 immediately following run 1's exit).
2. 2-BIT SATURATING COUNTER (starting "strongly taken"): iterations 1–9 of run 1 correctly predicted, staying at "strongly taken" throughout. Iteration 10 (exit) is a misprediction, moving the counter to "weakly taken" (NOT all the way to "not taken," since a single wrong prediction only steps back by one level). Iteration 1 of run 2 (taken) is now CORRECTLY predicted (the counter is still on the "taken" side, at "weakly taken"), moving back to "strongly taken." Iterations 2–9 of run 2 correctly predicted. Iteration 10 of run 2 is again a misprediction, moving back to "weakly taken." Total: 2 mispredictions (only at the two actual exits, with NO extra misprediction at the start of run 2), confirming directly that the 2-bit predictor avoids the 1-bit predictor's characteristic "double misprediction around one exception" weakness.

BRANCH DELAY AND DEEPER PIPELINE INTERACTION

The BRANCH PENALTY (in cycles) generally GROWS as a pipeline gets DEEPER (more stages between fetch and the point where a branch's outcome is finally resolved) — this is precisely why control hazards become a MORE SEVERE concern in superpipelined designs (covered above) than in a shallower pipeline, even when the underlying prediction ACCURACY (static or dynamic) stays exactly the same.

1. A branch resolving at stage j in a pipeline that fetches new instructions every cycle results in (j−1) WRONG-PATH (or simply not-yet-confirmed) instructions already having entered the pipeline by the time the branch's true outcome becomes known — a DEEPER pipeline (larger j, all else being equal) directly means MORE such instructions must potentially be FLUSHED on a misprediction, a LARGER penalty in cycles even though the UNDERLYING prediction logic and its accuracy have not changed at all.
2. This is exactly why REAL high-performance, deeply-pipelined processors invest HEAVILY in sophisticated dynamic branch prediction (multi-bit counters, branch target buffers, and even more elaborate history-based predictors beyond the scope of this chapter) — the DEEPER the pipeline, the MORE cycles a single misprediction costs, and the MORE VALUABLE every fractional improvement in prediction accuracy becomes as a direct consequence.

WORKED PROBLEMS

1. IDEAL SPEEDUP FOR A SMALL INSTRUCTION COUNT. A 4-stage pipeline processes only 6 instructions with no stalls. Compute the ideal speedup, and compare it to the theoretical maximum. Non-pipelined: 4×6=24 cycles. Pipelined: 4+6−1=9 cycles. Speedup: 24/9≈2.67 — noticeably BELOW the theoretical maximum of 4, since for such a SMALL n, the fixed fill/drain overhead (+3 cycles) represents a much LARGER fraction of the total pipelined time than it would for a large n, directly illustrating why the ideal speedup formula only APPROACHES k in the limit of a very long instruction stream.

2. STRUCTURAL HAZARD IDENTIFICATION AND FIX. A pipeline uses a single, unified memory for both instructions and data. Identify the structural hazard scenario, and state the standard fix. Whenever an instruction's own FETCH stage and an EARLIER instruction's own MEMORY-ACCESS stage coincide in the same cycle, BOTH need the single unified memory port simultaneously — a structural hazard. The standard fix: provide SEPARATE instruction and data memories (or caches), entirely eliminating the resource conflict by duplicating the contested hardware.

3. WHICH HAZARDS ARE IMPOSSIBLE IN A SIMPLE PIPELINE. State which of RAW, WAR, and WAW hazards can occur in a simple, in-order, single-issue 5-stage pipeline with exactly one write-back stage, and briefly justify your answer. Only RAW can occur — WAR requires a LATER instruction to write before an EARLIER one has read (impossible when write-backs happen strictly in program order with only one write-back stage), and WAW requires two writes completing OUT of program order (equally impossible under the same in-order, single-write-back-stage structural guarantee).

4. FORWARDING PATH IDENTIFICATION. An ADD instruction is immediately followed by a SUB instruction that uses ADD's result. Which forwarding path resolves this dependency, and would this dependency require a stall without forwarding? EX/MEM-to-EX forwarding resolves this (a one-cycle-away RAW hazard, since SUB immediately follows ADD in program order). Without forwarding, this WOULD require a stall (the exact number depending on how many stages separate ADD's result-production point from SUB's own need-point) — WITH forwarding, it requires zero stall cycles.

5. LOAD-USE STALL COUNT. A LOAD instruction is immediately followed by an ADD instruction that uses the loaded value. With full forwarding implemented, how many stall cycles are needed, and why can forwarding not eliminate this one entirely? Exactly 1 stall cycle is needed — the LOAD's result becomes available only at the END of its own MEM stage, but the immediately-following ADD needs that value at the START of its own EX stage, one cycle EARLIER than forwarding (which can only deliver a value from where it currently sits in the pipeline, never from one cycle in the future) is physically able to supply.

6. CPI COMPUTATION FROM HAZARD FREQUENCIES. A pipelined CPU has base CPI 1. 25% of instructions are loads immediately followed by a dependent instruction (1 stall cycle each), and 10% of instructions are taken branches with a 3-cycle penalty each. Compute CPI_pipelined, and the speedup versus a non-pipelined CPI of 5 (same clock period). CPI_pipelined = 1+(0.25×1)+(0.10×3) = 1+0.25+0.30 = 1.55. Speedup = 5/1.55 ≈ 3.23.

7. PREDICT-TAKEN CPI CONTRIBUTION. Using the same pipeline as the worked branch-penalty trace above (3-cycle resolution penalty, 20% of instructions are branches, 60% actually taken), compute the CPI contribution from branches under a PREDICT-TAKEN scheme instead of predict-not-taken. Predict-taken pays its penalty only on branches that are NOT taken: CPI contribution = 0.20 × 3 × (1−0.60) = 0.20×3×0.40 = 0.24 — LOWER than predict-not-taken's 0.36 for this SPECIFIC branch mix, since here MORE branches are actually taken (60%) than not (40%), making predict-taken the better strategy for this particular mix; the better choice between the two schemes depends entirely on the actual taken-branch fraction observed for a given workload.

8. DELAYED BRANCH FEASIBILITY. A branch resolves 1 stage after fetch, and the compiler can always find exactly 1 independent instruction to place in a single delay slot. Does this delayed-branch scheme fully hide the branch penalty? If the branch instead resolved 2 stages after fetch, would 1 delay slot still be enough? With resolution 1 stage after fetch, exactly 1 delay slot is needed and available — the penalty is FULLY hidden, since the delay-slot instruction always does useful work exactly during the single cycle that would otherwise be wasted. If resolution instead took 2 stages, a SINGLE delay slot would NOT be enough — 2 delay slots would be needed to fully hide the now-larger penalty, and if the compiler can only reliably fill 1 slot with independent work, at least 1 stall cycle would remain unavoidable regardless of the delayed-branch scheme being used at all.

9. SUPERSCALAR VS SUPERPIPELINED IDENTIFICATION. Processor A doubles its pipeline depth from 5 to 10 stages, keeping single-instruction issue per cycle. Processor B keeps 5 stages but can issue 2 independent instructions per cycle. Classify each design and state which bottleneck each one is actually attacking. Processor A is SUPERPIPELINED — it attacks the CLOCK PERIOD bottleneck (each of the now-simpler stages can run at a higher clock frequency), while still capped at one instruction completing per cycle in steady state. Processor B is SUPERSCALAR — it attacks the PER-CYCLE ISSUE-WIDTH bottleneck directly, potentially completing up to 2 instructions per cycle even without any change to the clock frequency at all.

10. LATENCY VS THROUGHPUT FOR A SINGLE INSTRUCTION. A 5-stage pipeline has a clock period of 2 ns. Find the LATENCY of a single, isolated instruction running with no other instructions in the pipeline, and contrast this with the STEADY-STATE throughput of a long instruction stream. Latency of the single isolated instruction: 5 stages × 2 ns = 10 ns (unaffected by pipelining — it takes exactly as long as it would in a non-pipelined design with the same 5 stages). Steady-state throughput: approaching 1 instruction completed every 2 ns (the clock period) once the pipeline is full with a long instruction stream — a MUCH higher completion RATE than the single instruction's own 10 ns latency might suggest, since throughput and latency are measuring genuinely different things.

11. 1-BIT VS 2-BIT PREDICTOR MISPREDICTION COUNT. A different loop runs 5 iterations taken, 1 not-taken (exit), repeated 3 times total (3 separate runs through this same 6-iteration pattern). Compute total mispredictions for a 1-bit predictor versus a 2-bit saturating counter, both starting in a "taken"-favouring state. 1-bit: each of the 3 runs contributes 1 misprediction at its own exit, PLUS 1 extra misprediction at the START of runs 2 and 3 (immediately following the PRIOR run's exit, exactly as in the worked trace above) — total: 3 (exits) + 2 (extra restarts, only after runs 1 and 2, since there is no run 4 to restart into) = 5 mispredictions. 2-bit: each of the 3 runs contributes exactly 1 misprediction at its own exit only (the restart-of-next-run case is correctly predicted, since a single wrong prediction only steps the counter back one level, not all the way) — total: 3 mispredictions, 2 fewer than the 1-bit predictor's 5.

12. DEEPER PIPELINE BRANCH PENALTY GROWTH. A branch resolves at stage 3 in a 5-stage pipeline, and at stage 6 in an equivalent 10-stage superpipelined redesign of the same processor. Compare the number of instructions that must be flushed on a misprediction in each case. 5-stage design: (3−1)=2 instructions flushed. 10-stage design: (6−1)=5 instructions flushed — more than double, directly illustrating why control hazards become a more severe concern specifically as a result of deepening the pipeline, even without any change to the branch itself or to how frequently it is actually taken.

CARRYING THIS FORWARD

Pipelining's entire subject reduces to one core tension: overlapping instructions BUYS speed (up to k times, in the theoretical limit), but every place two overlapping instructions might genuinely CONFLICT — a shared hardware resource (structural), a genuine data dependency (RAW, the only kind possible in a simple in-order design), or an unresolved branch outcome (control) — COSTS some of that speed back, in the form of stall cycles that must be counted carefully and folded into a realistic CPI_pipelined figure. Every named technique this chapter has covered (resource duplication for structural hazards; forwarding for ALU-to-ALU RAW hazards; an unavoidable single stall for load-use hazards specifically; stalling, static prediction, or dynamic prediction with a branch target buffer for control hazards) is best understood as a SPECIFIC ANSWER to the SAME general question — "given that a conflict of THIS particular kind exists, what is the CHEAPEST way to guarantee the pipeline still produces CORRECT results despite it" — and recognising each fix as an instance of that one recurring question, rather than as an independently memorised named rule, is what makes the hazard taxonomy in this chapter feel like a connected, derivable system rather than a list of separately-memorised facts.

KEY: three genuinely separate SPEEDUP-RELATED quantities recur throughout this chapter, and confusing any pair of them is a reliable source of avoidable error — the IDEAL speedup formula (k×n)/(k+n−1), valid ONLY under the explicit assumption of zero stalls; the REALISTIC, CPI-based speedup CPI_non-pipe/CPI_pipelined, valid once hazard frequencies and penalties are folded into a genuine CPI_pipelined figure; and EFFICIENCY, the ratio of the realistic speedup actually achieved to the theoretical maximum k, measuring specifically how CLOSE a real, hazard-laden design comes to the unattainable ideal. A well-posed question always specifies enough information to determine EXACTLY one of these three quantities correctly, and reaching for the wrong one of the three — most commonly, applying the ideal formula when hazard information has already been given, exactly the GATE TRAP flagged at the very start of this chapter — is the single most consequential mistake this entire topic can produce.

KEY: the superscalar-versus-superpipelined distinction, and the latency-versus-throughput distinction covered directly afterward, are two SEPARATE instances of the SAME broader caution this chapter has repeatedly emphasised — a term that SOUNDS like it should mean "generally faster in every sense" (a "wider" or "deeper" or "more pipelined" design) in fact targets one VERY SPECIFIC bottleneck (issue width; clock period; steady-state throughput) while leaving OTHER performance dimensions (a single instruction's own latency; the OTHER of the two scaling strategies) completely unaffected, or in some cases even made measurably WORSE (a deeper pipeline's larger branch-misprediction penalty, worked through explicitly above); precisely identifying WHICH specific bottleneck a given technique or design change actually addresses, rather than assuming a vague, general "this makes it faster" conclusion, is the single most transferable habit this entire chapter has built, and it applies directly to any future architectural improvement encountered that has not been explicitly named in this chapter at all.

This chapter's material also connects directly and explicitly back to the two chapters preceding it in this same Computer Organization and Architecture sequence. The instruction-cycle breakdown from the machine-instructions chapter (fetch, decode, operand fetch, execute, write-back) is EXACTLY the five stages a classic pipeline overlaps — pipelining does not introduce any new STAGE that was not already present in that earlier single-instruction model; it simply lets MULTIPLE instructions occupy those SAME already-established stages simultaneously rather than one at a time. The datapath chapter's RTL notation and control-step framework is likewise the DIRECT foundation for reasoning about WHAT happens within each pipeline stage during any given cycle — a stall, in RTL terms, is simply a control step that REPEATS the same microoperations (or performs no operation at all) for an extra cycle rather than advancing to the next stage's own distinct RTL statement, and a flush, similarly, is simply the deliberate INSERTION of a no-operation RTL step (a "bubble") into what would otherwise have been a genuinely useful instruction's own pipeline slot.

Recognising pipelining not as an entirely separate, self-contained topic, but as the DIRECT overlapping of stages and control steps ALREADY fully described in the two preceding chapters, is precisely the same connective habit this book has applied to every subject covered throughout its full length — from Boolean algebra's laws underlying every combinational block, through the digital-arithmetic chapter's shift-add-subtract foundation underlying every sequencing algorithm, and now through the machine-instructions and datapath chapters' own fetch-decode-execute framework underlying this chapter's entire hazard taxonomy and speedup analysis. A pipeline hazard is never a mysterious, freestanding new phenomenon to be memorised in isolation — it is always a specific, concrete CONSEQUENCE of two or more instructions' RTL-level control steps genuinely colliding over a shared register, a shared piece of hardware, or an as-yet-unresolved branch outcome, exactly the kind of concrete, re-derivable understanding this entire book has consistently aimed to build, chapter after chapter, subject after subject, across every single topic it has now covered from the very first page onward.

A last, practical closing note on how the various tools in this chapter combine in a typical, fully worked exam-style question: such a question will usually specify a pipeline's depth and clock period, a mix of hazard-causing instruction types with their respective frequencies and per-occurrence penalties, and possibly a branch-handling scheme with its own prediction accuracy, then ask for the resulting CPI, the realistic speedup over an equivalent non-pipelined design, and the total execution time for a stated instruction count. Working through such a combined question is nothing more than applying, in careful sequence, the SAME handful of formulas this chapter has built up individually and worked through repeatedly in isolation: first assemble CPI_pipelined by summing the base rate with each hazard type's own frequency-times-penalty contribution (exactly as done for the load-use-and-branch worked trace early in this chapter), then compute the realistic speedup by dividing the appropriate reference CPI by that assembled figure, and finally multiply by the given instruction count and clock period to reach a genuine total execution time. No new formula beyond what this chapter has already derived in full is ever needed for such a combined question — only the discipline of applying each already-familiar piece in the correct order, carrying each intermediate result forward accurately into the next calculation, exactly the same discipline the datapath chapter's own combined worked problem was built to exercise directly for a different, though closely related, part of this same overall Computer Organization and Architecture sequence.

One further habit worth carrying forward explicitly is checking a computed answer's PLAUSIBILITY before accepting it as final, rather than only checking that the correct formula was invoked. A realistic speedup figure, for instance, can never legitimately exceed the pipeline's own depth k — a five-stage pipeline simply has no mechanism by which it could ever deliver a computed speedup of, say, 7, and a result that large is a certain sign that some quantity was substituted incorrectly somewhere upstream, most often a CPI value that was accidentally inverted or a stall count that was silently doubled. Likewise, CPI_pipelined can never legitimately fall below 1 in the simple scalar pipeline model this chapter has developed throughout, since even a completely hazard-free instruction stream still requires at minimum one cycle per instruction to clear the pipeline in steady state; a computed CPI_pipelined below 1 in such a model again signals an arithmetic slip rather than a genuinely faster design, and would only become legitimate once a genuinely superscalar issue width is introduced explicitly, exactly the distinction this chapter drew carefully in its own superscalar-versus-superpipelined section above. Building this kind of quick plausibility check into every finished calculation, as a final habit alongside the formula-selection and hazard-identification habits already emphasised repeatedly throughout this chapter, catches exactly the kind of silent numerical slip that a purely mechanical formula-substitution approach, applied without any sanity check on the resulting figure, would otherwise let straight through undetected into a final, wrong answer. This single habit, applied consistently, is cheap, quick, and remarkably effective at catching careless mistakes before they cost otherwise-earned marks.
`
};
