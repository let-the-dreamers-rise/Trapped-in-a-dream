// Textbook chapter: Monoids & Groups.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['engmath-groups'] = {
  figs: [
    {
      id: 'algebraic-ladder',
      caption: 'The algebraic-structure ladder: each rung adds exactly one axiom to the rung below it. A structure at a given rung automatically satisfies every axiom of every rung beneath it.',
      svg: '<svg viewBox="0 0 360 220" width="100%" style="max-width:360px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.3" fill="none"><rect x="30" y="185" width="300" height="28"/><rect x="30" y="145" width="260" height="28"/><rect x="30" y="105" width="220" height="28"/><rect x="30" y="65" width="180" height="28"/><rect x="30" y="25" width="140" height="28"/></g><g font-size="11" fill="currentColor" text-anchor="middle"><text x="180" y="203">Magma — closure only</text><text x="160" y="163">Semigroup — + associativity</text><text x="140" y="123">Monoid — + identity</text><text x="120" y="83">Group — + inverses</text><text x="100" y="43">Abelian group — + commutativity</text></g></svg>'
    },
    {
      id: 'z12-subgroup-lattice',
      caption: 'The subgroup lattice of Z12, generated by each divisor of 12. Every subgroup of a cyclic group is itself cyclic, generated by a single element.',
      svg: '<svg viewBox="0 0 340 220" width="100%" style="max-width:340px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><line x1="170" y1="30" x2="90" y2="75"/><line x1="170" y1="30" x2="250" y2="75"/><line x1="90" y1="75" x2="50" y2="120"/><line x1="90" y1="75" x2="130" y2="120"/><line x1="250" y1="75" x2="210" y2="120"/><line x1="50" y1="120" x2="90" y2="165"/><line x1="130" y1="120" x2="90" y2="165"/><line x1="210" y1="120" x2="90" y2="165"/><line x1="90" y1="165" x2="170" y2="200"/></g><g font-size="10" fill="currentColor" text-anchor="middle"><text x="170" y="20">Z12 (order 12)</text><text x="70" y="80">&lt;2&gt; order 6</text><text x="260" y="80">&lt;3&gt; order 4</text><text x="40" y="130">&lt;4&gt; order 3</text><text x="140" y="130">&lt;6&gt; order 2</text><text x="220" y="130">&lt;6&gt; order 2</text><text x="90" y="180">wait: &lt;4&gt; ∩ ...</text><text x="170" y="215">{0} order 1</text></g></svg>'
    }
  ],
  text: `
WHY ALGEBRAIC STRUCTURES

Ordinary arithmetic uses a handful of operations — addition, multiplication — that happen to satisfy a comfortable set of properties: you can add numbers in either order, group them however you like, and always find something to add or subtract back to zero. Group theory asks a more general question: strip away the specific numbers, and ask what SET of properties an operation must satisfy for a given piece of reasoning to still go through. This turns out to classify an enormous range of mathematical objects — clock arithmetic, symmetries of a shape, permutations, matrix operations, even the addition of binary strings — under one shared theory, so that a fact proved once, abstractly, about "any group," applies immediately to all of them without having to be re-derived from scratch for each one. GATE tests this abstraction directly: recognising which axioms a given (set, operation) pair satisfies, and using the handful of theorems that follow from those axioms alone.

BINARY OPERATIONS AND THE OPERATION TABLE

A BINARY OPERATION on a set S is a rule ∗ that takes any two elements a, b ∈ S and produces a result a∗b. For ∗ to genuinely be a binary operation on S, it must satisfy CLOSURE: for every a, b ∈ S, the result a∗b must ALSO be in S — the operation is not allowed to "escape" the set. Subtraction on the natural numbers {0,1,2,...} is NOT closed, since 3−5=−2 is not a natural number; this single failure is enough to disqualify (ℕ, −) from being a binary operation on ℕ at all, before any other property is even considered.

For a FINITE set, a binary operation can be displayed completely as an OPERATION TABLE (also called a CAYLEY TABLE): a grid with one row and one column per element, where the entry in row a, column b is the value a∗b. Such a table makes several properties visible directly, without needing to check every case algebraically:

• CLOSURE is automatic by construction, once every cell of the table is filled with an element already in S — the table format itself enforces it, since there is nowhere else for an entry to be written.
• The IDENTITY element, if one exists, is the element e whose row (read left to right) and whose column (read top to bottom) exactly reproduce the table's own header row and header column — e∗x=x and x∗e=x for every x.
• An INVERSE for element a exists if the identity e appears somewhere in a's row (or column) — the specific column (or row) where it appears names the element that is a's inverse.
• COMMUTATIVITY (a∗b=b∗a for all a,b) is visible as the table being SYMMETRIC across its main diagonal — cell (a,b) equals cell (b,a) for every pair.

ASSOCIATIVITY, by contrast, is NOT visible from a Cayley table by any simple inspection — it must be checked by testing (a∗b)∗c against a∗(b∗c) for triples, or established from how the operation was defined in the first place. This is an important limitation, used directly in one of the standard GATE question types below.

THE ALGEBRAIC-STRUCTURE LADDER

Structures are classified by how many properties their operation satisfies, building up one property at a time. Each rung of the ladder automatically satisfies every property of the rungs beneath it.

[[FIG:algebraic-ladder]]

A MAGMA (or GROUPOID) is just a set with a closed binary operation — nothing more required. Essentially every operation anyone bothers to name is at least a magma, so this rung is rarely interesting on its own, but it is the honest starting point.

A SEMIGROUP adds ASSOCIATIVITY: (a∗b)∗c = a∗(b∗c) for all a,b,c. The positive integers under addition, (ℤ⁺, +), form a semigroup: closed (sum of two positive integers is positive), and associative (ordinary addition is associative). Strings under concatenation also form a semigroup: concatenating "ab" and "cd" gives "abcd", closed within the set of all strings, and grouping three concatenations either way gives the identical result.

A MONOID adds an IDENTITY element e satisfying a∗e = e∗a = a for every a. (ℕ, +) — the natural numbers INCLUDING zero — is a monoid: 0 is the identity, since n+0=0+n=n for every n. (Note (ℤ⁺, +) above was only a semigroup, not a monoid, precisely because 0 is excluded from ℤ⁺ — there is no identity for addition within the strictly positive integers alone.) Strings under concatenation, with the empty string "" included as the identity ("ab"+""="ab"), form a monoid. n×n matrices under matrix multiplication form a monoid, with the identity matrix I as the identity element — but NOT a group, as the next paragraph explains.

A GROUP adds INVERSES: for every a, there exists some a⁻¹ (in the same set) with a∗a⁻¹ = a⁻¹∗a = e. (ℤ, +) is a group: every integer n has an additive inverse −n, also an integer, with n+(−n)=0. The nonzero rational numbers under multiplication, (ℚ\\{0}, ×), form a group: every nonzero rational p/q has inverse q/p, itself a nonzero rational. n×n matrices under multiplication are a MONOID but NOT a group in general, because a SINGULAR matrix (determinant zero) has no multiplicative inverse at all — the monoid axioms (closure, associativity, identity) hold for all matrices, but the group axiom (every element has an inverse) fails for the singular ones.

An ABELIAN GROUP (named after Niels Abel) adds COMMUTATIVITY on top of the full group axioms: a∗b = b∗a for all a,b. (ℤ, +) is abelian. The set of invertible n×n matrices under multiplication IS a group (every element, by construction, is invertible) but is NOT abelian in general — matrix multiplication does not commute (AB ≠ BA in general), giving a genuine example of a non-abelian group, which matters below when permutation groups are introduced as the standard smallest example.

STANDARD NON-EXAMPLES, AND EXACTLY WHY EACH FAILS

Checking which specific axiom fails is the actual skill being tested, not just recognising "this isn't a group."

(ℤ, −) is NOT even a semigroup: subtraction is closed on ℤ (the difference of two integers is an integer), but it is NOT associative — (5−3)−1 = 1, while 5−(3−1) = 3, two different results, so associativity fails outright.

(ℕ, +) (including 0) is a monoid but NOT a group: 0 is a valid identity, but no positive integer n has an additive inverse within ℕ (−n is not a natural number), so the inverse axiom fails for every element except 0 itself.

(ℤ, ×) is a monoid (1 is the identity) but NOT a group: only 1 and −1 have multiplicative inverses that are themselves integers (2's inverse would be 1/2, not an integer), so the inverse axiom fails for every element except ±1.

The EVEN integers under multiplication, ({...,−4,−2,0,2,4,...}, ×), are NOT even a monoid: the operation is closed (product of two even numbers is even) and associative (ordinary multiplication), but there is no identity element WITHIN this set — the usual identity for multiplication, 1, is odd and therefore not a member of the set at all, so no candidate identity exists here.

GATE TRAP: The standard trick question gives a set and operation that is closed and associative (so it passes the semigroup test cleanly) and then asks whether it is a monoid or a group — testing specifically whether you check for an identity ELEMENT OF THE SET ITSELF, not merely whether "an identity" exists in some larger, more familiar system. The even-integers-under-multiplication example above is exactly this trap: 1 is a perfectly good identity for multiplication in general, but it is not IN the set being examined, so the monoid axiom genuinely fails here.

CONSEQUENCES OF THE GROUP AXIOMS

Once a structure is confirmed to be a group, several useful facts follow automatically from just the four axioms (closure, associativity, identity, inverses) — proving them once, in general, means never needing to re-derive them for any specific group.

UNIQUENESS OF THE IDENTITY. Suppose a group had two identities, e and e′. Then e∗e′ = e (treating e′ as the identity, applied to e) but also e∗e′ = e′ (treating e as the identity, applied to e′). So e = e′ — there can be only one identity, and "the identity" is a well-defined, single element.

UNIQUENESS OF INVERSES. Suppose a had two inverses, b and c, so a∗b=e and a∗c=e. Then b = b∗e = b∗(a∗c) = (b∗a)∗c = e∗c = c, using associativity in the middle step and the definition of inverse (b∗a=e, since inverses work on both sides) just before it. So b=c — each element has exactly one inverse, and "a⁻¹" unambiguously names it.

CANCELLATION LAWS. If a∗b = a∗c, then b = c (LEFT cancellation) — multiply both sides on the left by a⁻¹: a⁻¹∗(a∗b) = a⁻¹∗(a∗c), and by associativity this becomes (a⁻¹∗a)∗b = (a⁻¹∗a)∗c, i.e. e∗b = e∗c, i.e. b=c. The mirror argument, multiplying on the right by a⁻¹, proves RIGHT cancellation (b∗a=c∗a implies b=c). Both cancellation laws depend critically on a HAVING an inverse — this is exactly why cancellation fails in ordinary integer arithmetic under multiplication when the common factor is 0 (0 has no multiplicative inverse), even though 2×3=2×3 trivially cancels fine when the common factor DOES have an inverse.

THE SOCKS-AND-SHOES RULE: (a∗b)⁻¹ = b⁻¹∗a⁻¹ — note the ORDER REVERSES. Proof: to show b⁻¹∗a⁻¹ really is the inverse of a∗b, check that their product (in the right order) gives the identity: (a∗b)∗(b⁻¹∗a⁻¹) = a∗(b∗b⁻¹)∗a⁻¹ = a∗e∗a⁻¹ = a∗a⁻¹ = e, using associativity to regroup and the inverse and identity axioms in sequence. (The name comes from real socks and shoes: you put socks on, then shoes; to reverse the process, you take shoes off FIRST, then socks — the reversal order matters exactly the same way here.)

THE LATIN-SQUARE PROPERTY. In the Cayley table of a group, EVERY ROW and EVERY COLUMN is a PERMUTATION of all the elements of the group — each element appears EXACTLY ONCE in every row and exactly once in every column, never zero times and never twice. Proof from cancellation: suppose element x appeared twice in row a, at columns b and c, meaning a∗b = a∗c = x. By left cancellation (proved above, and available precisely because every element has an inverse in a group), b=c — so the two "different" columns b and c were actually the same column all along, meaning x cannot genuinely appear twice in distinct columns of that row. Since the row has exactly |G| cells and every element must appear at least once (by a similar closure-and-counting argument) and cannot appear more than once (just shown), each element appears in that row EXACTLY once. The identical argument, using right cancellation, gives the same conclusion for columns.

GATE TRAP: The Latin-square property is a NECESSARY condition for a table to represent a group (if a row or column repeats an element, or omits one, the table definitely does NOT represent a group), but it is NOT SUFFICIENT on its own — a table can satisfy the Latin-square property perfectly (every row and column a clean permutation) while still failing ASSOCIATIVITY, which the Latin-square check cannot detect at all. A "does this Cayley table define a group" question that gives you a table passing the Latin-square test is NOT yet answered — you must additionally verify associativity by testing enough triples (a∗b)∗c vs a∗(b∗c) directly, since there is no shortcut visible from the table's shape for that particular axiom.

ORDER OF A GROUP AND ORDER OF AN ELEMENT

The ORDER of a group G, written |G|, is simply the number of elements it contains. The ORDER of an element a within a group, written ord(a), is the smallest positive integer n such that aⁿ = e (writing the group operation multiplicatively, so aⁿ means a∗a∗...∗a, n times; in an additively-written group like (ℤ_n,+), the analogous quantity is the smallest positive n with n·a ≡ 0).

Compute every element's order in ℤ_6 = {0,1,2,3,4,5} under addition mod 6, where "aⁿ" becomes "n·a mod 6" and the identity is 0.
ord(0): 1·0=0 immediately, so ord(0)=1 (the identity always has order 1, in any group — a trivial but useful sanity check).
ord(1): 1·1=1, 2·1=2, 3·1=3, 4·1=4, 5·1=5, 6·1=6≡0. First hit at n=6, so ord(1)=6.
ord(2): 1·2=2, 2·2=4, 3·2=6≡0. First hit at n=3, so ord(2)=3.
ord(3): 1·3=3, 2·3=6≡0. First hit at n=2, so ord(3)=2.
ord(4): 1·4=4, 2·4=8≡2, 3·4=12≡0. First hit at n=3, so ord(4)=3.
ord(5): 1·5=5, 2·5=10≡4, 3·5=15≡3, 4·5=20≡2, 5·5=25≡1, 6·5=30≡0. First hit at n=6, so ord(5)=6.

Now the Klein four-group, ℤ_2×ℤ_2 = {(0,0),(0,1),(1,0),(1,1)} under componentwise addition mod 2. Every NON-identity element here has order 2: check (0,1) — 1·(0,1)=(0,1)≠identity, 2·(0,1)=(0,2 mod 2)=(0,0)=identity, so ord((0,1))=2. The same holds for (1,0) and (1,1) by identical reasoning. This is worth contrasting directly with ℤ_4, another group of the same order (4): ℤ_4 DOES have an element of order 4 (namely 1, since 1+1+1+1≡0 mod 4 and no smaller positive multiple gives 0), while the Klein four-group has no element of order 4 at all — every non-identity element tops out at order 2. Two groups can therefore have the identical NUMBER of elements while differing completely in which orders those elements actually take, and this exact difference is what proves the two groups are not isomorphic, worked out fully later in this chapter.

A fundamental fact, stated here and used repeatedly below: aⁿ = e if and only if ord(a) divides n. (One direction: if ord(a)=d and d|n, write n=dk, then a⁁n = (a^d)^k = e^k = e. The other direction requires the division algorithm and is a standard but slightly longer argument, taken as given at this level.)

CYCLIC GROUPS

A group G is CYCLIC if there exists some element g ∈ G, called a GENERATOR, such that every element of G can be written as some power of g (gⁿ for various integers n, including negative n meaning powers of g⁻¹). Write G = ⟨g⟩.

ℤ_n (under addition mod n) is always cyclic, generated by 1: every element k can be written as k = 1+1+...+1 (k times) = k·1.

THE ORDER FORMULA ord(k) = n/gcd(k,n) IN ℤ_n, DERIVED. Rather than take this on faith, derive it in four steps, since it is the single fact underneath every generator-counting question in this section.

1. In ℤ_n, ord(k) is the smallest positive m such that m·k ≡ 0 (mod n) — that is, the smallest m such that n divides m·k exactly.
2. Let d = gcd(k,n), and write k = d·k′ and n = d·n′, where k′ and n′ share NO common factor (dividing both by their gcd always leaves coprime quotients — this is the defining property of the gcd).
3. n | m·k becomes d·n′ | m·d·k′, and cancelling the common factor d from both sides gives n′ | m·k′. Since k′ and n′ are coprime, n′ cannot "help" divide m·k′ via any shared factor with k′ — so n′ must divide m directly.
4. The SMALLEST positive m satisfying n′ | m is m = n′ itself. Substituting back n′ = n/d = n/gcd(k,n) gives ord(k) = n/gcd(k,n), exactly as claimed.

The NUMBER OF GENERATORS of ℤ_n is exactly φ(n), Euler's totient function (the count of integers from 1 to n that are coprime to n) — an element k generates ℤ_n exactly when its order equals n (the maximum possible, meaning no smaller cyclic subgroup could contain it), and by the formula just derived, ord(k)=n/gcd(k,n) equals n exactly when gcd(k,n)=1.

Compute φ(6): integers 1 to 6 coprime to 6 are {1, 5} (2,3,4,6 all share a factor with 6), so φ(6)=2 — ℤ_6 has exactly 2 generators, namely 1 and 5, matching the computation above where both 1 and 5 were found to have order 6 (the full group order), while 2, 3, 4 had smaller orders and are therefore NOT generators.
Compute φ(8): integers 1 to 8 coprime to 8 are {1,3,5,7} (2,4,6,8 share the factor 2 with 8), so φ(8)=4.
Compute φ(12): integers 1 to 12 coprime to 12 are {1,5,7,11} (2,3,4,6,8,9,10,12 each share a factor with 12), so φ(12)=4.

KEY: ord(k) in ℤ_n is n/gcd(k,n), never gcd(k,n) itself and never k directly — this is the single most common slip in this topic. To sanity-check any computed order quickly: it must always be a DIVISOR of n (by Lagrange's theorem, proved later in this chapter), so an "order" that does not divide n signals an arithmetic mistake before you even check it against the definition.

EVERY CYCLIC GROUP IS ABELIAN — proof: if G=⟨g⟩, any two elements are gᵐ and gⁿ for some integers m,n, and gᵐ∗gⁿ = g^(m+n) = g^(n+m) = gⁿ∗gᵐ, using the fact that ordinary integer addition (in the exponent) commutes. So any two elements of a cyclic group automatically commute.

The CONVERSE FAILS: not every abelian group is cyclic. The Klein four-group ℤ_2×ℤ_2 is abelian (componentwise addition of numbers is commutative) but NOT cyclic — check every element's order: the identity (0,0) has order 1, and EVERY other element has order 2 (shown above), so NO element has order 4 (the group's own size), meaning no single element generates the whole group. This is the standard counterexample to memorise for "cyclic implies abelian, but not conversely."

EVERY SUBGROUP OF A CYCLIC GROUP IS ITSELF CYCLIC (stated here; the standard proof picks the smallest positive power of the generator lying in the subgroup and shows every other subgroup element is a power of that one, using the division algorithm — taken as given at this level).

THE NUMBER OF SUBGROUPS of ℤ_n equals the NUMBER OF DIVISORS of n, with exactly one subgroup for each divisor d of n, namely the subgroup generated by n/d (equivalently, the unique subgroup of order d). For ℤ_12, the divisors of 12 are {1,2,3,4,6,12}, so ℤ_12 has exactly 6 subgroups, of orders 1,2,3,4,6,12 respectively.

[[FIG:z12-subgroup-lattice]]

SUBGROUPS

A subset H of a group G is a SUBGROUP if H is itself a group under G's own operation (restricted to H). The SUBGROUP TEST makes checking this efficient: H is a subgroup of G if and only if (1) H is CLOSED under G's operation, and (2) H contains the INVERSE of every one of its own elements (associativity is inherited automatically from G, since it holds for all of G's elements and H's elements are a subset of those; the identity is also automatically obtained, since closure plus inverses forces e=a∗a⁻¹ to be in H for any a∈H). For a FINITE group, closure alone actually suffices (a finite closed subset automatically contains inverses too, by a counting argument), which shortens the check considerably when G is known to be finite.

TRIVIAL SUBGROUPS: every group G has at least the subgroup {e} (just the identity alone, order 1) and G itself (the whole group) as subgroups — these two are called trivial precisely because they exist automatically and reveal nothing special about G's structure.

KEY: The identity element e belongs to EVERY subgroup of G, without exception — a subset that omits e can never be a subgroup, however well-behaved it looks otherwise, since a group (and therefore a subgroup) must have an identity by definition. This is the fastest first check on any "is this subset a subgroup" question: if e is missing, stop immediately, no further checking needed.

GATE TRAP: A subset can satisfy closure while still failing to be a subgroup, if it lacks inverses — the two subgroup-test conditions are BOTH required for an infinite group, and checking only one is not enough. The positive integers under addition, (ℤ⁺, +), form a closed subset of (ℤ, +), but ℤ⁺ is NOT a subgroup of ℤ: it contains no identity (0 is excluded) and no element has an additive inverse within ℤ⁺ (−3 is not a positive integer). For a FINITE subset the "closure alone suffices" shortcut does apply, but that shortcut is only valid for a subset of a group that is already known to be finite — misapplying it to an infinite subset like ℤ⁺ inside ℤ produces exactly this kind of wrong conclusion.

The SUBGROUP GENERATED BY an element a, written ⟨a⟩, is the smallest subgroup containing a — concretely, it is the set of all powers of a: {..., a⁻², a⁻¹, e, a, a², ...}, which for a finite group is exactly {e, a, a², ..., a^(ord(a)−1)}, a cyclic subgroup of order equal to ord(a).

COSETS

For a subgroup H of G and an element a ∈ G, the LEFT COSET aH is the set {a∗h : h ∈ H} — take every element of H and combine it with a on the left. The RIGHT COSET Ha is defined symmetrically: {h∗a : h ∈ H}.

Compute the left cosets of H = {0, 4, 8} (a subgroup of order 3) in ℤ_12 under addition. H itself is the coset 0+H = {0,4,8}. Take an element not in H, say 1: coset 1+H = {1+0, 1+4, 1+8} = {1,5,9}. Take another element not yet covered, say 2: coset 2+H = {2,6,10}. Take another, say 3: coset 3+H = {3,7,11}. Every element of ℤ_12 (0 through 11) now appears in exactly one of these four cosets — {0,4,8}, {1,5,9}, {2,6,10}, {3,7,11} — and no element appears in two different cosets.

This is not a coincidence: COSETS ALWAYS PARTITION THE GROUP. Proof sketch: every element g is in at least one coset (its own, gH, since e∈H means g=g∗e∈gH), and if two cosets aH and bH share even one common element, they must actually be the ENTIRE SAME SET (a short algebraic argument: if a∗h₁=b∗h₂ for some h₁,h₂∈H, then b=a∗h₁∗h₂⁻¹, and since h₁∗h₂⁻¹∈H by closure and inverses, this shows b is in aH, and from there the entire coset bH collapses onto aH element by element). So distinct cosets never partially overlap — they are either completely identical or completely disjoint, exactly the definition of a partition.

ALL COSETS OF H HAVE THE SAME SIZE, namely |H|. Proof: the map h ↦ a∗h from H to aH is a BIJECTION — it is onto by the very definition of aH (every element of aH is a∗h for some h), and it is one-to-one because a∗h₁=a∗h₂ implies h₁=h₂ by left cancellation (available since we're in a group). A bijection between two sets means they have the same size, so |aH|=|H| for every a.

LAGRANGE'S THEOREM

Combining the two coset facts just proved — cosets partition G, and every coset has the same size |H| — gives LAGRANGE'S THEOREM directly: if H is a subgroup of a finite group G, then |H| DIVIDES |G|. Proof: G is partitioned into some number k of cosets of H, each of size |H|, so |G| = k·|H| — meaning |H| is a factor of |G| by construction, with the QUOTIENT k (called the INDEX of H in G) being exactly |G|/|H|.

Lagrange's theorem has several immediate, heavily-tested consequences:

1. THE ORDER OF ANY ELEMENT DIVIDES THE ORDER OF THE GROUP. Since ⟨a⟩ is itself a subgroup of G with |⟨a⟩| = ord(a), Lagrange's theorem applied to this particular subgroup gives ord(a) | |G| directly.

2. A GROUP OF PRIME ORDER p IS CYCLIC, AND HAS NO PROPER NONTRIVIAL SUBGROUPS. By Lagrange, any subgroup's order must divide p — but p is prime, so its only divisors are 1 and p itself, meaning the only possible subgroups are the trivial ones {e} and G. Take any non-identity element a ∈ G: its order must divide p (by consequence 1), and since ord(a)≠1 (a is not the identity), ord(a) must equal p — meaning a alone generates the entire group, so G is cyclic.

3. A GROUP OF ORDER 4 IS EITHER ℤ_4 OR ℤ_2×ℤ_2 (the Klein four-group), and no other structure is possible at this size. (This is a classification result, stated here; verifying it requires checking that these are the only two possibilities up to relabelling, which follows from analysing the possible element orders — by Lagrange, every element's order must divide 4, so orders are restricted to {1,2,4}; either some element has order 4, giving the cyclic ℤ_4, or every non-identity element has order 2, giving the Klein four-group structure — these are the only two cases.)

GATE TRAP: The CONVERSE of Lagrange's theorem is FALSE, and this is asked directly and often: just because a number d divides |G| does NOT guarantee G has a subgroup of order d. The standard counterexample is A_4 (the alternating group on 4 elements, the even permutations of {1,2,3,4}), which has order 12, and 6 divides 12 cleanly — yet A_4 has NO subgroup of order 6 at all. Lagrange's theorem is a one-directional constraint (subgroup order divides group order), never an existence guarantee in the reverse direction.

NORMAL SUBGROUPS AND QUOTIENT GROUPS

A subgroup N of G is NORMAL if its left and right cosets always coincide: aN = Na for every a ∈ G (equivalently, a∗N∗a⁻¹ = N for every a — conjugating the subgroup by any group element leaves it unchanged as a set). Normal subgroups matter because they are EXACTLY the subgroups for which the set of cosets can itself be given a well-defined group structure, called the QUOTIENT GROUP G/N, with coset multiplication (aN)∗(bN) = (a∗b)N — an operation that would not even be well-defined (would depend on which representative a or b you happened to pick from each coset) unless N is normal.

EVERY SUBGROUP OF AN ABELIAN GROUP IS AUTOMATICALLY NORMAL: since a∗n = n∗a for every a and n in an abelian group (commutativity holds for ALL pairs, subgroup elements included), aN = {a∗n : n∈N} = {n∗a : n∈N} = Na trivially, with no extra argument needed. This is why normality is only an interesting distinction to draw in NON-abelian groups — in every abelian group, the distinction simply never bites.

HOMOMORPHISMS AND ISOMORPHISMS

A HOMOMORPHISM φ: G → G′ between two groups is a function that PRESERVES the operation: φ(a∗b) = φ(a)∗′φ(b) for all a,b∈G (using ∗ for G's operation and ∗′ for G′'s operation, which may be entirely different operations on entirely different sets). An ISOMORPHISM is a homomorphism that is additionally a BIJECTION (one-to-one and onto) — meaning G and G′ are, structurally, "the same group" wearing different labels, and every group-theoretic property of one transfers automatically to the other via the correspondence φ.

Because an isomorphism preserves the operation exactly, it must preserve: the IDENTITY (φ(e_G) = e_{G′}, provable directly from the homomorphism property), the ORDER of every element (ord(a) in G equals ord(φ(a)) in G′), whether the group is CYCLIC (a generator maps to a generator), and whether the group is COMMUTATIVE (if G is abelian, φ(a)∗′φ(b)=φ(a∗b)=φ(b∗a)=φ(b)∗′φ(a), so G′ is abelian too, at least on the image of φ, and for a full isomorphism this extends to all of G′).

The KERNEL of a homomorphism φ, ker(φ) = {a ∈ G : φ(a) = e_{G′}}, is always a NORMAL subgroup of G, and φ is INJECTIVE (one-to-one) if and only if ker(φ) = {e} — the only element mapping to the target identity is the source identity itself, with no "collisions" possible.

KEY: A homomorphism's kernel gives an immediate, purely structural test for injectivity, without needing to check every pair of elements individually for a collision: compute ker(φ) once, and if it contains anything besides e, φ cannot be one-to-one — some non-identity element is being sent to the same place as e, so at least two distinct elements (that element and e itself) map to the same image.

DECIDING ISOMORPHISM BETWEEN SMALL GROUPS is done, in practice, by comparing an invariant that any isomorphism MUST preserve, and finding one that differs — this proves the groups are NOT isomorphic without needing to check every possible bijection by brute force. Compare ℤ_4 and ℤ_2×ℤ_2, both of order 4: ℤ_4 has an element of order 4 (namely 1, or 3), while ℤ_2×ℤ_2 has NO element of order 4 (every non-identity element has order 2, as computed earlier) — since an isomorphism must preserve element orders exactly, and the two groups have different SETS of element orders present (ℤ_4 has an order-4 element, ℤ_2×ℤ_2 does not), they cannot possibly be isomorphic.

Compare ℤ_6 and S_3 (the symmetric group on 3 elements, order 6): ℤ_6 is ABELIAN (addition mod 6 commutes), but S_3 is NOT abelian (composing two permutations of 3 objects does not commute in general — verified directly below). Since isomorphism preserves commutativity, and one group has it while the other doesn't, ℤ_6 and S_3 are NOT isomorphic, despite having the identical order 6.

PERMUTATION GROUPS

S_n, the SYMMETRIC GROUP on n elements, is the set of ALL possible bijections (rearrangements) of {1,2,...,n} to itself, under composition. |S_n| = n! (there are n! distinct ways to rearrange n distinct objects, a standard counting fact).

CYCLE NOTATION writes a permutation as a product of disjoint cycles: (1 2 3) means "1 goes to 2, 2 goes to 3, 3 goes back to 1," leaving any unmentioned element fixed. Composing permutations follows the convention (used consistently throughout this section) of applying the RIGHT permutation first, then the LEFT — so for σ=(1 2) and τ=(2 3), the composition στ means "first apply τ, then apply σ": under τ, 1→1, 2→3, 3→2; then applying σ (which swaps 1 and 2) to those results: 1→1→2 (wait, more carefully: τ sends 1 to 1, then σ sends that 1 to 2, so overall 1→2), 2→3→3 (τ sends 2 to 3, σ fixes 3, so overall 2→3), 3→2→1 (τ sends 3 to 2, σ sends 2 to 1, so overall 3→1). Reading off the overall effect 1→2, 2→3, 3→1 gives στ = (1 2 3), a single 3-cycle.

Now compute τσ (reverse order — apply σ first, then τ): σ sends 1→2, 2→1, 3→3; then apply τ (which swaps 2 and 3) to those results: 1→2→3 (σ sends 1 to 2, τ sends that 2 to 3, so overall 1→3), 2→1→1 (σ sends 2 to 1, τ fixes 1, so overall 2→1), 3→3→2 (σ fixes 3, τ sends that 3 to 2, so overall 3→2). Reading off: 1→3, 2→1, 3→2, giving τσ = (1 3 2).

στ = (1 2 3) while τσ = (1 3 2) — TWO DIFFERENT PERMUTATIONS, directly demonstrating that S_3 (and S_n for any n≥3) is NON-ABELIAN, confirming the claim used above to distinguish S_3 from the abelian ℤ_6.

THE ORDER OF A PERMUTATION equals the LCM of the lengths of its disjoint cycles. A permutation written as (1 2 3)(4 5) has a 3-cycle and a 2-cycle; its order is lcm(3,2)=6 — applying it 6 times returns everything to its starting position (the 3-cycle repeats every 3 applications, the 2-cycle every 2 applications, and both first coincide back at the identity simultaneously after lcm(3,2)=6 applications).

A TRANSPOSITION is a 2-cycle, swapping exactly two elements and fixing everything else. Every permutation can be written as a product of transpositions (not uniquely — the same permutation admits many different such decompositions), but the PARITY (whether the number of transpositions used is even or odd) is always the SAME regardless of which particular decomposition is chosen — this is a genuine theorem, not obvious on its face, and it is what makes "even permutation" and "odd permutation" well-defined labels rather than depending on an arbitrary choice.

A_n, the ALTERNATING GROUP, is the subgroup of S_n consisting of exactly the EVEN permutations, and it has order n!/2 (exactly half of S_n) — because the even permutations form a subgroup (composing two even permutations, meaning an even-plus-even count of transpositions, gives another even permutation) of index exactly 2, splitting S_n into precisely two equal-sized cosets, the even permutations and the odd ones.

S_3 IS THE SMALLEST NON-ABELIAN GROUP. Its full Cayley table, with elements e (identity), r=(1 2 3), r²=(1 3 2), and the three transpositions s=(1 2), sr=(2 3), sr²=(1 3):

∗   | e   r   r²  s   sr  sr²
e   | e   r   r²  s   sr  sr²
r   | r   r²  e   sr  sr² s
r²  | r²  e   r   sr² s   sr
s   | s   sr² sr  e   r²  r
sr  | sr  s   sr² r   e   r²
sr² | sr² sr  s   r²  r   e

Verify non-commutativity directly on one pair, r and s, using the composition convention above (apply the right permutation first). r∗s means "apply s, then r": s sends 1→2, then r sends 2→3, so overall 1→3; s sends 2→1, then r sends 1→2, so overall 2→2 (fixed); s sends 3→3, then r sends 3→1, so overall 3→1. That gives r∗s = (1 3) = sr². Now s∗r means "apply r, then s": r sends 1→2, then s sends 2→1, so overall 1→1 (fixed); r sends 2→3, then s fixes 3, so overall 2→3; r sends 3→1, then s sends 1→2, so overall 3→2. That gives s∗r = (2 3) = sr. Since r∗s = sr² and s∗r = sr are DIFFERENT elements of the table, this one pair alone proves the table is not symmetric across its diagonal — confirming S_3 is genuinely non-abelian, not merely by assertion but by an explicit computed pair that fails to commute.

GROUPS OF SMALL ORDER, CLASSIFIED

Order 1, 2, 3, 5, 7 (any prime p): exactly ONE group up to isomorphism, always the cyclic group ℤ_p — proved directly above as a consequence of Lagrange's theorem (any group of prime order is cyclic).
Order 4: exactly TWO groups up to isomorphism — ℤ_4 and the Klein four-group ℤ_2×ℤ_2, distinguished above by whether an element of order 4 exists.
Order 6: exactly TWO groups up to isomorphism — the cyclic ℤ_6 (abelian) and S_3 (non-abelian), distinguished above by commutativity.

THE DIHEDRAL GROUP D_n (the symmetries of a regular n-gon — n rotations and n reflections, giving order 2n) is worth recognising by name and structure: it is non-abelian for n≥3 (a rotation followed by a reflection is generally not the same as that reflection followed by that rotation), and D_3 (order 6, symmetries of an equilateral triangle) is in fact ISOMORPHIC to S_3 — both are "the" non-abelian group of order 6, just described two different ways (as permutations of 3 labelled vertices, or as geometric symmetries of a triangle).

KEY: The pattern across every one of these small-order classifications is the same two-step method: first use Lagrange's theorem to narrow down which element orders are even POSSIBLE at all (every order must divide the group's own size), and then use the actual PRESENCE OR ABSENCE of an element of the maximum possible order to split the remaining candidates into the cyclic case (a generator exists) versus every non-cyclic alternative. This is a general-purpose recipe, not a fact specific to order 4 or order 6 — apply the same two steps to classify a group of any small order you are handed.

RINGS AND FIELDS, AT RECOGNITION LEVEL

A RING is a set with TWO operations, usually called + and ×, where the set is an ABELIAN GROUP under + (with additive identity 0), a MONOID under × (with multiplicative identity 1, in a "ring with unity" — some definitions of ring omit even this), and × DISTRIBUTES over + (a×(b+c) = a×b + a×c). Note that × is NOT required to have inverses — this is exactly what separates a ring from a field.

A FIELD is a ring where, additionally, EVERY NONZERO element has a multiplicative inverse — i.e., the nonzero elements form a group (not just a monoid) under ×. ℚ, ℝ, ℂ are all fields under ordinary + and ×.

ℤ_n (integers mod n, under both + and ×) is a ring for every n, but it is a FIELD if and only if n is PRIME. When n is prime, every nonzero element of ℤ_n is coprime to n (since n has no factors other than 1 and itself), and an element coprime to the modulus is guaranteed to have a multiplicative inverse mod n (by the extended Euclidean algorithm, covered below) — so every nonzero element is invertible, making ℤ_n a field. When n is composite, ℤ_n has ZERO DIVISORS — nonzero elements a,b with a×b≡0 (mod n) despite neither being zero — for instance in ℤ_6, 2×3=6≡0 (mod 6), with neither 2 nor 3 being zero; a nonzero element that is a zero divisor can never have a multiplicative inverse (if a×b=0 and a had an inverse a⁻¹, multiplying both sides by a⁻¹ would give b=0, contradicting b≠0), so ℤ_n for composite n is a ring but never a field.

THE UNITS of ℤ_n (the elements that DO have a multiplicative inverse mod n) are exactly the elements coprime to n — precisely φ(n) of them, connecting straight back to Euler's totient function already used above to count generators of cyclic groups.

KEY: The SAME quantity, φ(n), answers two questions that look unrelated on the surface — "how many generators does ℤ_n have under addition" and "how many units (invertible elements) does ℤ_n have under multiplication" — because both questions reduce to exactly the same underlying condition, gcd(k,n)=1. Recognising this shared root avoids re-deriving the count from scratch every time the question is dressed up differently.

GATE TRAP: Do not confuse "ℤ_n is a ring" (true for every n, no exceptions) with "ℤ_n is a field" (true only for prime n). A question describing ℤ_n for some specific COMPOSITE n and asking whether a particular nonzero element has a multiplicative inverse cannot be answered by "yes, because ℤ_n is a field" — it depends entirely on whether that specific element happens to be coprime to n; some elements of ℤ_n for composite n do have inverses (the units), while others (the zero divisors and their multiples) never do, and both kinds coexist in the same ring.

MODULAR INVERSES BY EXTENDED EUCLID

An inverse of a modulo n exists if and only if gcd(a,n)=1, and when it exists, it can be found via the EXTENDED EUCLIDEAN ALGORITHM, which expresses gcd(a,n) as an integer combination xa + yn = gcd(a,n); if gcd(a,n)=1, this becomes xa + yn = 1, meaning xa ≡ 1 (mod n) — so x itself (adjusted into the range 0 to n−1 if needed) is exactly a's modular inverse.

Find the inverse of 3 modulo 7. Run the Euclidean algorithm: 7 = 2×3 + 1, and 3 = 3×1 + 0, so gcd(7,3)=1 (confirming an inverse exists, since 1 is the gcd). Work backward from the first line: 1 = 7 − 2×3. Reading off the coefficient of 3 directly: 1 = (−2)×3 + 1×7, so x=−2 is a valid coefficient — adjust into the range 0 to 6 by adding 7: x=5. Check: 3×5=15=2×7+1≡1 (mod 7). Confirmed — the inverse of 3 modulo 7 is 5.

WORKED PROBLEMS

1. STRUCTURE IDENTIFICATION. What structure is (ℚ, ×) — the rational numbers under ordinary multiplication — and which axiom, if any, fails to make it a group? Closure holds (product of two rationals is rational). Associativity holds (ordinary multiplication). Identity 1 exists and is rational. But 0 has NO multiplicative inverse (nothing times 0 gives 1), so the inverse axiom fails for exactly one element. (ℚ, ×) is therefore only a MONOID, not a group — though (ℚ\\{0}, ×), excluding zero specifically, IS a group, since removing the one problem element restores the inverse axiom for everything that remains.

2. CAYLEY TABLE GROUP CHECK. Given the table below on {e,a,b,c}, determine if it defines a group.
   ∗ | e a b c
   e | e a b c
   a | a e c b
   b | b c a e
   c | c b e a
   Latin-square check: row e is e,a,b,c (a clean permutation); row a is a,e,c,b (clean); row b is b,c,a,e (clean); row c is c,b,e,a (clean); and checking columns confirms the same. Identity is clearly e (its row and column reproduce the header exactly). Inverses: a∗a=e so a is its own inverse; b∗b=a (from row b, column b), not yet e — check further: does b have an inverse? Row b reads b,c,a,e — the identity e appears at column c, meaning b∗c=e, so b's inverse is c (and by the table's apparent structure, c∗b should also be e — check row c, column b: reads e — confirmed). This table passes the Latin-square check and every element has a visible inverse. The remaining, unavoidable step is verifying associativity directly on enough triples (for instance, (a∗b)∗c: a∗b=c from the table, then c∗c=a from the table, giving a; compare a∗(b∗c): b∗c=e from the table, then a∗e=a, giving a — these match) — a full check would verify all 4³=64 triples, or recognise the table's evident structure (it matches the Klein four-group's pattern) to shortcut the argument. Assuming associativity checks out on a representative sample as shown, this table defines a group of order 4.

3. ELEMENT ORDERS AND GENERATORS. List the order of every element of ℤ_8 and identify its generators. ord(0)=1. ord(1): powers 1,2,3,4,5,6,7,0 — first hit at n=8, ord(1)=8. ord(2): 2,4,6,0 — first hit at n=4, ord(2)=4. ord(3): 3,6,1,4,7,2,5,0 — first hit at n=8, ord(3)=8. ord(4): 4,0 — first hit at n=2, ord(4)=2. ord(5): 5,2,7,4,1,6,3,0 — first hit at n=8, ord(5)=8. ord(6): 6,4,2,0 — first hit at n=4, ord(6)=4. ord(7): 7,6,5,4,3,2,1,0 — first hit at n=8, ord(7)=8. Generators (elements of order 8, matching the group's own size): 1, 3, 5, 7 — exactly φ(8)=4 of them, matching the formula derived above.

4. SUBGROUPS OF ℤ_12. List all subgroups of ℤ_12 with their orders and generators. Divisors of 12: 1,2,3,4,6,12. Subgroup of order 1: {0} (generated by 0, or trivially by "no generator needed"). Order 2: {0,6}, generated by 6 (since 6+6=12≡0, ord(6)=2). Order 3: {0,4,8}, generated by 4 (4+4=8, 4+4+4=12≡0, ord(4)=3). Order 4: {0,3,6,9}, generated by 3 (ord(3)=4, since 3×4=12≡0 and no smaller multiple works). Order 6: {0,2,4,6,8,10}, generated by 2 (ord(2)=6). Order 12: all of ℤ_12, generated by 1 (or 5, 7, 11 — any element coprime to 12). Six subgroups total, matching the divisor count of 12 computed earlier.

5. COSETS AND LAGRANGE. In ℤ_12, take the subgroup H={0,3,6,9} (order 4). List all its cosets, and verify Lagrange's theorem numerically. Coset 0+H={0,3,6,9}=H itself. Coset 1+H={1,4,7,10}. Coset 2+H={2,5,8,11}. These three cosets — {0,3,6,9}, {1,4,7,10}, {2,5,8,11} — together contain all 12 elements of ℤ_12 with no overlaps, confirming the partition property. There are 3 cosets, each of size 4, so |G|=3×4=12, exactly matching |H|·(index of H)=|G|, with index 3 — confirming Lagrange's theorem (4 divides 12, quotient 3) concretely.

6. ISOMORPHISM DECISION FOR GROUPS OF ORDER 4. Are ℤ_4 and ℤ_2×ℤ_2 isomorphic? Already settled above by the element-order invariant: ℤ_4 has elements of order 4 (namely 1 and 3), while every non-identity element of ℤ_2×ℤ_2 has order 2 — since isomorphisms must preserve the exact set of element orders present, and these two groups have different such sets, they are NOT isomorphic, despite sharing the same order (4).

7. ORDER OF A PERMUTATION FROM ITS CYCLES. Find the order of the permutation (1 2 3 4)(5 6)(7 8 9) in S_9. Cycle lengths are 4, 2, and 3. The order is lcm(4,2,3). Factor each: 4=2², 2=2¹, 3=3¹. The lcm takes the highest power of each prime appearing: 2² and 3¹, giving lcm=4×3=12. The permutation's order is 12 — applying it 12 times returns every element to its original position, and no smaller positive number of applications does so for all three cycles simultaneously.

8. MODULAR INVERSE. Find the inverse of 4 modulo 9. Euclidean algorithm: 9=2×4+1, 4=4×1+0, so gcd(9,4)=1 (inverse exists). Working backward: 1=9−2×4, so the coefficient of 4 is −2; adjust into range 0 to 8 by adding 9: x=7. Check: 4×7=28=3×9+1≡1 (mod 9). Confirmed — the inverse of 4 modulo 9 is 7.

CARRYING THIS FORWARD

The counting arguments in this chapter — Lagrange's theorem constraining subgroup sizes, φ(n) counting generators and units — reappear directly in cryptography (RSA's security rests on exactly the structure of the multiplicative group of units modulo a product of two primes, and Euler's theorem, a direct generalisation of the "aⁿ=e iff ord(a) divides n" fact proved here, underlies why RSA decryption actually recovers the original message). The classification of small groups by their element-order profile is the same style of reasoning combinatorics and graph theory use to distinguish structures that "look the same size" but are not actually the same object — an isomorphism-invariant argument, proving two things different by finding ONE property they fail to share, is a technique that recurs constantly outside pure algebra as well.

The relations chapter's equivalence classes are, in fact, exactly the cosets met here under a different name: partitioning ℤ_n by "having the same remainder mod n" is the identical construction as partitioning a group by the cosets of a subgroup, just described in the language of relations rather than the language of algebra. And the Latin-square property proved here for group tables is precisely why a completed Sudoku grid, or a properly designed set of experimental trials in statistics, forces every symbol to appear exactly once per row and column — the same combinatorial fact, wearing yet another set of clothes.
`
};
