// Textbook chapter: Context-Free Languages, Grammars, and Pushdown Automata.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['toc-cfl'] = {
  figs: [
    {
      id: 'pda-stack-vs-dfa',
      caption: 'A pushdown automaton is a finite automaton plus one unbounded stack. The finite control alone cannot remember an unbounded count, but the stack can — this single addition is exactly what separates context-free languages from regular ones.',
      svg: '<svg viewBox="0 0 300 110" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><circle cx="70" cy="50" r="35"/><rect x="180" y="15" width="40" height="80"/><line x1="180" y1="35" x2="220" y2="35"/><line x1="180" y1="55" x2="220" y2="55"/><line x1="180" y1="75" x2="220" y2="75"/></g><g font-size="9" fill="currentColor" text-anchor="middle"><text x="70" y="54">finite control</text><text x="200" y="103">unbounded stack</text></g></svg>'
    },
    {
      id: 'cfl-closure-vs-regular',
      caption: 'CFL closure properties are strictly weaker than the closure list for regular languages: closed under union, concatenation, star, and intersection-with-regular, but NOT closed under general intersection or complement.',
      svg: '<svg viewBox="0 0 300 100" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.2" fill="none"><rect x="10" y="10" width="130" height="80"/><rect x="160" y="10" width="130" height="80"/></g><g font-size="9" fill="currentColor" text-anchor="middle"><text x="75" y="30">CFL closed under:</text><text x="75" y="50">union, concat,</text><text x="75" y="65">star, ∩ regular</text><text x="225" y="30">CFL NOT closed:</text><text x="225" y="50">∩ (general),</text><text x="225" y="65">complement</text></g></svg>'
    }
  ],
  text: `
WHY REGULAR LANGUAGES ARE NOT ENOUGH

The previous chapter proved, via both the pumping lemma and the Myhill-Nerode theorem, that {0^n 1^n : n ≥ 0} is NOT regular — no finite amount of memory suffices to count an UNBOUNDED number of 0's and then verify exactly that many 1's follow, since a finite automaton has only finitely many states to encode "how many 0's have I seen so far," and any bound on that count is eventually exceeded by a long enough input. CONTEXT-FREE LANGUAGES are precisely the class obtained by adding the SIMPLEST possible unbounded memory structure — a STACK — to a finite automaton, and this single addition is exactly powerful enough to recognize languages like {0^n 1^n}, while still falling short of full, unrestricted computation.

CONTEXT-FREE GRAMMARS: THE GENERATIVE DEFINITION

1. A CONTEXT-FREE GRAMMAR (CFG) is a 4-tuple G = (V, Σ, R, S), where V is a finite set of NONTERMINAL symbols, Σ is a finite set of TERMINAL symbols (disjoint from V), R is a finite set of PRODUCTIONS each of the form A → α with A ∈ V a SINGLE nonterminal on the left-hand side and α ∈ (V∪Σ)* an arbitrary string of terminals and nonterminals on the right, and S ∈ V is the designated START symbol.
2. THE DEFINING RESTRICTION that gives this grammar class its name: the left-hand side of EVERY production is a SINGLE, BARE nonterminal — never a string of several symbols, and never a nonterminal surrounded by any required CONTEXT that must also be present for the rule to apply. This is exactly what "context-free" means: a nonterminal A may always be rewritten according to any of its productions, REGARDLESS of what symbols happen to surround it elsewhere in the current derivation string.
3. THE LANGUAGE GENERATED, L(G) = {w ∈ Σ* : S ⇒* w} — the set of all TERMINAL strings reachable from the start symbol S via zero or more successive production applications (derivation steps), where each step replaces some nonterminal currently present with the right-hand side of one of its own productions.

PUSHDOWN AUTOMATA: THE RECOGNIZING DEFINITION

1. A PUSHDOWN AUTOMATON (PDA) is a 7-tuple M = (Q, Σ, Γ, δ, q0, Z0, F), where Q, Σ, q0, F carry their usual finite-automaton meanings, Γ is a finite STACK ALPHABET, Z0 ∈ Γ is the INITIAL stack symbol (present on the stack before any input is read), and δ: Q × (Σ∪{ε}) × Γ → finite subsets of Q × Γ* is the transition function — on a given state, an input symbol (or ε, consuming no input at all), AND the CURRENT TOP-OF-STACK symbol, the machine may move to a new state while REPLACING that top stack symbol with some (possibly empty, possibly multi-symbol) string.
2. TWO EQUIVALENT NOTIONS OF ACCEPTANCE for a general (nondeterministic) PDA: ACCEPTANCE BY FINAL STATE (the input is fully consumed while the machine sits in SOME state belonging to F, with the stack's own final contents entirely irrelevant), and ACCEPTANCE BY EMPTY STACK (the input is fully consumed and the stack becomes COMPLETELY EMPTY, with the machine's final state entirely irrelevant). These two modes are PROVABLY EQUIVALENT in expressive power for general PDAs — any PDA accepting some language by one mode can be mechanically converted into an equivalent PDA accepting the SAME language by the other mode.
3. THE FUNDAMENTAL EQUIVALENCE THEOREM OF THIS CHAPTER: CFGs and (nondeterministic) PDAs are EXACTLY equally expressive — every context-free language generated by SOME CFG is accepted by SOME PDA, and conversely every language accepted by SOME PDA is generated by SOME CFG, via a standard, fully mechanical construction where each grammar-derivation step is directly simulated by a corresponding stack-symbol REPLACEMENT operation.

[[FIG:pda-stack-vs-dfa]]

KEY: this CFG-PDA equivalence is the EXACT structural analogue, one level up, of the regex-automaton-grammar three-way equivalence the previous chapter established for regular languages — there, a regular expression, a finite automaton, and a right-linear grammar were three interchangeable descriptions of the SAME language class; here, a context-free grammar and a pushdown automaton play the SAME two complementary roles (a GENERATIVE description versus a RECOGNIZING machine) for the context-free language class, and recognising this recurring "generative description ↔ recognizing machine" pairing pattern is a genuinely transferable habit that will reappear again, in the very next chapter, for Turing machines and their own equivalent generative grammar formalism (unrestricted, or type-0, grammars).

AMBIGUITY: A PROPERTY OF GRAMMARS, NOT LANGUAGES

1. A CFG is AMBIGUOUS if SOME string in L(G) has TWO OR MORE genuinely distinct PARSE TREES — equivalently, two or more distinct LEFTMOST derivations (or, equivalently again, two or more distinct rightmost derivations) producing that same string.
2. A CONTEXT-FREE LANGUAGE is INHERENTLY AMBIGUOUS if EVERY SINGLE grammar that generates it is ambiguous — no unambiguous grammar exists for that language AT ALL, no matter how cleverly one might try to construct one. The classic textbook example: L = {a^i b^j c^k : i=j OR j=k} is inherently ambiguous, since any string with i=j=k simultaneously satisfies BOTH disjuncts, and any grammar built by combining separate rule-branches for "i=j" and for "j=k" will necessarily generate such strings via BOTH branches independently, producing two genuinely different parse trees for the identical string.

GATE TRAP: conflating "this specific grammar is ambiguous" with "this language is inherently ambiguous" is one of the most consequential terminology errors in this entire topic — ambiguity is a property of a PARTICULAR grammar, while inherent ambiguity is a property of the LANGUAGE ITSELF, independent of any one grammar; a language having ONE ambiguous grammar says NOTHING about whether some OTHER, cleverer, equivalent unambiguous grammar might still exist for that same language (the classic dangling-else problem in programming-language grammars, and typical arithmetic-expression grammars without explicit operator-precedence layering, are both merely AMBIGUOUS as commonly first written, yet can usually be rewritten as fully UNAMBIGUOUS grammars for the identical language once precedence and associativity are encoded directly into the grammar's own nonterminal structure).

CHOMSKY NORMAL FORM: A CANONICAL RESTRICTED FORM

1. A CFG is in CHOMSKY NORMAL FORM (CNF) if every production has EXACTLY one of two shapes: A → BC (a single nonterminal rewriting to EXACTLY two nonterminals, no terminals at all in that production) or A → a (a single nonterminal rewriting to EXACTLY one terminal symbol) — plus, as the sole exception, S → ε is permitted (and ONLY if ε ∈ L(G)), provided S never appears on the right-hand side of any OTHER production in the grammar.
2. THE CONVERSION PROCEDURE, applied in this strict order: (a) add a brand-new start symbol S0 with the single production S0 → S, ensuring the ORIGINAL start symbol S never needs to appear on any right-hand side later; (b) ELIMINATE ε-PRODUCTIONS, by first computing the full set of NULLABLE nonterminals (those that can derive ε, directly or transitively), then, for every production containing a nullable symbol on its right-hand side, adding EVERY combination of that production with each nullable occurrence both present and OMITTED; (c) ELIMINATE UNIT PRODUCTIONS (productions of the exact form A → B, a single nonterminal alone), by transitively copying every one of B's own productions directly onto A, then deleting the original unit production entirely; (d) replace any TERMINAL appearing ALONGSIDE other symbols within a longer right-hand side with a FRESH nonterminal dedicated solely to producing that one terminal (a production Xa → a); (e) BREAK UP any remaining right-hand side longer than two symbols into a CHAIN of strictly binary productions, introducing fresh intermediate nonterminals as needed.
3. EVERY CFG (over some language possibly excluding ε, or including it only via the single explicit S→ε exception above) has SOME equivalent CNF grammar — CNF is not merely a convenient restriction but a genuinely UNIVERSAL normal form that every context-free language can always be re-expressed in.

1. Worked example (CNF conversion): convert G: S → AB, A → aA | ε, B → bB | ε into CNF. Since both A and B are NULLABLE (each can derive ε directly), first add every ε-removed variant — and this must be done for EVERY production containing a nullable symbol, not just S's own: S → AB | A | B | ε (the last term added since S itself becomes nullable once both A and B can vanish); A → aA | a (the ε-removed variant of aA, since dropping the nullable A leaves just 'a' — without this term A could never actually terminate, since XaA alone recurses forever); B → bB | b similarly. If ε ∈ L(G) is acceptable, introduce a fresh start symbol S0 → S | ε and remove the old S → ε production. Then eliminate the remaining unit productions (S → A and S → B each copy over A's and B's own further productions, including their newly-added terminating alternatives), and finally replace terminals appearing alongside nonterminals (the 'a' in aA becomes a fresh Xa → a, giving A → XaA | a, and similarly for B → XbB | b) — producing a fully compliant CNF grammar generating exactly the original language {a^i b^j : i,j ≥ 0}.

GREIBACH NORMAL FORM: TERMINAL-FIRST DERIVATIONS

1. A CFG is in GREIBACH NORMAL FORM (GNF) if EVERY production has the shape A → aα, where a is a SINGLE terminal symbol appearing FIRST, followed by α ∈ V* (zero or more nonterminals, but NO further terminals anywhere in that same production).
2. CONVERSION TYPICALLY PROCEEDS CFG → CNF → GNF, using a SUBSTITUTION technique that systematically eliminates LEFT RECURSION (a production of the form A → Aβ, which a naive terminal-first reordering cannot directly handle) and reorders productions by nonterminal INDEX so that, working from the highest-indexed nonterminal downward, every production is progressively rewritten to begin with a terminal.
3. A DIRECT, EXAM-USEFUL CONSEQUENCE of GNF's terminal-first structure: EVERY derivation of a string of length n using a GNF grammar takes EXACTLY n derivation steps — since each individual production application produces EXACTLY one new terminal symbol (and possibly some further nonterminals still to be expanded later), a string of length n requires precisely n such terminal-producing steps to derive in full, no more and no fewer.

GATE TRAP: miscounting GNF derivation steps by assuming the step count depends on the GRAMMAR's own structure (number of nonterminals, branching factor, and so on) rather than purely on the DERIVED STRING's length is a common error — the terminal-per-step guarantee is a STRUCTURAL property of GNF itself, holding for EVERY GNF grammar and EVERY string it derives, making "exactly n steps for a length-n string" a reliable, grammar-independent numerical fact once GNF form is confirmed.

THE CFL PUMPING LEMMA: PUMPING TWO PIECES IN TANDEM

1. STATEMENT: if L is a context-free language, there exists a pumping length p such that every string z ∈ L with |z| ≥ p can be decomposed as z = uvwxy, satisfying: |vwx| ≤ p (the two pumped pieces and the region between them together span at most p symbols), |vx| ≥ 1 (v and x are not BOTH empty — at least one of the two pumped pieces is genuinely nonempty), and for EVERY i ≥ 0, the string u v^i w x^i y is ALSO in L.
2. THE CRITICAL STRUCTURAL DIFFERENCE from the regular pumping lemma: here TWO separate substrings, v and x, are pumped SIMULTANEOUSLY and IN TANDEM — always raised to the identical power i together in every single pumped variant — rather than the regular pumping lemma's single pumped substring y raised to power i alone. This reflects the underlying PDA's own stack-based mechanism: v and x correspond to matched push-and-pop activity around a single stack symbol, which is inherently a PAIRED operation.

GATE TRAP: attempting to pump v and x INDEPENDENTLY of one another (raising them to two DIFFERENT powers, or pumping only one of the two while leaving the other fixed) is a fundamental misapplication of the CFL pumping lemma — the lemma's own guarantee applies STRICTLY to pumping BOTH v and x together, to the SAME power i, in every iteration; a "proof" that relies on pumping only one of the two pieces independently is not a valid application of this specific lemma at all.

1. Worked example (CFL pumping lemma proving non-context-freeness): prove L = {a^n b^n c^n : n ≥ 0} is NOT context-free. Assume L is a CFL with pumping length p. Choose z = a^p b^p c^p (length 3p, genuinely in L). Since |vwx| ≤ p, the substring vwx is confined to a span of at most p CONSECUTIVE symbols — but the three blocks a^p, b^p, c^p are each themselves of length p, and any window of length at most p CANNOT simultaneously touch symbols from ALL THREE blocks (since bridging even just from the END of the a-block to the START of the c-block alone already requires spanning the ENTIRE b-block, more than p symbols). So vwx touches symbols from AT MOST TWO of the three distinct blocks. Pumping UP with i=2 (u v^2 w x^2 y) INCREASES the count of whichever one or two symbol TYPES v and x actually contain, while leaving the THIRD, untouched symbol type's count completely UNCHANGED — this immediately breaks the required equal-count invariant (all three counts must remain equal for membership in L), for EVERY possible decomposition satisfying the lemma's own constraints. Contradiction — L is not context-free.

DCFL VS CFL: THE DETERMINISTIC SUBCLASS

1. A DETERMINISTIC CFL (DCFL) is a language accepted by a DETERMINISTIC pushdown automaton (DPDA) — a PDA restricted to AT MOST ONE applicable transition in every possible configuration (current state, next input symbol or ε, and current stack top considered together), with DPDAs conventionally required to accept by FINAL STATE specifically (rather than by empty stack).
2. DCFL ⊊ CFL, STRICTLY: every DCFL is trivially also a CFL (a DPDA is, after all, already a special, more restricted kind of PDA), but the containment is STRICT — some CFLs genuinely have NO equivalent DPDA at all. The canonical witness: L = {ww^R : w ∈ {0,1}*} (the language of EVEN-length palindromes) is a CFL (a straightforward nondeterministic PDA pushes the first half, then nondeterministically GUESSES the midpoint and pops while matching against the second half) but is NOT a DCFL, since a fully deterministic machine, reading left to right with no lookahead, has no reliable way to determine WHERE the midpoint falls without first having somehow already seen the entire remaining input.
3. EVERY DCFL is UNAMBIGUOUS (has at least one unambiguous grammar), but the converse does NOT hold — being unambiguous is a strictly WEAKER property than being deterministic, giving the strict containment chain DCFL ⊊ (unambiguous CFL) ⊊ CFL.

GATE TRAP: acceptance by final state and acceptance by empty stack, PROVABLY EQUIVALENT for general nondeterministic PDAs (as established earlier in this chapter), are NOT equivalent for DETERMINISTIC PDAs specifically — this is EXACTLY why DCFL is conventionally defined using final-state acceptance rather than empty-stack acceptance; assuming the general PDA equivalence carries over unchanged to the deterministic case is a subtle but real error this specific topic is built to test.

CLOSURE PROPERTIES: WHERE CFL AND DCFL DIVERGE SHARPLY

[[FIG:cfl-closure-vs-regular]]

1. CFL IS closed under: union, concatenation, Kleene star (and Kleene plus), REVERSAL, homomorphism, and — critically — INTERSECTION WITH A REGULAR LANGUAGE SPECIFICALLY (built via a product construction running the CFL's own PDA and the regular language's DFA in lockstep, exactly mirroring the previous chapter's own product-construction idea for two DFAs, but now with one of the two machines carrying a genuine, unbounded stack).
2. CFL is NOT closed under: general INTERSECTION (of two arbitrary CFLs with each other), COMPLEMENT, and SET DIFFERENCE in general as well — the last of these follows cleanly from the complement failure alone: Σ* itself is trivially a CFL (it is even regular), and A−B is just Σ*−B whenever A=Σ*, so IF CFL were closed under set difference, taking A=Σ* would make complement(B)=Σ*−B a CFL for EVERY CFL B, i.e. CFL would be closed under complement — directly contradicting the already-established fact that it is NOT. So set-difference closure would silently smuggle in complement closure, which cannot hold.
3. DCFL IS closed under COMPLEMENT specifically — flip which states of the DPDA count as accepting versus non-accepting, exactly the SAME simple trick used for ordinary DFA complementation in the previous chapter — and this construction works SPECIFICALLY because determinism guarantees EXACTLY one well-defined computation path per input string, so flipping accept/reject correctly flips the machine's verdict on every string with no ambiguity about which of several possible paths to consider.
4. DCFL is NOT closed under union, intersection, concatenation, Kleene star, or reversal IN GENERAL.

GATE TRAP: the asymmetry that DCFL is closed under complement but NOT under union is one of the single most frequently tested, and most COUNTERINTUITIVE, facts in this entire topic — one might naturally (but WRONGLY) expect that closure under BOTH complement and union together would force closure under intersection as well (via De Morgan's law: A∩B = complement(complement(A) ∪ complement(B))), which would in turn seem to contradict DCFL's own KNOWN non-closure under intersection; the resolution is simply that DCFL is NOT closed under union in the first place, so this particular chain of implications never actually gets off the ground, and no contradiction arises at all — recognising precisely why no contradiction occurs is itself worth understanding fully, not merely memorising the closure table's individual entries as isolated facts.

1. Worked example (CFL not closed under intersection, using two ordinary CFLs): let L1 = {a^n b^n c^m : n,m ≥ 0} (a CFL, matching a's against b's while leaving c's entirely unconstrained) and L2 = {a^m b^n c^n : n,m ≥ 0} (also a CFL, matching b's against c's while leaving a's unconstrained). Then L1 ∩ L2 = {a^n b^n c^n : n ≥ 0} — exactly the language proved non-context-free via the CFL pumping lemma above — directly demonstrating CFL's own non-closure under intersection using two perfectly ordinary, individually unremarkable CFLs, neither of which is in any way pathological or contrived on its own.

CFG DERIVATIONS AND PARSE TREES

1. A LEFTMOST DERIVATION always expands the LEFTMOST remaining nonterminal at every single step; a RIGHTMOST DERIVATION always expands the RIGHTMOST remaining nonterminal at every step. Both, when they exist for the same string, correspond to the SAME underlying PARSE TREE — the derivation ORDER differs, but the resulting TREE STRUCTURE (which symbols group under which) is identical for a given tree, regardless of whether it was built leftmost-first or rightmost-first.
2. A string has TWO DISTINCT parse trees if and only if it has two genuinely distinct leftmost derivations (equivalently, two distinct rightmost derivations) — this equivalence is exactly what makes "count the number of distinct leftmost derivations" a fully rigorous, mechanically checkable TEST for a grammar's ambiguity on any specific string.

THE CYK ALGORITHM: MEMBERSHIP TESTING VIA DYNAMIC PROGRAMMING

Given a CFG in CHOMSKY NORMAL FORM specifically (which is exactly why CNF's universality, established earlier, matters practically and not just theoretically) and a string w of length n, the COCKE-YOUNGER-KASAMI (CYK) algorithm decides whether w ∈ L(G) in O(n³) time, using dynamic programming over all possible SUBSTRINGS of w.

1. THE CORE IDEA: build a triangular table where entry T[i][j] holds the SET of all nonterminals that can derive the substring of w starting at position i with length j. Base case (length-1 substrings): T[i][1] contains nonterminal A exactly when the grammar has a production A → w_i (the single terminal at position i). Recursive case (length-j substrings, j>1): T[i][j] contains A exactly when SOME production A → BC exists and SOME split point k (1 ≤ k < j) has B ∈ T[i][k] AND C ∈ T[i+k][j-k] — precisely the CNF restriction to exactly-two-nonterminal right-hand sides that makes this clean binary-split recursion possible at all.
2. w ∈ L(G) if and only if the START SYMBOL S appears in T[1][n] — the entry covering the ENTIRE string from position 1 with the full length n.
3. THE TIME COMPLEXITY, O(n³): there are O(n²) table entries T[i][j] (one for each of the O(n) starting positions times O(n) possible lengths), and computing each entry requires trying O(n) possible split points k — giving O(n²)×O(n) = O(n³) overall, a DIRECT consequence of CNF's guarantee that every production has EXACTLY two right-hand-side symbols, bounding the "how many ways can this substring split" question to a SIMPLE linear scan over split points rather than something more complex.

1. Worked example (CYK table construction): grammar in CNF: S → AB, A → a, B → BC | b, C → c. Test membership of w = "abc" (positions 1,2,3 = a,b,c). Length-1 entries: T[1][1]={A} (since A→a), T[2][1]={B} (since B→b), T[3][1]={C} (since C→c). Length-2 entries: T[1][2] (covers "ab"): split k=1 needs B∈T[1][1] and C∈T[2][1] — T[1][1]={A} does not contain any B-producing match for the S→AB or B→BC form directly at this split in the needed roles, so check productions systematically: for S→AB we need A∈T[1][1] (yes, A is present) AND B∈T[2][1] (yes, B is present) — so S ∈ T[1][2]. T[2][2] (covers "bc"): for B→BC, need B∈T[2][1] (yes) and C∈T[3][1] (yes) — so B ∈ T[2][2]. Length-3 entry T[1][3] (covers "abc"): try split k=1: need to combine T[1][1]={A} with T[2][2]={B} — is there a production X→AB? Yes, S→AB, so S ∈ T[1][3]. Since S ∈ T[1][3] (the entry covering the entire string), w="abc" IS in L(G).

GATE TRAP: attempting to run CYK directly on a grammar that is NOT already in Chomsky Normal Form produces an incorrect or ill-defined table, since the algorithm's recursive step relies SPECIFICALLY on every production having EXACTLY two right-hand-side nonterminals (for the binary-split recursion to make sense) or exactly one terminal (for the base case) — a grammar with longer right-hand sides, ε-productions, or unit productions must FIRST be converted to CNF via the full procedure covered earlier in this chapter before CYK can be applied correctly at all.

PARSE TREE HEIGHT AND DERIVATION LENGTH RELATIONSHIPS

1. For a CFG in CHOMSKY NORMAL FORM specifically, a parse tree of HEIGHT h can derive a string of length AT MOST 2^(h-1) — since CNF restricts every internal (non-leaf-producing) node to EXACTLY two children, a binary tree of height h has at most 2^(h-1) LEAF nodes (terminal symbols) at its deepest level, directly bounding the derivable string length by the tree's own height in the SAME way a balanced binary tree's leaf count is bounded by its height.
2. CONVERSELY, deriving a string of length n via a CNF grammar requires a parse tree of height AT LEAST ⌈log₂(n)⌉+1 — the SAME binary-tree height-versus-leaf-count relationship, now used in the opposite direction to establish a LOWER bound on how "deep" any valid derivation of a sufficiently long string must go, regardless of which specific CNF grammar is used.
3. This height bound is PRECISELY what underlies the standard proof of the CFL pumping lemma itself: for the pumping length p (chosen based on the grammar's own nonterminal count), any parse tree of height greater than the number of DISTINCT nonterminals must, by the PIGEONHOLE PRINCIPLE, repeat SOME nonterminal along its longest root-to-leaf path — and this REPEATED nonterminal is EXACTLY what allows the "pump out the repeated middle section, or pump it in again" argument that gives the CFL pumping lemma its own v and x pieces their concrete, derivable structure.

1. Worked example (height-to-length bound applied): a CNF parse tree has height 5. Find the maximum possible length of the string it derives. Maximum length = 2^(5-1) = 2^4 = 16 — a height-5 CNF parse tree can derive a string of AT MOST 16 terminal symbols, achieved only when the tree is FULLY, perfectly balanced at every internal node.

GATE TRAP: applying the height-versus-length bound to a grammar NOT in Chomsky Normal Form gives an incorrect result — the 2^(h-1) bound relies specifically on EVERY internal node having EXACTLY two children, a guarantee that only CNF's own strict binary-branching restriction provides; a grammar with productions having three or more right-hand-side symbols, or with unit productions creating single-child internal nodes, does not obey this same clean exponential relationship at all.

OGDEN'S LEMMA: A SHARPER PUMPING TOOL

Ogden's lemma STRENGTHENS the ordinary CFL pumping lemma by allowing certain POSITIONS within the chosen string z to be marked as "DISTINGUISHED" in advance, guaranteeing that the pumped pieces v and x must involve AT LEAST ONE distinguished position — giving significantly more CONTROL over where the pumping actually occurs, which resolves certain non-context-freeness proofs that the ordinary CFL pumping lemma alone cannot handle cleanly.

1. STATEMENT (informally): if L is a CFL, there exists p such that for ANY z ∈ L with AT LEAST p positions marked as distinguished, z can be decomposed as uvwxy where vwx contains AT MOST p distinguished positions, at least ONE of v or x contains AT LEAST one distinguished position, and for all i≥0, uv^iwx^iy ∈ L.
2. WHY THIS EXTRA CONTROL MATTERS: the ordinary CFL pumping lemma only guarantees |vwx|≤p in terms of raw STRING LENGTH, giving no control over WHICH specific symbols end up inside the pumped region when a language's string has some symbols that matter more than others for a given proof strategy (for instance, when a language mixes many "don't care" symbols alongside a few critical, countable ones); by marking ONLY the critical symbols as distinguished, Ogden's lemma forces the pumped pieces to interact meaningfully with those critical positions specifically, ruling out an adversary "escaping" the proof by choosing a decomposition that pumps only irrelevant, uncounted symbols.

1. Worked example (Ogden's lemma resolving a case the ordinary lemma struggles with): prove L = {a^i b^j c^k : i=j OR j=k} is not context-free. Each of the two disjuncts alone IS a CFL ({a^i b^j c^k : i=j} and {a^i b^j c^k : j=k} both have straightforward CFGs), so this is exactly the kind of "union of two CFLs that is itself not a CFL" case the ordinary pumping lemma struggles to nail down cleanly, since an adversary can always choose a decomposition that pumps through a region irrelevant to whichever disjunct the chosen string happens to satisfy. Ogden's lemma fixes this by letting the prover MARK specific positions in advance, forcing the pumped substrings to actually interact with the counted symbols that matter. Choose the string w = a^p b^p c^(p+p!), which satisfies i=j (both p) but NOT j=k, so it is in L purely via the FIRST disjunct — and mark only the p a's. Ogden's lemma then guarantees at least one of u,v,x,y,z (in the decomposition w=uvxyz, |vxy|≤p, vy≠ε) contains a marked position, meaning v or y contains at least one a. Pumping down (i=0) strictly decreases the a-count while leaving b's and c's alone, breaking i=j (since j stays at p while the a-count drops below p) — and since the c-count (p+p!) can never equal the b-count (p) either (as p+p!>p for any p≥1), the pumped-down string also fails j=k, so it lands outside BOTH disjuncts and hence outside L, contradicting the pumping lemma's own guarantee that all pumped strings remain in L. This forced failure — unavailable to the ordinary pumping lemma, which permits pumping through an unmarked c-only or b-only stretch without ever touching the a's — is exactly what marking specific positions in advance buys a prover facing a "union of two differently-shaped CFL constraints" language like this one.

GATE TRAP: GATE questions at the standard exam level RARELY require Ogden's lemma directly (it is considered a more advanced tool, more common in a full theory-of-computation course than in typical GATE-level questions), but recognising it BY NAME, and understanding conceptually that it exists specifically to add positional CONTROL beyond what the ordinary CFL pumping lemma provides, is occasionally tested as a conceptual, non-computational recognition question rather than as a full worked proof.

KEY: the CYK algorithm and the parse-tree height bound are BOTH direct, practical payoffs of Chomsky Normal Form's own strict binary-branching structure — CYK's O(n³) dynamic-programming recursion and the 2^(h-1) height-to-length bound are not separate, unrelated facts about CNF, but two different CONSEQUENCES of the exact same underlying structural guarantee (every internal node has exactly two children), once again illustrating this book's own recurring lesson that a seemingly narrow, technical restriction (CNF's binary-branching requirement) very often turns out to unlock MULTIPLE distinct, practically useful algorithmic and theoretical tools simultaneously, rather than serving only the one specific purpose it may have originally been introduced for.

LEFT RECURSION AND ITS ELIMINATION

1. A grammar has LEFT RECURSION when some derivation allows a nonterminal A to derive a string beginning with A ITSELF, either DIRECTLY (a production A → Aα for some α) or INDIRECTLY (A derives some string beginning with B, which in turn eventually derives a string beginning with A again, cycling back). Left recursion is a genuine PROBLEM for top-down parsing techniques (including the GNF-conversion procedure covered earlier in this chapter), since a naive top-down parser attempting to expand A would immediately re-encounter A itself, in the SAME leftmost position, with no input symbol yet consumed — leading to non-terminating, infinite expansion with no progress ever made.
2. THE STANDARD ELIMINATION TECHNIQUE for direct left recursion: given A → Aα₁ | Aα₂ | ... | Aαₖ | β₁ | β₂ | ... | βₘ (where the β's do NOT themselves start with A), rewrite as A → β₁A' | β₂A' | ... | βₘA' and A' → α₁A' | α₂A' | ... | αₖA' | ε — introducing a FRESH nonterminal A' that absorbs the recursive "tail" of repeated α-expansions, converting the LEFT recursion into an equivalent RIGHT-recursive structure that generates the identical language.

1. Worked example (left-recursion elimination): eliminate left recursion from A → Aa | Ab | c. Here α₁=a, α₂=b, β₁=c (the sole non-A-starting alternative). Rewritten: A → cA', A' → aA' | bA' | ε. Verify equivalence on a small case: the original grammar generates strings of the form c followed by any sequence of a's and b's (c, ca, cb, caa, cab, cba, ...) — and the rewritten grammar generates exactly the same: A ⇒ cA' ⇒ caA' ⇒ caaA' ⇒ caa (via A'⇒ε), matching the string "caa" that the original grammar's own repeated left-recursive expansion A⇒Aa⇒Aaa⇒caa (via A⇒c initially, note reversed application order for a left-recursive derivation) would also eventually produce.

WORKED PROBLEMS

1. CNF PRODUCTION-TYPE IDENTIFICATION. Given the productions S → AB, A → a, B → BC, C → c, D → d, state which are CNF-compliant and which are not, and why. S → AB: compliant (two nonterminals). A → a: compliant (single terminal). B → BC: compliant (two nonterminals). C → c: compliant. D → d: compliant. All five are already valid CNF productions — this grammar happens to already be in CNF.

2. GNF DERIVATION STEP COUNT. A GNF grammar derives the string "abcde" (length 5). How many derivation steps does this derivation require? Exactly 5 steps — one terminal is produced per derivation step in GNF by definition, so a length-5 string requires exactly 5 steps, regardless of the specific grammar's internal structure.

3. CFL PUMPING LEMMA ON A DIFFERENT LANGUAGE. Prove L = {a^i b^j c^i d^j : i,j ≥ 0} is not context-free. Assume L is a CFL with pumping length p. Choose z = a^p b^p c^p d^p (length 4p, in L with i=j=p). Since |vwx| ≤ p, vwx cannot span from the a-block all the way to the c-block (that gap already exceeds p), nor from the b-block to the d-block — so vwx touches symbols from at most two ADJACENT blocks. Pumping up with i=2 increases the count of whichever block(s) vwx touches while leaving at least one of the required matching pairs (a's with c's, or b's with d's) unequal, violating L's own i,j-matching requirement for every possible decomposition — contradiction, so L is not context-free.

4. AMBIGUITY VS INHERENT AMBIGUITY. A grammar G1 for L = {a^n b^n : n≥0} has the single production S → aSb | ε, and is easily checked to be unambiguous. A different grammar G2 for the SAME language L has an extra redundant production S → aSb | ε | aSbε that produces some strings via two different derivations. Does L become inherently ambiguous because of G2's ambiguity? No — inherent ambiguity is a property of the LANGUAGE, and since G1 (an unambiguous grammar for the exact same L) exists, L is demonstrably NOT inherently ambiguous, regardless of how many OTHER, poorly constructed ambiguous grammars might also happen to generate it.

5. DCFL VS CFL CLASSIFICATION. Classify L = {a^n b^n : n ≥ 0} as CFL, DCFL, both, or neither, and briefly justify. It is BOTH — a straightforward deterministic PDA can push one 'a' per input 'a', then deterministically pop one 'a' per input 'b' (with no need to guess a midpoint, since the transition from pushing to popping is unambiguously signaled by the symbol change from 'a' to 'b' itself), accepting by empty stack and final state together with no nondeterminism required anywhere.

6. CLOSURE PROPERTY APPLICATION. Given a CFL L and a regular language R, is L∩R guaranteed to be a CFL? Is L∪R guaranteed to be context-free? L∩R IS guaranteed to be a CFL (intersection with a regular language specifically is a guaranteed CFL closure property, via the PDA-DFA product construction). L∪R is ALSO guaranteed to be a CFL, since CFL is closed under general union with ANY other CFL, and every regular language is itself trivially also a CFL (regular ⊊ context-free), so R itself qualifies as a CFL for this purpose.

7. DPDA COMPLEMENT CONSTRUCTION. A DPDA M accepts L by final state. Describe how to build a DPDA for the complement of L, and explain why this construction specifically requires determinism. Flip the accepting/non-accepting status of every state in M (swap F for Q∖F), exactly as for DFA complementation. This requires determinism because a NONDETERMINISTIC PDA might have MULTIPLE possible runs for the same string, some ending in an originally-accepting state and others not — simply flipping accept/reject status would leave it ambiguous which run's verdict should actually determine acceptance of the complemented language, whereas a DETERMINISTIC machine guarantees exactly one run, and hence exactly one well-defined verdict, per string.

8. CFG DESIGN FOR A BALANCED-PARENTHESES LANGUAGE. Design a CFG for the language of balanced parentheses over {(,)}. S → (S)S | ε. Verify this generates "(())()" correctly: S ⇒ (S)S ⇒ ((S)S)S ⇒ (())S ⇒ (())(S)S ⇒ (())()S ⇒ (())() — matching the target string via a valid derivation, confirming the grammar's correctness for at least this one representative example.

9. INHERENT AMBIGUITY EXAMPLE VERIFICATION. For L = {a^i b^j c^k : i=j or j=k}, exhibit a single string with two genuinely distinct "reasons" for membership, illustrating why no single unambiguous grammar can exist. The string a^2 b^2 c^2 satisfies BOTH i=j (2=2) AND j=k (2=2) simultaneously — a natural grammar built from two separate rule-branches (one enforcing i=j while leaving k free, another enforcing j=k while leaving i free) would generate this SAME string via BOTH branches independently, producing two structurally different parse trees, illustrating concretely the source of this language's inherent ambiguity.

10. FINAL-STATE VS EMPTY-STACK ACCEPTANCE FOR A GENERAL PDA. A nondeterministic PDA M1 accepts L by final state. Describe the standard construction converting M1 into an equivalent PDA M2 accepting the SAME L by empty stack. Add a new initial stack symbol Z0' beneath M1's own original stack contents (pushed once at the very start), and a new state that, whenever M1 would have reached one of its own original accepting states, nondeterministically transitions to a special "cleanup" state that pops every remaining stack symbol (including the new Z0') via ε-moves, emptying the stack completely exactly at the moment M1's own acceptance condition would have been satisfied — giving an equivalent PDA that accepts precisely L, now via empty stack.

11. CNF CONVERSION OF A UNIT PRODUCTION CHAIN. A grammar has S → A, A → B, B → a. Eliminate the unit-production chain. Since S → A, A → B, and B → a, transitively S can reach 'a' via this entire chain; the unit-production-elimination step directly adds S → a (copying B's terminal-production all the way back to S), and similarly A → a is added — after which the original unit productions S→A and A→B are deleted entirely, leaving only the terminal productions S→a, A→a, B→a remaining.

12. VERIFYING vwx CANNOT SPAN THREE BLOCKS. For z = a^p b^p c^p (as in the {a^n b^n c^n} pumping-lemma proof), explicitly justify why a substring vwx of length at most p cannot touch all three of the a, b, and c blocks simultaneously. To touch even just the LAST symbol of the a-block and the FIRST symbol of the c-block, a substring must span across the ENTIRE b-block in between, which alone already has length p — so any substring touching both the a-block and the c-block must have length STRICTLY GREATER than p, directly contradicting the pumping lemma's own |vwx| ≤ p constraint; hence vwx can touch at most two of the three blocks, and only if those two blocks are ADJACENT to each other.

13. CYK MEMBERSHIP TEST. Using the same CNF grammar S→AB, A→a, B→BC|b, C→c, test whether "ac" is in the language via CYK, and explain the result. T[1][1]={A} (a→A), T[2][1]={C} (c→C). T[1][2]: check split k=1, need B∈T[1][1] and C∈T[2][1] for a production X→BC to match roles — but T[1][1]={A}, not containing any B, and the only 2-symbol productions are S→AB and B→BC; for S→AB we'd need A∈T[1][1] (yes) and B∈T[2][1] (no, T[2][1]={C}) — no match. T[1][2]=∅. Since S∉T[1][2] (the entire string "ac" has length 2, so T[1][2] IS the full-string entry here), "ac" is NOT in L(G) — correctly reflecting that this grammar only generates strings of the specific shape a(b|bc)*c-like structure via S→AB, B→BC|b, C→c, and "ac" (with no b at all) does not fit that required shape.

14. LEFT-RECURSION ELIMINATION ON A LARGER GRAMMAR. Eliminate left recursion from E → E+T | T, T → T*F | F, F → (E) | id (a classic expression grammar). For E: α₁="+T", β₁="T" (T does not start with E), giving E → TE', E' → +TE' | ε. For T: α₁="*F", β₁="F", giving T → FT', T' → *FT' | ε. F is unchanged (no left recursion). The fully rewritten grammar: E→TE', E'→+TE'|ε, T→FT', T'→*FT'|ε, F→(E)|id — generating the identical language of arithmetic expressions with the same precedence (T binds tighter than E, matching * binding tighter than +) but now free of left recursion, suitable for top-down parsing.

CARRYING THIS FORWARD

This chapter has extended the previous one's machinery in a precisely characterizable way — a PDA is exactly a finite automaton plus one additional, unbounded stack, and every result in this chapter can be understood as tracing through the CONSEQUENCES of that single addition. The CFL pumping lemma's own "pump two pieces in tandem" structure directly reflects the stack's own inherently PAIRED push-then-later-pop discipline; the CFL closure table's specific gaps (no general intersection, no complement) trace directly back to the fact that running two PDAs "in lockstep" (as the previous chapter's product construction did for two DFAs) does NOT, in general, produce another valid single-stack PDA, since there is no way to merge two separate unbounded stacks into one without losing the very information that made each stack meaningful on its own; and the CFL-versus-DCFL distinction traces directly to whether the stack-based nondeterminism (specifically, GUESSING when to switch from pushing to popping, as the even-palindrome witness language required) can be resolved deterministically by the input alone.

KEY: recognising WHY CFL fails a closure property, not merely THAT it fails, is what turns this chapter's closure-property table from a list of memorised facts into a genuinely predictive tool — general intersection fails because two independent stacks cannot be merged into one PDA's single stack without losing information, while intersection WITH A REGULAR language succeeds specifically because a DFA (unlike a second PDA) carries NO stack of its own at all, so pairing a PDA with a DFA introduces no such merging problem; anticipating this exact reasoning pattern is far more valuable, and far more robust when facing an unfamiliar variant question, than memorising the closure table's individual cells as isolated facts to recall by rote.

The next chapter in this Theory of Computation sequence removes the RESTRICTION that gave this chapter's machines their name — a Turing machine trades the pushdown automaton's specifically STACK-shaped (last-in-first-out) unbounded memory for a fully general, freely READABLE-AND-WRITABLE unbounded tape, and this further removal of restriction is exactly what allows a Turing machine to recognize languages (such as {a^n b^n c^n} itself, proved non-context-free directly above) that no pushdown automaton, however cleverly designed, can ever recognize — continuing this entire sequence's own consistent running theme of each successive machine model being motivated directly by a specific, PRECISELY IDENTIFIED limitation of the model that came immediately before it.

KEY: the CFG normal forms covered in this chapter (Chomsky Normal Form and Greibach Normal Form) exist for two genuinely DIFFERENT practical reasons that are worth keeping distinctly separate in mind — CNF exists PRIMARILY to enable algorithmic processing (the CYK membership-testing algorithm, and the parse-tree height-to-length bound underlying the pumping lemma's own proof, BOTH depend directly on CNF's strict binary-branching structure), while GNF exists PRIMARILY to guarantee a clean, PREDICTABLE correspondence between derivation length and derived-string length (the "exactly n steps for a length-n string" property), which matters most for TOP-DOWN parsing strategies that need to know, in advance, how much input remains to be consumed at any given point in a derivation; conflating the two normal forms' distinct PURPOSES, rather than only their differing PRODUCTION SHAPES, is a common source of confusion about which normal form a given question actually needs.

KEY: left-recursion elimination, encountered in this chapter specifically as a necessary preprocessing step toward Greibach Normal Form, is ALSO, independently, one of the most practically important techniques in the ENTIRE broader subject of compiler design (covered as its own separate later chapter in this book) — top-down, PREDICTIVE parsers (the most common style of hand-written or simple-tool-generated parser) cannot handle left-recursive grammars AT ALL without looping forever, making left-recursion elimination a genuinely load-bearing technique that this chapter's Theory-of-Computation treatment and the later Compiler Design chapter's own practical parsing treatment BOTH depend upon, from two different angles on the very same underlying grammar-transformation idea.

A CLOSING WORKED SYNTHESIS PROBLEM COMBINING MULTIPLE TOOLS

To see several of this chapter's tools chained together within one fully worked scenario, exactly as the previous two Computer-Organization chapters and the previous Theory-of-Computation chapter each closed with their own combined synthesis problems, consider the following.

1. SCENARIO: a grammar G: S → SS | (S) | ε, over the alphabet {(,)}, generates strings of correctly nested (though not necessarily single-rooted) parenthesis groupings. Convert G to CNF, then use the resulting CNF grammar to CYK-test the string "()()" for membership, and separately determine the maximum string length a CNF parse tree of height 4 built from this grammar could derive.
2. STEP 1 — HANDLE THE ε-PRODUCTION: S is nullable (S⇒ε directly). Since S itself is also the start symbol and appears as its own right-hand side elsewhere, introduce S0 → S | ε as the new start symbol, keeping the original S's own productions (now without the S→ε alternative, which has been absorbed into S0) as S → SS | (S) | (), where the last alternative "()" comes from removing the nullable S from "(S)" entirely.
3. STEP 2 — CONVERT TO CNF SHAPE: introduce X( → ( and X) → ) for the terminal symbols, giving S → SS | X(SX) | X(X). The production X(X) has exactly two symbols already (CNF-compliant); X(SX) has three symbols and must be split via a fresh nonterminal Y → SX), giving S → SS | X(Y | X(X), Y → SX).
4. STEP 3 — CYK TEST "()()": with S0 as start (accepting via S0→S or S0→ε), and using the CNF-shaped productions from Step 2, a careful table build (following exactly the same T[i][j] procedure worked through in this chapter's earlier CYK example) confirms S appears in the entry covering the full 4-symbol string "()()"," since it correctly decomposes as S⇒SS⇒(the first S deriving "()" via X(X), the second S deriving "()" via X(X) as well) — confirming membership, consistent with "()()" being two correctly nested, side-by-side parenthesis groups exactly as the original grammar's informal description intended.
5. STEP 4 — MAXIMUM DERIVABLE LENGTH AT HEIGHT 4: using the height-to-length bound established earlier in this chapter, a CNF parse tree of height 4 can derive a string of at most 2^(4-1) = 8 terminal symbols, achieved only by a fully, perfectly balanced derivation tree at every internal branching point.

This combined problem illustrates, once again, this book's own consistently recurring lesson that a real exam question rarely isolates any single tool from this chapter — CNF conversion, CYK testing, and the height-to-length bound all connect naturally within one single grammar-analysis scenario, exactly mirroring how the memory-hierarchy and I/O chapters' own closing synthesis problems each chained together several individually-simple tools from within their own respective chapters into one combined, realistic worked question.

A final habit worth carrying forward from this chapter, and from every chapter this book has covered so far: whenever a grammar or a language description looks unfamiliar or awkward at first glance, the reliable first move is not to guess directly at its membership behavior, but instead to first ask which of this chapter's STANDARD transformations (CNF conversion, left-recursion elimination, or simply constructing a small, direct PDA by hand for a few short example strings) would put the problem into a shape this chapter's already-established tools can handle mechanically and reliably, rather than attempting an ad-hoc, from-scratch argument under time pressure. This same discipline — reduce an unfamiliar-looking problem to an already-solved standard form before attempting to reason about it directly — has been the quiet organizing principle behind essentially every worked example and every worked problem presented across this entire chapter. It remains exactly as useful heading into the more powerful, less restricted machine models the remaining chapters of this Theory of Computation sequence are about to introduce: even as the underlying machines grow considerably more general in their raw computational power, that same disciplined habit of reducing an unfamiliar problem to a known standard form continues to pay off just as reliably as it has throughout this chapter's own worked material, from its very first worked example through to the final, fully combined closing synthesis problem presented just above.

Concretely, when a genuinely unfamiliar grammar shows up in an exam question, a reliable order of attack is to try, in sequence: first, generate three or four short strings directly from the grammar by hand and look for an obvious pattern in their structure; second, if the grammar is ambiguous or awkwardly recursive, attempt CNF conversion or left-recursion elimination to force it into a shape this chapter's standard tools already handle; and third, if a direct pattern still is not visible, build a small PDA by hand for just those same few short strings rather than for the language in full generality. Working the same short list of strings through all three approaches in turn, rather than committing early to just one, is often what actually reveals which standard technique the problem was quietly designed to test in the first place.
`
};
