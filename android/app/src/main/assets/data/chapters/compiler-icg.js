// Textbook chapter: Intermediate Code Generation.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['compiler-icg'] = {
  figs: [
    {
      id: 'tac-representations',
      caption: 'Three equivalent representations of the same three-address code: quadruples name their own result explicitly (freely reorderable), triples reference results by position (fragile under reordering), and indirect triples add a separate execution-order pointer list on top of triples to restore reorderability.',
      svg: '<svg viewBox="0 0 300 100" width="100%" style="max-width:300px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1" fill="none"><rect x="10" y="10" width="85" height="80"/><rect x="107" y="10" width="85" height="80"/><rect x="204" y="10" width="85" height="80"/></g><g font-size="8" fill="currentColor" text-anchor="middle"><text x="52" y="30">Quadruple</text><text x="52" y="45">(op,a1,a2,res)</text><text x="150" y="30">Triple</text><text x="150" y="45">(op,a1,a2)</text><text x="246" y="30">Indirect</text><text x="246" y="45">triple + order</text></g></svg>'
    },
    {
      id: 'dag-sharing',
      caption: 'A DAG shares a node whenever the same operator is applied to the same child nodes in the same order — here i*4 is computed once and reused for both array offsets, collapsing what a plain syntax tree would compute twice.',
      svg: '<svg viewBox="0 0 260 110" width="100%" style="max-width:260px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.1" fill="none"><circle cx="130" cy="20" r="14"/><circle cx="70" cy="60" r="14"/><circle cx="190" cy="60" r="14"/><circle cx="130" cy="95" r="12"/><line x1="122" y1="30" x2="80" y2="50"/><line x1="138" y1="30" x2="180" y2="50"/><line x1="70" y1="74" x2="125" y2="90"/><line x1="190" y1="74" x2="135" y2="90"/></g><g font-size="8" fill="currentColor" text-anchor="middle"><text x="130" y="24">+</text><text x="70" y="64">b[N1]</text><text x="190" y="64">c[N1]*2</text><text x="130" y="99">N1=i*4</text></g></svg>'
    }
  ],
  text: `
WHAT INTERMEDIATE CODE IS FOR, AND WHY IT SITS IN THE MIDDLE

Intermediate code generation CONSUMES the fully type-checked, annotated parse tree the previous two chapters' own semantic-analysis machinery produces, and PRODUCES an INTERMEDIATE REPRESENTATION (IR) — most commonly three-ADDRESS CODE (TAC) — that is deliberately independent of both the source language's own surface syntax AND the target machine's own specific instruction set. This deliberate independence is the entire point of having an intermediate representation at all: a single middle layer lets a compiler's OPTIMIZATION phase (the very next, and final, chapter in this Compiler Design sequence) work on one common representation regardless of which SOURCE language produced it, and lets multiple different target-machine BACK ENDS share that same optimized middle layer, rather than needing separate, from-scratch optimization logic duplicated for every source-language-and-target-machine PAIR.

1. A GOOD intermediate representation must satisfy TWO simultaneous, somewhat competing design goals: it must be EASY to GENERATE directly from a parse tree (so the front end's own job stays simple), AND it must be EASY to TRANSLATE into actual target machine code (so the back end's own job stays simple too) — three-address code satisfies both goals well specifically because its own individual instructions are simple enough to map DIRECTLY onto typical machine instructions, while still being STRUCTURED enough to support meaningful, systematic optimization analysis.

THREE-ADDRESS CODE: FORMS AND TRANSLATION TEMPLATES

1. A THREE-ADDRESS CODE instruction has AT MOST one operator on its right-hand side, and AT MOST three addresses total (one RESULT, and up to two OPERANDS) — any expression more complex than this gets systematically BROKEN DOWN into a SEQUENCE of such simple instructions, using compiler-GENERATED TEMPORARY variables (conventionally named t1, t2, t3, and so on) to hold each intermediate sub-result.
2. THE COMMON INSTRUCTION FORMS: x = y op z (a binary operation); x = op y (a unary operation, such as unary minus, or a plain copy x = y with no operator at all); an UNCONDITIONAL jump, goto L; CONDITIONAL jumps, if x goto L, ifFalse x goto L, or if x relop y goto L (combining a comparison and a jump into one instruction); PROCEDURE CALLS, param x (pushing one argument) followed by call p,n (invoking procedure p with n total arguments) or y = call p,n (capturing a returned value); and ARRAY/POINTER forms, x = y[i] (indexed load), x[i] = y (indexed store), x = &y (address-of), x = *y (dereference), and *x = y (indirect store through a pointer).
3. TRANSLATION TEMPLATES, directly extending this book's own immediately preceding Syntax-Directed Translation chapter's own attribute-driven machinery: for E → E1 + E2, allocate a fresh temporary via E.place = newtemp(), generate E1's own code, then E2's own code, then emit E.place = E1.place + E2.place. For E → −E1 (unary minus), E.place = newtemp(); emit(E.place = uminus E1.place). For S → id = E, emit(id.place = E.place) (an ordinary copy instruction, unless id.place can be reused directly to avoid the copy entirely). For S → if (B) S1, generate B's own code with B.true attached to a FRESH label L1 and B.false attached to S.next (the fall-through/exit label reached once the entire if statement completes), placing label L1 immediately before S1's own generated code. For S → while (B) S1, place a label L_begin immediately BEFORE B's own code; set B.true to a fresh label L1 placed immediately before S1's code; AFTER S1's code, emit an unconditional goto L_begin (closing the loop); set B.false to S.next.

[[FIG:tac-representations]]

4. FOR an ARRAY reference, E → id[E1]: the effective ADDRESS is computed as id's own BASE address plus E1.place TIMES the WIDTH of id's own element type (the standard "index times element size" address-arithmetic formula this book's own later Runtime Environments chapter will develop in even greater generality) — a temporary holds this computed OFFSET, and E.place becomes a further temporary loaded via an INDEXED copy instruction, t = base[offset].
5. BOOLEAN EXPRESSIONS can be translated EITHER "BY VALUE" (numerically — conventionally true=1, false=0 — a straightforward but, for SHORT-CIRCUIT control-flow contexts specifically, somewhat WASTEFUL approach, since it computes an explicit numeric result even when only a JUMP decision is actually needed) OR by "JUMP CODE" (flow-of-control translation, using true/false LABEL attributes exactly as this book's own previous chapter's backpatching machinery developed in full) — the if/while templates given directly above are BOTH examples of this jump-code style, and this jump-code approach is the style GATE-level questions on this specific topic overwhelmingly examine.

1. Worked example (TAC for an assignment with array references, independently verified instruction by instruction): translate a = b[i] + c[i] * 2, assuming an integer element width of 4 bytes for indexing arithmetic. t1 = i * 4 (offset for b[i]); t2 = b[t1] (load b[i]); t3 = i * 4 (offset for c[i] — NOTE this is textually IDENTICAL to t1's own computation, yet a naive left-to-right TAC generator, working without any common-subexpression awareness, emits it a SECOND, entirely separate time, since the two array accesses use different base arrays b and c even though their offset expression is identical); t4 = c[t3] (load c[i]); t5 = t4 * 2; t6 = t2 + t5; a = t6. SEVEN total TAC instructions, with t1 and t3's own redundant duplication being EXACTLY the specific inefficiency this chapter's own DAG-construction section, developed immediately below, is built to detect and eliminate.

REPRESENTATIONS OF TAC: QUADRUPLES, TRIPLES, AND INDIRECT TRIPLES

1. A QUADRUPLE representation stores each instruction as a 4-field record: (op, arg1, arg2, result) — the RESULT field is an EXPLICIT, named temporary or variable, meaning each quadruple is entirely SELF-CONTAINED and can be FREELY REORDERED relative to any other quadruple (since nothing references another instruction by its POSITION, only by an explicitly named variable) — a genuinely convenient property for the optimization phase this book's own very next chapter will develop in full, though quadruples DO use somewhat MORE storage space, and require care when RENAMING temporaries during optimization passes to avoid accidentally breaking a still-needed reference.
2. A TRIPLE representation instead stores (op, arg1, arg2) — WITH NO explicit result field at all; an operand that needs to refer to the RESULT of some OTHER, earlier triple is represented not by a named variable, but by that EARLIER triple's OWN POSITION or INDEX within the overall triple sequence (for instance, writing an operand as "(0)" to mean "whatever value triple number 0 produced"). This SAVES storage space relative to quadruples, but makes triples GENUINELY FRAGILE under REORDERING — MOVING a single triple to a different position in the sequence changes EVERY OTHER triple's own positional references pointing INTO it, a classic, frequently-tested GATE trade-off.
3. INDIRECT TRIPLES resolve this fragility while KEEPING triples' own compact storage: they are ORDINARY triples, PLUS a SEPARATE list of POINTERS to those triples, listed in the DESIRED execution order — reordering the actual generated CODE is then accomplished simply by REARRANGING this separate pointer LIST, WITHOUT touching the underlying triples (and their own internal positional references to EACH OTHER) at all. This is the standard "BEST OF BOTH WORLDS" answer in any comparison question directly weighing quadruples' reorderability against triples' compactness.

GATE TRAP: assuming quadruples and triples differ ONLY in raw "storage space used" is a genuine oversimplification that misses the DEEPER, exam-relevant distinction — the CRITICAL difference is REORDERABILITY specifically: triples BREAK under statement reordering (since they rely on POSITIONAL references into the sequence), while quadruples do NOT (since every reference is an explicitly NAMED variable, entirely independent of instruction POSITION); indirect triples exist SPECIFICALLY to restore this exact reorderability property while still retaining triples' own more compact underlying storage.

GATE TRAP: believing intermediate representation MUST specifically be "three-address code" as a single, fixed FORMAT is a category confusion — quadruples, triples, and indirect triples are all merely different REPRESENTATIONS of the exact SAME underlying three-address-code SEMANTIC instruction sequence, encoding IDENTICAL computational content in three structurally different ways; a question asking to "convert these quadruples into triples" is a PURE representational TRANSCODING exercise, never a re-derivation of the underlying COMPUTATION itself.

DAG CONSTRUCTION: DETECTING AND EXPLOITING SHARED SUBEXPRESSIONS

1. A DIRECTED ACYCLIC GRAPH (DAG) represents an expression MUCH the same way an ordinary syntax tree does, with ONE crucial difference: a node representing a PREVIOUSLY-computed subexpression is SHARED (REUSED) rather than DUPLICATED — this single structural difference is EXACTLY the mechanism by which a compiler detects and exploits COMMON SUBEXPRESSIONS, and, as a direct byproduct, identifies which computed values are genuinely USED versus silently DISCARDED (dead code, covered in full detail in this book's own very next Code Optimization chapter).
2. THE CONSTRUCTION ALGORITHM processes an expression BOTTOM-UP: before creating a BRAND-NEW node for some operation op(n1, n2), FIRST check whether a node ALREADY EXISTS carrying that EXACT SAME operator applied to the SAME child nodes, in the SAME order (checked by NODE IDENTITY, not merely by superficial textual similarity) — if such a node already exists, REUSE it directly rather than constructing a redundant duplicate; LEAF nodes (representing variables and constants) are LIKEWISE shared across every use of the SAME identifier or value within the current basic block.
3. EACH INTERIOR (non-leaf) node of the completed DAG corresponds to EXACTLY ONE three-address-code instruction that genuinely needs to be generated (assuming the sharing this DAG makes visible is actually EXPLOITED during code generation) — the TOTAL number of TAC instructions needed to evaluate an expression via its OWN DAG EQUALS the number of INTERIOR nodes, since each interior node's own value is computed EXACTLY ONCE, no matter how MANY separate places in the original expression happen to reference that same computed value afterward.
4. A DAG can be FURTHER paired with VALUE NUMBERING to detect ALGEBRAIC identities BEYOND simple textual/structural matching (recognising a+b and b+a as computing the SAME value, for instance, once commutativity is explicitly applied) — though the PLAIN sharing rule stated above (IDENTICAL operator plus IDENTICAL children in the SAME order) is what GATE-level questions on this specific topic typically expect BY DEFAULT, UNLESS a question explicitly invokes commutativity or some other specific algebraic identity directly.

[[FIG:dag-sharing]]

1. Worked example (DAG for the SAME expression as the TAC worked example above, with node counts independently verified): build a DAG, bottom-up, for the right-hand side b[i] + c[i] * 2, sharing identical subexpressions wherever they genuinely arise. LEAVES: i, 4, b, c, 2 — exactly 5 leaf nodes (the distinct identifiers and constants appearing anywhere in the expression). Node N1 = i*4 (needed for BOTH b[i]'s own offset AND c[i]'s own offset, since this SPECIFIC subexpression "i*4" is textually and structurally IDENTICAL in both places — it is therefore built exactly ONCE, and SHARED between both array-access computations) — 1 interior node. Node N2 = b[N1] (an array access using base b and offset N1) — 1 interior node. Node N3 = c[N1] (an array access using base c, REUSING the exact SAME offset node N1) — 1 interior node. Node N4 = N3*2 — 1 interior node. Node N5 = N2+N4 (the ROOT of the DAG, representing the value of the entire expression) — 1 interior node. TOTAL interior nodes = 5 (N1 through N5); TOTAL nodes overall (leaves plus interior) = 5+5 = 10 — CONTRASTED against a PLAIN syntax tree with NO interior-node sharing at all, which would need a SEPARATE, entirely duplicated i*4 subtree specifically for c[i]'s own offset, giving 6 interior nodes instead of this DAG's 5. Because the DAG has EXACTLY 5 interior nodes, only 5 TAC instructions are genuinely NEEDED to evaluate this expression (one instruction per interior node, N1 through N5) — INSTEAD of the 6 (or, as this chapter's own TAC worked example directly demonstrated, effectively 7 once the final assignment instruction is also counted) a NAIVE, sharing-unaware tree-walk would otherwise produce, EXACTLY the redundancy-elimination saving this chapter's own two paired worked examples were built to illustrate directly, side by side.

GATE TRAP: miscounting DAG nodes by CARELESSLY including the final assignment's OWN target variable as though it were itself a distinct DAG node, or by OMITTING leaf identifiers and constants from an overall node COUNT, is a common source of an off-by-a-small-amount wrong answer — ALWAYS check EXPLICITLY whether a specific question is asking for "INTERIOR nodes" (which directly equals the number of TAC instructions genuinely needed) or "TOTAL nodes" (interior PLUS leaves combined) — these two counts are GENUINELY different numbers, and BOTH phrasings are commonly asked, sometimes even within the SAME multi-part question.

GATE TRAP: emitting the SAME index-offset computation TWICE (as this chapter's own naive TAC worked example directly demonstrated, with i*4 appearing as BOTH t1 and t3) is NOT itself an error in the generated three-address code — it is simply TAC that has NOT yet had common-subexpression ELIMINATION applied to it; the DAG construction covered in this section is EXACTLY the analytical TOOL that reveals this specific redundancy and makes its subsequent removal possible, but the ORIGINAL, un-optimized TAC sequence itself remains entirely CORRECT and semantically valid throughout, merely inefficient rather than actually wrong.

A SYNTAX-DIRECTED SCHEME FOR GENERATING TAC EXPRESSIONS

1. THIS chapter's own translation templates connect directly to the previous chapter's own S-attributed and L-attributed machinery — E.place (WHERE a computed value lives) is a SYNTHESIZED attribute, built bottom-up from each sub-expression's own already-computed place, exactly mirroring the postorder evaluation order this book's own previous chapter established for S-attributed SDDs generally.
2. A COMPLETE SDD FOR EXPRESSIONS: E → E1 + E2 { E.place = newtemp(); E.code = E1.code || E2.code || gen(E.place '=' E1.place '+' E2.place) } — where the double-bar || denotes STRING (instruction-sequence) CONCATENATION, and gen(...) constructs one new TAC instruction directly. E → id { E.place = id.place; E.code = '' } (an identifier's own place is simply itself, with NO code needed to compute it, since its value already exists directly in memory).
3. THIS "E.code = E1.code || E2.code || gen(...)" PATTERN — concatenating each SUB-expression's own already-generated code, in LEFT-TO-RIGHT order, followed by ONE final new instruction combining their RESULTS — is the single, RECURRING structural template underlying EVERY binary-operator translation rule this chapter has introduced, with only the SPECIFIC operator symbol inside the final gen(...) call actually changing from one operator to another.

PROCEDURE CALL TRANSLATION

1. A PROCEDURE CALL S → call id (Elist) is translated using the param instruction FORM introduced earlier in this chapter: for EACH argument expression in Elist, in LEFT-TO-RIGHT order, first generate that argument's OWN code, THEN emit param argument.place (pushing that ALREADY-computed value as one positional argument) — and, once EVERY argument has been pushed this way, emit a SINGLE final call id, n instruction (where n is the TOTAL argument count), invoking the procedure.
2. A FUNCTION CALL used WITHIN a larger expression, E → id(Elist), follows the identical argument-pushing pattern, but the FINAL instruction instead becomes E.place = call id, n (or, in some textbook conventions, a separate y = call id, n instruction followed by treating y AS E.place) — CAPTURING the function's own RETURN value into a fresh temporary that the SURROUNDING expression can then use exactly like any other computed value.
3. THE ARGUMENT COUNT n is, in BOTH cases, simply the length of the Elist argument list — a SYNTHESIZED attribute computed directly by COUNTING how many individual argument expressions the parser actually recognized, exactly the kind of straightforward synthesized-attribute computation this book's own previous chapter developed in full for S-attributed SDDs generally.

1. Worked example (procedure-call TAC, independently traced through): translate the call statement "call foo(a+1, b)". FIRST argument, "a+1": generate t1 = a + 1, then emit param t1. SECOND argument, "b": no computation needed (b is already a place), emit param b directly. FINALLY, with n=2 total arguments pushed, emit call foo, 2. Full sequence: t1 = a+1; param t1; param b; call foo, 2 — FOUR total instructions, directly following the left-to-right argument-then-call pattern this chapter's own procedure-call translation rule specifies.

SWITCH-STATEMENT (MULTI-WAY BRANCH) TRANSLATION

1. A SWITCH (or CASE) statement, switch(E) { case V1: S1; case V2: S2; ...; default: Sn }, is TRANSLATED using ONE of two standard STRATEGIES, chosen based on the NUMBER and DENSITY of the case values involved.
2. THE "SEQUENTIAL TEST" strategy (suited to a SMALL or SPARSE set of case values): evaluate E ONCE into a temporary t; then generate a SEQUENCE of conditional jumps, if t == V1 goto L1, if t == V2 goto L2, and so on for every case value, FALLING THROUGH to a final goto L_default if none match — each Li then labels the corresponding case's own code, with an UNCONDITIONAL jump to the switch statement's own EXIT label placed at the END of each case's code block (to prevent an unintended FALL-THROUGH into the NEXT case's own code, unless the source language's own semantics explicitly call for fall-through behavior, as C's own switch statement famously does by default).
3. THE "JUMP TABLE" strategy (suited to a LARGE, DENSE range of case values, such as consecutive small integers): build a TABLE of TARGET ADDRESSES, indexed DIRECTLY by the (possibly offset-adjusted) value of E itself, and generate ONE single INDIRECT jump instruction, goto JumpTable[E − minValue] — this achieves CONSTANT-time dispatch REGARDLESS of how MANY case values exist, in sharp contrast to the sequential-test strategy's own linear-in-the-number-of-cases dispatch cost, at the expense of needing genuinely DENSE, closely-packed case values to avoid an impractically large, mostly-unused table.

GATE TRAP: assuming EVERY switch statement is ALWAYS translated using the SAME single strategy regardless of its own specific case-value distribution is incorrect — the sequential-test strategy is generally preferred for a SMALL or WIDELY-SPACED set of case values (where a jump table would need enormous, mostly-wasted space), while the jump-table strategy is preferred SPECIFICALLY for a LARGE, DENSE range of consecutive (or near-consecutive) integer case values, where its constant-time dispatch genuinely outperforms a long sequential chain of individual comparisons; a well-designed real compiler typically CHOOSES between these two strategies AUTOMATICALLY, based on analyzing the SPECIFIC case values a given switch statement actually contains.

WORKED PROBLEMS

1. TAC INSTRUCTION COUNT FOR A SIMPLE EXPRESSION. Generate TAC for x = (a + b) * (c - d), and state the total instruction count. t1 = a + b; t2 = c - d; t3 = t1 * t2; x = t3. FOUR total instructions.

2. QUADRUPLE-TO-TRIPLE CONVERSION. Convert the quadruple sequence (+, a, b, t1), (*, t1, c, t2) into an equivalent TRIPLE sequence. Triple 0: (+, a, b). Triple 1: (*, (0), c) — where "(0)" refers to triple 0's own result, replacing t1's explicit name with a positional reference, exactly as the triple representation's own defining convention requires.

3. TRIPLE REORDERING FRAGILITY DEMONSTRATION. Given the triple sequence from WP2 — triple 0: (+,a,b), triple 1: (*,(0),c) — explain concretely what breaks if these two triples are SWAPPED in position (triple 1 moved to position 0, and vice versa). After swapping, the (multiplication) triple now sits at position 0, and the (addition) triple sits at position 1 — but the multiplication's own operand still says "(0)" (meaning "whatever triple 0 produces"), and triple 0 is now the MULTIPLICATION itself, not the addition it originally meant to reference — the multiplication would now incorrectly refer to ITS OWN, not-yet-computed result as one of its own operands, a genuine, structurally broken reference directly illustrating triples' own fragility under reordering.

4. DAG INTERIOR NODE COUNT FOR A DIFFERENT EXPRESSION. Build a DAG for a*b + a*b (the SAME subexpression a*b appearing twice, added to itself), and state the interior node count. Leaves: a, b — 2 leaf nodes. Node N1 = a*b (built ONCE, since BOTH occurrences of "a*b" are textually and structurally identical, and hence SHARED as the exact same node). Node N2 = N1+N1 (the root, adding the shared N1 node to ITSELF). Interior nodes: N1, N2 — exactly 2 interior nodes, meaning only 2 TAC instructions are genuinely needed (t1 = a*b; t2 = t1+t1), a dramatic saving compared to the 3 instructions (t1=a*b; t2=a*b; t3=t1+t2) a naive, sharing-unaware left-to-right TAC generator would otherwise produce.

5. BOOLEAN EXPRESSION TRANSLATION STYLE IDENTIFICATION. For the statement "if (a < b) x = 1;", would a well-designed compiler typically generate value-based (0/1 numeric) TAC for the condition, or jump-code TAC? JUMP-CODE TAC is the standard, efficient choice — generating "if a<b goto L1" directly, rather than first computing an explicit numeric 0-or-1 value for "a<b" and THEN testing that value with a separate conditional jump; value-based translation would require an UNNECESSARY extra instruction (materializing the numeric truth value) that jump-code translation avoids entirely by combining the comparison and the jump into one single instruction.

6. WHILE-LOOP TAC TEMPLATE APPLIED DIRECTLY. Using this chapter's own while-loop translation template, generate TAC for "while (i < 10) i = i + 1;", with explicit label names. L_begin: if i < 10 goto L1; goto L_next; L1: t1 = i + 1; i = t1; goto L_begin; L_next: (code continues here after the loop). This directly matches the template's own structure: L_begin marks the re-test point, L1 marks the loop-body entry, and the final goto L_begin closes the loop back to the condition re-test.

7. INDIRECT TRIPLE REORDERING. Using the SAME two triples from WP2/WP3 (triple 0: (+,a,b); triple 1: (*,(0),c)), show how an INDIRECT-triple execution-order list would correctly express executing triple 1's OWN operation before triple 0's, WITHOUT actually needing to reorder the underlying triples themselves, and explain why this avoids WP3's own breakage. This is actually NOT achievable directly for these SPECIFIC two triples, since triple 1 GENUINELY depends on triple 0's own result (via the "(0)" reference) — no valid execution order can run triple 1 before triple 0 while preserving correctness. The GENUINE benefit indirect triples provide is: for two triples with NO such dependency between them, an execution-order list [1, 0] (listing triple 1 before triple 0) can safely swap their EXECUTION order WITHOUT altering either triple's own internal (op, arg1, arg2) fields at all — exactly the flexibility WP3's own plain-triple reordering attempt broke, restored here specifically for genuinely independent triples.

8. DAG SHARING WITH A COMMUTATIVITY TWIST. Does the plain DAG-sharing rule (identical operator, identical children, same order) automatically merge the two subexpressions a+b and b+a into a single shared node? No — the plain sharing rule requires the SAME operator applied to the SAME children in the SAME ORDER; a+b and b+a have their two operands in OPPOSITE order, so the plain rule does NOT merge them by default, even though addition is mathematically commutative. Merging them would require EXPLICITLY invoking a commutativity-aware value-numbering extension beyond the plain sharing rule this chapter's own GATE TRAP above specifically warns must not be assumed unless a question explicitly calls for it.

9. SWITCH-STRATEGY SELECTION. A switch statement has case values 1, 2, 3, 4, 5 (five consecutive small integers). Which translation strategy would a well-designed compiler most likely choose, and why? The JUMP-TABLE strategy — since the case values are DENSE and CONSECUTIVE, a table of just 5 target addresses (indexed directly by the value minus 1) gives constant-time dispatch with minimal wasted space, clearly preferable to a sequential chain of 5 separate comparisons.

10. SWITCH-STRATEGY SELECTION, A CONTRASTING CASE. A switch statement has case values 3, 47, 1000, and 250000 (four widely-spaced, sparse integers). Which strategy fits better here? The SEQUENTIAL-TEST strategy — a jump table indexed across this entire range would need roughly 250,000 entries, the overwhelming majority entirely unused, making it hopelessly wasteful; four ordinary sequential comparisons handle this sparse case far more efficiently.

11. FUNCTION-CALL-WITHIN-EXPRESSION TRANSLATION. Translate "x = foo(a, b+1) + 2" into TAC, using this chapter's own function-call and expression templates together. Following the LEFT-TO-RIGHT argument order this chapter's own procedure-call rule specifies (for EACH argument in order, first generate its own code, THEN emit param): param a (the first argument, a, needs no code of its own); t1 = b + 1 (the second argument's code); param t1; t2 = call foo, 2; t3 = t2 + 2; x = t3. SIX total instructions, combining the argument-evaluation-and-push sequence with the surrounding addition exactly as this chapter's own separate function-call and binary-operator templates each independently specify.

12. DAG SHARING ACROSS A LARGER EXPRESSION. Build a DAG for (a+b)*(a+b) - c, and state the interior node count. Leaves: a, b, c — 3 leaf nodes. Node N1 = a+b (built once, shared for both occurrences of "a+b" since they are textually and structurally identical). Node N2 = N1*N1 (reusing the shared N1 node twice). Node N3 = N2-c. Interior nodes: N1, N2, N3 — exactly 3 interior nodes, meaning only 3 TAC instructions are needed (t1=a+b; t2=t1*t1; t3=t2-c), compared to the 4 a naive, sharing-unaware generator would produce (recomputing a+b a second time).

SHORT-CIRCUIT EVALUATION: WHY JUMP CODE MATTERS BEYOND EFFICIENCY

1. BEYOND the raw efficiency argument already made above, jump-code translation for boolean expressions has a SECOND, genuinely important motivation: SHORT-CIRCUIT semantics, standard in essentially every widely-used programming language, require that for "A and B", B is EVALUATED ONLY if A is true, and for "A or B", B is evaluated ONLY if A is false — a VALUE-based (0/1 numeric) translation that computes BOTH operands' full numeric values unconditionally, then combines them with an ordinary logical AND/OR operator, would VIOLATE this required short-circuit behavior whenever evaluating B has an observable SIDE EFFECT (a function call that prints output, modifies a variable, or similar).
2. JUMP-CODE translation naturally PRESERVES short-circuit semantics AUTOMATICALLY, as a direct STRUCTURAL consequence of its own design — the "and" pattern's own marker-nonterminal-based backpatching (developed in full in this book's own immediately preceding Syntax-Directed Translation chapter) generates code that LITERALLY jumps AROUND the second operand's own evaluation entirely whenever the FIRST operand already determines the overall result, meaning B's own code is SIMPLY NEVER EXECUTED in exactly those cases where short-circuit semantics require it to be skipped.

KEY: short-circuit evaluation is a genuinely IMPORTANT, independent reason — beyond the raw instruction-count efficiency already discussed — that essentially EVERY real compiler chooses jump-code translation for boolean expressions specifically involving "and"/"or" operators, rather than the seemingly simpler value-based numeric approach; this is a direct, concrete illustration of how a compiler-construction DESIGN CHOICE can be driven by SEMANTIC correctness requirements (what the source language's own specification demands must happen), not merely by a narrower, purely EFFICIENCY-focused consideration.

VALUE NUMBERING: A MORE POWERFUL VARIANT OF DAG SHARING

1. VALUE NUMBERING assigns EACH distinct COMPUTED VALUE (rather than each distinct SYNTACTIC expression) a unique NUMBER, and RECOGNIZES two syntactically DIFFERENT expressions as computing the SAME value whenever some known ALGEBRAIC property (commutativity, associativity, or a specific arithmetic identity) proves they MUST be equal — a genuinely more powerful sharing criterion than the DAG's own plain "identical operator, identical children, same order" rule developed earlier in this chapter.
2. THE STANDARD ALGORITHM maintains a hash table mapping (operator, value-number-of-left-operand, value-number-of-right-operand) triples to an assigned VALUE NUMBER; when a genuinely NEW combination is encountered, a fresh value number is assigned; when an ALREADY-seen combination recurs, the EXISTING value number is reused directly — this is, structurally, EXACTLY the DAG-construction algorithm from earlier in this chapter, but GENERALIZED to also detect algebraically EQUIVALENT (not merely syntactically IDENTICAL) expressions, when the specific value-numbering implementation is extended with COMMUTATIVITY-AWARE hashing (treating a+b and b+a as hashing to the SAME table entry, for instance).
3. A commutativity-aware value-numbering scheme WOULD merge a+b and b+a into the SAME value number (unlike the PLAIN DAG-sharing rule, which — as this chapter's own earlier GATE TRAP explicitly warned — does NOT merge them by default) — but this ENHANCED merging capability comes at the cost of a MORE COMPLEX hashing and lookup scheme than the plain DAG's own simpler identity check requires.

GATE TRAP: assuming value numbering and plain DAG construction are simply TWO DIFFERENT NAMES for the exact SAME single technique is an oversimplification worth avoiding — DAG construction, exactly as this chapter developed it earlier, uses a STRICT identity-based sharing rule (same operator, same children, same order, checked purely by object/node identity); value numbering EXTENDS this same underlying idea with ADDITIONAL algebraic-equivalence detection (commutativity-aware hashing, and potentially further identities such as recognizing x+0 as always equal to x), making value numbering the STRICTLY more general and more powerful of the two closely related techniques, though also the more IMPLEMENTATION-complex one.

STATIC VS DYNAMIC TYPE CHECKING: A BRIEF CONNECTION BACK

1. THE type-checking machinery this book's own previous Syntax-Directed Translation chapter developed operates ENTIRELY at COMPILE time — this is STATIC type checking, verifying type consistency BEFORE the program ever actually RUNS, using ONLY the types that can be determined directly from the program's own SOURCE TEXT.
2. SOME languages instead defer SOME or ALL type checking to RUN time (DYNAMIC type checking) — generating TAC that includes explicit RUNTIME type-tag checks BEFORE performing an operation, rather than relying entirely on a COMPILE-time guarantee that the operation is already known to be type-safe.
3. FOR a STATICALLY type-checked language, intermediate code generation can SAFELY OMIT any runtime type-checking instructions entirely, since the earlier semantic-analysis phase has ALREADY verified, once and for all at compile time, that every operation in the generated code is guaranteed type-safe — a genuinely important EFFICIENCY advantage of static typing directly VISIBLE at the level of the generated intermediate code itself, connecting this chapter's own TAC-generation machinery DIRECTLY back to the type-checking discipline the immediately preceding chapter developed in full.

WORKED PROBLEMS, PART TWO

13. SHORT-CIRCUIT SEMANTICS VIOLATION CHECK. A naive value-based translation for "A and B" computes A's numeric value, computes B's numeric value UNCONDITIONALLY, then combines them with a logical AND. If B is the function call "risky()" that has a side effect (printing a message), does this value-based translation preserve correct short-circuit semantics when A is false? No — this translation VIOLATES short-circuit semantics, since B's own code (including its side effect) runs UNCONDITIONALLY regardless of A's value, even though correct "and" semantics require B to be evaluated ONLY when A is true; jump-code translation avoids this exact problem by literally skipping B's own code entirely whenever A already determines the result.

14. VALUE NUMBERING VS PLAIN DAG SHARING. Would a PLAIN DAG (using only the strict identity-based sharing rule) merge the two subexpressions (x+y) and (y+x) into a single shared node? Would a COMMUTATIVITY-AWARE value-numbering scheme? The PLAIN DAG would NOT merge them (different operand order violates the strict "same children, same order" rule). A COMMUTATIVITY-AWARE value-numbering scheme WOULD merge them, since it explicitly recognizes that addition is commutative and hashes both orderings to the same value number.

15. STATIC TYPING EFFICIENCY ADVANTAGE. A statically-typed language's compiler generates TAC for "z = x + y" where x and y are both already known, at compile time, to be int. Does the generated TAC need an explicit runtime type check before performing the addition? No — since static type checking has ALREADY verified, once and for all at compile time, that x and y are genuinely int, the generated TAC can safely omit any runtime type-tag check entirely, directly emitting "z = x + y" as an unconditional integer-addition instruction, with no wasted runtime overhead checking something already proven safe.

A FULLY WORKED SYNTHESIS: FROM SOURCE STATEMENT TO OPTIMIZED-READY TAC

To bring together the standard translation templates, the representation choice, and the DAG-based redundancy analysis this chapter has developed, consider carrying a single, moderately involved source statement all the way from its own raw form through to a genuinely redundancy-free intermediate representation.

1. THE SOURCE STATEMENT: "if (a[i] + a[i] > 10) x = a[i] * 2;" — deliberately chosen to contain a REPEATED array access (a[i], appearing three separate times) so that this chapter's own DAG-based sharing analysis has genuine, concrete work to do.
2. STEP 1 — NAIVE, LEFT-TO-RIGHT TAC GENERATION, following this chapter's own translation templates directly and without yet applying any sharing analysis: t1 = i * 4 (offset for the first a[i]); t2 = a[t1] (load first a[i]); t3 = i * 4 (offset for the second a[i] — textually identical to t1, yet regenerated); t4 = a[t3] (load second a[i]); t5 = t2 + t4; if t5 > 10 goto L1; goto L_next; L1: t6 = i * 4 (offset for the third a[i] — again textually identical); t7 = a[t6] (load third a[i]); t8 = t7 * 2; x = t8; L_next: (code continues, falling straight through from x = t8 with no extra jump needed, per this chapter's own if-without-else template). ELEVEN total instructions in this naive, unshared form.
3. STEP 2 — BUILD THE DAG for the boolean condition's own sub-expression, a[i]+a[i], recognizing the SAME sharing opportunity this chapter's own earlier worked DAG example demonstrated for a similar structure: leaves i, 4, a — 3 leaves. Node N1 = i*4 (built ONCE, shared for BOTH occurrences within the CONDITION specifically). Node N2 = a[N1] (the array load, ALSO shared, since both occurrences of a[i] within the condition are identical and reference the SAME already-computed offset N1). Node N3 = N2+N2 (the condition's own addition, reusing the SAME loaded value N2 on both sides). Interior nodes for the condition alone: N1, N2, N3 — 3 interior nodes, meaning the condition itself needs only 3 TAC instructions (t1=i*4; t2=a[t1]; t3=t2+t2), rather than the naive generation's own 5 (t1 through t5 above).
4. STEP 3 — RECOGNIZE THE FURTHER SHARING OPPORTUNITY ACROSS the if-branch's own body: the THIRD occurrence of a[i] (inside "x = a[i] * 2") is available for sharing TOO, but only IF this sharing analysis is applied ACROSS the entire basic block spanning both the condition and the (conditionally executed) body — a genuinely important scoping subtlety worth flagging explicitly: DAG-based common-subexpression sharing is normally only SAFELY applicable WITHIN a single basic block (a straight-line sequence of code with no branches INTO or OUT of its middle), and here the if-branch's own body is a SEPARATE basic block from the condition (since a branch — the conditional jump — separates them), meaning the offset node N1 (i*4) and the load node N2 (a[i]) computed for the CONDITION cannot, in general, be safely reused inside the BODY block without more advanced, whole-procedure analysis extending beyond a single basic block, which lies beyond a plain DAG's own normal scope.
5. STEP 4 — FINAL, CORRECTLY-SCOPED RESULT: applying DAG-based sharing ONLY within each basic block separately (the standard, safe practice), the CONDITION's own block shrinks from 5 instructions to 3 (as Step 2 established), while the BODY block still needs its OWN separate i*4 and a[i] computations (since it is a genuinely different basic block), giving a total of 3 (condition) + 2 (the branch instructions) + 3 (body's own offset, load, and multiply) + 1 (the assignment) = 9 total instructions — a genuine improvement over the naive 11, though NOT as large a reduction as would be possible if the sharing analysis could safely span BOTH blocks together (a capability the very next Code Optimization chapter's own more advanced techniques, operating across an entire procedure rather than one block at a time, are specifically built to provide).

GATE TRAP: assuming a DAG's own subexpression-sharing analysis can be applied SAFELY across an ENTIRE procedure, spanning MULTIPLE basic blocks connected by branches, without any further precaution, is a genuinely important oversimplification this chapter's own final worked synthesis directly illustrates — a value computed in ONE basic block may NOT still be valid, or even still exist in the expected register or temporary, by the time execution reaches a DIFFERENT basic block reached via a conditional branch, since the variables involved (here, specifically i and a) COULD have been modified somewhere along an intervening code path the straightforward single-block DAG analysis does not itself account for; genuinely safe CROSS-block sharing requires the more sophisticated whole-procedure data-flow analysis techniques this book's own next, and final, Compiler Design chapter develops in full.

KEY: this chapter's closing synthesis demonstrates directly why "generate the naive TAC first, then separately apply DAG-based analysis to find and remove redundancy" is not merely a convenient TEACHING sequence but reflects how a REAL compiler's own internal pipeline is typically structured — intermediate code generation (THIS chapter's own subject) and code optimization (the VERY NEXT chapter's own subject) are DELIBERATELY kept as two SEPARATE compiler phases specifically so that the code-generation phase can stay SIMPLE and mechanical (directly following the translation templates this chapter developed, without needing to reason about sharing or redundancy at generation time at all), while a SEPARATE, dedicated optimization phase handles the analytically harder work of detecting and eliminating redundancy afterward, using exactly the DAG-based and value-numbering techniques this chapter has already introduced as the conceptual FOUNDATION that next chapter's own more elaborate techniques build directly upon.

Recognising this DISTINCTION — REPRESENTATION choice versus actual COMPUTATIONAL content — is worth carrying forward explicitly, since it is precisely the same distinction this book's own earlier Lexical Analysis chapter drew between table-driven and direct-coded DFA IMPLEMENTATION (a pure implementation-style choice, with NO effect on which language gets recognized) and, correspondingly, exactly the kind of "implementation detail versus underlying substance" separation this book has modeled consistently across many, otherwise quite different, engineering contexts.

CARRYING THIS FORWARD

KEY: the DAG's own interior-node-equals-instruction-count relationship is, at its structural core, DIRECTLY analogous to this book's own earlier Theory of Computation material on parse-tree height and derivable-string length (from the Context-Free Languages chapter) — in BOTH cases, a TREE-shaped (or, here, DAG-shaped) data structure's own SIZE directly and precisely determines some OTHER quantity of genuine practical interest (derivable string length there; required instruction count here), and in BOTH cases, the KEY analytical move is recognising that SHARING or RESTRICTING the underlying graph's own STRUCTURE (Chomsky Normal Form's strict binary branching there; a DAG's own subexpression sharing here) is what makes that precise, countable relationship hold at all.

The FINAL chapter in this Compiler Design sequence, on CODE OPTIMIZATION, picks up exactly where this chapter leaves off — taking the three-ADDRESS CODE this chapter's own translation templates produce (potentially still containing the KIND of redundant, unshared computation this chapter's own DAG-construction section has already shown how to detect) and applying a SYSTEMATIC set of optimization TRANSFORMATIONS — common-subexpression elimination (directly using the DAG-sharing insight developed here), dead-code elimination, constant folding, and several further techniques — to produce a genuinely IMPROVED intermediate representation, before that representation is finally translated into actual target-machine code.

KEY: it is worth stating explicitly, as a bridge directly into that next and final chapter, precisely WHAT this chapter has and has NOT yet accomplished — this chapter has shown HOW to generate correct three-address code from a parse tree, and has shown, via the DAG, HOW to detect where redundant computation genuinely exists, but this chapter has deliberately stopped SHORT of actually rewriting the generated code to remove that detected redundancy across an entire procedure spanning multiple basic blocks — that ACTUAL rewriting step, extended safely across branches using proper data-flow analysis rather than the single-block DAG technique developed here, is precisely the subject matter the next chapter takes up as its own dedicated starting point, picking up the analytical groundwork this chapter has already carefully laid.

A closing practical summary of this chapter's own toolkit, worth holding onto as a compact whole rather than as several separately-memorised pieces: three-address code's own instruction forms (the basic vocabulary every translation template in this chapter builds from); the translation TEMPLATES themselves (mechanically converting an already-parsed, already-type-checked construct into a specific sequence of TAC instructions, extending directly from the previous chapter's own synthesized-attribute machinery); the quadruple-versus-triple-versus-indirect-triple REPRESENTATION choice (a pure storage-and-reorderability trade-off with zero effect on the underlying computation); and the DAG-construction technique (which does genuinely change what needs computing, by exposing and eliminating truly redundant recomputation). Every worked example and every worked problem in this entire chapter is, without exception, a direct application of one or more of these four core pieces, and a reader who has genuinely internalized all four, rather than merely the chapter's own specific worked numbers, is fully equipped to generate and analyze intermediate code for constructs this chapter never explicitly names at all.

A final connecting thought, appropriate for closing out this particular chapter specifically: the three-address code this chapter has spent its entire length carefully developing is, in a very real sense, the single most important artifact this entire Compiler Design sequence produces — every earlier chapter (lexical analysis, syntax analysis, syntax-directed translation) exists specifically to correctly PRODUCE this intermediate representation from raw source text, while the one remaining chapter (code optimization) exists specifically to IMPROVE it before it is finally handed off to a target-machine code generator. Three-address code is, quite literally, the pivot point this entire sequence has been building steadily toward from its very first chapter's own lexical-token stream onward — everything before this chapter has been building TOWARD it, and everything remaining in this sequence works directly FROM it.

KEY: it is worth restating, one final time, the specific practical reason three-address code sits at exactly this pivotal middle position rather than being generated directly as target-machine instructions from the parse tree in one single step — three-address code's own deliberate machine-independence, established at the very opening of this chapter, is precisely what lets the SAME front-end machinery (lexical analysis, parsing, syntax-directed translation) support MULTIPLE different target architectures without any duplicated effort, and lets the SAME optimization logic (the next chapter's own subject) improve code regardless of which specific source language originally produced it. A compiler architecture that skipped this intermediate layer entirely, translating directly from parse tree to target machine code in one step, would need to duplicate its entire front-end-plus-optimization logic separately for every single target architecture it wished to support, and would lose the ability to share optimization improvements across different source languages compiled through the same overall toolchain — exactly the two practical costs the intermediate-representation design this chapter has developed in full is specifically built to avoid, at the modest price of one additional translation step in the overall compilation pipeline.

A brief historical note, worth mentioning as this chapter draws to its own close: this general architecture — a shared, machine-independent middle layer separating language-specific front ends from architecture-specific back ends — is not merely a textbook abstraction but the genuine, deliberate design underlying essentially every serious, widely-used compiler infrastructure built over the past several decades, from the GNU Compiler Collection's own internal representations through to LLVM's own explicitly-named intermediate representation, both of which support numerous distinct source languages compiling down to numerous distinct target architectures through precisely this shared-middle-layer architecture. The three-address code this chapter has developed from first principles is, in this very real sense, a simplified but structurally faithful model of exactly the kind of intermediate representation genuinely powering the compilers most working programmers rely on every single day, whether or not they are ever aware of the specific internal representation doing the actual work underneath their own source code.

A final closing reflection tying this chapter back to the broader arc of this entire book: the machine-independence versus machine-dependence distinction driving this chapter's own architectural choices is, at its core, simply the front-end-versus-back-end distinction this book's very first Compiler Design chapter introduced when laying out the classical phase pipeline in full — lexical analysis, syntax analysis, semantic analysis, and this chapter's own intermediate code generation together form the source-language-dependent, machine-independent front end, while target code generation alone forms the machine-dependent back end, with code optimization occupying a genuinely flexible middle position that can operate either on the machine-independent intermediate representation or, separately, on already-generated machine-dependent target code via peephole techniques. Recognising each chapter in this sequence's own precise position within that original, opening classification, rather than treating each chapter as an isolated topic studied purely in its own right, is exactly the kind of connective, whole-picture understanding this entire Compiler Design sequence has been built, chapter by chapter, to deliver in full by the time its own final chapter is reached.

With this chapter's own material now fully in place, only one chapter remains in this sequence, and it is worth pausing briefly to note how much groundwork has already been laid for it specifically. The DAG-based sharing analysis developed here, the value-numbering extension sketched above, and the basic-block scoping caveat this chapter's own closing synthesis deliberately surfaced are not merely interesting side observations — they are, quite precisely, the exact starting vocabulary the next chapter's own treatment of common-subexpression elimination, data-flow analysis, and control-flow graphs will assume as already-familiar background, building its own more elaborate machinery directly and immediately on top of these specific ideas rather than introducing them again from scratch. A reader who has worked carefully through every worked example in this chapter, tracing each one by hand rather than skimming past the arithmetic, arrives at that next and final chapter already equipped with precisely the conceptual toolkit it assumes on its very first page, ready to build directly and confidently on everything this particular chapter has now carefully and thoroughly established from the ground up, one deliberate and thoroughly well-verified step at a time, consistently and carefully, all the way throughout this entire chapter's own material, from its own very start right through to its own truly final finish.
`
};
