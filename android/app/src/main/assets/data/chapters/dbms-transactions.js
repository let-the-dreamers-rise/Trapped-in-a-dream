// Textbook chapter: Transactions, Concurrency Control, and Recovery.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['dbms-transactions'] = {
  figs: [
    {
      id: 'precedence-graph',
      caption: 'A precedence (serialization) graph for a schedule: one node per transaction, one edge Ti→Tj whenever Ti has a conflicting operation that must execute before Tj\'s corresponding operation. The schedule is conflict serializable exactly when this graph is acyclic.',
      svg: '<svg viewBox="0 0 220 100" width="100%" style="max-width:220px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.1" fill="none"><circle cx="40" cy="50" r="18"/><circle cx="110" cy="20" r="18"/><circle cx="180" cy="50" r="18"/><line x1="56" y1="42" x2="94" y2="26"/><line x1="126" y1="26" x2="164" y2="42"/></g><g font-size="9" fill="currentColor" text-anchor="middle"><text x="40" y="54">T1</text><text x="110" y="24">T2</text><text x="180" y="54">T3</text></g></svg>'
    },
    {
      id: 'two-phase-locking',
      caption: 'Two-phase locking: every transaction has a growing phase (locks only acquired, never released) followed by a shrinking phase (locks only released, never acquired), with a single peak marking the transaction\'s lock point.',
      svg: '<svg viewBox="0 0 240 100" width="100%" style="max-width:240px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.3" fill="none"><polyline points="20,80 120,15 220,80"/></g><g font-size="8" fill="currentColor" text-anchor="middle"><text x="65" y="55">growing</text><text x="175" y="55">shrinking</text><text x="120" y="10">lock point</text></g></svg>'
    }
  ],
  text: `
TRANSACTIONS AND THE ACID PROPERTIES

This book's own preceding chapters built well-NORMALIZED tables (the Normalization chapter) made EFFICIENT to search (the Indexing chapter) — this FINAL Databases chapter examines a genuinely DIFFERENT concern: keeping those SAME tables SAFE and CONSISTENT when MANY operations execute CONCURRENTLY, and when FAILURES (crashes, power loss) can strike at ANY moment mid-operation.

1. A TRANSACTION is a SINGLE logical unit of work — a SEQUENCE of one or more database operations (reads and writes) that must be treated as an INDIVISIBLE whole: either ALL of its effects are applied, or NONE are, no matter what else happens concurrently, and no matter what specific kind of failure occurs partway through its own execution.
2. ATOMICITY: a transaction's operations are ALL executed, or NONE are — there is NO partial completion visible to the rest of the system. A transaction that fails partway through is ROLLED BACK, undoing any changes it had ALREADY made, restoring the database to its state BEFORE the transaction began.
3. CONSISTENCY: a transaction takes the database from ONE consistent state to ANOTHER consistent state — any integrity constraints (primary keys, foreign keys, check constraints) that held BEFORE the transaction must STILL hold AFTER it completes, though they may be MOMENTARILY violated DURING the transaction's own intermediate steps.
4. ISOLATION: CONCURRENTLY executing transactions must NOT interfere with each other's INTERMEDIATE (uncommitted) states — the EFFECT of running several transactions concurrently must be EQUIVALENT to running them in SOME serial (one-at-a-time) order, even though they physically INTERLEAVE their individual operations for performance. This is the property this chapter's own CONCURRENCY CONTROL sections below examine in the greatest depth.
5. DURABILITY: once a transaction COMMITS (successfully finishes), its effects must SURVIVE any SUBSEQUENT failure — a crash occurring immediately AFTER a commit must NOT lose that transaction's own changes, which is precisely why every commit is backed by a PERSISTENT log record written to STABLE (non-volatile) storage before the commit is acknowledged, exactly the RECOVERY mechanism this chapter's own closing sections examine.
6. THESE four properties are collectively known by the acronym ACID, and EVERY concurrency-control and recovery technique this chapter develops exists SOLELY to guarantee these four properties HOLD, even under concurrent execution and unpredictable, arbitrarily-timed failures — recognizing WHICH specific ACID property a given mechanism protects is the single most useful organizing lens for the entire remainder of this chapter.

TRANSACTION STATES

1. ACTIVE: the INITIAL state — the transaction is EXECUTING its own operations.
2. PARTIALLY COMMITTED: AFTER the FINAL statement has executed, but BEFORE the transaction is DECLARED fully committed (some final checks, such as flushing logs to STABLE storage, may still be pending).
3. FAILED: the transaction can NO LONGER proceed with NORMAL execution, typically because a HARDWARE failure or a LOGICAL error (such as a constraint violation) has occurred.
4. ABORTED: the transaction has been ROLLED BACK, and the database has been RESTORED to the state it was in BEFORE the transaction began. From here, the transaction may either be RESTARTED (if the failure was TRANSIENT) or KILLED (if it was a genuine LOGICAL error that will simply recur).
5. COMMITTED: the transaction has SUCCESSFULLY completed, and its effects are now PERMANENT (durable), surviving any subsequent failure.
6. THE state-transition diagram moves ACTIVE → PARTIALLY COMMITTED → COMMITTED along the SUCCESS path, with ACTIVE → FAILED → ABORTED as the alternate FAILURE path — importantly, a transaction can move to FAILED from EITHER the active OR the partially-committed state, but ONCE committed, a transaction can NEVER move back to failed or aborted; this ASYMMETRY (commit is a ONE-WAY, durable operation) is exactly why the DURABILITY property above is stated as an UNCONDITIONAL guarantee once commit has genuinely, verifiably occurred.

SCHEDULES: SERIAL VERSUS CONCURRENT EXECUTION

1. A SCHEDULE is a chronological SEQUENCE of the individual READ and WRITE operations belonging to one or MORE transactions, showing the ORDER in which the database system actually executes them.
2. A SERIAL schedule executes EACH transaction's operations completely, ONE transaction fully finished before the NEXT begins — with NO interleaving at all. A serial schedule TRIVIALLY preserves consistency (assuming each individual transaction, run ALONE, preserves consistency), but SACRIFICES the performance benefit of allowing MULTIPLE transactions to make progress concurrently (for instance, while one transaction waits on a slow disk read, another could otherwise be doing useful CPU work).
3. A CONCURRENT (non-serial) schedule INTERLEAVES operations from DIFFERENT transactions — offering BETTER throughput and resource utilization, but INTRODUCING the genuine RISK of anomalies (lost updates, dirty reads, unrepeatable reads) if the interleaving is NOT carefully controlled.
4. THE central goal of CONCURRENCY CONTROL is precisely to ALLOW concurrent (interleaved) schedules for PERFORMANCE, while GUARANTEEING that the RESULT is equivalent to SOME serial schedule — this equivalence notion is called SERIALIZABILITY, and is the SINGLE most important correctness criterion this entire chapter builds toward.

CONFLICT SERIALIZABILITY AND THE PRECEDENCE GRAPH

[[FIG:precedence-graph]]

1. TWO operations CONFLICT if they belong to DIFFERENT transactions, access the SAME data item, and AT LEAST one of them is a WRITE. Concretely: read-read pairs never conflict (both merely observe, neither changes anything); read-write and write-read pairs conflict (order matters — reading BEFORE versus AFTER a write of the SAME item gives genuinely DIFFERENT results); write-write pairs conflict (the FINAL value left behind depends on WHICH write happens LAST).
2. TWO schedules are CONFLICT EQUIVALENT if one can be transformed into the OTHER by a SEQUENCE of SWAPS of adjacent, NON-conflicting operations (swapping adjacent operations that DO conflict would change the outcome, so only non-conflicting swaps are permitted).
3. A schedule is CONFLICT SERIALIZABLE if it is CONFLICT EQUIVALENT to SOME serial schedule.
4. THE PRECEDENCE (SERIALIZATION) GRAPH TEST, the standard, MECHANICAL way to check conflict serializability: create ONE node per transaction; draw a DIRECTED edge Ti→Tj whenever Ti has an operation that CONFLICTS with, and occurs BEFORE, a corresponding operation of Tj. THE schedule is conflict serializable IF AND ONLY IF this graph is ACYCLIC (contains no directed cycle) — and if acyclic, ANY topological ordering of the graph's nodes gives a VALID equivalent serial order.
5. IF the precedence graph CONTAINS a cycle, the schedule is DEFINITELY NOT conflict serializable — no swapping of non-conflicting adjacent operations can ever reorder the transactions into ANY serial sequence, since the cycle represents a genuine, UNRESOLVABLE circular dependency among the transactions' own conflicting operations.

GATE TRAP: forgetting to check EVERY pair of conflicting operations between two transactions (rather than just the FIRST one encountered) can cause a genuine edge to be MISSED, or an edge drawn in the WRONG direction — the precedence-graph edge direction is determined STRICTLY by the ORDER the conflicting operations actually appear in the schedule, and a transaction pair can, in principle, contribute conflicting operations in BOTH directions across different data items, which (if it occurs) IMMEDIATELY creates a two-node cycle and proves the schedule is NOT conflict serializable.

VIEW SERIALIZABILITY

1. A schedule is VIEW SERIALIZABLE if it is VIEW EQUIVALENT to some serial schedule, where VIEW EQUIVALENCE requires THREE conditions to hold between the two schedules: (a) if Ti reads the INITIAL value of a data item in one schedule, Ti must ALSO read the initial value of that SAME item in the other; (b) if Ti reads a value WRITTEN by Tj in one schedule, Ti must read a value written by that SAME Tj in the other; (c) the FINAL writer of each data item must be the SAME transaction in BOTH schedules.
2. EVERY conflict-serializable schedule is ALSO view serializable, but the CONVERSE does NOT hold — there EXIST view-serializable schedules that are NOT conflict serializable, specifically ones containing BLIND WRITES (a write operation on a data item that is NOT preceded by a READ of that SAME item within the SAME transaction), since blind writes can satisfy the view-equivalence conditions above WITHOUT necessarily satisfying the stricter, operation-order-based conflict-equivalence test.
3. VIEW serializability is, in general, COMPUTATIONALLY harder to TEST than conflict serializability (view-serializability testing is NP-complete in general, whereas the precedence-graph cycle test for conflict serializability runs in POLYNOMIAL time) — this is PRECISELY why real database systems, and essentially ALL GATE questions, rely on the conflict-serializability precedence-graph test as the PRACTICAL, computationally tractable correctness criterion, treating view serializability mainly as a THEORETICAL, strictly WEAKER-in-practical-use notion that explains WHY conflict serializability is only a SUFFICIENT, not a NECESSARY, condition for genuine correctness.

KEY: the relationship CONFLICT SERIALIZABLE ⊊ VIEW SERIALIZABLE ⊊ (schedules producing a correct, "as if serial" outcome) is structurally the SAME kind of strict-containment hierarchy this book has met REPEATEDLY in entirely different contexts — most directly, the earlier Theory of Computation chapter's own REGULAR ⊊ CFL ⊊ CSL ⊊ REC ⊊ RE hierarchy — reinforcing that "a SUFFICIENT but not NECESSARY test that is computationally TRACTABLE, trading a small amount of theoretical completeness for practical usability" is a recurring engineering trade-off pattern across entirely different subjects, not a coincidence specific to schedules alone.

WORKED PROBLEMS

1. PRECEDENCE GRAPH CONSTRUCTION AND SERIALIZABILITY CHECK. A schedule has operations in this order: R1(A), R2(A), W1(A), R2(B), W2(B), W1(B). Determine whether this schedule is conflict serializable. Identify conflicts: R1(A) before W2? No W2(A) exists, so no conflict there. R2(A) and W1(A): R2(A) occurs BEFORE W1(A), both on item A, one is a write — CONFLICT, giving edge T2→T1. R2(B) and W1(B): R2(B) occurs BEFORE W1(B), both on item B — CONFLICT, giving edge T2→T1 again (same direction, no new information). W2(B) and W1(B): W2(B) occurs BEFORE W1(B), both writes on B — CONFLICT, giving edge T2→T1 again. The complete precedence graph has a SINGLE edge, T2→T1, with NO cycle — the schedule IS conflict serializable, equivalent to the serial order T2 followed by T1.

2. A CYCLIC PRECEDENCE GRAPH EXAMPLE. A schedule has operations: R1(A), W2(A), R2(B), W1(B). Determine serializability. R1(A) before W2(A): conflict, edge T1→T2. R2(B) before W1(B): conflict, edge T2→T1. The precedence graph now has edges T1→T2 AND T2→T1 — a two-node CYCLE — so this schedule is definitely NOT conflict serializable; no valid serial order can reproduce its effect, since T1 must genuinely precede T2 for item A's sake, while T2 must ALSO genuinely precede T1 for item B's sake, an unresolvable contradiction.

3. THREE-TRANSACTION CYCLE DETECTION. A schedule involves T1, T2, T3 with conflicts producing edges T1→T2, T2→T3, and T3→T1. Determine serializability and explain. This is a THREE-node cycle (T1→T2→T3→T1), so the schedule is NOT conflict serializable — exactly analogous to WP2's two-node cycle, just involving one additional transaction; the general rule (acyclic precedence graph if and only if conflict serializable) applies identically regardless of how many transactions or edges are involved, which is precisely why the MECHANICAL graph-cycle test scales cleanly to any number of transactions, unlike attempting to reason about serializability by inspection alone.

RECOVERABILITY: RECOVERABLE, CASCADELESS, AND STRICT SCHEDULES

1. A schedule is RECOVERABLE if, whenever a transaction Tj reads a data item PREVIOUSLY written by Ti, Tj does NOT commit until Ti has ALSO committed. Recoverability is essential because if Ti were later to ABORT after Tj has ALREADY committed, Tj's own commit would be based on a value that NO LONGER exists in the database, an inconsistency that CANNOT be undone once Tj has committed (since durability forbids un-committing a committed transaction).
2. A schedule AVOIDS CASCADING ROLLBACKS (is CASCADELESS) if every transaction reads ONLY values written by ALREADY-COMMITTED transactions — this is STRICTLY stronger than plain recoverability, since it prevents the situation where ABORTING one transaction forces a WHOLE CHAIN of other transactions that read its (now-invalid) uncommitted values to ALSO be aborted, a potentially expensive, CASCADING rollback.
3. A schedule is STRICT if a transaction can neither READ nor WRITE a data item until the transaction that PREVIOUSLY wrote it has EITHER committed OR aborted — strict schedules are the STRONGEST of these three categories, and are ADDITIONALLY trivial to UNDO on abort (simply restore the OLD value directly, since NO other transaction could possibly have read or overwritten the aborting transaction's own uncommitted write in the meantime).
4. THE containment relationship, heavily tested: STRICT ⊊ CASCADELESS ⊊ RECOVERABLE ⊊ (all schedules) — every strict schedule is cascadeless, every cascadeless schedule is recoverable, but NOT conversely at either step; a schedule that is NEITHER recoverable NOR strict NOR cascadeless is fundamentally UNSAFE to run, since an abort can propagate uncontrollably or leave the database in a state from which correct recovery is IMPOSSIBLE.

GATE TRAP: confusing RECOVERABILITY (a property about COMMIT ordering — Tj must not commit before Ti commits, if Tj read Ti's write) with CASCADELESSNESS (a property about READ ordering — Tj must not even READ Ti's uncommitted write in the first place) is an extremely common source of error; a schedule can be recoverable WITHOUT being cascadeless (Tj reads Ti's uncommitted write, but the SCHEDULE happens to commit Ti before Tj commits), so always check the SPECIFIC condition a given question is actually asking about, rather than treating the three categories as interchangeable synonyms for "safe."

LOCK-BASED CONCURRENCY CONTROL: TWO-PHASE LOCKING

[[FIG:two-phase-locking]]

1. A LOCK is a mechanism a transaction ACQUIRES on a data item BEFORE accessing it, preventing OTHER transactions from making CONFLICTING accesses to the SAME item simultaneously. A SHARED (S) lock permits READING only, and MULTIPLE transactions may hold a shared lock on the SAME item SIMULTANEOUSLY; an EXCLUSIVE (X) lock permits both reading AND writing, and is held by AT MOST one transaction at a time, with NO other transaction (shared or exclusive) permitted concurrently on that item.
2. TWO-PHASE LOCKING (2PL) requires every transaction to acquire ALL the locks it will ever need BEFORE releasing ANY lock — formally, each transaction has a GROWING phase (locks are ONLY acquired, never released) FOLLOWED by a SHRINKING phase (locks are ONLY released, never acquired), with the transition point between the two phases called the transaction's LOCK POINT.
3. THE fundamental theorem of 2PL: EVERY schedule produced by transactions strictly following the two-phase locking protocol is GUARANTEED to be CONFLICT SERIALIZABLE — this is the SINGLE most important result in this entire concurrency-control section, since it means a database system can enforce CORRECTNESS purely through this LOCAL, per-transaction locking discipline, without ever needing to construct a GLOBAL precedence graph at runtime.
4. BASIC 2PL, however, does NOT by itself prevent CASCADING ROLLBACKS, since a transaction MAY release a lock (entering its shrinking phase) and then LATER abort, after some OTHER transaction has ALREADY read the value it wrote while still holding that lock — this genuine weakness is exactly what the STRICT and RIGOROUS variants below are designed to fix.

STRICT AND RIGOROUS TWO-PHASE LOCKING

1. STRICT 2PL additionally requires that ALL EXCLUSIVE locks be held UNTIL the transaction COMMITS or ABORTS (rather than being releasable earlier, during an ordinary shrinking phase) — this guarantees the schedule is CASCADELESS (and, in fact, STRICT, in the recoverability sense defined above), since no other transaction can ever read a value written under an exclusive lock until the writer has definitively finished.
2. RIGOROUS 2PL goes ONE step further, requiring BOTH shared AND exclusive locks to be held until commit or abort — this is the SIMPLEST variant to REASON about and IMPLEMENT correctly, and is the variant MOST WIDELY used in actual commercial database systems, at a MODEST cost in concurrency (transactions hold their locks for the LONGEST possible duration, somewhat reducing the opportunity for other transactions to proceed concurrently).
3. THE practical trade-off, heavily tested: basic 2PL gives the WEAKEST guarantee (serializability alone, without cascadelessness) but the HIGHEST concurrency (locks released as EARLY as the shrinking phase permits); strict 2PL adds cascadelessness at a MODEST concurrency cost (only exclusive locks held longer); rigorous 2PL adds the SIMPLEST implementation and the STRONGEST safety guarantee, at the HIGHEST concurrency cost (both lock kinds held to the very end).

DEADLOCKS: DETECTION AND PREVENTION

1. A DEADLOCK occurs when TWO or more transactions are EACH waiting for a lock currently held by ANOTHER transaction in the SAME cycle, such that NONE can EVER proceed — this is the SAME fundamental circular-wait phenomenon this book's own earlier Operating Systems Deadlock chapter examined for GENERAL resource allocation, now specialized to the SPECIFIC case of database locks.
2. DEADLOCK DETECTION uses a WAIT-FOR GRAPH: a node per transaction, and a DIRECTED edge Ti→Tj whenever Ti is BLOCKED, waiting for a lock currently held by Tj. A CYCLE in this graph indicates a deadlock — EXACTLY analogous to the RESOURCE-ALLOCATION-GRAPH cycle-detection technique from the Operating Systems Deadlock chapter, but simplified here since EACH data item typically has only ONE holder at a time under exclusive locking (or several under shared locking, requiring an edge to EACH current holder).
3. WHEN a deadlock is detected, the system must select a VICTIM transaction to ABORT (releasing its locks, thereby breaking the cycle) — the choice of victim TYPICALLY favors aborting the transaction that has done the LEAST work so far (minimizing wasted computation), though a badly designed victim-selection policy risks STARVATION (repeatedly picking the SAME unlucky transaction as victim every time it retries).
4. DEADLOCK PREVENTION, an ALTERNATIVE to detection-and-recovery, avoids deadlocks ENTIRELY by imposing an ORDERING on lock requests — for instance, the WAIT-DIE and WOUND-WAIT schemes both assign each transaction a UNIQUE TIMESTAMP (older transactions have SMALLER timestamps) and use RELATIVE age to decide whether a REQUESTING transaction should WAIT or be forced to ABORT (or, symmetrically, force the HOLDER to abort) whenever a conflict arises, GUARANTEEING that no cycle of mutual waiting can ever form, since the timestamp ordering itself prevents any circular dependency.
5. WAIT-DIE (a NON-PREEMPTIVE scheme): if the REQUESTING transaction Ti is OLDER than the HOLDING transaction Tj, Ti is allowed to WAIT; if Ti is YOUNGER, Ti must DIE (abort) rather than wait — older transactions are NEVER forced to abort by a younger one, they simply WAIT it out.
6. WOUND-WAIT (a PREEMPTIVE scheme, the MIRROR IMAGE of wait-die): if the REQUESTING transaction Ti is OLDER than the holder Tj, Ti WOUNDS (forces the abort of) Tj and takes the lock; if Ti is YOUNGER, Ti simply WAITS — here, an OLDER transaction can preemptively abort a younger one, but a younger transaction never forces an older one to abort.

GATE TRAP: mixing up WAIT-DIE and WOUND-WAIT is extremely common, since both use the SAME relative-age comparison but assign the OPPOSITE outcome to the identical scenario — the reliable way to remember the distinction is that WAIT-DIE lets the OLDER transaction merely wait (never forcing anyone to abort), while WOUND-WAIT lets the OLDER transaction actively wound (abort) the younger holder; in BOTH schemes, it is always the YOUNGER of the two transactions that ends up aborting when an abort actually occurs, but WHICH of the two transactions is the one CAUSING that abort to happen differs between the two schemes.

TIMESTAMP-ORDERING PROTOCOL

1. EVERY transaction Ti is assigned a UNIQUE TIMESTAMP TS(Ti) at the moment it BEGINS, with EARLIER-starting transactions receiving SMALLER timestamps — the protocol's entire goal is to produce a schedule EQUIVALENT to the SERIAL order given by these timestamps, WITHOUT using locks at all.
2. EACH data item Q maintains TWO values: W-TIMESTAMP(Q), the LARGEST timestamp of any transaction that SUCCESSFULLY wrote Q, and R-TIMESTAMP(Q), the LARGEST timestamp of any transaction that SUCCESSFULLY read Q.
3. READ RULE: if TS(Ti) < W-TIMESTAMP(Q), Ti is attempting to read a value that was ALREADY overwritten by a LATER transaction — Ti is REJECTED (rolled back and restarted with a NEW, later timestamp); otherwise, the read PROCEEDS, and R-TIMESTAMP(Q) is updated to max(R-TIMESTAMP(Q), TS(Ti)).
4. WRITE RULE: if TS(Ti) < R-TIMESTAMP(Q) OR TS(Ti) < W-TIMESTAMP(Q), Ti is attempting to write a value that is ALREADY "too late" relative to some transaction that has already read OR written a more current value — Ti is REJECTED; otherwise, the write PROCEEDS and W-TIMESTAMP(Q) is updated to TS(Ti).
5. THE timestamp-ordering protocol GUARANTEES conflict serializability by CONSTRUCTION (every conflicting pair of operations is FORCED into timestamp order or one side is rejected), and is DEADLOCK-FREE (unlike lock-based schemes), since a transaction is NEVER made to WAIT — it is EITHER allowed to proceed immediately, or IMMEDIATELY rejected and restarted; the trade-off is a POTENTIALLY higher rate of ABORTS (restarts) under heavy contention, compared to a lock-based scheme that simply makes a transaction WAIT rather than restarting it outright.

THOMAS WRITE RULE

1. THE THOMAS WRITE RULE is a RELAXATION of the basic timestamp-ordering WRITE rule specifically: if TS(Ti) < W-TIMESTAMP(Q) (an OUTDATED write), rather than rejecting Ti outright, the write is simply IGNORED (skipped) rather than applied — since a LATER transaction has already written a MORE CURRENT value, Ti's own outdated write would IMMEDIATELY be overwritten anyway, so applying it serves NO purpose and can be safely SKIPPED without rejecting Ti's entire transaction. The READ rule, and the R-TIMESTAMP-based rejection condition within the write rule, remain UNCHANGED.
2. BY allowing this specific class of "doomed to be overwritten anyway" writes to be SILENTLY ignored rather than causing a full rejection, the Thomas write rule REDUCES the number of unnecessary transaction restarts, IMPROVING throughput relative to basic timestamp ordering.
3. IN PRACTICE, the Thomas write rule is applied only to the WRITE side of the protocol, since silently dropping a doomed write carries no observable risk once a strictly later write has already been recorded, whereas silently dropping a READ could hide genuinely needed information from the requesting transaction. THE Thomas write rule is the CLASSIC example of a protocol that is NOT conflict serializable (it can produce schedules that VIOLATE the strict conflict-based ordering, since an outdated write is simply DROPPED rather than reordered) but is STILL genuinely CORRECT — specifically, it produces schedules that are VIEW SERIALIZABLE, directly connecting BACK to this chapter's own earlier VIEW SERIALIZABILITY discussion, and providing a CONCRETE, practically-motivated example of exactly the "view serializable but not conflict serializable" gap that section described only abstractly.

VALIDATION (OPTIMISTIC) CONCURRENCY CONTROL

1. VALIDATION-BASED (OPTIMISTIC) concurrency control assumes CONFLICTS are RARE, and so avoids the OVERHEAD of locking or timestamp bookkeeping DURING normal execution, instead CHECKING for conflicts only at the very END, before allowing a transaction to commit.
2. EACH transaction passes through THREE phases: a READ phase (the transaction reads data and computes its OWN local, tentative results, WITHOUT yet making any changes visible to others), a VALIDATION phase (the system checks whether committing this transaction would VIOLATE serializability with respect to OTHER, concurrently validated transactions), and a WRITE phase (only if validation SUCCEEDS, the transaction's writes are actually applied to the database).
3. VALIDATION succeeds for a transaction Ti (assigned validation timestamp TS(Ti)) against an EARLIER-validated Tj (TS(Tj) < TS(Ti)) if ONE of: Tj completes its ENTIRE write phase before Ti even BEGINS its read phase (no overlap at all); OR Ti's read set does NOT intersect Tj's write set, AND Tj finishes writing before Ti begins WRITING (partial overlap, but no actual conflicting access); OR Ti's read set does not intersect Tj's write set AND Tj's read phase finishes before Ti's read phase finishes (an even looser, but still safe, timing condition) — in essence, validation is checking that NO genuine read-write conflict could have actually occurred given the transactions' own relative timing.
4. OPTIMISTIC concurrency control performs BEST when conflicts are genuinely rare (workloads dominated by READS, with few overlapping writes), since transactions almost NEVER need to be rolled back at validation time; it performs POORLY under HIGH-contention, write-heavy workloads, where FREQUENT validation failures force EXPENSIVE restarts AFTER a transaction has ALREADY done its full read-phase work, WASTING more computation than a pessimistic (lock-based) scheme that would have simply made the transaction WAIT instead.

KEY: lock-based 2PL, timestamp ordering, and validation-based control represent THREE fundamentally different PHILOSOPHIES toward the SAME serializability goal — 2PL is PESSIMISTIC and makes transactions WAIT to prevent conflicts before they occur; timestamp ordering is ALSO fundamentally pessimistic but avoids waiting by immediately REJECTING out-of-order operations; validation is genuinely OPTIMISTIC, allowing full, unimpeded execution and checking for conflicts only at the very end — recognizing which philosophy a given protocol follows immediately predicts its performance characteristics under low versus high contention, without needing to memorize each protocol's mechanics from scratch every time.

MULTIPLE GRANULARITY LOCKING

1. MULTIPLE GRANULARITY locking allows transactions to lock data items at DIFFERENT levels of a HIERARCHY (typically: database → table → block/page → individual record/tuple), letting a transaction needing to access MANY records choose a SINGLE, COARSE-grained lock (such as locking an entire table) rather than acquiring THOUSANDS of individual fine-grained record locks — trading some CONCURRENCY (a table-level lock blocks EVERYONE else from that whole table) for dramatically REDUCED locking OVERHEAD.
2. INTENTION LOCKS (IS — intention-shared, IX — intention-exclusive, SIX — shared and intention-exclusive) are placed on ANCESTOR nodes in the hierarchy BEFORE a transaction locks a DESCENDANT node in shared or exclusive mode — this lets OTHER transactions QUICKLY detect, just by checking the ANCESTOR's own intention lock, whether SOME descendant is already locked, WITHOUT needing to individually inspect every descendant node.
3. THE multiple-granularity locking protocol requires: intention locks be acquired on the PATH from the root DOWN to the node actually being locked, in TOP-DOWN order; and locks be RELEASED in BOTTOM-UP order (the reverse) — this ordering GUARANTEES the SAME two-phase-locking correctness guarantee extends cleanly across the ENTIRE hierarchy, rather than only within a single, fixed granularity level.

LOG-BASED RECOVERY: UNDO, REDO, AND CHECKPOINTING

1. EVERY database modification is FIRST recorded in a LOG (a sequential, append-only file on STABLE storage) BEFORE the actual data page itself is modified — this is the WRITE-AHEAD LOGGING (WAL) rule, and it is THE single foundational principle underlying essentially ALL modern crash-recovery mechanisms.
2. A log record for an update typically contains: the transaction identifier, the data item modified, its OLD value (the "before-image," needed for UNDO), and its NEW value (the "after-image," needed for REDO) — plus special records marking a transaction's <Ti, start>, <Ti, commit>, or <Ti, abort>.
3. UNDO reverses the effects of a transaction that did NOT successfully commit (using the log's OLD-value before-images, applied in REVERSE chronological order); REDO re-applies the effects of a transaction that DID commit but whose changes may not YET have been physically written to disk at the moment of a crash (using the log's NEW-value after-images, applied in FORWARD chronological order).
4. AFTER a crash, RECOVERY scans the log and classifies every transaction as either: UNDO (it has a <Ti, start> record but NO matching <Ti, commit>) — its effects must be rolled back; or REDO (it has BOTH a <Ti, start> AND a <Ti, commit> record) — its effects must be reapplied to guarantee durability, in case the actual data pages were not yet flushed to disk before the crash occurred.
5. CHECKPOINTING periodically records a SNAPSHOT marker in the log, noting which transactions were ACTIVE at that moment, and GUARANTEEING all log records and data pages BEFORE the checkpoint have been safely flushed to stable storage — this lets RECOVERY skip scanning the log all the way back to the very BEGINNING, starting instead from the MOST RECENT checkpoint, dramatically REDUCING recovery time after a crash, at a small ONGOING cost during normal operation of periodically pausing to record the checkpoint itself.

GATE TRAP: assuming UNDO and REDO are simply "opposite directions of the same operation" and can be applied in EITHER chronological order is incorrect — UNDO must proceed in REVERSE chronological order (undoing the MOST RECENT change first, working backward, since later changes may depend on earlier ones being present when they were made), while REDO must proceed in FORWARD chronological order (reapplying changes in the SAME order they originally occurred, since a later change may depend on an earlier one having ALREADY been reapplied) — applying either in the wrong direction can produce a genuinely INCORRECT final database state.

ISOLATION LEVELS AND THE CONCURRENCY ANOMALIES THEY PERMIT

1. THE SQL standard defines FOUR isolation levels, each permitting PROGRESSIVELY fewer concurrency anomalies at the cost of PROGRESSIVELY less concurrency: READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, and SERIALIZABLE.
2. A DIRTY READ occurs when a transaction reads a value written by ANOTHER, still-UNCOMMITTED transaction — if that writer later ABORTS, the reader has observed a value that NEVER genuinely existed in the database. READ UNCOMMITTED permits dirty reads; ALL three higher levels forbid them.
3. A NON-REPEATABLE (fuzzy) READ occurs when a transaction reads the SAME data item TWICE and gets DIFFERENT values, because some OTHER, concurrently COMMITTED transaction modified that item in between the two reads. READ COMMITTED still permits non-repeatable reads (it only forbids reading UNCOMMITTED data, not a later committed CHANGE to the same item); REPEATABLE READ and SERIALIZABLE both forbid it.
4. A PHANTOM READ occurs when a transaction re-executes a QUERY (rather than re-reading a single, specific item) and finds NEW rows that satisfy the query's condition, because ANOTHER transaction INSERTED a qualifying row in between the two executions — REPEATABLE READ (which locks or version-tracks only the SPECIFIC rows already read) still permits phantoms; ONLY the strictest level, SERIALIZABLE, forbids them, typically by locking the entire RANGE or predicate a query scans over (predicate locking), not merely the individual rows it happened to find.
5. THE complete permitted-anomaly table, heavily tested: READ UNCOMMITTED permits dirty reads, non-repeatable reads, AND phantoms; READ COMMITTED permits non-repeatable reads and phantoms, but NOT dirty reads; REPEATABLE READ permits ONLY phantoms; SERIALIZABLE permits NONE of the three. Each level going UP this list forbids exactly one ADDITIONAL anomaly category that the level below it still allowed, giving a clean, strictly ordered permission hierarchy.

GATE TRAP: assuming REPEATABLE READ is the SAME as SERIALIZABLE is a common and costly confusion — REPEATABLE READ genuinely guarantees that RE-READING an ALREADY-read row gives the SAME value, but it does NOT guarantee that RE-RUNNING an entire QUERY returns the SAME SET of rows, since brand-new rows (phantoms) inserted by other transactions can still appear; only the SERIALIZABLE level closes this final, query-level gap by locking against the underlying predicate range itself, not merely the individual rows already materialized.

SHADOW-PAGING: AN ALTERNATIVE TO LOG-BASED RECOVERY

1. SHADOW PAGING is an ALTERNATIVE recovery technique that avoids maintaining a separate log entirely, instead relying on a PAGE TABLE indirection: a transaction is given its OWN private COPY of the current page table (the "shadow" of the real, CURRENT page table) at the moment it begins.
2. WHENEVER the transaction modifies a page, it writes the CHANGE to a BRAND-NEW physical disk block, and updates ONLY its OWN, private (shadow) page table entry to point to this new block — the ORIGINAL page, and the database's genuinely CURRENT page table, remain COMPLETELY untouched throughout the transaction's execution.
3. ON COMMIT, the CURRENT page table is atomically REPLACED by the transaction's own shadow page table (a single, indivisible pointer-swap operation) — making ALL of the transaction's changes visible AT ONCE; ON ABORT, the shadow page table is simply DISCARDED, and the ORIGINAL current page table (never having been touched) remains exactly as it was, requiring NO undo processing at all.
4. THE key comparative trade-off, heavily tested: shadow paging needs NO undo/redo log processing at all (making both commit and abort structurally simple and fast), but suffers from DATA FRAGMENTATION (modified pages are scattered to new disk locations rather than updated in place, harming the CONTIGUOUS locality that sequential file organizations from the earlier Indexing chapter rely upon) and makes COMMITTING MULTIPLE, concurrent transactions genuinely awkward (since each holds its OWN full shadow copy of the page table, and garbage-collecting OLD, now-unreachable pages after a commit adds its own overhead) — which is PRECISELY why write-ahead logging, not shadow paging, is the technique OVERWHELMINGLY used in real, modern database systems, with shadow paging remaining primarily of THEORETICAL and historical interest for GATE purposes.

KEY: shadow paging's own commit-by-atomic-pointer-swap technique is structurally the SAME idea as the earlier Compiler Design Runtime chapter's own discussion of value-versus-reference semantics achieving atomicity through INDIRECTION rather than through in-place mutation — in BOTH cases, making an entire, potentially large set of changes appear to happen "all at once" is achieved not by touching every affected piece of data directly, but by redirecting a SINGLE pointer (the page table, or a reference) to an entirely new, already-fully-prepared structure.

WORKED PROBLEMS, PART TWO

4. WAIT-DIE VERSUS WOUND-WAIT DECISION TRACE. Transaction T1 has timestamp 5 (older), and T2 has timestamp 10 (younger). T2 currently holds a lock that T1 now requests. Determine the outcome under BOTH wait-die and wound-wait. Under WAIT-DIE: T1 (the requester) is OLDER than T2 (the holder) — per the rule "if requester is older, it WAITS" — T1 simply WAITS for T2 to release the lock. Under WOUND-WAIT: T1 (the requester) is OLDER than T2 (the holder) — per the rule "if requester is older, it WOUNDS the holder" — T1 forces T2 to ABORT and immediately takes the lock. This directly illustrates that the SAME relative-age scenario produces genuinely DIFFERENT outcomes depending on which of the two schemes is in force.

5. THE REVERSE SCENARIO. Using the same T1 (timestamp 5) and T2 (timestamp 10), now suppose T2 (the YOUNGER transaction) requests a lock currently held by T1 (the OLDER transaction). Determine the outcome under both schemes. Under WAIT-DIE: T2 (requester) is YOUNGER than T1 (holder) — per the rule "if requester is younger, it DIES" — T2 is aborted and must restart with a fresh timestamp. Under WOUND-WAIT: T2 (requester) is YOUNGER than T1 (holder) — per the rule "if requester is younger, it WAITS" — T2 simply waits for T1 to finish. Combined with WP4, this confirms the general pattern: in wait-die, an older requester waits and a younger requester dies; in wound-wait, an older requester wounds the holder and a younger requester waits — the two schemes are precise mirror images of each other.

6. RECOVERABILITY CLASSIFICATION. A schedule has T2 reading a value written by T1, and T2 commits BEFORE T1 commits. Classify this schedule (recoverable? cascadeless? strict?) and explain the risk. This schedule is NOT recoverable, since T2 committed before T1 (the transaction whose write it read) committed — if T1 subsequently ABORTS, T2's already-committed result is now based on a value that no longer exists in the database, an inconsistency that cannot be fixed since T2's commit is durable and irreversible; this is precisely the unsafe scenario the RECOVERABILITY definition exists to rule out.

7. LOG-BASED UNDO/REDO CLASSIFICATION. The log at crash time shows: <T1,start>, <T1,commit>, <T2,start>, <T3,start>, <T3,commit>. Classify T1, T2, and T3 for recovery purposes. T1 has both a start and a commit record — classify as REDO (reapply its changes to guarantee durability). T2 has only a start record, no commit — classify as UNDO (roll back any partial changes). T3 has both a start and a commit record — classify as REDO, exactly like T1. The recovery process would therefore UNDO T2's effects (in reverse chronological order) and REDO T1's and T3's effects (in forward chronological order), exactly following the general rule stated in the LOG-BASED RECOVERY section above.

WORKED PROBLEMS, PART THREE: ISOLATION LEVELS AND A FULL SYNTHESIS

8. ISOLATION-LEVEL ANOMALY IDENTIFICATION. Transaction T1 reads a row with Salary=50000, then LATER (still within the same transaction) reads the SAME row again and now finds Salary=55000, because T2 committed an update in between. Identify the anomaly and the WEAKEST isolation level that would have PREVENTED it. This is a NON-REPEATABLE (fuzzy) read — the SAME row was read twice within one transaction and returned DIFFERENT values because another transaction's COMMITTED write intervened. READ COMMITTED does NOT prevent this (it only blocks uncommitted, dirty reads); REPEATABLE READ is the WEAKEST level that prevents it, since repeatable read specifically guarantees that once a row has been read, subsequent reads of that SAME row within the SAME transaction return the SAME value.

9. A SECOND ANOMALY IDENTIFICATION. Transaction T1 runs a query "SELECT * FROM Orders WHERE Amount > 1000" and gets 5 rows; later, still within the same transaction, T1 re-runs the IDENTICAL query and now gets 6 rows, because T2 committed a NEW row satisfying the condition in between. Identify the anomaly and the isolation level required to prevent it. This is a PHANTOM READ — a brand-new row satisfying the query's predicate appeared between two executions of the SAME query. REPEATABLE READ does NOT prevent this (it only guarantees stability for rows ALREADY read, not for the full result set of a re-executed query); only the SERIALIZABLE isolation level, which locks against the underlying predicate range itself, prevents phantom rows from appearing.

10. FULL PROTOCOL-SELECTION SYNTHESIS. A banking system processes a HIGH volume of small, mostly non-conflicting transfer transactions (most transfers touch entirely different account pairs), with an OCCASIONAL, low-frequency batch reconciliation job that reads a very large number of accounts at once. Recommend a concurrency-control approach for the transfer transactions, and separately for the reconciliation job, justifying each choice against the philosophies compared in this chapter's own KEY card above. For the frequent, mostly non-conflicting transfer transactions: VALIDATION-BASED (optimistic) concurrency control, or alternatively fine-grained STRICT 2PL at the individual-account (leaf) granularity, is well suited, since genuine conflicts (two transfers touching the exact same account pair concurrently) are RARE, matching the low-contention scenario where optimistic control performs best, or where fine-grained locking imposes minimal blocking. For the occasional, large-scale reconciliation job: a COARSE-GRAINED lock (using the MULTIPLE GRANULARITY locking scheme, taking a single table-level or database-level shared lock) is preferable to acquiring thousands of individual fine-grained locks one at a time, trading some concurrency during the (infrequent) reconciliation window for dramatically reduced locking overhead — directly applying the multiple-granularity locking trade-off from earlier in this chapter to a realistic, mixed workload rather than treating the two techniques as applicable only in isolation from each other.

11. RECOVERY-MECHANISM COMPARISON SYNTHESIS. A newly designed embedded database, expected to run on a SINGLE-user device with NO concurrent transactions ever, needs a recovery mechanism. Would shadow paging or write-ahead logging be the more reasonable choice here, and why does this differ from the recommendation for a general-purpose, multi-user server database? Since this embedded system has NO concurrent transactions at all, shadow paging's own PRIMARY weakness (the awkwardness of maintaining separate shadow page tables across MANY simultaneously active transactions) simply does not arise, while its own PRIMARY strength (no undo/redo log processing needed, structurally simple commit and abort) becomes fully usable without any offsetting cost — making shadow paging a genuinely REASONABLE choice in this SPECIFIC, single-user context, even though write-ahead logging remains the overwhelmingly preferred choice for the general-purpose, highly concurrent server databases this chapter's own SHADOW-PAGING section describes as the dominant real-world case; this worked problem illustrates that even a technique flagged as "primarily of theoretical interest" for the general case can still be the objectively correct engineering choice once the workload's own actual constraints are taken into account.

CARRYING THIS FORWARD

This chapter, and this book's entire Databases sequence with it, has now traced a COMPLETE arc: conceptual design (the ER-Model chapter), the RELATIONAL ALGEBRA and SQL query languages built on top of that design, NORMALIZATION eliminating redundancy from the resulting tables, INDEXING making those tables efficiently searchable, and finally this chapter's own TRANSACTIONS material keeping everything SAFE and CONSISTENT under concurrent access and unpredictable failure. The single most valuable connecting insight to carry forward from THIS chapter specifically is that every mechanism examined here — 2PL, timestamp ordering, validation, write-ahead logging — exists to guarantee EXACTLY one or more of the four ACID properties stated at this chapter's very start, and recognizing WHICH specific property a given mechanism protects (isolation, for the concurrency-control protocols; durability and atomicity, for the log-based recovery mechanisms) is far more useful, for both GATE and genuine understanding, than memorizing each protocol's mechanics as an isolated, unconnected fact.

KEY: two-phase locking's own growing-and-shrinking-phase structure is, at its structural core, enforcing the SAME kind of "commit to a decision before observing further information, then only retreat, never advance again" discipline this book has encountered in other guises — most directly, the earlier Compiler Design Runtime Environments chapter's own activation-record stack discipline (a frame is fully built during a call's "growing" phase before any of its locals are used, and only torn down, never rebuilt, once the call returns) — different domains, but the SAME underlying "one-way phase transition" structural principle providing the correctness guarantee in both cases.

KEY: the four-way relationship between STRICT ⊊ CASCADELESS ⊊ RECOVERABLE schedules and the equally strict-containment BASIC 2PL vs STRICT 2PL vs RIGOROUS 2PL locking-protocol hierarchy is not a coincidence — strict 2PL was SPECIFICALLY designed to guarantee the "strict" schedule property, and rigorous 2PL guarantees it just as strongly while additionally simplifying implementation; recognizing that EACH concurrency-control protocol variant maps DIRECTLY onto a specific point in the schedule-classification hierarchy turns what might otherwise seem like a large, disconnected list of named protocols into a single, systematically organized picture.

KEY: the isolation-level hierarchy (READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, SERIALIZABLE) and the recoverability hierarchy (RECOVERABLE, CASCADELESS, STRICT) both describe STRICTLY ordered families of PROGRESSIVELY stronger guarantees, but they answer DIFFERENT questions — isolation levels describe WHAT a single transaction is permitted to OBSERVE about concurrently executing transactions, while recoverability describes WHEN a transaction is permitted to COMMIT relative to the transactions whose writes it has read; treating these as the SAME hierarchy, rather than as two SEPARATE but similarly-shaped hierarchies addressing different concerns, is a subtle but genuine conceptual error worth guarding against.

Before closing, it is worth explicitly connecting this chapter's OWN two major halves — concurrency control and recovery — back to the FOUR ACID properties stated at the very beginning. CONCURRENCY CONTROL (two-phase locking, timestamp ordering, validation, and multiple-granularity locking) exists ENTIRELY to guarantee ISOLATION: it ensures that whatever INTERLEAVING of operations the system actually chooses to execute for performance reasons produces a RESULT indistinguishable from SOME serial execution, so that no transaction ever observes another's incomplete, in-progress work in a way that could corrupt its own logic. RECOVERY (write-ahead logging, undo/redo processing, checkpointing, and the shadow-paging alternative) exists to guarantee ATOMICITY and DURABILITY together: atomicity, because a crash mid-transaction must leave NO partial trace behind once undo processing completes; durability, because a crash immediately after a commit must NOT lose that transaction's effects once redo processing completes. CONSISTENCY, the remaining ACID property, is guaranteed neither by concurrency control nor by recovery directly, but by the INTEGRITY CONSTRAINTS this book's own earlier ER-Model and Relational-Algebra-and-SQL chapters already established — primary keys, foreign keys, and check constraints — which every individual transaction is separately responsible for respecting on its own, with concurrency control and recovery merely ensuring that this per-transaction consistency guarantee is not ACCIDENTALLY broken by the mechanics of concurrent execution or by an ill-timed failure.

This division of labor across the four ACID properties — one pair of mechanisms (concurrency control) responsible for isolation, a second pair of mechanisms (recovery) responsible jointly for atomicity and durability, and consistency left to the application-level integrity constraints defined much earlier in this book's own Databases sequence — is itself the single most useful summary fact to retain from this entire chapter, since almost any exam question about "which mechanism guarantees which ACID property" can be answered directly from this three-way division, rather than by attempting to recall each individual protocol's mechanics from first principles under time pressure.

The completion of this chapter brings the Databases subject to its full FIVE-chapter conclusion — ER-Model, Relational Algebra and SQL, Normalization, Indexing, and now Transactions — mirroring the SAME systematic, foundations-to-implementation-to-safety progression this book's earlier Computer Organization, Theory of Computation, and Compiler Design sequences each followed in their own respective subjects, reflecting the deliberate, book-wide organizing principle that every subject in this book proceeds from first principles through to the concrete, examinable mechanics a GATE candidate must be able to trace by hand, one numbered step and one worked example at a time, rather than as an abstract collection of definitions to be memorized independently of any concrete procedure for applying them under actual exam conditions.

Carrying this progression forward, a candidate who has worked through all five Databases chapters in sequence should now be able to take a single, moderately complex application scenario — a university enrollment system, say, or a banking ledger — and walk it through the ENTIRE pipeline this book has built: sketch its entities and relationships, translate that design into normalized relational tables, choose appropriate indexes for its expected query patterns, and finally reason correctly about which concurrency-control and recovery mechanisms that system would need to remain safe under real, concurrent, occasionally failure-prone operation. That END-TO-END fluency, rather than the ability to recite any single chapter's definitions in isolation, is the genuine mark of having mastered this entire five-chapter Databases sequence, and is precisely the kind of integrated, multi-chapter reasoning that the hardest GATE Databases questions are specifically designed to probe.
`
};
