// Textbook chapter: Runtime Environments.
// Written directly (no subagent) to match the depth and voice of the other
// chapters in data/chapters/.

window.GATE_DATA = window.GATE_DATA || {};
window.GATE_DATA.chapters = window.GATE_DATA.chapters || {};
window.GATE_DATA.chapters['compiler-runtime'] = {
  figs: [
    {
      id: 'activation-record-layout',
      caption: 'A standard activation-record layout, high memory at top: actual parameters and return address set up by the caller, then the control link, access link, saved status, locals, and temporaries set up by the prologue.',
      svg: '<svg viewBox="0 0 260 200" width="100%" style="max-width:260px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1" fill="none"><rect x="20" y="10" width="220" height="24"/><rect x="20" y="34" width="220" height="24"/><rect x="20" y="58" width="220" height="24"/><rect x="20" y="82" width="220" height="24"/><rect x="20" y="106" width="220" height="24"/><rect x="20" y="130" width="220" height="24"/><rect x="20" y="154" width="220" height="24"/></g><g font-size="8" fill="currentColor" text-anchor="middle"><text x="130" y="26">Actual parameters</text><text x="130" y="50">Return address</text><text x="130" y="74">Control (dynamic) link</text><text x="130" y="98">Access (static) link</text><text x="130" y="122">Saved machine status</text><text x="130" y="146">Local data</text><text x="130" y="170">Temporaries</text></g></svg>'
    },
    {
      id: 'static-vs-dynamic-link',
      caption: 'The static (access) link follows lexical nesting to resolve non-local names by source-code structure; the dynamic (control) link follows the actual call chain to know where to restore the calling frame. The two can point to entirely different frames.',
      svg: '<svg viewBox="0 0 280 90" width="100%" style="max-width:280px;height:auto" xmlns="http://www.w3.org/2000/svg"><g stroke="currentColor" stroke-width="1.1" fill="none"><rect x="10" y="30" width="70" height="30"/><rect x="105" y="30" width="70" height="30"/><rect x="200" y="30" width="70" height="30"/></g><g font-size="8" fill="currentColor" text-anchor="middle"><text x="45" y="49">Frame A</text><text x="140" y="49">Frame B</text><text x="235" y="49">Frame C</text><text x="140" y="15" font-size="7">dynamic link: actual call order</text><text x="140" y="80" font-size="7">static link: lexical nesting order</text></g></svg>'
    }
  ],
  text: `
WHAT A RUNTIME ENVIRONMENT ACTUALLY PROVIDES

A compiler's job does not end once correct intermediate code has been generated — that code must ALSO be given a concrete, WORKING mapping from every source-level NAME to an actual memory LOCATION, and a concrete CONVENTION for how procedures CALL one another and correctly RETURN, including how PARAMETERS and RETURN VALUES actually move between caller and callee. This chapter's own subject, the RUNTIME ENVIRONMENT, is EXACTLY this storage-allocation strategy plus the CALLING-SEQUENCE code (the PROLOGUE executed on entering a procedure, and the EPILOGUE executed on leaving it) that the code generator emits at every single procedure call and return.

1. THIS CHAPTER CONSUMES: the STRUCTURE of the program — its procedure and function NESTING, its DECLARATIONS, and its CALL relationships — exactly as recorded by the semantic-analysis phase and the symbol table this book's own earlier Syntax-Directed Translation chapter developed in full.
2. THIS CHAPTER PRODUCES: a mapping from NAMES to runtime memory LOCATIONS, plus the actual calling-sequence CODE (prologue and epilogue) a code generator emits at every call site and every procedure's own entry and exit points.

THE THREE STORAGE CLASSES

1. STATIC ALLOCATION: a FIXED memory address, assigned ONCE, for the ENTIRE lifetime of the running program — used for GLOBAL variables and STATIC local variables (variables declared inside a procedure but whose value must PERSIST across separate calls to that procedure). CRITICALLY, static allocation provides NO support whatsoever for RECURSION — since only ONE single instance of the variable's storage exists at all, a recursive call would immediately OVERWRITE the SAME storage location an outer, still-active call is still relying on.
2. STACK ALLOCATION: ONE fresh ACTIVATION RECORD is pushed for EACH active procedure call, and popped automatically the moment that call RETURNS — this directly SUPPORTS recursion, since EACH separate, simultaneously-active call to the SAME procedure gets its OWN entirely separate storage for its own local variables, with NO interference between them whatsoever.
3. HEAP ALLOCATION: used specifically for data whose LIFETIME is NOT nested with any particular procedure CALL at all (a dynamically allocated object that must persist and remain accessible even AFTER the procedure that created it has already returned) — heap storage is reclaimed EITHER via automatic GARBAGE COLLECTION, or via EXPLICIT programmer-issued deallocation (an explicit free or delete call), depending entirely on the specific source language's own design.

GATE TRAP: assuming ALL heap-allocated data automatically REQUIRES garbage collection is a category confusion worth avoiding explicitly — SOME languages (C, for instance) use PURELY explicit alloc/free operations on the heap, with NO automatic garbage collector involved at all; the GENUINELY important distinction this chapter's own three-way storage-class framework tests is recognising WHICH of the three storage classes (static, stack, or heap) correctly fits a GIVEN variable's own required LIFETIME, rather than which specific reclamation MECHANISM that storage class happens to use in any one particular language.

THE ACTIVATION RECORD: A COMPLETE LAYOUT

[[FIG:activation-record-layout]]

1. THE STANDARD LAYOUT, from HIGH memory down to LOW memory (with the stack conventionally GROWING toward lower addresses): ACTUAL PARAMETERS (the arguments PUSHED by the CALLER, immediately before the call instruction itself); RETURN ADDRESS (recording exactly where control should RESUME, back in the caller, once the callee finishes); CONTROL LINK (also called the DYNAMIC LINK — a pointer directly to the CALLER's OWN activation record, needed to correctly restore the caller's frame once the callee returns); ACCESS LINK (also called the STATIC LINK — a pointer to the activation record of the LEXICALLY ENCLOSING procedure, used specifically for resolving NON-LOCAL variable access in languages permitting NESTED procedure declarations); SAVED MACHINE STATUS (saved register values, saved program-counter information, and any other state needed to correctly RESTORE the caller's own execution context); LOCAL DATA (this specific procedure's own local variables); and TEMPORARIES (compiler-generated temporary values needed only for the DURATION of this specific activation).
2. THE CURRENT activation's own record is addressed via a FRAME POINTER (FP) and STACK POINTER (SP) pair — the CALLER is typically responsible for setting up the actual parameters and the return address; the CALLEE's own PROLOGUE code then sets up the control link, the access link, saves whatever registers need preserving, and allocates space for its OWN locals and temporaries; the EPILOGUE reverses this ENTIRE sequence, in the opposite order, immediately before actually returning (restoring saved registers, popping the frame, and finally jumping back to the recorded return address).
3. THE STATIC (ACCESS) LINK is needed ONLY for languages that permit NESTED procedure declarations with NON-LOCAL lexical access (the classic Pascal-style nested-procedure design) — a language like PLAIN C, which does NOT permit nested procedure declarations at all, has NO NEED for this specific link, and simply OMITS it from its own activation-record layout entirely.

GATE TRAP: forgetting that the RETURN ADDRESS and the CONTROL LINK are, across different textbook conventions and different real architectures, sometimes set up by DIFFERENT parties (some conventions have the CALLER explicitly push the return address before transferring control; others have it pushed IMPLICITLY as a direct side effect of a dedicated "call" machine instruction) is a genuine source of confusion when a specific question's own stated convention differs from whatever convention one might have memorised by default — ALWAYS read the SPECIFIC convention a given question states explicitly, rather than assuming any single, universally fixed layout applies unconditionally to every activation-record question encountered.

STATIC VS DYNAMIC SCOPE RESOLUTION

[[FIG:static-vs-dynamic-link]]

1. STATIC (LEXICAL) SCOPING: a name refers to the declaration that is TEXTUALLY, LEXICALLY closest ENCLOSING it in the actual SOURCE CODE — determined ENTIRELY at COMPILE time, by walking the chain of enclosing lexical blocks and procedures exactly as they are WRITTEN in the source, completely REGARDLESS of the actual runtime CALL sequence. Implemented, at runtime, via the ACCESS/STATIC link CHAIN developed above: to find a non-local variable declared n LEXICAL levels up from the current procedure, follow the static link EXACTLY n times, starting from the current activation record.
2. DYNAMIC SCOPING: a name instead refers to the declaration in the MOST RECENTLY ACTIVATED, STILL-ACTIVE procedure that declares it — determined by the ACTUAL runtime CALL SEQUENCE, entirely INDEPENDENT of how the source text happens to be lexically nested. Implemented by SEARCHING the chain of activation records via the DYNAMIC (control) link instead of the static link, or, in some implementations, via a separate runtime "association list" (or a central table) explicitly PUSHED and POPPED as procedures are entered and exited.
3. THE CLASSIC DISTINGUISHING EXAMPLE: procedure a declares x; procedure b (LEXICALLY nested inside, or otherwise textually positioned within, a's own scope) refers to x WITHOUT declaring its own separate x; if some OTHER, entirely different procedure c ALSO declares its own x and happens to CALL b directly, STATIC scoping still resolves b's own reference to x as a's x (based PURELY on where b is textually WRITTEN in the source), while DYNAMIC scoping instead resolves it to c's x (based on WHO actually, dynamically, CALLED b) whenever c happens to be the ACTUAL, runtime caller instead of a.

1. Worked example (static vs dynamic scope trace, independently verified by tracing both rules explicitly): consider the program int x=1; proc a(){print(x);} proc b(){int x=2; a();} main(){b();}. Under STATIC scoping: a() is textually nested at the TOP level of the program, so its own reference to x resolves DIRECTLY to the GLOBAL x=1, completely REGARDLESS of who happens to call a() at runtime — OUTPUT: 1. Under DYNAMIC scoping: at the EXACT moment a() actually executes, the MOST RECENTLY activated declaration of x is b's OWN local x=2 (since b called a, and b's own activation, with its local x=2, is STILL on the runtime call stack at the moment a looks up x) — OUTPUT: 2. This SINGLE trace is the standard mechanism GATE uses to test whether a student can genuinely distinguish these two rules: the EXACT SAME program yields TWO DIFFERENT outputs, PURELY depending on which resolution rule is assumed, with the entire difference hinging on whether "enclosing" means LEXICAL nesting (static) or the LIVE call chain (dynamic).

GATE TRAP: believing dynamic scoping is a purely ACADEMIC curiosity, with no practical exam relevance, is a mistake worth correcting directly — GATE REGULARLY presents a SHORT trace program, structurally similar to this chapter's own worked example directly above, and asks for the resulting OUTPUT under BOTH static and dynamic scoping SEPARATELY, EXPECTING two genuinely DIFFERENT numeric answers as the correct, complete response.

PARAMETER PASSING SEMANTICS

1. CALL BY VALUE: a COPY of the actual parameter's own VALUE is passed — the caller's own variable is NEVER modified by anything the callee does to its own local copy; ALIASING is NOT possible under this mechanism.
2. CALL BY REFERENCE: the ADDRESS (l-value) of the actual parameter is passed directly — the callee's own WRITES directly modify the caller's OWN variable, since both are, in effect, the SAME underlying storage location; ALIASING IS possible whenever TWO OR MORE formal parameters end up bound to the SAME underlying address.
3. CALL BY COPY-RESTORE (also called COPY-IN COPY-OUT, or VALUE-RESULT): a COPY is made on ENTRY (exactly like call by value); that SAME copy is then explicitly WRITTEN BACK to the caller's own variable, but ONLY at the moment of RETURN, using whatever FINAL value the copy holds at that point.
4. CALL BY NAME: the UNEVALUATED actual-parameter EXPRESSION itself is passed, and RE-EVALUATED (effectively, TEXTUALLY substituted, in the classic Algol-60 style) at EVERY single USE of that formal parameter INSIDE the callee's own body — this can behave EXACTLY like reference passing in some cases, but can ALSO produce genuinely SURPRISING re-evaluation behavior at every use (the classic textbook illustration being "Jensen's device"), particularly whenever the actual-parameter expression itself has a SIDE EFFECT, or depends on an INDEX variable that CHANGES value across the callee's own multiple uses of that parameter.

GATE TRAP: treating call-by-name as simply EQUIVALENT to call-by-reference is a genuine and consequential error — call-by-name RE-EVALUATES the ACTUAL-parameter EXPRESSION at EVERY textual use inside the callee's own body, which can produce RESULTS genuinely DIFFERENT from ordinary reference passing whenever that actual-parameter expression is itself something MORE than a simple, unchanging variable reference (an expression carrying a side effect, or an array-index expression depending on a loop variable that the callee itself modifies during its own execution, being the two classic triggering cases).

1. Worked example (call by value vs reference vs copy-restore, with a genuine aliasing trap, fully traced and independently verified): global int a=2, b=3; procedure swap(x,y) { int t=x; x=y; y=t; }; call swap(a,b). CALL BY VALUE: x,y are PRIVATE copies of a,b — swapping x,y internally has NO effect on the caller at all; AFTER the call, a=2, b=3, entirely UNCHANGED. CALL BY REFERENCE: x,y are bound directly to the ADDRESSES of a,b — t=x reads a's own slot (2); x=y WRITES b's value (3) INTO a's own slot; y=t writes the ORIGINAL x-value (2) INTO b's own slot — AFTER the call, a=3, b=2 (a GENUINE, correct swap). CALL BY COPY-RESTORE: x,y START as copies of a(2) and b(3); the SAME internal swap happens purely on the COPIES (x becomes 3, y becomes 2); ON RETURN, x's own FINAL value (3) is written BACK to a, and y's own final value (2) is written back to b — AFTER the call, a=3, b=2, IDENTICAL to reference semantics HERE specifically, since a and b do NOT alias each other in this particular call.

2. Worked example (the classic ALIASING trap — swap(a,a), independently traced through both mechanisms): NOW call swap(a,a) instead — BOTH actual parameters are the SAME single variable. CALL BY REFERENCE: x AND y are BOTH bound to a's OWN single address — t=x reads a (2); x=y writes a's OWN current value BACK into a itself (a genuine no-op, still 2); y=t writes 2 back into a (ALSO a no-op) — a ENDS as 2, meaning the intended swap SILENTLY FAILS entirely, purely because of ALIASING. CALL BY COPY-RESTORE: x and y are INDEPENDENT copies (2 and 2 respectively); the internal swap gives x=3 (the OLD y-copy's value), y=2 (the OLD x-copy's value); on RETURN, the write-backs to a happen in SEQUENCE — so the FINAL value of a depends ENTIRELY on the language's own DEFINED write-back ORDER (whichever write-back happens LAST wins) — this ORDER-DEPENDENCY is the genuinely EXTRA subtlety copy-restore introduces BEYOND pure reference semantics, precisely BECAUSE of this aliasing situation.

GATE TRAP: assuming call-by-reference and call-by-copy-restore ALWAYS produce IDENTICAL results in every situation is incorrect — as this chapter's own two paired worked examples directly demonstrate, the two mechanisms DIVERGE PRECISELY when genuine ALIASING exists among the actual parameters (swap(a,a), specifically); WITHOUT such aliasing, they typically agree exactly, as this chapter's own first worked example (swap(a,b), with a and b genuinely distinct) also directly confirmed.

ADDRESSING VARIABLES: THE OFFSET ARITHMETIC BEHIND EVERY ACCESS

1. EVERY local variable within an activation record is assigned a FIXED OFFSET, computed at COMPILE time, relative to the frame pointer (FP) — accessing that variable at RUNTIME then requires nothing more than a SINGLE address computation, FP + offset, with NO search or lookup needed at all, since the offset itself was already determined once and for all during compilation.
2. FOR a NON-LOCAL variable (declared in a LEXICALLY enclosing procedure, accessed from within a nested one), the ADDRESS computation instead FIRST follows the static-link chain the appropriate NUMBER of levels (exactly as this chapter's own earlier section on static scoping developed), arriving at the CORRECT ancestor activation record, and THEN applies that variable's own fixed offset WITHIN that specific ancestor record — TWO separate pieces of information (the LEXICAL-LEVEL difference, and the OFFSET within the target level's own record) are needed together to fully specify a non-local variable's access, a pair of values conventionally written (level, offset).
3. THIS combination — traverse the STATIC-link chain a fixed NUMBER of times, then apply a FIXED offset — is EXACTLY analogous to this book's own earlier Intermediate Code Generation chapter's own array-indexing formula (base address plus index times element width): in BOTH cases, a SINGLE runtime memory access is correctly computed from TWO separately-tracked pieces of compile-time-known information, combined via straightforward ADDRESS ARITHMETIC, rather than requiring any runtime SEARCH or lookup procedure at all.

1. Worked example (non-local variable access via level/offset, traced through concretely): a variable z is declared in procedure P, and accessed from within procedure Q, nested TWO lexical levels DEEPER than P (so Q's own static link, followed TWICE, reaches P's activation record). z itself sits at offset 12 within P's own record. Accessing z from within Q requires: (a) follow Q's OWN static link ONCE, reaching the intermediate enclosing procedure's record; (b) follow THAT record's own static link a SECOND time, finally reaching P's record; (c) compute the address as (P's own frame pointer) + 12. This two-step link traversal, followed by one final fixed-offset addition, is the COMPLETE runtime cost of a non-local access under static scoping — genuinely more expensive than a purely LOCAL variable's own single FP+offset computation, but STILL entirely determined at COMPILE time (the number of link-hops, and the final offset, are BOTH known in advance, with no runtime search involved).

GARBAGE COLLECTION: RECLAIMING HEAP STORAGE AUTOMATICALLY

1. FOR languages using HEAP allocation with AUTOMATIC reclamation (rather than explicit alloc/free calls), a GARBAGE COLLECTOR periodically identifies heap objects that are NO LONGER reachable from any live program variable, and reclaims their storage for REUSE — this section briefly surveys the two classical, most commonly examined collection STRATEGIES.
2. REFERENCE COUNTING: EACH heap object carries an explicit COUNT of how many OTHER references currently point TO it; incrementing this count on EVERY new reference created, and decrementing it whenever a reference is DESTROYED or REASSIGNED — the MOMENT an object's own count reaches ZERO, it is IMMEDIATELY reclaimed. The genuine ADVANTAGE: reclamation happens PROMPTLY, the instant an object becomes unreachable, with NO need to periodically pause the entire program to scan memory. The genuine, WELL-KNOWN LIMITATION: reference counting CANNOT reclaim a CYCLE of mutually-referencing objects (object A referencing B, and B referencing BACK to A, with NEITHER reachable from anywhere else) — since EACH object in such a cycle still shows a NONZERO count from the OTHER object in the cycle, even though the ENTIRE cycle is, taken together, genuinely unreachable from the rest of the live program.
3. MARK-AND-SWEEP: periodically, the ENTIRE collector PAUSES ordinary program execution, MARKS every heap object REACHABLE from some designated set of ROOT references (global variables, and every variable on every currently-active activation record), then SWEEPS through the ENTIRE heap, reclaiming every object that was NOT marked as reachable. The genuine advantage: mark-and-sweep CORRECTLY handles reference CYCLES (a cycle entirely unreachable from the roots is correctly identified as garbage and reclaimed, regardless of its own internal reference counts). The genuine LIMITATION: it requires periodically PAUSING the entire running program (a "stop-the-world" pause), and its own cost scales with the TOTAL size of the heap being scanned, rather than scaling with only the amount of ACTUALLY reclaimed garbage.

GATE TRAP: assuming reference counting can correctly reclaim EVERY unreachable heap object, with no exceptions, overlooks its own single, well-known, and heavily-tested LIMITATION — a CYCLE of mutually-referencing, otherwise entirely unreachable objects is NEVER reclaimed by pure reference counting alone, since each object in the cycle continues to show a nonzero count from ITS OWN partner within that same cycle; correctly handling such cycles requires EITHER a separate, periodic cycle-detection pass layered on top of reference counting, OR abandoning reference counting entirely in favor of mark-and-sweep (or one of its many more sophisticated modern descendants), which handles cycles correctly by construction.

DANGLING REFERENCES: A GENUINE RUNTIME-ENVIRONMENT HAZARD

1. A DANGLING REFERENCE occurs when a pointer or reference CONTINUES to refer to a memory location whose OWN storage has ALREADY been reclaimed or reused for something else ENTIRELY — a classic, concrete example: a procedure returns the ADDRESS of one of its OWN local variables (stored in its own, now-popped activation record) to its caller, and the caller then attempts to USE that returned address AFTER the procedure's own activation record has already been popped from the stack and its storage potentially REUSED by some entirely different, later call.
2. THIS specific hazard connects DIRECTLY back to this chapter's own opening storage-class framework: it is PRECISELY the reason a language's own runtime design must be CAREFUL about which storage class is used for which purpose — a value that must OUTLIVE the specific procedure call that created it CANNOT safely live in that call's own STACK-allocated activation record (which is reclaimed the instant that call returns), and instead genuinely REQUIRES either static allocation (if a single, program-lifetime instance suffices) or heap allocation (if a NEW, independently-lived instance is needed on EACH call).

GATE TRAP: overlooking that RETURNING the address of a STACK-allocated local variable creates a genuine dangling reference the MOMENT the containing procedure returns is a common oversight in questions asking to identify or explain unsafe runtime behavior — the underlying activation record's own storage becomes AVAILABLE FOR REUSE the instant the procedure returns, and a LATER call (to the SAME or even a DIFFERENT procedure) may reuse that EXACT SAME stack memory for its OWN, entirely unrelated locals, meaning the dangling reference may appear to "still work" by pure coincidence in a quick test, while remaining a genuine, latent correctness bug that can fail unpredictably depending on what happens to occupy that same memory region afterward.

WORKED PROBLEMS

1. STORAGE-CLASS IDENTIFICATION. A variable is declared "static int counter;" inside a function, meant to retain its value ACROSS separate calls to that function. Which storage class fits this requirement, and why NOT stack allocation? STATIC allocation fits — the variable needs a SINGLE, PERSISTENT storage location that survives BETWEEN separate calls, exactly what static allocation provides; STACK allocation would NOT work, since each new activation would receive its OWN fresh, uninitialized storage, losing the value from any PREVIOUS call the moment that previous call's own activation record was popped.

2. RECURSION AND STORAGE CLASS. Explain, in one connected argument, why a recursive procedure's LOCAL variables cannot use static allocation. A recursive call creates a NEW, logically separate instance of the SAME procedure while an OUTER call is still active — if locals used static allocation, ALL simultaneously-active instances would SHARE the exact SAME single storage location, so the INNER, more deeply recursive call's own writes would OVERWRITE the OUTER call's still-needed values, corrupting the outer call's own computation the moment it eventually resumes; STACK allocation avoids this entirely by giving EACH active call its own SEPARATE activation record.

3. STATIC VS DYNAMIC SCOPE, A SECOND TRACE. Using this chapter's own worked example's exact program structure, but with x declared as 5 (globally), b's own local x declared as 10, trace the output of a() under both scoping rules. STATIC scoping: OUTPUT 5 (a() is lexically nested at the top level, resolving to the global x). DYNAMIC scoping: OUTPUT 10 (b's own local x=10 is the most recently activated declaration at the moment a() executes, since b called a).

4. STATIC LINK TRAVERSAL COUNT. A variable is declared 3 lexical levels above the currently executing procedure. How many times must the static link chain be followed to locate it? EXACTLY 3 times — the static link chain directly encodes lexical nesting depth, so reaching a variable declared n levels up requires following the static link exactly n times from the current activation record.

5. STATIC VS DYNAMIC LINK DIVERGENCE. Give a concrete scenario where a procedure's static link and dynamic link point to two DIFFERENT activation records. A procedure P, lexically nested inside procedure Q, is called NOT directly by Q but by some OTHER, unrelated procedure R (which merely has access to call P, perhaps via a passed procedure parameter) — P's own STATIC link points to Q's activation record (P's lexically enclosing procedure), while P's own DYNAMIC link points to R's activation record (P's actual, runtime caller) — these are two genuinely DIFFERENT records whenever the calling sequence does not mirror the lexical nesting structure.

6. PARAMETER-PASSING MECHANISM IDENTIFICATION FROM BEHAVIOR. A procedure's formal parameter change is observed to modify the caller's own actual-parameter variable IMMEDIATELY, during the callee's own execution, rather than only at return. Which parameter-passing mechanism is this consistent with, and which is it INCONSISTENT with? This is consistent with CALL BY REFERENCE (writes are visible immediately, since both refer to the same address) and INCONSISTENT with call by copy-restore (which would only update the caller's variable at the RETURN point, not during execution).

7. JENSEN'S DEVICE STYLE TRAP. A procedure sum(x) computes a running total by referencing x repeatedly in a loop, and is called as sum(a[i]) where i is itself a loop-controlled index variable that CHANGES during the procedure's own execution. Under CALL BY NAME, does x refer to a FIXED array element, or does it re-resolve differently on each use? Under call by name, x RE-EVALUATES the actual-parameter expression "a[i]" at EVERY use inside sum's own body — since i itself CHANGES during execution, EACH use of x may refer to a genuinely DIFFERENT array element, a classic Jensen's-device-style behavior that differs sharply from what call-by-reference (which would bind ONCE, to whichever specific address a[i] denoted AT THE MOMENT of the call) would produce instead.

8. VARIABLE-LENGTH ARRAY ACTIVATION-RECORD HANDLING. A procedure declares a local array whose SIZE depends on a runtime parameter (not known until the procedure is actually called). How is this typically handled within a FIXED-layout activation record? The array's OWN data is allocated SEPARATELY (often on the heap, or in an extension area beyond the fixed-size portion of the record), while the activation record ITSELF stores only a POINTER to that separately-allocated array, at a FIXED, KNOWN offset within the record — this way, the record's own fixed portion retains a CONSTANT, predictable size and fixed-offset addressing, even though the array's OWN actual size can vary freely from one call to the next.

9. LEVEL/OFFSET ADDRESS COMPUTATION. Using this chapter's own level/offset scheme, a variable w is declared in procedure M, and accessed from within procedure N, nested exactly ONE lexical level deeper than M, with w sitting at offset 8 within M's own record. Describe the exact runtime address computation. Follow N's own static link EXACTLY ONCE, reaching M's activation record directly; compute the address as (M's own frame pointer) + 8 — a single link-hop followed by one fixed-offset addition, cheaper than the two-hop example worked through above specifically because N is only ONE lexical level below M rather than two.

10. REFERENCE COUNTING CYCLE FAILURE TRACE. Two heap objects, X and Y, are created such that X holds a reference to Y, and Y holds a reference BACK to X, with NO other live reference to EITHER object anywhere else in the program. Trace what happens to each object's own reference count, and state whether reference counting alone reclaims them. X's own count = 1 (from Y's reference to it). Y's own count = 1 (from X's reference to it). Since NEITHER count ever reaches zero (each object's count is sustained entirely by the OTHER object within this same cycle), reference counting alone NEVER reclaims either object, even though the entire pair is genuinely unreachable from any live program root — exactly the classic cycle-failure case this chapter's own GATE TRAP above described directly.

11. MARK-AND-SWEEP ON THE SAME CYCLE. Using the SAME X/Y cycle from WP10, would mark-and-sweep collection correctly reclaim both objects? Yes — mark-and-sweep starts from the program's own ROOTS (global variables and active-activation-record variables), and since NEITHER X nor Y is reachable from any such root (the only references to them are from EACH OTHER, entirely internal to the unreachable cycle), NEITHER gets marked during the mark phase, and BOTH are correctly reclaimed during the sweep phase — directly illustrating mark-and-sweep's own genuine advantage over pure reference counting for precisely this cycle-containing scenario.

12. DANGLING REFERENCE IDENTIFICATION. A procedure "int* getLocal() { int x = 5; return &x; }" returns the address of its OWN local variable x. Explain precisely why the returned pointer is unsafe to use after the call returns. x is allocated within getLocal's OWN stack-based activation record; the MOMENT getLocal returns, that ENTIRE activation record is popped and its storage becomes available for reuse by any SUBSEQUENT call (to getLocal itself, or to any other procedure) — the returned pointer therefore points to memory that may be OVERWRITTEN by completely unrelated data the instant any later call occurs, making any subsequent use of that returned pointer a genuine, classic dangling-reference bug.

CALLER-SAVE VS CALLEE-SAVE REGISTER CONVENTIONS

1. WHEN a procedure call occurs, SOME register values the CALLER was actively using may need to be PRESERVED across that call, since the CALLEE itself may freely use those SAME physical registers for its own, entirely separate purposes — two DISTINCT conventions exist for deciding WHO is responsible for this preservation.
2. CALLER-SAVE: the CALLER itself is responsible for SAVING any register values it still needs, BEFORE making the call (typically by pushing them onto the stack), and RESTORING them AFTER the call returns — this convention is EFFICIENT specifically when the caller happens to need FEW of its own registers preserved across a GIVEN call, since it saves ONLY exactly what that specific caller actually needs, rather than everything the callee might potentially touch.
3. CALLEE-SAVE: the CALLEE itself is responsible for SAVING any registers it intends to USE internally, at its OWN prologue, and RESTORING them in its OWN epilogue before returning — this convention is efficient specifically when a GIVEN callee uses FEW registers overall, since it saves only what that SPECIFIC callee actually touches, regardless of how many DIFFERENT call sites might invoke it.
4. REAL, PRACTICAL calling conventions (as used by actual compilers targeting actual hardware architectures) typically use a MIX of both: SOME registers are designated caller-save (used freely by callees, with the caller responsible for preserving anything it needs across a call), while OTHER registers are designated callee-save (any callee using them MUST preserve their original values), striking a practical BALANCE that avoids EITHER extreme (caller-save for everything wastes effort when a callee barely touches any registers at all; callee-save for everything wastes effort when a caller barely has anything WORTH preserving across a specific call).

GATE TRAP: assuming ONE single convention (either purely caller-save, or purely callee-save) is UNIVERSALLY superior in every situation is an oversimplification — WHICH convention is more EFFICIENT for a GIVEN register genuinely depends on the SPECIFIC pattern of usage across BOTH the caller's own needs and the callee's own register usage, which is PRECISELY why real architectures adopt a MIXED convention (designating SOME registers caller-save and OTHERS callee-save) rather than committing to either extreme uniformly across an entire register file.

A FULLY WORKED SYNTHESIS: TRACING A RECURSIVE CALL'S COMPLETE ACTIVATION-RECORD LIFECYCLE

To bring together storage classes, activation-record layout, and the static/dynamic link distinction into one connected scenario, consider tracing the COMPLETE runtime behavior of a small recursive procedure through several successive calls.

1. THE PROCEDURE: factorial(n) { if (n <= 1) return 1; else return n * factorial(n-1); }, called initially as factorial(3).
2. STEP 1 — INITIAL CALL, factorial(3): a NEW activation record is pushed, containing n=3, a return address pointing back to the original caller, a control link pointing to the ORIGINAL caller's own activation record (this procedure has no lexical nesting, so no static link is needed for THIS specific example), and space for factorial's own local computation.
3. STEP 2 — RECURSIVE CALL, factorial(2): SINCE n=3 is not ≤1, a SECOND, entirely SEPARATE activation record is pushed ON TOP of the first, containing n=2, with its OWN control link pointing back to the FIRST activation record (factorial(3)'s own record) — CRITICALLY, this is a genuinely SEPARATE, independent storage location for n, entirely distinct from the FIRST call's own n=3, exactly the property STACK allocation (rather than static allocation) makes possible for recursive calls, as this chapter's own opening section established directly.
4. STEP 3 — FURTHER RECURSION, factorial(1): a THIRD activation record is pushed, containing n=1, with its own control link pointing back to the SECOND record (factorial(2)'s own record). Since n=1 ≤ 1 here, this call returns IMMEDIATELY with the value 1, WITHOUT any further recursive call.
5. STEP 4 — UNWINDING, in REVERSE order: factorial(1)'s own activation record is POPPED, its return value (1) is used by factorial(2)'s own remaining computation (n * factorial(n-1) = 2 * 1 = 2), and factorial(2)'s OWN record is then popped in turn, its return value (2) feeding into factorial(3)'s own remaining computation (3 * 2 = 6), which is FINALLY returned to the original caller once factorial(3)'s own record is ALSO popped.
6. FINAL RESULT: at the DEEPEST point of this recursion, THREE separate activation records exist SIMULTANEOUSLY on the runtime stack (for n=3, n=2, and n=1 respectively) — directly demonstrating why stack allocation is not merely a convenient IMPLEMENTATION choice but a genuinely NECESSARY one for correct recursive execution, exactly as this chapter's own opening storage-class discussion, and its own dedicated worked problem on recursion and storage class, both established from the very start of this chapter.

This synthesis demonstrates concretely how EVERY piece of this chapter's own machinery — the storage-class distinction motivating STACK allocation specifically for recursive locals, the activation-record layout providing each call's own separate, self-contained storage, and the control-link chain correctly threading together the sequence of NESTED, simultaneously-active calls — cooperates as ONE single, coherent runtime mechanism, rather than as several unrelated, independently-memorised facts about compiler implementation.

KEY: the recursive factorial trace above illustrates a genuinely important POINT worth stating explicitly as its own standalone observation — the MAXIMUM DEPTH of recursion a program can safely reach is DIRECTLY bounded by the AVAILABLE stack SPACE, since EACH recursive call consumes ONE additional activation record's worth of stack memory; a recursion that goes TOO deep (whether from a genuine algorithmic requirement, or from a MISSING or INCORRECT base-case check) EXHAUSTS the available stack space entirely, producing the well-known "STACK OVERFLOW" runtime error — directly connecting this chapter's own activation-record machinery to a genuinely common, practically important runtime failure mode every programmer eventually encounters directly.

FROM RUNTIME ENVIRONMENTS BACK TO SOURCE-LANGUAGE DESIGN

1. IT IS worth stating explicitly, before this chapter closes, how the runtime machinery developed here connects DIRECTLY back to source-LANGUAGE design DECISIONS a language designer makes, rather than being a purely IMPLEMENTATION-side afterthought bolted on separately after a language's own semantics are already fixed.
2. A language permitting NESTED procedure declarations with non-local access (Pascal-style) FORCES its own compiler to implement the STATIC-link machinery this chapter developed in full; a language WITHOUT nested procedures (C-style) simply has NO NEED for it, and every compiler for such a language OMITS this specific piece of machinery entirely, exactly as this chapter's own earlier section on activation-record layout already noted directly.
3. A language choosing DYNAMIC scoping, rather than the FAR more common static scoping, must implement an ENTIRELY different non-local-name-resolution mechanism (searching the DYNAMIC/control-link chain, or maintaining a separate association list, rather than the static-link chain) — a genuinely FUNDAMENTAL runtime-architecture difference DIRECTLY traceable to a single, specific SOURCE-language design choice made LONG before any actual program is ever compiled or run.
4. A language's own CHOICE of parameter-passing mechanism (value, reference, copy-restore, or name) likewise DIRECTLY determines what CALLING-SEQUENCE code a compiler for that language must generate at EVERY single call site — call-by-value requires generating code to COPY argument values; call-by-reference requires generating code to compute and pass ADDRESSES instead; call-by-copy-restore requires GENERATING additional write-back code specifically at the RETURN point; and call-by-name, MOST demandingly of all, requires generating an entire, separate CALLABLE "thunk" (a small procedure computing the actual-parameter's address or value FRESH on each individual use) rather than a SINGLE, simple value or address passed just once at the call itself.

KEY: recognising that EVERY piece of runtime-environment machinery this chapter has developed — activation-record layout, the static-versus-dynamic scoping choice, and the specific parameter-passing mechanism selected — is ULTIMATELY a DIRECT CONSEQUENCE of some SPECIFIC, identifiable source-language design decision, rather than an arbitrary IMPLEMENTATION detail a compiler writer happens to choose independently, is the single most valuable connecting insight this chapter offers: a compiler's own runtime environment is not designed in isolation, but is instead the DIRECT, NECESSARY response to whatever semantic GUARANTEES the source language itself has already promised to its own programmers, exactly mirroring this book's own recurring theme (first developed explicitly in the Theory of Computation sequence) that a system's own underlying MACHINERY is very often best understood as a DIRECT, provable CONSEQUENCE of some more fundamental, already-stated REQUIREMENT, rather than as an independent, freestanding design choice made purely on its own separate terms.

CARRYING THIS FORWARD

This chapter has developed the CONCRETE machinery — storage classes, activation-record layout, static-versus-dynamic scope resolution, and the four parameter-passing mechanisms — that makes it possible to actually EXECUTE the three-address code the previous chapter's own translation templates generate, correctly and efficiently, on real hardware. The single most valuable connecting insight to carry forward is that STATIC scoping's own access-link chain and DYNAMIC scoping's own control-link chain are BOTH, at their structural core, simply DIFFERENT ways of TRAVERSING the SAME underlying activation-record data structure this chapter developed at its own very start — the CHOICE between them is entirely a LANGUAGE-DESIGN decision about WHAT "enclosing" is even supposed to MEAN (lexical text structure, or live call-sequence structure), rather than a difference in the underlying RUNTIME machinery itself, which remains the SAME activation-record stack either way.

KEY: the parameter-passing mechanisms' own aliasing sensitivity — call by reference and call by copy-restore agreeing exactly EXCEPT when genuine aliasing exists — is worth recognising as a SPECIFIC instance of a much MORE general principle this book has now encountered repeatedly across several, otherwise quite different subjects: TWO mechanisms that are "usually equivalent" can still DIVERGE sharply in some SPECIFIC, identifiable EDGE case (here, aliasing; the SLR-versus-LALR distinction in this book's own Syntax Analysis chapter's own reduce/reduce-only merging direction being a directly structurally analogous earlier example) — and the DISCIPLINED habit of actively SEEKING OUT that specific edge case, rather than assuming general "usual" equivalence holds UNCONDITIONALLY, is precisely what a careful compiler-construction (and broader computer-science) education is built to instill as a genuinely transferable analytical reflex.

The FINAL chapter in this Compiler Design sequence, on CODE OPTIMIZATION, returns DIRECTLY to the three-address code this chapter's own runtime-environment machinery now knows how to correctly EXECUTE — applying systematic transformations to make that SAME code run FASTER, using EXACTLY the activation-record and calling-convention structure this chapter has developed as the concrete, physical FOUNDATION every one of those optimizations must ultimately respect and correctly preserve.

KEY: it is worth stating one final, practically important observation connecting this chapter's own garbage-collection material back to the code-optimization chapter immediately following it — an optimizer must be genuinely CAREFUL never to eliminate a computation that a garbage collector's own reachability analysis still depends upon, even if that computation's own RESULT appears, from a purely local, syntactic standpoint, to go entirely UNUSED afterward; a reference stored purely to KEEP some heap object reachable (preventing its premature reclamation) can look, to a naive dead-code-elimination pass, exactly like a genuinely useless, discardable computation, when it is in fact serving a genuinely load-bearing PURPOSE the optimizer's own analysis must be sophisticated enough to recognise and correctly preserve. This is a genuinely real, practically important interaction between runtime-environment design (this chapter's own subject) and code optimization (the very next chapter's own subject), and it is precisely the kind of subtle, cross-chapter interaction a reader who has studied both chapters carefully, rather than treating each as an entirely self-contained, independent topic, is far better equipped to reason about correctly.

A closing summary of this chapter's own five major pieces, worth holding in mind as a connected whole rather than as separately memorised facts: the THREE storage classes (static, stack, heap), each matched to a specific variable LIFETIME requirement; the activation-record LAYOUT, providing each procedure call its own self-contained storage; the static-versus-dynamic SCOPING choice, determining how non-local names are resolved via either the access-link or the control-link chain; the FOUR parameter-passing mechanisms, each with its own specific aliasing behavior; and, more briefly, garbage collection's two classical strategies and the dangling-reference hazard that motivates careful storage-class selection in the first place. Every worked example and worked problem in this entire chapter, from the very first storage-class identification exercise through to the closing recursive-factorial synthesis, is an application of one or more of these five pieces working together, and a reader who has genuinely internalized all five as one connected system, rather than as isolated definitions to recall separately, is fully equipped to reason correctly about a runtime-environment question covering a source-language feature this chapter has never explicitly named.

One final connecting thought, appropriate for closing out this particular chapter specifically: everything this chapter has developed — activation records, calling conventions, scoping mechanisms, parameter-passing semantics — represents the exact boundary where a compiler's own abstract, machine-independent reasoning (the subject of every preceding chapter in this sequence) finally meets the concrete, physical reality of an actual running program occupying actual memory on an actual machine. This chapter is, in a very real sense, where the increasingly abstract theoretical machinery this entire Compiler Design sequence has built up, chapter after chapter, finally comes back down to earth, translated into the literal bytes and addresses that make a compiled program actually execute correctly when a user finally runs it.

KEY: it is worth closing this chapter with one final, unifying observation about WHY runtime environments occupy exactly the position they do within this book's own overall Compiler Design sequence, positioned deliberately AFTER intermediate code generation but BEFORE the final code-optimization chapter. Intermediate code generation produces three-address code that REFERENCES names symbolically, without yet committing to any specific memory address at all — this chapter's own machinery is precisely what RESOLVES those symbolic references into concrete, physical addresses (via activation-record offsets, static-link traversal, and the parameter-passing mechanism a given call site actually uses), while the FINAL code-optimization chapter then improves the RESULTING, now fully address-resolved code before it is handed off to an actual target-machine code generator. Placing this chapter exactly here, at this specific point in the sequence, is itself a direct, deliberate reflection of the classical compiler pipeline's own natural ordering: a compiler cannot meaningfully OPTIMIZE memory-access patterns (a major concern of the next chapter) until it already KNOWS, concretely, what those memory accesses actually resolve to — precisely the knowledge this chapter's own activation-record and scoping machinery exists specifically to supply.

A final practical note on how this chapter's material tends to appear in examination settings, worth stating explicitly for a reader using this book specifically to prepare for GATE. Questions on runtime environments fall into a small number of recurring, easily recognised formats: identifying the correct storage class for a described variable's lifetime requirement; tracing a short program's output under static versus dynamic scoping (this chapter's own worked example being the canonical template for this exact question style); tracing the final values of variables passed under one or more of the four parameter-passing mechanisms, especially when aliasing is deliberately introduced to create a counter-intuitive result; and, somewhat less frequently, reasoning about activation-record layout details (which party sets up which field, or how many bytes a given record occupies given its declared contents). Recognising which of these several recurring formats a specific unfamiliar question belongs to, and then reaching directly for the corresponding tool this chapter has already developed and thoroughly exercised, is, as with every other chapter in this book, considerably more valuable under real time pressure than attempting to recall this chapter's own specific worked numbers by rote memorisation alone.

It is worth adding one further, concrete illustration of how deeply activation-record structure and language semantics intertwine, drawn from a slightly more advanced scenario than the plain recursive factorial trace worked through above. Consider a language permitting a procedure to accept ANOTHER procedure as a parameter, and to later invoke that passed-in procedure from within its own body — a design feature requiring genuinely careful handling of the static link specifically, since the passed-in procedure's own correct non-local access still needs to resolve according to WHERE that procedure was originally, lexically DEFINED, not according to wherever it happens to be INVOKED from after being passed around as a value. The standard solution bundles a procedure value together with the static link that was current at the moment it was first passed, forming what is commonly called a CLOSURE — a pairing of code together with the specific lexical environment that code's own non-local references must resolve against, regardless of how many times that procedure value is subsequently passed onward through further procedure calls before it is finally invoked. This closure mechanism is, at its structural core, nothing more than an explicit, first-class packaging of exactly the static-link concept this chapter has already developed in full, now made portable enough to travel alongside a procedure value itself rather than being implicitly recomputed fresh at every single call site. Recognising closures as a direct, natural extension of the static-link machinery already covered here, rather than as an entirely separate and unrelated language feature requiring its own independent runtime mechanism, is exactly the kind of connective habit this chapter, and this book as a whole, has consistently aimed to build.

This closure discussion, brief as it is, closes out this chapter on precisely the right note: every seemingly advanced or exotic language feature a compiler must eventually support, however unfamiliar it first appears, tends to resolve, on sufficiently careful inspection, into some combination of the same small handful of foundational mechanisms this chapter has developed from first principles — a storage class matched to a lifetime, an activation record providing self-contained per-call storage, and a link chain resolving names according to whichever scoping discipline the source language has chosen to adopt. Mastering these foundational pieces thoroughly, rather than attempting to separately memorise every individual advanced feature a real programming language might someday present, is what genuinely prepares a reader to reason correctly about runtime behavior far beyond anything this one chapter has had the space to name explicitly by title, and it is exactly this same disciplined foundation the final chapter of this Compiler Design sequence now builds directly upon, one careful, well-verified step further still, continuing the exact same connective pattern this entire book has followed, deliberately and consistently, from its very first page onward, all the way through to this exact point, and continuing on directly and confidently from this exact point into whatever specific material comes next in this book's own ongoing, carefully and deliberately connected sequence of chapters, exactly as every earlier chapter in this same book has already so reliably and so consistently done before it, time and time again.
`
};
